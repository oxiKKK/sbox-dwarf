
//
// ./rapidjson/include/rapidjson/encodings.h
//
//	referenced by: librendersystemvulkan.so
//
//	functions: 4
//

// <019DCDEB> ./rapidjson/include/rapidjson/encodings.h:102
// function calls: 81
void Encode<rapidjson::GenericReader<rapidjson::UTF8<>, rapidjson::UTF8<> >::StackStream<char> >(StackStream<char>& os, unsigned int codepoint)
{
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 104
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 106
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 112
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 111
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 116
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 117
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 118
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 119
	Stack<rapidjson::CrtAllocator>::GetCapacity(); // 191
	Stack<rapidjson::CrtAllocator>::GetSize(); // 194
	CrtAllocator::Realloc(
		void* originalPtr,
		size_t originalSize,
		size_t newSize);  // 203
	Stack<rapidjson::CrtAllocator>::Resize(
		size_t newCapacity);  // 198
	Stack<rapidjson::CrtAllocator>::Expand<char>(
			size_t count);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 110
} /* size: 0, inline expansions: 81 (0 bytes) */

// <0185F8DE> ./rapidjson/include/rapidjson/encodings.h:102
// function calls: 40
void Encode<rapidjson::GenericReader<rapidjson::UTF8<>, rapidjson::UTF8<> >::StackStream<char> >(StackStream<char>& os, unsigned int codepoint)
{
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 104
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 119
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 106
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 107
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 110
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 111
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 112
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 116
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 117
	Stack<rapidjson::CrtAllocator>::Reserve<char>(
			size_t count);  // 124
	Stack<rapidjson::CrtAllocator>::PushUnsafe<char>(
			size_t count);  // 125
	Stack<rapidjson::CrtAllocator>::Push<char>(
			size_t count);  // 934
	StackStream<char>::Put(
		Ch c);  // 118
} /* size: 1037, inline expansions: 40 (2496 bytes) */

// <019DCDB6> ./rapidjson/include/rapidjson/encodings.h:693
inline void Transcode<rapidjson::EncodedInputStream<rapidjson::UTF8<>, rapidjson::MemoryStream>, rapidjson::GenericReader<rapidjson::UTF8<>, rapidjson::UTF8<> >::StackStream<char> >(EncodedInputStream<rapidjson::UTF8<char>, rapidjson::MemoryStream>& is, StackStream<char>& os)
{
} /* size: 0 */

// <018798C9> ./rapidjson/include/rapidjson/encodings.h:699
inline void TranscodeUnsafe<rapidjson::GenericStringStream<rapidjson::UTF8<> >, rapidjson::GenericStringBuffer<rapidjson::UTF8<> > >(GenericStringStream<rapidjson::UTF8<char> >& is, GenericStringBuffer<rapidjson::UTF8<char>, rapidjson::CrtAllocator>& os)
{
} /* size: 0 */


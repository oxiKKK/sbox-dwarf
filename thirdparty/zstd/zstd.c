
//
// thirdparty/zstd/zstd.c
//
//	referenced by: libengine2.so
//				   libmaterialsystem2.so
//				   librendersystemvulkan.so
//				   libvfx_vulkan.so
//
//	functions: 1,243
//	structs: 117
//

// <07016FA1> thirdparty/zstd/zstd.c:890
inline const unsigned char* ZSTD_wrappedPtrAdd(const unsigned char* ptr, ptrdiff_t add)
{
} /* size: 0 */

// <07016F74> thirdparty/zstd/zstd.c:903
inline const unsigned char* ZSTD_wrappedPtrSub(const unsigned char* ptr, ptrdiff_t sub)
{
} /* size: 0 */

// <07016F47> thirdparty/zstd/zstd.c:915
inline unsigned char* ZSTD_maybeNullPtrAdd(unsigned char* ptr, ptrdiff_t add)
{
} /* size: 0 */

// <07016F38> thirdparty/zstd/zstd.c:1114
inline unsigned int MEM_32bits(void)
{
} /* size: 0 */

// <07016F29> thirdparty/zstd/zstd.c:1115
inline unsigned int MEM_64bits(void)
{
} /* size: 0 */

// <07016F1A> thirdparty/zstd/zstd.c:1117
inline unsigned int MEM_isLittleEndian(void)
{
} /* size: 0 */

// <07016EFA> thirdparty/zstd/zstd.c:1159
inline U16 MEM_read16(const void* ptr)
{
} /* size: 0 */

// <07016EDA> thirdparty/zstd/zstd.c:1160
inline U32 MEM_read32(const void* ptr)
{
} /* size: 0 */

// <07016EBA> thirdparty/zstd/zstd.c:1161
inline U64 MEM_read64(const void* ptr)
{
} /* size: 0 */

// <07016E9A> thirdparty/zstd/zstd.c:1162
inline size_t MEM_readST(const void* ptr)
{
} /* size: 0 */

// <07016E71> thirdparty/zstd/zstd.c:1164
inline void MEM_write16(void* memPtr, U16 value)
{
} /* size: 0 */

// <07016E48> thirdparty/zstd/zstd.c:1165
inline void MEM_write32(void* memPtr, U32 value)
{
} /* size: 0 */

// <07016E1F> thirdparty/zstd/zstd.c:1166
inline void MEM_write64(void* memPtr, U64 value)
{
} /* size: 0 */

// <07016E00> thirdparty/zstd/zstd.c:1218
inline U32 MEM_swap32(U32 in)
{
} /* size: 0 */

// <07016DE1> thirdparty/zstd/zstd.c:1244
inline U64 MEM_swap64(U64 in)
{
} /* size: 0 */

// <07016DB4> thirdparty/zstd/zstd.c:1266
// variable: 1
inline U16 MEM_readLE16(const void* memPtr)
{
	{
		const BYTE* p; // 1271
	}
} /* size: 0 */

// <07016D7E> thirdparty/zstd/zstd.c:1276
// variable: 1
inline void MEM_writeLE16(void* memPtr, U16 val)
{
	{
		BYTE* p; // 1281
	}
} /* size: 0 */

// <07016D5E> thirdparty/zstd/zstd.c:1287
inline U32 MEM_readLE24(const void* memPtr)
{
} /* size: 0 */

// <07016D35> thirdparty/zstd/zstd.c:1292
inline void MEM_writeLE24(void* memPtr, U32 val)
{
} /* size: 0 */

// <07016D15> thirdparty/zstd/zstd.c:1298
inline U32 MEM_readLE32(const void* memPtr)
{
} /* size: 0 */

// <07016CEC> thirdparty/zstd/zstd.c:1306
inline void MEM_writeLE32(void* memPtr, U32 val32)
{
} /* size: 0 */

// <07016CCC> thirdparty/zstd/zstd.c:1314
inline U64 MEM_readLE64(const void* memPtr)
{
} /* size: 0 */

// <07016CA3> thirdparty/zstd/zstd.c:1322
inline void MEM_writeLE64(void* memPtr, U64 val64)
{
} /* size: 0 */

// <07016C83> thirdparty/zstd/zstd.c:1330
inline size_t MEM_readLEST(const void* memPtr)
{
} /* size: 0 */

// <07016C5A> thirdparty/zstd/zstd.c:1338
inline void MEM_writeLEST(void* memPtr, size_t val)
{
} /* size: 0 */

// <07016C3A> thirdparty/zstd/zstd.c:1561
unsigned int ERR_isError(size_t code)
{
} /* size: 0 */

// <07016C1A> thirdparty/zstd/zstd.c:1563
ERR_ERR_getErrorCode(size_t code)
{
} /* size: 0 */

// <07016BFA> thirdparty/zstd/zstd.c:1581
const char* ERR_getErrorName(size_t code)
{
} /* size: 0 */

// <07016BDA> thirdparty/zstd/zstd.c:1957
inline unsigned int ZSTD_countTrailingZeros32(U32 val)
{
} /* size: 0 */

// <07016BBA> thirdparty/zstd/zstd.c:1998
inline unsigned int ZSTD_countLeadingZeros32(U32 val)
{
} /* size: 0 */

// <07016B9A> thirdparty/zstd/zstd.c:2022
inline unsigned int ZSTD_countTrailingZeros64(U64 val)
{
} /* size: 0 */

// <07016B7A> thirdparty/zstd/zstd.c:2054
inline unsigned int ZSTD_countLeadingZeros64(U64 val)
{
} /* size: 0 */

// <07016B5A> thirdparty/zstd/zstd.c:2086
inline unsigned int ZSTD_NbCommonBytes(size_t val)
{
} /* size: 0 */

// <07016B3A> thirdparty/zstd/zstd.c:2103
inline unsigned int ZSTD_highbit32(U32 val)
{
} /* size: 0 */

// <07016B0D> thirdparty/zstd/zstd.c:2114
inline U64 ZSTD_rotateRight_U64(const U64 value, U32 count)
{
} /* size: 0 */

// <07016AE0> thirdparty/zstd/zstd.c:2121
inline U32 ZSTD_rotateRight_U32(const U32 value, U32 count)
{
} /* size: 0 */

// <07016AB3> thirdparty/zstd/zstd.c:2128
inline U16 ZSTD_rotateRight_U16(const U16 value, U32 count)
{
} /* size: 0 */

// <06F39D1B> thirdparty/zstd/zstd.c:2161
// member variables: 5
// struct size: 40
typedef struct {
	BitContainerType bitContainer; /* 0 8 */
	unsigned int bitPos; /* 8 4 */
	char * startPtr; /* 16 8 */
	char * ptr; /* 24 8 */
	char * endPtr; /* 32 8 */
} BIT_CStream_t;

// <06F39D87> thirdparty/zstd/zstd.c:2195
// member variables: 5
// struct size: 40
typedef struct {
	BitContainerType bitContainer; /* 0 8 */
	unsigned int bitsConsumed; /* 8 4 */
	const char * ptr; /* 16 8 */
	const char * start; /* 24 8 */
	const char * limitPtr; /* 32 8 */
} BIT_DStream_t;

// <07016A79> thirdparty/zstd/zstd.c:2255
inline size_t BIT_initCStream(BIT_CStream_t* bitC, void* startPtr, size_t dstCapacity)
{
} /* size: 0 */

// <07016A4C> thirdparty/zstd/zstd.c:2267
inline BitContainerType BIT_getLowerBits(BitContainerType bitContainer, const U32 nbBits)
{
} /* size: 0 */

// <07016A16> thirdparty/zstd/zstd.c:2285
inline void BIT_addBits(BIT_CStream_t* bitC, BitContainerType value, unsigned int nbBits)
{
} /* size: 0 */

// <070169E0> thirdparty/zstd/zstd.c:2298
inline void BIT_addBitsFast(BIT_CStream_t* bitC, BitContainerType value, unsigned int nbBits)
{
} /* size: 0 */

// <070169B7> thirdparty/zstd/zstd.c:2310
// variable: 1
inline void BIT_flushBitsFast(BIT_CStream_t* bitC)
{
	const size_t  nbBytes; // 2312
} /* size: 0, variables: 1 */

// <0701698E> thirdparty/zstd/zstd.c:2326
// variable: 1
inline void BIT_flushBits(BIT_CStream_t* bitC)
{
	const size_t  nbBytes; // 2328
} /* size: 0, variables: 1 */

// <0701696E> thirdparty/zstd/zstd.c:2341
inline size_t BIT_closeCStream(BIT_CStream_t* bitC)
{
} /* size: 0 */

// <07016912> thirdparty/zstd/zstd.c:2359
// variables: 2
inline size_t BIT_initDStream(BIT_DStream_t* bitD, const void* srcBuffer, size_t srcSize)
{
	{
		const BYTE  lastByte; // 2369
	}
	{
		const BYTE  lastByte; // 2397
	}
} /* size: 0 */

// <070168CB> thirdparty/zstd/zstd.c:2412
// variable: 1
inline BitContainerType BIT_getMiddleBits(BitContainerType bitContainer, const U32 start, const U32 nbBits)
{
	const U32  regMask; // 2414
} /* size: 0, variables: 1 */

// <0701689E> thirdparty/zstd/zstd.c:2435
inline BitContainerType BIT_lookBits(const BIT_DStream_t* bitD, U32 nbBits)
{
} /* size: 0 */

// <07016864> thirdparty/zstd/zstd.c:2451
// variable: 1
inline BitContainerType BIT_lookBitsFast(const BIT_DStream_t* bitD, U32 nbBits)
{
	const U32  regMask; // 2453
} /* size: 0, variables: 1 */

// <0701683B> thirdparty/zstd/zstd.c:2458
inline void BIT_skipBits(BIT_DStream_t* bitD, U32 nbBits)
{
} /* size: 0 */

// <07016801> thirdparty/zstd/zstd.c:2467
// variable: 1
inline BitContainerType BIT_readBits(BIT_DStream_t* bitD, unsigned int nbBits)
{
	const BitContainerType  value; // 2469
} /* size: 0, variables: 1 */

// <070167C7> thirdparty/zstd/zstd.c:2476
// variable: 1
inline BitContainerType BIT_readBitsFast(BIT_DStream_t* bitD, unsigned int nbBits)
{
	const BitContainerType  value; // 2478
} /* size: 0, variables: 1 */

// <070167A7> thirdparty/zstd/zstd.c:2489
inline BIT_DStream_status BIT_reloadDStream_internal(BIT_DStream_t* bitD)
{
} /* size: 0 */

// <07016787> thirdparty/zstd/zstd.c:2505
inline BIT_DStream_status BIT_reloadDStreamFast(BIT_DStream_t* bitD)
{
} /* size: 0 */

// <0701672E> thirdparty/zstd/zstd.c:2517
// variables: 3
inline BIT_DStream_status BIT_reloadDStream(BIT_DStream_t* bitD)
{
	{
		const BitContainerType  zeroFilled; // 2521
	}
	{
		U32 nbBytes; // 2538
		BIT_DStream_status result; // 2539
	}
} /* size: 0 */

// <07016708> thirdparty/zstd/zstd.c:2554
inline unsigned int BIT_endOfDStream(const BIT_DStream_t* DStream)
{
} /* size: 0 */

// <06F39E90> thirdparty/zstd/zstd.c:2622
// member variables: 4
// struct size: 32
typedef struct {
	ptrdiff_t value; /* 0 8 */
	const void * stateTable; /* 8 8 */
	const void * symbolTT; /* 16 8 */
	unsigned int stateLog; /* 24 4 */
} FSE_CState_t;

// <06F39EF1> thirdparty/zstd/zstd.c:2682
// member variables: 2
// struct size: 16
typedef struct {
	size_t state; /* 0 8 */
	const void * table; /* 8 8 */
} FSE_DState_t;

// <06F39F2D> thirdparty/zstd/zstd.c:2754
// member variables: 2
// struct size: 8
typedef struct {
	int deltaFindState; /* 0 4 */
	U32 deltaNbBits; /* 4 4 */
} FSE_symbolCompressionTransform;

// <070166B9> thirdparty/zstd/zstd.c:2759
// variables: 3
inline void FSE_initCState(FSE_CState_t* statePtr, const FSE_CTable* ct)
{
	const void* ptr; // 2761
	const U16* u16ptr; // 2762
	const U32  tableLog; // 2763
} /* size: 0, variables: 3 */

// <0701665B> thirdparty/zstd/zstd.c:2774
// variables: 3
inline void FSE_initCState2(FSE_CState_t* statePtr, const FSE_CTable* ct, U32 symbol)
{
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
} /* size: 0 */

// <070165F8> thirdparty/zstd/zstd.c:2785
// variables: 3
inline void FSE_encodeSymbol(BIT_CStream_t* bitC, FSE_CState_t* statePtr, unsigned int symbol)
{
	const FSE_symbolCompressionTransform  symbolTT; // 2787
	const U16* stateTable; // 2788
	const U32  nbBitsOut; // 2789
} /* size: 0, variables: 3 */

// <070165C3> thirdparty/zstd/zstd.c:2794
inline void FSE_flushCState(BIT_CStream_t* bitC, const FSE_CState_t* statePtr)
{
} /* size: 0 */

// <07016589> thirdparty/zstd/zstd.c:2806
// variable: 1
inline U32 FSE_getMaxNbBits(const void* symbolTTPtr, U32 symbolValue)
{
	const FSE_symbolCompressionTransform* symbolTT; // 2808
} /* size: 0, variables: 1 */

// <070164DF> thirdparty/zstd/zstd.c:2816
// variables: 7
inline U32 FSE_bitCost(const void* symbolTTPtr, U32 tableLog, U32 symbolValue, U32 accuracyLog)
{
	const FSE_symbolCompressionTransform* symbolTT; // 2818
	const U32  minNbBits; // 2819
	const U32  threshold; // 2820
	{
		const U32  tableSize; // 2823
		const U32  deltaFromThreshold; // 2824
		const U32  normalizedDeltaFromThreshold; // 2825
		const U32  bitMultiplier; // 2826
	}
} /* size: 0, variables: 3 */

// <06F39F6E> thirdparty/zstd/zstd.c:2836
// member variables: 2
// struct size: 4
typedef struct {
	U16 tableLog; /* 0 2 */
	U16 fastMode; /* 2 2 */
} FSE_DTableHeader;

// <06F39FAF> thirdparty/zstd/zstd.c:2842
// member variables: 3
// struct size: 4
typedef struct {
	short unsigned int newState; /* 0 2 */
	unsigned char symbol; /* 2 1 */
	unsigned char nbBits; /* 3 1 */
} FSE_decode_t;

// <07016490> thirdparty/zstd/zstd.c:2848
// variables: 2
inline void FSE_initDState(FSE_DState_t* DStatePtr, BIT_DStream_t* bitD, const FSE_DTable* dt)
{
	const void* ptr; // 2850
	const FSE_DTableHeader* DTableH; // 2851
} /* size: 0, variables: 2 */

// <0701642F> thirdparty/zstd/zstd.c:2871
// variables: 4
inline BYTE FSE_decodeSymbol(FSE_DState_t* DStatePtr, BIT_DStream_t* bitD)
{
	const FSE_decode_t  DInfo; // 2873
	const U32  nbBits; // 2874
	const BYTE  symbol; // 2875
	const size_t  lowBits; // 2876
} /* size: 0, variables: 4 */

// <070163C8> thirdparty/zstd/zstd.c:2884
// variables: 4
inline BYTE FSE_decodeSymbolFast(FSE_DState_t* DStatePtr, BIT_DStream_t* bitD)
{
	const FSE_decode_t  DInfo; // 2886
	const U32  nbBits; // 2887
	const BYTE  symbol; // 2888
	const size_t  lowBits; // 2889
} /* size: 0, variables: 4 */

// <06F3A09C> thirdparty/zstd/zstd.c:3158
// member variables: 3
// struct size: 8
typedef struct {
	BYTE tableLog; /* 0 1 */
	BYTE maxSymbolValue; /* 1 1 */
	BYTE unused[6]; /* 2 6 */
} HUF_CTableHeader;

// <070163A1> thirdparty/zstd/zstd.c:3241
unsigned int FSE_versionNumber(void)
{
} /* size: 0 */

// <0701637C> thirdparty/zstd/zstd.c:3245
unsigned int FSE_isError(size_t code)
{
} /* size: 0 */

// <07016357> thirdparty/zstd/zstd.c:3246
const char* FSE_getErrorName(size_t code)
{
} /* size: 0 */

// <07016332> thirdparty/zstd/zstd.c:3248
unsigned int HUF_isError(size_t code)
{
} /* size: 0 */

// <0701630D> thirdparty/zstd/zstd.c:3249
const char* HUF_getErrorName(size_t code)
{
} /* size: 0 */

// <070161DA> thirdparty/zstd/zstd.c:3256
// variables: 16
inline size_t FSE_readNCount_body(short int* normalizedCounter, unsigned int* maxSVPtr, unsigned int* tableLogPtr, const void* headerBuffer, size_t hbSize)
{
	const BYTE* istart; // 3259
	const BYTE* iend; // 3260
	const BYTE* ip; // 3261
	int nbBits; // 3262
	int remaining; // 3263
	int threshold; // 3264
	U32 bitStream; // 3265
	int bitCount; // 3266
	unsigned int charnum; // 3267
	const unsigned int  maxSV1; // 3268
	int previous0; // 3269
	{
		char buffer; // 3273
		{
			const size_t  countSize; // 3275
		}
	}
	{
		int repeats; // 3302
	}
	{
		const int  max; // 3346
		int count; // 3347
	}
} /* size: 0, variables: 11 */

// <07015C85> thirdparty/zstd/zstd.c:3404
// variables: 5
// function calls: 15
size_t FSE_readNCount_body_default(short int* normalizedCounter, unsigned int* maxSVPtr, unsigned int* tableLogPtr, const void* headerBuffer, size_t hbSize)
{
	{
		char buffer; // 3273
		{
			const size_t  countSize; // 3275
			FSE_readNCount_bmi2(short int* normalizedCounter,
						unsigned int* maxSVPtr,
						unsigned int* tableLogPtr,
						const void* headerBuffer,
						size_t hbSize,
						int bmi2);  // 3437
			FSE_readNCount(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize);  // 3275
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 3285
	{
		const int  max; // 3346
		int count; // 3347
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 3378
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 3391
	}
	{
		int repeats; // 3302
		ZSTD_countTrailingZeros32(U32 val); // 3302
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 3312
		ZSTD_countTrailingZeros32(U32 val); // 3313
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 3343
	}
	FSE_readNCount_body(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 3408
} /* size: 874, inline expansions: 3 (785 bytes) */

// <07015781> thirdparty/zstd/zstd.c:3412
// variables: 5
// function calls: 15
size_t FSE_readNCount_body_bmi2(short int* normalizedCounter, unsigned int* maxSVPtr, unsigned int* tableLogPtr, const void* headerBuffer, size_t hbSize)
{
	{
		char buffer; // 3273
		{
			const size_t  countSize; // 3275
			FSE_readNCount_bmi2(short int* normalizedCounter,
						unsigned int* maxSVPtr,
						unsigned int* tableLogPtr,
						const void* headerBuffer,
						size_t hbSize,
						int bmi2);  // 3437
			FSE_readNCount(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize);  // 3275
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 3285
	{
		const int  max; // 3346
		int count; // 3347
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 3378
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 3391
	}
	{
		int repeats; // 3302
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 3312
		ZSTD_countTrailingZeros32(U32 val); // 3313
		ZSTD_countTrailingZeros32(U32 val); // 3302
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 3343
	}
	FSE_readNCount_body(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 3416
} /* size: 797, inline expansions: 3 (716 bytes) */

// <0701571B> thirdparty/zstd/zstd.c:3420
size_t FSE_readNCount_bmi2(short int* normalizedCounter, unsigned int* maxSVPtr, unsigned int* tableLogPtr, const void* headerBuffer, size_t hbSize, int bmi2)
{
} /* size: 0 */

// <070156C2> thirdparty/zstd/zstd.c:3433
size_t FSE_readNCount(short int* normalizedCounter, unsigned int* maxSVPtr, unsigned int* tableLogPtr, const void* headerBuffer, size_t hbSize)
{
} /* size: 0 */

// <07015642> thirdparty/zstd/zstd.c:3448
// variable: 1
size_t HUF_readStats(BYTE* huffWeight, size_t hwSize, U32* rankStats, U32* nbSymbolsPtr, U32* tableLogPtr, const void* src, size_t srcSize)
{
	U32 wksp; // 3452
} /* size: 0, variables: 1 */

// <07015513> thirdparty/zstd/zstd.c:3457
// variables: 11
inline size_t HUF_readStats_body(BYTE* huffWeight, size_t hwSize, U32* rankStats, U32* nbSymbolsPtr, U32* tableLogPtr, const void* src, size_t srcSize, void* workSpace, size_t wkspSize, int bmi2)
{
	U32 weightTotal; // 3463
	const BYTE* ip; // 3464
	size_t iSize; // 3465
	size_t oSize; // 3466
	{
		U32 n; // 3478
	}
	{
		U32 n; // 3493
	}
	{
		const U32  tableLog; // 3501
		{
			const U32  total; // 3505
			const U32  rest; // 3506
			const U32  verif; // 3507
			const U32  lastWeight; // 3508
		}
	}
} /* size: 0, variables: 4 */

// <07015151> thirdparty/zstd/zstd.c:3523
// variables: 7
// function calls: 6
size_t HUF_readStats_body_default(BYTE* huffWeight, size_t hwSize, U32* rankStats, U32* nbSymbolsPtr, U32* tableLogPtr, const void* src, size_t srcSize, void* workSpace, size_t wkspSize)
{
	{
		U32 n; // 3478
	}
	FSE_decompress_wksp_bmi2(void* dst,
				size_t dstCapacity,
				const void* cSrc,
				size_t cSrcSize,
				unsigned int maxLog,
				void* workSpace,
				size_t wkspSize,
				int bmi2);  // 3486
	{
		U32 n; // 3493
	}
	{
		const U32  tableLog; // 3501
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 3501
		{
			const U32  total; // 3505
			const U32  rest; // 3506
			const U32  verif; // 3507
			const U32  lastWeight; // 3508
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 3507
		}
	}
	HUF_readStats_body(BYTE* huffWeight,
				size_t hwSize,
				U32* rankStats,
				U32* nbSymbolsPtr,
				U32* tableLogPtr,
				const void* src,
				size_t srcSize,
				void* workSpace,
				size_t wkspSize,
				int bmi2);  // 3528
} /* size: 539, inline expansions: 2 (541 bytes) */

// <07014D8F> thirdparty/zstd/zstd.c:3532
// variables: 7
// function calls: 6
size_t HUF_readStats_body_bmi2(BYTE* huffWeight, size_t hwSize, U32* rankStats, U32* nbSymbolsPtr, U32* tableLogPtr, const void* src, size_t srcSize, void* workSpace, size_t wkspSize)
{
	{
		U32 n; // 3478
	}
	FSE_decompress_wksp_bmi2(void* dst,
				size_t dstCapacity,
				const void* cSrc,
				size_t cSrcSize,
				unsigned int maxLog,
				void* workSpace,
				size_t wkspSize,
				int bmi2);  // 3486
	{
		U32 n; // 3493
	}
	{
		const U32  tableLog; // 3501
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 3501
		{
			const U32  total; // 3505
			const U32  rest; // 3506
			const U32  verif; // 3507
			const U32  lastWeight; // 3508
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 3507
		}
	}
	HUF_readStats_body(BYTE* huffWeight,
				size_t hwSize,
				U32* rankStats,
				U32* nbSymbolsPtr,
				U32* tableLogPtr,
				const void* src,
				size_t srcSize,
				void* workSpace,
				size_t wkspSize,
				int bmi2);  // 3537
} /* size: 539, inline expansions: 2 (545 bytes) */

// <07014CF5> thirdparty/zstd/zstd.c:3541
size_t HUF_readStats_wksp(BYTE* huffWeight, size_t hwSize, U32* rankStats, U32* nbSymbolsPtr, U32* tableLogPtr, const void* src, size_t srcSize, void* workSpace, size_t wkspSize, int flags)
{
} /* size: 0 */

// <07014C9D> thirdparty/zstd/zstd.c:3571
// variable: 1
const char* ERR_getErrorString(ERR_code)
{
	const char* notErrorCode; // 3577
} /* size: 320, variables: 1 */

// <07014847> thirdparty/zstd/zstd.c:3680
// variables: 31
// function calls: 4
size_t FSE_buildDTable_internal(FSE_DTable* dt, const short int* normalizedCounter, unsigned int maxSymbolValue, unsigned int tableLog, void* workSpace, size_t wkspSize)
{
	const void* tdPtr; // 3682
	const FSE_decode_t* tableDecode; // 3683
	U16* symbolNext; // 3684
	BYTE* spread; // 3685
	const U32  maxSV1; // 3687
	const U32  tableSize; // 3688
	U32 highThreshold; // 3689
	{
		FSE_DTableHeader DTableH; // 3697
		{
			const S16  largeLimit; // 3700
			U32 s; // 3701
		}
	}
	{
		const size_t  tableMask; // 3715
		const size_t  step; // 3716
		{
			const U64  add; // 3723
			size_t pos; // 3724
			U64 sv; // 3725
			U32 s; // 3726
			{
				int i; // 3728
				const int  n; // 3729
				MEM_write64(void* memPtr,
						U64 value);  // 3730
				MEM_write64(void* memPtr,
						U64 value);  // 3732
			}
		}
		{
			size_t position; // 3743
			size_t s; // 3744
			const size_t  unroll; // 3745
			{
				size_t u; // 3748
				{
					const size_t  uPosition; // 3750
				}
			}
		}
	}
	{
		const U32  tableMask; // 3758
		const U32  step; // 3759
		U32 s; // 3760
		U32 position; // 3760
		{
			int i; // 3762
		}
	}
	{
		U32 u; // 3772
		{
			const BYTE  symbol; // 3774
			const U32  nextState; // 3775
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 3776
		}
	}
} /* size: 644, variables: 7 */

// <07014774> thirdparty/zstd/zstd.c:3783
size_t FSE_buildDTable_wksp(FSE_DTable* dt, const short int* normalizedCounter, unsigned int maxSymbolValue, unsigned int tableLog, void* workSpace, size_t wkspSize)
{
} /* size: 0 */

// <070146A5> thirdparty/zstd/zstd.c:3795
// variables: 8
inline size_t FSE_decompress_usingDTable_generic(void* dst, size_t maxDstSize, const void* cSrc, size_t cSrcSize, const FSE_DTable* dt, const unsigned int fast)
{
	const BYTE* ostart; // 3800
	BYTE* op; // 3801
	const BYTE* omax; // 3802
	const BYTE* olimit; // 3803
	BIT_DStream_t bitD; // 3805
	FSE_DState_t state1; // 3806
	FSE_DState_t state2; // 3807
	{
		const size_t  _var_err__; // 3810
	}
} /* size: 0, variables: 7 */

// <06F3A0F8> thirdparty/zstd/zstd.c:3860
// member variable: 1
// struct size: 512
typedef struct {
	short int ncount[256]; /* 0 512 */
} FSE_DecompressWksp;

// <07014560> thirdparty/zstd/zstd.c:3865
// variables: 12
inline size_t FSE_decompress_wksp_body(void* dst, size_t dstCapacity, const void* cSrc, size_t cSrcSize, unsigned int maxLog, void* workSpace, size_t wkspSize, int bmi2)
{
	const BYTE* istart; // 3871
	const BYTE* ip; // 3872
	unsigned int tableLog; // 3873
	unsigned int maxSymbolValue; // 3874
	const FSE_DecompressWksp* wksp; // 3875
	const size_t  dtablePos; // 3876
	const FSE_DTable* dtable; // 3877
	{
		const size_t  NCountLength; // 3886
	}
	{
		const size_t  _var_err__; // 3900
	}
	{
		const void* ptr; // 3903
		const FSE_DTableHeader* DTableH; // 3904
		const U32  fastMode; // 3905
	}
} /* size: 0, variables: 7 */

// <0701193D> thirdparty/zstd/zstd.c:3914
// variables: 35
// function calls: 191
size_t FSE_decompress_wksp_body_default(void* dst, size_t dstCapacity, const void* cSrc, size_t cSrcSize, unsigned int maxLog, void* workSpace, size_t wkspSize)
{
	{
		const size_t  NCountLength; // 3886
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3887
	}
	{
		const size_t  _var_err__; // 3900
	}
	{
		const void* ptr; // 3903
		const FSE_DTableHeader* DTableH; // 3904
		const U32  fastMode; // 3905
		{
			const size_t  _var_err__; // 3810
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 3810
		}
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2852
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 2853
		FSE_initDState(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD,
				const FSE_DTable* dt);  // 3812
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2852
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 2853
		FSE_initDState(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD,
				const FSE_DTable* dt);  // 3813
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3815
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3826
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3831
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3836
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3843
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3844
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3850
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3851
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3845
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3852
		FSE_decompress_usingDTable_generic(void* dst,
							size_t maxDstSize,
							const void* cSrc,
							size_t cSrcSize,
							const FSE_DTable* dt,
							const unsigned int  fast);  // 3908
		{
			const size_t  _var_err__; // 3810
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 3810
		}
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2852
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 2853
		FSE_initDState(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD,
				const FSE_DTable* dt);  // 3812
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2852
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 2853
		FSE_initDState(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD,
				const FSE_DTable* dt);  // 3813
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3815
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3820
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3821
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3826
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3831
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3836
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3843
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3844
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3850
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3851
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3845
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3852
		FSE_decompress_usingDTable_generic(void* dst,
							size_t maxDstSize,
							const void* cSrc,
							size_t cSrcSize,
							const FSE_DTable* dt,
							const unsigned int  fast);  // 3909
	}
	FSE_decompress_wksp_body(void* dst,
				size_t dstCapacity,
				const void* cSrc,
				size_t cSrcSize,
				unsigned int maxLog,
				void* workSpace,
				size_t wkspSize,
				int bmi2);  // 3916
} /* size: 3454, inline expansions: 1 (3363 bytes) */

// <0700ECE5> thirdparty/zstd/zstd.c:3920
// variables: 35
// function calls: 191
size_t FSE_decompress_wksp_body_bmi2(void* dst, size_t dstCapacity, const void* cSrc, size_t cSrcSize, unsigned int maxLog, void* workSpace, size_t wkspSize)
{
	{
		const size_t  NCountLength; // 3886
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3887
	}
	{
		const size_t  _var_err__; // 3900
	}
	{
		const void* ptr; // 3903
		const FSE_DTableHeader* DTableH; // 3904
		const U32  fastMode; // 3905
		{
			const size_t  _var_err__; // 3810
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 3810
		}
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2852
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 2853
		FSE_initDState(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD,
				const FSE_DTable* dt);  // 3812
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2852
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 2853
		FSE_initDState(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD,
				const FSE_DTable* dt);  // 3813
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3815
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3826
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3831
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3836
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3843
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3844
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2478
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2480
		BIT_readBitsFast(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2889
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3850
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3851
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3845
		FSE_decodeSymbolFast(FSE_DState_t* DStatePtr,
					BIT_DStream_t* bitD);  // 3852
		FSE_decompress_usingDTable_generic(void* dst,
							size_t maxDstSize,
							const void* cSrc,
							size_t cSrcSize,
							const FSE_DTable* dt,
							const unsigned int  fast);  // 3908
		{
			const size_t  _var_err__; // 3810
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 3810
		}
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2852
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 2853
		FSE_initDState(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD,
				const FSE_DTable* dt);  // 3812
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2852
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 2853
		FSE_initDState(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD,
				const FSE_DTable* dt);  // 3813
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3815
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3820
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3821
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3826
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3831
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3836
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3843
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3844
		BIT_getMiddleBits(BitContainerType bitContainer,
					const U32  start,
					const U32  nbBits);  // 2441
		BIT_lookBits(const BIT_DStream_t* bitD,
				U32 nbBits);  // 2469
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 2470
		BIT_readBits(BIT_DStream_t* bitD,
				unsigned int nbBits);  // 2876
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3850
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 3851
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3845
		FSE_decodeSymbol(FSE_DState_t* DStatePtr,
				BIT_DStream_t* bitD);  // 3852
		FSE_decompress_usingDTable_generic(void* dst,
							size_t maxDstSize,
							const void* cSrc,
							size_t cSrcSize,
							const FSE_DTable* dt,
							const unsigned int  fast);  // 3909
	}
	FSE_decompress_wksp_body(void* dst,
				size_t dstCapacity,
				const void* cSrc,
				size_t cSrcSize,
				unsigned int maxLog,
				void* workSpace,
				size_t wkspSize,
				int bmi2);  // 3922
} /* size: 3290, inline expansions: 1 (3204 bytes) */

// <0700EC65> thirdparty/zstd/zstd.c:3926
size_t FSE_decompress_wksp_bmi2(void* dst, size_t dstCapacity, const void* cSrc, size_t cSrcSize, unsigned int maxLog, void* workSpace, size_t wkspSize, int bmi2)
{
} /* size: 0 */

// <06F3AB2B> thirdparty/zstd/zstd.c:4842
// member variables: 3
// struct size: 16
typedef struct {
	size_t error; /* 0 8 */
	int lowerBound; /* 8 4 */
	int upperBound; /* 12 4 */
} ZSTD_bounds;

// <06F3AC00> thirdparty/zstd/zstd.c:4999
// member variables: 3
// struct size: 24
struct ZSTD_inBuffer_s {
	const void * src; /* 0 8 */
	size_t size; /* 8 8 */
	size_t pos; /* 16 8 */
};

// <06F3AC51> thirdparty/zstd/zstd.c:5005
// member variables: 3
// struct size: 24
struct ZSTD_outBuffer_s {
	void * dst; /* 0 8 */
	size_t size; /* 8 8 */
	size_t pos; /* 16 8 */
};

// <06F3B07C> thirdparty/zstd/zstd.c:5613
// member variables: 4
// struct size: 16
typedef struct {
	unsigned int offset; /* 0 4 */
	unsigned int litLength; /* 4 4 */
	unsigned int matchLength; /* 8 4 */
	unsigned int rep; /* 12 4 */
} ZSTD_Sequence;

// <06F3B0DD> thirdparty/zstd/zstd.c:5650
// member variables: 7
// struct size: 28
typedef struct {
	unsigned int windowLog; /* 0 4 */
	unsigned int chainLog; /* 4 4 */
	unsigned int hashLog; /* 8 4 */
	unsigned int searchLog; /* 12 4 */
	unsigned int minMatch; /* 16 4 */
	unsigned int targetLength; /* 20 4 */
	ZSTD_strategy strategy; /* 24 4 */
} ZSTD_compressionParameters;

// <06F3B16E> thirdparty/zstd/zstd.c:5660
// member variables: 3
// struct size: 12
typedef struct {
	int contentSizeFlag; /* 0 4 */
	int checksumFlag; /* 4 4 */
	int noDictIDFlag; /* 8 4 */
} ZSTD_frameParameters;

// <06F3B1BF> thirdparty/zstd/zstd.c:5666
// member variables: 2
// struct size: 40
typedef struct {
	ZSTD_compressionParameters cParams; /* 0 28 */
	ZSTD_frameParameters fParams; /* 28 12 */
} ZSTD_parameters;

// <06F3B38A> thirdparty/zstd/zstd.c:5810
// member variables: 9
// struct size: 48
typedef struct {
	long long unsigned int frameContentSize; /* 0 8 */
	long long unsigned int windowSize; /* 8 8 */
	unsigned int blockSizeMax; /* 16 4 */
	ZSTD_FrameType_e frameType; /* 20 4 */
	unsigned int headerSize; /* 24 4 */
	unsigned int dictID; /* 28 4 */
	unsigned int checksumFlag; /* 32 4 */
	unsigned int _reserved1; /* 36 4 */
	unsigned int _reserved2; /* 40 4 */
} ZSTD_FrameHeader;

// <06F3B4AD> thirdparty/zstd/zstd.c:6168
// member variables: 3
// struct size: 24
typedef struct {
	ZSTD_allocFunction customAlloc; /* 0 8 */
	ZSTD_freeFunction customFree; /* 8 8 */
	void * opaque; /* 16 8 */
} ZSTD_customMem;

// <06F3B614> thirdparty/zstd/zstd.c:7034
// member variables: 6
// struct size: 40
typedef struct {
	long long unsigned int ingested; /* 0 8 */
	long long unsigned int consumed; /* 8 8 */
	long long unsigned int produced; /* 16 8 */
	long long unsigned int flushed; /* 24 8 */
	unsigned int currentJobID; /* 32 4 */
	unsigned int nbActiveWorkers; /* 36 4 */
} ZSTD_frameProgression;

// <0700EC38> thirdparty/zstd/zstd.c:7504
inline void* ZSTD_customMalloc(size_t size, ZSTD_customMem customMem)
{
} /* size: 0 */

// <0700EBFC> thirdparty/zstd/zstd.c:7511
// variable: 1
inline void* ZSTD_customCalloc(size_t size, ZSTD_customMem customMem)
{
	{
		const void* ptr; // 7516
	}
} /* size: 0 */

// <0700EBD3> thirdparty/zstd/zstd.c:7523
inline void ZSTD_customFree(void* ptr, ZSTD_customMem customMem)
{
} /* size: 0 */

// <06F3B744> thirdparty/zstd/zstd.c:7632
// member variables: 2
// struct size: 16
struct POOL_job_s {
	POOL_function function; /* 0 8 */
	void * opaque; /* 8 8 */
};

// <06F3B522> thirdparty/zstd/zstd.c:7637
// member variables: 14
// struct size: 240
struct POOL_ctx_s {
	ZSTD_customMem customMem; /* 0 24 */
	pthread_t * threads; /* 24 8 */
	size_t threadCapacity; /* 32 8 */
	size_t threadLimit; /* 40 8 */
	POOL_job * queue; /* 48 8 */
	size_t queueHead; /* 56 8 */
	size_t queueTail; /* 64 8 */
	size_t queueSize; /* 72 8 */
	size_t numThreadsBusy; /* 80 8 */
	int queueEmpty; /* 88 4 */
	pthread_mutex_t queueMutex; /* 96 40 */
	pthread_cond_t queuePushCond; /* 136 48 */
	pthread_cond_t queuePopCond; /* 184 48 */
	int shutdown; /* 232 4 */
};

// <07017543> thirdparty/zstd/zstd.c:7670
// variables: 2
// function call: 1
void* POOL_thread(void* opaque)
{
	const POOL_ctx* ctx; // 7671
	{
		const POOL_job  job; // 7689
	}
	POOL_thread(void* opaque); // 7670
} /* size: 254, variables: 1, inline expansions: 1 (207 bytes) */

// <0700EB97> thirdparty/zstd/zstd.c:7670
// variables: 2
void* POOL_thread(void* opaque)
{
	const POOL_ctx* ctx; // 7671
	{
		const POOL_job  job; // 7689
	}
} /* size: 0, variables: 1 */

// <0700EB0E> thirdparty/zstd/zstd.c:7710
// function call: 1
POOL_ctx* ZSTD_createThreadPool(size_t numThreads)
{
	POOL_create(size_t numThreads,
			size_t queueSize);  // 7711
} /* size: 56, inline expansions: 1 (46 bytes) */

// <0700EADC> thirdparty/zstd/zstd.c:7714
POOL_ctx* POOL_create(size_t numThreads, size_t queueSize)
{
} /* size: 0 */

// <0700E6DA> thirdparty/zstd/zstd.c:7718
// variables: 6
// function calls: 8
POOL_ctx* POOL_create_advanced(size_t numThreads, size_t queueSize, ZSTD_customMem customMem)
{
	POOL_ctx* ctx; // 7721
	{
		int error; // 7738
	}
	{
		size_t i; // 7752
		POOL_free(POOL_ctx* ctx); // 7756
	}
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7725
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7732
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7746
	POOL_free(POOL_ctx* ctx); // 7750
} /* size: 506, variables: 1, inline expansions: 7 (229 bytes) */

// <0700E6B1> thirdparty/zstd/zstd.c:7768
// variable: 1
void POOL_join(POOL_ctx* ctx)
{
	{
		size_t i; // 7777
	}
} /* size: 0 */

// <0700E690> thirdparty/zstd/zstd.c:7783
void POOL_free(POOL_ctx* ctx)
{
} /* size: 0 */

// <0700E602> thirdparty/zstd/zstd.c:7797
void POOL_joinJobs(POOL_ctx* ctx)
{
} /* size: 0 */

// <0700E583> thirdparty/zstd/zstd.c:7805
// function call: 1
void ZSTD_freeThreadPool(ZSTD_threadPool* pool)
{
	POOL_free(POOL_ctx* ctx); // 7806
} /* size: 17, inline expansions: 1 (12 bytes) */

// <0700E55E> thirdparty/zstd/zstd.c:7809
size_t POOL_sizeof(const POOL_ctx* ctx)
{
} /* size: 0 */

// <0700E36A> thirdparty/zstd/zstd.c:7818
// variables: 3
// function calls: 3
int POOL_resize_internal(POOL_ctx* ctx, size_t numThreads)
{
	{
		const pthread_t* threadPool; // 7826
		{
			size_t threadId; // 7833
		}
		{
			const void* ptr; // 7516
		}
		ZSTD_customCalloc(size_t size,
					ZSTD_customMem customMem);  // 7511
		ZSTD_customCalloc(size_t size,
					ZSTD_customMem customMem);  // 7826
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 7830
	}
} /* size: 317 */

// <0700E32B> thirdparty/zstd/zstd.c:7847
// variable: 1
int POOL_resize(POOL_ctx* ctx, size_t numThreads)
{
	int result; // 7849
} /* size: 0, variables: 1 */

// <0700E305> thirdparty/zstd/zstd.c:7864
int isQueueFull(const POOL_ctx* ctx)
{
} /* size: 0 */

// <0700E2C2> thirdparty/zstd/zstd.c:7875
// variable: 1
void POOL_add_internal(POOL_ctx* ctx, POOL_function function, void* opaque)
{
	POOL_job job; // 7877
} /* size: 0, variables: 1 */

// <0700E0F7> thirdparty/zstd/zstd.c:7889
// function calls: 4
void POOL_add(POOL_ctx* ctx, POOL_function function, void* opaque)
{
	isQueueFull(const POOL_ctx* ctx); // 7864
	isQueueFull(const POOL_ctx* ctx); // 7894
	POOL_add_internal(POOL_ctx* ctx,
				POOL_function function,
				void* opaque);  // 7875
	POOL_add_internal(POOL_ctx* ctx,
				POOL_function function,
				void* opaque);  // 7897
} /* size: 209, inline expansions: 4 (179 bytes) */

// <0700DF3E> thirdparty/zstd/zstd.c:7902
// function calls: 4
int POOL_tryAdd(POOL_ctx* ctx, POOL_function function, void* opaque)
{
	isQueueFull(const POOL_ctx* ctx); // 7864
	isQueueFull(const POOL_ctx* ctx); // 7906
	POOL_add_internal(POOL_ctx* ctx,
				POOL_function function,
				void* opaque);  // 7875
	POOL_add_internal(POOL_ctx* ctx,
				POOL_function function,
				void* opaque);  // 7910
} /* size: 197, inline expansions: 4 (195 bytes) */

// <0700DEB7> thirdparty/zstd/zstd.c:8049
// variables: 8
inline ZSTD_cpuid_t ZSTD_cpuid(void)
{
	U32 f1c; // 8050
	U32 f1d; // 8051
	U32 f7b; // 8052
	U32 f7c; // 8053
	U32 n; // 8139
	{
		U32 f1a; // 8142
	}
	{
		U32 f7a; // 8146
	}
	{
		ZSTD_cpuid_t cpuid; // 8154
	}
} /* size: 0, variables: 5 */

// <0700DE97> thirdparty/zstd/zstd.c:8234
inline int ZSTD_cpuid_bmi1(const ZSTD_cpuid_t cpuid)
{
} /* size: 0 */

// <0700DE77> thirdparty/zstd/zstd.c:8238
inline int ZSTD_cpuid_bmi2(const ZSTD_cpuid_t cpuid)
{
} /* size: 0 */

// <06F3B860> thirdparty/zstd/zstd.c:9922
// member variables: 6
// struct size: 88
struct XXH_NAMESPACEXXH64_state_s {
	XXH64_hash_t total_len; /* 0 8 */
	XXH64_hash_t v[4]; /* 8 32 */
	XXH64_hash_t mem64[4]; /* 40 32 */
	XXH32_hash_t memsize; /* 72 4 */
	XXH32_hash_t reserved32; /* 76 4 */
	XXH64_hash_t reserved64; /* 80 8 */
};

// <0700DE3D> thirdparty/zstd/zstd.c:10637
void* XXH_memcpy(void* dest, const void* src, size_t size)
{
} /* size: 0 */

// <0700DE1D> thirdparty/zstd/zstd.c:10860
xxh_u32 XXH_read32(const void* ptr)
{
} /* size: 0 */

// <0700DDFD> thirdparty/zstd/zstd.c:11079
inline xxh_u32 XXH_readLE32(const void* ptr)
{
} /* size: 0 */

// <0700DDD0> thirdparty/zstd/zstd.c:11091
inline xxh_u32 XXH_readLE32_align(const void* ptr, XXH_alignment align)
{
} /* size: 0 */

// <0700DDB0> thirdparty/zstd/zstd.c:11539
xxh_u64 XXH_read64(const void* ptr)
{
} /* size: 0 */

// <0700DD90> thirdparty/zstd/zstd.c:11609
inline xxh_u64 XXH_readLE64(const void* ptr)
{
} /* size: 0 */

// <0700DD63> thirdparty/zstd/zstd.c:11621
inline xxh_u64 XXH_readLE64_align(const void* ptr, XXH_alignment align)
{
} /* size: 0 */

// <0700DD36> thirdparty/zstd/zstd.c:11655
xxh_u64 XXH64_round(xxh_u64 acc, xxh_u64 input)
{
} /* size: 0 */

// <0700DD09> thirdparty/zstd/zstd.c:11680
xxh_u64 XXH64_mergeRound(xxh_u64 acc, xxh_u64 val)
{
} /* size: 0 */

// <0700DCE9> thirdparty/zstd/zstd.c:11689
xxh_u64 XXH64_avalanche(xxh_u64 hash)
{
} /* size: 0 */

// <0700DB75> thirdparty/zstd/zstd.c:11718
// variable: 1
// function calls: 4
xxh_u64 XXH64_finalize(xxh_u64 hash, const xxh_u8* ptr, size_t len, XXH_alignment align)
{
	{
		const xxh_u64  k1; // 11723
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11723
		XXH_readLE64_align(const void* ptr,
					XXH_alignment align);  // 11723
	}
	XXH_readLE32_align(const void* ptr,
				XXH_alignment align);  // 11730
	XXH64_avalanche(xxh_u64 hash); // 11740
} /* size: 309, inline expansions: 2 (58 bytes) */

// <0700DAD5> thirdparty/zstd/zstd.c:11762
// variables: 7
inline xxh_u64 XXH64_endian_align(const xxh_u8* input, size_t len, xxh_u64 seed, XXH_alignment align)
{
	xxh_u64 h64; // 11764
	{
		const xxh_u8* bEnd; // 11768
		const xxh_u8* limit; // 11769
		xxh_u64 v1; // 11770
		xxh_u64 v2; // 11771
		xxh_u64 v3; // 11772
		xxh_u64 v4; // 11773
	}
} /* size: 0, variables: 1 */

// <0700DA9B> thirdparty/zstd/zstd.c:11799
inline XXH64_hash_t XXH_INLINE_XXH64(const void* input, size_t len, XXH64_hash_t seed)
{
} /* size: 0 */

// <0700DA6E> thirdparty/zstd/zstd.c:11839
inline XXH_NAMESPACEXXH_errorcode XXH_INLINE_XXH64_reset(XXH_NAMESPACEXXH64_state_t* statePtr, XXH64_hash_t seed)
{
} /* size: 0 */

// <0701A146> thirdparty/zstd/zstd.c:11852
// variables: 3
// function calls: 30
XXH_NAMESPACEXXH_errorcode XXH_INLINE_XXH64_update(XXH_NAMESPACEXXH64_state_t* state, const void* input, size_t len)
{
	{
		const xxh_u8* p; // 11859
		const xxh_u8* bEnd; // 11860
		{
			const xxh_u8* limit; // 11881
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11884
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11884
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11885
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11885
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11886
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11886
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11887
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11887
		}
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 10639
		XXH_memcpy(void* dest,
				const void* src,
				size_t size);  // 11871
		XXH_read64(const void* ptr); // 11611
		XXH_readLE64(const void* ptr); // 11872
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11872
		XXH_read64(const void* ptr); // 11611
		XXH_readLE64(const void* ptr); // 11873
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11873
		XXH_read64(const void* ptr); // 11611
		XXH_readLE64(const void* ptr); // 11874
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11874
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11875
		XXH_read64(const void* ptr); // 11611
		XXH_readLE64(const void* ptr); // 11875
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 10639
		XXH_memcpy(void* dest,
				const void* src,
				size_t size);  // 11893
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 10639
		XXH_memcpy(void* dest,
				const void* src,
				size_t size);  // 11865
	}
} /* size: 550 */

// <0700D9FA> thirdparty/zstd/zstd.c:11852
// variables: 3
inline XXH_NAMESPACEXXH_errorcode XXH_INLINE_XXH64_update(XXH_NAMESPACEXXH64_state_t* state, const void* input, size_t len)
{
	{
		const xxh_u8* p; // 11859
		const xxh_u8* bEnd; // 11860
		{
			const xxh_u8* limit; // 11881
		}
	}
} /* size: 0 */

// <0700D9C7> thirdparty/zstd/zstd.c:11903
// variable: 1
inline XXH64_hash_t XXH_INLINE_XXH64_digest(const XXH_NAMESPACEXXH64_state_t* state)
{
	xxh_u64 h64; // 11905
} /* size: 0, variables: 1 */

// <0700D99E> thirdparty/zstd/zstd.c:15666
void ZSTD_copy8(void* dst, const void* src)
{
} /* size: 0 */

// <0700D975> thirdparty/zstd/zstd.c:15679
void ZSTD_copy16(void* dst, const void* src)
{
} /* size: 0 */

// <0700D900> thirdparty/zstd/zstd.c:15712
// variables: 4
inline void ZSTD_wildcopy(void* dst, const void* src, ptrdiff_t length, const ZSTD_overlap_e ovtype)
{
	ptrdiff_t diff; // 15714
	const BYTE* ip; // 15715
	BYTE* op; // 15716
	const BYTE* oend; // 15717
} /* size: 0, variables: 4 */

// <0700D8AC> thirdparty/zstd/zstd.c:15744
// variable: 1
inline size_t ZSTD_limitCopy(void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	const size_t  length; // 15746
} /* size: 0, variables: 1 */

// <06F3BB8F> thirdparty/zstd/zstd.c:15780
// member variables: 3
// struct size: 24
typedef struct {
	size_t nbBlocks; /* 0 8 */
	size_t compressedSize; /* 8 8 */
	long long unsigned int decompressedBound; /* 16 8 */
} ZSTD_frameSizeInfo;

// <06F3BBE0> thirdparty/zstd/zstd.c:15793
// member variables: 3
// struct size: 12
typedef struct {
	blockType_e blockType; /* 0 4 */
	U32 lastBlock; /* 4 4 */
	U32 origSize; /* 8 4 */
} blockProperties_t;

// <0700D88C> thirdparty/zstd/zstd.c:15814
// variable: 1
inline int ZSTD_cpuSupportsBmi2(void)
{
	ZSTD_cpuid_t cpuid; // 15816
} /* size: 0, variables: 1 */

// <0700D86A> thirdparty/zstd/zstd.c:15827
unsigned int ZSTD_versionNumber(void)
{
} /* size: 10 */

// <0700D848> thirdparty/zstd/zstd.c:15829
const char* ZSTD_versionString(void)
{
} /* size: 12 */

// <0700D827> thirdparty/zstd/zstd.c:15839
unsigned int ZSTD_isError(size_t code)
{
} /* size: 0 */

// <0700D806> thirdparty/zstd/zstd.c:15843
const char* ZSTD_getErrorName(size_t code)
{
} /* size: 0 */

// <0700D79F> thirdparty/zstd/zstd.c:15847
// function call: 1
ZSTD_ErrorCode ZSTD_getErrorCode(size_t code)
{
	ERR_getErrorCode(size_t code); // 15847
} /* size: 18, inline expansions: 1 (13 bytes) */

// <0700D74D> thirdparty/zstd/zstd.c:15851
const char* ZSTD_getErrorString(ZSTD_ErrorCode code)
{
} /* size: 9 */

// <0700D28D> thirdparty/zstd/zstd.c:16005
// variables: 34
// function calls: 4
size_t FSE_buildCTable_wksp(FSE_CTable* ct, const short int* normalizedCounter, unsigned int maxSymbolValue, unsigned int tableLog, void* workSpace, size_t wkspSize)
{
	const U32  tableSize; // 16009
	const U32  tableMask; // 16010
	const void* ptr; // 16011
	const U16* tableU16; // 16012
	const void* FSCT; // 16013
	const FSE_symbolCompressionTransform* symbolTT; // 16014
	const U32  step; // 16015
	const U32  maxSV1; // 16016
	U16* cumul; // 16018
	const BYTE* tableSymbol; // 16019
	U32 highThreshold; // 16021
	{
		U32 u; // 16038
	}
	{
		const BYTE* spread; // 16057
		{
			const U64  add; // 16058
			size_t pos; // 16059
			U64 sv; // 16060
			U32 s; // 16061
			{
				int i; // 16063
				const int  n; // 16064
				MEM_write64(void* memPtr,
						U64 value);  // 16065
				MEM_write64(void* memPtr,
						U64 value);  // 16067
			}
		}
		{
			size_t position; // 16077
			size_t s; // 16078
			const size_t  unroll; // 16079
			{
				size_t u; // 16082
				{
					const size_t  uPosition; // 16084
				}
			}
		}
	}
	{
		U32 position; // 16092
		U32 symbol; // 16093
		{
			int nbOccurrences; // 16095
			const int  freq; // 16096
		}
	}
	{
		U32 u; // 16107
		{
			BYTE s; // 16108
		}
	}
	{
		unsigned int total; // 16113
		unsigned int s; // 16114
		{
			const U32  maxBitsOut; // 16132
			const U32  minStatePlus; // 16133
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 16132
		}
	}
} /* size: 919, variables: 11 */

// <0700D24E> thirdparty/zstd/zstd.c:16160
// variable: 1
size_t FSE_NCountWriteBound(unsigned int maxSymbolValue, unsigned int tableLog)
{
	const size_t  maxHeaderSize; // 16162
} /* size: 0, variables: 1 */

// <0700D05E> thirdparty/zstd/zstd.c:16171
// variables: 15
size_t FSE_writeNCount_generic(void* header, size_t headerBufferSize, const short int* normalizedCounter, unsigned int maxSymbolValue, unsigned int tableLog, unsigned int writeIsSafe)
{
	const BYTE* ostart; // 16175
	BYTE* out; // 16176
	const BYTE* oend; // 16177
	int nbBits; // 16178
	const int  tableSize; // 16179
	int remaining; // 16180
	int threshold; // 16181
	U32 bitStream; // 16182
	int bitCount; // 16183
	unsigned int symbol; // 16184
	const unsigned int  alphabetSize; // 16185
	int previousIs0; // 16186
	{
		unsigned int start; // 16199
	}
	{
		int count; // 16228
		const int  max; // 16229
	}
} /* size: 683, variables: 12 */

// <0700D005> thirdparty/zstd/zstd.c:16267
size_t FSE_writeNCount(void* buffer, size_t bufferSize, const short int* normalizedCounter, unsigned int maxSymbolValue, unsigned int tableLog)
{
} /* size: 0 */

// <0700CFB1> thirdparty/zstd/zstd.c:16285
// variables: 3
unsigned int FSE_minTableLog(size_t srcSize, unsigned int maxSymbolValue)
{
	U32 minBitsSrc; // 16287
	U32 minBitsSymbols; // 16288
	U32 minBits; // 16289
} /* size: 0, variables: 3 */

// <0700CF3E> thirdparty/zstd/zstd.c:16294
// variables: 3
unsigned int FSE_optimalTableLog_internal(unsigned int maxTableLog, size_t srcSize, unsigned int maxSymbolValue, unsigned int minus)
{
	U32 maxBitsSrc; // 16296
	U32 tableLog; // 16297
	U32 minBits; // 16298
} /* size: 0, variables: 3 */

// <0700CEFF> thirdparty/zstd/zstd.c:16308
unsigned int FSE_optimalTableLog(unsigned int maxTableLog, size_t srcSize, unsigned int maxSymbolValue)
{
} /* size: 0 */

// <0700CDC6> thirdparty/zstd/zstd.c:16316
// variables: 16
size_t FSE_normalizeM2(short int* norm, U32 tableLog, const unsigned int* count, size_t total, U32 maxSymbolValue, short int lowProbCount)
{
	const short int  NOT_YET_ASSIGNED; // 16318
	U32 s; // 16319
	U32 distributed; // 16320
	U32 ToDistribute; // 16321
	const U32  lowThreshold; // 16324
	U32 lowOne; // 16325
	{
		U32 maxV; // 16369
		U32 maxC; // 16369
	}
	{
		const U64  vStepLog; // 16383
		const U64  mid; // 16384
		const U64  rStep; // 16385
		U64 tmpTotal; // 16386
		{
			const U64  end; // 16389
			const U32  sStart; // 16390
			const U32  sEnd; // 16391
			const U32  weight; // 16392
		}
	}
} /* size: 0, variables: 6 */

// <0700C93F> thirdparty/zstd/zstd.c:16402
// variables: 23
// function calls: 6
size_t FSE_normalizeCount(short int* normalizedCounter, unsigned int tableLog, const unsigned int* count, size_t total, unsigned int maxSymbolValue, unsigned int useLowProbCount)
{
	{
		const U32   rtbTable; // 16412
		const short int  lowProbCount; // 16413
		const U64  scale; // 16414
		const U64  step; // 16415
		const U64  vStep; // 16416
		int stillToDistribute; // 16417
		unsigned int s; // 16418
		unsigned int largest; // 16419
		short int largestP; // 16420
		U32 lowThreshold; // 16421
		{
			short int proba; // 16430
			{
				U64 restToBeat; // 16432
			}
		}
		{
			const size_t  errorCode; // 16441
			{
				U32 maxV; // 16369
				U32 maxC; // 16369
			}
			{
				const U64  vStepLog; // 16383
				const U64  mid; // 16384
				const U64  rStep; // 16385
				U64 tmpTotal; // 16386
				{
					const U64  end; // 16389
					const U32  sStart; // 16390
					const U32  sEnd; // 16391
					const U32  weight; // 16392
				}
			}
			FSE_normalizeM2(short int* norm,
					U32 tableLog,
					const unsigned int* count,
					size_t total,
					U32 maxSymbolValue,
					short int lowProbCount);  // 16441
		}
	}
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 16287
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 16288
	FSE_minTableLog(size_t srcSize,
			unsigned int maxSymbolValue);  // 16410
} /* size: 961, inline expansions: 5 (18 bytes) */

// <0700C8CF> thirdparty/zstd/zstd.c:16465
// variables: 4
size_t FSE_buildCTable_rle(FSE_CTable* ct, BYTE symbolValue)
{
	void* ptr; // 16467
	U16* tableU16; // 16468
	void* FSCTptr; // 16469
	FSE_symbolCompressionTransform* symbolTT; // 16470
} /* size: 0, variables: 4 */

// <0700B621> thirdparty/zstd/zstd.c:16488
// variables: 19
// function calls: 71
size_t FSE_compress_usingCTable_generic(void* dst, size_t dstSize, const void* src, size_t srcSize, const FSE_CTable* ct, const unsigned int fast)
{
	const BYTE* istart; // 16492
	const BYTE* iend; // 16493
	const BYTE* ip; // 16494
	BIT_CStream_t bitC; // 16496
	FSE_CState_t CState1; // 16497
	FSE_CState_t CState2; // 16497
	{
		const size_t  initError; // 16501
		BIT_initCStream(BIT_CStream_t* bitC,
				void* startPtr,
				size_t dstCapacity);  // 16501
	}
	MEM_read16(const void* ptr); // 2763
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 16507
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2315
	BIT_flushBitsFast(BIT_CStream_t* bitC); // 16510
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2790
	FSE_encodeSymbol(BIT_CStream_t* bitC,
			FSE_CState_t* statePtr,
			unsigned int symbol);  // 16509
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 16508
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2790
	FSE_encodeSymbol(BIT_CStream_t* bitC,
			FSE_CState_t* statePtr,
			unsigned int symbol);  // 16519
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2790
	FSE_encodeSymbol(BIT_CStream_t* bitC,
			FSE_CState_t* statePtr,
			unsigned int symbol);  // 16520
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2315
	BIT_flushBitsFast(BIT_CStream_t* bitC); // 16521
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 16512
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 16513
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2790
	FSE_encodeSymbol(BIT_CStream_t* bitC,
			FSE_CState_t* statePtr,
			unsigned int symbol);  // 16527
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2790
	FSE_encodeSymbol(BIT_CStream_t* bitC,
			FSE_CState_t* statePtr,
			unsigned int symbol);  // 16532
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2790
	FSE_encodeSymbol(BIT_CStream_t* bitC,
			FSE_CState_t* statePtr,
			unsigned int symbol);  // 16535
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2315
	BIT_flushBitsFast(BIT_CStream_t* bitC); // 16539
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2790
	FSE_encodeSymbol(BIT_CStream_t* bitC,
			FSE_CState_t* statePtr,
			unsigned int symbol);  // 16536
	BIT_flushBits(BIT_CStream_t* bitC); // 16539
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2796
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2797
	FSE_flushCState(BIT_CStream_t* bitC,
			const FSE_CState_t* statePtr);  // 16542
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2796
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2797
	FSE_flushCState(BIT_CStream_t* bitC,
			const FSE_CState_t* statePtr);  // 16543
	BIT_addBitsFast(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2343
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2344
	BIT_closeCStream(BIT_CStream_t* bitC); // 16544
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 16510
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 16521
} /* size: 1327, variables: 6, inline expansions: 70 (1663 bytes) */

// <0700B5BC> thirdparty/zstd/zstd.c:16547
// variable: 1
size_t FSE_compress_usingCTable(void* dst, size_t dstSize, const void* src, size_t srcSize, const FSE_CTable* ct)
{
	const unsigned int  fast; // 16551
} /* size: 0, variables: 1 */

// <0700B582> thirdparty/zstd/zstd.c:16560
size_t FSE_compressBound(size_t size)
{
} /* size: 0 */

// <0700B55D> thirdparty/zstd/zstd.c:16588
unsigned int HIST_isError(size_t code)
{
} /* size: 0 */

// <0700B509> thirdparty/zstd/zstd.c:16593
// variables: 2
void HIST_add(unsigned int* count, const void* src, size_t srcSize)
{
	const BYTE* ip; // 16595
	const BYTE* end; // 16596
} /* size: 0, variables: 2 */

// <0700B3F1> thirdparty/zstd/zstd.c:16603
// variables: 5
unsigned int HIST_count_simple(unsigned int* count, unsigned int* maxSymbolValuePtr, const void* src, size_t srcSize)
{
	const BYTE* ip; // 16606
	const BYTE* end; // 16607
	unsigned int maxSymbolValue; // 16608
	unsigned int largestCount; // 16609
	{
		U32 s; // 16622
	}
} /* size: 180, variables: 4 */

// <0700B0C6> thirdparty/zstd/zstd.c:16640
// variables: 12
// function calls: 5
size_t HIST_count_parallel_wksp(unsigned int* count, unsigned int* maxSymbolValuePtr, const void* source, size_t sourceSize, HIST_checkInput_e check, const U32* workSpace)
{
	const BYTE* ip; // 16646
	const BYTE* iend; // 16647
	const size_t  countSize; // 16648
	unsigned int max; // 16649
	const U32* Counting1; // 16650
	const U32* Counting2; // 16651
	const U32* Counting3; // 16652
	const U32* Counting4; // 16653
	{
		U32 cached; // 16665
		{
			U32 c; // 16667
			MEM_read32(const void* ptr); // 16667
			MEM_read32(const void* ptr); // 16672
			MEM_read32(const void* ptr); // 16677
			MEM_read32(const void* ptr); // 16682
		}
		MEM_read32(const void* ptr); // 16665
	}
	{
		U32 s; // 16694
	}
	{
		unsigned int maxSymbolValue; // 16700
	}
} /* size: 705, variables: 8 */

// <0700B060> thirdparty/zstd/zstd.c:16714
size_t HIST_countFast_wksp(unsigned int* count, unsigned int* maxSymbolValuePtr, const void* source, size_t sourceSize, void* workSpace, size_t workSpaceSize)
{
} /* size: 0 */

// <0700AFFA> thirdparty/zstd/zstd.c:16728
size_t HIST_count_wksp(unsigned int* count, unsigned int* maxSymbolValuePtr, const void* source, size_t sourceSize, void* workSpace, size_t workSpaceSize)
{
} /* size: 0 */

// <0700AE39> thirdparty/zstd/zstd.c:16742
// variable: 1
// function calls: 2
size_t HIST_countFast(unsigned int* count, unsigned int* maxSymbolValuePtr, const void* source, size_t sourceSize)
{
	unsigned int tmpCounters; // 16745
	HIST_countFast_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16714
	HIST_countFast_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16746
} /* size: 0, variables: 1, inline expansions: 2 (0 bytes) */

// <0700AB72> thirdparty/zstd/zstd.c:16749
// variable: 1
// function calls: 4
size_t HIST_count(unsigned int* count, unsigned int* maxSymbolValuePtr, const void* src, size_t srcSize)
{
	unsigned int tmpCounters; // 16752
	HIST_countFast_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16714
	HIST_countFast_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16737
	HIST_count_wksp(unsigned int* count,
			unsigned int* maxSymbolValuePtr,
			const void* source,
			size_t sourceSize,
			void* workSpace,
			size_t workSpaceSize);  // 16728
	HIST_count_wksp(unsigned int* count,
			unsigned int* maxSymbolValuePtr,
			const void* source,
			size_t sourceSize,
			void* workSpace,
			size_t workSpaceSize);  // 16753
} /* size: 0, variables: 1, inline expansions: 4 (0 bytes) */

// <06F3BC59> thirdparty/zstd/zstd.c:16804
// member variables: 4
// struct size: 8
struct nodeElt_s {
	U32 count; /* 0 4 */
	U16 parent; /* 4 2 */
	BYTE byte; /* 6 1 */
	BYTE nbBits; /* 7 1 */
};

// <0700AB04> thirdparty/zstd/zstd.c:16869
// variables: 4
void* HUF_alignUpWorkspace(void* workspace, size_t* workspaceSizePtr, size_t align)
{
	const size_t  mask; // 16871
	const size_t  rem; // 16872
	const size_t  add; // 16873
	const BYTE* aligned; // 16874
} /* size: 0, variables: 4 */

// <06F3BCBA> thirdparty/zstd/zstd.c:16896
// member variables: 4
// struct size: 480
typedef struct {
	FSE_CTable CTable[59]; /* 0 236 */
	U32 scratchBuffer[41]; /* 236 164 */
	unsigned int count[13]; /* 400 52 */
	S16 norm[13]; /* 452 26 */
} HUF_CompressWeightsWksp;

// <0700A9F5> thirdparty/zstd/zstd.c:16904
// variables: 11
size_t HUF_compressWeights(void* dst, size_t dstSize, const void* weightTable, size_t wtSize, void* workspace, size_t workspaceSize)
{
	const BYTE* ostart; // 16908
	BYTE* op; // 16909
	const BYTE* oend; // 16910
	unsigned int maxSymbolValue; // 16912
	U32 tableLog; // 16913
	HUF_CompressWeightsWksp* wksp; // 16914
	{
		const unsigned int  maxCount; // 16922
	}
	{
		const size_t  _var_err__; // 16928
	}
	{
		const size_t  hSize; // 16931
	}
	{
		const size_t  _var_err__; // 16936
	}
	{
		const size_t  cSize; // 16937
	}
} /* size: 0, variables: 6 */

// <0700A9D5> thirdparty/zstd/zstd.c:16945
size_t HUF_getNbBits(HUF_CElt elt)
{
} /* size: 0 */

// <0700A9B5> thirdparty/zstd/zstd.c:16950
size_t HUF_getNbBitsFast(HUF_CElt elt)
{
} /* size: 0 */

// <0700A995> thirdparty/zstd/zstd.c:16955
size_t HUF_getValue(HUF_CElt elt)
{
} /* size: 0 */

// <0700A975> thirdparty/zstd/zstd.c:16960
size_t HUF_getValueFast(HUF_CElt elt)
{
} /* size: 0 */

// <0700A94C> thirdparty/zstd/zstd.c:16965
void HUF_setNbBits(HUF_CElt* elt, size_t nbBits)
{
} /* size: 0 */

// <0700A916> thirdparty/zstd/zstd.c:16971
// variable: 1
void HUF_setValue(HUF_CElt* elt, size_t value)
{
	const size_t  nbBits; // 16973
} /* size: 0, variables: 1 */

// <0700A8E4> thirdparty/zstd/zstd.c:16980
// variable: 1
HUF_CTableHeader HUF_readCTableHeader(const HUF_CElt* ctable)
{
	HUF_CTableHeader header; // 16982
} /* size: 0, variables: 1 */

// <0700A8A1> thirdparty/zstd/zstd.c:16987
// variable: 1
void HUF_writeCTableHeader(HUF_CElt* ctable, U32 tableLog, U32 maxSymbolValue)
{
	HUF_CTableHeader header; // 16989
} /* size: 0, variables: 1 */

// <06F3BD5A> thirdparty/zstd/zstd.c:16999
// member variables: 3
// struct size: 748
typedef struct {
	HUF_CompressWeightsWksp wksp; /* 0 480 */
	BYTE bitsToWeight[13]; /* 480 13 */
	BYTE huffWeight[255]; /* 493 255 */
} HUF_WriteCTableWksp;

// <0700A0D3> thirdparty/zstd/zstd.c:17005
// variables: 10
// function calls: 15
size_t HUF_writeCTable_wksp(void* dst, size_t maxDstSize, const HUF_CElt* CTable, unsigned int maxSymbolValue, unsigned int huffLog, void* workspace, size_t workspaceSize)
{
	const HUF_CElt* ct; // 17009
	BYTE* op; // 17010
	U32 n; // 17011
	HUF_WriteCTableWksp* wksp; // 17012
	{
		const size_t  hSize; // 17032
		HUF_alignUpWorkspace(void* workspace,
					size_t* workspaceSizePtr,
					size_t align);  // 16914
		{
			const unsigned int  maxCount; // 16922
		}
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 16296
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 16287
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 16288
		FSE_minTableLog(size_t srcSize,
				unsigned int maxSymbolValue);  // 16298
		FSE_optimalTableLog_internal(unsigned int maxTableLog,
						size_t srcSize,
						unsigned int maxSymbolValue,
						unsigned int minus);  // 16310
		FSE_optimalTableLog(unsigned int maxTableLog,
					size_t srcSize,
					unsigned int maxSymbolValue);  // 16927
		{
			const size_t  _var_err__; // 16928
		}
		{
			const size_t  hSize; // 16931
		}
		{
			const size_t  _var_err__; // 16936
		}
		{
			const size_t  cSize; // 16937
			FSE_compress_usingCTable(void* dst,
						size_t dstSize,
						const void* src,
						size_t srcSize,
						const FSE_CTable* ct);  // 16547
			FSE_compress_usingCTable(void* dst,
						size_t dstSize,
						const void* src,
						size_t srcSize,
						const FSE_CTable* ct);  // 16937
		}
		HUF_compressWeights(void* dst,
					size_t dstSize,
					const void* weightTable,
					size_t wtSize,
					void* workspace,
					size_t workspaceSize);  // 17032
	}
	HUF_alignUpWorkspace(void* workspace,
				size_t* workspaceSizePtr,
				size_t align);  // 17012
	HUF_getNbBits(HUF_CElt elt); // 17028
} /* size: 1099, variables: 4, inline expansions: 2 (20 bytes) */

// <07009C1E> thirdparty/zstd/zstd.c:17049
// variables: 17
// function calls: 7
size_t HUF_readCTable(HUF_CElt* CTable, unsigned int* maxSymbolValuePtr, const void* src, size_t srcSize, unsigned int* hasZeroWeights)
{
	BYTE huffWeight; // 17051
	U32 rankVal; // 17052
	U32 tableLog; // 17053
	U32 nbSymbols; // 17054
	const HUF_CElt* ct; // 17055
	const size_t  readSize; // 17058
	{
		U32 n; // 17070
		U32 nextRankStart; // 17070
		{
			U32 curr; // 17072
		}
	}
	{
		U32 n; // 17078
		{
			const U32  w; // 17079
			HUF_setNbBits(HUF_CElt* elt,
					size_t nbBits);  // 17080
		}
	}
	{
		U16 nbPerRank; // 17084
		U16 valPerRank; // 17085
		{
			U32 n; // 17086
			HUF_getNbBits(HUF_CElt elt); // 17086
		}
		{
			U16 min; // 17089
			U32 n; // 17090
		}
		{
			U32 n; // 17096
			HUF_getNbBits(HUF_CElt elt); // 17096
			HUF_setValue(HUF_CElt* elt,
					size_t value);  // 17096
		}
	}
	HUF_readStats_wksp(BYTE* huffWeight,
				size_t hwSize,
				U32* rankStats,
				U32* nbSymbolsPtr,
				U32* tableLogPtr,
				const void* src,
				size_t srcSize,
				void* workSpace,
				size_t wkspSize,
				int flags);  // 3453
	HUF_readStats(BYTE* huffWeight,
			size_t hwSize,
			U32* rankStats,
			U32* nbSymbolsPtr,
			U32* tableLogPtr,
			const void* src,
			size_t srcSize);  // 17058
	HUF_writeCTableHeader(HUF_CElt* ctable,
				U32 tableLog,
				U32 maxSymbolValue);  // 17067
} /* size: 1207, variables: 6, inline expansions: 3 (170 bytes) */

// <07009BE0> thirdparty/zstd/zstd.c:17102
// variable: 1
U32 HUF_getNbBitsFromCTable(const HUF_CElt* CTable, U32 symbolValue)
{
	const HUF_CElt* ct; // 17104
} /* size: 0, variables: 1 */

// <07009ADF> thirdparty/zstd/zstd.c:17133
// variables: 13
U32 HUF_setMaxHeight(nodeElt* huffNode, U32 lastNonNull, U32 targetNbBits)
{
	const U32  largestBits; // 17135
	{
		int totalCost; // 17142
		const U32  baseCost; // 17143
		int n; // 17144
		{
			const U32  noSymbol; // 17167
			U32 rankLast; // 17168
			{
				U32 currentNbBits; // 17172
				int pos; // 17173
			}
			{
				U32 nBitsToDecrease; // 17184
				{
					const U32  highPos; // 17186
					const U32  lowPos; // 17187
					{
						const U32  highTotal; // 17193
						const U32  lowTotal; // 17194
					}
				}
			}
		}
	}
} /* size: 0, variables: 1 */

// <06F3BDC9> thirdparty/zstd/zstd.c:17257
// member variables: 2
// struct size: 4
typedef struct {
	U16 base; /* 0 2 */
	U16 curr; /* 2 2 */
} rankPos;

// <06F3BE23> thirdparty/zstd/zstd.c:17267
// member variables: 2
// struct size: 4,864
typedef struct {
	huffNodeTable huffNodeTbl; /* 0 4096 */
	rankPos rankPosition[192]; /* 4096 768 */
} HUF_buildCTable_wksp_tables;

// <07009ABF> thirdparty/zstd/zstd.c:17287
U32 HUF_getIndex(const U32 count)
{
} /* size: 0 */

// <07009A8D> thirdparty/zstd/zstd.c:17294
// variable: 1
void HUF_swapNodes(nodeElt* a, nodeElt* b)
{
	nodeElt tmp; // 17295
} /* size: 0, variables: 1 */

// <07009A25> thirdparty/zstd/zstd.c:17312
// variables: 4
inline void HUF_insertionSort(nodeElt* huffNode, const int low, const int high)
{
	int i; // 17313
	const int  size; // 17314
	{
		const nodeElt  key; // 17317
		int j; // 17318
	}
} /* size: 0, variables: 2 */

// <070099C8> thirdparty/zstd/zstd.c:17328
// variables: 3
int HUF_quickSortPartition(nodeElt* arr, const int low, const int high)
{
	const U32  pivot; // 17332
	int i; // 17333
	int j; // 17334
} /* size: 0, variables: 3 */

// <0700979C> thirdparty/zstd/zstd.c:17348
// variables: 4
// function calls: 4
void HUF_simpleQuickSort(nodeElt* arr, int low, int high)
{
	const int  kInsertionSortThreshold; // 17349
	{
		const int  idx; // 17355
		HUF_swapNodes(nodeElt* a,
				nodeElt* b);  // 17338
		HUF_swapNodes(nodeElt* a,
				nodeElt* b);  // 17341
		HUF_quickSortPartition(nodeElt* arr,
					const int  low,
					const int  high);  // 17355
	}
	{
		const nodeElt  key; // 17317
		int j; // 17318
	}
	HUF_insertionSort(nodeElt* huffNode,
				const int  low,
				const int  high);  // 17351
} /* size: 450, variables: 1, inline expansions: 1 (133 bytes) */

// <070096E3> thirdparty/zstd/zstd.c:17377
// variables: 8
void HUF_sort(nodeElt* huffNode, const unsigned int* count, const U32 maxSymbolValue, rankPos* rankPosition)
{
	U32 n; // 17378
	const U32  maxSymbolValue1; // 17379
	{
		U32 lowerRank; // 17389
	}
	{
		const U32  c; // 17403
		const U32  r; // 17404
		const U32  pos; // 17405
	}
	{
		const int  bucketSize; // 17413
		const U32  bucketStartIdx; // 17414
	}
} /* size: 0, variables: 2 */

// <07009643> thirdparty/zstd/zstd.c:17438
// variables: 9
int HUF_buildTree(nodeElt* huffNode, U32 maxSymbolValue)
{
	const nodeElt* huffNode0; // 17440
	int nonNullRank; // 17441
	int lowS; // 17442
	int lowN; // 17442
	int nodeNb; // 17443
	int n; // 17444
	int nodeRoot; // 17444
	{
		const int  n1; // 17458
		const int  n2; // 17459
	}
} /* size: 0, variables: 7 */

// <07009590> thirdparty/zstd/zstd.c:17487
// variables: 6
void HUF_buildCTableFromTree(HUF_CElt* CTable, const nodeElt* huffNode, int nonNullRank, U32 maxSymbolValue, U32 maxNbBits)
{
	const HUF_CElt* ct; // 17489
	int n; // 17491
	U16 nbPerRank; // 17492
	U16 valPerRank; // 17493
	const int  alphabetSize; // 17494
	{
		U16 min; // 17498
	}
} /* size: 0, variables: 5 */

// <07008DBC> thirdparty/zstd/zstd.c:17513
// variables: 25
// function calls: 17
size_t HUF_buildCTable_wksp(HUF_CElt* CTable, const unsigned int* count, U32 maxSymbolValue, U32 maxNbBits, void* workSpace, size_t wkspSize)
{
	const HUF_buildCTable_wksp_tables* wksp_tables; // 17516
	const nodeElt* huffNode0; // 17518
	const nodeElt* huffNode; // 17519
	int nonNullRank; // 17520
	HUF_alignUpWorkspace(void* workspace,
				size_t* workspaceSizePtr,
				size_t align);  // 17517
	{
		U32 lowerRank; // 17389
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 17290
		HUF_getIndex(const U32 count); // 17389
	}
	{
		const U32  c; // 17403
		const U32  r; // 17404
		const U32  pos; // 17405
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 17290
		HUF_getIndex(const U32 count); // 17404
	}
	{
		const int  bucketSize; // 17413
		const U32  bucketStartIdx; // 17414
	}
	HUF_sort(nodeElt* huffNode,
		const unsigned int* count,
		const U32  maxSymbolValue,
		rankPos* rankPosition);  // 17535
	{
		const int  n1; // 17458
		const int  n2; // 17459
	}
	HUF_buildTree(nodeElt* huffNode,
			U32 maxSymbolValue);  // 17539
	{
		int totalCost; // 17142
		const U32  baseCost; // 17143
		int n; // 17144
		{
			const U32  noSymbol; // 17167
			U32 rankLast; // 17168
			{
				U32 currentNbBits; // 17172
				int pos; // 17173
			}
			{
				U32 nBitsToDecrease; // 17184
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 17184
				{
					const U32  highPos; // 17186
					const U32  lowPos; // 17187
					{
						const U32  highTotal; // 17193
						const U32  lowTotal; // 17194
					}
				}
			}
		}
	}
	HUF_setMaxHeight(nodeElt* huffNode,
			U32 lastNonNull,
			U32 targetNbBits);  // 17542
	{
		U16 min; // 17498
	}
	HUF_setNbBits(HUF_CElt* elt,
			size_t nbBits);  // 17505
	HUF_getNbBits(HUF_CElt elt); // 17507
	HUF_setValue(HUF_CElt* elt,
			size_t value);  // 17507
	HUF_writeCTableHeader(HUF_CElt* ctable,
				U32 tableLog,
				U32 maxSymbolValue);  // 17509
	HUF_buildCTableFromTree(HUF_CElt* CTable,
				const nodeElt* huffNode,
				int nonNullRank,
				U32 maxSymbolValue,
				U32 maxNbBits);  // 17545
} /* size: 2514, variables: 4, inline expansions: 9 (2386 bytes) */

// <07008D59> thirdparty/zstd/zstd.c:17550
// variables: 3
size_t HUF_estimateCompressedSize(const HUF_CElt* CTable, const unsigned int* count, unsigned int maxSymbolValue)
{
	const HUF_CElt* ct; // 17552
	size_t nbBits; // 17553
	int s; // 17554
} /* size: 0, variables: 3 */

// <07008CE9> thirdparty/zstd/zstd.c:17561
// variables: 4
int HUF_validateCTable(const HUF_CElt* CTable, const unsigned int* count, unsigned int maxSymbolValue)
{
	HUF_CTableHeader header; // 17562
	const HUF_CElt* ct; // 17563
	int bad; // 17564
	int s; // 17565
} /* size: 0, variables: 4 */

// <07008CAF> thirdparty/zstd/zstd.c:17578
size_t HUF_compressBound(size_t size)
{
} /* size: 0 */

// <06F3BE71> thirdparty/zstd/zstd.c:17600
// member variables: 5
// struct size: 56
typedef struct {
	size_t bitContainer[2]; /* 0 16 */
	size_t bitPos[2]; /* 16 16 */
	BYTE * startPtr; /* 32 8 */
	BYTE * ptr; /* 40 8 */
	BYTE * endPtr; /* 48 8 */
} HUF_CStream_t;

// <07008C75> thirdparty/zstd/zstd.c:17613
size_t HUF_initCStream(HUF_CStream_t* bitC, void* startPtr, size_t dstCapacity)
{
} /* size: 0 */

// <07008C32> thirdparty/zstd/zstd.c:17634
inline void HUF_addBits(HUF_CStream_t* bitC, HUF_CElt elt, int idx, int kFast)
{
} /* size: 0 */

// <07008BFA> thirdparty/zstd/zstd.c:17678
inline void HUF_mergeIndex1(HUF_CStream_t* bitC)
{
} /* size: 0 */

// <07008BAA> thirdparty/zstd/zstd.c:17694
// variables: 3
inline void HUF_flushBits(HUF_CStream_t* bitC, int kFast)
{
	const size_t  nbBits; // 17697
	const size_t  nbBytes; // 17698
	const size_t  bitContainer; // 17700
} /* size: 0, variables: 3 */

// <07008B8A> thirdparty/zstd/zstd.c:17719
// variable: 1
HUF_CElt HUF_endMark(void)
{
	HUF_CElt endMark; // 17721
} /* size: 0, variables: 1 */

// <07008B5B> thirdparty/zstd/zstd.c:17730
// variable: 1
size_t HUF_closeCStream(HUF_CStream_t* bitC)
{
	{
		const size_t  nbBits; // 17735
	}
} /* size: 0 */

// <07008B0B> thirdparty/zstd/zstd.c:17742
inline void HUF_encodeSymbol(HUF_CStream_t* bitCPtr, U32 symbol, const HUF_CElt* CTable, int idx, int fast)
{
} /* size: 0 */

// <07008A67> thirdparty/zstd/zstd.c:17748
// variables: 4
inline void HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC, const BYTE* ip, size_t srcSize, const HUF_CElt* ct, int kUnroll, int kFastFlush, int kLastFast)
{
	int n; // 17754
	int rem; // 17755
	{
		int u; // 17766
	}
	{
		int u; // 17778
	}
} /* size: 0, variables: 2 */

// <07008A3A> thirdparty/zstd/zstd.c:17806
size_t HUF_tightCompressBound(size_t srcSize, size_t tableLog)
{
} /* size: 0 */

// <0700897F> thirdparty/zstd/zstd.c:17813
// variables: 8
inline size_t HUF_compress1X_usingCTable_internal_body(void* dst, size_t dstSize, const void* src, size_t srcSize, const HUF_CElt* CTable)
{
	const U32  tableLog; // 17817
	const HUF_CElt* ct; // 17818
	const BYTE* ip; // 17819
	const BYTE* ostart; // 17820
	const BYTE* oend; // 17821
	HUF_CStream_t bitC; // 17822
	{
		BYTE* op; // 17826
		const size_t  initErr; // 17827
	}
} /* size: 0, variables: 6 */

// <07003271> thirdparty/zstd/zstd.c:17880
// variables: 17
// function calls: 312
size_t HUF_compress1X_usingCTable_internal_bmi2(void* dst, size_t dstSize, const void* src, size_t srcSize, const HUF_CElt* CTable)
{
	HUF_readCTableHeader(const HUF_CElt* ctable); // 17817
	{
		BYTE* op; // 17826
		const size_t  initErr; // 17827
		HUF_initCStream(HUF_CStream_t* bitC,
				void* startPtr,
				size_t dstCapacity);  // 17827
	}
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17851
	HUF_tightCompressBound(size_t srcSize,
				size_t tableLog);  // 17830
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17831
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17732
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17733
	{
		const size_t  nbBits; // 17735
	}
	HUF_closeCStream(HUF_CStream_t* bitC); // 17874
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17854
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17857
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17860
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17863
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17867
	HUF_compress1X_usingCTable_internal_body(void* dst,
						size_t dstSize,
						const void* src,
						size_t srcSize,
						const HUF_CElt* CTable);  // 17884
} /* size: 3838, inline expansions: 72 (7924 bytes) */

// <06FFDD41> thirdparty/zstd/zstd.c:17888
// variables: 17
// function calls: 312
size_t HUF_compress1X_usingCTable_internal_default(void* dst, size_t dstSize, const void* src, size_t srcSize, const HUF_CElt* CTable)
{
	HUF_readCTableHeader(const HUF_CElt* ctable); // 17817
	{
		BYTE* op; // 17826
		const size_t  initErr; // 17827
		HUF_initCStream(HUF_CStream_t* bitC,
				void* startPtr,
				size_t dstCapacity);  // 17827
	}
	HUF_tightCompressBound(size_t srcSize,
				size_t tableLog);  // 17830
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17851
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17831
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17732
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17733
	{
		const size_t  nbBits; // 17735
	}
	HUF_closeCStream(HUF_CStream_t* bitC); // 17874
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17854
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17857
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17860
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_getValue(HUF_CElt elt); // 17644
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17863
	HUF_getNbBits(HUF_CElt elt); // 17643
	HUF_getValue(HUF_CElt elt); // 17644
	HUF_addBits(HUF_CStream_t* bitC,
			HUF_CElt elt,
			int idx,
			int kFast);  // 17744
	HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
			U32 symbol,
			const HUF_CElt* CTable,
			int idx,
			int fast);  // 17758
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 17706
	HUF_flushBits(HUF_CStream_t* bitC,
			int kFast);  // 17760
	{
		int u; // 17766
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17768
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17770
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17771
	}
	{
		int u; // 17778
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17780
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17782
		HUF_zeroIndex1(HUF_CStream_t* bitC); // 17788
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17783
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17790
		HUF_getNbBits(HUF_CElt elt); // 17643
		HUF_addBits(HUF_CStream_t* bitC,
				HUF_CElt elt,
				int idx,
				int kFast);  // 17744
		HUF_encodeSymbol(HUF_CStream_t* bitCPtr,
				U32 symbol,
				const HUF_CElt* CTable,
				int idx,
				int fast);  // 17792
		HUF_mergeIndex1(HUF_CStream_t* bitC); // 17794
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 17706
		HUF_flushBits(HUF_CStream_t* bitC,
				int kFast);  // 17795
	}
	HUF_compress1X_usingCTable_internal_body_loop(HUF_CStream_t* bitC,
							const BYTE* ip,
							size_t srcSize,
							const HUF_CElt* ct,
							int kUnroll,
							int kFastFlush,
							int kLastFast);  // 17867
	HUF_compress1X_usingCTable_internal_body(void* dst,
						size_t dstSize,
						const void* src,
						size_t srcSize,
						const HUF_CElt* CTable);  // 17892
} /* size: 3801, inline expansions: 72 (7773 bytes) */

// <06FFDCE0> thirdparty/zstd/zstd.c:17896
size_t HUF_compress1X_usingCTable_internal(void* dst, size_t dstSize, const void* src, size_t srcSize, const HUF_CElt* CTable, const int flags)
{
} /* size: 0 */

// <06FFDC7A> thirdparty/zstd/zstd.c:17919
size_t HUF_compress1X_usingCTable(void* dst, size_t dstSize, const void* src, size_t srcSize, const HUF_CElt* CTable, int flags)
{
} /* size: 0 */

// <06FFD699> thirdparty/zstd/zstd.c:17925
// variables: 10
// function calls: 10
size_t HUF_compress4X_usingCTable_internal(void* dst, size_t dstSize, const void* src, size_t srcSize, const HUF_CElt* CTable, int flags)
{
	const size_t  segmentSize; // 17929
	const BYTE* ip; // 17930
	const BYTE* iend; // 17931
	const BYTE* ostart; // 17932
	const BYTE* oend; // 17933
	BYTE* op; // 17934
	{
		const size_t  cSize; // 17941
		HUF_compress1X_usingCTable_internal(void* dst,
							size_t dstSize,
							const void* src,
							size_t srcSize,
							const HUF_CElt* CTable,
							const int  flags);  // 17941
		MEM_write16(void* memPtr,
				U16 value);  // 1279
		MEM_writeLE16(void* memPtr,
				U16 val);  // 17943
	}
	{
		const size_t  cSize; // 17949
		HUF_compress1X_usingCTable_internal(void* dst,
							size_t dstSize,
							const void* src,
							size_t srcSize,
							const HUF_CElt* CTable,
							const int  flags);  // 17949
		MEM_write16(void* memPtr,
				U16 value);  // 1279
		MEM_writeLE16(void* memPtr,
				U16 val);  // 17951
	}
	{
		const size_t  cSize; // 17957
		HUF_compress1X_usingCTable_internal(void* dst,
							size_t dstSize,
							const void* src,
							size_t srcSize,
							const HUF_CElt* CTable,
							const int  flags);  // 17957
		MEM_write16(void* memPtr,
				U16 value);  // 1279
		MEM_writeLE16(void* memPtr,
				U16 val);  // 17959
	}
	{
		const size_t  cSize; // 17966
		HUF_compress1X_usingCTable_internal(void* dst,
							size_t dstSize,
							const void* src,
							size_t srcSize,
							const HUF_CElt* CTable,
							const int  flags);  // 17966
	}
} /* size: 398, variables: 6 */

// <06FFD633> thirdparty/zstd/zstd.c:17974
size_t HUF_compress4X_usingCTable(void* dst, size_t dstSize, const void* src, size_t srcSize, const HUF_CElt* CTable, int flags)
{
} /* size: 0 */

// <06FFD47D> thirdparty/zstd/zstd.c:17981
// variable: 1
// function call: 1
size_t HUF_compressCTable_internal(const BYTE* ostart, BYTE* op, const BYTE* oend, const void* src, size_t srcSize, HUF_nbStreams_e nbStreams, const HUF_CElt* CTable, const int flags)
{
	const size_t  cSize; // 17986
	HUF_compress1X_usingCTable_internal(void* dst,
						size_t dstSize,
						const void* src,
						size_t srcSize,
						const HUF_CElt* CTable,
						const int  flags);  // 17987
} /* size: 151, variables: 1, inline expansions: 1 (31 bytes) */

// <06F3BF25> thirdparty/zstd/zstd.c:17998
// member variables: 3
// struct size: 7,944
typedef struct {
	/* thirdparty/zstd/zstd.c:18001 */
	typedef union {
		HUF_buildCTable_wksp_tables buildCTable_wksp; /* 0 4864 */
		HUF_WriteCTableWksp writeCTable_wksp; /* 0 748 */
		U32 hist_wksp[1024]; /* 0 4096 */
	} HUF_compress_tables_t;
	unsigned int count[256]; /* 0 1024 */
	HUF_CElt CTable[257]; /* 1024 2056 */
	union {
		HUF_buildCTable_wksp_tables buildCTable_wksp; /* 3080 4864 */
		HUF_WriteCTableWksp writeCTable_wksp; /* 3080 748 */
		U32 hist_wksp[1024]; /* 3080 4096 */
	} wksps; /* 3080 4864 */
} HUF_compress_tables_t;

// <06FFD433> thirdparty/zstd/zstd.c:18011
// variables: 2
unsigned int HUF_cardinality(const unsigned int* count, unsigned int maxSymbolValue)
{
	unsigned int cardinality; // 18013
	unsigned int i; // 18014
} /* size: 0, variables: 2 */

// <06FFD401> thirdparty/zstd/zstd.c:18023
// variable: 1
unsigned int HUF_minTableLog(unsigned int symbolCardinality)
{
	U32 minBitsSymbols; // 18025
} /* size: 0, variables: 1 */

// <06FFD2FB> thirdparty/zstd/zstd.c:18029
// variables: 10
unsigned int HUF_optimalTableLog(unsigned int maxTableLog, size_t srcSize, unsigned int maxSymbolValue, void* workSpace, size_t wkspSize, HUF_CElt* table, const unsigned int* count, int flags)
{
	{
		BYTE* dst; // 18046
		size_t dstSize; // 18047
		size_t hSize; // 18048
		size_t newSize; // 18048
		const unsigned int  symbolCardinality; // 18049
		const unsigned int  minTableLog; // 18050
		size_t optSize; // 18051
		unsigned int optLog; // 18052
		unsigned int optLogGuess; // 18052
		{
			size_t maxBits; // 18060
		}
	}
} /* size: 0 */

// <06FFC9A3> thirdparty/zstd/zstd.c:18090
// variables: 15
// function calls: 13
size_t HUF_compress_internal(void* dst, size_t dstSize, const void* src, size_t srcSize, unsigned int maxSymbolValue, unsigned int huffLog, HUF_nbStreams_e nbStreams, void* workSpace, size_t wkspSize, HUF_CElt* oldHufTable, HUF_repeat* repeat, int flags)
{
	const HUF_compress_tables_t* table; // 18097
	const BYTE* ostart; // 18098
	const BYTE* oend; // 18099
	BYTE* op; // 18100
	{
		size_t largestTotal; // 18125
		{
			unsigned int maxSymbolValueBegin; // 18127
			const size_t  largestBegin; // 18128
		}
		{
			unsigned int maxSymbolValueEnd; // 18131
			const size_t  largestEnd; // 18132
		}
	}
	{
		const size_t  largest; // 18139
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16714
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16737
		HIST_count_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16728
		HIST_count_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 18139
	}
	{
		const size_t  maxBits; // 18160
		{
			const size_t  _var_err__; // 18163
		}
	}
	{
		const size_t  hSize; // 18169
		{
			const size_t  oldSize; // 18173
			const size_t  newSize; // 18174
			HUF_getNbBits(HUF_CElt elt); // 17556
			HUF_estimateCompressedSize(const HUF_CElt* CTable,
							const unsigned int* count,
							unsigned int maxSymbolValue);  // 18173
			HUF_getNbBits(HUF_CElt elt); // 17556
			HUF_estimateCompressedSize(const HUF_CElt* CTable,
							const unsigned int* count,
							unsigned int maxSymbolValue);  // 18174
		}
	}
	HUF_alignUpWorkspace(void* workspace,
				size_t* workspaceSizePtr,
				size_t align);  // 18097
	HUF_readCTableHeader(const HUF_CElt* ctable); // 17562
	HUF_getNbBits(HUF_CElt elt); // 17573
	HUF_validateCTable(const HUF_CElt* CTable,
				const unsigned int* count,
				unsigned int maxSymbolValue);  // 17561
	HUF_validateCTable(const HUF_CElt* CTable,
				const unsigned int* count,
				unsigned int maxSymbolValue);  // 18148
} /* size: 1451, variables: 4, inline expansions: 5 (181 bytes) */

// <06FFC85D> thirdparty/zstd/zstd.c:18193
size_t HUF_compress1X_repeat(void* dst, size_t dstSize, const void* src, size_t srcSize, unsigned int maxSymbolValue, unsigned int huffLog, void* workSpace, size_t wkspSize, HUF_CElt* hufTable, HUF_repeat* repeat, int flags)
{
} /* size: 33 */

// <06FFC717> thirdparty/zstd/zstd.c:18210
size_t HUF_compress4X_repeat(void* dst, size_t dstSize, const void* src, size_t srcSize, unsigned int maxSymbolValue, unsigned int huffLog, void* workSpace, size_t wkspSize, HUF_CElt* hufTable, HUF_repeat* repeat, int flags)
{
} /* size: 33 */

// <06F3C045> thirdparty/zstd/zstd.c:18428
// member variables: 11
// struct size: 72
typedef struct {
	void * workspace; /* 0 8 */
	void * workspaceEnd; /* 8 8 */
	void * objectEnd; /* 16 8 */
	void * tableEnd; /* 24 8 */
	void * tableValidEnd; /* 32 8 */
	void * allocStart; /* 40 8 */
	void * initOnceStart; /* 48 8 */
	BYTE allocFailed; /* 56 1 */
	int workspaceOversizedDuration; /* 60 4 */
	ZSTD_cwksp_alloc_phase_e phase; /* 64 4 */
	ZSTD_cwksp_static_alloc_e isStatic; /* 68 4 */
} ZSTD_cwksp;

// <06FFC6FC> thirdparty/zstd/zstd.c:18451
inline void ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC6C2> thirdparty/zstd/zstd.c:18479
// variable: 1
inline size_t ZSTD_cwksp_align(size_t size, size_t align)
{
	const size_t  mask; // 18480
} /* size: 0, variables: 1 */

// <06FFC6A2> thirdparty/zstd/zstd.c:18497
inline size_t ZSTD_cwksp_alloc_size(size_t size)
{
} /* size: 0 */

// <06FFC675> thirdparty/zstd/zstd.c:18507
inline size_t ZSTD_cwksp_aligned_alloc_size(size_t size, size_t alignment)
{
} /* size: 0 */

// <06FFC655> thirdparty/zstd/zstd.c:18515
inline size_t ZSTD_cwksp_aligned64_alloc_size(size_t size)
{
} /* size: 0 */

// <06FFC635> thirdparty/zstd/zstd.c:18523
// variable: 1
inline size_t ZSTD_cwksp_slack_space_required(void)
{
	const size_t  slackSpace; // 18527
} /* size: 0, variables: 1 */

// <06FFC5EE> thirdparty/zstd/zstd.c:18536
// variables: 2
inline size_t ZSTD_cwksp_bytes_to_align_ptr(void* ptr, const size_t alignBytes)
{
	const size_t  alignBytesMask; // 18537
	const size_t  bytes; // 18538
} /* size: 0, variables: 2 */

// <06FFC5C2> thirdparty/zstd/zstd.c:18548
// variable: 1
inline void* ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws)
{
	char* endPtr; // 18550
} /* size: 0, variables: 1 */

// <06FFC57C> thirdparty/zstd/zstd.c:18564
// variables: 2
inline void* ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws, const size_t bytes)
{
	const void* alloc; // 18566
	const void* bottom; // 18567
} /* size: 0, variables: 2 */

// <06FFC527> thirdparty/zstd/zstd.c:18592
// variables: 3
inline size_t ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws, ZSTD_cwksp_alloc_phase_e phase)
{
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
	}
} /* size: 0 */

// <06FFC4FB> thirdparty/zstd/zstd.c:18625
inline int ZSTD_cwksp_owns_buffer(const ZSTD_cwksp* ws, const void* ptr)
{
} /* size: 0 */

// <06FFC4B5> thirdparty/zstd/zstd.c:18634
// variable: 1
inline void* ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws, size_t bytes, ZSTD_cwksp_alloc_phase_e phase)
{
	void* alloc; // 18636
} /* size: 0, variables: 1 */

// <06FFC489> thirdparty/zstd/zstd.c:18667
inline BYTE* ZSTD_cwksp_reserve_buffer(ZSTD_cwksp* ws, size_t bytes)
{
} /* size: 0 */

// <06FFC443> thirdparty/zstd/zstd.c:18681
// variables: 2
inline void* ZSTD_cwksp_reserve_aligned_init_once(ZSTD_cwksp* ws, size_t bytes)
{
	const size_t  alignedBytes; // 18683
	void* ptr; // 18684
} /* size: 0, variables: 2 */

// <06FFC40A> thirdparty/zstd/zstd.c:18705
// variable: 1
inline void* ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws, size_t bytes)
{
	const void* ptr; // 18707
} /* size: 0, variables: 1 */

// <06FFC3AA> thirdparty/zstd/zstd.c:18719
// variables: 4
inline void* ZSTD_cwksp_reserve_table(ZSTD_cwksp* ws, size_t bytes)
{
	const ZSTD_cwksp_alloc_phase_e  phase; // 18721
	void* alloc; // 18722
	void* end; // 18723
	void* top; // 18724
} /* size: 0, variables: 4 */

// <06FFC357> thirdparty/zstd/zstd.c:18764
// variables: 3
inline void* ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws, size_t bytes)
{
	const size_t  roundedBytes; // 18766
	void* alloc; // 18767
	void* end; // 18768
} /* size: 0, variables: 3 */

// <06FFC33C> thirdparty/zstd/zstd.c:18817
inline void ZSTD_cwksp_mark_tables_dirty(ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC321> thirdparty/zstd/zstd.c:18846
inline void ZSTD_cwksp_mark_tables_clean(ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC306> thirdparty/zstd/zstd.c:18859
inline void ZSTD_cwksp_clean_tables(ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC2EB> thirdparty/zstd/zstd.c:18873
inline void ZSTD_cwksp_clear_tables(ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC2D0> thirdparty/zstd/zstd.c:18896
inline void ZSTD_cwksp_clear(ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC2B1> thirdparty/zstd/zstd.c:18933
inline size_t ZSTD_cwksp_sizeof(const ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC26F> thirdparty/zstd/zstd.c:18947
inline void ZSTD_cwksp_init(ZSTD_cwksp* ws, void* start, size_t size, ZSTD_cwksp_static_alloc_e isStatic)
{
} /* size: 0 */

// <06FFC229> thirdparty/zstd/zstd.c:18962
// variable: 1
inline size_t ZSTD_cwksp_create(ZSTD_cwksp* ws, size_t size, ZSTD_customMem customMem)
{
	void* workspace; // 18963
} /* size: 0, variables: 1 */

// <06FFC1F4> thirdparty/zstd/zstd.c:18970
// variable: 1
inline void ZSTD_cwksp_free(ZSTD_cwksp* ws, ZSTD_customMem customMem)
{
	void* ptr; // 18971
} /* size: 0, variables: 1 */

// <06FFC1CB> thirdparty/zstd/zstd.c:18986
inline void ZSTD_cwksp_move(ZSTD_cwksp* dst, ZSTD_cwksp* src)
{
} /* size: 0 */

// <06FFC1A6> thirdparty/zstd/zstd.c:18991
inline int ZSTD_cwksp_reserve_failed(const ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC187> thirdparty/zstd/zstd.c:19011
inline size_t ZSTD_cwksp_available_space(ZSTD_cwksp* ws)
{
} /* size: 0 */

// <06FFC15B> thirdparty/zstd/zstd.c:19015
inline int ZSTD_cwksp_check_available(ZSTD_cwksp* ws, size_t additionalNeededSpace)
{
} /* size: 0 */

// <06FFC12F> thirdparty/zstd/zstd.c:19019
inline int ZSTD_cwksp_check_too_large(ZSTD_cwksp* ws, size_t additionalNeededSpace)
{
} /* size: 0 */

// <06FFC103> thirdparty/zstd/zstd.c:19024
inline int ZSTD_cwksp_check_wasteful(ZSTD_cwksp* ws, size_t additionalNeededSpace)
{
} /* size: 0 */

// <06FFC0DB> thirdparty/zstd/zstd.c:19029
inline void ZSTD_cwksp_bump_oversized_duration(ZSTD_cwksp* ws, size_t additionalNeededSpace)
{
} /* size: 0 */

// <06F3C34B> thirdparty/zstd/zstd.c:19203
// member variables: 3
// struct size: 24
struct ZSTD_prefixDict_s {
	const void * dict; /* 0 8 */
	size_t dictSize; /* 8 8 */
	ZSTD_dictContentType_e dictContentType; /* 16 4 */
};

// <06F3C39C> thirdparty/zstd/zstd.c:19209
// member variables: 5
// struct size: 40
typedef struct {
	void * dictBuffer; /* 0 8 */
	const void * dict; /* 8 8 */
	size_t dictSize; /* 16 8 */
	ZSTD_dictContentType_e dictContentType; /* 24 4 */
	ZSTD_CDict * cdict; /* 32 8 */
} ZSTD_localDict;

// <06F3C413> thirdparty/zstd/zstd.c:19217
// member variables: 2
// struct size: 2,064
typedef struct {
	HUF_CElt CTable[257]; /* 0 2056 */
	HUF_repeat repeatMode; /* 2056 4 */
} ZSTD_hufCTables_t;

// <06F3C456> thirdparty/zstd/zstd.c:19222
// member variables: 6
// struct size: 3,552
typedef struct {
	FSE_CTable offcodeCTable[193]; /* 0 772 */
	FSE_CTable matchlengthCTable[363]; /* 772 1452 */
	FSE_CTable litlengthCTable[329]; /* 2224 1316 */
	FSE_repeat offcode_repeatMode; /* 3540 4 */
	FSE_repeat matchlength_repeatMode; /* 3544 4 */
	FSE_repeat litlength_repeatMode; /* 3548 4 */
} ZSTD_fseCTables_t;

// <06F3C50F> thirdparty/zstd/zstd.c:19231
// member variables: 2
// struct size: 5,616
typedef struct {
	ZSTD_hufCTables_t huf; /* 0 2064 */
	ZSTD_fseCTables_t fse; /* 2064 3552 */
} ZSTD_entropyCTables_t;

// <06F3C552> thirdparty/zstd/zstd.c:19239
// member variables: 3
// struct size: 8
struct SeqDef_s {
	U32 offBase; /* 0 4 */
	U16 litLength; /* 4 2 */
	U16 mlBase; /* 6 2 */
};

// <06F3C5D6> thirdparty/zstd/zstd.c:19252
// member variables: 11
// struct size: 80
typedef struct {
	SeqDef * sequencesStart; /* 0 8 */
	SeqDef * sequences; /* 8 8 */
	BYTE * litStart; /* 16 8 */
	BYTE * lit; /* 24 8 */
	BYTE * llCode; /* 32 8 */
	BYTE * mlCode; /* 40 8 */
	BYTE * ofCode; /* 48 8 */
	size_t maxNbSeq; /* 56 8 */
	size_t maxNbLit; /* 64 8 */
	ZSTD_longLengthType_e longLengthType; /* 72 4 */
	U32 longLengthPos; /* 76 4 */
} SeqStore_t;

// <06F3C6B2> thirdparty/zstd/zstd.c:19271
// member variables: 2
// struct size: 8
typedef struct {
	U32 litLength; /* 0 4 */
	U32 matchLength; /* 4 4 */
} ZSTD_SequenceLength;

// <06FFC0A1> thirdparty/zstd/zstd.c:19280
// variable: 1
inline ZSTD_SequenceLength ZSTD_getSequenceLength(const SeqStore_t* seqStore, const SeqDef* seq)
{
	ZSTD_SequenceLength seqLen; // 19282
} /* size: 0, variables: 1 */

// <06F3C6F3> thirdparty/zstd/zstd.c:19308
// member variables: 3
// struct size: 144
typedef struct {
	SymbolEncodingType_e hType; /* 0 4 */
	BYTE hufDesBuffer[128]; /* 4 128 */
	size_t hufDesSize; /* 136 8 */
} ZSTD_hufCTablesMetadata_t;

// <06F3C755> thirdparty/zstd/zstd.c:19319
// member variables: 6
// struct size: 168
typedef struct {
	SymbolEncodingType_e llType; /* 0 4 */
	SymbolEncodingType_e ofType; /* 4 4 */
	SymbolEncodingType_e mlType; /* 8 4 */
	BYTE fseTablesBuffer[133]; /* 12 133 */
	size_t fseTablesSize; /* 152 8 */
	size_t lastCountSize; /* 160 8 */
} ZSTD_fseCTablesMetadata_t;

// <06F3C7E8> thirdparty/zstd/zstd.c:19328
// member variables: 2
// struct size: 312
typedef struct {
	ZSTD_hufCTablesMetadata_t hufMetadata; /* 0 144 */
	ZSTD_fseCTablesMetadata_t fseMetadata; /* 144 168 */
} ZSTD_entropyCTablesMetadata_t;

// <06F3C82B> thirdparty/zstd/zstd.c:19348
// member variables: 2
// struct size: 8
typedef struct {
	U32 off; /* 0 4 */
	U32 len; /* 4 4 */
} ZSTD_match_t;

// <06F3C867> thirdparty/zstd/zstd.c:19353
// member variables: 3
// struct size: 12
typedef struct {
	U32 offset; /* 0 4 */
	U32 litLength; /* 4 4 */
	U32 matchLength; /* 8 4 */
} rawSeq;

// <06F3C8B8> thirdparty/zstd/zstd.c:19359
// member variables: 5
// struct size: 40
typedef struct {
	rawSeq * seq; /* 0 8 */
	size_t pos; /* 8 8 */
	size_t posInSequence; /* 16 8 */
	size_t size; /* 24 8 */
	size_t capacity; /* 32 8 */
} RawSeqStore_t;

// <06F3C94B> thirdparty/zstd/zstd.c:19370
// member variables: 5
// struct size: 28
typedef struct {
	int price; /* 0 4 */
	U32 off; /* 4 4 */
	U32 mlen; /* 8 4 */
	U32 litlen; /* 12 4 */
	U32 rep[3]; /* 16 12 */
} ZSTD_optimal_t;

// <06F3C9F9> thirdparty/zstd/zstd.c:19381
// member variables: 17
// struct size: 104
typedef struct {
	unsigned int * litFreq; /* 0 8 */
	unsigned int * litLengthFreq; /* 8 8 */
	unsigned int * matchLengthFreq; /* 16 8 */
	unsigned int * offCodeFreq; /* 24 8 */
	ZSTD_match_t * matchTable; /* 32 8 */
	ZSTD_optimal_t * priceTable; /* 40 8 */
	U32 litSum; /* 48 4 */
	U32 litLengthSum; /* 52 4 */
	U32 matchLengthSum; /* 56 4 */
	U32 offCodeSum; /* 60 4 */
	U32 litSumBasePrice; /* 64 4 */
	U32 litLengthSumBasePrice; /* 68 4 */
	U32 matchLengthSumBasePrice; /* 72 4 */
	U32 offCodeSumBasePrice; /* 76 4 */
	ZSTD_OptPrice_e priceType; /* 80 4 */
	const ZSTD_entropyCTables_t * symbolCosts; /* 88 8 */
	ZSTD_ParamSwitch_e literalCompressionMode; /* 96 4 */
} optState_t;

// <06F3CB51> thirdparty/zstd/zstd.c:19403
// member variables: 2
// struct size: 5,632
typedef struct {
	ZSTD_entropyCTables_t entropy; /* 0 5616 */
	U32 rep[3]; /* 5616 12 */
} ZSTD_compressedBlockState_t;

// <06F3CB94> thirdparty/zstd/zstd.c:19408
// member variables: 6
// struct size: 40
typedef struct {
	const BYTE * nextSrc; /* 0 8 */
	const BYTE * base; /* 8 8 */
	const BYTE * dictBase; /* 16 8 */
	U32 dictLimit; /* 24 4 */
	U32 lowLimit; /* 28 4 */
	U32 nbOverflowCorrections; /* 32 4 */
} ZSTD_window_t;

// <06F3CC32> thirdparty/zstd/zstd.c:19426
// member variables: 20
// struct size: 304
struct ZSTD_MatchState_t {
	ZSTD_window_t window; /* 0 40 */
	U32 loadedDictEnd; /* 40 4 */
	U32 nextToUpdate; /* 44 4 */
	U32 hashLog3; /* 48 4 */
	U32 rowHashLog; /* 52 4 */
	BYTE * tagTable; /* 56 8 */
	U32 hashCache[8]; /* 64 32 */
	U64 hashSalt; /* 96 8 */
	U32 hashSaltEntropy; /* 104 4 */
	U32 * hashTable; /* 112 8 */
	U32 * hashTable3; /* 120 8 */
	U32 * chainTable; /* 128 8 */
	int forceNonContiguous; /* 136 4 */
	int dedicatedDictSearch; /* 140 4 */
	optState_t opt; /* 144 104 */
	const class ZSTD_MatchState_t * dictMatchState; /* 248 8 */
	ZSTD_compressionParameters cParams; /* 256 28 */
	const RawSeqStore_t * ldmSeqStore; /* 288 8 */
	int prefetchCDictTables; /* 296 4 */
	int lazySkipping; /* 300 4 */
};

// <06F3CDB7> thirdparty/zstd/zstd.c:19471
// member variables: 3
// struct size: 320
typedef struct {
	ZSTD_compressedBlockState_t * prevCBlock; /* 0 8 */
	ZSTD_compressedBlockState_t * nextCBlock; /* 8 8 */
	ZSTD_MatchState_t matchState; /* 16 304 */
} ZSTD_blockState_t;

// <06F3CE50> thirdparty/zstd/zstd.c:19482
// member variables: 4
// struct size: 24
typedef struct {
	const BYTE * split; /* 0 8 */
	U32 hash; /* 8 4 */
	U32 checksum; /* 12 4 */
	ldmEntry_t * bucket; /* 16 8 */
} ldmMatchCandidate_t;

// <06F3CEB7> thirdparty/zstd/zstd.c:19491
// member variables: 6
// struct size: 2,112
typedef struct {
	ZSTD_window_t window; /* 0 40 */
	ldmEntry_t * hashTable; /* 40 8 */
	U32 loadedDictEnd; /* 48 4 */
	BYTE * bucketOffsets; /* 56 8 */
	size_t splitIndices[64]; /* 64 512 */
	ldmMatchCandidate_t matchCandidates[64]; /* 576 1536 */
} ldmState_t;

// <06F3CF5A> thirdparty/zstd/zstd.c:19500
// member variables: 6
// struct size: 24
typedef struct {
	ZSTD_ParamSwitch_e enableLdm; /* 0 4 */
	U32 hashLog; /* 4 4 */
	U32 bucketSizeLog; /* 8 4 */
	U32 minMatchLength; /* 12 4 */
	U32 hashRateLog; /* 16 4 */
	U32 windowLog; /* 20 4 */
} ldmParams_t;

// <06F3CFDB> thirdparty/zstd/zstd.c:19509
// member variables: 4
// struct size: 32
typedef struct {
	int collectSequences; /* 0 4 */
	ZSTD_Sequence * seqStart; /* 8 8 */
	size_t seqIndex; /* 16 8 */
	size_t maxSequences; /* 24 8 */
} SeqCollector;

// <06F3AE7F> thirdparty/zstd/zstd.c:19516
// member variables: 30
// struct size: 224
struct ZSTD_CCtx_params_s {
	ZSTD_format_e format; /* 0 4 */
	ZSTD_compressionParameters cParams; /* 4 28 */
	ZSTD_frameParameters fParams; /* 32 12 */
	int compressionLevel; /* 44 4 */
	int forceWindow; /* 48 4 */
	size_t targetCBlockSize; /* 56 8 */
	int srcSizeHint; /* 64 4 */
	ZSTD_dictAttachPref_e attachDictPref; /* 68 4 */
	ZSTD_ParamSwitch_e literalCompressionMode; /* 72 4 */
	int nbWorkers; /* 76 4 */
	size_t jobSize; /* 80 8 */
	int overlapLog; /* 88 4 */
	int rsyncable; /* 92 4 */
	ldmParams_t ldmParams; /* 96 24 */
	int enableDedicatedDictSearch; /* 120 4 */
	ZSTD_bufferMode_e inBufferMode; /* 124 4 */
	ZSTD_bufferMode_e outBufferMode; /* 128 4 */
	ZSTD_SequenceFormat_e blockDelimiters; /* 132 4 */
	int validateSequences; /* 136 4 */
	ZSTD_ParamSwitch_e postBlockSplitter; /* 140 4 */
	int preBlockSplitter_level; /* 144 4 */
	size_t maxBlockSize; /* 152 8 */
	ZSTD_ParamSwitch_e useRowMatchFinder; /* 160 4 */
	int deterministicRefPrefix; /* 164 4 */
	ZSTD_customMem customMem; /* 168 24 */
	ZSTD_ParamSwitch_e prefetchCDictTables; /* 192 4 */
	int enableMatchFinderFallback; /* 196 4 */
	void * extSeqProdState; /* 200 8 */
	ZSTD_sequenceProducer_F extSeqProdFunc; /* 208 8 */
	ZSTD_ParamSwitch_e searchForExternalRepcodes; /* 216 4 */
};

// <06F3D069> thirdparty/zstd/zstd.c:19615
// member variables: 7
// struct size: 1,496
typedef struct {
	SeqStore_t fullSeqStoreChunk; /* 0 80 */
	SeqStore_t firstHalfSeqStore; /* 80 80 */
	SeqStore_t secondHalfSeqStore; /* 160 80 */
	SeqStore_t currSeqStore; /* 240 80 */
	SeqStore_t nextSeqStore; /* 320 80 */
	U32 partitions[196]; /* 400 784 */
	ZSTD_entropyCTablesMetadata_t entropyMetadata; /* 1184 312 */
} ZSTD_blockSplitCtx;

// <06F3A20B> thirdparty/zstd/zstd.c:19626
// member variables: 50
// struct size: 5,280
struct ZSTD_CCtx_s {
	ZSTD_compressionStage_e stage; /* 0 4 */
	int cParamsChanged; /* 4 4 */
	int bmi2; /* 8 4 */
	ZSTD_CCtx_params requestedParams; /* 16 224 */
	ZSTD_CCtx_params appliedParams; /* 240 224 */
	ZSTD_CCtx_params simpleApiParams; /* 464 224 */
	U32 dictID; /* 688 4 */
	size_t dictContentSize; /* 696 8 */
	ZSTD_cwksp workspace; /* 704 72 */
	size_t blockSizeMax; /* 776 8 */
	long long unsigned int pledgedSrcSizePlusOne; /* 784 8 */
	long long unsigned int consumedSrcSize; /* 792 8 */
	long long unsigned int producedCSize; /* 800 8 */
	XXH_NAMESPACEXXH64_state_t xxhState; /* 808 88 */
	ZSTD_customMem customMem; /* 896 24 */
	ZSTD_threadPool * pool; /* 920 8 */
	size_t staticSize; /* 928 8 */
	SeqCollector seqCollector; /* 936 32 */
	int isFirstBlock; /* 968 4 */
	int initialized; /* 972 4 */
	SeqStore_t seqStore; /* 976 80 */
	ldmState_t ldmState; /* 1056 2112 */
	rawSeq * ldmSequences; /* 3168 8 */
	size_t maxNbLdmSequences; /* 3176 8 */
	RawSeqStore_t externSeqStore; /* 3184 40 */
	ZSTD_blockState_t blockState; /* 3224 320 */
	void * tmpWorkspace; /* 3544 8 */
	size_t tmpWkspSize; /* 3552 8 */
	ZSTD_buffered_policy_e bufferedPolicy; /* 3560 4 */
	char * inBuff; /* 3568 8 */
	size_t inBuffSize; /* 3576 8 */
	size_t inToCompress; /* 3584 8 */
	size_t inBuffPos; /* 3592 8 */
	size_t inBuffTarget; /* 3600 8 */
	char * outBuff; /* 3608 8 */
	size_t outBuffSize; /* 3616 8 */
	size_t outBuffContentSize; /* 3624 8 */
	size_t outBuffFlushedSize; /* 3632 8 */
	ZSTD_cStreamStage streamStage; /* 3640 4 */
	U32 frameEnded; /* 3644 4 */
	ZSTD_inBuffer expectedInBuffer; /* 3648 24 */
	size_t stableIn_notConsumed; /* 3672 8 */
	size_t expectedOutBufferSize; /* 3680 8 */
	ZSTD_localDict localDict; /* 3688 40 */
	const ZSTD_CDict * cdict; /* 3728 8 */
	ZSTD_prefixDict prefixDict; /* 3736 24 */
	ZSTDMT_CCtx * mtctx; /* 3760 8 */
	ZSTD_blockSplitCtx blockSplitCtx; /* 3768 1496 */
	ZSTD_Sequence * extSeqBuf; /* 5264 8 */
	size_t extSeqBufCapacity; /* 5272 8 */
};

// <06FFC048> thirdparty/zstd/zstd.c:19738
// variables: 2
inline U32 ZSTD_LLcode(U32 litLength)
{
	const BYTE   LL_Code; // 19740
	const U32  LL_deltaCode; // 19748
} /* size: 0, variables: 2 */

// <06FFBFEF> thirdparty/zstd/zstd.c:19755
// variables: 2
inline U32 ZSTD_MLcode(U32 mlBase)
{
	const BYTE   ML_Code; // 19757
	const U32  ML_deltaCode; // 19765
} /* size: 0, variables: 2 */

// <06FFBFB5> thirdparty/zstd/zstd.c:19772
// variable: 1
inline int ZSTD_cParam_withinBounds(ZSTD_cParameter cParam, int value)
{
	const ZSTD_bounds  bounds; // 19774
} /* size: 0, variables: 1 */

// <06FFBF6E> thirdparty/zstd/zstd.c:19785
inline const BYTE* ZSTD_selectAddr(U32 index, U32 lowLimit, const BYTE* candidate, const BYTE* backup)
{
} /* size: 0 */

// <06FFBF0D> thirdparty/zstd/zstd.c:19804
// variable: 1
inline size_t ZSTD_noCompressBlock(void* dst, size_t dstCapacity, const void* src, size_t srcSize, U32 lastBlock)
{
	const U32  cBlockHeader24; // 19806
} /* size: 0, variables: 1 */

// <06FFBEA0> thirdparty/zstd/zstd.c:19816
// variables: 2
inline size_t ZSTD_rleCompressBlock(void* dst, size_t dstCapacity, BYTE src, size_t srcSize, U32 lastBlock)
{
	const BYTE* op; // 19818
	const U32  cBlockHeader; // 19819
} /* size: 0, variables: 2 */

// <06FFBE66> thirdparty/zstd/zstd.c:19831
// variable: 1
inline size_t ZSTD_minGain(size_t srcSize, ZSTD_strategy strat)
{
	const U32  minlog; // 19833
} /* size: 0, variables: 1 */

// <06FFBE46> thirdparty/zstd/zstd.c:19839
inline int ZSTD_literalsCompressionIsDisabled(const ZSTD_CCtx_params* cctxParams)
{
} /* size: 0 */

// <06FFBB93> thirdparty/zstd/zstd.c:19860
// function calls: 10
void ZSTD_safecopyLiterals(BYTE* op, const BYTE* ip, const BYTE* iend, const BYTE* ilimit_w)
{
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19864
} /* size: 129, inline expansions: 10 (128 bytes) */

// <06FFBB41> thirdparty/zstd/zstd.c:19889
// variable: 1
inline void ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr, size_t litLength, U32 offBase, size_t matchLength)
{
	{
		const size_t  mlBase; // 19911
	}
} /* size: 0 */

// <06FFBACA> thirdparty/zstd/zstd.c:19930
// variables: 2
inline void ZSTD_storeSeq(SeqStore_t* seqStorePtr, size_t litLength, const BYTE* literals, const BYTE* litLimit, U32 offBase, size_t matchLength)
{
	const BYTE* litLimit_w; // 19935
	const BYTE* litEnd; // 19936
} /* size: 0, variables: 2 */

// <06FFBA76> thirdparty/zstd/zstd.c:19972
// variables: 2
inline void ZSTD_updateRep(U32* rep, const U32 offBase, const U32 ll0)
{
	{
		const U32  repCode; // 19979
		{
			const U32  currentOffset; // 19981
		}
	}
} /* size: 0 */

// <06F3D249> thirdparty/zstd/zstd.c:19991
// member variable: 1
// struct size: 12
struct repcodes_s {
	U32 rep[3]; /* 0 12 */
};

// <06FFBA2F> thirdparty/zstd/zstd.c:19996
// variable: 1
inline Repcodes_t ZSTD_newRep(const U32* rep, const U32 offBase, const U32 ll0)
{
	Repcodes_t newReps; // 19998
} /* size: 0, variables: 1 */

// <06FFB9B9> thirdparty/zstd/zstd.c:20008
// variables: 4
inline size_t ZSTD_count(const BYTE* pIn, const BYTE* pMatch, const BYTE* pInLimit)
{
	const BYTE* pStart; // 20010
	const BYTE* pInLoopLimit; // 20011
	{
		const size_t  diff; // 20014
	}
	{
		const size_t  diff; // 20018
	}
} /* size: 0, variables: 2 */

// <0703F81A> thirdparty/zstd/zstd.c:20034
// variables: 6
// function calls: 26
size_t ZSTD_count_2segments(const BYTE* ip, const BYTE* match, const BYTE* iEnd, const BYTE* mEnd, const BYTE* iStart)
{
	const BYTE* vEnd; // 20037
	const size_t  matchLength; // 20038
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 20038
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 20045
} /* size: 530, variables: 2, inline expansions: 10 (457 bytes) */

// <06FFB94C> thirdparty/zstd/zstd.c:20034
// variables: 2
inline size_t ZSTD_count_2segments(const BYTE* ip, const BYTE* match, const BYTE* iEnd, const BYTE* mEnd, const BYTE* iStart)
{
	const BYTE* vEnd; // 20037
	const size_t  matchLength; // 20038
} /* size: 0, variables: 2 */

// <06FFB918> thirdparty/zstd/zstd.c:20053
U32 ZSTD_hash3(U32 u, U32 h, U32 s)
{
} /* size: 0 */

// <06FFB8ED> thirdparty/zstd/zstd.c:20054
inline size_t ZSTD_hash3Ptr(const void* ptr, U32 h)
{
} /* size: 0 */

// <06FFB8B9> thirdparty/zstd/zstd.c:20058
U32 ZSTD_hash4(U32 u, U32 h, U32 s)
{
} /* size: 0 */

// <06FFB88E> thirdparty/zstd/zstd.c:20059
size_t ZSTD_hash4Ptr(const void* ptr, U32 h)
{
} /* size: 0 */

// <06FFB858> thirdparty/zstd/zstd.c:20060
size_t ZSTD_hash4PtrS(const void* ptr, U32 h, U32 s)
{
} /* size: 0 */

// <06FFB824> thirdparty/zstd/zstd.c:20063
size_t ZSTD_hash5(U64 u, U32 h, U64 s)
{
} /* size: 0 */

// <06FFB7FB> thirdparty/zstd/zstd.c:20064
size_t ZSTD_hash5Ptr(const void* p, U32 h)
{
} /* size: 0 */

// <06FFB7C7> thirdparty/zstd/zstd.c:20065
size_t ZSTD_hash5PtrS(const void* p, U32 h, U64 s)
{
} /* size: 0 */

// <06FFB793> thirdparty/zstd/zstd.c:20068
size_t ZSTD_hash6(U64 u, U32 h, U64 s)
{
} /* size: 0 */

// <06FFB76A> thirdparty/zstd/zstd.c:20069
size_t ZSTD_hash6Ptr(const void* p, U32 h)
{
} /* size: 0 */

// <06FFB736> thirdparty/zstd/zstd.c:20070
size_t ZSTD_hash6PtrS(const void* p, U32 h, U64 s)
{
} /* size: 0 */

// <06FFB702> thirdparty/zstd/zstd.c:20073
size_t ZSTD_hash7(U64 u, U32 h, U64 s)
{
} /* size: 0 */

// <06FFB6D9> thirdparty/zstd/zstd.c:20074
size_t ZSTD_hash7Ptr(const void* p, U32 h)
{
} /* size: 0 */

// <06FFB6A5> thirdparty/zstd/zstd.c:20075
size_t ZSTD_hash7PtrS(const void* p, U32 h, U64 s)
{
} /* size: 0 */

// <06FFB671> thirdparty/zstd/zstd.c:20078
size_t ZSTD_hash8(U64 u, U32 h, U64 s)
{
} /* size: 0 */

// <06FFB648> thirdparty/zstd/zstd.c:20079
size_t ZSTD_hash8Ptr(const void* p, U32 h)
{
} /* size: 0 */

// <06FFB5DC> thirdparty/zstd/zstd.c:20084
inline size_t ZSTD_hashPtr(const void* p, U32 hBits, U32 mls)
{
} /* size: 0 */

// <06FFB597> thirdparty/zstd/zstd.c:20102
inline size_t ZSTD_hashPtrSalted(const void* p, U32 hBits, U32 mls, const U64 hashSalt)
{
} /* size: 0 */

// <06FFB55D> thirdparty/zstd/zstd.c:20122
// variable: 1
U64 ZSTD_ipow(U64 base, U64 exponent)
{
	U64 power; // 20124
} /* size: 0, variables: 1 */

// <06FFB509> thirdparty/zstd/zstd.c:20138
// variables: 2
U64 ZSTD_rollingHash_append(U64 hash, const void* buf, size_t size)
{
	const BYTE* istart; // 20140
	size_t pos; // 20141
} /* size: 0, variables: 2 */

// <06FFB4DC> thirdparty/zstd/zstd.c:20152
inline U64 ZSTD_rollingHash_compute(const void* buf, size_t size)
{
} /* size: 0 */

// <06FFB4BC> thirdparty/zstd/zstd.c:20161
inline U64 ZSTD_rollingHash_primePower(U32 length)
{
} /* size: 0 */

// <06FFB475> thirdparty/zstd/zstd.c:20169
inline U64 ZSTD_rollingHash_rotate(U64 hash, BYTE toRemove, BYTE toAdd, U64 primePower)
{
} /* size: 0 */

// <06FFB43F> thirdparty/zstd/zstd.c:20195
// variables: 2
inline void ZSTD_window_clear(ZSTD_window_t* window)
{
	const size_t  endT; // 20197
	const U32  end; // 20198
} /* size: 0, variables: 2 */

// <06FFB41F> thirdparty/zstd/zstd.c:20215
inline U32 ZSTD_window_hasExtDict(const ZSTD_window_t window)
{
} /* size: 0 */

// <06FFB400> thirdparty/zstd/zstd.c:20225
inline ZSTD_dictMode_e ZSTD_matchState_dictMode(const ZSTD_MatchState_t* ms)
{
} /* size: 0 */

// <06FFB392> thirdparty/zstd/zstd.c:20286
// variable: 1
inline U32 ZSTD_window_needOverflowCorrection(const ZSTD_window_t window, U32 cycleLog, U32 maxDist, U32 loadedDictEnd, const void* src, const void* srcEnd)
{
	const U32  curr; // 20293
} /* size: 0, variables: 1 */

// <06FFB2F0> thirdparty/zstd/zstd.c:20313
// variables: 7
inline U32 ZSTD_window_correctOverflow(ZSTD_window_t* window, U32 cycleLog, U32 maxDist, const void* src)
{
	const U32  cycleSize; // 20335
	const U32  cycleMask; // 20336
	const U32  curr; // 20337
	const U32  currentCycle; // 20338
	const U32  currentCycleCorrection; // 20340
	const U32  newCurrent; // 20343
	const U32  correction; // 20346
} /* size: 0, variables: 7 */

// <06FFB277> thirdparty/zstd/zstd.c:20410
// variables: 3
inline void ZSTD_window_enforceMaxDist(ZSTD_window_t* window, const void* blockEnd, U32 maxDist, U32* loadedDictEndPtr, const ZSTD_MatchState_t ** dictMatchStatePtr)
{
	const U32  blockEndIdx; // 20416
	const U32  loadedDictEnd; // 20417
	{
		const U32  newLowLimit; // 20435
	}
} /* size: 0, variables: 2 */

// <06FFB1FF> thirdparty/zstd/zstd.c:20455
// variables: 2
inline void ZSTD_checkDictValidity(const ZSTD_window_t* window, const void* blockEnd, U32 maxDist, U32* loadedDictEndPtr, const ZSTD_MatchState_t ** dictMatchStatePtr)
{
	{
		const U32  blockEndIdx; // 20463
		const U32  loadedDictEnd; // 20464
	}
} /* size: 0 */

// <06FFB1E3> thirdparty/zstd/zstd.c:20488
inline void ZSTD_window_init(ZSTD_window_t* window)
{
} /* size: 0 */

// <06FFB14E> thirdparty/zstd/zstd.c:20508
// variables: 5
inline U32 ZSTD_window_update(ZSTD_window_t* window, const void* src, size_t srcSize, int forceNonContiguous)
{
	const BYTE* ip; // 20512
	U32 contiguous; // 20513
	{
		const size_t  distanceFromBase; // 20522
	}
	{
		const size_t  highInputIdx; // 20537
		const U32  lowLimitMax; // 20538
	}
} /* size: 0, variables: 2 */

// <06FFB0D4> thirdparty/zstd/zstd.c:20549
// variables: 5
inline U32 ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms, U32 curr, unsigned int windowLog)
{
	const U32  maxDistance; // 20551
	const U32  lowestValid; // 20552
	const U32  withinWindow; // 20553
	const U32  isDictionary; // 20554
	const U32  matchLowest; // 20559
} /* size: 0, variables: 5 */

// <06FFB05A> thirdparty/zstd/zstd.c:20566
// variables: 5
inline U32 ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms, U32 curr, unsigned int windowLog)
{
	const U32  maxDistance; // 20568
	const U32  lowestValid; // 20569
	const U32  withinWindow; // 20570
	const U32  isDictionary; // 20571
	const U32  matchLowest; // 20575
} /* size: 0, variables: 5 */

// <06FFB02D> thirdparty/zstd/zstd.c:20583
inline int ZSTD_index_overlap_check(const U32 prefixLowestIndex, const U32 repIndex)
{
} /* size: 0 */

// <06FFAFDD> thirdparty/zstd/zstd.c:20641
// variables: 2
inline void ZSTD_writeTaggedIndex(const U32* hashTable, size_t hashAndTag, U32 index)
{
	const size_t  hash; // 20642
	const U32  tag; // 20643
} /* size: 0, variables: 2 */

// <06FFAF96> thirdparty/zstd/zstd.c:20650
// variables: 2
inline int ZSTD_comparePackedTags(size_t packedTag1, size_t packedTag2)
{
	const U32  tag1; // 20651
	const U32  tag2; // 20652
} /* size: 0, variables: 2 */

// <06F3D2F0> thirdparty/zstd/zstd.c:20671
// member variables: 3
// struct size: 16
typedef struct {
	U32 idx; /* 0 4 */
	U32 posInSequence; /* 4 4 */
	size_t posInSrc; /* 8 8 */
} ZSTD_SequencePosition;

// <06F3D33C> thirdparty/zstd/zstd.c:20682
// member variables: 3
// struct size: 24
typedef struct {
	size_t nbSequences; /* 0 8 */
	size_t blockSize; /* 8 8 */
	size_t litSize; /* 16 8 */
} BlockSummary;

// <06FFAF76> thirdparty/zstd/zstd.c:20767
inline int ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params)
{
} /* size: 0 */

// <06FFADCF> thirdparty/zstd/zstd.c:20842
// variables: 2
// function calls: 4
size_t ZSTD_noCompressLiterals(void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	const BYTE* ostart; // 20844
	const U32  flSize; // 20845
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 20860
	MEM_write16(void* memPtr,
			U16 value);  // 1279
	MEM_writeLE16(void* memPtr,
			U16 val);  // 20857
} /* size: 168, variables: 2, inline expansions: 4 (28 bytes) */

// <06FFAD8A> thirdparty/zstd/zstd.c:20871
// variables: 2
int allBytesIdentical(const void* src, size_t srcSize)
{
	{
		const BYTE  b; // 20875
		size_t p; // 20876
	}
} /* size: 0 */

// <06FFAC03> thirdparty/zstd/zstd.c:20884
// variables: 2
// function calls: 4
size_t ZSTD_compressRleLiteralsBlock(void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	const BYTE* ostart; // 20886
	const U32  flSize; // 20887
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 20901
	MEM_write16(void* memPtr,
			U16 value);  // 1279
	MEM_writeLE16(void* memPtr,
			U16 val);  // 20898
} /* size: 106, variables: 2, inline expansions: 4 (20 bytes) */

// <06FFABBA> thirdparty/zstd/zstd.c:20918
// variables: 2
size_t ZSTD_minLiteralsToCompress(ZSTD_strategy strategy, HUF_repeat huf_repeat)
{
	{
		const int  shift; // 20925
		const size_t  mintc; // 20926
	}
} /* size: 0 */

// <06FFA530> thirdparty/zstd/zstd.c:20932
// variables: 16
// function calls: 10
size_t ZSTD_compressLiterals(void* dst, size_t dstCapacity, const void* src, size_t srcSize, void* entropyWorkspace, size_t entropyWorkspaceSize, const ZSTD_hufCTables_t* prevHuf, ZSTD_hufCTables_t* nextHuf, ZSTD_strategy strategy, int disableLiteralCompression, int suspectUncompressible, int bmi2)
{
	const size_t  lhSize; // 20943
	const BYTE* ostart; // 20944
	U32 singleStream; // 20945
	SymbolEncodingType_e hType; // 20946
	size_t cLitSize; // 20947
	{
		HUF_repeat repeat; // 20965
		const int  flags; // 20966
		huf_compress_f huf_compress; // 20973
	}
	{
		const size_t  minGain; // 20990
		ZSTD_minGain(size_t srcSize,
				ZSTD_strategy strat);  // 20990
	}
	{
		const U32  lhc; // 21016
		MEM_write16(void* memPtr,
				U16 value);  // 1279
		MEM_writeLE16(void* memPtr,
				U16 val);  // 1294
		MEM_writeLE24(void* memPtr,
				U32 val);  // 21017
	}
	{
		const U32  lhc; // 21022
		MEM_write32(void* memPtr,
				U32 value);  // 1309
		MEM_writeLE32(void* memPtr,
				U32 val32);  // 21023
	}
	{
		const U32  lhc; // 21028
		MEM_write32(void* memPtr,
				U32 value);  // 1309
		MEM_writeLE32(void* memPtr,
				U32 val32);  // 21029
	}
	{
		const int  shift; // 20925
		const size_t  mintc; // 20926
	}
	ZSTD_minLiteralsToCompress(ZSTD_strategy strategy,
					HUF_repeat huf_repeat);  // 20961
	{
		const BYTE  b; // 20875
		size_t p; // 20876
	}
	allBytesIdentical(const void* src,
				size_t srcSize);  // 21001
} /* size: 875, variables: 5, inline expansions: 2 (72 bytes) */

// <06FFA4E9> thirdparty/zstd/zstd.c:21142
// variables: 3
unsigned int ZSTD_getFSEMaxSymbolValue(const FSE_CTable* ctable)
{
	const void* ptr; // 21143
	const U16* u16ptr; // 21144
	const U32  maxSymbolValue; // 21145
} /* size: 0, variables: 3 */

// <06FFA4C9> thirdparty/zstd/zstd.c:21153
unsigned int ZSTD_useLowProbCount(const size_t nbSeq)
{
} /* size: 0 */

// <06FFA0E5> thirdparty/zstd/zstd.c:21166
// variables: 4
// function calls: 10
size_t ZSTD_NCountCost(const unsigned int* count, const unsigned int max, const size_t nbSeq, const unsigned int FSELog)
{
	BYTE wksp; // 21169
	S16 norm; // 21170
	const U32  tableLog; // 21171
	{
		const size_t  err_code; // 21172
		ZSTD_useLowProbCount(const size_t nbSeq); // 21172
	}
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 16287
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 16288
	FSE_minTableLog(size_t srcSize,
			unsigned int maxSymbolValue);  // 16298
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 16296
	FSE_optimalTableLog_internal(unsigned int maxTableLog,
					size_t srcSize,
					unsigned int maxSymbolValue,
					unsigned int minus);  // 16310
	FSE_optimalTableLog(unsigned int maxTableLog,
				size_t srcSize,
				unsigned int maxSymbolValue);  // 21171
} /* size: 205, variables: 3, inline expansions: 9 (149 bytes) */

// <06FFA084> thirdparty/zstd/zstd.c:21180
// variables: 3
size_t ZSTD_entropyCost(const unsigned int* count, const unsigned int max, const size_t total)
{
	unsigned int cost; // 21182
	unsigned int s; // 21183
	{
		unsigned int norm; // 21187
	}
} /* size: 0, variables: 2 */

// <06FF9E13> thirdparty/zstd/zstd.c:21200
// variables: 11
// function calls: 5
size_t ZSTD_fseBitCost(const FSE_CTable* ctable, const unsigned int* count, const unsigned int max)
{
	const unsigned int  kAccuracyLog; // 21205
	size_t cost; // 21206
	unsigned int s; // 21207
	FSE_CState_t cstate; // 21208
	{
		const unsigned int  tableLog; // 21216
		const unsigned int  badCost; // 21217
		const unsigned int  bitCost; // 21218
		{
			const U32  tableSize; // 2823
			const U32  deltaFromThreshold; // 2824
			const U32  normalizedDeltaFromThreshold; // 2825
			const U32  bitMultiplier; // 2826
		}
		FSE_bitCost(const void* symbolTTPtr,
				U32 tableLog,
				U32 symbolValue,
				U32 accuracyLog);  // 21218
	}
	MEM_read16(const void* ptr); // 2763
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 21209
	MEM_read16(const void* ptr); // 21145
	ZSTD_getFSEMaxSymbolValue(const FSE_CTable* ctable); // 21210
} /* size: 198, variables: 4, inline expansions: 4 (34 bytes) */

// <06FF9D86> thirdparty/zstd/zstd.c:21235
// variables: 5
size_t ZSTD_crossEntropyCost(const short int* norm, unsigned int accuracyLog, const unsigned int* count, const unsigned int max)
{
	const unsigned int  shift; // 21238
	size_t cost; // 21239
	unsigned int s; // 21240
	{
		const unsigned int  normAcc; // 21243
		const unsigned int  norm256; // 21244
	}
} /* size: 0, variables: 3 */

// <06FF9A6F> thirdparty/zstd/zstd.c:21253
// variables: 11
// function calls: 2
SymbolEncodingType_e ZSTD_selectEncodingType(FSE_repeat* repeatMode, const unsigned int* count, const unsigned int max, const size_t mostFrequent, size_t nbSeq, const unsigned int FSELog, const FSE_CTable* prevCTable, const short int* defaultNorm, U32 defaultNormLog, const ZSTD_DefaultPolicy_e isDefaultAllowed, const ZSTD_strategy strategy)
{
	{
		const size_t  staticFse_nbSeq_max; // 21277
		const size_t  mult; // 21278
		const size_t  baseLog; // 21279
		const size_t  dynamicFse_nbSeq_min; // 21280
	}
	{
		const size_t  basicCost; // 21302
		const size_t  repeatCost; // 21303
		const size_t  NCountCost; // 21304
		const size_t  compressedCost; // 21305
		{
			const unsigned int  normAcc; // 21243
			const unsigned int  norm256; // 21244
		}
		ZSTD_crossEntropyCost(const short int* norm,
					unsigned int accuracyLog,
					const unsigned int* count,
					const unsigned int  max);  // 21302
		{
			unsigned int norm; // 21187
		}
		ZSTD_entropyCost(const unsigned int* count,
				const unsigned int  max,
				const size_t  total);  // 21305
	}
} /* size: 535 */

// <06F3D3EB> thirdparty/zstd/zstd.c:21333
// member variables: 2
// struct size: 1,248
typedef struct {
	S16 norm[53]; /* 0 106 */
	U32 wksp[285]; /* 108 1140 */
} ZSTD_BuildCTableWksp;

// <06FF9401> thirdparty/zstd/zstd.c:21339
// variables: 11
// function calls: 11
size_t ZSTD_buildCTable(void* dst, size_t dstCapacity, FSE_CTable* nextCTable, U32 FSELog, SymbolEncodingType_e type, unsigned int* count, U32 max, const BYTE* codeTable, size_t nbSeq, const S16* defaultNorm, U32 defaultNormLog, U32 defaultMax, const FSE_CTable* prevCTable, size_t prevCTableSize, void* entropyWorkspace, size_t entropyWorkspaceSize)
{
	BYTE* op; // 21347
	const BYTE* oend; // 21348
	{
		const size_t  err_code; // 21353
		FSE_buildCTable_rle(FSE_CTable* ct,
					BYTE symbolValue);  // 21353
	}
	{
		const size_t  err_code; // 21361
	}
	{
		ZSTD_BuildCTableWksp* wksp; // 21364
		size_t nbSeq_1; // 21365
		const U32  tableLog; // 21366
		{
			const size_t  err_code; // 21374
			ZSTD_useLowProbCount(const size_t nbSeq); // 21374
		}
		{
			const size_t  NCountSize; // 21376
			{
				const size_t  err_code; // 21377
			}
			{
				const size_t  err_code; // 21378
			}
		}
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 16287
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 16288
		FSE_minTableLog(size_t srcSize,
				unsigned int maxSymbolValue);  // 16298
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 16296
		FSE_optimalTableLog_internal(unsigned int maxTableLog,
						size_t srcSize,
						unsigned int maxSymbolValue,
						unsigned int minus);  // 16310
		FSE_optimalTableLog(unsigned int maxTableLog,
					size_t srcSize,
					unsigned int maxSymbolValue);  // 21366
	}
} /* size: 478, variables: 2 */

// <06FF928C> thirdparty/zstd/zstd.c:21387
// variables: 15
inline size_t ZSTD_encodeSequences_body(void* dst, size_t dstCapacity, const FSE_CTable* CTable_MatchLength, const BYTE* mlCodeTable, const FSE_CTable* CTable_OffsetBits, const BYTE* ofCodeTable, const FSE_CTable* CTable_LitLength, const BYTE* llCodeTable, const SeqDef* sequences, size_t nbSeq, int longOffsets)
{
	BIT_CStream_t blockStream; // 21394
	FSE_CState_t stateMatchLength; // 21395
	FSE_CState_t stateOffsetBits; // 21396
	FSE_CState_t stateLitLength; // 21397
	{
		const U32  ofBits; // 21415
		const unsigned int  extraBits; // 21416
	}
	{
		size_t n; // 21428
		{
			const BYTE  llCode; // 21430
			const BYTE  ofCode; // 21431
			const BYTE  mlCode; // 21432
			const U32  llBits; // 21433
			const U32  ofBits; // 21434
			const U32  mlBits; // 21435
			{
				const unsigned int  extraBits; // 21453
			}
		}
	}
	{
		const size_t  streamSize; // 21474
	}
} /* size: 0, variables: 4 */

// <06FF7AE2> thirdparty/zstd/zstd.c:21481
// variables: 20
// function calls: 91
size_t ZSTD_encodeSequences_default(void* dst, size_t dstCapacity, const FSE_CTable* CTable_MatchLength, const BYTE* mlCodeTable, const FSE_CTable* CTable_OffsetBits, const BYTE* ofCodeTable, const FSE_CTable* CTable_LitLength, const BYTE* llCodeTable, const SeqDef* sequences, size_t nbSeq, int longOffsets)
{
	BIT_initCStream(BIT_CStream_t* bitC,
			void* startPtr,
			size_t dstCapacity);  // 21399
	MEM_read16(const void* ptr); // 2763
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 21407
	MEM_read16(const void* ptr); // 2763
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 21408
	MEM_read16(const void* ptr); // 2763
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 21409
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 21412
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 21410
	{
		const U32  ofBits; // 21415
		const unsigned int  extraBits; // 21416
		BIT_getLowerBits(BitContainerType bitContainer,
				const U32  nbBits);  // 2291
		BIT_addBits(BIT_CStream_t* bitC,
				BitContainerType value,
				unsigned int nbBits);  // 21421
		BIT_getLowerBits(BitContainerType bitContainer,
				const U32  nbBits);  // 2291
		BIT_addBits(BIT_CStream_t* bitC,
				BitContainerType value,
				unsigned int nbBits);  // 21418
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 2331
		BIT_flushBits(BIT_CStream_t* bitC); // 21419
	}
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 21424
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 21426
	{
		size_t n; // 21428
		{
			const BYTE  llCode; // 21430
			const BYTE  ofCode; // 21431
			const BYTE  mlCode; // 21432
			const U32  llBits; // 21433
			const U32  ofBits; // 21434
			const U32  mlBits; // 21435
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 21461
			{
				const unsigned int  extraBits; // 21453
				BIT_getLowerBits(BitContainerType bitContainer,
						const U32  nbBits);  // 2291
				BIT_addBits(BIT_CStream_t* bitC,
						BitContainerType value,
						unsigned int nbBits);  // 21458
				BIT_getLowerBits(BitContainerType bitContainer,
						const U32  nbBits);  // 2291
				BIT_addBits(BIT_CStream_t* bitC,
						BitContainerType value,
						unsigned int nbBits);  // 21455
				MEM_write64(void* memPtr,
						U64 value);  // 1325
				MEM_writeLE64(void* memPtr,
						U64 val64);  // 1343
				MEM_writeLEST(void* memPtr,
						size_t val);  // 2331
				BIT_flushBits(BIT_CStream_t* bitC); // 21456
			}
			MEM_write64(void* memPtr,
					U64 value);  // 1325
			MEM_writeLE64(void* memPtr,
					U64 val64);  // 1343
			MEM_writeLEST(void* memPtr,
					size_t val);  // 2331
			BIT_flushBits(BIT_CStream_t* bitC); // 21463
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 2790
			FSE_encodeSymbol(BIT_CStream_t* bitC,
					FSE_CState_t* statePtr,
					unsigned int symbol);  // 21442
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 2790
			FSE_encodeSymbol(BIT_CStream_t* bitC,
					FSE_CState_t* statePtr,
					unsigned int symbol);  // 21443
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 2790
			FSE_encodeSymbol(BIT_CStream_t* bitC,
					FSE_CState_t* statePtr,
					unsigned int symbol);  // 21445
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 21448
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 21450
			MEM_write64(void* memPtr,
					U64 value);  // 1325
			MEM_writeLE64(void* memPtr,
					U64 val64);  // 1343
			MEM_writeLEST(void* memPtr,
					size_t val);  // 2331
			BIT_flushBits(BIT_CStream_t* bitC); // 21451
			MEM_write64(void* memPtr,
					U64 value);  // 1325
			MEM_writeLE64(void* memPtr,
					U64 val64);  // 1343
			MEM_writeLEST(void* memPtr,
					size_t val);  // 2331
			BIT_flushBits(BIT_CStream_t* bitC); // 21447
		}
	}
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2796
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2797
	FSE_flushCState(BIT_CStream_t* bitC,
			const FSE_CState_t* statePtr);  // 21468
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2796
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2797
	FSE_flushCState(BIT_CStream_t* bitC,
			const FSE_CState_t* statePtr);  // 21470
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2796
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2797
	FSE_flushCState(BIT_CStream_t* bitC,
			const FSE_CState_t* statePtr);  // 21472
	{
		const size_t  streamSize; // 21474
		BIT_addBitsFast(BIT_CStream_t* bitC,
				BitContainerType value,
				unsigned int nbBits);  // 2343
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 2331
		BIT_flushBits(BIT_CStream_t* bitC); // 2344
		BIT_closeCStream(BIT_CStream_t* bitC); // 21474
	}
	ZSTD_encodeSequences_body(void* dst,
					size_t dstCapacity,
					const FSE_CTable* CTable_MatchLength,
					const BYTE* mlCodeTable,
					const FSE_CTable* CTable_OffsetBits,
					const BYTE* ofCodeTable,
					const FSE_CTable* CTable_LitLength,
					const BYTE* llCodeTable,
					const SeqDef* sequences,
					size_t nbSeq,
					int longOffsets);  // 21488
} /* size: 1736, inline expansions: 42 (2748 bytes) */

// <06FF6328> thirdparty/zstd/zstd.c:21499
// variables: 20
// function calls: 91
size_t ZSTD_encodeSequences_bmi2(void* dst, size_t dstCapacity, const FSE_CTable* CTable_MatchLength, const BYTE* mlCodeTable, const FSE_CTable* CTable_OffsetBits, const BYTE* ofCodeTable, const FSE_CTable* CTable_LitLength, const BYTE* llCodeTable, const SeqDef* sequences, size_t nbSeq, int longOffsets)
{
	BIT_initCStream(BIT_CStream_t* bitC,
			void* startPtr,
			size_t dstCapacity);  // 21399
	MEM_read16(const void* ptr); // 2763
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 21407
	MEM_read16(const void* ptr); // 2763
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 21408
	MEM_read16(const void* ptr); // 2763
	FSE_initCState(FSE_CState_t* statePtr,
			const FSE_CTable* ct);  // 2776
	{
		const FSE_symbolCompressionTransform  symbolTT; // 2777
		const U16* stateTable; // 2778
		U32 nbBitsOut; // 2779
	}
	FSE_initCState2(FSE_CState_t* statePtr,
			const FSE_CTable* ct,
			U32 symbol);  // 21409
	{
		const U32  ofBits; // 21415
		const unsigned int  extraBits; // 21416
		BIT_getLowerBits(BitContainerType bitContainer,
				const U32  nbBits);  // 2291
		BIT_addBits(BIT_CStream_t* bitC,
				BitContainerType value,
				unsigned int nbBits);  // 21421
		BIT_getLowerBits(BitContainerType bitContainer,
				const U32  nbBits);  // 2291
		BIT_addBits(BIT_CStream_t* bitC,
				BitContainerType value,
				unsigned int nbBits);  // 21418
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 2331
		BIT_flushBits(BIT_CStream_t* bitC); // 21419
	}
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 21424
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 21412
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 21410
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 21426
	{
		size_t n; // 21428
		{
			const BYTE  llCode; // 21430
			const BYTE  ofCode; // 21431
			const BYTE  mlCode; // 21432
			const U32  llBits; // 21433
			const U32  ofBits; // 21434
			const U32  mlBits; // 21435
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 21461
			{
				const unsigned int  extraBits; // 21453
				BIT_getLowerBits(BitContainerType bitContainer,
						const U32  nbBits);  // 2291
				BIT_addBits(BIT_CStream_t* bitC,
						BitContainerType value,
						unsigned int nbBits);  // 21458
				BIT_getLowerBits(BitContainerType bitContainer,
						const U32  nbBits);  // 2291
				BIT_addBits(BIT_CStream_t* bitC,
						BitContainerType value,
						unsigned int nbBits);  // 21455
				MEM_write64(void* memPtr,
						U64 value);  // 1325
				MEM_writeLE64(void* memPtr,
						U64 val64);  // 1343
				MEM_writeLEST(void* memPtr,
						size_t val);  // 2331
				BIT_flushBits(BIT_CStream_t* bitC); // 21456
			}
			MEM_write64(void* memPtr,
					U64 value);  // 1325
			MEM_writeLE64(void* memPtr,
					U64 val64);  // 1343
			MEM_writeLEST(void* memPtr,
					size_t val);  // 2331
			BIT_flushBits(BIT_CStream_t* bitC); // 21463
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 2790
			FSE_encodeSymbol(BIT_CStream_t* bitC,
					FSE_CState_t* statePtr,
					unsigned int symbol);  // 21442
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 2790
			FSE_encodeSymbol(BIT_CStream_t* bitC,
					FSE_CState_t* statePtr,
					unsigned int symbol);  // 21443
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 2790
			FSE_encodeSymbol(BIT_CStream_t* bitC,
					FSE_CState_t* statePtr,
					unsigned int symbol);  // 21445
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 21448
			BIT_getLowerBits(BitContainerType bitContainer,
					const U32  nbBits);  // 2291
			BIT_addBits(BIT_CStream_t* bitC,
					BitContainerType value,
					unsigned int nbBits);  // 21450
			MEM_write64(void* memPtr,
					U64 value);  // 1325
			MEM_writeLE64(void* memPtr,
					U64 val64);  // 1343
			MEM_writeLEST(void* memPtr,
					size_t val);  // 2331
			BIT_flushBits(BIT_CStream_t* bitC); // 21451
			MEM_write64(void* memPtr,
					U64 value);  // 1325
			MEM_writeLE64(void* memPtr,
					U64 val64);  // 1343
			MEM_writeLEST(void* memPtr,
					size_t val);  // 2331
			BIT_flushBits(BIT_CStream_t* bitC); // 21447
		}
	}
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2796
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2797
	FSE_flushCState(BIT_CStream_t* bitC,
			const FSE_CState_t* statePtr);  // 21468
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2796
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2797
	FSE_flushCState(BIT_CStream_t* bitC,
			const FSE_CState_t* statePtr);  // 21470
	BIT_getLowerBits(BitContainerType bitContainer,
			const U32  nbBits);  // 2291
	BIT_addBits(BIT_CStream_t* bitC,
			BitContainerType value,
			unsigned int nbBits);  // 2796
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 1343
	MEM_writeLEST(void* memPtr,
			size_t val);  // 2331
	BIT_flushBits(BIT_CStream_t* bitC); // 2797
	FSE_flushCState(BIT_CStream_t* bitC,
			const FSE_CState_t* statePtr);  // 21472
	{
		const size_t  streamSize; // 21474
		BIT_addBitsFast(BIT_CStream_t* bitC,
				BitContainerType value,
				unsigned int nbBits);  // 2343
		MEM_write64(void* memPtr,
				U64 value);  // 1325
		MEM_writeLE64(void* memPtr,
				U64 val64);  // 1343
		MEM_writeLEST(void* memPtr,
				size_t val);  // 2331
		BIT_flushBits(BIT_CStream_t* bitC); // 2344
		BIT_closeCStream(BIT_CStream_t* bitC); // 21474
	}
	ZSTD_encodeSequences_body(void* dst,
					size_t dstCapacity,
					const FSE_CTable* CTable_MatchLength,
					const BYTE* mlCodeTable,
					const FSE_CTable* CTable_OffsetBits,
					const BYTE* ofCodeTable,
					const FSE_CTable* CTable_LitLength,
					const BYTE* llCodeTable,
					const SeqDef* sequences,
					size_t nbSeq,
					int longOffsets);  // 21506
} /* size: 1714, inline expansions: 42 (2711 bytes) */

// <06FF6274> thirdparty/zstd/zstd.c:21515
size_t ZSTD_encodeSequences(void* dst, size_t dstCapacity, const FSE_CTable* CTable_MatchLength, const BYTE* mlCodeTable, const FSE_CTable* CTable_OffsetBits, const BYTE* ofCodeTable, const FSE_CTable* CTable_LitLength, const BYTE* llCodeTable, const SeqDef* sequences, size_t nbSeq, int longOffsets, int bmi2)
{
} /* size: 0 */

// <06FF6125> thirdparty/zstd/zstd.c:21615
// variables: 13
size_t ZSTD_compressSubBlock_literal(const HUF_CElt* hufTable, const ZSTD_hufCTablesMetadata_t* hufMetadata, const BYTE* literals, size_t litSize, void* dst, size_t dstSize, const int bmi2, int writeEntropy, int* entropyWritten)
{
	const size_t  header; // 21621
	const size_t  lhSize; // 21622
	const BYTE* ostart; // 21623
	const BYTE* oend; // 21624
	BYTE* op; // 21625
	const U32  singleStream; // 21626
	SymbolEncodingType_e hType; // 21627
	size_t cLitSize; // 21628
	{
		const int  flags; // 21651
		const size_t  cSize; // 21652
	}
	{
		const U32  lhc; // 21678
	}
	{
		const U32  lhc; // 21683
	}
	{
		const U32  lhc; // 21688
	}
} /* size: 0, variables: 8 */

// <06FF609D> thirdparty/zstd/zstd.c:21702
// variables: 4
size_t ZSTD_seqDecompressedSize(const SeqStore_t* seqStore, const SeqDef* sequences, size_t nbSeqs, size_t litSize, int lastSubBlock)
{
	size_t matchLengthSum; // 21706
	size_t litLengthSum; // 21707
	size_t n; // 21708
	{
		const ZSTD_SequenceLength  seqLen; // 21710
	}
} /* size: 0, variables: 3 */

// <06FF5F43> thirdparty/zstd/zstd.c:21736
// variables: 11
size_t ZSTD_compressSubBlock_sequences(const ZSTD_fseCTables_t* fseTables, const ZSTD_fseCTablesMetadata_t* fseMetadata, const SeqDef* sequences, size_t nbSeq, const BYTE* llCode, const BYTE* mlCode, const BYTE* ofCode, const ZSTD_CCtx_params* cctxParams, void* dst, size_t dstCapacity, const int bmi2, int writeEntropy, int* entropyWritten)
{
	const int  longOffsets; // 21744
	const BYTE* ostart; // 21745
	const BYTE* oend; // 21746
	BYTE* op; // 21747
	BYTE* seqHead; // 21748
	{
		const U32  LLtype; // 21772
		const U32  Offtype; // 21773
		const U32  MLtype; // 21774
	}
	{
		const U32  repeat; // 21780
	}
	{
		const size_t  bitstreamSize; // 21784
		{
			const size_t  err_code; // 21791
		}
	}
} /* size: 0, variables: 5 */

// <07094D36> thirdparty/zstd/zstd.c:21836
// variables: 20
// function calls: 18
size_t ZSTD_compressSubBlock(const ZSTD_entropyCTables_t* entropy, const ZSTD_entropyCTablesMetadata_t* entropyMetadata, const SeqDef* sequences, size_t nbSeq, const BYTE* literals, size_t litSize, const BYTE* llCode, const BYTE* mlCode, const BYTE* ofCode, const ZSTD_CCtx_params* cctxParams, void* dst, size_t dstCapacity, const int bmi2, int writeLitEntropy, int writeSeqEntropy, int* litEntropyWritten, int* seqEntropyWritten, U32 lastBlock)
{
	const BYTE* ostart; // 21848
	const BYTE* oend; // 21849
	BYTE* op; // 21850
	{
		size_t cLitSize; // 21853
		{
			const int  flags; // 21651
			const size_t  cSize; // 21652
			HUF_compress4X_usingCTable(void* dst,
							size_t dstSize,
							const void* src,
							size_t srcSize,
							const HUF_CElt* CTable,
							int flags);  // 21653
			HUF_compress1X_usingCTable_internal(void* dst,
								size_t dstSize,
								const void* src,
								size_t srcSize,
								const HUF_CElt* CTable,
								const int  flags);  // 17921
			HUF_compress1X_usingCTable(void* dst,
							size_t dstSize,
							const void* src,
							size_t srcSize,
							const HUF_CElt* CTable,
							int flags);  // 21652
		}
		{
			const U32  lhc; // 21678
			MEM_write16(void* memPtr,
					U16 value);  // 1279
			MEM_writeLE16(void* memPtr,
					U16 val);  // 1294
			MEM_writeLE24(void* memPtr,
					U32 val);  // 21679
		}
		{
			const U32  lhc; // 21683
			MEM_write32(void* memPtr,
					U32 value);  // 1309
			MEM_writeLE32(void* memPtr,
					U32 val32);  // 21684
		}
		{
			const U32  lhc; // 21688
			MEM_write32(void* memPtr,
					U32 value);  // 1309
			MEM_writeLE32(void* memPtr,
					U32 val32);  // 21689
		}
		ZSTD_compressSubBlock_literal(const HUF_CElt* hufTable,
						const ZSTD_hufCTablesMetadata_t* hufMetadata,
						const BYTE* literals,
						size_t litSize,
						void* dst,
						size_t dstSize,
						const int  bmi2,
						int writeEntropy,
						int* entropyWritten);  // 21853
		{
			const size_t  err_code; // 21857
		}
	}
	{
		size_t cSeqSize; // 21861
		{
			const U32  repeat; // 21780
		}
		{
			const U32  LLtype; // 21772
			const U32  Offtype; // 21773
			const U32  MLtype; // 21774
		}
		{
			const size_t  bitstreamSize; // 21784
			ZSTD_encodeSequences(void* dst,
						size_t dstCapacity,
						const FSE_CTable* CTable_MatchLength,
						const BYTE* mlCodeTable,
						const FSE_CTable* CTable_OffsetBits,
						const BYTE* ofCodeTable,
						const FSE_CTable* CTable_LitLength,
						const BYTE* llCodeTable,
						const SeqDef* sequences,
						size_t nbSeq,
						int longOffsets,
						int bmi2);  // 21784
			{
				const size_t  err_code; // 21791
			}
		}
		MEM_write16(void* memPtr,
				U16 value);  // 1279
		MEM_writeLE16(void* memPtr,
				U16 val);  // 21761
		ZSTD_compressSubBlock_sequences(const ZSTD_fseCTables_t* fseTables,
						const ZSTD_fseCTablesMetadata_t* fseMetadata,
						const SeqDef* sequences,
						size_t nbSeq,
						const BYTE* llCode,
						const BYTE* mlCode,
						const BYTE* ofCode,
						const ZSTD_CCtx_params* cctxParams,
						void* dst,
						size_t dstCapacity,
						const int  bmi2,
						int writeEntropy,
						int* entropyWritten);  // 21861
		{
			const size_t  err_code; // 21868
		}
	}
	{
		size_t cSize; // 21873
		const U32  cBlockHeader24; // 21874
		MEM_write16(void* memPtr,
				U16 value);  // 1279
		MEM_writeLE16(void* memPtr,
				U16 val);  // 1294
		MEM_writeLE24(void* memPtr,
				U32 val);  // 21875
	}
} /* size: 1401, variables: 3 */

// <06FF5DC0> thirdparty/zstd/zstd.c:21836
// variables: 9
size_t ZSTD_compressSubBlock(const ZSTD_entropyCTables_t* entropy, const ZSTD_entropyCTablesMetadata_t* entropyMetadata, const SeqDef* sequences, size_t nbSeq, const BYTE* literals, size_t litSize, const BYTE* llCode, const BYTE* mlCode, const BYTE* ofCode, const ZSTD_CCtx_params* cctxParams, void* dst, size_t dstCapacity, const int bmi2, int writeLitEntropy, int writeSeqEntropy, int* litEntropyWritten, int* seqEntropyWritten, U32 lastBlock)
{
	const BYTE* ostart; // 21848
	const BYTE* oend; // 21849
	BYTE* op; // 21850
	{
		size_t cLitSize; // 21853
		{
			const size_t  err_code; // 21857
		}
	}
	{
		size_t cSeqSize; // 21861
		{
			const size_t  err_code; // 21868
		}
	}
	{
		size_t cSize; // 21873
		const U32  cBlockHeader24; // 21874
	}
} /* size: 0, variables: 3 */

// <06FF5D0D> thirdparty/zstd/zstd.c:21880
// variables: 5
size_t ZSTD_estimateSubBlockSize_literal(const BYTE* literals, size_t litSize, const ZSTD_hufCTables_t* huf, const ZSTD_hufCTablesMetadata_t* hufMetadata, void* workspace, size_t wkspSize, int writeEntropy)
{
	const unsigned int* countWksp; // 21886
	unsigned int maxSymbolValue; // 21887
	size_t literalSectionHeaderSize; // 21888
	{
		const size_t  largest; // 21893
		{
			size_t cLitSizeEstimate; // 21895
		}
	}
} /* size: 0, variables: 3 */

// <06FF5981> thirdparty/zstd/zstd.c:21903
// variables: 8
// function calls: 3
size_t ZSTD_estimateSubBlockSize_symbolType(SymbolEncodingType_e type, const BYTE* codeTable, unsigned int maxCode, size_t nbSeq, const FSE_CTable* fseCTable, const U8* additionalBits, const short int* defaultNorm, U32 defaultNormLog, U32 defaultMax, void* workspace, size_t wkspSize)
{
	const unsigned int* countWksp; // 21910
	const BYTE* ctp; // 21911
	const BYTE* ctStart; // 21912
	const BYTE* ctEnd; // 21913
	size_t cSymbolTypeSizeEstimateInBits; // 21914
	unsigned int max; // 21915
	HIST_countFast_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16714
	HIST_countFast_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 21917
	{
		const unsigned int  normAcc; // 21243
		const unsigned int  norm256; // 21244
	}
	ZSTD_crossEntropyCost(const short int* norm,
				unsigned int accuracyLog,
				const unsigned int* count,
				const unsigned int  max);  // 21922
} /* size: 505, variables: 6, inline expansions: 3 (206 bytes) */

// <06FF58DF> thirdparty/zstd/zstd.c:21938
// variables: 2
size_t ZSTD_estimateSubBlockSize_sequences(const BYTE* ofCodeTable, const BYTE* llCodeTable, const BYTE* mlCodeTable, size_t nbSeq, const ZSTD_fseCTables_t* fseTables, const ZSTD_fseCTablesMetadata_t* fseMetadata, void* workspace, size_t wkspSize, int writeEntropy)
{
	const size_t  sequencesSectionHeaderSize; // 21947
	size_t cSeqSizeEstimate; // 21948
} /* size: 0, variables: 2 */

// <06F3D449> thirdparty/zstd/zstd.c:21966
// member variables: 2
// struct size: 16
typedef struct {
	size_t estLitSize; /* 0 8 */
	size_t estBlockSize; /* 8 8 */
} EstimatedBlockSize;

// <06FF5823> thirdparty/zstd/zstd.c:21970
// variable: 1
EstimatedBlockSize ZSTD_estimateSubBlockSize(const BYTE* literals, size_t litSize, const BYTE* ofCodeTable, const BYTE* llCodeTable, const BYTE* mlCodeTable, size_t nbSeq, const ZSTD_entropyCTables_t* entropy, const ZSTD_entropyCTablesMetadata_t* entropyMetadata, void* workspace, size_t wkspSize, int writeLitEntropy, int writeSeqEntropy)
{
	EstimatedBlockSize ebs; // 21980
} /* size: 0, variables: 1 */

// <06FF5803> thirdparty/zstd/zstd.c:21991
int ZSTD_needSequenceEntropyTables(const ZSTD_fseCTablesMetadata_t* fseMetadata)
{
} /* size: 0 */

// <06FF57B2> thirdparty/zstd/zstd.c:22002
// variables: 2
size_t countLiterals(const SeqStore_t* seqStore, const SeqDef* sp, size_t seqCount)
{
	size_t n; // 22004
	size_t total; // 22004
} /* size: 0, variables: 2 */

// <06FF5711> thirdparty/zstd/zstd.c:22015
// variables: 5
size_t sizeBlockSequences(const SeqDef* sp, size_t nbSeqs, size_t targetBudget, size_t avgLitCost, size_t avgSeqCost, int firstSubBlock)
{
	size_t n; // 22019
	size_t budget; // 22019
	size_t inSize; // 22019
	const size_t  headerSize; // 22021
	{
		size_t currentCost; // 22032
	}
} /* size: 0, variables: 4 */

// <06FF53E1> thirdparty/zstd/zstd.c:22052
// variables: 46
size_t ZSTD_compressSubBlock_multi(const SeqStore_t* seqStorePtr, const ZSTD_compressedBlockState_t* prevCBlock, ZSTD_compressedBlockState_t* nextCBlock, const ZSTD_entropyCTablesMetadata_t* entropyMetadata, const ZSTD_CCtx_params* cctxParams, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const int bmi2, U32 lastBlock, void* workspace, size_t wkspSize)
{
	const SeqDef* sstart; // 22062
	const SeqDef* send; // 22063
	const SeqDef* sp; // 22064
	const size_t  nbSeqs; // 22065
	const BYTE* lstart; // 22066
	const BYTE* lend; // 22067
	const BYTE* lp; // 22068
	const size_t  nbLiterals; // 22069
	const BYTE* ip; // 22070
	const BYTE* iend; // 22071
	const BYTE* ostart; // 22072
	const BYTE* oend; // 22073
	BYTE* op; // 22074
	const BYTE* llCodePtr; // 22075
	const BYTE* mlCodePtr; // 22076
	const BYTE* ofCodePtr; // 22077
	const size_t  minTarget; // 22078
	const size_t  targetCBlockSize; // 22079
	int writeLitEntropy; // 22080
	int writeSeqEntropy; // 22081
	{
		const EstimatedBlockSize  ebs; // 22088
		const size_t  avgLitCost; // 22095
		const size_t  avgSeqCost; // 22096
		const size_t  nbSubBlocks; // 22097
		size_t n; // 22098
		size_t avgBlockBudget; // 22098
		size_t blockBudgetSupp; // 22098
		{
			const size_t  seqCount; // 22111
			{
				int litEntropyWritten; // 22118
				int seqEntropyWritten; // 22119
				size_t litSize; // 22120
				const size_t  decompressedSize; // 22121
				const size_t  cSize; // 22123
				{
					const size_t  err_code; // 22132
				}
			}
		}
	}
	{
		int litEntropyWritten; // 22161
		int seqEntropyWritten; // 22162
		size_t litSize; // 22163
		size_t seqCount; // 22164
		const size_t  decompressedSize; // 22165
		const size_t  cSize; // 22167
		{
			const size_t  err_code; // 22176
		}
	}
	{
		const size_t  rSize; // 22215
		const size_t  cSize; // 22216
		{
			const size_t  err_code; // 22218
		}
		{
			const SeqDef* seq; // 22223
			Repcodes_t rep; // 22224
		}
	}
} /* size: 0, variables: 20 */

// <06FF4478> thirdparty/zstd/zstd.c:22238
// variables: 35
// function calls: 25
size_t ZSTD_compressSuperBlock(ZSTD_CCtx* zc, void* dst, size_t dstCapacity, const void* src, size_t srcSize, unsigned int lastBlock)
{
	ZSTD_entropyCTablesMetadata_t entropyMetadata; // 22243
	{
		const size_t  err_code; // 22245
	}
	{
		const EstimatedBlockSize  ebs; // 22088
		const size_t  avgLitCost; // 22095
		const size_t  avgSeqCost; // 22096
		const size_t  nbSubBlocks; // 22097
		size_t n; // 22098
		size_t avgBlockBudget; // 22098
		size_t blockBudgetSupp; // 22098
		{
			const size_t  largest; // 21893
			HIST_countFast_wksp(unsigned int* count,
						unsigned int* maxSymbolValuePtr,
						const void* source,
						size_t sourceSize,
						void* workSpace,
						size_t workSpaceSize);  // 16714
			HIST_countFast_wksp(unsigned int* count,
						unsigned int* maxSymbolValuePtr,
						const void* source,
						size_t sourceSize,
						void* workSpace,
						size_t workSpaceSize);  // 16737
			HIST_count_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16728
			HIST_count_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 21893
			{
				size_t cLitSizeEstimate; // 21895
				HUF_getNbBits(HUF_CElt elt); // 17556
				HUF_estimateCompressedSize(const HUF_CElt* CTable,
								const unsigned int* count,
								unsigned int maxSymbolValue);  // 21895
			}
		}
		ZSTD_estimateSubBlockSize_literal(const BYTE* literals,
							size_t litSize,
							const ZSTD_hufCTables_t* huf,
							const ZSTD_hufCTablesMetadata_t* hufMetadata,
							void* workspace,
							size_t wkspSize,
							int writeEntropy);  // 21981
		ZSTD_estimateSubBlockSize_sequences(const BYTE* ofCodeTable,
							const BYTE* llCodeTable,
							const BYTE* mlCodeTable,
							size_t nbSeq,
							const ZSTD_fseCTables_t* fseTables,
							const ZSTD_fseCTablesMetadata_t* fseMetadata,
							void* workspace,
							size_t wkspSize,
							int writeEntropy);  // 21984
		ZSTD_estimateSubBlockSize(const BYTE* literals,
						size_t litSize,
						const BYTE* ofCodeTable,
						const BYTE* llCodeTable,
						const BYTE* mlCodeTable,
						size_t nbSeq,
						const ZSTD_entropyCTables_t* entropy,
						const ZSTD_entropyCTablesMetadata_t* entropyMetadata,
						void* workspace,
						size_t wkspSize,
						int writeLitEntropy,
						int writeSeqEntropy);  // 22089
		{
			const size_t  seqCount; // 22111
			{
				size_t currentCost; // 22032
			}
			sizeBlockSequences(const SeqDef* sp,
						size_t nbSeqs,
						size_t targetBudget,
						size_t avgLitCost,
						size_t avgSeqCost,
						int firstSubBlock);  // 22111
			{
				int litEntropyWritten; // 22118
				int seqEntropyWritten; // 22119
				size_t litSize; // 22120
				const size_t  decompressedSize; // 22121
				const size_t  cSize; // 22123
				ZSTD_getSequenceLength(const SeqStore_t* seqStore,
							const SeqDef* seq);  // 22007
				countLiterals(const SeqStore_t* seqStore,
						const SeqDef* sp,
						size_t seqCount);  // 22120
				{
					const ZSTD_SequenceLength  seqLen; // 21710
					ZSTD_getSequenceLength(const SeqStore_t* seqStore,
								const SeqDef* seq);  // 21710
				}
				ZSTD_seqDecompressedSize(const SeqStore_t* seqStore,
							const SeqDef* sequences,
							size_t nbSeqs,
							size_t litSize,
							int lastSubBlock);  // 22122
				{
					const size_t  err_code; // 22132
				}
			}
		}
	}
	{
		int litEntropyWritten; // 22161
		int seqEntropyWritten; // 22162
		size_t litSize; // 22163
		size_t seqCount; // 22164
		const size_t  decompressedSize; // 22165
		const size_t  cSize; // 22167
		{
			const ZSTD_SequenceLength  seqLen; // 21710
			ZSTD_getSequenceLength(const SeqStore_t* seqStore,
						const SeqDef* seq);  // 21710
		}
		ZSTD_seqDecompressedSize(const SeqStore_t* seqStore,
					const SeqDef* sequences,
					size_t nbSeqs,
					size_t litSize,
					int lastSubBlock);  // 22166
		{
			const size_t  err_code; // 22176
		}
	}
	ZSTD_needSequenceEntropyTables(const ZSTD_fseCTablesMetadata_t* fseMetadata); // 22205
	{
		const size_t  rSize; // 22215
		const size_t  cSize; // 22216
		{
			const size_t  err_code; // 22218
		}
		MEM_write16(void* memPtr,
				U16 value);  // 1279
		MEM_writeLE16(void* memPtr,
				U16 val);  // 1294
		MEM_writeLE24(void* memPtr,
				U32 val);  // 19810
		ZSTD_noCompressBlock(void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize,
					U32 lastBlock);  // 22216
		{
			const SeqDef* seq; // 22223
			Repcodes_t rep; // 22224
			ZSTD_getSequenceLength(const SeqStore_t* seqStore,
						const SeqDef* seq);  // 22227
			{
				const U32  repCode; // 19979
				{
					const U32  currentOffset; // 19981
				}
			}
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 19972
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 22227
		}
	}
	ZSTD_compressSubBlock_multi(const SeqStore_t* seqStorePtr,
					const ZSTD_compressedBlockState_t* prevCBlock,
					ZSTD_compressedBlockState_t* nextCBlock,
					const ZSTD_entropyCTablesMetadata_t* entropyMetadata,
					const ZSTD_CCtx_params* cctxParams,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize,
					const int  bmi2,
					U32 lastBlock,
					void* workspace,
					size_t wkspSize);  // 22252
} /* size: 3231, variables: 1, inline expansions: 2 (3009 bytes) */

// <06FF444D> thirdparty/zstd/zstd.c:22296
inline unsigned int hash2(const void* p, unsigned int hashLog)
{
} /* size: 0 */

// <06F3D48A> thirdparty/zstd/zstd.c:22305
// member variables: 2
// struct size: 4,104
typedef struct {
	unsigned int events[1024]; /* 0 4096 */
	size_t nbEvents; /* 4096 8 */
} Fingerprint;

// <06F3D4DE> thirdparty/zstd/zstd.c:22309
// member variables: 2
// struct size: 8,208
typedef struct {
	Fingerprint pastEvents; /* 0 4104 */
	Fingerprint newEvents; /* 4104 4104 */
} FPStats;

// <06FF4431> thirdparty/zstd/zstd.c:22314
void initStats(FPStats* fpstats)
{
} /* size: 0 */

// <06FF43BF> thirdparty/zstd/zstd.c:22320
// variables: 3
inline void addEvents_generic(Fingerprint* fp, const void* src, size_t srcSize, size_t samplingRate, unsigned int hashLog)
{
	const char* p; // 22322
	size_t limit; // 22323
	size_t n; // 22324
} /* size: 0, variables: 3 */

// <06FF4370> thirdparty/zstd/zstd.c:22333
inline void recordFingerprint_generic(Fingerprint* fp, const void* src, size_t srcSize, size_t samplingRate, unsigned int hashLog)
{
} /* size: 0 */

// <06FF41BC> thirdparty/zstd/zstd.c:22350
// function calls: 4
void ZSTD_recordFingerprint_1(Fingerprint* fp, const void* src, size_t srcSize)
{
	MEM_read16(const void* ptr); // 22301
	hash2(const void* p,
		unsigned int hashLog);  // 22327
	addEvents_generic(Fingerprint* fp,
				const void* src,
				size_t srcSize,
				size_t samplingRate,
				unsigned int hashLog);  // 22337
	recordFingerprint_generic(Fingerprint* fp,
					const void* src,
					size_t srcSize,
					size_t samplingRate,
					unsigned int hashLog);  // 22350
} /* size: 101, inline expansions: 4 (141 bytes) */

// <06FF4008> thirdparty/zstd/zstd.c:22351
// function calls: 4
void ZSTD_recordFingerprint_5(Fingerprint* fp, const void* src, size_t srcSize)
{
	MEM_read16(const void* ptr); // 22301
	hash2(const void* p,
		unsigned int hashLog);  // 22327
	addEvents_generic(Fingerprint* fp,
				const void* src,
				size_t srcSize,
				size_t samplingRate,
				unsigned int hashLog);  // 22337
	recordFingerprint_generic(Fingerprint* fp,
					const void* src,
					size_t srcSize,
					size_t samplingRate,
					unsigned int hashLog);  // 22351
} /* size: 120, inline expansions: 4 (181 bytes) */

// <06FF3E54> thirdparty/zstd/zstd.c:22352
// function calls: 4
void ZSTD_recordFingerprint_11(Fingerprint* fp, const void* src, size_t srcSize)
{
	MEM_read16(const void* ptr); // 22301
	hash2(const void* p,
		unsigned int hashLog);  // 22327
	addEvents_generic(Fingerprint* fp,
				const void* src,
				size_t srcSize,
				size_t samplingRate,
				unsigned int hashLog);  // 22337
	recordFingerprint_generic(Fingerprint* fp,
					const void* src,
					size_t srcSize,
					size_t samplingRate,
					unsigned int hashLog);  // 22352
} /* size: 119, inline expansions: 4 (179 bytes) */

// <06FF3CC5> thirdparty/zstd/zstd.c:22353
// function calls: 3
void ZSTD_recordFingerprint_43(Fingerprint* fp, const void* src, size_t srcSize)
{
	hash2(const void* p,
		unsigned int hashLog);  // 22327
	addEvents_generic(Fingerprint* fp,
				const void* src,
				size_t srcSize,
				size_t samplingRate,
				unsigned int hashLog);  // 22337
	recordFingerprint_generic(Fingerprint* fp,
					const void* src,
					size_t srcSize,
					size_t samplingRate,
					unsigned int hashLog);  // 22353
} /* size: 111, inline expansions: 3 (152 bytes) */

// <06FF3CA5> thirdparty/zstd/zstd.c:22356
U64 abs64(S64 s64)
{
} /* size: 0 */

// <06FF3C53> thirdparty/zstd/zstd.c:22358
// variables: 2
U64 fpDistance(const Fingerprint* fp1, const Fingerprint* fp2, unsigned int hashLog)
{
	U64 distance; // 22360
	size_t n; // 22361
} /* size: 0, variables: 2 */

// <06FF3BE3> thirdparty/zstd/zstd.c:22373
// variables: 3
int compareFingerprints(const Fingerprint* ref, const Fingerprint* newfp, int penalty, unsigned int hashLog)
{
	{
		U64 p50; // 22380
		U64 deviation; // 22381
		U64 threshold; // 22382
	}
} /* size: 0 */

// <06FF3BA9> thirdparty/zstd/zstd.c:22387
// variable: 1
void mergeEvents(Fingerprint* acc, const Fingerprint* newfp)
{
	size_t n; // 22389
} /* size: 0, variables: 1 */

// <06FF3ABE> thirdparty/zstd/zstd.c:22417
// variables: 7
size_t ZSTD_splitBlock_byChunks(const void* blockStart, size_t blockSize, int level, void* workspace, size_t wkspSize)
{
	const RecordEvents_f   records_fs; // 22421
	const unsigned int   hashParams; // 22424
	const RecordEvents_f  record_f; // 22425
	const FPStats* fpstats; // 22426
	const char* p; // 22427
	int penalty; // 22428
	size_t pos; // 22429
} /* size: 0, variables: 7 */

// <06FF3A29> thirdparty/zstd/zstd.c:22461
// variables: 5
size_t ZSTD_splitBlock_fromBorders(const void* blockStart, size_t blockSize, void* workspace, size_t wkspSize)
{
	const FPStats* fpstats; // 22465
	Fingerprint* middleEvents; // 22466
	{
		const U64  distFromBegin; // 22482
		const U64  distFromEnd; // 22483
		const U64  minDistance; // 22484
	}
} /* size: 0, variables: 2 */

// <06FF39D0> thirdparty/zstd/zstd.c:22491
size_t ZSTD_splitBlock(const void* blockStart, size_t blockSize, int level, void* workspace, size_t wkspSize)
{
} /* size: 0 */

// <06F3AD10> thirdparty/zstd/zstd.c:23034
// member variables: 11
// struct size: 6,080
struct ZSTD_CDict_s {
	const void * dictContent; /* 0 8 */
	size_t dictContentSize; /* 8 8 */
	ZSTD_dictContentType_e dictContentType; /* 16 4 */
	U32 * entropyWorkspace; /* 24 8 */
	ZSTD_cwksp workspace; /* 32 72 */
	ZSTD_MatchState_t matchState; /* 104 304 */
	ZSTD_compressedBlockState_t cBlockState; /* 408 5632 */
	ZSTD_customMem customMem; /* 6040 24 */
	U32 dictID; /* 6064 4 */
	int compressionLevel; /* 6068 4 */
	ZSTD_ParamSwitch_e useRowMatchFinder; /* 6072 4 */
};

// <06FF3922> thirdparty/zstd/zstd.c:23056
// variable: 1
void ZSTD_initCCtx(ZSTD_CCtx* cctx, ZSTD_customMem memManager)
{
	{
		const size_t  err; // 23062
	}
} /* size: 0 */

// <06FF3725> thirdparty/zstd/zstd.c:23068
// variables: 4
// function calls: 4
ZSTD_CCtx* ZSTD_createCCtx_advanced(ZSTD_customMem customMem)
{
	{
		const ZSTD_CCtx* cctx; // 23073
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 23073
		{
			U32 f1a; // 8142
		}
		{
			U32 f7a; // 8146
		}
		ZSTD_cpuid(void); // 15816
		ZSTD_cpuSupportsBmi2(void); // 23061
		{
			const size_t  err; // 23062
		}
		ZSTD_initCCtx(ZSTD_CCtx* cctx,
				ZSTD_customMem memManager);  // 23075
	}
} /* size: 238 */

// <06FF3350> thirdparty/zstd/zstd.c:23080
// variables: 4
// function calls: 11
ZSTD_CCtx* ZSTD_initStaticCCtx(void* workspace, size_t workspaceSize)
{
	ZSTD_cwksp ws; // 23082
	ZSTD_CCtx* cctx; // 23083
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18954
	ZSTD_cwksp_init(ZSTD_cwksp* ws,
			void* start,
			size_t size,
			ZSTD_cwksp_static_alloc_e isStatic);  // 23086
	ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
					size_t bytes);  // 23088
	ZSTD_cwksp_move(ZSTD_cwksp* dst,
			ZSTD_cwksp* src);  // 23092
	ZSTD_cwksp_available_space(ZSTD_cwksp* ws); // 19016
	ZSTD_cwksp_check_available(ZSTD_cwksp* ws,
					size_t additionalNeededSpace);  // 23096
	ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
					size_t bytes);  // 23097
	ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
					size_t bytes);  // 23098
	ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
					size_t bytes);  // 23099
	{
		U32 f1a; // 8142
	}
	{
		U32 f7a; // 8146
	}
	ZSTD_cpuid(void); // 23101
	ZSTD_cpuid_bmi2(const ZSTD_cpuid_t cpuid); // 23101
} /* size: 462, variables: 2, inline expansions: 11 (305 bytes) */

// <06FF32C7> thirdparty/zstd/zstd.c:23108
// function call: 1
void ZSTD_clearAllDicts(ZSTD_CCtx* cctx)
{
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 23110
} /* size: 189, inline expansions: 1 (43 bytes) */

// <06FF328D> thirdparty/zstd/zstd.c:23117
// variables: 2
size_t ZSTD_sizeof_localDict(ZSTD_localDict dict)
{
	const size_t  bufferSize; // 23119
	const size_t  cdictSize; // 23120
} /* size: 0, variables: 2 */

// <06FF3271> thirdparty/zstd/zstd.c:23124
void ZSTD_freeCCtxContent(ZSTD_CCtx* cctx)
{
} /* size: 0 */

// <06FF306E> thirdparty/zstd/zstd.c:23149
size_t ZSTD_sizeof_mtctx(const ZSTD_CCtx* cctx)
{
} /* size: 0 */

// <06FF2F82> thirdparty/zstd/zstd.c:23160
// function calls: 3
size_t ZSTD_sizeof_CCtx(const ZSTD_CCtx* cctx)
{
	ZSTD_cwksp_sizeof(const ZSTD_cwksp* ws); // 23165
	ZSTD_sizeof_localDict(ZSTD_localDict dict); // 23166
	ZSTD_sizeof_mtctx(const ZSTD_CCtx* cctx); // 23167
} /* size: 115, inline expansions: 3 (57 bytes) */

// <06FF2F30> thirdparty/zstd/zstd.c:23170
size_t ZSTD_sizeof_CStream(const ZSTD_CStream* zcs)
{
} /* size: 9 */

// <06FF2F0B> thirdparty/zstd/zstd.c:23176
const SeqStore_t* ZSTD_getSeqStore(const ZSTD_CCtx* ctx)
{
} /* size: 0 */

// <06FF2EEB> thirdparty/zstd/zstd.c:23179
int ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy)
{
} /* size: 0 */

// <06FF2EBE> thirdparty/zstd/zstd.c:23186
int ZSTD_rowMatchFinderUsed(const ZSTD_strategy strategy, const ZSTD_ParamSwitch_e mode)
{
} /* size: 0 */

// <06FF2E91> thirdparty/zstd/zstd.c:23192
ZSTD_ParamSwitch_e ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode, const ZSTD_compressionParameters* cParams)
{
} /* size: 0 */

// <06FF2E64> thirdparty/zstd/zstd.c:23202
ZSTD_ParamSwitch_e ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode, const ZSTD_compressionParameters* cParams)
{
} /* size: 0 */

// <06FF2E2A> thirdparty/zstd/zstd.c:23209
int ZSTD_allocateChainTable(const ZSTD_strategy strategy, const ZSTD_ParamSwitch_e useRowMatchFinder, const U32 forDDSDict)
{
} /* size: 0 */

// <06FF2DFD> thirdparty/zstd/zstd.c:23223
ZSTD_ParamSwitch_e ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode, const ZSTD_compressionParameters* cParams)
{
} /* size: 0 */

// <06FF2DDD> thirdparty/zstd/zstd.c:23229
int ZSTD_resolveExternalSequenceValidation(int mode)
{
} /* size: 0 */

// <06FF2DBD> thirdparty/zstd/zstd.c:23234
size_t ZSTD_resolveMaxBlockSize(size_t maxBlockSize)
{
} /* size: 0 */

// <06FF2D90> thirdparty/zstd/zstd.c:23242
ZSTD_ParamSwitch_e ZSTD_resolveExternalRepcodeSearch(ZSTD_ParamSwitch_e value, int cLevel)
{
} /* size: 0 */

// <06FF2D70> thirdparty/zstd/zstd.c:23253
int ZSTD_CDictIndicesAreTagged(const ZSTD_compressionParameters* cParams)
{
} /* size: 0 */

// <06FF2AE4> thirdparty/zstd/zstd.c:23257
// variable: 1
// function calls: 9
ZSTD_CCtx_params ZSTD_makeCCtxParamsFromCParams(ZSTD_compressionParameters cParams)
{
	ZSTD_CCtx_params cctxParams; // 23260
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23223
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23266
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23202
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23272
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23273
	ZSTD_resolveMaxBlockSize(size_t maxBlockSize); // 23275
	ZSTD_resolveExternalRepcodeSearch(ZSTD_ParamSwitch_e value,
						int cLevel);  // 23276
} /* size: 302, variables: 1, inline expansions: 9 (196 bytes) */

// <06FF2AB7> thirdparty/zstd/zstd.c:23282
// variable: 1
ZSTD_CCtx_params* ZSTD_createCCtxParams_advanced(ZSTD_customMem customMem)
{
	ZSTD_CCtx_params* params; // 23285
} /* size: 0, variables: 1 */

// <06FF29CA> thirdparty/zstd/zstd.c:23295
// function calls: 3
ZSTD_CCtx_params* ZSTD_createCCtxParams(void)
{
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 23287
	ZSTD_createCCtxParams_advanced(ZSTD_customMem customMem); // 23297
} /* size: 83, inline expansions: 3 (102 bytes) */

// <06FF2939> thirdparty/zstd/zstd.c:23300
// function call: 1
size_t ZSTD_freeCCtxParams(ZSTD_CCtx_params* params)
{
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 23303
} /* size: 63, inline expansions: 1 (27 bytes) */

// <06FF28E2> thirdparty/zstd/zstd.c:23307
size_t ZSTD_CCtxParams_reset(ZSTD_CCtx_params* params)
{
} /* size: 14 */

// <06FF2897> thirdparty/zstd/zstd.c:23312
size_t ZSTD_CCtxParams_init(ZSTD_CCtx_params* cctxParams, int compressionLevel)
{
} /* size: 48 */

// <07019B6B> thirdparty/zstd/zstd.c:23327
// function calls: 8
void ZSTD_CCtxParams_init_internal(ZSTD_CCtx_params* cctxParams, const ZSTD_parameters* params, int compressionLevel)
{
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23339
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23202
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23340
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23223
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23341
	ZSTD_resolveExternalRepcodeSearch(ZSTD_ParamSwitch_e value,
						int cLevel);  // 23344
} /* size: 185, inline expansions: 8 (196 bytes) */

// <06FF2861> thirdparty/zstd/zstd.c:23327
void ZSTD_CCtxParams_init_internal(ZSTD_CCtx_params* cctxParams, const ZSTD_parameters* params, int compressionLevel)
{
} /* size: 0 */

// <06FF2668> thirdparty/zstd/zstd.c:23349
// variable: 1
// function calls: 8
size_t ZSTD_CCtxParams_init_advanced(ZSTD_CCtx_params* cctxParams, ZSTD_parameters params)
{
	{
		const size_t  err_code; // 23352
	}
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23339
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23202
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23340
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23223
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23341
	ZSTD_CCtxParams_init_internal(ZSTD_CCtx_params* cctxParams,
					const ZSTD_parameters* params,
					int compressionLevel);  // 23353
} /* size: 298, inline expansions: 8 (323 bytes) */

// <06FF263F> thirdparty/zstd/zstd.c:23361
void ZSTD_CCtxParams_setZstdParams(ZSTD_CCtx_params* cctxParams, const ZSTD_parameters* params)
{
} /* size: 0 */

// <06FF25D3> thirdparty/zstd/zstd.c:23373
// variable: 1
ZSTD_bounds ZSTD_cParam_getBounds(ZSTD_cParameter param)
{
	ZSTD_bounds bounds; // 23375
} /* size: 606, variables: 1 */

// <06FF2599> thirdparty/zstd/zstd.c:23596
// variable: 1
size_t ZSTD_cParam_clampBounds(ZSTD_cParameter cParam, int* value)
{
	const ZSTD_bounds  bounds; // 23598
} /* size: 0, variables: 1 */

// <06FF2579> thirdparty/zstd/zstd.c:23612
int ZSTD_isUpdateAuthorized(ZSTD_cParameter param)
{
} /* size: 0 */

// <06FF24D2> thirdparty/zstd/zstd.c:23662
// function call: 1
size_t ZSTD_CCtx_setParameter(ZSTD_CCtx* cctx, ZSTD_cParameter param, int value)
{
	ZSTD_isUpdateAuthorized(ZSTD_cParameter param); // 23666
} /* size: 311, inline expansions: 1 (63 bytes) */

// <06FF143F> thirdparty/zstd/zstd.c:23724
// variables: 7
// function calls: 34
size_t ZSTD_CCtxParams_setParameter(ZSTD_CCtx_params* CCtxParams, ZSTD_cParameter param, int value)
{
	{
		const size_t  err_code; // 23736
		ZSTD_cParam_clampBounds(ZSTD_cParameter cParam,
					int* value);  // 23736
	}
	{
		const ZSTD_dictAttachPref_e  pref; // 23807
		ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
					int value);  // 23808
	}
	{
		const ZSTD_ParamSwitch_e  lcm; // 23814
		ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
					int value);  // 23815
	}
	{
		const size_t  err_code; // 23825
		ZSTD_cParam_clampBounds(ZSTD_cParameter cParam,
					int* value);  // 23825
	}
	{
		const size_t  err_code; // 23838
		ZSTD_cParam_clampBounds(ZSTD_cParameter cParam,
					int* value);  // 23838
	}
	{
		const size_t  err_code; // 23849
		ZSTD_cParam_clampBounds(ZSTD_cParameter cParam,
					int* value);  // 23849
	}
	{
		const size_t  err_code; // 23859
		ZSTD_cParam_clampBounds(ZSTD_cParameter cParam,
					int* value);  // 23859
	}
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23912
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23917
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23922
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23927
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23932
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23942
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23947
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23952
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23957
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23969
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23937
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23776
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23869
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23907
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23731
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23747
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23771
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23963
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23893
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23765
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23900
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23759
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23782
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23753
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23887
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23875
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 23881
} /* size: 2986, inline expansions: 27 (1501 bytes) */

// <06FF13B3> thirdparty/zstd/zstd.c:23977
size_t ZSTD_CCtx_getParameter(const ZSTD_CCtx* cctx, ZSTD_cParameter param, int* value)
{
} /* size: 13 */

// <06FF1359> thirdparty/zstd/zstd.c:23982
size_t ZSTD_CCtxParams_getParameter(const ZSTD_CCtx_params* CCtxParams, ZSTD_cParameter param, int* value)
{
} /* size: 683 */

// <06FF12E3> thirdparty/zstd/zstd.c:24132
size_t ZSTD_CCtx_setParametersUsingCCtxParams(ZSTD_CCtx* cctx, const ZSTD_CCtx_params* params)
{
} /* size: 57 */

// <06FF105F> thirdparty/zstd/zstd.c:24146
// variables: 8
size_t ZSTD_CCtx_setCParams(ZSTD_CCtx* cctx, ZSTD_compressionParameters cparams)
{
	{
		const size_t  err_code; // 24151
	}
	{
		const size_t  err_code; // 24152
	}
	{
		const size_t  err_code; // 24153
	}
	{
		const size_t  err_code; // 24154
	}
	{
		const size_t  err_code; // 24155
	}
	{
		const size_t  err_code; // 24156
	}
	{
		const size_t  err_code; // 24157
	}
	{
		const size_t  err_code; // 24158
	}
} /* size: 247 */

// <06FF0F2C> thirdparty/zstd/zstd.c:24162
// variables: 3
size_t ZSTD_CCtx_setFParams(ZSTD_CCtx* cctx, ZSTD_frameParameters fparams)
{
	{
		const size_t  err_code; // 24166
	}
	{
		const size_t  err_code; // 24167
	}
	{
		const size_t  err_code; // 24168
	}
} /* size: 124 */

// <06FF0E39> thirdparty/zstd/zstd.c:24172
// variables: 3
size_t ZSTD_CCtx_setParams(ZSTD_CCtx* cctx, ZSTD_parameters params)
{
	{
		const size_t  err_code; // 24176
	}
	{
		const size_t  err_code; // 24178
	}
	{
		const size_t  err_code; // 24180
	}
} /* size: 166 */

// <06FF0DEE> thirdparty/zstd/zstd.c:24184
size_t ZSTD_CCtx_setPledgedSrcSize(ZSTD_CCtx* cctx, long long unsigned int pledgedSrcSize)
{
} /* size: 35 */

// <06FF0DB7> thirdparty/zstd/zstd.c:24206
// variable: 1
size_t ZSTD_initLocalDict(ZSTD_CCtx* cctx)
{
	const ZSTD_localDict* dl; // 24208
} /* size: 0, variables: 1 */

// <06FF0D53> thirdparty/zstd/zstd.c:24237
// variable: 1
size_t ZSTD_CCtx_loadDictionary_advanced(ZSTD_CCtx* cctx, const void* dict, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType)
{
	{
		void* dictBuffer; // 24253
	}
} /* size: 0 */

// <06FF0CBF> thirdparty/zstd/zstd.c:24268
size_t ZSTD_CCtx_loadDictionary_byReference(ZSTD_CCtx* cctx, const void* dict, size_t dictSize)
{
} /* size: 17 */

// <06FF0C2B> thirdparty/zstd/zstd.c:24275
size_t ZSTD_CCtx_loadDictionary(ZSTD_CCtx* cctx, const void* dict, size_t dictSize)
{
} /* size: 14 */

// <06FF0BC4> thirdparty/zstd/zstd.c:24282
size_t ZSTD_CCtx_refCDict(ZSTD_CCtx* cctx, const ZSTD_CDict* cdict)
{
} /* size: 72 */

// <06FF0B7F> thirdparty/zstd/zstd.c:24292
size_t ZSTD_CCtx_refThreadPool(ZSTD_CCtx* cctx, ZSTD_threadPool* pool)
{
} /* size: 31 */

// <06FF0AF0> thirdparty/zstd/zstd.c:24300
size_t ZSTD_CCtx_refPrefix(ZSTD_CCtx* cctx, const void* prefix, size_t prefixSize)
{
} /* size: 14 */

// <06FF0AA8> thirdparty/zstd/zstd.c:24305
size_t ZSTD_CCtx_refPrefix_advanced(ZSTD_CCtx* cctx, const void* prefix, size_t prefixSize, ZSTD_dictContentType_e dictContentType)
{
} /* size: 0 */

// <06FF0A7A> thirdparty/zstd/zstd.c:24321
size_t ZSTD_CCtx_reset(ZSTD_CCtx* cctx, ZSTD_ResetDirective reset)
{
} /* size: 0 */

// <06FF07A6> thirdparty/zstd/zstd.c:24342
// function calls: 7
size_t ZSTD_checkCParams(ZSTD_compressionParameters cParams)
{
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 24344
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 24345
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 24346
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 24347
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 24348
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 24349
	ZSTD_cParam_withinBounds(ZSTD_cParameter cParam,
				int value);  // 24350
} /* size: 380, inline expansions: 7 (327 bytes) */

// <06FF0705> thirdparty/zstd/zstd.c:24358
// variables: 7
ZSTD_compressionParameters ZSTD_clampCParams(ZSTD_compressionParameters cParams)
{
	{
		const ZSTD_bounds  bounds; // 24367
	}
	{
		const ZSTD_bounds  bounds; // 24368
	}
	{
		const ZSTD_bounds  bounds; // 24369
	}
	{
		const ZSTD_bounds  bounds; // 24370
	}
	{
		const ZSTD_bounds  bounds; // 24371
	}
	{
		const ZSTD_bounds  bounds; // 24372
	}
	{
		const ZSTD_bounds  bounds; // 24373
	}
} /* size: 0 */

// <06FF06C6> thirdparty/zstd/zstd.c:24379
// variable: 1
U32 ZSTD_cycleLog(U32 hashLog, ZSTD_strategy strat)
{
	const U32  btScale; // 24381
} /* size: 0, variables: 1 */

// <06FF0663> thirdparty/zstd/zstd.c:24393
// variables: 3
U32 ZSTD_dictAndWindowLog(U32 windowLog, U64 srcSize, U64 dictSize)
{
	const U64  maxWindowSize; // 24395
	{
		const U64  windowSize; // 24403
		const U64  dictAndWindowSize; // 24404
	}
} /* size: 0, variables: 1 */

// <06FF02EA> thirdparty/zstd/zstd.c:24427
// variables: 13
// function calls: 9
ZSTD_compressionParameters ZSTD_adjustCParams_internal(ZSTD_compressionParameters cPar, long long unsigned int srcSize, size_t dictSize, ZSTD_CParamMode_e mode, ZSTD_ParamSwitch_e useRowMatchFinder)
{
	const U64  minSrcSize; // 24433
	const U64  maxWindowResize; // 24434
	{
		const U32  tSize; // 24509
		const U32  hashSizeMin; // 24510
		const U32  srcLog; // 24511
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 24512
	}
	{
		const U32  dictAndWindowLog; // 24516
		const U32  cycleLog; // 24517
		{
			const U64  windowSize; // 24403
			const U64  dictAndWindowSize; // 24404
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 24414
		}
		ZSTD_dictAndWindowLog(U32 windowLog,
					U64 srcSize,
					U64 dictSize);  // 24516
		ZSTD_cycleLog(U32 hashLog,
				ZSTD_strategy strat);  // 24517
	}
	{
		const U32  maxShortCacheHashLog; // 24530
	}
	{
		const U32  rowLog; // 24553
		const U32  maxRowHashLog; // 24554
		const U32  maxHashLog; // 24555
	}
	ZSTD_CDictIndicesAreTagged(const ZSTD_compressionParameters* cParams); // 24529
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
	ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
				const ZSTD_ParamSwitch_e  mode);  // 24551
} /* size: 627, variables: 2, inline expansions: 3 (42 bytes) */

// <06FF0100> thirdparty/zstd/zstd.c:24566
// variables: 7
// function call: 1
ZSTD_compressionParameters ZSTD_adjustCParams(ZSTD_compressionParameters cPar, long long unsigned int srcSize, size_t dictSize)
{
	{
		const ZSTD_bounds  bounds; // 24367
	}
	{
		const ZSTD_bounds  bounds; // 24368
	}
	{
		const ZSTD_bounds  bounds; // 24369
	}
	{
		const ZSTD_bounds  bounds; // 24370
	}
	{
		const ZSTD_bounds  bounds; // 24371
	}
	{
		const ZSTD_bounds  bounds; // 24372
	}
	{
		const ZSTD_bounds  bounds; // 24373
	}
	ZSTD_clampCParams(ZSTD_compressionParameters cParams); // 24570
} /* size: 503, inline expansions: 1 (299 bytes) */

// <06FF00D7> thirdparty/zstd/zstd.c:24578
void ZSTD_overrideCParams(ZSTD_compressionParameters* cParams, const ZSTD_compressionParameters* overrides)
{
} /* size: 0 */

// <06FEFFA4> thirdparty/zstd/zstd.c:24591
// variable: 1
// function call: 1
ZSTD_compressionParameters ZSTD_getCParamsFromCCtxParams(const ZSTD_CCtx_params* CCtxParams, U64 srcSizeHint, size_t dictSize, ZSTD_CParamMode_e mode)
{
	ZSTD_compressionParameters cParams; // 24594
	ZSTD_overrideCParams(ZSTD_compressionParameters* cParams,
				const ZSTD_compressionParameters* overrides);  // 24601
} /* size: 303, variables: 1, inline expansions: 1 (34 bytes) */

// <06FEFEE8> thirdparty/zstd/zstd.c:24608
// variables: 9
size_t ZSTD_sizeof_matchState(const ZSTD_compressionParameters* cParams, const ZSTD_ParamSwitch_e useRowMatchFinder, const int enableDedicatedDictSearch, const U32 forCCtx)
{
	const size_t  chainSize; // 24614
	const size_t  hSize; // 24617
	const U32  hashLog3; // 24618
	const size_t  h3Size; // 24619
	const size_t  tableSpace; // 24622
	const size_t  optPotentialSpace; // 24625
	const size_t  lazyAdditionalSpace; // 24632
	const size_t  optSpace; // 24635
	const size_t  slackSpace; // 24638
} /* size: 0, variables: 9 */

// <06FEFEA1> thirdparty/zstd/zstd.c:24651
// variable: 1
size_t ZSTD_maxNbSeq(size_t blockSize, unsigned int minMatch, int useSequenceProducer)
{
	const U32  divider; // 24652
} /* size: 0, variables: 1 */

// <06FEF7B6> thirdparty/zstd/zstd.c:24656
// variables: 15
// function calls: 21
size_t ZSTD_estimateCCtxSize_usingCCtxParams_internal(const ZSTD_compressionParameters* cParams, const ldmParams_t* ldmParams, const int isStatic, const ZSTD_ParamSwitch_e useRowMatchFinder, const size_t buffInSize, const size_t buffOutSize, const U64 pledgedSrcSize, int useSequenceProducer, size_t maxBlockSize)
{
	const size_t  windowSize; // 24667
	const size_t  blockSize; // 24668
	const size_t  maxNbSeq; // 24669
	const size_t  tokenSpace; // 24670
	const size_t  tmpWorkSpace; // 24673
	const size_t  blockStateSpace; // 24674
	const size_t  matchStateSize; // 24675
	const size_t  ldmSpace; // 24677
	const size_t  maxNbLdmSeq; // 24678
	const size_t  ldmSeqSpace; // 24679
	const size_t  bufferSpace; // 24683
	const size_t  cctxSpace; // 24686
	const size_t  maxNbExternalSeq; // 24688
	const size_t  externalSeqSpace; // 24689
	const size_t  neededSpace; // 24693
	ZSTD_maxNbSeq(size_t blockSize,
			unsigned int minMatch,
			int useSequenceProducer);  // 24669
	ZSTD_resolveMaxBlockSize(size_t maxBlockSize); // 24668
	ZSTD_cwksp_align(size_t size,
			size_t align);  // 18508
	ZSTD_cwksp_aligned_alloc_size(size_t size,
					size_t alignment);  // 18516
	ZSTD_cwksp_aligned64_alloc_size(size_t size); // 24671
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
	ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
				const ZSTD_ParamSwitch_e  mode);  // 23216
	ZSTD_allocateChainTable(const ZSTD_strategy  strategy,
				const ZSTD_ParamSwitch_e  useRowMatchFinder,
				const U32  forDDSDict);  // 24614
	ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
				const ZSTD_ParamSwitch_e  mode);  // 24632
	ZSTD_cwksp_align(size_t size,
			size_t align);  // 18508
	ZSTD_cwksp_aligned_alloc_size(size_t size,
					size_t alignment);  // 18516
	ZSTD_cwksp_aligned64_alloc_size(size_t size); // 24633
	ZSTD_sizeof_matchState(const ZSTD_compressionParameters* cParams,
				const ZSTD_ParamSwitch_e  useRowMatchFinder,
				const int  enableDedicatedDictSearch,
				const U32  forCCtx);  // 24675
	ZSTD_ldm_getTableSize(ldmParams_t params); // 24677
	ZSTD_ldm_getMaxNbSeq(ldmParams_t params,
				size_t maxChunkSize);  // 24678
	ZSTD_cwksp_align(size_t size,
			size_t align);  // 18508
	ZSTD_cwksp_aligned_alloc_size(size_t size,
					size_t alignment);  // 18516
	ZSTD_cwksp_aligned64_alloc_size(size_t size); // 24680
	ZSTD_cwksp_align(size_t size,
			size_t align);  // 18508
	ZSTD_cwksp_aligned_alloc_size(size_t size,
					size_t alignment);  // 18516
	ZSTD_cwksp_aligned64_alloc_size(size_t size); // 24690
} /* size: 751, variables: 15, inline expansions: 21 (778 bytes) */

// <06FEF622> thirdparty/zstd/zstd.c:24708
// variables: 2
// function calls: 4
size_t ZSTD_estimateCCtxSize_usingCCtxParams(const ZSTD_CCtx_params* params)
{
	const ZSTD_compressionParameters  cParams; // 24710
	const ZSTD_ParamSwitch_e  useRowMatchFinder; // 24712
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 24712
	ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params); // 24719
} /* size: 192, variables: 2, inline expansions: 4 (83 bytes) */

// <06FEF503> thirdparty/zstd/zstd.c:24723
// variables: 3
// function call: 1
size_t ZSTD_estimateCCtxSize_usingCParams(ZSTD_compressionParameters cParams)
{
	ZSTD_CCtx_params initialParams; // 24725
	{
		size_t noRowCCtxSize; // 24728
		size_t rowCCtxSize; // 24729
	}
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 24726
} /* size: 194, variables: 1, inline expansions: 1 (6 bytes) */

// <06FEF48E> thirdparty/zstd/zstd.c:24740
// variables: 4
size_t ZSTD_estimateCCtxSize_internal(int compressionLevel)
{
	int tier; // 24742
	size_t largestSize; // 24743
	const long long unsigned int   srcSizeTiers; // 24744
	{
		const ZSTD_compressionParameters  cParams; // 24747
	}
} /* size: 0, variables: 3 */

// <06FEF377> thirdparty/zstd/zstd.c:24753
// variables: 4
// function call: 1
size_t ZSTD_estimateCCtxSize(int compressionLevel)
{
	int level; // 24755
	size_t memBudget; // 24756
	{
		const size_t  newMB; // 24759
		{
			const ZSTD_compressionParameters  cParams; // 24747
		}
		ZSTD_estimateCCtxSize_internal(int compressionLevel); // 24759
	}
} /* size: 272, variables: 2 */

// <06FEF313> thirdparty/zstd/zstd.c:24765
// variables: 5
size_t ZSTD_estimateCStreamSize_usingCCtxParams(const ZSTD_CCtx_params* params)
{
	{
		const ZSTD_compressionParameters  cParams; // 24768
		const size_t  blockSize; // 24770
		const size_t  inBuffSize; // 24771
		const size_t  outBuffSize; // 24774
		const ZSTD_ParamSwitch_e  useRowMatchFinder; // 24777
	}
} /* size: 0 */

// <06FEF1F4> thirdparty/zstd/zstd.c:24785
// variables: 3
// function call: 1
size_t ZSTD_estimateCStreamSize_usingCParams(ZSTD_compressionParameters cParams)
{
	ZSTD_CCtx_params initialParams; // 24787
	{
		size_t noRowCCtxSize; // 24790
		size_t rowCCtxSize; // 24791
	}
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 24788
} /* size: 194, variables: 1, inline expansions: 1 (6 bytes) */

// <06FEF1C7> thirdparty/zstd/zstd.c:24802
// variable: 1
size_t ZSTD_estimateCStreamSize_internal(int compressionLevel)
{
	const ZSTD_compressionParameters  cParams; // 24804
} /* size: 0, variables: 1 */

// <06FEF0B2> thirdparty/zstd/zstd.c:24808
// variables: 3
// function call: 1
size_t ZSTD_estimateCStreamSize(int compressionLevel)
{
	int level; // 24810
	size_t memBudget; // 24811
	{
		const size_t  newMB; // 24813
		ZSTD_estimateCStreamSize_internal(int compressionLevel); // 24813
	}
} /* size: 176, variables: 2 */

// <06FEF010> thirdparty/zstd/zstd.c:24823
// variables: 2
ZSTD_frameProgression ZSTD_getFrameProgression(const ZSTD_CCtx* cctx)
{
	{
		ZSTD_frameProgression fp; // 24830
		const size_t  buffered; // 24831
	}
} /* size: 155 */

// <06FEEFC6> thirdparty/zstd/zstd.c:24847
size_t ZSTD_toFlushNow(ZSTD_CCtx* cctx)
{
} /* size: 36 */

// <06FEEF9D> thirdparty/zstd/zstd.c:24858
void ZSTD_assertEqualCParams(ZSTD_compressionParameters cParams1, ZSTD_compressionParameters cParams2)
{
} /* size: 0 */

// <06FEEF72> thirdparty/zstd/zstd.c:24872
// variable: 1
void ZSTD_reset_compressedBlockState(ZSTD_compressedBlockState_t* bs)
{
	int i; // 24874
} /* size: 0, variables: 1 */

// <06FEEF57> thirdparty/zstd/zstd.c:24887
void ZSTD_invalidateMatchState(ZSTD_MatchState_t* ms)
{
} /* size: 0 */

// <06FEEF2A> thirdparty/zstd/zstd.c:24925
U64 ZSTD_bitmix(U64 val, U64 len)
{
} /* size: 0 */

// <06FEEF0F> thirdparty/zstd/zstd.c:24934
void ZSTD_advanceHashSalt(ZSTD_MatchState_t* ms)
{
} /* size: 0 */

// <06FED522> thirdparty/zstd/zstd.c:24939
// variables: 39
// function calls: 90
size_t ZSTD_reset_matchState(ZSTD_MatchState_t* ms, ZSTD_cwksp* ws, const ZSTD_compressionParameters* cParams, const ZSTD_ParamSwitch_e useRowMatchFinder, const ZSTD_compResetPolicy_e crp, const ZSTD_indexResetPolicy_e forceResetIndex, const ZSTD_resetTarget_e forWho)
{
	const size_t  chainSize; // 24948
	const size_t  hSize; // 24952
	const U32  hashLog3; // 24953
	const size_t  h3Size; // 24954
	{
		const size_t  tagTableSize; // 24988
		{
			const U32  rowLog; // 25001
		}
		ZSTD_cwksp_align(size_t size,
				size_t align);  // 18683
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18684
		ZSTD_cwksp_reserve_aligned_init_once(ZSTD_cwksp* ws,
							size_t bytes);  // 24992
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 24926
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 24926
		ZSTD_bitmix(U64 val,
				U64 len);  // 24935
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 24926
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 24926
		ZSTD_bitmix(U64 val,
				U64 len);  // 24935
		ZSTD_advanceHashSalt(ZSTD_MatchState_t* ms); // 24993
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18707
		ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
						size_t bytes);  // 24996
	}
	ZSTD_window_clear(ZSTD_window_t* window); // 24889
	ZSTD_invalidateMatchState(ZSTD_MatchState_t* ms); // 24966
	ZSTD_cwksp_clear_tables(ZSTD_cwksp* ws); // 24970
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18729
	ZSTD_cwksp_reserve_table(ZSTD_cwksp* ws,
				size_t bytes);  // 24974
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18729
	ZSTD_cwksp_reserve_table(ZSTD_cwksp* ws,
				size_t bytes);  // 24975
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18729
	ZSTD_cwksp_reserve_table(ZSTD_cwksp* ws,
				size_t bytes);  // 24976
	ZSTD_cwksp_reserve_failed(const ZSTD_cwksp* ws); // 24977
	ZSTD_cwksp_mark_tables_clean(ZSTD_cwksp* ws); // 18866
	ZSTD_cwksp_clean_tables(ZSTD_cwksp* ws); // 24983
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
	ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
				const ZSTD_ParamSwitch_e  mode);  // 24986
	ZSTD_cwksp_reserve_failed(const ZSTD_cwksp* ws); // 25020
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
	ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
				const ZSTD_ParamSwitch_e  mode);  // 23216
	ZSTD_allocateChainTable(const ZSTD_strategy  strategy,
				const ZSTD_ParamSwitch_e  useRowMatchFinder,
				const U32  forDDSDict);  // 24948
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18637
	ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
						const size_t  bytes);  // 18646
	ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
					size_t bytes,
					ZSTD_cwksp_alloc_phase_e phase);  // 18707
	ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
					size_t bytes);  // 25010
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18637
	ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
						const size_t  bytes);  // 18646
	ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
					size_t bytes,
					ZSTD_cwksp_alloc_phase_e phase);  // 18707
	ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
					size_t bytes);  // 25011
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18637
	ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
						const size_t  bytes);  // 18646
	ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
					size_t bytes,
					ZSTD_cwksp_alloc_phase_e phase);  // 18707
	ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
					size_t bytes);  // 25012
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18637
	ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
						const size_t  bytes);  // 18646
	ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
					size_t bytes,
					ZSTD_cwksp_alloc_phase_e phase);  // 18707
	ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
					size_t bytes);  // 25013
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18637
	ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
						const size_t  bytes);  // 18646
	ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
					size_t bytes,
					ZSTD_cwksp_alloc_phase_e phase);  // 18707
	ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
					size_t bytes);  // 25014
	ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
	{
		const void* alloc; // 18603
		const size_t  bytesToAlign; // 18604
		const void* objectEnd; // 18605
		ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
						const size_t  alignBytes);  // 18604
	}
	ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
	ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
						ZSTD_cwksp_alloc_phase_e phase);  // 18637
	ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
						const size_t  bytes);  // 18646
	ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
					size_t bytes,
					ZSTD_cwksp_alloc_phase_e phase);  // 18707
	ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
					size_t bytes);  // 25015
	ZSTD_window_init(ZSTD_window_t* window); // 24959
	ZSTD_cwksp_mark_tables_dirty(ZSTD_cwksp* ws); // 24960
} /* size: 2926, variables: 4, inline expansions: 59 (4237 bytes) */

// <06FED504> thirdparty/zstd/zstd.c:25033
int ZSTD_indexTooCloseToMax(ZSTD_window_t w)
{
} /* size: 0 */

// <06FED4E4> thirdparty/zstd/zstd.c:25043
int ZSTD_dictTooBig(const size_t loadedDictSize)
{
} /* size: 0 */

// <06FEB419> thirdparty/zstd/zstd.c:25054
// variables: 53
// function calls: 110
size_t ZSTD_resetCCtx_internal(ZSTD_CCtx* zc, const ZSTD_CCtx_params* params, const U64 pledgedSrcSize, const size_t loadedDictSize, const ZSTD_compResetPolicy_e crp, const ZSTD_buffered_policy_e zbuff)
{
	const ZSTD_cwksp* ws; // 25061
	{
		const size_t  windowSize; // 25085
		const size_t  blockSize; // 25086
		const size_t  maxNbSeq; // 25087
		const size_t  buffOutSize; // 25088
		const size_t  buffInSize; // 25091
		const size_t  maxNbLdmSeq; // 25094
		const int  indexTooClose; // 25096
		const int  dictTooBig; // 25097
		ZSTD_indexResetPolicy_e needsIndexReset; // 25098
		const size_t  neededSpace; // 25101
		{
			const size_t  err_code; // 25106
		}
		{
			const int  workspaceTooSmall; // 25111
			const int  workspaceWasteful; // 25112
			int resizeWorkspace; // 25113
			{
				const size_t  err_code; // 25127
				ZSTD_customMalloc(size_t size,
							ZSTD_customMem customMem);  // 18963
				ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18954
				ZSTD_cwksp_init(ZSTD_cwksp* ws,
						void* start,
						size_t size,
						ZSTD_cwksp_static_alloc_e isStatic);  // 18966
				ZSTD_cwksp_create(ZSTD_cwksp* ws,
							size_t size,
							ZSTD_customMem customMem);  // 25127
			}
			ZSTD_cwksp_sizeof(const ZSTD_cwksp* ws); // 25111
			ZSTD_cwksp_available_space(ZSTD_cwksp* ws); // 19016
			ZSTD_cwksp_check_available(ZSTD_cwksp* ws,
							size_t additionalNeededSpace);  // 19020
			ZSTD_cwksp_check_too_large(ZSTD_cwksp* ws,
							size_t additionalNeededSpace);  // 19025
			ZSTD_cwksp_check_wasteful(ZSTD_cwksp* ws,
							size_t additionalNeededSpace);  // 25112
			ZSTD_customFree(void* ptr,
					ZSTD_customMem customMem);  // 18979
			ZSTD_cwksp_free(ZSTD_cwksp* ws,
					ZSTD_customMem customMem);  // 25126
			ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
							size_t bytes);  // 25134
			ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
							size_t bytes);  // 25136
			ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
							size_t bytes);  // 25138
		}
		{
			const size_t  err_code; // 25164
		}
		{
			const size_t  ldmHSize; // 25178
			ZSTD_cwksp_align(size_t size,
					size_t align);  // 18707
			ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
			{
				const void* alloc; // 18603
				const size_t  bytesToAlign; // 18604
				const void* objectEnd; // 18605
				ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
								const size_t  alignBytes);  // 18604
			}
			ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
			ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
								ZSTD_cwksp_alloc_phase_e phase);  // 18637
			ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
								const size_t  bytes);  // 18646
			ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
							size_t bytes,
							ZSTD_cwksp_alloc_phase_e phase);  // 18707
			ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
							size_t bytes);  // 25179
			ZSTD_cwksp_align(size_t size,
					size_t align);  // 18707
			ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
			{
				const void* alloc; // 18603
				const size_t  bytesToAlign; // 18604
				const void* objectEnd; // 18605
				ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
								const size_t  alignBytes);  // 18604
			}
			ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
			ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
								ZSTD_cwksp_alloc_phase_e phase);  // 18637
			ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
								const size_t  bytes);  // 18646
			ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
							size_t bytes,
							ZSTD_cwksp_alloc_phase_e phase);  // 18707
			ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
							size_t bytes);  // 25181
			ZSTD_window_init(ZSTD_window_t* window); // 25184
		}
		{
			const size_t  maxNbExternalSeq; // 25190
			ZSTD_cwksp_align(size_t size,
					size_t align);  // 18707
			ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
			{
				const void* alloc; // 18603
				const size_t  bytesToAlign; // 18604
				const void* objectEnd; // 18605
				ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
								const size_t  alignBytes);  // 18604
			}
			ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
			ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
								ZSTD_cwksp_alloc_phase_e phase);  // 18637
			ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
								const size_t  bytes);  // 18646
			ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
							size_t bytes,
							ZSTD_cwksp_alloc_phase_e phase);  // 18707
			ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
							size_t bytes);  // 25193
		}
		{
			const size_t  numBuckets; // 25213
			ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
			{
				const void* alloc; // 18603
				const size_t  bytesToAlign; // 18604
				const void* objectEnd; // 18605
				ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
								const size_t  alignBytes);  // 18604
			}
			ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
			ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
								ZSTD_cwksp_alloc_phase_e phase);  // 18637
			ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
								const size_t  bytes);  // 18646
			ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
							size_t bytes,
							ZSTD_cwksp_alloc_phase_e phase);  // 18669
			ZSTD_cwksp_reserve_buffer(ZSTD_cwksp* ws,
							size_t bytes);  // 25216
		}
		ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params); // 25087
		ZSTD_maxNbSeq(size_t blockSize,
				unsigned int minMatch,
				int useSequenceProducer);  // 25087
		ZSTD_ldm_getMaxNbSeq(ldmParams_t params,
					size_t maxChunkSize);  // 25094
		ZSTD_indexTooCloseToMax(ZSTD_window_t w); // 25096
		ZSTD_dictTooBig(const size_t loadedDictSize); // 25097
		ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params); // 25102
		ZSTD_cwksp_bump_oversized_duration(ZSTD_cwksp* ws,
							size_t additionalNeededSpace);  // 25108
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18925
		ZSTD_cwksp_clear(ZSTD_cwksp* ws); // 25143
		memset(void* __dest,
			int __ch,
			size_t __len);  // 11842
		XXH_INLINE_XXH64_reset(XXH_NAMESPACEXXH64_state_t* statePtr,
					XXH64_hash_t seed);  // 25157
		ZSTD_reset_compressedBlockState(ZSTD_compressedBlockState_t* bs); // 25162
		ZSTD_cwksp_align(size_t size,
				size_t align);  // 18707
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18707
		ZSTD_cwksp_reserve_aligned64(ZSTD_cwksp* ws,
						size_t bytes);  // 25173
		ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params); // 25189
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18669
		ZSTD_cwksp_reserve_buffer(ZSTD_cwksp* ws,
						size_t bytes);  // 25201
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18669
		ZSTD_cwksp_reserve_buffer(ZSTD_cwksp* ws,
						size_t bytes);  // 25206
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18669
		ZSTD_cwksp_reserve_buffer(ZSTD_cwksp* ws,
						size_t bytes);  // 25208
		ZSTD_referenceExternalSequences(ZSTD_CCtx* cctx,
						rawSeq* seq,
						size_t nbSeq);  // 25221
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18669
		ZSTD_cwksp_reserve_buffer(ZSTD_cwksp* ws,
						size_t bytes);  // 25223
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18669
		ZSTD_cwksp_reserve_buffer(ZSTD_cwksp* ws,
						size_t bytes);  // 25224
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18600
		{
			const void* alloc; // 18603
			const size_t  bytesToAlign; // 18604
			const void* objectEnd; // 18605
			ZSTD_cwksp_bytes_to_align_ptr(void* ptr,
							const size_t  alignBytes);  // 18604
		}
		ZSTD_cwksp_assert_internal_consistency(ZSTD_cwksp* ws); // 18617
		ZSTD_cwksp_internal_advance_phase(ZSTD_cwksp* ws,
							ZSTD_cwksp_alloc_phase_e phase);  // 18637
		ZSTD_cwksp_reserve_internal_buffer_space(ZSTD_cwksp* ws,
							const size_t  bytes);  // 18646
		ZSTD_cwksp_reserve_internal(ZSTD_cwksp* ws,
						size_t bytes,
						ZSTD_cwksp_alloc_phase_e phase);  // 18669
		ZSTD_cwksp_reserve_buffer(ZSTD_cwksp* ws,
						size_t bytes);  // 25225
	}
} /* size: 4530, variables: 1 */

// <06FEB3ED> thirdparty/zstd/zstd.c:25240
// variable: 1
void ZSTD_invalidateRepCodes(ZSTD_CCtx* cctx)
{
	int i; // 25241
} /* size: 0, variables: 1 */

// <06FEB399> thirdparty/zstd/zstd.c:25263
// variables: 2
int ZSTD_shouldAttachDict(const ZSTD_CDict* cdict, const ZSTD_CCtx_params* params, U64 pledgedSrcSize)
{
	size_t cutoff; // 25267
	const int  dedicatedDictSearch; // 25268
} /* size: 0, variables: 2 */

// <06FEB2FA> thirdparty/zstd/zstd.c:25279
// variables: 5
size_t ZSTD_resetCCtx_byAttachingCDict(ZSTD_CCtx* cctx, const ZSTD_CDict* cdict, ZSTD_CCtx_params params, U64 pledgedSrcSize, ZSTD_buffered_policy_e zbuff)
{
	{
		ZSTD_compressionParameters adjusted_cdict_cParams; // 25288
		const unsigned int  windowLog; // 25289
		{
			const size_t  err_code; // 25304
		}
	}
	{
		const U32  cdictEnd; // 25310
		const U32  cdictLen; // 25312
	}
} /* size: 0 */

// <06FEB28E> thirdparty/zstd/zstd.c:25340
// variables: 3
void ZSTD_copyCDictTableIntoCCtx(U32* dst, const U32* src, size_t tableSize, const ZSTD_compressionParameters* cParams)
{
	{
		size_t i; // 25345
		{
			const U32  taggedIndex; // 25347
			const U32  index; // 25348
		}
	}
} /* size: 0 */

// <06FEB1A0> thirdparty/zstd/zstd.c:25356
// variables: 10
size_t ZSTD_resetCCtx_byCopyingCDict(ZSTD_CCtx* cctx, const ZSTD_CDict* cdict, ZSTD_CCtx_params params, U64 pledgedSrcSize, ZSTD_buffered_policy_e zbuff)
{
	const ZSTD_compressionParameters* cdict_cParams; // 25362
	{
		const unsigned int  windowLog; // 25368
		{
			const size_t  err_code; // 25374
		}
	}
	{
		const size_t  chainSize; // 25386
		const size_t  hSize; // 25389
		{
			const size_t  tagTableSize; // 25403
		}
	}
	{
		const U32  h3log; // 25413
		const size_t  h3Size; // 25414
	}
	{
		const ZSTD_MatchState_t* srcMatchState; // 25422
		ZSTD_MatchState_t* dstMatchState; // 25423
	}
} /* size: 0, variables: 1 */

// <06FEB14C> thirdparty/zstd/zstd.c:25441
size_t ZSTD_resetCCtx_usingCDict(ZSTD_CCtx* cctx, const ZSTD_CDict* cdict, const ZSTD_CCtx_params* params, U64 pledgedSrcSize, ZSTD_buffered_policy_e zbuff)
{
} /* size: 0 */

// <06FEB08F> thirdparty/zstd/zstd.c:25467
// variables: 7
size_t ZSTD_copyCCtx_internal(ZSTD_CCtx* dstCCtx, const ZSTD_CCtx* srcCCtx, ZSTD_frameParameters fParams, U64 pledgedSrcSize, ZSTD_buffered_policy_e zbuff)
{
	{
		ZSTD_CCtx_params params; // 25477
	}
	{
		const size_t  chainSize; // 25501
		const size_t  hSize; // 25506
		const U32  h3log; // 25507
		const size_t  h3Size; // 25508
	}
	{
		const ZSTD_MatchState_t* srcMatchState; // 25525
		ZSTD_MatchState_t* dstMatchState; // 25526
	}
} /* size: 0 */

// <06FEAD5C> thirdparty/zstd/zstd.c:25545
// variables: 9
// function calls: 6
size_t ZSTD_copyCCtx(ZSTD_CCtx* dstCCtx, const ZSTD_CCtx* srcCCtx, long long unsigned int pledgedSrcSize)
{
	ZSTD_frameParameters fParams; // 25547
	const ZSTD_buffered_policy_e  zbuff; // 25548
	{
		ZSTD_CCtx_params params; // 25477
	}
	{
		const size_t  chainSize; // 25501
		const size_t  hSize; // 25506
		const U32  h3log; // 25507
		const size_t  h3Size; // 25508
		ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
		ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  mode);  // 23216
		ZSTD_allocateChainTable(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  useRowMatchFinder,
					const U32  forDDSDict);  // 25501
	}
	ZSTD_cwksp_mark_tables_dirty(ZSTD_cwksp* ws); // 25498
	ZSTD_cwksp_mark_tables_clean(ZSTD_cwksp* ws); // 25521
	{
		const ZSTD_MatchState_t* srcMatchState; // 25525
		ZSTD_MatchState_t* dstMatchState; // 25526
	}
	ZSTD_copyCCtx_internal(ZSTD_CCtx* dstCCtx,
				const ZSTD_CCtx* srcCCtx,
				ZSTD_frameParameters fParams,
				U64 pledgedSrcSize,
				ZSTD_buffered_policy_e zbuff);  // 25553
} /* size: 777, variables: 2, inline expansions: 3 (691 bytes) */

// <06FEACC7> thirdparty/zstd/zstd.c:25567
// variables: 6
inline void ZSTD_reduceTable_internal(const U32* table, const U32 size, const U32 reducerValue, const int preserveMark)
{
	const int  nbRows; // 25569
	int cellNb; // 25570
	int rowNb; // 25571
	const U32  reducerThreshold; // 25573
	{
		int column; // 25591
		{
			U32 newVal; // 25593
		}
	}
} /* size: 0, variables: 4 */

// <06FEAC91> thirdparty/zstd/zstd.c:25608
void ZSTD_reduceTable(const U32* table, const U32 size, const U32 reducerValue)
{
} /* size: 0 */

// <06FEAC5B> thirdparty/zstd/zstd.c:25613
void ZSTD_reduceTable_btlazy2(const U32* table, const U32 size, const U32 reducerValue)
{
} /* size: 0 */

// <06FEABF1> thirdparty/zstd/zstd.c:25620
// variables: 3
void ZSTD_reduceIndex(ZSTD_MatchState_t* ms, const ZSTD_CCtx_params* params, const U32 reducerValue)
{
	{
		const U32  hSize; // 25622
	}
	{
		const U32  chainSize; // 25627
	}
	{
		const U32  h3Size; // 25635
	}
} /* size: 0 */

// <06FEA9B1> thirdparty/zstd/zstd.c:25647
// variables: 10
// function calls: 8
int ZSTD_seqToCodes(const SeqStore_t* seqStorePtr)
{
	const SeqDef* sequences; // 25649
	const BYTE* llCodeTable; // 25650
	const BYTE* ofCodeTable; // 25651
	const BYTE* mlCodeTable; // 25652
	const U32  nbSeq; // 25653
	U32 u; // 25654
	int longOffsets; // 25655
	{
		const U32  llv; // 25658
		const U32  ofCode; // 25659
		const U32  mlv; // 25660
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 19749
		ZSTD_LLcode(U32 litLength); // 25661
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 19766
		ZSTD_MLcode(U32 mlBase); // 25663
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 25659
	}
} /* size: 291, variables: 7 */

// <06FEA991> thirdparty/zstd/zstd.c:25679
int ZSTD_useTargetCBlockSize(const ZSTD_CCtx_params* cctxParams)
{
} /* size: 0 */

// <06FEA971> thirdparty/zstd/zstd.c:25690
int ZSTD_blockSplitterEnabled(ZSTD_CCtx_params* cctxParams)
{
} /* size: 0 */

// <06F3D611> thirdparty/zstd/zstd.c:25700
// member variables: 6
// struct size: 40
typedef struct {
	U32 LLtype; /* 0 4 */
	U32 Offtype; /* 4 4 */
	U32 MLtype; /* 8 4 */
	size_t size; /* 16 8 */
	size_t lastCountSize; /* 24 8 */
	int longOffsets; /* 32 4 */
} ZSTD_symbolEncodingTypeStats_t;

// <06FEA160> thirdparty/zstd/zstd.c:25717
// variables: 20
// function calls: 6
ZSTD_symbolEncodingTypeStats_t ZSTD_buildSequencesStatistics(const SeqStore_t* seqStorePtr, size_t nbSeq, const ZSTD_fseCTables_t* prevEntropy, ZSTD_fseCTables_t* nextEntropy, BYTE* dst, const BYTE* dstEnd, ZSTD_strategy strategy, unsigned int* countWorkspace, void* entropyWorkspace, size_t entropyWkspSize)
{
	const BYTE* ostart; // 25724
	const BYTE* oend; // 25725
	BYTE* op; // 25726
	FSE_CTable* CTable_LitLength; // 25727
	FSE_CTable* CTable_OffsetBits; // 25728
	FSE_CTable* CTable_MatchLength; // 25729
	const BYTE* ofCodeTable; // 25730
	const BYTE* llCodeTable; // 25731
	const BYTE* mlCodeTable; // 25732
	ZSTD_symbolEncodingTypeStats_t stats; // 25733
	{
		unsigned int max; // 25741
		const size_t  mostFrequent; // 25742
		{
			const size_t  countSize; // 25752
		}
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16714
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 25742
	}
	{
		unsigned int max; // 25771
		const size_t  mostFrequent; // 25772
		const ZSTD_DefaultPolicy_e  defaultPolicy; // 25775
		{
			const size_t  countSize; // 25784
		}
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16714
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 25772
	}
	{
		unsigned int max; // 25803
		const size_t  mostFrequent; // 25804
		{
			const size_t  countSize; // 25814
		}
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16714
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 25804
	}
} /* size: 1302, variables: 10 */

// <06FE9B87> thirdparty/zstd/zstd.c:25842
// variables: 24
// function calls: 5
size_t ZSTD_entropyCompressSeqStore_internal(void* dst, size_t dstCapacity, const void* literals, size_t litSize, const SeqStore_t* seqStorePtr, const ZSTD_entropyCTables_t* prevEntropy, ZSTD_entropyCTables_t* nextEntropy, const ZSTD_CCtx_params* cctxParams, void* entropyWorkspace, size_t entropyWkspSize, const int bmi2)
{
	const ZSTD_strategy  strategy; // 25852
	unsigned int* count; // 25853
	FSE_CTable* CTable_LitLength; // 25854
	FSE_CTable* CTable_OffsetBits; // 25855
	FSE_CTable* CTable_MatchLength; // 25856
	const SeqDef* sequences; // 25857
	const size_t  nbSeq; // 25858
	const BYTE* ofCodeTable; // 25859
	const BYTE* llCodeTable; // 25860
	const BYTE* mlCodeTable; // 25861
	const BYTE* ostart; // 25862
	const BYTE* oend; // 25863
	BYTE* op; // 25864
	size_t lastCountSize; // 25865
	int longOffsets; // 25866
	{
		const size_t  numSequences; // 25876
		const int  suspectUncompressible; // 25878
		const size_t  cSize; // 25880
		{
			const size_t  err_code; // 25888
		}
		ZSTD_literalsCompressionIsDisabled(const ZSTD_CCtx_params* cctxParams); // 19839
		ZSTD_literalsCompressionIsDisabled(const ZSTD_CCtx_params* cctxParams); // 25880
	}
	{
		const BYTE* seqHead; // 25913
		const ZSTD_symbolEncodingTypeStats_t  stats; // 25915
		{
			const size_t  err_code; // 25921
		}
	}
	{
		const size_t  bitstreamSize; // 25928
		{
			const size_t  err_code; // 25935
		}
		ZSTD_encodeSequences(void* dst,
					size_t dstCapacity,
					const FSE_CTable* CTable_MatchLength,
					const BYTE* mlCodeTable,
					const FSE_CTable* CTable_OffsetBits,
					const BYTE* ofCodeTable,
					const FSE_CTable* CTable_LitLength,
					const BYTE* llCodeTable,
					const SeqDef* sequences,
					size_t nbSeq,
					int longOffsets,
					int bmi2);  // 25928
	}
	MEM_write16(void* memPtr,
			U16 value);  // 1279
	MEM_writeLE16(void* memPtr,
			U16 val);  // 25904
} /* size: 844, variables: 15, inline expansions: 2 (0 bytes) */

// <06FE9AA9> thirdparty/zstd/zstd.c:25960
// variables: 3
size_t ZSTD_entropyCompressSeqStore_wExtLitBuffer(void* dst, size_t dstCapacity, const void* literals, size_t litSize, size_t blockSize, const SeqStore_t* seqStorePtr, const ZSTD_entropyCTables_t* prevEntropy, ZSTD_entropyCTables_t* nextEntropy, const ZSTD_CCtx_params* cctxParams, void* entropyWorkspace, size_t entropyWkspSize, int bmi2)
{
	const size_t  cSize; // 25971
	{
		const size_t  err_code; // 25984
	}
	{
		const size_t  maxCSize; // 25987
	}
} /* size: 0, variables: 1 */

// <07096506> thirdparty/zstd/zstd.c:25999
// variables: 2
// function calls: 2
size_t ZSTD_entropyCompressSeqStore(const SeqStore_t* seqStorePtr, const ZSTD_entropyCTables_t* prevEntropy, ZSTD_entropyCTables_t* nextEntropy, const ZSTD_CCtx_params* cctxParams, void* dst, size_t dstCapacity, size_t srcSize, void* entropyWorkspace, size_t entropyWkspSize, int bmi2)
{
	{
		const size_t  err_code; // 25984
	}
	{
		const size_t  maxCSize; // 25987
		ZSTD_minGain(size_t srcSize,
				ZSTD_strategy strat);  // 25987
	}
	ZSTD_entropyCompressSeqStore_wExtLitBuffer(void* dst,
							size_t dstCapacity,
							const void* literals,
							size_t litSize,
							size_t blockSize,
							const SeqStore_t* seqStorePtr,
							const ZSTD_entropyCTables_t* prevEntropy,
							ZSTD_entropyCTables_t* nextEntropy,
							const ZSTD_CCtx_params* cctxParams,
							void* entropyWorkspace,
							size_t entropyWkspSize,
							int bmi2);  // 26009
} /* size: 162, inline expansions: 1 (108 bytes) */

// <06FE9A14> thirdparty/zstd/zstd.c:25999
size_t ZSTD_entropyCompressSeqStore(const SeqStore_t* seqStorePtr, const ZSTD_entropyCTables_t* prevEntropy, ZSTD_entropyCTables_t* nextEntropy, const ZSTD_CCtx_params* cctxParams, void* dst, size_t dstCapacity, size_t srcSize, void* entropyWorkspace, size_t entropyWkspSize, int bmi2)
{
} /* size: 0 */

// <06FE9962> thirdparty/zstd/zstd.c:26023
// variables: 3
ZSTD_BlockCompressor_f ZSTD_selectBlockCompressor(ZSTD_strategy strat, ZSTD_ParamSwitch_e useRowMatchFinder, ZSTD_dictMode_e dictMode)
{
	const ZSTD_BlockCompressor_f   blockCompressor; // 26025
	ZSTD_BlockCompressor_f selectedCompressor; // 26070
	{
		const ZSTD_BlockCompressor_f   rowBasedBlockCompressors; // 26076
	}
} /* size: 0, variables: 2 */

// <06FE992C> thirdparty/zstd/zstd.c:26108
void ZSTD_storeLastLiterals(SeqStore_t* seqStorePtr, const BYTE* anchor, size_t lastLLSize)
{
} /* size: 0 */

// <06FE990B> thirdparty/zstd/zstd.c:26115
void ZSTD_resetSeqStore(SeqStore_t* ssPtr)
{
} /* size: 0 */

// <06FE98B5> thirdparty/zstd/zstd.c:26128
// variable: 1
size_t ZSTD_postProcessSequenceProducerResult(ZSTD_Sequence* outSeqs, size_t nbExternalSeqs, size_t outSeqsCapacity, size_t srcSize)
{
	{
		const ZSTD_Sequence  lastSeq; // 26150
	}
} /* size: 0 */

// <06FE9863> thirdparty/zstd/zstd.c:26177
// variables: 3
size_t ZSTD_fastSequenceLengthSum(const ZSTD_Sequence* seqBuf, size_t seqBufSize)
{
	size_t matchLenSum; // 26178
	size_t litLenSum; // 26178
	size_t i; // 26178
} /* size: 0, variables: 3 */

// <06FE983A> thirdparty/zstd/zstd.c:26191
void ZSTD_validateSeqStore(const SeqStore_t* seqStore, const ZSTD_compressionParameters* cParams)
{
} /* size: 0 */

// <06FE90DA> thirdparty/zstd/zstd.c:26218
// variables: 19
// function calls: 14
size_t ZSTD_buildSeqStore(ZSTD_CCtx* zc, const void* src, size_t srcSize)
{
	const ZSTD_MatchState_t* ms; // 26220
	{
		const BYTE* base; // 26246
		const BYTE* istart; // 26247
		const U32  curr; // 26248
	}
	{
		const ZSTD_dictMode_e  dictMode; // 26255
		size_t lastLLSize; // 26256
		{
			int i; // 26257
		}
		{
			RawSeqStore_t ldmSeqStore; // 26281
			{
				const size_t  err_code; // 26294
			}
		}
		{
			const U32  windowSize; // 26311
			const size_t  nbExternalSeqs; // 26313
			const size_t  nbPostProcessedSeqs; // 26323
			{
				ZSTD_SequencePosition seqPos; // 26332
				const size_t  seqLenSum; // 26333
				{
					const size_t  err_code; // 26335
				}
				ZSTD_fastSequenceLengthSum(const ZSTD_Sequence* seqBuf,
								size_t seqBufSize);  // 26333
			}
			{
				const ZSTD_BlockCompressor_f  blockCompressor; // 26355
				ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
				ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
							const ZSTD_ParamSwitch_e  mode);  // 26075
				{
				}
				ZSTD_selectBlockCompressor(ZSTD_strategy strat,
								ZSTD_ParamSwitch_e useRowMatchFinder,
								ZSTD_dictMode_e dictMode);  // 26356
			}
			{
				const ZSTD_Sequence  lastSeq; // 26150
			}
			ZSTD_postProcessSequenceProducerResult(ZSTD_Sequence* outSeqs,
								size_t nbExternalSeqs,
								size_t outSeqsCapacity,
								size_t srcSize);  // 26323
		}
		{
			const ZSTD_BlockCompressor_f  blockCompressor; // 26369
			ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
			ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
						const ZSTD_ParamSwitch_e  mode);  // 26075
			{
			}
			ZSTD_selectBlockCompressor(ZSTD_strategy strat,
							ZSTD_ParamSwitch_e useRowMatchFinder,
							ZSTD_dictMode_e dictMode);  // 26369
		}
		{
			const BYTE* lastLiterals; // 26376
			ZSTD_storeLastLiterals(SeqStore_t* seqStorePtr,
						const BYTE* anchor,
						size_t lastLLSize);  // 26377
		}
		ZSTD_window_hasExtDict(const ZSTD_window_t window); // 20227
		ZSTD_matchState_dictMode(const ZSTD_MatchState_t* ms); // 20225
		ZSTD_matchState_dictMode(const ZSTD_MatchState_t* ms); // 26255
		ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params); // 26266
	}
	ZSTD_resetSeqStore(SeqStore_t* ssPtr); // 26235
} /* size: 1446, variables: 1, inline expansions: 1 (41 bytes) */

// <06FE8EBE> thirdparty/zstd/zstd.c:26383
// variables: 13
// function calls: 2
size_t ZSTD_copyBlockSequences(SeqCollector* seqCollector, const SeqStore_t* seqStore, const U32* prevRepcodes)
{
	const SeqDef* inSeqs; // 26385
	const size_t  nbInSequences; // 26386
	const size_t  nbInLiterals; // 26387
	ZSTD_Sequence* outSeqs; // 26389
	const size_t  nbOutSequences; // 26390
	size_t nbOutLiterals; // 26391
	Repcodes_t repcodes; // 26392
	size_t i; // 26393
	{
		U32 rawOffset; // 26406
		{
			const U32  repcode; // 26425
		}
		{
			const U32  repCode; // 19979
			{
				const U32  currentOffset; // 19981
			}
		}
		ZSTD_updateRep(U32* rep,
				const U32  offBase,
				const U32  ll0);  // 19972
		ZSTD_updateRep(U32* rep,
				const U32  offBase,
				const U32  ll0);  // 26444
	}
	{
		const size_t  lastLLSize; // 26456
	}
} /* size: 607, variables: 8 */

// <06FE8E58> thirdparty/zstd/zstd.c:26468
// variables: 2
size_t ZSTD_sequenceBound(size_t srcSize)
{
	const size_t  maxNbSeq; // 26469
	const size_t  maxNbDelims; // 26470
} /* size: 30, variables: 2 */

// <06FE8B91> thirdparty/zstd/zstd.c:26474
// variables: 9
// function calls: 2
size_t ZSTD_generateSequences(ZSTD_CCtx* zc, ZSTD_Sequence* outSeqs, size_t outSeqsSize, const void* src, size_t srcSize)
{
	const size_t  dstCapacity; // 26477
	void* dst; // 26478
	SeqCollector seqCollector; // 26479
	{
		int targetCBlockSize; // 26481
		{
			const size_t  err_code; // 26482
		}
	}
	{
		int nbWorkers; // 26486
		{
			const size_t  err_code; // 26487
		}
	}
	{
		const size_t  ret; // 26501
		{
			const size_t  err_code; // 26503
		}
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 26502
	}
	ZSTD_customMalloc(size_t size,
				ZSTD_customMem customMem);  // 26491
} /* size: 313, variables: 3, inline expansions: 1 (11 bytes) */

// <06FE8B17> thirdparty/zstd/zstd.c:26509
// variables: 2
size_t ZSTD_mergeBlockDelimiters(ZSTD_Sequence* sequences, size_t seqsSize)
{
	size_t in; // 26510
	size_t out; // 26511
} /* size: 126, variables: 2 */

// <06FE8725> thirdparty/zstd/zstd.c:26526
// variables: 10
// function calls: 14
int ZSTD_isRLE(const BYTE* src, size_t length)
{
	const BYTE* ip; // 26527
	const BYTE  value; // 26528
	const size_t  valueST; // 26529
	const size_t  unrollSize; // 26530
	const size_t  unrollMask; // 26531
	const size_t  prefixLength; // 26532
	size_t i; // 26533
	{
		size_t u; // 26540
		MEM_readST(const void* ptr); // 26542
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 26536
} /* size: 322, variables: 7, inline expansions: 5 (192 bytes) */

// <06FE86EB> thirdparty/zstd/zstd.c:26552
// variables: 2
int ZSTD_maybeRLE(const SeqStore_t* seqStore)
{
	const size_t  nbSeqs; // 26554
	const size_t  nbLits; // 26555
} /* size: 0, variables: 2 */

// <06FE86B8> thirdparty/zstd/zstd.c:26561
// variable: 1
void ZSTD_blockState_confirmRepcodesAndEntropyTables(const ZSTD_blockState_t* bs)
{
	const ZSTD_compressedBlockState_t* tmp; // 26563
} /* size: 0, variables: 1 */

// <06FE8669> thirdparty/zstd/zstd.c:26570
// variable: 1
void writeBlockHeader(void* op, size_t cSize, size_t blockSize, U32 lastBlock)
{
	const U32  cBlockHeader; // 26572
} /* size: 0, variables: 1 */

// <06FE84D1> thirdparty/zstd/zstd.c:26587
// variables: 18
size_t ZSTD_buildBlockEntropyStats_literals(const void* src, size_t srcSize, const ZSTD_hufCTables_t* prevHuf, ZSTD_hufCTables_t* nextHuf, ZSTD_hufCTablesMetadata_t* hufMetadata, const int literalsCompressionIsDisabled, void* workspace, size_t wkspSize, int hufFlags)
{
	const BYTE* wkspStart; // 26595
	const BYTE* wkspEnd; // 26596
	const BYTE* countWkspStart; // 26597
	const unsigned int* countWksp; // 26598
	const size_t  countWkspSize; // 26599
	const BYTE* nodeWksp; // 26600
	const size_t  nodeWkspSize; // 26601
	unsigned int maxSymbolValue; // 26602
	unsigned int huffLog; // 26603
	HUF_repeat repeat; // 26604
	{
		const size_t  minLitSize; // 26620
	}
	{
		const size_t  largest; // 26628
		{
			const size_t  err_code; // 26632
		}
	}
	{
		const size_t  maxBits; // 26656
		{
			const size_t  err_code; // 26659
		}
	}
	{
		const size_t  newCSize; // 26663
		const size_t  hSize; // 26665
		{
			const size_t  oldCSize; // 26671
		}
	}
} /* size: 0, variables: 10 */

// <06FE84A4> thirdparty/zstd/zstd.c:26698
// variable: 1
ZSTD_symbolEncodingTypeStats_t ZSTD_buildDummySequencesStatistics(ZSTD_fseCTables_t* nextEntropy)
{
	ZSTD_symbolEncodingTypeStats_t stats; // 26700
} /* size: 0, variables: 1 */

// <06FE83A7> thirdparty/zstd/zstd.c:26713
// variables: 10
size_t ZSTD_buildBlockEntropyStats_sequences(const SeqStore_t* seqStorePtr, const ZSTD_fseCTables_t* prevEntropy, ZSTD_fseCTables_t* nextEntropy, const ZSTD_CCtx_params* cctxParams, ZSTD_fseCTablesMetadata_t* fseMetadata, void* workspace, size_t wkspSize)
{
	const ZSTD_strategy  strategy; // 26721
	const size_t  nbSeq; // 26722
	const BYTE* ostart; // 26723
	const BYTE* oend; // 26724
	BYTE* op; // 26725
	unsigned int* countWorkspace; // 26726
	unsigned int* entropyWorkspace; // 26727
	size_t entropyWorkspaceSize; // 26728
	ZSTD_symbolEncodingTypeStats_t stats; // 26729
	{
		const size_t  err_code; // 26737
	}
} /* size: 0, variables: 9 */

// <06FE78A6> thirdparty/zstd/zstd.c:26752
// variables: 14
// function calls: 17
size_t ZSTD_buildBlockEntropyStats(const SeqStore_t* seqStorePtr, const ZSTD_entropyCTables_t* prevEntropy, ZSTD_entropyCTables_t* nextEntropy, const ZSTD_CCtx_params* cctxParams, ZSTD_entropyCTablesMetadata_t* entropyMetadata, void* workspace, size_t wkspSize)
{
	const size_t  litSize; // 26760
	const int  huf_useOptDepth; // 26761
	const int  hufFlags; // 26762
	{
		const size_t  err_code; // 26771
	}
	{
		const size_t  err_code; // 26778
	}
	ZSTD_literalsCompressionIsDisabled(const ZSTD_CCtx_params* cctxParams); // 19839
	ZSTD_literalsCompressionIsDisabled(const ZSTD_CCtx_params* cctxParams); // 26765
	{
		const size_t  newCSize; // 26663
		const size_t  hSize; // 26665
		HUF_getNbBits(HUF_CElt elt); // 17556
		HUF_estimateCompressedSize(const HUF_CElt* CTable,
						const unsigned int* count,
						unsigned int maxSymbolValue);  // 26663
		{
			const size_t  oldCSize; // 26671
			HUF_getNbBits(HUF_CElt elt); // 17556
			HUF_estimateCompressedSize(const HUF_CElt* CTable,
							const unsigned int* count,
							unsigned int maxSymbolValue);  // 26671
		}
	}
	{
		const size_t  minLitSize; // 26620
	}
	{
		const size_t  largest; // 26628
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16714
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16737
		HIST_count_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16728
		HIST_count_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 26629
		{
			const size_t  err_code; // 26632
		}
	}
	{
		const size_t  maxBits; // 26656
		{
			const size_t  err_code; // 26659
		}
	}
	HUF_readCTableHeader(const HUF_CElt* ctable); // 17562
	HUF_getNbBits(HUF_CElt elt); // 17573
	HUF_validateCTable(const HUF_CElt* CTable,
				const unsigned int* count,
				unsigned int maxSymbolValue);  // 17561
	HUF_validateCTable(const HUF_CElt* CTable,
				const unsigned int* count,
				unsigned int maxSymbolValue);  // 26648
	ZSTD_buildBlockEntropyStats_literals(const void* src,
						size_t srcSize,
						const ZSTD_hufCTables_t* prevHuf,
						ZSTD_hufCTables_t* nextHuf,
						ZSTD_hufCTablesMetadata_t* hufMetadata,
						const int  literalsCompressionIsDisabled,
						void* workspace,
						size_t wkspSize,
						int hufFlags);  // 26765
	ZSTD_buildDummySequencesStatistics(ZSTD_fseCTables_t* nextEntropy); // 26736
	{
		const size_t  err_code; // 26737
	}
	ZSTD_buildBlockEntropyStats_sequences(const SeqStore_t* seqStorePtr,
						const ZSTD_fseCTables_t* prevEntropy,
						ZSTD_fseCTables_t* nextEntropy,
						const ZSTD_CCtx_params* cctxParams,
						ZSTD_fseCTablesMetadata_t* fseMetadata,
						void* workspace,
						size_t wkspSize);  // 26773
} /* size: 1592, variables: 3, inline expansions: 9 (1446 bytes) */

// <06FE77DA> thirdparty/zstd/zstd.c:26784
// variables: 6
size_t ZSTD_estimateBlockSize_literal(const BYTE* literals, size_t litSize, const ZSTD_hufCTables_t* huf, const ZSTD_hufCTablesMetadata_t* hufMetadata, void* workspace, size_t wkspSize, int writeEntropy)
{
	const unsigned int* countWksp; // 26790
	unsigned int maxSymbolValue; // 26791
	size_t literalSectionHeaderSize; // 26792
	U32 singleStream; // 26793
	{
		const size_t  largest; // 26798
		{
			size_t cLitSizeEstimate; // 26800
		}
	}
} /* size: 0, variables: 4 */

// <07095835> thirdparty/zstd/zstd.c:26811
// variables: 8
// function calls: 3
size_t ZSTD_estimateBlockSize_symbolType(SymbolEncodingType_e type, const BYTE* codeTable, size_t nbSeq, unsigned int maxCode, const FSE_CTable* fseCTable, const U8* additionalBits, const short int* defaultNorm, U32 defaultNormLog, void* workspace, size_t wkspSize, U32 defaultMax)
{
	const unsigned int* countWksp; // 26818
	const BYTE* ctp; // 26819
	const BYTE* ctStart; // 26820
	const BYTE* ctEnd; // 26821
	size_t cSymbolTypeSizeEstimateInBits; // 26822
	unsigned int max; // 26823
	HIST_countFast_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16714
	HIST_countFast_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 26825
	{
		const unsigned int  normAcc; // 21243
		const unsigned int  norm256; // 21244
	}
	ZSTD_crossEntropyCost(const short int* norm,
				unsigned int accuracyLog,
				const unsigned int* count,
				const unsigned int  max);  // 26830
} /* size: 500, variables: 6, inline expansions: 3 (227 bytes) */

// <06FE76E4> thirdparty/zstd/zstd.c:26811
// variables: 6
size_t ZSTD_estimateBlockSize_symbolType(SymbolEncodingType_e type, const BYTE* codeTable, size_t nbSeq, unsigned int maxCode, const FSE_CTable* fseCTable, const U8* additionalBits, const short int* defaultNorm, U32 defaultNormLog, U32 defaultMax, void* workspace, size_t wkspSize)
{
	const unsigned int* countWksp; // 26818
	const BYTE* ctp; // 26819
	const BYTE* ctStart; // 26820
	const BYTE* ctEnd; // 26821
	size_t cSymbolTypeSizeEstimateInBits; // 26822
	unsigned int max; // 26823
} /* size: 0, variables: 6 */

// <06FE7636> thirdparty/zstd/zstd.c:26849
// variables: 2
size_t ZSTD_estimateBlockSize_sequences(const BYTE* ofCodeTable, const BYTE* llCodeTable, const BYTE* mlCodeTable, size_t nbSeq, const ZSTD_fseCTables_t* fseTables, const ZSTD_fseCTablesMetadata_t* fseMetadata, void* workspace, size_t wkspSize, int writeEntropy)
{
	size_t sequencesSectionHeaderSize; // 26858
	size_t cSeqSizeEstimate; // 26859
} /* size: 0, variables: 2 */

// <06FE7567> thirdparty/zstd/zstd.c:26878
// variables: 2
size_t ZSTD_estimateBlockSize(const BYTE* literals, size_t litSize, const BYTE* ofCodeTable, const BYTE* llCodeTable, const BYTE* mlCodeTable, size_t nbSeq, const ZSTD_entropyCTables_t* entropy, const ZSTD_entropyCTablesMetadata_t* entropyMetadata, void* workspace, size_t wkspSize, int writeLitEntropy, int writeSeqEntropy)
{
	const size_t  literalsSize; // 26888
	const size_t  seqSize; // 26891
} /* size: 0, variables: 2 */

// <06FE6F4D> thirdparty/zstd/zstd.c:26902
// variables: 4
// function calls: 9
size_t ZSTD_buildEntropyStatisticsAndEstimateSubBlockSize(SeqStore_t* seqStore, ZSTD_CCtx* zc)
{
	const ZSTD_entropyCTablesMetadata_t* entropyMetadata; // 26904
	{
		const size_t  err_code; // 26906
	}
	{
		const size_t  largest; // 26798
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16714
		HIST_countFast_wksp(unsigned int* count,
					unsigned int* maxSymbolValuePtr,
					const void* source,
					size_t sourceSize,
					void* workSpace,
					size_t workSpaceSize);  // 16737
		HIST_count_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 16728
		HIST_count_wksp(unsigned int* count,
				unsigned int* maxSymbolValuePtr,
				const void* source,
				size_t sourceSize,
				void* workSpace,
				size_t workSpaceSize);  // 26798
		{
			size_t cLitSizeEstimate; // 26800
			HUF_getNbBits(HUF_CElt elt); // 17556
			HUF_estimateCompressedSize(const HUF_CElt* CTable,
							const unsigned int* count,
							unsigned int maxSymbolValue);  // 26800
		}
	}
	ZSTD_estimateBlockSize_literal(const BYTE* literals,
					size_t litSize,
					const ZSTD_hufCTables_t* huf,
					const ZSTD_hufCTablesMetadata_t* hufMetadata,
					void* workspace,
					size_t wkspSize,
					int writeEntropy);  // 26888
	ZSTD_estimateBlockSize_sequences(const BYTE* ofCodeTable,
					const BYTE* llCodeTable,
					const BYTE* mlCodeTable,
					size_t nbSeq,
					const ZSTD_fseCTables_t* fseTables,
					const ZSTD_fseCTablesMetadata_t* fseMetadata,
					void* workspace,
					size_t wkspSize,
					int writeEntropy);  // 26891
	ZSTD_estimateBlockSize(const BYTE* literals,
				size_t litSize,
				const BYTE* ofCodeTable,
				const BYTE* llCodeTable,
				const BYTE* mlCodeTable,
				size_t nbSeq,
				const ZSTD_entropyCTables_t* entropy,
				const ZSTD_entropyCTablesMetadata_t* entropyMetadata,
				void* workspace,
				size_t wkspSize,
				int writeLitEntropy,
				int writeSeqEntropy);  // 26912
} /* size: 795, variables: 1, inline expansions: 3 (1104 bytes) */

// <06FE6EF9> thirdparty/zstd/zstd.c:26923
// variables: 4
size_t ZSTD_countSeqStoreLiteralsBytes(const SeqStore_t* seqStore)
{
	size_t literalsBytes; // 26925
	const size_t  nbSeqs; // 26926
	size_t i; // 26927
	{
		const SeqDef  seq; // 26929
	}
} /* size: 0, variables: 3 */

// <06FE6EA5> thirdparty/zstd/zstd.c:26938
// variables: 4
size_t ZSTD_countSeqStoreMatchBytes(const SeqStore_t* seqStore)
{
	size_t matchBytes; // 26940
	const size_t  nbSeqs; // 26941
	size_t i; // 26942
	{
		SeqDef seq; // 26944
	}
} /* size: 0, variables: 3 */

// <06FE6D4F> thirdparty/zstd/zstd.c:26955
// variables: 3
// function calls: 2
void ZSTD_deriveSeqStoreChunk(SeqStore_t* resultSeqStore, const SeqStore_t* originalSeqStore, size_t startIdx, size_t endIdx)
{
	{
		const size_t  literalsBytes; // 26979
		{
			const SeqDef  seq; // 26929
		}
		ZSTD_countSeqStoreLiteralsBytes(const SeqStore_t* seqStore); // 26979
	}
	{
		const SeqDef  seq; // 26929
	}
	ZSTD_countSeqStoreLiteralsBytes(const SeqStore_t* seqStore); // 26962
} /* size: 431, inline expansions: 1 (91 bytes) */

// <06FE6D08> thirdparty/zstd/zstd.c:26992
// variable: 1
U32 ZSTD_resolveRepcodeToRawOffset(const U32* rep, const U32 offBase, const U32 ll0)
{
	const U32  adjustedRepCode; // 26994
} /* size: 0, variables: 1 */

// <06FE6C66> thirdparty/zstd/zstd.c:27024
// variables: 7
void ZSTD_seqStore_resolveOffCodes(const Repcodes_t* dRepcodes, const Repcodes_t* cRepcodes, const SeqStore_t* seqStore, const U32 nbSeq)
{
	U32 idx; // 27027
	const U32  longLitLenIdx; // 27028
	{
		const SeqDef* seq; // 27030
		const U32  ll0; // 27031
		const U32  offBase; // 27032
		{
			const U32  dRawOffset; // 27035
			const U32  cRawOffset; // 27036
		}
	}
} /* size: 0, variables: 2 */

// <06FE6381> thirdparty/zstd/zstd.c:27059
// variables: 19
// function calls: 21
size_t ZSTD_compressSeqStore_singleBlock(ZSTD_CCtx* zc, const SeqStore_t* seqStore, const Repcodes_t* dRep, const Repcodes_t* cRep, void* dst, size_t dstCapacity, const void* src, size_t srcSize, U32 lastBlock, U32 isPartition)
{
	const U32  rleMaxLength; // 27066
	BYTE* op; // 27067
	const BYTE* ip; // 27068
	size_t cSize; // 27069
	size_t cSeqsSize; // 27070
	const Repcodes_t  dRepOriginal; // 27073
	{
		const size_t  err_code; // 27086
	}
	{
		const size_t  err_code; // 27100
	}
	{
		const size_t  err_code; // 27107
	}
	{
		const size_t  err_code; // 27112
	}
	ZSTD_blockState_confirmRepcodesAndEntropyTables(const ZSTD_blockState_t* bs); // 27101
	MEM_write16(void* memPtr,
			U16 value);  // 1279
	MEM_writeLE16(void* memPtr,
			U16 val);  // 1294
	MEM_writeLE24(void* memPtr,
			U32 val);  // 19821
	ZSTD_rleCompressBlock(void* dst,
				size_t dstCapacity,
				BYTE src,
				size_t srcSize,
				U32 lastBlock);  // 27111
	ZSTD_blockState_confirmRepcodesAndEntropyTables(const ZSTD_blockState_t* bs); // 27116
	MEM_write16(void* memPtr,
			U16 value);  // 1279
	MEM_writeLE16(void* memPtr,
			U16 val);  // 1294
	MEM_writeLE24(void* memPtr,
			U32 val);  // 26575
	writeBlockHeader(void* op,
			size_t cSize,
			size_t blockSize,
			U32 lastBlock);  // 27117
	MEM_write16(void* memPtr,
			U16 value);  // 1279
	MEM_writeLE16(void* memPtr,
			U16 val);  // 1294
	MEM_writeLE24(void* memPtr,
			U32 val);  // 19810
	ZSTD_noCompressBlock(void* dst,
				size_t dstCapacity,
				const void* src,
				size_t srcSize,
				U32 lastBlock);  // 27106
	{
		const SeqDef* seq; // 27030
		const U32  ll0; // 27031
		const U32  offBase; // 27032
		{
			const U32  repCode; // 19979
			{
				const U32  currentOffset; // 19981
			}
		}
		ZSTD_updateRep(U32* rep,
				const U32  offBase,
				const U32  ll0);  // 19972
		ZSTD_updateRep(U32* rep,
				const U32  offBase,
				const U32  ll0);  // 27048
		{
			const U32  dRawOffset; // 27035
			const U32  cRawOffset; // 27036
			ZSTD_resolveRepcodeToRawOffset(const U32* rep,
							const U32  offBase,
							const U32  ll0);  // 27035
			ZSTD_resolveRepcodeToRawOffset(const U32* rep,
							const U32  offBase,
							const U32  ll0);  // 27036
		}
		{
			const U32  repCode; // 19979
			{
				const U32  currentOffset; // 19981
			}
		}
		ZSTD_updateRep(U32* rep,
				const U32  offBase,
				const U32  ll0);  // 19972
		ZSTD_updateRep(U32* rep,
				const U32  offBase,
				const U32  ll0);  // 27049
	}
	ZSTD_seqStore_resolveOffCodes(const Repcodes_t* dRepcodes,
					const Repcodes_t* cRepcodes,
					const SeqStore_t* seqStore,
					const U32  nbSeq);  // 27076
} /* size: 1252, variables: 6, inline expansions: 15 (778 bytes) */

// <06F3D6BF> thirdparty/zstd/zstd.c:27129
// member variables: 2
// struct size: 16
typedef struct {
	U32 * splitLocations; /* 0 8 */
	size_t idx; /* 8 8 */
} seqStoreSplits;

// <06FE6103> thirdparty/zstd/zstd.c:27151
// variables: 7
void ZSTD_deriveBlockSplitsHelper(seqStoreSplits* splits, size_t startIdx, size_t endIdx, ZSTD_CCtx* zc, const SeqStore_t* origSeqStore)
{
	const SeqStore_t* fullSeqStoreChunk; // 27154
	const SeqStore_t* firstHalfSeqStore; // 27155
	const SeqStore_t* secondHalfSeqStore; // 27156
	size_t estimatedOriginalSize; // 27157
	size_t estimatedFirstHalfSize; // 27158
	size_t estimatedSecondHalfSize; // 27159
	size_t midIdx; // 27160
} /* size: 364, variables: 7 */

// <06FE60BD> thirdparty/zstd/zstd.c:27193
// variable: 1
size_t ZSTD_deriveBlockSplits(ZSTD_CCtx* zc, U32* partitions, U32 nbSeq)
{
	seqStoreSplits splits; // 27195
} /* size: 0, variables: 1 */

// <06FE5F5E> thirdparty/zstd/zstd.c:27215
// variables: 18
size_t ZSTD_compressBlock_splitBlock_internal(ZSTD_CCtx* zc, void* dst, size_t dstCapacity, const void* src, size_t blockSize, U32 lastBlock, U32 nbSeq)
{
	size_t cSize; // 27220
	const BYTE* ip; // 27221
	BYTE* op; // 27222
	size_t i; // 27223
	size_t srcBytesTotal; // 27224
	const U32* partitions; // 27225
	const SeqStore_t* nextSeqStore; // 27226
	const SeqStore_t* currSeqStore; // 27227
	const size_t  numSplits; // 27228
	Repcodes_t dRep; // 27244
	Repcodes_t cRep; // 27245
	{
		size_t cSizeSingleBlock; // 27255
		{
			const size_t  err_code; // 27261
		}
	}
	{
		size_t cSizeChunk; // 27270
		const U32  lastPartition; // 27271
		U32 lastBlockEntireSrc; // 27272
		size_t srcBytes; // 27274
		{
			const size_t  err_code; // 27291
		}
	}
} /* size: 0, variables: 11 */

// <06FE5EA0> thirdparty/zstd/zstd.c:27308
// variables: 6
size_t ZSTD_compressBlock_splitBlock(ZSTD_CCtx* zc, void* dst, size_t dstCapacity, const void* src, size_t srcSize, U32 lastBlock)
{
	U32 nbSeq; // 27312
	size_t cSize; // 27313
	{
		const size_t  bss; // 27317
		{
			const size_t  err_code; // 27318
		}
		{
			const size_t  err_code; // 27324
		}
	}
	{
		const size_t  err_code; // 27332
	}
} /* size: 0, variables: 2 */

// <06FE5BC9> thirdparty/zstd/zstd.c:27337
// variables: 7
// function calls: 3
size_t ZSTD_compressBlock_internal(ZSTD_CCtx* zc, void* dst, size_t dstCapacity, const void* src, size_t srcSize, U32 frame)
{

out: // 27390
	const U32  rleMaxLength; // 27345
	size_t cSize; // 27346
	const BYTE* ip; // 27347
	BYTE* op; // 27348
	{
		const size_t  bss; // 27353
		{
			const size_t  err_code; // 27354
		}
	}
	{
		const size_t  err_code; // 27363
		ZSTD_getSeqStore(const ZSTD_CCtx* ctx); // 27363
	}
	ZSTD_blockState_confirmRepcodesAndEntropyTables(const ZSTD_blockState_t* bs); // 27364
	ZSTD_blockState_confirmRepcodesAndEntropyTables(const ZSTD_blockState_t* bs); // 27392
} /* size: 492, variables: 4, goto labels: 1, inline expansions: 2 (78 bytes) */

// <06FE5B2F> thirdparty/zstd/zstd.c:27404
// variables: 3
size_t ZSTD_compressBlock_targetCBlockSize_body(ZSTD_CCtx* zc, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const size_t bss, U32 lastBlock)
{
	{
		const size_t  cSize; // 27439
		{
			const size_t  maxCSize; // 27442
			{
				const size_t  err_code; // 27444
			}
		}
	}
} /* size: 0 */

// <06FE5A93> thirdparty/zstd/zstd.c:27460
// variables: 4
size_t ZSTD_compressBlock_targetCBlockSize(ZSTD_CCtx* zc, void* dst, size_t dstCapacity, const void* src, size_t srcSize, U32 lastBlock)
{
	size_t cSize; // 27465
	const size_t  bss; // 27466
	{
		const size_t  err_code; // 27469
	}
	{
		const size_t  err_code; // 27472
	}
} /* size: 0, variables: 2 */

// <06FE52FD> thirdparty/zstd/zstd.c:27480
// variables: 14
// function calls: 17
void ZSTD_overflowCorrectIfNeeded(ZSTD_MatchState_t* ms, ZSTD_cwksp* ws, const ZSTD_CCtx_params* params, const void* ip, const void* iend)
{
	const U32  cycleLog; // 27486
	const U32  maxDist; // 27487
	{
		const U32  correction; // 27489
		ZSTD_window_correctOverflow(ZSTD_window_t* window,
						U32 cycleLog,
						U32 maxDist,
						const void* src);  // 27489
		ZSTD_cwksp_mark_tables_dirty(ZSTD_cwksp* ws); // 27493
		{
			const U32  hSize; // 25622
			{
				int column; // 25591
				{
					U32 newVal; // 25593
				}
			}
			ZSTD_reduceTable_internal(const U32* table,
							const U32  size,
							const U32  reducerValue,
							const int  preserveMark);  // 25610
			ZSTD_reduceTable(const U32* table,
					const U32  size,
					const U32  reducerValue);  // 25623
		}
		ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
		ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  mode);  // 23216
		ZSTD_allocateChainTable(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  useRowMatchFinder,
					const U32  forDDSDict);  // 25626
		{
			const U32  chainSize; // 25627
			{
				int column; // 25591
				{
					U32 newVal; // 25593
				}
			}
			ZSTD_reduceTable_internal(const U32* table,
							const U32  size,
							const U32  reducerValue,
							const int  preserveMark);  // 25610
			ZSTD_reduceTable(const U32* table,
					const U32  size,
					const U32  reducerValue);  // 25631
			{
				int column; // 25591
				{
					U32 newVal; // 25593
				}
			}
			ZSTD_reduceTable_internal(const U32* table,
							const U32  size,
							const U32  reducerValue,
							const int  preserveMark);  // 25615
			ZSTD_reduceTable_btlazy2(const U32* table,
						const U32  size,
						const U32  reducerValue);  // 25629
		}
		{
			const U32  h3Size; // 25635
			{
				int column; // 25591
				{
					U32 newVal; // 25593
				}
			}
			ZSTD_reduceTable_internal(const U32* table,
							const U32  size,
							const U32  reducerValue,
							const int  preserveMark);  // 25610
			ZSTD_reduceTable(const U32* table,
					const U32  size,
					const U32  reducerValue);  // 25636
		}
		ZSTD_reduceIndex(ZSTD_MatchState_t* ms,
				const ZSTD_CCtx_params* params,
				const U32  reducerValue);  // 27494
		ZSTD_cwksp_mark_tables_clean(ZSTD_cwksp* ws); // 27495
	}
	ZSTD_cycleLog(U32 hashLog,
			ZSTD_strategy strat);  // 27486
	ZSTD_window_needOverflowCorrection(const ZSTD_window_t  window,
						U32 cycleLog,
						U32 maxDist,
						U32 loadedDictEnd,
						const void* src,
						const void* srcEnd);  // 27488
} /* size: 1094, variables: 2, inline expansions: 2 (20 bytes) */

// <06FE5263> thirdparty/zstd/zstd.c:27506
// variable: 1
size_t ZSTD_optimalBlockSize(ZSTD_CCtx* cctx, const void* src, size_t srcSize, size_t blockSizeMax, int splitLevel, ZSTD_strategy strat, S64 savings)
{
	const int   splitLevels; // 27509
} /* size: 0, variables: 1 */

// <06FE5117> thirdparty/zstd/zstd.c:27545
// variables: 16
size_t ZSTD_compress_frameChunk(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, U32 lastFrameChunk)
{
	size_t blockSizeMax; // 27550
	size_t remaining; // 27551
	const BYTE* ip; // 27552
	const BYTE* ostart; // 27553
	BYTE* op; // 27554
	const U32  maxDist; // 27555
	S64 savings; // 27556
	{
		const ZSTD_MatchState_t* ms; // 27565
		const size_t  blockSize; // 27566
		const U32  lastBlock; // 27572
		{
			size_t cSize; // 27589
			{
				const size_t  err_code; // 27592
			}
			{
				const size_t  err_code; // 27597
			}
			{
				const size_t  err_code; // 27603
			}
			{
				const size_t  err_code; // 27607
			}
			{
				const U32  cBlockHeader; // 27609
			}
		}
	}
} /* size: 0, variables: 7 */

// <06FE4D28> thirdparty/zstd/zstd.c:27649
// variables: 10
// function calls: 12
size_t ZSTD_writeFrameHeader(void* dst, size_t dstCapacity, const ZSTD_CCtx_params* params, U64 pledgedSrcSize, U32 dictID)
{
	const BYTE* op; // 27653
	const U32  dictIDSizeCodeLength; // 27654
	const U32  dictIDSizeCode; // 27655
	const U32  checksumFlag; // 27656
	const U32  windowSize; // 27657
	const U32  singleSegment; // 27658
	const BYTE  windowLogByte; // 27659
	const U32  fcsCode; // 27660
	const BYTE  frameHeaderDescriptionByte; // 27662
	size_t pos; // 27663
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 27671
	MEM_write16(void* memPtr,
			U16 value);  // 1279
	MEM_writeLE16(void* memPtr,
			U16 val);  // 27692
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 27693
	MEM_write64(void* memPtr,
			U64 value);  // 1325
	MEM_writeLE64(void* memPtr,
			U64 val64);  // 27694
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 27684
	MEM_write16(void* memPtr,
			U16 value);  // 1279
	MEM_writeLE16(void* memPtr,
			U16 val);  // 27683
} /* size: 510, variables: 10, inline expansions: 12 (30 bytes) */

// <06FE4BF4> thirdparty/zstd/zstd.c:27705
// variable: 1
// function calls: 2
size_t ZSTD_writeSkippableFrame(void* dst, size_t dstCapacity, const void* src, size_t srcSize, unsigned int magicVariant)
{
	BYTE* op; // 27707
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 27713
} /* size: 139, variables: 1, inline expansions: 2 (32 bytes) */

// <06FE4BB3> thirdparty/zstd/zstd.c:27724
// variable: 1
size_t ZSTD_writeLastEmptyBlock(void* dst, size_t dstCapacity)
{
	{
		const U32  cBlockHeader24; // 27728
	}
} /* size: 0 */

// <06FE4B78> thirdparty/zstd/zstd.c:27734
void ZSTD_referenceExternalSequences(ZSTD_CCtx* cctx, rawSeq* seq, size_t nbSeq)
{
} /* size: 0 */

// <07098413> thirdparty/zstd/zstd.c:27746
// variables: 41
// function calls: 40
size_t ZSTD_compressContinue_internal(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, U32 frame, U32 lastFrameChunk)
{
	const ZSTD_MatchState_t* ms; // 27751
	size_t fhSize; // 27752
	{
		const size_t  err_code; // 27762
	}
	{
		const size_t  distanceFromBase; // 20522
	}
	{
		const size_t  highInputIdx; // 20537
		const U32  lowLimitMax; // 20538
	}
	ZSTD_window_update(ZSTD_window_t* window,
				const void* src,
				size_t srcSize,
				int forceNonContiguous);  // 20508
	ZSTD_window_update(ZSTD_window_t* window,
				const void* src,
				size_t srcSize,
				int forceNonContiguous);  // 27771
	{
		const size_t  cSize; // 27787
		{
			const size_t  err_code; // 27790
		}
		{
			const ZSTD_MatchState_t* ms; // 27565
			const size_t  blockSize; // 27566
			const U32  lastBlock; // 27572
			{
				size_t cSize; // 27589
				ZSTD_blockSplitterEnabled(ZSTD_CCtx_params* cctxParams); // 27595
				{
					const size_t  err_code; // 27603
				}
				{
					const U32  cBlockHeader; // 27609
					MEM_write16(void* memPtr,
							U16 value);  // 1279
					MEM_writeLE16(void* memPtr,
							U16 val);  // 1294
					MEM_writeLE24(void* memPtr,
							U32 val);  // 27612
				}
				{
					const size_t  err_code; // 27607
				}
				MEM_write16(void* memPtr,
						U16 value);  // 1279
				MEM_writeLE16(void* memPtr,
						U16 val);  // 1294
				MEM_writeLE24(void* memPtr,
						U32 val);  // 19810
				ZSTD_noCompressBlock(void* dst,
							size_t dstCapacity,
							const void* src,
							size_t srcSize,
							U32 lastBlock);  // 27606
				{
					const size_t  bss; // 27317
					{
						const size_t  err_code; // 27318
					}
					MEM_write16(void* memPtr,
							U16 value);  // 1279
					MEM_writeLE16(void* memPtr,
							U16 val);  // 1294
					MEM_writeLE24(void* memPtr,
							U32 val);  // 19810
					ZSTD_noCompressBlock(void* dst,
								size_t dstCapacity,
								const void* src,
								size_t srcSize,
								U32 lastBlock);  // 27323
				}
				ZSTD_deriveBlockSplits(ZSTD_CCtx* zc,
							U32* partitions,
							U32 nbSeq);  // 27228
				{
					size_t cSizeSingleBlock; // 27255
					{
						const size_t  err_code; // 27261
					}
				}
				{
					size_t cSizeChunk; // 27270
					const U32  lastPartition; // 27271
					U32 lastBlockEntireSrc; // 27272
					size_t srcBytes; // 27274
					{
						const SeqDef  seq; // 26929
					}
					ZSTD_countSeqStoreLiteralsBytes(const SeqStore_t* seqStore); // 27274
					{
						SeqDef seq; // 26944
					}
					ZSTD_countSeqStoreMatchBytes(const SeqStore_t* seqStore); // 27274
					{
						const size_t  err_code; // 27291
					}
				}
				ZSTD_compressBlock_splitBlock_internal(ZSTD_CCtx* zc,
									void* dst,
									size_t dstCapacity,
									const void* src,
									size_t blockSize,
									U32 lastBlock,
									U32 nbSeq);  // 27331
				{
					const size_t  err_code; // 27332
				}
				ZSTD_compressBlock_splitBlock(ZSTD_CCtx* zc,
								void* dst,
								size_t dstCapacity,
								const void* src,
								size_t srcSize,
								U32 lastBlock);  // 27596
				{
					const size_t  err_code; // 27597
				}
				ZSTD_useTargetCBlockSize(const ZSTD_CCtx_params* cctxParams); // 27590
				{
					const size_t  err_code; // 27469
				}
				ZSTD_maybeRLE(const SeqStore_t* seqStore); // 27416
				MEM_write16(void* memPtr,
						U16 value);  // 1279
				MEM_writeLE16(void* memPtr,
						U16 val);  // 1294
				MEM_writeLE24(void* memPtr,
						U32 val);  // 19821
				ZSTD_rleCompressBlock(void* dst,
							size_t dstCapacity,
							BYTE src,
							size_t srcSize,
							U32 lastBlock);  // 27419
				{
					const size_t  cSize; // 27439
					{
						const size_t  maxCSize; // 27442
						ZSTD_minGain(size_t srcSize,
								ZSTD_strategy strat);  // 27443
						{
							const size_t  err_code; // 27444
						}
						ZSTD_blockState_confirmRepcodesAndEntropyTables(const ZSTD_blockState_t* bs); // 27446
					}
				}
				MEM_write16(void* memPtr,
						U16 value);  // 1279
				MEM_writeLE16(void* memPtr,
						U16 val);  // 1294
				MEM_writeLE24(void* memPtr,
						U32 val);  // 19810
				ZSTD_noCompressBlock(void* dst,
							size_t dstCapacity,
							const void* src,
							size_t srcSize,
							U32 lastBlock);  // 27457
				ZSTD_compressBlock_targetCBlockSize_body(ZSTD_CCtx* zc,
									void* dst,
									size_t dstCapacity,
									const void* src,
									size_t srcSize,
									const size_t  bss,
									U32 lastBlock);  // 27471
				{
					const size_t  err_code; // 27472
				}
				ZSTD_compressBlock_targetCBlockSize(ZSTD_CCtx* zc,
									void* dst,
									size_t dstCapacity,
									const void* src,
									size_t srcSize,
									U32 lastBlock);  // 27591
				{
					const size_t  err_code; // 27592
				}
			}
			ZSTD_optimalBlockSize(ZSTD_CCtx* cctx,
						const void* src,
						size_t srcSize,
						size_t blockSizeMax,
						int splitLevel,
						ZSTD_strategy strat,
						S64 savings);  // 27566
			{
				const U32  blockEndIdx; // 20463
				const U32  loadedDictEnd; // 20464
			}
			ZSTD_checkDictValidity(const ZSTD_window_t* window,
						const void* blockEnd,
						U32 maxDist,
						U32* loadedDictEndPtr,
						const ZSTD_MatchState_t ** dictMatchStatePtr);  // 27583
			{
				const U32  newLowLimit; // 20435
			}
			{
				const U32  newLowLimit; // 20435
			}
			ZSTD_window_enforceMaxDist(ZSTD_window_t* window,
							const void* blockEnd,
							U32 maxDist,
							U32* loadedDictEndPtr,
							const ZSTD_MatchState_t ** dictMatchStatePtr);  // 20410
			ZSTD_window_enforceMaxDist(ZSTD_window_t* window,
							const void* blockEnd,
							U32 maxDist,
							U32* loadedDictEndPtr,
							const ZSTD_MatchState_t ** dictMatchStatePtr);  // 27584
		}
		ZSTD_compress_frameChunk(ZSTD_CCtx* cctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize,
					U32 lastFrameChunk);  // 27788
	}
	{
		const size_t  distanceFromBase; // 20522
	}
	{
		const size_t  highInputIdx; // 20537
		const U32  lowLimitMax; // 20538
	}
	ZSTD_window_update(ZSTD_window_t* window,
				const void* src,
				size_t srcSize,
				int forceNonContiguous);  // 20508
	ZSTD_window_update(ZSTD_window_t* window,
				const void* src,
				size_t srcSize,
				int forceNonContiguous);  // 27776
} /* size: 3394, variables: 2, inline expansions: 4 (806 bytes) */

// <06FE4A89> thirdparty/zstd/zstd.c:27746
// variables: 5
size_t ZSTD_compressContinue_internal(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, U32 frame, U32 lastFrameChunk)
{
	const ZSTD_MatchState_t* ms; // 27751
	size_t fhSize; // 27752
	{
		const size_t  err_code; // 27762
	}
	{
		const size_t  cSize; // 27787
		{
			const size_t  err_code; // 27790
		}
	}
} /* size: 0, variables: 2 */

// <06FE4A30> thirdparty/zstd/zstd.c:27807
size_t ZSTD_compressContinue_public(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FE4910> thirdparty/zstd/zstd.c:27816
// function call: 1
size_t ZSTD_compressContinue(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	ZSTD_compressContinue_public(ZSTD_CCtx* cctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize);  // 27820
} /* size: 27, inline expansions: 1 (17 bytes) */

// <06FE48E3> thirdparty/zstd/zstd.c:27823
// variable: 1
size_t ZSTD_getBlockSize_deprecated(const ZSTD_CCtx* cctx)
{
	const ZSTD_compressionParameters  cParams; // 27825
} /* size: 0, variables: 1 */

// <06FE486F> thirdparty/zstd/zstd.c:27831
// function call: 1
size_t ZSTD_getBlockSize(const ZSTD_CCtx* cctx)
{
	ZSTD_getBlockSize_deprecated(const ZSTD_CCtx* cctx); // 27833
} /* size: 39, inline expansions: 1 (34 bytes) */

// <06FE4807> thirdparty/zstd/zstd.c:27837
// variable: 1
size_t ZSTD_compressBlock_deprecated(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	{
		const size_t  blockSizeMax; // 27840
	}
} /* size: 0 */

// <06FE4641> thirdparty/zstd/zstd.c:27847
// variable: 1
// function calls: 3
size_t ZSTD_compressBlock(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	{
		const size_t  blockSizeMax; // 27840
		ZSTD_getBlockSize_deprecated(const ZSTD_CCtx* cctx); // 27840
	}
	ZSTD_compressBlock_deprecated(ZSTD_CCtx* cctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize);  // 27837
	ZSTD_compressBlock_deprecated(ZSTD_CCtx* cctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize);  // 27849
} /* size: 88, inline expansions: 2 (71 bytes) */

// <07097137> thirdparty/zstd/zstd.c:27856
// variables: 14
// function calls: 9
size_t ZSTD_loadDictionaryContent(ZSTD_MatchState_t* ms, ldmState_t* ls, ZSTD_cwksp* ws, const ZSTD_CCtx_params* params, const void* src, size_t srcSize, ZSTD_dictTableLoadMethod_e dtlm, ZSTD_tableFillPurpose_e tfp)
{
	const BYTE* ip; // 27864
	const BYTE* iend; // 27865
	const int  loadLdmDict; // 27866
	{
		U32 maxDictSize; // 27877
		const int  CDictTaggedIndices; // 27879
		ZSTD_CDictIndicesAreTagged(const ZSTD_compressionParameters* cParams); // 27879
	}
	{
		const size_t  distanceFromBase; // 20522
	}
	{
		const size_t  highInputIdx; // 20537
		const U32  lowLimitMax; // 20538
	}
	ZSTD_window_update(ZSTD_window_t* window,
				const void* src,
				size_t srcSize,
				int forceNonContiguous);  // 20508
	ZSTD_window_update(ZSTD_window_t* window,
				const void* src,
				size_t srcSize,
				int forceNonContiguous);  // 27904
	{
		const size_t  distanceFromBase; // 20522
	}
	{
		const size_t  highInputIdx; // 20537
		const U32  lowLimitMax; // 20538
	}
	ZSTD_window_update(ZSTD_window_t* window,
				const void* src,
				size_t srcSize,
				int forceNonContiguous);  // 20508
	ZSTD_window_update(ZSTD_window_t* window,
				const void* src,
				size_t srcSize,
				int forceNonContiguous);  // 27910
	{
		U32 maxDictSize; // 27917
	}
	ZSTD_fillDoubleHashTable(ZSTD_MatchState_t* ms,
				const void* end,
				ZSTD_dictTableLoadMethod_e dtlm,
				ZSTD_tableFillPurpose_e tfp);  // 27940
	ZSTD_fillHashTable(ZSTD_MatchState_t* ms,
				const void* end,
				ZSTD_dictTableLoadMethod_e dtlm,
				ZSTD_tableFillPurpose_e tfp);  // 27936
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36340
	ZSTD_updateTree(ZSTD_MatchState_t* ms,
			const BYTE* ip,
			const BYTE* iend);  // 27982
	{
		const size_t  tagTableSize; // 27959
	}
} /* size: 1089, variables: 3, inline expansions: 8 (831 bytes) */

// <06FE4552> thirdparty/zstd/zstd.c:27856
// variables: 8
size_t ZSTD_loadDictionaryContent(ZSTD_MatchState_t* ms, ldmState_t* ls, ZSTD_cwksp* ws, const ZSTD_CCtx_params* params, const void* src, size_t srcSize, ZSTD_dictTableLoadMethod_e dtlm, ZSTD_tableFillPurpose_e tfp)
{
	const BYTE* ip; // 27864
	const BYTE* iend; // 27865
	const int  loadLdmDict; // 27866
	{
		U32 maxDictSize; // 27877
		const int  CDictTaggedIndices; // 27879
		{
			const U32  shortCacheMaxDictSize; // 27886
		}
	}
	{
		U32 maxDictSize; // 27917
	}
	{
		const size_t  tagTableSize; // 27959
	}
} /* size: 0, variables: 3 */

// <06FE4507> thirdparty/zstd/zstd.c:28001
// variable: 1
FSE_repeat ZSTD_dictNCountRepeat(short int* normalizedCounter, unsigned int dictMaxSymbolValue, unsigned int maxSymbolValue)
{
	U32 s; // 28003
} /* size: 0, variables: 1 */

// <06FE3BE0> thirdparty/zstd/zstd.c:28015
// variables: 21
// function calls: 20
size_t ZSTD_loadCEntropy(ZSTD_compressedBlockState_t* bs, void* workspace, const void* dict, size_t dictSize)
{
	short int offcodeNCount; // 28018
	unsigned int offcodeMaxValue; // 28019
	const BYTE* dictPtr; // 28020
	const BYTE* dictEnd; // 28021
	{
		unsigned int maxSymbolValue; // 28025
		unsigned int hasZeroWeights; // 28026
		const size_t  hufHeaderSize; // 28027
	}
	{
		unsigned int offcodeLog; // 28039
		const size_t  offcodeHeaderSize; // 28040
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3437
		FSE_readNCount(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 28040
	}
	{
		short int matchlengthNCount; // 28053
		unsigned int matchlengthMaxValue; // 28054
		unsigned int matchlengthLog; // 28054
		const size_t  matchlengthHeaderSize; // 28055
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3437
		FSE_readNCount(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 28055
		ZSTD_dictNCountRepeat(short int* normalizedCounter,
					unsigned int dictMaxSymbolValue,
					unsigned int maxSymbolValue);  // 28001
		ZSTD_dictNCountRepeat(short int* normalizedCounter,
					unsigned int dictMaxSymbolValue,
					unsigned int maxSymbolValue);  // 28063
	}
	{
		short int litlengthNCount; // 28067
		unsigned int litlengthMaxValue; // 28068
		unsigned int litlengthLog; // 28068
		const size_t  litlengthHeaderSize; // 28069
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3437
		FSE_readNCount(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 28069
		ZSTD_dictNCountRepeat(short int* normalizedCounter,
					unsigned int dictMaxSymbolValue,
					unsigned int maxSymbolValue);  // 28001
		ZSTD_dictNCountRepeat(short int* normalizedCounter,
					unsigned int dictMaxSymbolValue,
					unsigned int maxSymbolValue);  // 28077
	}
	{
		const size_t  dictContentSize; // 28087
		U32 offcodeMax; // 28088
		{
			const U32  maxOffset; // 28090
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 28091
		}
		{
			U32 u; // 28097
		}
		ZSTD_dictNCountRepeat(short int* normalizedCounter,
					unsigned int dictMaxSymbolValue,
					unsigned int maxSymbolValue);  // 28001
		ZSTD_dictNCountRepeat(short int* normalizedCounter,
					unsigned int dictMaxSymbolValue,
					unsigned int maxSymbolValue);  // 28094
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 28082
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 28083
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 28084
} /* size: 985, variables: 4, inline expansions: 6 (22 bytes) */

// <06FE3AF6> thirdparty/zstd/zstd.c:28115
// variables: 7
size_t ZSTD_loadZstdDictionary(ZSTD_compressedBlockState_t* bs, ZSTD_MatchState_t* ms, ZSTD_cwksp* ws, const ZSTD_CCtx_params* params, const void* dict, size_t dictSize, ZSTD_dictTableLoadMethod_e dtlm, ZSTD_tableFillPurpose_e tfp, void* workspace)
{
	const BYTE* dictPtr; // 28124
	const BYTE* dictEnd; // 28125
	size_t dictID; // 28126
	size_t eSize; // 28127
	{
		const size_t  err_code; // 28134
	}
	{
		const size_t  dictContentSize; // 28138
		{
			const size_t  err_code; // 28139
		}
	}
} /* size: 0, variables: 4 */

// <06FE374E> thirdparty/zstd/zstd.c:28148
// variables: 3
// function calls: 6
size_t ZSTD_compress_insertDictionary(ZSTD_compressedBlockState_t* bs, ZSTD_MatchState_t* ms, ldmState_t* ls, ZSTD_cwksp* ws, const ZSTD_CCtx_params* params, const void* dict, size_t dictSize, ZSTD_dictContentType_e dictContentType, ZSTD_dictTableLoadMethod_e dtlm, ZSTD_tableFillPurpose_e tfp, void* workspace)
{
	ZSTD_reset_compressedBlockState(ZSTD_compressedBlockState_t* bs); // 28165
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 28171
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 28132
	{
		const size_t  err_code; // 28134
	}
	{
		const size_t  dictContentSize; // 28138
		{
			const size_t  err_code; // 28139
		}
	}
	ZSTD_loadZstdDictionary(ZSTD_compressedBlockState_t* bs,
				ZSTD_MatchState_t* ms,
				ZSTD_cwksp* ws,
				const ZSTD_CCtx_params* params,
				const void* dict,
				size_t dictSize,
				ZSTD_dictTableLoadMethod_e dtlm,
				ZSTD_tableFillPurpose_e tfp,
				void* workspace);  // 28182
} /* size: 311, inline expansions: 6 (155 bytes) */

// <07097712> thirdparty/zstd/zstd.c:28192
// variables: 24
// function calls: 21
size_t ZSTD_compressBegin_internal(ZSTD_CCtx* cctx, const void* dict, size_t dictSize, ZSTD_dictContentType_e dictContentType, ZSTD_dictTableLoadMethod_e dtlm, const ZSTD_CDict* cdict, const ZSTD_CCtx_params* params, U64 pledgedSrcSize, ZSTD_buffered_policy_e zbuff)
{
	const size_t  dictContentSize; // 28200
	{
		const size_t  err_code; // 28218
	}
	{
		const size_t  dictID; // 28221
		{
			const size_t  err_code; // 28231
		}
	}
	ZSTD_shouldAttachDict(const ZSTD_CDict* cdict,
				const ZSTD_CCtx_params* params,
				U64 pledgedSrcSize);  // 25451
	{
		ZSTD_compressionParameters adjusted_cdict_cParams; // 25288
		const unsigned int  windowLog; // 25289
		ZSTD_dedicatedDictSearch_revertCParams(ZSTD_compressionParameters* cParams); // 25296
		{
			const size_t  err_code; // 25304
		}
	}
	{
		const U32  cdictEnd; // 25310
		const U32  cdictLen; // 25312
		ZSTD_window_clear(ZSTD_window_t* window); // 25325
	}
	ZSTD_resetCCtx_byAttachingCDict(ZSTD_CCtx* cctx,
					const ZSTD_CDict* cdict,
					ZSTD_CCtx_params params,
					U64 pledgedSrcSize,
					ZSTD_buffered_policy_e zbuff);  // 25452
	{
		const unsigned int  windowLog; // 25368
		{
			const size_t  err_code; // 25374
		}
	}
	ZSTD_cwksp_mark_tables_dirty(ZSTD_cwksp* ws); // 25382
	{
		const size_t  chainSize; // 25386
		const size_t  hSize; // 25389
		ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
		ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  mode);  // 23216
		ZSTD_allocateChainTable(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  useRowMatchFinder,
					const U32  forDDSDict);  // 25386
		ZSTD_copyCDictTableIntoCCtx(U32* dst,
						const U32* src,
						size_t tableSize,
						const ZSTD_compressionParameters* cParams);  // 25340
		{
			size_t i; // 25345
			{
				const U32  taggedIndex; // 25347
				const U32  index; // 25348
			}
		}
		ZSTD_copyCDictTableIntoCCtx(U32* dst,
						const U32* src,
						size_t tableSize,
						const ZSTD_compressionParameters* cParams);  // 25391
		ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
		ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  mode);  // 23216
		ZSTD_allocateChainTable(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  useRowMatchFinder,
					const U32  forDDSDict);  // 25396
		ZSTD_CDictIndicesAreTagged(const ZSTD_compressionParameters* cParams); // 25342
		{
			size_t i; // 25345
			{
				const U32  taggedIndex; // 25347
				const U32  index; // 25348
			}
		}
		ZSTD_copyCDictTableIntoCCtx(U32* dst,
						const U32* src,
						size_t tableSize,
						const ZSTD_compressionParameters* cParams);  // 25340
		ZSTD_copyCDictTableIntoCCtx(U32* dst,
						const U32* src,
						size_t tableSize,
						const ZSTD_compressionParameters* cParams);  // 25397
		ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
		ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  mode);  // 25402
		{
			const size_t  tagTableSize; // 25403
		}
	}
	{
		const U32  h3log; // 25413
		const size_t  h3Size; // 25414
	}
	ZSTD_cwksp_mark_tables_clean(ZSTD_cwksp* ws); // 25419
	{
		const ZSTD_MatchState_t* srcMatchState; // 25422
		ZSTD_MatchState_t* dstMatchState; // 25423
	}
	ZSTD_resetCCtx_byCopyingCDict(ZSTD_CCtx* cctx,
					const ZSTD_CDict* cdict,
					ZSTD_CCtx_params params,
					U64 pledgedSrcSize,
					ZSTD_buffered_policy_e zbuff);  // 25455
	ZSTD_resetCCtx_usingCDict(ZSTD_CCtx* cctx,
					const ZSTD_CDict* cdict,
					const ZSTD_CCtx_params* params,
					U64 pledgedSrcSize,
					ZSTD_buffered_policy_e zbuff);  // 28215
} /* size: 1768, variables: 1, inline expansions: 6 (2638 bytes) */

// <06FE35CA> thirdparty/zstd/zstd.c:28192
// variables: 4
size_t ZSTD_compressBegin_internal(ZSTD_CCtx* cctx, const void* dict, size_t dictSize, ZSTD_dictContentType_e dictContentType, ZSTD_dictTableLoadMethod_e dtlm, const ZSTD_CDict* cdict, const ZSTD_CCtx_params* params, U64 pledgedSrcSize, ZSTD_buffered_policy_e zbuff)
{
	const size_t  dictContentSize; // 28200
	{
		const size_t  err_code; // 28218
	}
	{
		const size_t  dictID; // 28221
		{
			const size_t  err_code; // 28231
		}
	}
} /* size: 0, variables: 1 */

// <06FE352E> thirdparty/zstd/zstd.c:28239
// variable: 1
size_t ZSTD_compressBegin_advanced_internal(ZSTD_CCtx* cctx, const void* dict, size_t dictSize, ZSTD_dictContentType_e dictContentType, ZSTD_dictTableLoadMethod_e dtlm, const ZSTD_CDict* cdict, const ZSTD_CCtx_params* params, long long unsigned int pledgedSrcSize)
{
	{
		const size_t  err_code; // 28249
	}
} /* size: 0 */

// <06FE3170> thirdparty/zstd/zstd.c:28259
// variables: 2
// function calls: 10
size_t ZSTD_compressBegin_advanced(ZSTD_CCtx* cctx, const void* dict, size_t dictSize, ZSTD_parameters params, long long unsigned int pledgedSrcSize)
{
	ZSTD_CCtx_params cctxParams; // 28263
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23339
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23202
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23340
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23223
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23341
	ZSTD_CCtxParams_init_internal(ZSTD_CCtx_params* cctxParams,
					const ZSTD_parameters* params,
					int compressionLevel);  // 28264
	{
		const size_t  err_code; // 28249
	}
	ZSTD_compressBegin_advanced_internal(ZSTD_CCtx* cctx,
						const void* dict,
						size_t dictSize,
						ZSTD_dictContentType_e dictContentType,
						ZSTD_dictTableLoadMethod_e dtlm,
						const ZSTD_CDict* cdict,
						const ZSTD_CCtx_params* params,
						long long unsigned int pledgedSrcSize);  // 28239
	ZSTD_compressBegin_advanced_internal(ZSTD_CCtx* cctx,
						const void* dict,
						size_t dictSize,
						ZSTD_dictContentType_e dictContentType,
						ZSTD_dictTableLoadMethod_e dtlm,
						const ZSTD_CDict* cdict,
						const ZSTD_CCtx_params* params,
						long long unsigned int pledgedSrcSize);  // 28265
} /* size: 427, variables: 1, inline expansions: 10 (546 bytes) */

// <06FE2FD3> thirdparty/zstd/zstd.c:28272
// variables: 2
// function call: 1
size_t ZSTD_compressBegin_usingDict_deprecated(ZSTD_CCtx* cctx, const void* dict, size_t dictSize, int compressionLevel)
{
	ZSTD_CCtx_params cctxParams; // 28274
	{
		const ZSTD_parameters  params; // 28275
		ZSTD_getParams_internal(int compressionLevel,
					long long unsigned int srcSizeHint,
					size_t dictSize,
					ZSTD_CParamMode_e mode);  // 28275
	}
} /* size: 243, variables: 1 */

// <06FE2F2D> thirdparty/zstd/zstd.c:28284
size_t ZSTD_compressBegin_usingDict(ZSTD_CCtx* cctx, const void* dict, size_t dictSize, int compressionLevel)
{
} /* size: 9 */

// <06FE2EB5> thirdparty/zstd/zstd.c:28289
size_t ZSTD_compressBegin(ZSTD_CCtx* cctx, int compressionLevel)
{
} /* size: 15 */

// <06FE2E1E> thirdparty/zstd/zstd.c:28298
// variables: 6
size_t ZSTD_writeEpilogue(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity)
{
	const BYTE* ostart; // 28300
	BYTE* op; // 28301
	{
		size_t fhSize; // 28308
		{
			const size_t  err_code; // 28309
		}
	}
	{
		const U32  cBlockHeader24; // 28317
	}
	{
		const U32  checksum; // 28326
	}
} /* size: 0, variables: 2 */

// <06FE2DF0> thirdparty/zstd/zstd.c:28337
void ZSTD_CCtx_trace(ZSTD_CCtx* cctx, size_t extraCSize)
{
} /* size: 0 */

// <06FE28C0> thirdparty/zstd/zstd.c:28361
// variables: 8
// function calls: 15
size_t ZSTD_compressEnd_public(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	size_t endResult; // 28365
	const size_t  cSize; // 28366
	{
		const size_t  err_code; // 28369
	}
	{
		const size_t  err_code; // 28371
	}
	{
		size_t fhSize; // 28308
		{
			const size_t  err_code; // 28309
		}
	}
	{
		const U32  cBlockHeader24; // 28317
		MEM_write16(void* memPtr,
				U16 value);  // 1279
		MEM_writeLE16(void* memPtr,
				U16 val);  // 1294
		MEM_writeLE24(void* memPtr,
				U32 val);  // 28320
	}
	{
		const U32  checksum; // 28326
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11682
		XXH64_mergeRound(xxh_u64 acc,
				xxh_u64 val);  // 11909
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11682
		XXH64_mergeRound(xxh_u64 acc,
				xxh_u64 val);  // 11911
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11682
		XXH64_mergeRound(xxh_u64 acc,
				xxh_u64 val);  // 11910
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11682
		XXH64_mergeRound(xxh_u64 acc,
				xxh_u64 val);  // 11912
		XXH_INLINE_XXH64_digest(const XXH_NAMESPACEXXH64_state_t* state); // 28326
		MEM_write32(void* memPtr,
				U32 value);  // 1309
		MEM_writeLE32(void* memPtr,
				U32 val32);  // 28329
	}
	ZSTD_writeEpilogue(ZSTD_CCtx* cctx,
				void* dst,
				size_t dstCapacity);  // 28370
} /* size: 676, variables: 2, inline expansions: 1 (474 bytes) */

// <06FE27FE> thirdparty/zstd/zstd.c:28388
size_t ZSTD_compressEnd(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
} /* size: 9 */

// <06FE25D8> thirdparty/zstd/zstd.c:28395
// variables: 2
// function call: 1
size_t ZSTD_compress_advanced(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const void* dict, size_t dictSize, ZSTD_parameters params)
{
	{
		const size_t  err_code; // 28402
	}
	{
		const size_t  err_code; // 28420
	}
	ZSTD_compress_advanced_internal(ZSTD_CCtx* cctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize,
					const void* dict,
					size_t dictSize,
					const ZSTD_CCtx_params* params);  // 28404
} /* size: 212, inline expansions: 1 (63 bytes) */

// <06FE2549> thirdparty/zstd/zstd.c:28412
// variable: 1
size_t ZSTD_compress_advanced_internal(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const void* dict, size_t dictSize, const ZSTD_CCtx_params* params)
{
	{
		const size_t  err_code; // 28420
	}
} /* size: 0 */

// <06FE2293> thirdparty/zstd/zstd.c:28426
// variables: 2
// function calls: 2
size_t ZSTD_compress_usingDict(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const void* dict, size_t dictSize, int compressionLevel)
{
	{
		const ZSTD_parameters  params; // 28433
		ZSTD_getParams_internal(int compressionLevel,
					long long unsigned int srcSizeHint,
					size_t dictSize,
					ZSTD_CParamMode_e mode);  // 28433
	}
	{
		const size_t  err_code; // 28420
	}
	ZSTD_compress_advanced_internal(ZSTD_CCtx* cctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize,
					const void* dict,
					size_t dictSize,
					const ZSTD_CCtx_params* params);  // 28438
} /* size: 322, inline expansions: 1 (88 bytes) */

// <06FE21B7> thirdparty/zstd/zstd.c:28441
size_t ZSTD_compressCCtx(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, int compressionLevel)
{
} /* size: 22 */

// <06FE1C72> thirdparty/zstd/zstd.c:28475
// function calls: 7
size_t ZSTD_estimateCDictSize_advanced(size_t dictSize, ZSTD_compressionParameters cParams, ZSTD_dictLoadMethod_e dictLoadMethod)
{
	ZSTD_cwksp_align(size_t size,
			size_t align);  // 18508
	ZSTD_cwksp_aligned_alloc_size(size_t size,
					size_t alignment);  // 18516
	ZSTD_cwksp_aligned64_alloc_size(size_t size); // 24633
	ZSTD_sizeof_matchState(const ZSTD_compressionParameters* cParams,
				const ZSTD_ParamSwitch_e  useRowMatchFinder,
				const int  enableDedicatedDictSearch,
				const U32  forCCtx);  // 28484
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 28484
} /* size: 91, inline expansions: 7 (113 bytes) */

// <06FE1BB4> thirdparty/zstd/zstd.c:28490
// variable: 1
size_t ZSTD_estimateCDictSize(size_t dictSize, int compressionLevel)
{
	const ZSTD_compressionParameters  cParams; // 28492
} /* size: 127, variables: 1 */

// <06FE185A> thirdparty/zstd/zstd.c:28505
// variables: 4
// function calls: 5
size_t ZSTD_initCDict_internal(ZSTD_CDict* cdict, const void* dictBuffer, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType, ZSTD_CCtx_params params)
{
	{
		void* internalBuffer; // 28519
		ZSTD_cwksp_align(size_t size,
				size_t align);  // 18766
		ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
						size_t bytes);  // 28519
		ZSTD_cwksp_align(size_t size,
				size_t align);  // 28519
	}
	{
		const size_t  err_code; // 28532
	}
	{
		const size_t  dictID; // 28545
		{
			const size_t  err_code; // 28549
		}
	}
	ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
					size_t bytes);  // 28527
	ZSTD_reset_compressedBlockState(ZSTD_compressedBlockState_t* bs); // 28531
} /* size: 455, inline expansions: 2 (104 bytes) */

// <06FE17C4> thirdparty/zstd/zstd.c:28559
// variables: 4
ZSTD_CDict* ZSTD_createCDict_advanced_internal(size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_compressionParameters cParams, ZSTD_ParamSwitch_e useRowMatchFinder, int enableDedicatedDictSearch, ZSTD_customMem customMem)
{
	{
		const size_t  workspaceSize; // 28569
		const void* workspace; // 28575
		ZSTD_cwksp ws; // 28576
		ZSTD_CDict* cdict; // 28577
	}
} /* size: 0 */

// <06FE16B8> thirdparty/zstd/zstd.c:28596
// variable: 1
ZSTD_CDict* ZSTD_createCDict_advanced(const void* dictBuffer, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType, ZSTD_compressionParameters cParams, ZSTD_customMem customMem)
{
	ZSTD_CCtx_params cctxParams; // 28602
} /* size: 225, variables: 1 */

// <06FE0EF0> thirdparty/zstd/zstd.c:28614
// variables: 7
// function calls: 22
ZSTD_CDict* ZSTD_createCDict_advanced2(const void* dict, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType, const ZSTD_CCtx_params* originalCctxParams, ZSTD_customMem customMem)
{
	ZSTD_CCtx_params cctxParams; // 28621
	ZSTD_compressionParameters cParams; // 28622
	ZSTD_CDict* cdict; // 28623
	ZSTD_dedicatedDictSearch_isSupported(const ZSTD_compressionParameters* cParams); // 28637
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 28646
	{
		const size_t  workspaceSize; // 28569
		const void* workspace; // 28575
		ZSTD_cwksp ws; // 28576
		ZSTD_CDict* cdict; // 28577
		ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
		ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  mode);  // 23216
		ZSTD_allocateChainTable(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  useRowMatchFinder,
					const U32  forDDSDict);  // 24614
		ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
		ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
					const ZSTD_ParamSwitch_e  mode);  // 24632
		ZSTD_cwksp_align(size_t size,
				size_t align);  // 18508
		ZSTD_cwksp_aligned_alloc_size(size_t size,
						size_t alignment);  // 18516
		ZSTD_cwksp_aligned64_alloc_size(size_t size); // 24633
		ZSTD_sizeof_matchState(const ZSTD_compressionParameters* cParams,
					const ZSTD_ParamSwitch_e  useRowMatchFinder,
					const int  enableDedicatedDictSearch,
					const U32  forCCtx);  // 28572
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 28575
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18954
		ZSTD_cwksp_clear(ZSTD_cwksp* ws); // 18957
		ZSTD_cwksp_init(ZSTD_cwksp* ws,
				void* start,
				size_t size,
				ZSTD_cwksp_static_alloc_e isStatic);  // 28584
		ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
						size_t bytes);  // 28586
		ZSTD_cwksp_move(ZSTD_cwksp* dst,
				ZSTD_cwksp* src);  // 28588
	}
	ZSTD_createCDict_advanced_internal(size_t dictSize,
						ZSTD_dictLoadMethod_e dictLoadMethod,
						ZSTD_compressionParameters cParams,
						ZSTD_ParamSwitch_e useRowMatchFinder,
						int enableDedicatedDictSearch,
						ZSTD_customMem customMem);  // 28648
	ZSTD_dedicatedDictSearch_getCParams(const int  compressionLevel,
						const size_t  dictSize);  // 28629
	ZSTD_overrideCParams(ZSTD_compressionParameters* cParams,
				const ZSTD_compressionParameters* overrides);  // 28631
} /* size: 0, variables: 3, inline expansions: 7 (1035 bytes) */

// <06FE0DFE> thirdparty/zstd/zstd.c:28664
// variables: 2
ZSTD_CDict* ZSTD_createCDict(const void* dict, size_t dictSize, int compressionLevel)
{
	ZSTD_compressionParameters cParams; // 28666
	const ZSTD_CDict* cdict; // 28667
} /* size: 209, variables: 2 */

// <06FE0B56> thirdparty/zstd/zstd.c:28686
// variables: 2
// function calls: 4
size_t ZSTD_freeCDict(ZSTD_CDict* cdict)
{
	{
		const ZSTD_customMem  cMem; // 28689
		int cdictInWorkspace; // 28690
		ZSTD_cwksp_owns_buffer(const ZSTD_cwksp* ws,
					const void* ptr);  // 28690
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 18979
		ZSTD_cwksp_free(ZSTD_cwksp* ws,
				ZSTD_customMem customMem);  // 28691
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 28693
	}
} /* size: 256 */

// <06FE0649> thirdparty/zstd/zstd.c:28712
// variables: 6
// function calls: 13
const ZSTD_CDict* ZSTD_initStaticCDict(void* workspace, size_t workspaceSize, const void* dict, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType, ZSTD_compressionParameters cParams)
{
	const ZSTD_ParamSwitch_e  useRowMatchFinder; // 28719
	const size_t  matchStateSize; // 28721
	const size_t  neededSize; // 28722
	ZSTD_CDict* cdict; // 28727
	ZSTD_CCtx_params params; // 28728
	{
		ZSTD_cwksp ws; // 28734
		ZSTD_cwksp_initialAllocStart(ZSTD_cwksp* ws); // 18954
		ZSTD_cwksp_clear(ZSTD_cwksp* ws); // 18957
		ZSTD_cwksp_init(ZSTD_cwksp* ws,
				void* start,
				size_t size,
				ZSTD_cwksp_static_alloc_e isStatic);  // 28735
		ZSTD_cwksp_reserve_object(ZSTD_cwksp* ws,
						size_t bytes);  // 28736
		ZSTD_cwksp_move(ZSTD_cwksp* dst,
				ZSTD_cwksp* src);  // 28738
	}
	ZSTD_cwksp_align(size_t size,
			size_t align);  // 18508
	ZSTD_cwksp_aligned_alloc_size(size_t size,
					size_t alignment);  // 18516
	ZSTD_cwksp_aligned64_alloc_size(size_t size); // 24633
	ZSTD_sizeof_matchState(const ZSTD_compressionParameters* cParams,
				const ZSTD_ParamSwitch_e  useRowMatchFinder,
				const int  enableDedicatedDictSearch,
				const U32  forCCtx);  // 28721
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 28719
	ZSTD_cwksp_align(size_t size,
			size_t align);  // 28724
} /* size: 465, variables: 5, inline expansions: 8 (146 bytes) */

// <06FE0624> thirdparty/zstd/zstd.c:28758
ZSTD_compressionParameters ZSTD_getCParamsFromCDict(const ZSTD_CDict* cdict)
{
} /* size: 0 */

// <06FE05EE> thirdparty/zstd/zstd.c:28768
unsigned int ZSTD_getDictID_fromCDict(const ZSTD_CDict* cdict)
{
} /* size: 19 */

// <06FE03D5> thirdparty/zstd/zstd.c:28777
// variables: 4
// function calls: 3
size_t ZSTD_compressBegin_usingCDict_internal(const ZSTD_CCtx* cctx, const ZSTD_CDict* cdict, const ZSTD_frameParameters fParams, const long long unsigned int pledgedSrcSize)
{
	ZSTD_CCtx_params cctxParams; // 28781
	{
		ZSTD_parameters params; // 28786
		ZSTD_getCParamsFromCDict(const ZSTD_CDict* cdict); // 28792
	}
	{
		const U32  limitedSrcSize; // 28803
		const U32  limitedSrcLog; // 28804
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 28804
	}
} /* size: 613, variables: 1 */

// <06FE033B> thirdparty/zstd/zstd.c:28818
size_t ZSTD_compressBegin_usingCDict_advanced(const ZSTD_CCtx* cctx, const ZSTD_CDict* cdict, const ZSTD_frameParameters fParams, const long long unsigned int pledgedSrcSize)
{
} /* size: 11 */

// <06FE02FC> thirdparty/zstd/zstd.c:28827
// variable: 1
size_t ZSTD_compressBegin_usingCDict_deprecated(ZSTD_CCtx* cctx, const ZSTD_CDict* cdict)
{
	const ZSTD_frameParameters  fParams; // 28829
} /* size: 0, variables: 1 */

// <06FE0242> thirdparty/zstd/zstd.c:28833
// function call: 1
size_t ZSTD_compressBegin_usingCDict(ZSTD_CCtx* cctx, const ZSTD_CDict* cdict)
{
	ZSTD_compressBegin_usingCDict_deprecated(ZSTD_CCtx* cctx,
						const ZSTD_CDict* cdict);  // 28835
} /* size: 26, inline expansions: 1 (22 bytes) */

// <06FE01C5> thirdparty/zstd/zstd.c:28841
// variable: 1
size_t ZSTD_compress_usingCDict_internal(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const ZSTD_CDict* cdict, ZSTD_frameParameters fParams)
{
	{
		const size_t  err_code; // 28846
	}
} /* size: 0 */

// <06FE0030> thirdparty/zstd/zstd.c:28853
// variable: 1
// function call: 1
size_t ZSTD_compress_usingCDict_advanced(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const ZSTD_CDict* cdict, ZSTD_frameParameters fParams)
{
	{
		const size_t  err_code; // 28846
	}
	ZSTD_compress_usingCDict_internal(ZSTD_CCtx* cctx,
						void* dst,
						size_t dstCapacity,
						const void* src,
						size_t srcSize,
						const ZSTD_CDict* cdict,
						ZSTD_frameParameters fParams);  // 28858
} /* size: 113, inline expansions: 1 (49 bytes) */

// <06FDFE61> thirdparty/zstd/zstd.c:28881
ZSTD_CStream* ZSTD_createCStream(void)
{
} /* size: 54 */

// <06FDFDF3> thirdparty/zstd/zstd.c:28887
ZSTD_CStream* ZSTD_initStaticCStream(void* workspace, size_t workspaceSize)
{
} /* size: 9 */

// <06FDFDAE> thirdparty/zstd/zstd.c:28892
ZSTD_CStream* ZSTD_createCStream_advanced(ZSTD_customMem customMem)
{
} /* size: 9 */

// <06FDFD5C> thirdparty/zstd/zstd.c:28897
size_t ZSTD_freeCStream(ZSTD_CStream* zcs)
{
} /* size: 9 */

// <06FDFD4C> thirdparty/zstd/zstd.c:28906
size_t ZSTD_CStreamInSize(void)
{
} /* size: 0 */

// <06FDFD0F> thirdparty/zstd/zstd.c:28908
size_t ZSTD_CStreamOutSize(void)
{
} /* size: 24 */

// <06FDFCD5> thirdparty/zstd/zstd.c:28913
ZSTD_CParamMode_e ZSTD_getCParamMode(const ZSTD_CDict* cdict, const ZSTD_CCtx_params* params, U64 pledgedSrcSize)
{
} /* size: 0 */

// <06FDFBF5> thirdparty/zstd/zstd.c:28923
// variables: 3
size_t ZSTD_resetCStream(ZSTD_CStream* zcs, long long unsigned int pss)
{
	const U64  pledgedSrcSize; // 28929
	{
		const size_t  err_code; // 28931
	}
	{
		const size_t  err_code; // 28932
	}
} /* size: 73, variables: 1 */

// <06FDFA1C> thirdparty/zstd/zstd.c:28940
// variables: 4
size_t ZSTD_initCStream_internal(ZSTD_CStream* zcs, const void* dict, size_t dictSize, const ZSTD_CDict* cdict, const ZSTD_CCtx_params* params, long long unsigned int pledgedSrcSize)
{
	{
		const size_t  err_code; // 28946
	}
	{
		const size_t  err_code; // 28947
	}
	{
		const size_t  err_code; // 28952
	}
	{
		const size_t  err_code; // 28955
	}
} /* size: 0 */

// <06FDF8F3> thirdparty/zstd/zstd.c:28962
// variables: 3
size_t ZSTD_initCStream_usingCDict_advanced(ZSTD_CStream* zcs, const ZSTD_CDict* cdict, ZSTD_frameParameters fParams, long long unsigned int pledgedSrcSize)
{
	{
		const size_t  err_code; // 28968
	}
	{
		const size_t  err_code; // 28969
	}
	{
		const size_t  err_code; // 28971
	}
} /* size: 127 */

// <06FDF828> thirdparty/zstd/zstd.c:28976
// variables: 2
size_t ZSTD_initCStream_usingCDict(ZSTD_CStream* zcs, const ZSTD_CDict* cdict)
{
	{
		const size_t  err_code; // 28979
	}
	{
		const size_t  err_code; // 28980
	}
} /* size: 59 */

// <06FDF655> thirdparty/zstd/zstd.c:28989
// variables: 5
// function call: 1
size_t ZSTD_initCStream_advanced(ZSTD_CStream* zcs, const void* dict, size_t dictSize, ZSTD_parameters params, long long unsigned int pss)
{
	const U64  pledgedSrcSize; // 28997
	{
		const size_t  err_code; // 28999
	}
	{
		const size_t  err_code; // 29000
	}
	{
		const size_t  err_code; // 29001
	}
	{
		const size_t  err_code; // 29003
	}
	ZSTD_CCtxParams_setZstdParams(ZSTD_CCtx_params* cctxParams,
					const ZSTD_parameters* params);  // 29002
} /* size: 237, variables: 1, inline expansions: 1 (51 bytes) */

// <06FDF50F> thirdparty/zstd/zstd.c:29007
// variables: 3
size_t ZSTD_initCStream_usingDict(ZSTD_CStream* zcs, const void* dict, size_t dictSize, int compressionLevel)
{
	{
		const size_t  err_code; // 29010
	}
	{
		const size_t  err_code; // 29011
	}
	{
		const size_t  err_code; // 29012
	}
} /* size: 111 */

// <06FDF38B> thirdparty/zstd/zstd.c:29016
// variables: 5
size_t ZSTD_initCStream_srcSize(ZSTD_CStream* zcs, int compressionLevel, long long unsigned int pss)
{
	const U64  pledgedSrcSize; // 29022
	{
		const size_t  err_code; // 29024
	}
	{
		const size_t  err_code; // 29025
	}
	{
		const size_t  err_code; // 29026
	}
	{
		const size_t  err_code; // 29027
	}
} /* size: 142, variables: 1 */

// <06FDF276> thirdparty/zstd/zstd.c:29031
// variables: 3
size_t ZSTD_initCStream(ZSTD_CStream* zcs, int compressionLevel)
{
	{
		const size_t  err_code; // 29034
	}
	{
		const size_t  err_code; // 29035
	}
	{
		const size_t  err_code; // 29036
	}
} /* size: 95 */

// <06FDF247> thirdparty/zstd/zstd.c:29042
// variable: 1
size_t ZSTD_nextInputSizeHint(const ZSTD_CCtx* cctx)
{
	{
		size_t hintInSize; // 29048
	}
} /* size: 0 */

// <06FDF0C2> thirdparty/zstd/zstd.c:29057
// variables: 22
size_t ZSTD_compressStream_generic(ZSTD_CStream* zcs, ZSTD_outBuffer* output, ZSTD_inBuffer* input, const ZSTD_EndDirective flushMode)
{
	const char* istart; // 29062
	const char* iend; // 29063
	const char* ip; // 29064
	const char* ostart; // 29065
	const char* oend; // 29066
	char* op; // 29067
	U32 someMoreWork; // 29068
	{
		const size_t  cSize; // 29105
		{
			const size_t  err_code; // 29109
		}
	}
	{
		const size_t  toLoad; // 29118
		const size_t  loaded; // 29119
	}
	{
		const int  inputBuffered; // 29151
		void* cDst; // 29152
		size_t cSize; // 29153
		size_t oSize; // 29154
		const size_t  iSize; // 29155
		{
			const unsigned int  lastBlock; // 29162
			{
				const size_t  err_code; // 29168
			}
		}
		{
			const unsigned int  lastBlock; // 29180
			{
				const size_t  err_code; // 29186
			}
		}
	}
	{
		const size_t  toFlush; // 29207
		const size_t  flushed; // 29208
	}
} /* size: 0, variables: 7 */

// <06FDF0A2> thirdparty/zstd/zstd.c:29243
size_t ZSTD_nextInputSizeHint_MTorST(const ZSTD_CCtx* cctx)
{
} /* size: 0 */

// <06FDEF46> thirdparty/zstd/zstd.c:29255
// variables: 2
// function calls: 3
size_t ZSTD_compressStream(ZSTD_CStream* zcs, ZSTD_outBuffer* output, ZSTD_inBuffer* input)
{
	{
		const size_t  err_code; // 29257
	}
	ZSTDMT_nextInputSizeHint(const ZSTDMT_CCtx* mtctx); // 29248
	{
		size_t hintInSize; // 29048
	}
	ZSTD_nextInputSizeHint(const ZSTD_CCtx* cctx); // 29251
	ZSTD_nextInputSizeHint_MTorST(const ZSTD_CCtx* cctx); // 29258
} /* size: 144, inline expansions: 3 (155 bytes) */

// <06FDEE9B> thirdparty/zstd/zstd.c:29279
// variables: 2
size_t ZSTD_checkBufferStability(const ZSTD_CCtx* cctx, const ZSTD_outBuffer* output, const ZSTD_inBuffer* input, ZSTD_EndDirective endOp)
{
	{
		const ZSTD_inBuffer  expect; // 29285
	}
	{
		const size_t  outBufferSize; // 29291
	}
} /* size: 0 */

// <06FDE8D5> thirdparty/zstd/zstd.c:29303
// variables: 8
// function calls: 14
size_t ZSTD_CCtx_init_compressStream2(ZSTD_CCtx* cctx, ZSTD_EndDirective endOp, size_t inSize)
{
	ZSTD_CCtx_params params; // 29307
	const ZSTD_prefixDict  prefixDict; // 29308
	{
		const size_t  err_code; // 29309
		ZSTD_initLocalDict(ZSTD_CCtx* cctx); // 29309
	}
	{
		const size_t  dictSize; // 29322
		const ZSTD_CParamMode_e  mode; // 29325
		ZSTD_shouldAttachDict(const ZSTD_CDict* cdict,
					const ZSTD_CCtx_params* params,
					U64 pledgedSrcSize);  // 28915
		ZSTD_getCParamMode(const ZSTD_CDict* cdict,
					const ZSTD_CCtx_params* params,
					U64 pledgedSrcSize);  // 29325
	}
	{
		const size_t  err_code; // 29362
	}
	{
		const U64  pledgedSrcSize; // 29374
		{
			const size_t  err_code; // 29376
		}
	}
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23202
	ZSTD_resolveBlockSplitterMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 29331
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 23223
	ZSTD_resolveEnableLdm(ZSTD_ParamSwitch_e mode,
				const ZSTD_compressionParameters* cParams);  // 29332
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23196
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 23192
	ZSTD_resolveRowMatchFinderMode(ZSTD_ParamSwitch_e mode,
					const ZSTD_compressionParameters* cParams);  // 29333
	ZSTD_resolveMaxBlockSize(size_t maxBlockSize); // 29335
	ZSTD_resolveExternalRepcodeSearch(ZSTD_ParamSwitch_e value,
						int cLevel);  // 29336
	ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params); // 29340
	ZSTDMT_createCCtx_advanced(unsigned int nbWorkers,
					ZSTD_customMem cMem,
					ZSTD_threadPool* pool);  // 29357
} /* size: 1806, variables: 2, inline expansions: 11 (298 bytes) */

// <06FDDF92> thirdparty/zstd/zstd.c:29401
// variables: 27
// function calls: 13
size_t ZSTD_compressStream2(ZSTD_CCtx* cctx, ZSTD_outBuffer* output, ZSTD_inBuffer* input, ZSTD_EndDirective endOp)
{
	{
		const size_t  inputSize; // 29415
		const size_t  totalInputSize; // 29416
		{
			const size_t  err_code; // 29434
		}
		ZSTD_setBufferExpectations(ZSTD_CCtx* cctx,
						const ZSTD_outBuffer* output,
						const ZSTD_inBuffer* input);  // 29265
		ZSTD_setBufferExpectations(ZSTD_CCtx* cctx,
						const ZSTD_outBuffer* output,
						const ZSTD_inBuffer* input);  // 29435
	}
	{
		const size_t  err_code; // 29439
		{
			const ZSTD_inBuffer  expect; // 29285
		}
		{
			const size_t  outBufferSize; // 29291
		}
		ZSTD_checkBufferStability(const ZSTD_CCtx* cctx,
						const ZSTD_outBuffer* output,
						const ZSTD_inBuffer* input,
						ZSTD_EndDirective endOp);  // 29439
	}
	{
		size_t flushMin; // 29443
		{
			const size_t  ipos; // 29456
			const size_t  opos; // 29457
			{
				const size_t  err_code; // 29467
			}
		}
		ZSTD_setBufferExpectations(ZSTD_CCtx* cctx,
						const ZSTD_outBuffer* output,
						const ZSTD_inBuffer* input);  // 29265
		ZSTD_setBufferExpectations(ZSTD_CCtx* cctx,
						const ZSTD_outBuffer* output,
						const ZSTD_inBuffer* input);  // 29490
	}
	{
		const size_t  err_code; // 29494
		{
			const size_t  toFlush; // 29207
			const size_t  flushed; // 29208
			ZSTD_limitCopy(void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize);  // 29208
		}
		{
			const size_t  cSize; // 29105
			{
				const size_t  err_code; // 29109
			}
		}
		{
			const size_t  toLoad; // 29118
			const size_t  loaded; // 29119
			ZSTD_limitCopy(void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize);  // 29119
		}
		{
			const int  inputBuffered; // 29151
			void* cDst; // 29152
			size_t cSize; // 29153
			size_t oSize; // 29154
			const size_t  iSize; // 29155
			{
				const unsigned int  lastBlock; // 29162
				{
					const size_t  err_code; // 29168
				}
				ZSTD_compressContinue_public(ZSTD_CCtx* cctx,
								void* dst,
								size_t dstCapacity,
								const void* src,
								size_t srcSize);  // 29166
			}
			{
				const unsigned int  lastBlock; // 29180
				ZSTD_compressContinue_public(ZSTD_CCtx* cctx,
								void* dst,
								size_t dstCapacity,
								const void* src,
								size_t srcSize);  // 29183
				{
					const size_t  err_code; // 29186
				}
			}
		}
		{
			size_t hintInSize; // 29048
		}
		ZSTD_nextInputSizeHint(const ZSTD_CCtx* cctx); // 29240
		ZSTD_compressStream_generic(ZSTD_CStream* zcs,
						ZSTD_outBuffer* output,
						ZSTD_inBuffer* input,
						const ZSTD_EndDirective  flushMode);  // 29494
	}
	ZSTD_setBufferExpectations(ZSTD_CCtx* cctx,
					const ZSTD_outBuffer* output,
					const ZSTD_inBuffer* input);  // 29265
	ZSTD_setBufferExpectations(ZSTD_CCtx* cctx,
					const ZSTD_outBuffer* output,
					const ZSTD_inBuffer* input);  // 29496
} /* size: 2707, inline expansions: 2 (87 bytes) */

// <06FDDE55> thirdparty/zstd/zstd.c:29500
// variables: 3
size_t ZSTD_compressStream2_simpleArgs(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, size_t* dstPos, const void* src, size_t srcSize, size_t* srcPos, ZSTD_EndDirective endOp)
{
	ZSTD_outBuffer output; // 29506
	ZSTD_inBuffer input; // 29507
	{
		const size_t  cErr; // 29515
	}
} /* size: 131, variables: 2 */

// <06FDDCCF> thirdparty/zstd/zstd.c:29522
// variables: 6
size_t ZSTD_compress2(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	const ZSTD_bufferMode_e  originalInBufferMode; // 29526
	const ZSTD_bufferMode_e  originalOutBufferMode; // 29527
	{
		size_t oPos; // 29533
		size_t iPos; // 29534
		const size_t  result; // 29535
		{
			const size_t  err_code; // 29543
		}
	}
} /* size: 194, variables: 2 */

// <06FDDC3A> thirdparty/zstd/zstd.c:29558
// variables: 3
size_t ZSTD_validateSequence(U32 offBase, U32 matchLength, U32 minMatch, size_t posInSrc, U32 windowLog, size_t dictSize, int useSequenceProducer)
{
	const U32  windowSize; // 29561
	const size_t  offsetBound; // 29567
	const size_t  matchLenLowerBound; // 29568
} /* size: 0, variables: 3 */

// <06FDDBF3> thirdparty/zstd/zstd.c:29576
// variable: 1
U32 ZSTD_finalizeOffBase(U32 rawOffset, const U32* rep, U32 ll0)
{
	U32 offBase; // 29578
} /* size: 0, variables: 1 */

// <06FDD41A> thirdparty/zstd/zstd.c:29599
// variables: 16
// function calls: 21
size_t ZSTD_transferSequences_wBlockDelim(ZSTD_CCtx* cctx, ZSTD_SequencePosition* seqPos, const ZSTD_Sequence* inSeqs, size_t inSeqsSize, const void* src, size_t blockSize, ZSTD_ParamSwitch_e externalRepSearch)
{
	U32 idx; // 29605
	const U32  startIdx; // 29606
	const BYTE* ip; // 29607
	const BYTE* iend; // 29608
	Repcodes_t updatedRepcodes; // 29609
	U32 dictSize; // 29610
	{
		const U32  litLength; // 29623
		const U32  matchLength; // 29624
		U32 offBase; // 29625
		{
			const U32  ll0; // 29630
			ZSTD_finalizeOffBase(U32 rawOffset,
						const U32* rep,
						U32 ll0);  // 29631
			{
				const U32  repCode; // 19979
				{
					const U32  currentOffset; // 19981
				}
			}
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 19972
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 29632
		}
		{
			const size_t  err_code; // 29638
			ZSTD_validateSequence(U32 offBase,
						U32 matchLength,
						U32 minMatch,
						size_t posInSrc,
						U32 windowLog,
						size_t dictSize,
						int useSequenceProducer);  // 29638
			ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params); // 29638
		}
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 29646
	}
	{
		const U32* rep; // 29655
		U32 lastSeqIdx; // 29656
	}
	ZSTD_storeLastLiterals(SeqStore_t* seqStorePtr,
				const BYTE* anchor,
				size_t lastLLSize);  // 29678
} /* size: 1349, variables: 6, inline expansions: 1 (26 bytes) */

// <06FDCB9A> thirdparty/zstd/zstd.c:29699
// variables: 23
// function calls: 21
size_t ZSTD_transferSequences_noDelim(ZSTD_CCtx* cctx, ZSTD_SequencePosition* seqPos, const ZSTD_Sequence* inSeqs, size_t inSeqsSize, const void* src, size_t blockSize, ZSTD_ParamSwitch_e externalRepSearch)
{
	U32 idx; // 29705
	U32 startPosInSequence; // 29706
	U32 endPosInSequence; // 29707
	size_t dictSize; // 29708
	const BYTE* istart; // 29709
	const BYTE* ip; // 29710
	const BYTE* iend; // 29711
	Repcodes_t updatedRepcodes; // 29712
	U32 bytesAdjustment; // 29713
	U32 finalMatchSplit; // 29714
	{
		const ZSTD_Sequence  currSeq; // 29730
		U32 litLength; // 29731
		U32 matchLength; // 29732
		const U32  rawOffset; // 29733
		U32 offBase; // 29734
		{
			U32 firstHalfMatchLength; // 29754
			{
				U32 secondHalfMatchLength; // 29759
			}
		}
		{
			const U32  ll0; // 29786
			ZSTD_finalizeOffBase(U32 rawOffset,
						const U32* rep,
						U32 ll0);  // 29787
			{
				const U32  repCode; // 19979
				{
					const U32  currentOffset; // 19981
				}
			}
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 19972
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 29788
		}
		{
			const size_t  err_code; // 29793
			ZSTD_validateSequence(U32 offBase,
						U32 matchLength,
						U32 minMatch,
						size_t posInSrc,
						U32 windowLog,
						size_t dictSize,
						int useSequenceProducer);  // 29793
			ZSTD_hasExtSeqProd(const ZSTD_CCtx_params* params); // 29793
		}
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 29800
	}
	{
		const U32  lastLLSize; // 29814
		ZSTD_storeLastLiterals(SeqStore_t* seqStorePtr,
					const BYTE* anchor,
					size_t lastLLSize);  // 29817
	}
} /* size: 1553, variables: 10 */

// <06FDCB7A> thirdparty/zstd/zstd.c:29835
ZSTD_SequenceCopier_f ZSTD_selectSequenceCopier(ZSTD_SequenceFormat_e mode)
{
} /* size: 0 */

// <06FDCB19> thirdparty/zstd/zstd.c:29850
// variables: 3
size_t blockSize_explicitDelimiter(const ZSTD_Sequence* inSeqs, size_t inSeqsSize, ZSTD_SequencePosition seqPos)
{
	int end; // 29852
	size_t blockSize; // 29853
	size_t spos; // 29854
} /* size: 0, variables: 3 */

// <06FDCA9A> thirdparty/zstd/zstd.c:29872
// variables: 2
size_t determine_blockSize(ZSTD_SequenceFormat_e mode, size_t blockSize, size_t remaining, const ZSTD_Sequence* inSeqs, size_t inSeqsSize, ZSTD_SequencePosition seqPos)
{
	{
		const size_t  explicitBlockSize; // 29883
		{
			const size_t  err_code; // 29884
		}
	}
} /* size: 0 */

// <06FDC916> thirdparty/zstd/zstd.c:29899
// variables: 18
size_t ZSTD_compressSequences_internal(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const ZSTD_Sequence* inSeqs, size_t inSeqsSize, const void* src, size_t srcSize)
{
	size_t cSize; // 29904
	size_t remaining; // 29905
	ZSTD_SequencePosition seqPos; // 29906
	const BYTE* ip; // 29908
	BYTE* op; // 29909
	const ZSTD_SequenceCopier_f  sequenceCopier; // 29910
	{
		const U32  cBlockHeader24; // 29915
	}
	{
		size_t compressedSeqsSize; // 29924
		size_t cBlockSize; // 29925
		size_t blockSize; // 29926
		const U32  lastBlock; // 29929
		{
			const size_t  err_code; // 29930
		}
		{
			const size_t  err_code; // 29938
		}
		{
			const size_t  err_code; // 29945
		}
		{
			const size_t  err_code; // 29963
		}
		{
			const size_t  err_code; // 29979
		}
		{
			const size_t  err_code; // 29983
		}
		{
			U32 cBlockHeader; // 29986
		}
	}
} /* size: 0, variables: 6 */

// <06FDBACD> thirdparty/zstd/zstd.c:30017
// variables: 21
// function calls: 38
size_t ZSTD_compressSequences(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const ZSTD_Sequence* inSeqs, size_t inSeqsSize, const void* src, size_t srcSize)
{
	BYTE* op; // 30022
	size_t cSize; // 30023
	{
		const size_t  err_code; // 30028
	}
	{
		const size_t  frameHeaderSize; // 30031
	}
	{
		const size_t  cBlocksSize; // 30043
		{
			const size_t  err_code; // 30047
		}
		{
			const U32  cBlockHeader24; // 29915
			MEM_write32(void* memPtr,
					U32 value);  // 1309
			MEM_writeLE32(void* memPtr,
					U32 val32);  // 29917
		}
		{
			size_t compressedSeqsSize; // 29924
			size_t cBlockSize; // 29925
			size_t blockSize; // 29926
			const U32  lastBlock; // 29929
			{
				const size_t  explicitBlockSize; // 29883
				blockSize_explicitDelimiter(const ZSTD_Sequence* inSeqs,
								size_t inSeqsSize,
								ZSTD_SequencePosition seqPos);  // 29883
				{
					const size_t  err_code; // 29884
				}
			}
			determine_blockSize(ZSTD_SequenceFormat_e mode,
						size_t blockSize,
						size_t remaining,
						const ZSTD_Sequence* inSeqs,
						size_t inSeqsSize,
						ZSTD_SequencePosition seqPos);  // 29926
			{
				const size_t  err_code; // 29930
			}
			ZSTD_resetSeqStore(SeqStore_t* ssPtr); // 29932
			{
				const size_t  err_code; // 29938
			}
			MEM_write16(void* memPtr,
					U16 value);  // 1279
			MEM_writeLE16(void* memPtr,
					U16 val);  // 1294
			MEM_writeLE24(void* memPtr,
					U32 val);  // 19810
			ZSTD_noCompressBlock(void* dst,
						size_t dstCapacity,
						const void* src,
						size_t srcSize,
						U32 lastBlock);  // 29944
			{
				const size_t  err_code; // 25984
			}
			{
				const size_t  maxCSize; // 25987
				ZSTD_minGain(size_t srcSize,
						ZSTD_strategy strat);  // 25987
			}
			ZSTD_entropyCompressSeqStore_wExtLitBuffer(void* dst,
									size_t dstCapacity,
									const void* literals,
									size_t litSize,
									size_t blockSize,
									const SeqStore_t* seqStorePtr,
									const ZSTD_entropyCTables_t* prevEntropy,
									ZSTD_entropyCTables_t* nextEntropy,
									const ZSTD_CCtx_params* cctxParams,
									void* entropyWorkspace,
									size_t entropyWkspSize,
									int bmi2);  // 26009
			ZSTD_entropyCompressSeqStore(const SeqStore_t* seqStorePtr,
							const ZSTD_entropyCTables_t* prevEntropy,
							ZSTD_entropyCTables_t* nextEntropy,
							const ZSTD_CCtx_params* cctxParams,
							void* dst,
							size_t dstCapacity,
							size_t srcSize,
							void* entropyWorkspace,
							size_t entropyWkspSize,
							int bmi2);  // 29956
			{
				const size_t  err_code; // 29963
			}
			ZSTD_maybeRLE(const SeqStore_t* seqStore); // 29967
			{
				U32 cBlockHeader; // 29986
				ZSTD_blockState_confirmRepcodesAndEntropyTables(const ZSTD_blockState_t* bs); // 29988
				MEM_write16(void* memPtr,
						U16 value);  // 1279
				MEM_writeLE16(void* memPtr,
						U16 val);  // 1294
				MEM_writeLE24(void* memPtr,
						U32 val);  // 29994
			}
			MEM_write16(void* memPtr,
					U16 value);  // 1279
			MEM_writeLE16(void* memPtr,
					U16 val);  // 1294
			MEM_writeLE24(void* memPtr,
					U32 val);  // 19821
			ZSTD_rleCompressBlock(void* dst,
						size_t dstCapacity,
						BYTE src,
						size_t srcSize,
						U32 lastBlock);  // 29982
			MEM_write16(void* memPtr,
					U16 value);  // 1279
			MEM_writeLE16(void* memPtr,
					U16 val);  // 1294
			MEM_writeLE24(void* memPtr,
					U32 val);  // 19810
			ZSTD_noCompressBlock(void* dst,
						size_t dstCapacity,
						const void* src,
						size_t srcSize,
						U32 lastBlock);  // 29978
			{
				const size_t  err_code; // 29979
			}
		}
		ZSTD_selectSequenceCopier(ZSTD_SequenceFormat_e mode); // 29910
		ZSTD_compressSequences_internal(ZSTD_CCtx* cctx,
						void* dst,
						size_t dstCapacity,
						const ZSTD_Sequence* inSeqs,
						size_t inSeqsSize,
						const void* src,
						size_t srcSize);  // 30043
	}
	{
		const U32  checksum; // 30055
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11682
		XXH64_mergeRound(xxh_u64 acc,
				xxh_u64 val);  // 11909
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11682
		XXH64_mergeRound(xxh_u64 acc,
				xxh_u64 val);  // 11910
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11682
		XXH64_mergeRound(xxh_u64 acc,
				xxh_u64 val);  // 11911
		XXH64_round(xxh_u64 acc,
				xxh_u64 input);  // 11682
		XXH64_mergeRound(xxh_u64 acc,
				xxh_u64 val);  // 11912
		XXH_INLINE_XXH64_digest(const XXH_NAMESPACEXXH64_state_t* state); // 30055
		MEM_write32(void* memPtr,
				U32 value);  // 1309
		MEM_writeLE32(void* memPtr,
				U32 val32);  // 30058
	}
} /* size: 1903, variables: 2 */

// <06FDBA7B> thirdparty/zstd/zstd.c:30239
// variables: 2
size_t convertSequences_noRepcodes(SeqDef* dstSeqs, const ZSTD_Sequence* inSeqs, size_t nbSequences)
{
	size_t longLen; // 30244
	size_t n; // 30245
} /* size: 0, variables: 2 */

// <06FDB9AA> thirdparty/zstd/zstd.c:30272
// variables: 9
size_t ZSTD_convertBlockSequences(ZSTD_CCtx* cctx, const ZSTD_Sequence* inSeqs, size_t nbSequences, int repcodeResolution)
{
	Repcodes_t updatedRepcodes; // 30276
	size_t seqNb; // 30277
	{
		const size_t  longl; // 30293
	}
	{
		const U32  litLength; // 30311
		const U32  matchLength; // 30312
		const U32  ll0; // 30313
		const U32  offBase; // 30314
	}
	{
		const U32* rep; // 30324
		{
			U32 lastSeqIdx; // 30327
		}
	}
} /* size: 0, variables: 2 */

// <06FDB933> thirdparty/zstd/zstd.c:30402
// variables: 5
BlockSummary ZSTD_get1BlockSummary(const ZSTD_Sequence* seqs, size_t nbSeqs)
{
	size_t totalMatchSize; // 30404
	size_t litSize; // 30405
	size_t n; // 30406
	{
		BlockSummary bs; // 30417
	}
	{
		BlockSummary bs; // 30421
	}
} /* size: 0, variables: 3 */

// <06FDB7E7> thirdparty/zstd/zstd.c:30432
// variables: 14
size_t ZSTD_compressSequencesAndLiterals_internal(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const ZSTD_Sequence* inSeqs, size_t nbSequences, const void* literals, size_t litSize, size_t srcSize)
{
	size_t remaining; // 30437
	size_t cSize; // 30438
	BYTE* op; // 30439
	const int  repcodeResolution; // 30440
	{
		const U32  cBlockHeader24; // 30448
	}
	{
		size_t compressedSeqsSize; // 30457
		size_t cBlockSize; // 30457
		size_t conversionStatus; // 30457
		const BlockSummary  block; // 30458
		const U32  lastBlock; // 30459
		{
			const size_t  err_code; // 30460
		}
		{
			const size_t  err_code; // 30468
		}
		{
			const size_t  err_code; // 30488
		}
		{
			U32 cBlockHeader; // 30506
		}
	}
} /* size: 0, variables: 4 */

// <06FDB1E9> thirdparty/zstd/zstd.c:30539
// variables: 16
// function calls: 11
size_t ZSTD_compressSequencesAndLiterals(ZSTD_CCtx* cctx, void* dst, size_t dstCapacity, const ZSTD_Sequence* inSeqs, size_t inSeqsSize, const void* literals, size_t litSize, size_t litCapacity, size_t decompressedSize)
{
	BYTE* op; // 30545
	size_t cSize; // 30546
	{
		const size_t  err_code; // 30554
	}
	{
		const size_t  frameHeaderSize; // 30567
	}
	{
		const size_t  cBlocksSize; // 30576
		{
			const size_t  err_code; // 30580
		}
		{
			const U32  cBlockHeader24; // 30448
			MEM_write16(void* memPtr,
					U16 value);  // 1279
			MEM_writeLE16(void* memPtr,
					U16 val);  // 1294
			MEM_writeLE24(void* memPtr,
					U32 val);  // 30450
		}
		{
			size_t compressedSeqsSize; // 30457
			size_t cBlockSize; // 30457
			size_t conversionStatus; // 30457
			const BlockSummary  block; // 30458
			const U32  lastBlock; // 30459
			{
				BlockSummary bs; // 30417
			}
			{
				BlockSummary bs; // 30421
			}
			ZSTD_get1BlockSummary(const ZSTD_Sequence* seqs,
						size_t nbSeqs);  // 30458
			ZSTD_resetSeqStore(SeqStore_t* ssPtr); // 30463
			ZSTD_convertBlockSequences(ZSTD_CCtx* cctx,
							const ZSTD_Sequence* inSeqs,
							size_t nbSequences,
							int repcodeResolution);  // 30465
			{
				const size_t  err_code; // 30488
			}
			{
				U32 cBlockHeader; // 30506
				ZSTD_blockState_confirmRepcodesAndEntropyTables(const ZSTD_blockState_t* bs); // 30509
				MEM_write16(void* memPtr,
						U16 value);  // 1279
				MEM_writeLE16(void* memPtr,
						U16 val);  // 1294
				MEM_writeLE24(void* memPtr,
						U32 val);  // 30515
			}
		}
		ZSTD_compressSequencesAndLiterals_internal(ZSTD_CCtx* cctx,
								void* dst,
								size_t dstCapacity,
								const ZSTD_Sequence* inSeqs,
								size_t nbSequences,
								const void* literals,
								size_t litSize,
								size_t srcSize);  // 30576
	}
} /* size: 895, variables: 2 */

// <06FDB1A9> thirdparty/zstd/zstd.c:30592
// variables: 2
ZSTD_inBuffer inBuffer_forEndFlush(const ZSTD_CStream* zcs)
{
	const ZSTD_inBuffer  nullInput; // 30594
	const int  stableInput; // 30595
} /* size: 0, variables: 2 */

// <06FDB0CF> thirdparty/zstd/zstd.c:30601
// variable: 1
// function call: 1
size_t ZSTD_flushStream(ZSTD_CStream* zcs, ZSTD_outBuffer* output)
{
	ZSTD_inBuffer input; // 30603
	inBuffer_forEndFlush(const ZSTD_CStream* zcs); // 30603
} /* size: 120, variables: 1, inline expansions: 1 (53 bytes) */

// <06FDAF6F> thirdparty/zstd/zstd.c:30608
// variables: 6
// function call: 1
size_t ZSTD_endStream(ZSTD_CStream* zcs, ZSTD_outBuffer* output)
{
	ZSTD_inBuffer input; // 30610
	const size_t  remainingToFlush; // 30611
	{
		const size_t  err_code; // 30612
	}
	{
		const size_t  lastBlockSize; // 30615
		const size_t  checksumSize; // 30616
		const size_t  toFlush; // 30617
	}
	inBuffer_forEndFlush(const ZSTD_CStream* zcs); // 30610
} /* size: 184, variables: 2, inline expansions: 1 (61 bytes) */

// <06FDAF4D> thirdparty/zstd/zstd.c:30762
int ZSTD_maxCLevel(void)
{
} /* size: 10 */

// <06FDAF2B> thirdparty/zstd/zstd.c:30763
int ZSTD_minCLevel(void)
{
} /* size: 10 */

// <06FDAF09> thirdparty/zstd/zstd.c:30764
int ZSTD_defaultCLevel(void)
{
} /* size: 10 */

// <06FDAECF> thirdparty/zstd/zstd.c:30766
// variable: 1
ZSTD_compressionParameters ZSTD_dedicatedDictSearch_getCParams(const int compressionLevel, const size_t dictSize)
{
	ZSTD_compressionParameters cParams; // 30768
} /* size: 0, variables: 1 */

// <06FDAEAF> thirdparty/zstd/zstd.c:30787
int ZSTD_dedicatedDictSearch_isSupported(const ZSTD_compressionParameters* cParams)
{
} /* size: 0 */

// <06FDAE8D> thirdparty/zstd/zstd.c:30801
void ZSTD_dedicatedDictSearch_revertCParams(ZSTD_compressionParameters* cParams)
{
} /* size: 0 */

// <06FDAE37> thirdparty/zstd/zstd.c:30823
// variables: 2
U64 ZSTD_getCParamRowSize(U64 srcSizeHint, size_t dictSize, ZSTD_CParamMode_e mode)
{
	{
		const int  unknown; // 30837
		const size_t  addedSize; // 30838
	}
} /* size: 0 */

// <06FDACAC> thirdparty/zstd/zstd.c:30848
// variables: 7
// function call: 1
ZSTD_compressionParameters ZSTD_getCParams_internal(int compressionLevel, long long unsigned int srcSizeHint, size_t dictSize, ZSTD_CParamMode_e mode)
{
	const U64  rSize; // 30850
	const U32  tableID; // 30851
	int row; // 30852
	{
		ZSTD_compressionParameters cp; // 30861
		{
			const int  clampedCompressionLevel; // 30865
		}
	}
	{
		const int  unknown; // 30837
		const size_t  addedSize; // 30838
	}
	ZSTD_getCParamRowSize(U64 srcSizeHint,
				size_t dictSize,
				ZSTD_CParamMode_e mode);  // 30850
} /* size: 490, variables: 3, inline expansions: 1 (92 bytes) */

// <06FDABFA> thirdparty/zstd/zstd.c:30876
ZSTD_compressionParameters ZSTD_getCParams(int compressionLevel, long long unsigned int srcSizeHint, size_t dictSize)
{
} /* size: 83 */

// <06FDAB99> thirdparty/zstd/zstd.c:30887
// variables: 2
ZSTD_parameters ZSTD_getParams_internal(int compressionLevel, long long unsigned int srcSizeHint, size_t dictSize, ZSTD_CParamMode_e mode)
{
	ZSTD_parameters params; // 30889
	const ZSTD_compressionParameters  cParams; // 30890
} /* size: 0, variables: 2 */

// <06FDAA8C> thirdparty/zstd/zstd.c:30902
// function call: 1
ZSTD_parameters ZSTD_getParams(int compressionLevel, long long unsigned int srcSizeHint, size_t dictSize)
{
	ZSTD_getParams_internal(int compressionLevel,
				long long unsigned int srcSizeHint,
				size_t dictSize,
				ZSTD_CParamMode_e mode);  // 30905
} /* size: 131, inline expansions: 1 (59 bytes) */

// <06FDAA05> thirdparty/zstd/zstd.c:30908
void ZSTD_registerSequenceProducer(ZSTD_CCtx* zc, void* extSeqProdState, ZSTD_sequenceProducer_F extSeqProdFunc)
{
} /* size: 13 */

// <06FDA9AF> thirdparty/zstd/zstd.c:30919
void ZSTD_CCtxParams_registerSequenceProducer(ZSTD_CCtx_params* params, void* extSeqProdState, ZSTD_sequenceProducer_F extSeqProdFunc)
{
} /* size: 28 */

// <06FDA410> thirdparty/zstd/zstd.c:30952
// variables: 14
// function calls: 20
void ZSTD_fillDoubleHashTableForCDict(ZSTD_MatchState_t* ms, const void* end, ZSTD_dictTableLoadMethod_e dtlm)
{
	const ZSTD_compressionParameters* cParams; // 30955
	const U32* hashLarge; // 30956
	const U32  hBitsL; // 30957
	const U32  mls; // 30958
	const U32* hashSmall; // 30959
	const U32  hBitsS; // 30960
	const BYTE* base; // 30961
	const BYTE* ip; // 30962
	const BYTE* iend; // 30963
	const U32  fastHashFillStep; // 30964
	{
		const U32  curr; // 30971
		U32 i; // 30972
		{
			const size_t  smHashAndTag; // 30974
			const size_t  lgHashAndTag; // 30975
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 30975
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20064
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 30974
			ZSTD_writeTaggedIndex(const U32* hashTable,
						size_t hashAndTag,
						U32 index);  // 30980
			ZSTD_writeTaggedIndex(const U32* hashTable,
						size_t hashAndTag,
						U32 index);  // 30977
		}
	}
} /* size: 442, variables: 10 */

// <06FD9F33> thirdparty/zstd/zstd.c:30990
// variables: 14
// function calls: 18
void ZSTD_fillDoubleHashTableForCCtx(ZSTD_MatchState_t* ms, const void* end, ZSTD_dictTableLoadMethod_e dtlm)
{
	const ZSTD_compressionParameters* cParams; // 30993
	const U32* hashLarge; // 30994
	const U32  hBitsL; // 30995
	const U32  mls; // 30996
	const U32* hashSmall; // 30997
	const U32  hBitsS; // 30998
	const BYTE* base; // 30999
	const BYTE* ip; // 31000
	const BYTE* iend; // 31001
	const U32  fastHashFillStep; // 31002
	{
		const U32  curr; // 31009
		U32 i; // 31010
		{
			const size_t  smHash; // 31012
			const size_t  lgHash; // 31013
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31013
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20064
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31012
		}
	}
} /* size: 395, variables: 10 */

// <06FD9EEC> thirdparty/zstd/zstd.c:31024
void ZSTD_fillDoubleHashTable(ZSTD_MatchState_t* ms, const void* end, ZSTD_dictTableLoadMethod_e dtlm, ZSTD_tableFillPurpose_e tfp)
{
} /* size: 0 */

// <06FD9C01> thirdparty/zstd/zstd.c:31039
// variables: 44
inline size_t ZSTD_compressBlock_doubleFast_noDict_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const U32 mls)
{

_search_next_long: // 31187

_match_found: // 31209

_match_stored: // 31226

_cleanup: // 31175
	const ZSTD_compressionParameters* cParams; // 31043
	const U32* hashLong; // 31044
	const U32  hBitsL; // 31045
	const U32* hashSmall; // 31046
	const U32  hBitsS; // 31047
	const BYTE* base; // 31048
	const BYTE* istart; // 31049
	const BYTE* anchor; // 31050
	const U32  endIndex; // 31051
	const U32  prefixLowestIndex; // 31053
	const BYTE* prefixLowest; // 31054
	const BYTE* iend; // 31055
	const BYTE* ilimit; // 31056
	U32 offset_1; // 31057
	U32 offset_2; // 31057
	U32 offsetSaved1; // 31058
	U32 offsetSaved2; // 31058
	size_t mLength; // 31060
	U32 offset; // 31061
	U32 curr; // 31062
	const size_t  kStepIncr; // 31065
	const BYTE* nextStep; // 31067
	size_t step; // 31068
	size_t hl0; // 31070
	size_t hl1; // 31071
	U32 idxl0; // 31073
	U32 idxl1; // 31074
	const BYTE* matchl0; // 31076
	const BYTE* matchs0; // 31077
	const BYTE* matchl1; // 31078
	const BYTE* matchs0_safe; // 31079
	const BYTE* ip; // 31081
	const BYTE* ip1; // 31082
	const BYTE  dummy; // 31086
	{
		const U32  current; // 31093
		const U32  windowLow; // 31094
		const U32  maxRep; // 31095
	}
	{
		const size_t  hs0; // 31116
		const U32  idxs0; // 31117
		{
			const BYTE* matchl0_safe; // 31137
		}
	}
	{
		const size_t  l1len; // 31195
	}
	{
		const U32  indexToInsert; // 31234
	}
	{
		const size_t  rLength; // 31246
		const U32  tmpOff; // 31247
	}
} /* size: 0, variables: 34, goto labels: 4 */

// <06FD97AE> thirdparty/zstd/zstd.c:31262
// variables: 72
inline size_t ZSTD_compressBlock_doubleFast_dictMatchState_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const U32 mls)
{

_search_next_long: // 31390

_match_found: // 31430

_match_stored: // 31436
	const ZSTD_compressionParameters* cParams; // 31267
	const U32* hashLong; // 31268
	const U32  hBitsL; // 31269
	const U32* hashSmall; // 31270
	const U32  hBitsS; // 31271
	const BYTE* base; // 31272
	const BYTE* istart; // 31273
	const BYTE* ip; // 31274
	const BYTE* anchor; // 31275
	const U32  endIndex; // 31276
	const U32  prefixLowestIndex; // 31278
	const BYTE* prefixLowest; // 31279
	const BYTE* iend; // 31280
	const BYTE* ilimit; // 31281
	U32 offset_1; // 31282
	U32 offset_2; // 31282
	const ZSTD_MatchState_t* dms; // 31284
	const ZSTD_compressionParameters* dictCParams; // 31285
	const U32* dictHashLong; // 31286
	const U32* dictHashSmall; // 31287
	const U32  dictStartIndex; // 31288
	const BYTE* dictBase; // 31289
	const BYTE* dictStart; // 31290
	const BYTE* dictEnd; // 31291
	const U32  dictIndexDelta; // 31292
	const U32  dictHBitsL; // 31293
	const U32  dictHBitsS; // 31294
	const U32  dictAndPrefixLength; // 31295
	{
		const size_t  hashTableBytes; // 31303
		const size_t  chainTableBytes; // 31304
		{
			const char* _ptr; // 31305
			const size_t  _size; // 31305
			size_t _pos; // 31305
		}
		{
			const char* _ptr; // 31306
			const size_t  _size; // 31306
			size_t _pos; // 31306
		}
	}
	{
		size_t mLength; // 31319
		U32 offset; // 31320
		const size_t  h2; // 31321
		const size_t  h; // 31322
		const size_t  dictHashAndTagL; // 31323
		const size_t  dictHashAndTagS; // 31324
		const U32  dictMatchIndexAndTagL; // 31325
		const U32  dictMatchIndexAndTagS; // 31326
		const int  dictTagsMatchL; // 31327
		const int  dictTagsMatchS; // 31328
		const U32  curr; // 31329
		const U32  matchIndexL; // 31330
		U32 matchIndexS; // 31331
		const BYTE* matchLong; // 31332
		const BYTE* match; // 31333
		const U32  repIndex; // 31334
		const BYTE* repMatch; // 31335
		{
			const BYTE* repMatchEnd; // 31343
		}
		{
			const U32  dictMatchIndexL; // 31358
			const BYTE* dictMatchL; // 31359
		}
		{
			const U32  dictMatchIndexS; // 31376
		}
		{
			const size_t  hl3; // 31391
			const size_t  dictHashAndTagL3; // 31392
			const U32  matchIndexL3; // 31393
			const U32  dictMatchIndexAndTagL3; // 31394
			const int  dictTagsMatchL3; // 31395
			const BYTE* matchL3; // 31396
			{
				const U32  dictMatchIndexL3; // 31408
				const BYTE* dictMatchL3; // 31409
			}
		}
		{
			const U32  indexToInsert; // 31444
		}
		{
			const U32  current2; // 31453
			const U32  repIndex2; // 31454
			const BYTE* repMatch2; // 31455
			{
				const BYTE* repEnd2; // 31460
				const size_t  repLength2; // 31461
				U32 tmpOffset; // 31462
			}
		}
	}
} /* size: 0, variables: 28, goto labels: 3 */

// <06FD975B> thirdparty/zstd/zstd.c:31491
size_t ZSTD_compressBlock_doubleFast_noDict_4(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FD9708> thirdparty/zstd/zstd.c:31492
size_t ZSTD_compressBlock_doubleFast_noDict_5(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FD96B5> thirdparty/zstd/zstd.c:31493
size_t ZSTD_compressBlock_doubleFast_noDict_6(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FD9662> thirdparty/zstd/zstd.c:31494
size_t ZSTD_compressBlock_doubleFast_noDict_7(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FD960F> thirdparty/zstd/zstd.c:31496
size_t ZSTD_compressBlock_doubleFast_dictMatchState_4(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FD95BC> thirdparty/zstd/zstd.c:31497
size_t ZSTD_compressBlock_doubleFast_dictMatchState_5(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FD9569> thirdparty/zstd/zstd.c:31498
size_t ZSTD_compressBlock_doubleFast_dictMatchState_6(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FD9516> thirdparty/zstd/zstd.c:31499
size_t ZSTD_compressBlock_doubleFast_dictMatchState_7(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FCF89E> thirdparty/zstd/zstd.c:31502
// variables: 93
// function calls: 632
size_t ZSTD_compressBlock_doubleFast(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
	const U32  mls; // 31506

_search_next_long: // 31187

_match_found: // 31209

_match_stored: // 31226

_cleanup: // 31175
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31053
	{
		const U32  current; // 31093
		const U32  windowLow; // 31094
		const U32  maxRep; // 31095
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31094
	}
	ZSTD_hash8(U64 u,
			U32 h,
			U64 s);  // 20079
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20079
	ZSTD_hash8Ptr(const void* p,
			U32 h);  // 20097
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31110
	{
		const size_t  hs0; // 31116
		const U32  idxs0; // 31117
		{
			const BYTE* matchl0_safe; // 31137
			ZSTD_selectAddr(U32 index,
					U32 lowLimit,
					const BYTE* candidate,
					const BYTE* backup);  // 31137
			MEM_read64(const void* ptr); // 31140
			MEM_read64(const void* ptr); // 31140
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 31141
		}
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31116
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31131
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31151
		MEM_read32(const void* ptr); // 31154
		MEM_read32(const void* ptr); // 31154
		MEM_read32(const void* ptr); // 31124
		MEM_read32(const void* ptr); // 31124
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31125
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31127
	}
	{
		const U32  indexToInsert; // 31234
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31235
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31236
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31237
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31238
	}
	MEM_read32(const void* ptr); // 31244
	MEM_read32(const void* ptr); // 31244
	{
		const size_t  rLength; // 31246
		const U32  tmpOff; // 31247
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31246
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31248
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31249
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31250
	}
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 31224
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 31190
	MEM_read64(const void* ptr); // 31194
	{
		const size_t  l1len; // 31195
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31195
	}
	ZSTD_compressBlock_doubleFast_noDict_generic(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize,
							const U32  mls);  // 31491
	ZSTD_compressBlock_doubleFast_noDict_4(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 31511

_search_next_long: // 31187

_match_found: // 31209

_match_stored: // 31226

_cleanup: // 31175
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31053
	{
		const U32  current; // 31093
		const U32  windowLow; // 31094
		const U32  maxRep; // 31095
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31094
	}
	ZSTD_hash8(U64 u,
			U32 h,
			U64 s);  // 20079
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20079
	ZSTD_hash8Ptr(const void* p,
			U32 h);  // 20097
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31110
	{
		const size_t  hs0; // 31116
		const U32  idxs0; // 31117
		{
			const BYTE* matchl0_safe; // 31137
			ZSTD_selectAddr(U32 index,
					U32 lowLimit,
					const BYTE* candidate,
					const BYTE* backup);  // 31137
			MEM_read64(const void* ptr); // 31140
			MEM_read64(const void* ptr); // 31140
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 31141
		}
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31116
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31131
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31151
		MEM_read32(const void* ptr); // 31154
		MEM_read32(const void* ptr); // 31154
		MEM_read32(const void* ptr); // 31124
		MEM_read32(const void* ptr); // 31124
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31125
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31127
	}
	{
		const U32  indexToInsert; // 31234
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31235
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31236
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31237
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31238
	}
	MEM_read32(const void* ptr); // 31244
	MEM_read32(const void* ptr); // 31244
	{
		const size_t  rLength; // 31246
		const U32  tmpOff; // 31247
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31246
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31248
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31249
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31250
	}
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 31224
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 31190
	MEM_read64(const void* ptr); // 31194
	{
		const size_t  l1len; // 31195
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31195
	}
	ZSTD_compressBlock_doubleFast_noDict_generic(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize,
							const U32  mls);  // 31494
	ZSTD_compressBlock_doubleFast_noDict_7(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 31517

_search_next_long: // 31187

_match_found: // 31209

_match_stored: // 31226

_cleanup: // 31175
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31053
	{
		const U32  current; // 31093
		const U32  windowLow; // 31094
		const U32  maxRep; // 31095
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31094
	}
	ZSTD_hash8(U64 u,
			U32 h,
			U64 s);  // 20079
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20079
	ZSTD_hash8Ptr(const void* p,
			U32 h);  // 20097
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31110
	{
		const size_t  hs0; // 31116
		const U32  idxs0; // 31117
		{
			const BYTE* matchl0_safe; // 31137
			ZSTD_selectAddr(U32 index,
					U32 lowLimit,
					const BYTE* candidate,
					const BYTE* backup);  // 31137
			MEM_read64(const void* ptr); // 31140
			MEM_read64(const void* ptr); // 31140
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 31141
		}
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31116
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31131
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31151
		MEM_read32(const void* ptr); // 31154
		MEM_read32(const void* ptr); // 31154
		MEM_read32(const void* ptr); // 31124
		MEM_read32(const void* ptr); // 31124
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31125
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31127
	}
	{
		const U32  indexToInsert; // 31234
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31235
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31236
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31237
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31238
	}
	MEM_read32(const void* ptr); // 31244
	MEM_read32(const void* ptr); // 31244
	{
		const size_t  rLength; // 31246
		const U32  tmpOff; // 31247
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31246
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31248
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31249
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31250
	}
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 31224
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 31190
	MEM_read64(const void* ptr); // 31194
	{
		const size_t  l1len; // 31195
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31195
	}
	ZSTD_compressBlock_doubleFast_noDict_generic(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize,
							const U32  mls);  // 31493
	ZSTD_compressBlock_doubleFast_noDict_6(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 31515

_search_next_long: // 31187

_match_found: // 31209

_match_stored: // 31226

_cleanup: // 31175
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31053
	{
		const U32  current; // 31093
		const U32  windowLow; // 31094
		const U32  maxRep; // 31095
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31094
	}
	ZSTD_hash8(U64 u,
			U32 h,
			U64 s);  // 20079
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20079
	ZSTD_hash8Ptr(const void* p,
			U32 h);  // 20097
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31110
	{
		const size_t  hs0; // 31116
		const U32  idxs0; // 31117
		{
			const BYTE* matchl0_safe; // 31137
			ZSTD_selectAddr(U32 index,
					U32 lowLimit,
					const BYTE* candidate,
					const BYTE* backup);  // 31137
			MEM_read64(const void* ptr); // 31140
			MEM_read64(const void* ptr); // 31140
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 31141
		}
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31116
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31131
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31151
		MEM_read32(const void* ptr); // 31154
		MEM_read32(const void* ptr); // 31154
		MEM_read32(const void* ptr); // 31124
		MEM_read32(const void* ptr); // 31124
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31125
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31127
	}
	{
		const U32  indexToInsert; // 31234
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31235
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31236
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31237
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31238
	}
	MEM_read32(const void* ptr); // 31244
	MEM_read32(const void* ptr); // 31244
	{
		const size_t  rLength; // 31246
		const U32  tmpOff; // 31247
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31246
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31248
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31249
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31250
	}
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 31224
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 31190
	MEM_read64(const void* ptr); // 31194
	{
		const size_t  l1len; // 31195
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31195
	}
	ZSTD_compressBlock_doubleFast_noDict_generic(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize,
							const U32  mls);  // 31492
	ZSTD_compressBlock_doubleFast_noDict_5(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 31513
} /* size: 13817, variables: 1, goto labels: 16, inline expansions: 124 (31541 bytes) */

// <06FC5536> thirdparty/zstd/zstd.c:31522
// variables: 213
// function calls: 601
size_t ZSTD_compressBlock_doubleFast_dictMatchState(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
	const U32  mls; // 31526

_search_next_long: // 31390

_match_found: // 31430

_match_stored: // 31436
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31278
	{
		const size_t  hashTableBytes; // 31303
		const size_t  chainTableBytes; // 31304
		{
			const char* _ptr; // 31305
			const size_t  _size; // 31305
			size_t _pos; // 31305
		}
		{
			const char* _ptr; // 31306
			const size_t  _size; // 31306
			size_t _pos; // 31306
		}
	}
	{
		size_t mLength; // 31319
		U32 offset; // 31320
		const size_t  h2; // 31321
		const size_t  h; // 31322
		const size_t  dictHashAndTagL; // 31323
		const size_t  dictHashAndTagS; // 31324
		const U32  dictMatchIndexAndTagL; // 31325
		const U32  dictMatchIndexAndTagS; // 31326
		const int  dictTagsMatchL; // 31327
		const int  dictTagsMatchS; // 31328
		const U32  curr; // 31329
		const U32  matchIndexL; // 31330
		U32 matchIndexS; // 31331
		const BYTE* matchLong; // 31332
		const BYTE* match; // 31333
		const U32  repIndex; // 31334
		const BYTE* repMatch; // 31335
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31321
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31322
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31323
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31324
		ZSTD_index_overlap_check(const U32  prefixLowestIndex,
					const U32  repIndex);  // 31341
		MEM_read32(const void* ptr); // 31342
		MEM_read32(const void* ptr); // 31342
		{
			const BYTE* repMatchEnd; // 31343
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 31346
		}
		MEM_read64(const void* ptr); // 31350
		MEM_read64(const void* ptr); // 31350
		{
			const U32  dictMatchIndexL; // 31358
			const BYTE* dictMatchL; // 31359
			MEM_read64(const void* ptr); // 31362
			MEM_read64(const void* ptr); // 31362
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31352
		MEM_read32(const void* ptr); // 31371
		MEM_read32(const void* ptr); // 31371
		{
			const U32  dictMatchIndexS; // 31376
			MEM_read32(const void* ptr); // 31380
			MEM_read32(const void* ptr); // 31380
		}
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 31327
		{
			const size_t  hl3; // 31391
			const size_t  dictHashAndTagL3; // 31392
			const U32  matchIndexL3; // 31393
			const U32  dictMatchIndexAndTagL3; // 31394
			const int  dictTagsMatchL3; // 31395
			const BYTE* matchL3; // 31396
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31391
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31392
			MEM_read64(const void* ptr); // 31400
			MEM_read64(const void* ptr); // 31400
			{
				const U32  dictMatchIndexL3; // 31408
				const BYTE* dictMatchL3; // 31409
				MEM_read64(const void* ptr); // 31411
				MEM_read64(const void* ptr); // 31411
			}
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 31401
			ZSTD_comparePackedTags(size_t packedTag1,
						size_t packedTag2);  // 31395
		}
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31434
		{
			const U32  indexToInsert; // 31444
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31445
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31446
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31447
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31448
		}
		{
			const U32  current2; // 31453
			const U32  repIndex2; // 31454
			const BYTE* repMatch2; // 31455
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 31458
			MEM_read32(const void* ptr); // 31459
			MEM_read32(const void* ptr); // 31459
			{
				const BYTE* repEnd2; // 31460
				const size_t  repLength2; // 31461
				U32 tmpOffset; // 31462
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 31463
				MEM_read32(const void* ptr); // 1301
				MEM_readLE32(const void* memPtr); // 20059
				ZSTD_hash4(U32 u,
						U32 h,
						U32 s);  // 20059
				ZSTD_hash4Ptr(const void* ptr,
						U32 h);  // 20093
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31464
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 20097
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31465
			}
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31425
	}
	ZSTD_compressBlock_doubleFast_dictMatchState_generic(ZSTD_MatchState_t* ms,
								SeqStore_t* seqStore,
								U32* rep,
								const void* src,
								size_t srcSize,
								const U32  mls);  // 31496
	ZSTD_compressBlock_doubleFast_dictMatchState_4(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize);  // 31531

_search_next_long: // 31390

_match_found: // 31430

_match_stored: // 31436
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31278
	{
		const size_t  hashTableBytes; // 31303
		const size_t  chainTableBytes; // 31304
		{
			const char* _ptr; // 31305
			const size_t  _size; // 31305
			size_t _pos; // 31305
		}
		{
			const char* _ptr; // 31306
			const size_t  _size; // 31306
			size_t _pos; // 31306
		}
	}
	{
		size_t mLength; // 31319
		U32 offset; // 31320
		const size_t  h2; // 31321
		const size_t  h; // 31322
		const size_t  dictHashAndTagL; // 31323
		const size_t  dictHashAndTagS; // 31324
		const U32  dictMatchIndexAndTagL; // 31325
		const U32  dictMatchIndexAndTagS; // 31326
		const int  dictTagsMatchL; // 31327
		const int  dictTagsMatchS; // 31328
		const U32  curr; // 31329
		const U32  matchIndexL; // 31330
		U32 matchIndexS; // 31331
		const BYTE* matchLong; // 31332
		const BYTE* match; // 31333
		const U32  repIndex; // 31334
		const BYTE* repMatch; // 31335
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31321
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31322
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31323
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31324
		MEM_read32(const void* ptr); // 31371
		MEM_read32(const void* ptr); // 31371
		{
			const U32  dictMatchIndexS; // 31376
			MEM_read32(const void* ptr); // 31380
			MEM_read32(const void* ptr); // 31380
		}
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 31327
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 31328
		ZSTD_index_overlap_check(const U32  prefixLowestIndex,
					const U32  repIndex);  // 31341
		MEM_read32(const void* ptr); // 31342
		MEM_read32(const void* ptr); // 31342
		{
			const BYTE* repMatchEnd; // 31343
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 31346
		}
		MEM_read64(const void* ptr); // 31350
		MEM_read64(const void* ptr); // 31350
		{
			const U32  dictMatchIndexL; // 31358
			const BYTE* dictMatchL; // 31359
			MEM_read64(const void* ptr); // 31362
			MEM_read64(const void* ptr); // 31362
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31352
		{
			const size_t  hl3; // 31391
			const size_t  dictHashAndTagL3; // 31392
			const U32  matchIndexL3; // 31393
			const U32  dictMatchIndexAndTagL3; // 31394
			const int  dictTagsMatchL3; // 31395
			const BYTE* matchL3; // 31396
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31391
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31392
			ZSTD_comparePackedTags(size_t packedTag1,
						size_t packedTag2);  // 31395
			MEM_read64(const void* ptr); // 31400
			MEM_read64(const void* ptr); // 31400
			{
				const U32  dictMatchIndexL3; // 31408
				const BYTE* dictMatchL3; // 31409
				MEM_read64(const void* ptr); // 31411
				MEM_read64(const void* ptr); // 31411
			}
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 31401
		}
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31434
		{
			const U32  indexToInsert; // 31444
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31445
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31446
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20074
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31447
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20074
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31448
		}
		{
			const U32  current2; // 31453
			const U32  repIndex2; // 31454
			const BYTE* repMatch2; // 31455
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 31458
			MEM_read32(const void* ptr); // 31459
			MEM_read32(const void* ptr); // 31459
			{
				const BYTE* repEnd2; // 31460
				const size_t  repLength2; // 31461
				U32 tmpOffset; // 31462
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 31463
				ZSTD_hash7(U64 u,
						U32 h,
						U64 s);  // 20074
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20074
				ZSTD_hash7Ptr(const void* p,
						U32 h);  // 20096
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31464
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 20097
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31465
			}
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31425
	}
	ZSTD_compressBlock_doubleFast_dictMatchState_generic(ZSTD_MatchState_t* ms,
								SeqStore_t* seqStore,
								U32* rep,
								const void* src,
								size_t srcSize,
								const U32  mls);  // 31499
	ZSTD_compressBlock_doubleFast_dictMatchState_7(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize);  // 31537

_search_next_long: // 31390

_match_found: // 31430

_match_stored: // 31436
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31278
	{
		const size_t  hashTableBytes; // 31303
		const size_t  chainTableBytes; // 31304
		{
			const char* _ptr; // 31305
			const size_t  _size; // 31305
			size_t _pos; // 31305
		}
		{
			const char* _ptr; // 31306
			const size_t  _size; // 31306
			size_t _pos; // 31306
		}
	}
	{
		size_t mLength; // 31319
		U32 offset; // 31320
		const size_t  h2; // 31321
		const size_t  h; // 31322
		const size_t  dictHashAndTagL; // 31323
		const size_t  dictHashAndTagS; // 31324
		const U32  dictMatchIndexAndTagL; // 31325
		const U32  dictMatchIndexAndTagS; // 31326
		const int  dictTagsMatchL; // 31327
		const int  dictTagsMatchS; // 31328
		const U32  curr; // 31329
		const U32  matchIndexL; // 31330
		U32 matchIndexS; // 31331
		const BYTE* matchLong; // 31332
		const BYTE* match; // 31333
		const U32  repIndex; // 31334
		const BYTE* repMatch; // 31335
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31321
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31322
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31323
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31324
		MEM_read32(const void* ptr); // 31371
		MEM_read32(const void* ptr); // 31371
		{
			const U32  dictMatchIndexS; // 31376
			MEM_read32(const void* ptr); // 31380
			MEM_read32(const void* ptr); // 31380
		}
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 31327
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 31328
		ZSTD_index_overlap_check(const U32  prefixLowestIndex,
					const U32  repIndex);  // 31341
		MEM_read32(const void* ptr); // 31342
		MEM_read32(const void* ptr); // 31342
		{
			const BYTE* repMatchEnd; // 31343
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 31346
		}
		MEM_read64(const void* ptr); // 31350
		MEM_read64(const void* ptr); // 31350
		{
			const U32  dictMatchIndexL; // 31358
			const BYTE* dictMatchL; // 31359
			MEM_read64(const void* ptr); // 31362
			MEM_read64(const void* ptr); // 31362
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31352
		{
			const size_t  hl3; // 31391
			const size_t  dictHashAndTagL3; // 31392
			const U32  matchIndexL3; // 31393
			const U32  dictMatchIndexAndTagL3; // 31394
			const int  dictTagsMatchL3; // 31395
			const BYTE* matchL3; // 31396
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31391
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31392
			MEM_read64(const void* ptr); // 31400
			MEM_read64(const void* ptr); // 31400
			{
				const U32  dictMatchIndexL3; // 31408
				const BYTE* dictMatchL3; // 31409
				MEM_read64(const void* ptr); // 31411
				MEM_read64(const void* ptr); // 31411
			}
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 31401
			ZSTD_comparePackedTags(size_t packedTag1,
						size_t packedTag2);  // 31395
		}
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31434
		{
			const U32  indexToInsert; // 31444
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31445
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31446
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20064
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31447
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20064
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31448
		}
		{
			const U32  current2; // 31453
			const U32  repIndex2; // 31454
			const BYTE* repMatch2; // 31455
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 31458
			MEM_read32(const void* ptr); // 31459
			MEM_read32(const void* ptr); // 31459
			{
				const BYTE* repEnd2; // 31460
				const size_t  repLength2; // 31461
				U32 tmpOffset; // 31462
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 31463
				ZSTD_hash5(U64 u,
						U32 h,
						U64 s);  // 20064
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20064
				ZSTD_hash5Ptr(const void* p,
						U32 h);  // 20094
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31464
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 20097
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31465
			}
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31425
	}
	ZSTD_compressBlock_doubleFast_dictMatchState_generic(ZSTD_MatchState_t* ms,
								SeqStore_t* seqStore,
								U32* rep,
								const void* src,
								size_t srcSize,
								const U32  mls);  // 31497
	ZSTD_compressBlock_doubleFast_dictMatchState_5(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize);  // 31533

_search_next_long: // 31390

_match_found: // 31430

_match_stored: // 31436
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31278
	{
		const size_t  hashTableBytes; // 31303
		const size_t  chainTableBytes; // 31304
		{
			const char* _ptr; // 31305
			const size_t  _size; // 31305
			size_t _pos; // 31305
		}
		{
			const char* _ptr; // 31306
			const size_t  _size; // 31306
			size_t _pos; // 31306
		}
	}
	{
		size_t mLength; // 31319
		U32 offset; // 31320
		const size_t  h2; // 31321
		const size_t  h; // 31322
		const size_t  dictHashAndTagL; // 31323
		const size_t  dictHashAndTagS; // 31324
		const U32  dictMatchIndexAndTagL; // 31325
		const U32  dictMatchIndexAndTagS; // 31326
		const int  dictTagsMatchL; // 31327
		const int  dictTagsMatchS; // 31328
		const U32  curr; // 31329
		const U32  matchIndexL; // 31330
		U32 matchIndexS; // 31331
		const BYTE* matchLong; // 31332
		const BYTE* match; // 31333
		const U32  repIndex; // 31334
		const BYTE* repMatch; // 31335
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31321
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31322
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31323
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31324
		MEM_read32(const void* ptr); // 31371
		MEM_read32(const void* ptr); // 31371
		{
			const U32  dictMatchIndexS; // 31376
			MEM_read32(const void* ptr); // 31380
			MEM_read32(const void* ptr); // 31380
		}
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 31327
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 31328
		ZSTD_index_overlap_check(const U32  prefixLowestIndex,
					const U32  repIndex);  // 31341
		MEM_read32(const void* ptr); // 31342
		MEM_read32(const void* ptr); // 31342
		{
			const BYTE* repMatchEnd; // 31343
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 31346
		}
		MEM_read64(const void* ptr); // 31350
		MEM_read64(const void* ptr); // 31350
		{
			const U32  dictMatchIndexL; // 31358
			const BYTE* dictMatchL; // 31359
			MEM_read64(const void* ptr); // 31362
			MEM_read64(const void* ptr); // 31362
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31352
		{
			const size_t  hl3; // 31391
			const size_t  dictHashAndTagL3; // 31392
			const U32  matchIndexL3; // 31393
			const U32  dictMatchIndexAndTagL3; // 31394
			const int  dictTagsMatchL3; // 31395
			const BYTE* matchL3; // 31396
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31391
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31392
			ZSTD_comparePackedTags(size_t packedTag1,
						size_t packedTag2);  // 31395
			MEM_read64(const void* ptr); // 31400
			MEM_read64(const void* ptr); // 31400
			{
				const U32  dictMatchIndexL3; // 31408
				const BYTE* dictMatchL3; // 31409
				MEM_read64(const void* ptr); // 31411
				MEM_read64(const void* ptr); // 31411
			}
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 31401
		}
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15732
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15737
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 15738
		ZSTD_wildcopy(void* dst,
				const void* src,
				ptrdiff_t length,
				const ZSTD_overlap_e  ovtype);  // 19957
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 31434
		{
			const U32  indexToInsert; // 31444
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31445
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31446
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20069
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31447
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20069
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31448
		}
		{
			const U32  current2; // 31453
			const U32  repIndex2; // 31454
			const BYTE* repMatch2; // 31455
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 31458
			MEM_read32(const void* ptr); // 31459
			MEM_read32(const void* ptr); // 31459
			{
				const BYTE* repEnd2; // 31460
				const size_t  repLength2; // 31461
				U32 tmpOffset; // 31462
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 31463
				ZSTD_hash6(U64 u,
						U32 h,
						U64 s);  // 20069
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20069
				ZSTD_hash6Ptr(const void* p,
						U32 h);  // 20095
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31464
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 20097
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31465
			}
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 31425
	}
	ZSTD_compressBlock_doubleFast_dictMatchState_generic(ZSTD_MatchState_t* ms,
								SeqStore_t* seqStore,
								U32* rep,
								const void* src,
								size_t srcSize,
								const U32  mls);  // 31498
	ZSTD_compressBlock_doubleFast_dictMatchState_6(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize);  // 31535
} /* size: 14822, variables: 1, goto labels: 12, inline expansions: 12 (29520 bytes) */

// <06FC2DA8> thirdparty/zstd/zstd.c:31544
// variables: 58
// function calls: 141
size_t ZSTD_compressBlock_doubleFast_extDict_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const U32 mls)
{
	const ZSTD_compressionParameters* cParams; // 31549
	const U32* hashLong; // 31550
	const U32  hBitsL; // 31551
	const U32* hashSmall; // 31552
	const U32  hBitsS; // 31553
	const BYTE* istart; // 31554
	const BYTE* ip; // 31555
	const BYTE* anchor; // 31556
	const BYTE* iend; // 31557
	const BYTE* ilimit; // 31558
	const BYTE* base; // 31559
	const U32  endIndex; // 31560
	const U32  lowLimit; // 31561
	const U32  dictStartIndex; // 31562
	const U32  dictLimit; // 31563
	const U32  prefixStartIndex; // 31564
	const BYTE* prefixStart; // 31565
	const BYTE* dictBase; // 31566
	const BYTE* dictStart; // 31567
	const BYTE* dictEnd; // 31568
	U32 offset_1; // 31569
	U32 offset_2; // 31569
	{
		const size_t  hSmall; // 31579
		const U32  matchIndex; // 31580
		const BYTE* matchBase; // 31581
		const BYTE* match; // 31582
		const size_t  hLong; // 31584
		const U32  matchLongIndex; // 31585
		const BYTE* matchLongBase; // 31586
		const BYTE* matchLong; // 31587
		const U32  curr; // 31589
		const U32  repIndex; // 31590
		const BYTE* repBase; // 31591
		const BYTE* repMatch; // 31592
		size_t mLength; // 31593
		{
			const BYTE* repMatchEnd; // 31599
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 31602
		}
		{
			const BYTE* matchEnd; // 31605
			const BYTE* lowMatchPtr; // 31606
			U32 offset; // 31607
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 31613
		}
		{
			const size_t  h3; // 31616
			const U32  matchIndex3; // 31617
			const BYTE* match3Base; // 31618
			const BYTE* match3; // 31619
			U32 offset; // 31620
			{
				const BYTE* matchEnd; // 31623
				const BYTE* lowMatchPtr; // 31624
			}
			{
				const BYTE* matchEnd; // 31630
				const BYTE* lowMatchPtr; // 31631
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31616
			MEM_read64(const void* ptr); // 31622
			MEM_read64(const void* ptr); // 31622
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 31638
		}
		{
			const U32  indexToInsert; // 31652
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31653
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20079
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31654
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20074
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20064
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20069
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31655
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20074
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31656
		}
		{
			const U32  current2; // 31661
			const U32  repIndex2; // 31662
			const BYTE* repMatch2; // 31663
			{
				const BYTE* repEnd2; // 31667
				const size_t  repLength2; // 31668
				const U32  tmpOffset; // 31669
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 31670
				MEM_read32(const void* ptr); // 1301
				MEM_readLE32(const void* memPtr); // 20059
				ZSTD_hash4(U32 u,
						U32 h,
						U32 s);  // 20059
				ZSTD_hash4Ptr(const void* ptr,
						U32 h);  // 20093
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20064
				ZSTD_hash5(U64 u,
						U32 h,
						U64 s);  // 20064
				ZSTD_hash5Ptr(const void* p,
						U32 h);  // 20094
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20069
				ZSTD_hash6(U64 u,
						U32 h,
						U64 s);  // 20069
				ZSTD_hash6Ptr(const void* p,
						U32 h);  // 20095
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20074
				ZSTD_hash7(U64 u,
						U32 h,
						U64 s);  // 20074
				ZSTD_hash7Ptr(const void* p,
						U32 h);  // 20096
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31671
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 20097
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31672
			}
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 31664
			MEM_read32(const void* ptr); // 31666
			MEM_read32(const void* ptr); // 31666
		}
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31584
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31579
		ZSTD_index_overlap_check(const U32  prefixLowestIndex,
					const U32  repIndex);  // 31596
		MEM_read32(const void* ptr); // 31598
		MEM_read32(const void* ptr); // 31598
		MEM_read64(const void* ptr); // 31604
		MEM_read64(const void* ptr); // 31604
		MEM_read32(const void* ptr); // 31615
		MEM_read32(const void* ptr); // 31615
	}
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 31561
} /* size: 3051, variables: 22, inline expansions: 1 (46 bytes) */

// <06FC2D55> thirdparty/zstd/zstd.c:31688
size_t ZSTD_compressBlock_doubleFast_extDict_4(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FC2D02> thirdparty/zstd/zstd.c:31689
size_t ZSTD_compressBlock_doubleFast_extDict_5(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FC2CAF> thirdparty/zstd/zstd.c:31690
size_t ZSTD_compressBlock_doubleFast_extDict_6(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FC2C5C> thirdparty/zstd/zstd.c:31691
size_t ZSTD_compressBlock_doubleFast_extDict_7(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FC2938> thirdparty/zstd/zstd.c:31693
// variable: 1
// function calls: 4
size_t ZSTD_compressBlock_doubleFast_extDict(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
	const U32  mls; // 31697
	ZSTD_compressBlock_doubleFast_extDict_4(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 31702
	ZSTD_compressBlock_doubleFast_extDict_7(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 31708
	ZSTD_compressBlock_doubleFast_extDict_6(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 31706
	ZSTD_compressBlock_doubleFast_extDict_5(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 31704
} /* size: 83, variables: 1, inline expansions: 4 (58 bytes) */

// <06FC1F4B> thirdparty/zstd/zstd.c:31730
// variables: 12
// function calls: 40
void ZSTD_fillHashTableForCDict(ZSTD_MatchState_t* ms, const void* end, ZSTD_dictTableLoadMethod_e dtlm)
{
	const ZSTD_compressionParameters* cParams; // 31734
	const U32* hashTable; // 31735
	const U32  hBits; // 31736
	const U32  mls; // 31737
	const BYTE* base; // 31738
	const BYTE* ip; // 31739
	const BYTE* iend; // 31740
	const U32  fastHashFillStep; // 31741
	{
		const U32  curr; // 31751
		{
			const size_t  hashAndTag; // 31752
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 20097
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20064
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 31752
			ZSTD_writeTaggedIndex(const U32* hashTable,
						size_t hashAndTag,
						U32 index);  // 31753
		}
		{
			U32 p; // 31757
			{
				const size_t  hashAndTag; // 31759
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20069
				ZSTD_hash6(U64 u,
						U32 h,
						U64 s);  // 20069
				ZSTD_hash6Ptr(const void* p,
						U32 h);  // 20095
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20074
				ZSTD_hash7(U64 u,
						U32 h,
						U64 s);  // 20074
				ZSTD_hash7Ptr(const void* p,
						U32 h);  // 20096
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20079
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 20097
				MEM_read32(const void* ptr); // 1301
				MEM_readLE32(const void* memPtr); // 20059
				ZSTD_hash4(U32 u,
						U32 h,
						U32 s);  // 20059
				ZSTD_hash4Ptr(const void* ptr,
						U32 h);  // 20093
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20064
				ZSTD_hash5(U64 u,
						U32 h,
						U64 s);  // 20064
				ZSTD_hash5Ptr(const void* p,
						U32 h);  // 20094
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31759
				ZSTD_writeTaggedIndex(const U32* hashTable,
							size_t hashAndTag,
							U32 index);  // 31761
			}
		}
	}
} /* size: 491, variables: 8 */

// <06FC14F6> thirdparty/zstd/zstd.c:31767
// variables: 12
// function calls: 42
void ZSTD_fillHashTableForCCtx(ZSTD_MatchState_t* ms, const void* end, ZSTD_dictTableLoadMethod_e dtlm)
{
	const ZSTD_compressionParameters* cParams; // 31771
	const U32* hashTable; // 31772
	const U32  hBits; // 31773
	const U32  mls; // 31774
	const BYTE* base; // 31775
	const BYTE* ip; // 31776
	const BYTE* iend; // 31777
	const U32  fastHashFillStep; // 31778
	{
		const U32  curr; // 31788
		const size_t  hash0; // 31789
		{
			U32 p; // 31793
			{
				const size_t  hash; // 31795
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20069
				ZSTD_hash6(U64 u,
						U32 h,
						U64 s);  // 20069
				ZSTD_hash6Ptr(const void* p,
						U32 h);  // 20095
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20074
				ZSTD_hash7(U64 u,
						U32 h,
						U64 s);  // 20074
				ZSTD_hash7Ptr(const void* p,
						U32 h);  // 20096
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20079
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 20097
				MEM_read32(const void* ptr); // 1301
				MEM_readLE32(const void* memPtr); // 20059
				ZSTD_hash4(U32 u,
						U32 h,
						U32 s);  // 20059
				ZSTD_hash4Ptr(const void* ptr,
						U32 h);  // 20093
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20064
				ZSTD_hash5(U64 u,
						U32 h,
						U64 s);  // 20064
				ZSTD_hash5Ptr(const void* p,
						U32 h);  // 20094
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 31795
			}
		}
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 31789
	}
} /* size: 539, variables: 8 */

// <06FC14AF> thirdparty/zstd/zstd.c:31801
void ZSTD_fillHashTable(ZSTD_MatchState_t* ms, const void* end, ZSTD_dictTableLoadMethod_e dtlm, ZSTD_tableFillPurpose_e tfp)
{
} /* size: 0 */

// <06FC142F> thirdparty/zstd/zstd.c:31817
// variables: 2
int ZSTD_match4Found_cmov(const BYTE* currentPtr, const BYTE* matchAddress, U32 matchIdx, U32 idxLowLimit)
{
	const BYTE   dummy; // 31822
	const BYTE* mvalAddr; // 31827
} /* size: 0, variables: 2 */

// <06FC13DB> thirdparty/zstd/zstd.c:31842
// variable: 1
int ZSTD_match4Found_branch(const BYTE* currentPtr, const BYTE* matchAddress, U32 matchIdx, U32 idxLowLimit)
{
	U32 mval; // 31847
} /* size: 0, variables: 1 */

// <06FC1159> thirdparty/zstd/zstd.c:31906
// variables: 37
inline size_t ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const U32 mls, int useCmov)
{

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	const ZSTD_compressionParameters* cParams; // 31911
	const U32* hashTable; // 31912
	const U32  hlog; // 31913
	const size_t  stepSize; // 31914
	const BYTE* base; // 31915
	const BYTE* istart; // 31916
	const U32  endIndex; // 31917
	const U32  prefixStartIndex; // 31918
	const BYTE* prefixStart; // 31919
	const BYTE* iend; // 31920
	const BYTE* ilimit; // 31921
	const BYTE* anchor; // 31923
	const BYTE* ip0; // 31924
	const BYTE* ip1; // 31925
	const BYTE* ip2; // 31926
	const BYTE* ip3; // 31927
	U32 current0; // 31928
	U32 rep_offset1; // 31930
	U32 rep_offset2; // 31931
	U32 offsetSaved1; // 31932
	U32 offsetSaved2; // 31932
	size_t hash0; // 31934
	size_t hash1; // 31935
	U32 matchIdx; // 31936
	U32 offcode; // 31938
	const BYTE* match0; // 31939
	size_t mLength; // 31940
	size_t step; // 31946
	const BYTE* nextStep; // 31947
	const size_t  kStepIncr; // 31948
	const ZSTD_match4Found  matchFound; // 31949
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
	}
	{
		const U32  rval; // 31982
	}
	{
		const size_t  rLength; // 32127
		{
			const U32  tmpOff; // 32128
		}
	}
} /* size: 0, variables: 31, goto labels: 4 */

// <06FBF934> thirdparty/zstd/zstd.c:32147
// variables: 11
// function calls: 92
size_t ZSTD_compressBlock_fast_noDict_4_1(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31918
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31954
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31975
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31976
	{
		const U32  rval; // 31982
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31827
		MEM_read32(const void* ptr); // 31833
		MEM_read32(const void* ptr); // 31833
		ZSTD_match4Found_cmov(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32006
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32020
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31827
		MEM_read32(const void* ptr); // 31833
		MEM_read32(const void* ptr); // 31833
		ZSTD_match4Found_cmov(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32031
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32047
		MEM_read32(const void* ptr); // 31982
		MEM_read32(const void* ptr); // 31989
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 32110
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32112
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32121
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32122
	{
		const size_t  rLength; // 32127
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 32127
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32129
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 32131
	}
	MEM_read32(const void* ptr); // 32125
	MEM_read32(const void* ptr); // 32125
	ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const U32  mls,
						int useCmov);  // 32147
} /* size: 2284, goto labels: 4, inline expansions: 44 (3296 bytes) */

// <06FBF8E1> thirdparty/zstd/zstd.c:32148
size_t ZSTD_compressBlock_fast_noDict_5_1(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FBF88E> thirdparty/zstd/zstd.c:32149
size_t ZSTD_compressBlock_fast_noDict_6_1(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FBF83B> thirdparty/zstd/zstd.c:32150
size_t ZSTD_compressBlock_fast_noDict_7_1(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FBF7E8> thirdparty/zstd/zstd.c:32152
size_t ZSTD_compressBlock_fast_noDict_4_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FBF795> thirdparty/zstd/zstd.c:32153
size_t ZSTD_compressBlock_fast_noDict_5_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FB4BC6> thirdparty/zstd/zstd.c:32157
// variables: 79
// function calls: 667
size_t ZSTD_compressBlock_fast(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
	const U32  mml; // 32161
	const int  useCmov; // 32163

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31918
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31954
	}
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31975
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31976
	{
		const U32  rval; // 31982
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31827
		MEM_read32(const void* ptr); // 31833
		MEM_read32(const void* ptr); // 31833
		ZSTD_match4Found_cmov(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32006
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31827
		MEM_read32(const void* ptr); // 31833
		MEM_read32(const void* ptr); // 31833
		ZSTD_match4Found_cmov(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32031
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32020
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32047
		MEM_read32(const void* ptr); // 31982
		MEM_read32(const void* ptr); // 31989
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 32110
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32112
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32121
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32122
	MEM_read32(const void* ptr); // 32125
	MEM_read32(const void* ptr); // 32125
	{
		const size_t  rLength; // 32127
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 32127
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32129
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 32131
	}
	ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const U32  mls,
						int useCmov);  // 32148
	ZSTD_compressBlock_fast_noDict_5_1(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32172

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31918
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31954
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31975
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 32110
	{
		const U32  rval; // 31982
		MEM_read32(const void* ptr); // 31851
		MEM_read32(const void* ptr); // 31849
		ZSTD_match4Found_branch(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32006
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32020
		MEM_read32(const void* ptr); // 31851
		MEM_read32(const void* ptr); // 31849
		ZSTD_match4Found_branch(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32031
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32047
		MEM_read32(const void* ptr); // 31982
		MEM_read32(const void* ptr); // 31989
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31976
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32112
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32121
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32122
	MEM_read32(const void* ptr); // 32125
	MEM_read32(const void* ptr); // 32125
	{
		const size_t  rLength; // 32127
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 32127
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32129
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 32131
	}
	ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const U32  mls,
						int useCmov);  // 32152
	ZSTD_compressBlock_fast_noDict_4_0(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32184

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31918
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31954
	}
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31975
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31976
	{
		const U32  rval; // 31982
		MEM_read32(const void* ptr); // 31851
		MEM_read32(const void* ptr); // 31849
		ZSTD_match4Found_branch(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32006
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32020
		MEM_read32(const void* ptr); // 31851
		MEM_read32(const void* ptr); // 31849
		ZSTD_match4Found_branch(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32031
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32047
		MEM_read32(const void* ptr); // 31982
		MEM_read32(const void* ptr); // 31989
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 32110
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32112
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32121
	MEM_read32(const void* ptr); // 32125
	MEM_read32(const void* ptr); // 32125
	{
		const size_t  rLength; // 32127
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 32127
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32129
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 32131
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32122
	ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const U32  mls,
						int useCmov);  // 32153
	ZSTD_compressBlock_fast_noDict_5_0(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32186

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31918
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31954
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31975
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31976
	{
		const U32  rval; // 31982
		MEM_read32(const void* ptr); // 31851
		MEM_read32(const void* ptr); // 31849
		ZSTD_match4Found_branch(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32006
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32020
		MEM_read32(const void* ptr); // 31851
		MEM_read32(const void* ptr); // 31849
		ZSTD_match4Found_branch(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32031
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32047
		MEM_read32(const void* ptr); // 31982
		MEM_read32(const void* ptr); // 31989
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 32110
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32112
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32121
	MEM_read32(const void* ptr); // 32125
	MEM_read32(const void* ptr); // 32125
	{
		const size_t  rLength; // 32127
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 32127
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32129
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 32131
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32122
	ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const U32  mls,
						int useCmov);  // 32154
	ZSTD_compressBlock_fast_noDict_6_0(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32188

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31918
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31954
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31975
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31976
	{
		const U32  rval; // 31982
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31827
		MEM_read32(const void* ptr); // 31833
		MEM_read32(const void* ptr); // 31833
		ZSTD_match4Found_cmov(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32006
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31827
		MEM_read32(const void* ptr); // 31833
		MEM_read32(const void* ptr); // 31833
		ZSTD_match4Found_cmov(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32031
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32020
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32047
		MEM_read32(const void* ptr); // 31982
		MEM_read32(const void* ptr); // 31989
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 32110
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32112
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32121
	MEM_read32(const void* ptr); // 32125
	MEM_read32(const void* ptr); // 32125
	{
		const size_t  rLength; // 32127
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 32127
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32129
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 32131
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32122
	ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const U32  mls,
						int useCmov);  // 32149
	ZSTD_compressBlock_fast_noDict_6_1(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32174

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31918
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31954
	}
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31975
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31976
	{
		const U32  rval; // 31982
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31827
		MEM_read32(const void* ptr); // 31833
		MEM_read32(const void* ptr); // 31833
		ZSTD_match4Found_cmov(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32006
		ZSTD_selectAddr(U32 index,
				U32 lowLimit,
				const BYTE* candidate,
				const BYTE* backup);  // 31827
		MEM_read32(const void* ptr); // 31833
		MEM_read32(const void* ptr); // 31833
		ZSTD_match4Found_cmov(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32031
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32020
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32047
		MEM_read32(const void* ptr); // 31982
		MEM_read32(const void* ptr); // 31989
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 32110
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32112
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32121
	MEM_read32(const void* ptr); // 32125
	MEM_read32(const void* ptr); // 32125
	{
		const size_t  rLength; // 32127
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 32127
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32129
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 32131
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32122
	ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const U32  mls,
						int useCmov);  // 32150
	ZSTD_compressBlock_fast_noDict_7_1(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32176

_offset: // 32091

_match: // 32107

_cleanup: // 32064

_start: // 31961
	ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
					U32 curr,
					unsigned int windowLog);  // 31918
	{
		const U32  curr; // 31953
		const U32  windowLow; // 31954
		const U32  maxRep; // 31955
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 31954
	}
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31975
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 31976
	{
		const U32  rval; // 31982
		MEM_read32(const void* ptr); // 31851
		MEM_read32(const void* ptr); // 31849
		ZSTD_match4Found_branch(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32006
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32020
		MEM_read32(const void* ptr); // 31851
		MEM_read32(const void* ptr); // 31849
		ZSTD_match4Found_branch(const BYTE* currentPtr,
					const BYTE* matchAddress,
					U32 matchIdx,
					U32 idxLowLimit);  // 32031
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32047
		MEM_read32(const void* ptr); // 31982
		MEM_read32(const void* ptr); // 31989
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 32110
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32112
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32121
	MEM_read32(const void* ptr); // 32125
	MEM_read32(const void* ptr); // 32125
	{
		const size_t  rLength; // 32127
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 32127
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32129
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 32131
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32122
	ZSTD_compressBlock_fast_noDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const U32  mls,
						int useCmov);  // 32155
	ZSTD_compressBlock_fast_noDict_7_0(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32190
} /* size: 15883, variables: 2, goto labels: 28, inline expansions: 315 (39468 bytes) */

// <06FB4847> thirdparty/zstd/zstd.c:32197
// variables: 57
inline size_t ZSTD_compressBlock_fast_dictMatchState_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const U32 mls, const U32 hasStep)
{

_cleanup: // 32385
	const ZSTD_compressionParameters* cParams; // 32201
	const U32* hashTable; // 32202
	const U32  hlog; // 32203
	const U32  stepSize; // 32205
	const BYTE* base; // 32206
	const BYTE* istart; // 32207
	const BYTE* ip0; // 32208
	const BYTE* ip1; // 32209
	const BYTE* anchor; // 32210
	const U32  prefixStartIndex; // 32211
	const BYTE* prefixStart; // 32212
	const BYTE* iend; // 32213
	const BYTE* ilimit; // 32214
	U32 offset_1; // 32215
	U32 offset_2; // 32215
	const ZSTD_MatchState_t* dms; // 32217
	const ZSTD_compressionParameters* dictCParams; // 32218
	const U32* dictHashTable; // 32219
	const U32  dictStartIndex; // 32220
	const BYTE* dictBase; // 32221
	const BYTE* dictStart; // 32222
	const BYTE* dictEnd; // 32223
	const U32  dictIndexDelta; // 32224
	const U32  dictAndPrefixLength; // 32225
	const U32  dictHBits; // 32226
	const U32  maxDistance; // 32230
	const U32  endIndex; // 32231
	{
		const size_t  hashTableBytes; // 32242
		{
			const char* _ptr; // 32243
			const size_t  _size; // 32243
			size_t _pos; // 32243
		}
	}
	{
		size_t mLength; // 32257
		size_t hash0; // 32258
		const size_t  dictHashAndTag0; // 32260
		U32 dictMatchIndexAndTag; // 32261
		int dictTagsMatch; // 32262
		U32 matchIndex; // 32264
		U32 curr; // 32265
		size_t step; // 32266
		const size_t  kStepIncr; // 32267
		const BYTE* nextStep; // 32268
		{
			const BYTE* match; // 32272
			const U32  repIndex; // 32273
			const BYTE* repMatch; // 32274
			const size_t  hash1; // 32277
			const size_t  dictHashAndTag1; // 32278
			{
				const BYTE* repMatchEnd; // 32283
			}
			{
				const U32  dictMatchIndex; // 32292
				const BYTE* dictMatch; // 32293
				{
					const U32  offset; // 32298
				}
			}
			{
				const U32  offset; // 32316
			}
		}
		{
			const U32  current2; // 32360
			const U32  repIndex2; // 32361
			const BYTE* repMatch2; // 32362
			{
				const BYTE* repEnd2; // 32367
				const size_t  repLength2; // 32368
				U32 tmpOffset; // 32369
			}
		}
	}
} /* size: 0, variables: 27, goto labels: 1 */

// <06FB47F4> thirdparty/zstd/zstd.c:32395
size_t ZSTD_compressBlock_fast_dictMatchState_4_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FB47A1> thirdparty/zstd/zstd.c:32396
size_t ZSTD_compressBlock_fast_dictMatchState_5_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FB474E> thirdparty/zstd/zstd.c:32397
size_t ZSTD_compressBlock_fast_dictMatchState_6_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FB46FB> thirdparty/zstd/zstd.c:32398
size_t ZSTD_compressBlock_fast_dictMatchState_7_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FAD068> thirdparty/zstd/zstd.c:32400
// variables: 141
// function calls: 432
size_t ZSTD_compressBlock_fast_dictMatchState(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
	const U32  mls; // 32404

_cleanup: // 32385
	{
		const size_t  hashTableBytes; // 32242
		{
			const char* _ptr; // 32243
			const size_t  _size; // 32243
			size_t _pos; // 32243
		}
	}
	{
		size_t mLength; // 32257
		size_t hash0; // 32258
		const size_t  dictHashAndTag0; // 32260
		U32 dictMatchIndexAndTag; // 32261
		int dictTagsMatch; // 32262
		U32 matchIndex; // 32264
		U32 curr; // 32265
		size_t step; // 32266
		const size_t  kStepIncr; // 32267
		const BYTE* nextStep; // 32268
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32258
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32260
		{
			const BYTE* match; // 32272
			const U32  repIndex; // 32273
			const BYTE* repMatch; // 32274
			const size_t  hash1; // 32277
			const size_t  dictHashAndTag1; // 32278
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32277
			ZSTD_comparePackedTags(size_t packedTag1,
						size_t packedTag2);  // 32332
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32278
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 32281
			MEM_read32(const void* ptr); // 32282
			MEM_read32(const void* ptr); // 32282
			{
				const BYTE* repMatchEnd; // 32283
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 19957
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32286
			}
			{
				const U32  dictMatchIndex; // 32292
				const BYTE* dictMatch; // 32293
				MEM_read32(const void* ptr); // 32295
				MEM_read32(const void* ptr); // 32295
				{
					const U32  offset; // 32298
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 19955
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 19957
					ZSTD_storeSeq(SeqStore_t* seqStorePtr,
							size_t litLength,
							const BYTE* literals,
							const BYTE* litLimit,
							U32 offBase,
							size_t matchLength);  // 32308
				}
			}
			ZSTD_selectAddr(U32 index,
					U32 lowLimit,
					const BYTE* candidate,
					const BYTE* backup);  // 31827
			MEM_read32(const void* ptr); // 31833
			ZSTD_match4Found_cmov(const BYTE* currentPtr,
						const BYTE* matchAddress,
						U32 matchIdx,
						U32 idxLowLimit);  // 32314
			{
				const U32  offset; // 32316
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 32317
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 19957
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32326
			}
		}
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 32262
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32355
		{
			const U32  current2; // 32360
			const U32  repIndex2; // 32361
			const BYTE* repMatch2; // 32362
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 32365
			MEM_read32(const void* ptr); // 32366
			MEM_read32(const void* ptr); // 32366
			{
				const BYTE* repEnd2; // 32367
				const size_t  repLength2; // 32368
				U32 tmpOffset; // 32369
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32370
				MEM_read32(const void* ptr); // 1301
				MEM_readLE32(const void* memPtr); // 20059
				ZSTD_hash4(U32 u,
						U32 h,
						U32 s);  // 20059
				ZSTD_hash4Ptr(const void* ptr,
						U32 h);  // 20093
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 32371
			}
		}
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32356
	}
	ZSTD_compressBlock_fast_dictMatchState_generic(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize,
							const U32  mls,
							const U32  hasStep);  // 32395
	ZSTD_compressBlock_fast_dictMatchState_4_0(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize);  // 32410

_cleanup: // 32385
	{
		const size_t  hashTableBytes; // 32242
		{
			const char* _ptr; // 32243
			const size_t  _size; // 32243
			size_t _pos; // 32243
		}
	}
	{
		size_t mLength; // 32257
		size_t hash0; // 32258
		const size_t  dictHashAndTag0; // 32260
		U32 dictMatchIndexAndTag; // 32261
		int dictTagsMatch; // 32262
		U32 matchIndex; // 32264
		U32 curr; // 32265
		size_t step; // 32266
		const size_t  kStepIncr; // 32267
		const BYTE* nextStep; // 32268
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32258
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32260
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 32262
		{
			const BYTE* match; // 32272
			const U32  repIndex; // 32273
			const BYTE* repMatch; // 32274
			const size_t  hash1; // 32277
			const size_t  dictHashAndTag1; // 32278
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32277
			ZSTD_comparePackedTags(size_t packedTag1,
						size_t packedTag2);  // 32332
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32278
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 32281
			MEM_read32(const void* ptr); // 32282
			MEM_read32(const void* ptr); // 32282
			{
				const BYTE* repMatchEnd; // 32283
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 19957
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32286
			}
			{
				const U32  dictMatchIndex; // 32292
				const BYTE* dictMatch; // 32293
				MEM_read32(const void* ptr); // 32295
				MEM_read32(const void* ptr); // 32295
				{
					const U32  offset; // 32298
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 19955
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 19957
					ZSTD_storeSeq(SeqStore_t* seqStorePtr,
							size_t litLength,
							const BYTE* literals,
							const BYTE* litLimit,
							U32 offBase,
							size_t matchLength);  // 32308
				}
			}
			ZSTD_selectAddr(U32 index,
					U32 lowLimit,
					const BYTE* candidate,
					const BYTE* backup);  // 31827
			MEM_read32(const void* ptr); // 31833
			ZSTD_match4Found_cmov(const BYTE* currentPtr,
						const BYTE* matchAddress,
						U32 matchIdx,
						U32 idxLowLimit);  // 32314
			{
				const U32  offset; // 32316
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 32317
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 19957
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32326
			}
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32355
		{
			const U32  current2; // 32360
			const U32  repIndex2; // 32361
			const BYTE* repMatch2; // 32362
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 32365
			MEM_read32(const void* ptr); // 32366
			MEM_read32(const void* ptr); // 32366
			{
				const BYTE* repEnd2; // 32367
				const size_t  repLength2; // 32368
				U32 tmpOffset; // 32369
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32370
				ZSTD_hash7(U64 u,
						U32 h,
						U64 s);  // 20074
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20074
				ZSTD_hash7Ptr(const void* p,
						U32 h);  // 20096
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 32371
			}
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32356
	}
	ZSTD_compressBlock_fast_dictMatchState_generic(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize,
							const U32  mls,
							const U32  hasStep);  // 32398
	ZSTD_compressBlock_fast_dictMatchState_7_0(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize);  // 32416

_cleanup: // 32385
	{
		const size_t  hashTableBytes; // 32242
		{
			const char* _ptr; // 32243
			const size_t  _size; // 32243
			size_t _pos; // 32243
		}
	}
	{
		size_t mLength; // 32257
		size_t hash0; // 32258
		const size_t  dictHashAndTag0; // 32260
		U32 dictMatchIndexAndTag; // 32261
		int dictTagsMatch; // 32262
		U32 matchIndex; // 32264
		U32 curr; // 32265
		size_t step; // 32266
		const size_t  kStepIncr; // 32267
		const BYTE* nextStep; // 32268
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32258
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32260
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 32262
		{
			const BYTE* match; // 32272
			const U32  repIndex; // 32273
			const BYTE* repMatch; // 32274
			const size_t  hash1; // 32277
			const size_t  dictHashAndTag1; // 32278
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32277
			ZSTD_comparePackedTags(size_t packedTag1,
						size_t packedTag2);  // 32332
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32278
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 32281
			MEM_read32(const void* ptr); // 32282
			MEM_read32(const void* ptr); // 32282
			{
				const BYTE* repMatchEnd; // 32283
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 19957
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32286
			}
			{
				const U32  dictMatchIndex; // 32292
				const BYTE* dictMatch; // 32293
				MEM_read32(const void* ptr); // 32295
				MEM_read32(const void* ptr); // 32295
				{
					const U32  offset; // 32298
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 19955
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 19957
					ZSTD_storeSeq(SeqStore_t* seqStorePtr,
							size_t litLength,
							const BYTE* literals,
							const BYTE* litLimit,
							U32 offBase,
							size_t matchLength);  // 32308
				}
			}
			ZSTD_selectAddr(U32 index,
					U32 lowLimit,
					const BYTE* candidate,
					const BYTE* backup);  // 31827
			MEM_read32(const void* ptr); // 31833
			ZSTD_match4Found_cmov(const BYTE* currentPtr,
						const BYTE* matchAddress,
						U32 matchIdx,
						U32 idxLowLimit);  // 32314
			{
				const U32  offset; // 32316
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 32317
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 19957
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32326
			}
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32355
		{
			const U32  current2; // 32360
			const U32  repIndex2; // 32361
			const BYTE* repMatch2; // 32362
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 32365
			MEM_read32(const void* ptr); // 32366
			MEM_read32(const void* ptr); // 32366
			{
				const BYTE* repEnd2; // 32367
				const size_t  repLength2; // 32368
				U32 tmpOffset; // 32369
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32370
				ZSTD_hash6(U64 u,
						U32 h,
						U64 s);  // 20069
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20069
				ZSTD_hash6Ptr(const void* p,
						U32 h);  // 20095
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 32371
			}
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32356
	}
	ZSTD_compressBlock_fast_dictMatchState_generic(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize,
							const U32  mls,
							const U32  hasStep);  // 32397
	ZSTD_compressBlock_fast_dictMatchState_6_0(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize);  // 32414

_cleanup: // 32385
	{
		const size_t  hashTableBytes; // 32242
		{
			const char* _ptr; // 32243
			const size_t  _size; // 32243
			size_t _pos; // 32243
		}
	}
	{
		size_t mLength; // 32257
		size_t hash0; // 32258
		const size_t  dictHashAndTag0; // 32260
		U32 dictMatchIndexAndTag; // 32261
		int dictTagsMatch; // 32262
		U32 matchIndex; // 32264
		U32 curr; // 32265
		size_t step; // 32266
		const size_t  kStepIncr; // 32267
		const BYTE* nextStep; // 32268
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32258
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32260
		ZSTD_comparePackedTags(size_t packedTag1,
					size_t packedTag2);  // 32262
		{
			const BYTE* match; // 32272
			const U32  repIndex; // 32273
			const BYTE* repMatch; // 32274
			const size_t  hash1; // 32277
			const size_t  dictHashAndTag1; // 32278
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32277
			ZSTD_comparePackedTags(size_t packedTag1,
						size_t packedTag2);  // 32332
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32278
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 32281
			MEM_read32(const void* ptr); // 32282
			MEM_read32(const void* ptr); // 32282
			{
				const BYTE* repMatchEnd; // 32283
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 19957
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32286
			}
			{
				const U32  dictMatchIndex; // 32292
				const BYTE* dictMatch; // 32293
				MEM_read32(const void* ptr); // 32295
				MEM_read32(const void* ptr); // 32295
				{
					const U32  offset; // 32298
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 19955
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 19957
					ZSTD_storeSeq(SeqStore_t* seqStorePtr,
							size_t litLength,
							const BYTE* literals,
							const BYTE* litLimit,
							U32 offBase,
							size_t matchLength);  // 32308
				}
			}
			ZSTD_selectAddr(U32 index,
					U32 lowLimit,
					const BYTE* candidate,
					const BYTE* backup);  // 31827
			MEM_read32(const void* ptr); // 31833
			ZSTD_match4Found_cmov(const BYTE* currentPtr,
						const BYTE* matchAddress,
						U32 matchIdx,
						U32 idxLowLimit);  // 32314
			{
				const U32  offset; // 32316
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 32317
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 19957
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32326
			}
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32355
		{
			const U32  current2; // 32360
			const U32  repIndex2; // 32361
			const BYTE* repMatch2; // 32362
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 32365
			MEM_read32(const void* ptr); // 32366
			MEM_read32(const void* ptr); // 32366
			{
				const BYTE* repEnd2; // 32367
				const size_t  repLength2; // 32368
				U32 tmpOffset; // 32369
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 32370
				ZSTD_hash5(U64 u,
						U32 h,
						U64 s);  // 20064
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20064
				ZSTD_hash5Ptr(const void* p,
						U32 h);  // 20094
				ZSTD_hashPtr(const void* p,
						U32 hBits,
						U32 mls);  // 32371
			}
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32356
	}
	ZSTD_compressBlock_fast_dictMatchState_generic(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize,
							const U32  mls,
							const U32  hasStep);  // 32396
	ZSTD_compressBlock_fast_dictMatchState_5_0(ZSTD_MatchState_t* ms,
							SeqStore_t* seqStore,
							U32* rep,
							const void* src,
							size_t srcSize);  // 32412
} /* size: 9589, variables: 1, goto labels: 4, inline expansions: 8 (18980 bytes) */

// <0706E6C8> thirdparty/zstd/zstd.c:32423
// variables: 58
// function calls: 159
size_t ZSTD_compressBlock_fast_extDict_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const U32 mls, const U32 hasStep)
{

_offset: // 32614

_match: // 32633

_cleanup: // 32598

_start: // 32485
	const ZSTD_compressionParameters* cParams; // 32427
	const U32* hashTable; // 32428
	const U32  hlog; // 32429
	const size_t  stepSize; // 32431
	const BYTE* base; // 32432
	const BYTE* dictBase; // 32433
	const BYTE* istart; // 32434
	const BYTE* anchor; // 32435
	const U32  endIndex; // 32436
	const U32  lowLimit; // 32437
	const U32  dictStartIndex; // 32438
	const BYTE* dictStart; // 32439
	const U32  dictLimit; // 32440
	const U32  prefixStartIndex; // 32441
	const BYTE* prefixStart; // 32442
	const BYTE* dictEnd; // 32443
	const BYTE* iend; // 32444
	const BYTE* ilimit; // 32445
	U32 offset_1; // 32446
	U32 offset_2; // 32446
	U32 offsetSaved1; // 32447
	U32 offsetSaved2; // 32447
	const BYTE* ip0; // 32449
	const BYTE* ip1; // 32450
	const BYTE* ip2; // 32451
	const BYTE* ip3; // 32452
	U32 current0; // 32453
	size_t hash0; // 32456
	size_t hash1; // 32457
	U32 idx; // 32458
	const BYTE* idxBase; // 32459
	U32 offcode; // 32461
	const BYTE* match0; // 32462
	size_t mLength; // 32463
	const BYTE* matchEnd; // 32464
	size_t step; // 32466
	const BYTE* nextStep; // 32467
	const size_t  kStepIncr; // 32468
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 32437
	{
		const U32  curr; // 32478
		const U32  maxRep; // 32479
	}
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32553
	{
		const U32  current2; // 32507
		const U32  repIndex; // 32508
		const BYTE* repBase; // 32509
		U32 rval; // 32510
		MEM_read32(const void* ptr); // 32513
		MEM_read32(const void* ptr); // 32523
		MEM_read32(const void* ptr); // 32515
	}
	{
		const U32  mval; // 32537
		MEM_read32(const void* ptr); // 32539
		MEM_read32(const void* ptr); // 32538
	}
	{
		const U32  mval; // 32565
		MEM_read32(const void* ptr); // 32567
		MEM_read32(const void* ptr); // 32566
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32581
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32499
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32500
	{
		const U32  offset; // 32617
		const BYTE* lowMatchPtr; // 32618
	}
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 20038
	{
		const size_t  diff; // 20014
		MEM_readST(const void* ptr); // 20014
		MEM_readST(const void* ptr); // 20014
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20015
	}
	{
		const size_t  diff; // 20018
		MEM_readST(const void* ptr); // 20018
		MEM_readST(const void* ptr); // 20018
		ZSTD_countTrailingZeros64(U64 val); // 2090
		ZSTD_NbCommonBytes(size_t val); // 20020
	}
	MEM_read32(const void* ptr); // 20023
	MEM_read32(const void* ptr); // 20023
	MEM_read16(const void* ptr); // 20024
	MEM_read16(const void* ptr); // 20024
	ZSTD_count(const BYTE* pIn,
			const BYTE* pMatch,
			const BYTE* pInLimit);  // 20045
	ZSTD_count_2segments(const BYTE* ip,
				const BYTE* match,
				const BYTE* iEnd,
				const BYTE* mEnd,
				const BYTE* iStart);  // 32637
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 19955
	{
		const size_t  mlBase; // 19911
	}
	ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
				size_t litLength,
				U32 offBase,
				size_t matchLength);  // 19964
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 19957
	ZSTD_storeSeq(SeqStore_t* seqStorePtr,
			size_t litLength,
			const BYTE* literals,
			const BYTE* litLimit,
			U32 offBase,
			size_t matchLength);  // 32639
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32653
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32654
	{
		const U32  repIndex2; // 32657
		const BYTE* repMatch2; // 32658
		ZSTD_index_overlap_check(const U32  prefixLowestIndex,
					const U32  repIndex);  // 32659
		MEM_read32(const void* ptr); // 32660
		MEM_read32(const void* ptr); // 32660
		{
			const BYTE* repEnd2; // 32661
			const size_t  repLength2; // 32662
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 32664
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20064
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20064
			ZSTD_hash5Ptr(const void* p,
					U32 h);  // 20094
			ZSTD_hash7(U64 u,
					U32 h,
					U64 s);  // 20074
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20074
			ZSTD_hash7Ptr(const void* p,
					U32 h);  // 20096
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20059
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20059
			ZSTD_hash4Ptr(const void* ptr,
					U32 h);  // 20093
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20069
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 20095
			ZSTD_hashPtr(const void* p,
					U32 hBits,
					U32 mls);  // 32665
		}
	}
} /* size: 3305, variables: 38, goto labels: 4, inline expansions: 111 (3502 bytes) */

// <06FACD02> thirdparty/zstd/zstd.c:32423
// variables: 53
size_t ZSTD_compressBlock_fast_extDict_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const U32 mls, const U32 hasStep)
{

_offset: // 32614

_match: // 32633

_cleanup: // 32598

_start: // 32485
	const ZSTD_compressionParameters* cParams; // 32427
	const U32* hashTable; // 32428
	const U32  hlog; // 32429
	const size_t  stepSize; // 32431
	const BYTE* base; // 32432
	const BYTE* dictBase; // 32433
	const BYTE* istart; // 32434
	const BYTE* anchor; // 32435
	const U32  endIndex; // 32436
	const U32  lowLimit; // 32437
	const U32  dictStartIndex; // 32438
	const BYTE* dictStart; // 32439
	const U32  dictLimit; // 32440
	const U32  prefixStartIndex; // 32441
	const BYTE* prefixStart; // 32442
	const BYTE* dictEnd; // 32443
	const BYTE* iend; // 32444
	const BYTE* ilimit; // 32445
	U32 offset_1; // 32446
	U32 offset_2; // 32446
	U32 offsetSaved1; // 32447
	U32 offsetSaved2; // 32447
	const BYTE* ip0; // 32449
	const BYTE* ip1; // 32450
	const BYTE* ip2; // 32451
	const BYTE* ip3; // 32452
	U32 current0; // 32453
	size_t hash0; // 32456
	size_t hash1; // 32457
	U32 idx; // 32458
	const BYTE* idxBase; // 32459
	U32 offcode; // 32461
	const BYTE* match0; // 32462
	size_t mLength; // 32463
	const BYTE* matchEnd; // 32464
	size_t step; // 32466
	const BYTE* nextStep; // 32467
	const size_t  kStepIncr; // 32468
	{
		const U32  curr; // 32478
		const U32  maxRep; // 32479
	}
	{
		const U32  current2; // 32507
		const U32  repIndex; // 32508
		const BYTE* repBase; // 32509
		U32 rval; // 32510
	}
	{
		const U32  mval; // 32537
	}
	{
		const U32  mval; // 32565
	}
	{
		const U32  offset; // 32617
		const BYTE* lowMatchPtr; // 32618
	}
	{
		const U32  repIndex2; // 32657
		const BYTE* repMatch2; // 32658
		{
			const BYTE* repEnd2; // 32661
			const size_t  repLength2; // 32662
			{
				const U32  tmpOffset; // 32663
			}
		}
	}
} /* size: 0, variables: 38, goto labels: 4 */

// <06FACCAF> thirdparty/zstd/zstd.c:32676
size_t ZSTD_compressBlock_fast_extDict_4_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FACC5C> thirdparty/zstd/zstd.c:32677
size_t ZSTD_compressBlock_fast_extDict_5_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FACC09> thirdparty/zstd/zstd.c:32678
size_t ZSTD_compressBlock_fast_extDict_6_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FACBB6> thirdparty/zstd/zstd.c:32679
size_t ZSTD_compressBlock_fast_extDict_7_0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06FAC876> thirdparty/zstd/zstd.c:32681
// variable: 1
// function calls: 4
size_t ZSTD_compressBlock_fast_extDict(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
	const U32  mls; // 32685
	ZSTD_compressBlock_fast_extDict_4_0(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32691
	ZSTD_compressBlock_fast_extDict_7_0(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32697
	ZSTD_compressBlock_fast_extDict_6_0(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32695
	ZSTD_compressBlock_fast_extDict_5_0(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize);  // 32693
} /* size: 83, variables: 1, inline expansions: 4 (58 bytes) */

// <07018DF6> thirdparty/zstd/zstd.c:32730
// variables: 13
// function calls: 13
void ZSTD_updateDUBT(ZSTD_MatchState_t* ms, const BYTE* ip, U32 mls, const BYTE* iend)
{
	const ZSTD_compressionParameters* cParams; // 32734
	const U32* hashTable; // 32735
	const U32  hashLog; // 32736
	const U32* bt; // 32738
	const U32  btLog; // 32739
	const U32  btMask; // 32740
	const BYTE* base; // 32742
	const U32  target; // 32743
	U32 idx; // 32744
	{
		const size_t  h; // 32754
		const U32  matchIndex; // 32755
		const U32* nextCandidatePtr; // 32757
		const U32* sortMarkPtr; // 32758
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32754
	}
} /* size: 260, variables: 9 */

// <06FAC78D> thirdparty/zstd/zstd.c:32730
// variables: 13
void ZSTD_updateDUBT(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iend, U32 mls)
{
	const ZSTD_compressionParameters* cParams; // 32734
	const U32* hashTable; // 32735
	const U32  hashLog; // 32736
	const U32* bt; // 32738
	const U32  btLog; // 32739
	const U32  btMask; // 32740
	const BYTE* base; // 32742
	const U32  target; // 32743
	U32 idx; // 32744
	{
		const size_t  h; // 32754
		const U32  matchIndex; // 32755
		const U32* nextCandidatePtr; // 32757
		const U32* sortMarkPtr; // 32758
	}
} /* size: 0, variables: 9 */

// <06FAC5F7> thirdparty/zstd/zstd.c:32775
// variables: 24
void ZSTD_insertDUBT1(const ZSTD_MatchState_t* ms, U32 curr, const BYTE* inputEnd, U32 nbCompares, U32 btLow, const ZSTD_dictMode_e dictMode)
{
	const ZSTD_compressionParameters* cParams; // 32780
	const U32* bt; // 32781
	const U32  btLog; // 32782
	const U32  btMask; // 32783
	size_t commonLengthSmaller; // 32784
	size_t commonLengthLarger; // 32784
	const BYTE* base; // 32785
	const BYTE* dictBase; // 32786
	const U32  dictLimit; // 32787
	const BYTE* ip; // 32788
	const BYTE* iend; // 32789
	const BYTE* dictEnd; // 32790
	const BYTE* prefixStart; // 32791
	const BYTE* match; // 32792
	U32* smallerPtr; // 32793
	U32* largerPtr; // 32794
	U32 matchIndex; // 32795
	U32 dummy32; // 32796
	const U32  windowValid; // 32797
	const U32  maxDistance; // 32798
	const U32  windowLow; // 32799
	{
		const U32* nextPtr; // 32808
		size_t matchLength; // 32809
		{
			const BYTE* mBase; // 32818
		}
	}
} /* size: 0, variables: 21 */

// <06FAC431> thirdparty/zstd/zstd.c:32865
// variables: 25
size_t ZSTD_DUBT_findBetterDictMatch(const ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iend, size_t* offsetPtr, size_t bestLength, U32 nbCompares, const U32 mls, const ZSTD_dictMode_e dictMode)
{
	const ZSTD_MatchState_t* dms; // 32874
	const ZSTD_compressionParameters* dmsCParams; // 32875
	const U32* dictHashTable; // 32876
	const U32  hashLog; // 32877
	const size_t  h; // 32878
	U32 dictMatchIndex; // 32879
	const BYTE* base; // 32881
	const BYTE* prefixStart; // 32882
	const U32  curr; // 32883
	const BYTE* dictBase; // 32884
	const BYTE* dictEnd; // 32885
	const U32  dictHighLimit; // 32886
	const U32  dictLowLimit; // 32887
	const U32  dictIndexDelta; // 32888
	const U32* dictBt; // 32890
	const U32  btLog; // 32891
	const U32  btMask; // 32892
	const U32  btLow; // 32893
	size_t commonLengthSmaller; // 32895
	size_t commonLengthLarger; // 32895
	{
		const U32* nextPtr; // 32901
		size_t matchLength; // 32902
		const BYTE* match; // 32903
		{
			U32 matchIndex; // 32909
		}
	}
	{
		const U32  mIndex; // 32933
	}
} /* size: 0, variables: 20 */

// <06FA9EA2> thirdparty/zstd/zstd.c:32944
// variables: 58
// function calls: 144
size_t ZSTD_DUBT_findBestMatch(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iend, size_t* offBasePtr, const U32 mls, const ZSTD_dictMode_e dictMode)
{
	const ZSTD_compressionParameters* cParams; // 32950
	const U32* hashTable; // 32951
	const U32  hashLog; // 32952
	const size_t  h; // 32953
	U32 matchIndex; // 32954
	const BYTE* base; // 32956
	const U32  curr; // 32957
	const U32  windowLow; // 32958
	const U32* bt; // 32960
	const U32  btLog; // 32961
	const U32  btMask; // 32962
	const U32  btLow; // 32963
	const U32  unsortLimit; // 32964
	U32* nextCandidate; // 32966
	U32* unsortedMark; // 32967
	U32 nbCompares; // 32968
	U32 nbCandidates; // 32969
	U32 previousCandidate; // 32970
	{
		const U32* nextCandidateIdxPtr; // 33002
		const U32  nextCandidateIdx; // 33003
		{
			const U32* nextPtr; // 32808
			size_t matchLength; // 32809
			{
				const BYTE* mBase; // 32818
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 32824
			}
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 32827
		}
		ZSTD_insertDUBT1(const ZSTD_MatchState_t* ms,
				U32 curr,
				const BYTE* inputEnd,
				U32 nbCompares,
				U32 btLow,
				const ZSTD_dictMode_e  dictMode);  // 33004
	}
	{
		size_t commonLengthSmaller; // 33011
		size_t commonLengthLarger; // 33011
		const BYTE* dictBase; // 33012
		const U32  dictLimit; // 33013
		const BYTE* dictEnd; // 33014
		const BYTE* prefixStart; // 33015
		U32* smallerPtr; // 33016
		U32* largerPtr; // 33017
		U32 matchEndIdx; // 33018
		U32 dummy32; // 33019
		size_t bestLength; // 33020
		{
			const U32* nextPtr; // 33026
			size_t matchLength; // 33027
			const BYTE* match; // 33028
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33032
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 33043
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 33043
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33035
		}
		{
			const U32  mIndex; // 33084
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 32878
		{
			const U32* nextPtr; // 32901
			size_t matchLength; // 32902
			const BYTE* match; // 32903
			{
				U32 matchIndex; // 32909
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 32910
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 32910
			}
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 32904
		}
		ZSTD_DUBT_findBetterDictMatch(const ZSTD_MatchState_t* ms,
						const BYTE* ip,
						const BYTE* iend,
						size_t* offsetPtr,
						size_t bestLength,
						U32 nbCompares,
						const U32  mls,
						const ZSTD_dictMode_e  dictMode);  // 33075
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 32953
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 32958
} /* size: 5219, variables: 18, inline expansions: 14 (277 bytes) */

// <06FA9E43> thirdparty/zstd/zstd.c:33096
inline size_t ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr, const U32 mls, const ZSTD_dictMode_e dictMode)
{
} /* size: 0 */

// <06FA9316> thirdparty/zstd/zstd.c:33112
// variables: 27
// function calls: 42
void ZSTD_dedicatedDictSearch_lazy_loadDictionary(ZSTD_MatchState_t* ms, const BYTE* ip)
{
	const BYTE* base; // 33114
	const U32  target; // 33115
	const U32* hashTable; // 33116
	const U32* chainTable; // 33117
	const U32  chainSize; // 33118
	U32 idx; // 33119
	const U32  minChain; // 33120
	const U32  bucketSize; // 33121
	const U32  cacheSize; // 33122
	const U32  chainAttempts; // 33123
	const U32  chainLimit; // 33124
	const U32  hashLog; // 33131
	const U32* tmpHashTable; // 33132
	const U32* tmpChainTable; // 33133
	const U32  tmpChainSize; // 33134
	const U32  tmpMinChain; // 33135
	U32 hashIdx; // 33136
	{
		const U32  h; // 33145
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33145
	}
	{
		U32 chainPos; // 33154
		{
			U32 count; // 33156
			U32 countBeyondMinChain; // 33157
			U32 i; // 33158
		}
	}
	{
		const U32  bucketIdx; // 33203
		const U32  chainPackedPointer; // 33204
		U32 i; // 33205
	}
	{
		const U32  h; // 33214
		U32 i; // 33216
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33214
	}
} /* size: 1001, variables: 17 */

// <06FA9162> thirdparty/zstd/zstd.c:33230
// variables: 21
inline size_t ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr, size_t ml, U32 nbAttempts, const ZSTD_MatchState_t* dms, const BYTE* ip, const BYTE* iLimit, const BYTE* prefixStart, const U32 curr, const U32 dictLimit, const size_t ddsIdx)
{
	const U32  ddsLowestIndex; // 33235
	const BYTE* ddsBase; // 33236
	const BYTE* ddsEnd; // 33237
	const U32  ddsSize; // 33238
	const U32  ddsIndexDelta; // 33239
	const U32  bucketSize; // 33240
	const U32  bucketLimit; // 33241
	U32 ddsAttempt; // 33242
	U32 matchIndex; // 33243
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
		}
	}
} /* size: 0, variables: 9 */

// <06FA90A8> thirdparty/zstd/zstd.c:33333
// variables: 8
inline U32 ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms, const ZSTD_compressionParameters* cParams, const BYTE* ip, const U32 mls, const U32 lazySkipping)
{
	const U32* hashTable; // 33338
	const U32  hashLog; // 33339
	const U32* chainTable; // 33340
	const U32  chainMask; // 33341
	const BYTE* base; // 33342
	const U32  target; // 33343
	U32 idx; // 33344
	{
		const size_t  h; // 33347
	}
} /* size: 0, variables: 7 */

// <06FA878F> thirdparty/zstd/zstd.c:33360
// variables: 2
// function calls: 39
U32 ZSTD_insertAndFindFirstIndex(ZSTD_MatchState_t* ms, const BYTE* ip)
{
	const ZSTD_compressionParameters* cParams; // 33361
	{
		const size_t  h; // 33347
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hash7(U64 u,
				U32 h,
				U64 s);  // 20074
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20074
		ZSTD_hash7Ptr(const void* p,
				U32 h);  // 20096
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20079
		ZSTD_hash8(U64 u,
				U32 h,
				U64 s);  // 20079
		ZSTD_hash8Ptr(const void* p,
				U32 h);  // 20097
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hash8(U64 u,
			U32 h,
			U64 s);  // 20079
	ZSTD_hash8Ptr(const void* p,
			U32 h);  // 20097
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33362
} /* size: 460, variables: 1, inline expansions: 18 (645 bytes) */

// <06FA8538> thirdparty/zstd/zstd.c:33368
// variables: 37
inline size_t ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr, const U32 mls, const ZSTD_dictMode_e dictMode)
{
	const ZSTD_compressionParameters* cParams; // 33374
	const U32* chainTable; // 33375
	const U32  chainSize; // 33376
	const U32  chainMask; // 33377
	const BYTE* base; // 33378
	const BYTE* dictBase; // 33379
	const U32  dictLimit; // 33380
	const BYTE* prefixStart; // 33381
	const BYTE* dictEnd; // 33382
	const U32  curr; // 33383
	const U32  maxDistance; // 33384
	const U32  lowestValid; // 33385
	const U32  withinMaxDistance; // 33386
	const U32  isDictionary; // 33387
	const U32  lowLimit; // 33388
	const U32  minChain; // 33389
	U32 nbAttempts; // 33390
	size_t ml; // 33391
	const ZSTD_MatchState_t* dms; // 33393
	const U32  ddsHashLog; // 33394
	const size_t  ddsIdx; // 33396
	U32 matchIndex; // 33399
	{
		const U32* entry; // 33402
	}
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
		}
		{
			const BYTE* match; // 33418
		}
	}
	{
		const U32* dmsChainTable; // 33440
		const U32  dmsChainSize; // 33441
		const U32  dmsChainMask; // 33442
		const U32  dmsLowestIndex; // 33443
		const BYTE* dmsBase; // 33444
		const BYTE* dmsEnd; // 33445
		const U32  dmsSize; // 33446
		const U32  dmsIndexDelta; // 33447
		const U32  dmsMinChain; // 33448
		{
			size_t currentMl; // 33453
			const BYTE* match; // 33454
		}
	}
} /* size: 0, variables: 22 */

// <06FA8518> thirdparty/zstd/zstd.c:33491
inline U32 ZSTD_VecMask_next(ZSTD_VecMask val)
{
} /* size: 0 */

// <06FA84DE> thirdparty/zstd/zstd.c:33499
// variable: 1
inline U32 ZSTD_row_nextIndex(const BYTE* tagRow, const U32 rowMask)
{
	U32 next; // 33500
} /* size: 0, variables: 1 */

// <06FA849B> thirdparty/zstd/zstd.c:33517
inline void ZSTD_row_prefetch(const U32* hashTable, const BYTE* tagTable, const U32 relRow, const U32 rowLog)
{
} /* size: 0 */

// <06FA83E2> thirdparty/zstd/zstd.c:33538
// variables: 7
inline void ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms, const BYTE* base, const U32 rowLog, const U32 mls, U32 idx, const BYTE* iLimit)
{
	const U32* hashTable; // 33542
	const BYTE* tagTable; // 33543
	const U32  hashLog; // 33544
	const U32  maxElemsToPrefetch; // 33545
	const U32  lim; // 33546
	{
		const U32  hash; // 33549
		const U32  row; // 33550
	}
} /* size: 0, variables: 5 */

// <06FA8331> thirdparty/zstd/zstd.c:33566
// variables: 3
inline U32 ZSTD_row_nextCachedHash(U32* cache, const U32* hashTable, const BYTE* tagTable, const BYTE* base, U32 idx, const U32 hashLog, const U32 rowLog, const U32 mls, const U64 hashSalt)
{
	const U32  newHash; // 33572
	const U32  row; // 33573
	{
		const U32  hash; // 33575
	}
} /* size: 0, variables: 2 */

// <06FA8251> thirdparty/zstd/zstd.c:33586
// variables: 9
inline void ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms, U32 updateStartIdx, const U32 updateEndIdx, const U32 mls, const U32 rowLog, const U32 rowMask, const U32 useCache)
{
	const U32* hashTable; // 33591
	const BYTE* tagTable; // 33592
	const U32  hashLog; // 33593
	const BYTE* base; // 33594
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
	}
} /* size: 0, variables: 4 */

// <06FA8199> thirdparty/zstd/zstd.c:33617
// variables: 7
inline void ZSTD_row_update_internal(ZSTD_MatchState_t* ms, const BYTE* ip, const U32 mls, const U32 rowLog, const U32 rowMask, const U32 useCache)
{
	U32 idx; // 33621
	const BYTE* base; // 33622
	const U32  target; // 33623
	const U32  kSkipThreshold; // 33624
	const U32  kMaxMatchStartPositionsToUpdate; // 33625
	const U32  kMaxMatchEndPositionsToUpdate; // 33626
	{
		const U32  bound; // 33635
	}
} /* size: 0, variables: 6 */

// <06FA7C02> thirdparty/zstd/zstd.c:33650
// variables: 8
// function calls: 16
void ZSTD_row_update(const ZSTD_MatchState_t* ms, const BYTE* ip)
{
	const U32  rowLog; // 33651
	const U32  rowMask; // 33652
	const U32  mls; // 33653
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33599
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33656
} /* size: 252, variables: 3, inline expansions: 2 (325 bytes) */

// <06FA7BE2> thirdparty/zstd/zstd.c:33664
inline U32 ZSTD_row_matchMaskGroupWidth(const U32 rowEntries)
{
} /* size: 0 */

// <06FA7B4A> thirdparty/zstd/zstd.c:33689
// variables: 5
inline ZSTD_VecMask ZSTD_row_getSSEMask(int nbChunks, const BYTE* src, const BYTE tag, const U32 head)
{
	const __m128i  comparisonMask; // 33691
	int matches; // 33692
	int i; // 33693
	{
		const __m128i  chunk; // 33696
		const __m128i  equalMask; // 33697
	}
} /* size: 0, variables: 3 */

// <06FA7AF6> thirdparty/zstd/zstd.c:33762
// variable: 1
inline ZSTD_VecMask ZSTD_row_getMatchMask(const BYTE* tagRow, const BYTE tag, const U32 headGrouped, const U32 rowEntries)
{
	const BYTE* src; // 33764
} /* size: 0, variables: 1 */

// <06FA76FB> thirdparty/zstd/zstd.c:33842
// variables: 66
inline size_t ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr, const U32 mls, const ZSTD_dictMode_e dictMode, const U32 rowLog)
{
	const U32* hashTable; // 33849
	const BYTE* tagTable; // 33850
	const U32* hashCache; // 33851
	const U32  hashLog; // 33852
	const ZSTD_compressionParameters* cParams; // 33853
	const BYTE* base; // 33854
	const BYTE* dictBase; // 33855
	const U32  dictLimit; // 33856
	const BYTE* prefixStart; // 33857
	const BYTE* dictEnd; // 33858
	const U32  curr; // 33859
	const U32  maxDistance; // 33860
	const U32  lowestValid; // 33861
	const U32  withinMaxDistance; // 33862
	const U32  isDictionary; // 33863
	const U32  lowLimit; // 33864
	const U32  rowEntries; // 33865
	const U32  rowMask; // 33866
	const U32  cappedSearchLog; // 33867
	const U32  groupWidth; // 33868
	const U64  hashSalt; // 33869
	U32 nbAttempts; // 33870
	size_t ml; // 33871
	U32 hash; // 33872
	const ZSTD_MatchState_t* dms; // 33875
	size_t ddsIdx; // 33878
	U32 ddsExtraAttempts; // 33879
	U32 dmsTag; // 33880
	U32* dmsRow; // 33881
	BYTE* dmsTagRow; // 33882
	{
		const U32  ddsHashLog; // 33885
	}
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
	}
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
		}
		{
			const U32  pos; // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
			}
			{
				const BYTE* match; // 33968
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
				}
			}
		}
	}
} /* size: 0, variables: 30 */

// <0706A5F9> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 127
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_4_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3839, inline expansions: 14 (6892 bytes) */

// <07068111> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 127
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_5_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3855, inline expansions: 14 (6935 bytes) */

// <07065C29> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 127
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_6_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3855, inline expansions: 14 (6935 bytes) */

// <07063761> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 127
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_4_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3775, inline expansions: 14 (6810 bytes) */

// <07061279> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 127
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_5_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3791, inline expansions: 14 (6873 bytes) */

// <0705ED91> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 127
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_6_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3791, inline expansions: 14 (6873 bytes) */

// <0705C8DF> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 126
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_4_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	{
		const U32  hash; // 33575
	}
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3647, inline expansions: 14 (6530 bytes) */

// <0705A40D> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 126
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_5_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	{
		const U32  hash; // 33575
	}
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3695, inline expansions: 14 (6647 bytes) */

// <07057F3B> thirdparty/zstd/zstd.c:34119
// variables: 56
// function calls: 126
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_6_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  ddsHashLog; // 33885
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33887
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	{
		const U32  hash; // 33575
	}
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33985
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3695, inline expansions: 14 (6647 bytes) */

// <070507AE> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 94
size_t ZSTD_RowFindBestMatch_extDict_4_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2624, inline expansions: 15 (3691 bytes) */

// <0704EBB5> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 94
size_t ZSTD_RowFindBestMatch_extDict_5_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2736, inline expansions: 15 (3971 bytes) */

// <0704CFBC> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 94
size_t ZSTD_RowFindBestMatch_extDict_6_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2736, inline expansions: 15 (3971 bytes) */

// <0704B3E9> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 94
size_t ZSTD_RowFindBestMatch_extDict_4_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2567, inline expansions: 15 (3669 bytes) */

// <070497FC> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 94
size_t ZSTD_RowFindBestMatch_extDict_5_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2632, inline expansions: 15 (3846 bytes) */

// <07047C0F> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 94
size_t ZSTD_RowFindBestMatch_extDict_6_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2632, inline expansions: 15 (3846 bytes) */

// <0704604E> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 93
size_t ZSTD_RowFindBestMatch_extDict_4_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2503, inline expansions: 15 (3532 bytes) */

// <0704446B> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 93
size_t ZSTD_RowFindBestMatch_extDict_5_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2584, inline expansions: 15 (3751 bytes) */

// <07042888> thirdparty/zstd/zstd.c:34119
// variables: 40
// function calls: 93
size_t ZSTD_RowFindBestMatch_extDict_6_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33968
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 33971
				MEM_read32(const void* ptr); // 33970
				MEM_read32(const void* ptr); // 33970
			}
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2584, inline expansions: 15 (3751 bytes) */

// <0703D723> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 108
size_t ZSTD_RowFindBestMatch_dictMatchState_4_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_rotateRight_U64(const U64  value,
						U32 count);  // 33703
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3102, inline expansions: 13 (4209 bytes) */

// <0703B5C4> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 108
size_t ZSTD_RowFindBestMatch_dictMatchState_5_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_rotateRight_U64(const U64  value,
						U32 count);  // 33703
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3159, inline expansions: 13 (4406 bytes) */

// <07039465> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 108
size_t ZSTD_RowFindBestMatch_dictMatchState_6_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_rotateRight_U64(const U64  value,
						U32 count);  // 33703
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3159, inline expansions: 13 (4406 bytes) */

// <070372E2> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 108
size_t ZSTD_RowFindBestMatch_dictMatchState_4_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_rotateRight_U32(const U32  value,
						U32 count);  // 33701
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2951, inline expansions: 13 (4131 bytes) */

// <07035163> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 108
size_t ZSTD_RowFindBestMatch_dictMatchState_5_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_rotateRight_U32(const U32  value,
						U32 count);  // 33701
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3007, inline expansions: 13 (4225 bytes) */

// <07032FE4> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 108
size_t ZSTD_RowFindBestMatch_dictMatchState_6_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_rotateRight_U32(const U32  value,
						U32 count);  // 33701
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 3007, inline expansions: 13 (4225 bytes) */

// <07030EF9> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 106
size_t ZSTD_RowFindBestMatch_dictMatchState_4_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2887, inline expansions: 13 (3979 bytes) */

// <0702EDF6> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 106
size_t ZSTD_RowFindBestMatch_dictMatchState_5_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2895, inline expansions: 13 (3983 bytes) */

// <0702CCF3> thirdparty/zstd/zstd.c:34119
// variables: 60
// function calls: 106
size_t ZSTD_RowFindBestMatch_dictMatchState_6_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32* dmsHashTable; // 33895
		const BYTE* dmsTagTable; // 33896
		const U32  dmsHash; // 33897
		const U32  dmsRelRow; // 33898
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33897
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33902
	}
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	{
		const U32  dmsLowestIndex; // 33989
		const BYTE* dmsBase; // 33990
		const BYTE* dmsEnd; // 33991
		const U32  dmsSize; // 33992
		const U32  dmsIndexDelta; // 33993
		{
			const U32  headGrouped; // 33995
			U32 matchBuffer; // 33996
			size_t numMatches; // 33997
			size_t currMatch; // 33998
			ZSTD_VecMask matches; // 33999
			_mm_set_epi8(char __q15,
					char __q14,
					char __q13,
					char __q12,
					char __q11,
					char __q10,
					char __q09,
					char __q08,
					char __q07,
					char __q06,
					char __q05,
					char __q04,
					char __q03,
					char __q02,
					char __q01,
					char __q00);  // 660
			_mm_set1_epi8(char __A); // 33691
			{
				const __m128i  chunk; // 33696
				const __m128i  equalMask; // 33697
				_mm_loadu_si128(const __m128i_u* __P); // 33696
				_mm_cmpeq_epi8(__m128i __A,
						__m128i __B);  // 33697
				_mm_movemask_epi8(__m128i __A); // 33698
			}
			ZSTD_row_getSSEMask(int nbChunks,
						const BYTE* src,
						const BYTE  tag,
						const U32  head);  // 33771
			ZSTD_row_getMatchMask(const BYTE* tagRow,
						const BYTE  tag,
						const U32  headGrouped,
						const U32  rowEntries);  // 33999
			{
				const U32  matchPos; // 34002
				const U32  matchIndex; // 34003
				ZSTD_countTrailingZeros64(U64 val); // 33492
				ZSTD_VecMask_next(ZSTD_VecMask val); // 34002
			}
			{
				const U32  matchIndex; // 34014
				size_t currentMl; // 34015
				{
					const BYTE* match; // 34019
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20045
					{
						const size_t  diff; // 20014
						MEM_readST(const void* ptr); // 20014
						MEM_readST(const void* ptr); // 20014
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20015
					}
					{
						const size_t  diff; // 20018
						MEM_readST(const void* ptr); // 20018
						MEM_readST(const void* ptr); // 20018
						ZSTD_countTrailingZeros64(U64 val); // 2090
						ZSTD_NbCommonBytes(size_t val); // 20020
					}
					MEM_read32(const void* ptr); // 20023
					MEM_read32(const void* ptr); // 20023
					MEM_read16(const void* ptr); // 20024
					MEM_read16(const void* ptr); // 20024
					ZSTD_count(const BYTE* pIn,
							const BYTE* pMatch,
							const BYTE* pInLimit);  // 20038
					ZSTD_count_2segments(const BYTE* ip,
								const BYTE* match,
								const BYTE* iEnd,
								const BYTE* mEnd,
								const BYTE* iStart);  // 34022
					MEM_read32(const void* ptr); // 34021
					MEM_read32(const void* ptr); // 34021
				}
			}
		}
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 2895, inline expansions: 13 (3983 bytes) */

// <07028791> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 65
size_t ZSTD_RowFindBestMatch_noDict_4_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1798, inline expansions: 15 (2908 bytes) */

// <07027196> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 65
size_t ZSTD_RowFindBestMatch_noDict_5_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1938, inline expansions: 15 (3227 bytes) */

// <07025B9B> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 65
size_t ZSTD_RowFindBestMatch_noDict_6_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U64(const U64  value,
					U32 count);  // 33703
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1938, inline expansions: 15 (3227 bytes) */

// <070245EA> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 65
size_t ZSTD_RowFindBestMatch_noDict_4_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1664, inline expansions: 15 (2748 bytes) */

// <07023013> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 65
size_t ZSTD_RowFindBestMatch_noDict_5_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1815, inline expansions: 15 (3043 bytes) */

// <07021A3C> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 65
size_t ZSTD_RowFindBestMatch_noDict_6_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_loadu_si128(const __m128i_u* __P); // 33696
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
		}
		ZSTD_rotateRight_U32(const U32  value,
					U32 count);  // 33701
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1815, inline expansions: 15 (3043 bytes) */

// <0702047D> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 64
size_t ZSTD_RowFindBestMatch_noDict_4_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20060
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20060
	ZSTD_hash4PtrS(const void* ptr,
			U32 h,
			U32 s);  // 20110
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1642, inline expansions: 15 (2658 bytes) */

// <0701EEB0> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 64
size_t ZSTD_RowFindBestMatch_noDict_5_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20065
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20065
	ZSTD_hash5PtrS(const void* p,
			U32 h,
			U64 s);  // 20111
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1793, inline expansions: 15 (2977 bytes) */

// <0701D8E3> thirdparty/zstd/zstd.c:34119
// variables: 35
// function calls: 64
size_t ZSTD_RowFindBestMatch_noDict_6_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const U32  bound; // 33635
		{
			const U32  hash; // 33598
			const U32  relRow; // 33600
			const U32* row; // 33601
			BYTE* tagRow; // 33602
			const U32  pos; // 33603
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33572
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33574
			{
				const U32  hash; // 33575
			}
			ZSTD_row_nextCachedHash(U32* cache,
						const U32* hashTable,
						const BYTE* tagTable,
						const BYTE* base,
						U32 idx,
						const U32  hashLog,
						const U32  rowLog,
						const U32  mls,
						const U64  hashSalt);  // 33598
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33603
		}
		ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
						U32 updateStartIdx,
						const U32  updateEndIdx,
						const U32  mls,
						const U32  rowLog,
						const U32  rowMask,
						const U32  useCache);  // 33636
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 33638
	}
	{
		const U32  hash; // 33598
		const U32  relRow; // 33600
		const U32* row; // 33601
		BYTE* tagRow; // 33602
		const U32  pos; // 33603
		ZSTD_row_nextIndex(const BYTE* tagRow,
					const U32  rowMask);  // 33603
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33572
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33574
		{
			const U32  hash; // 33575
		}
		ZSTD_row_nextCachedHash(U32* cache,
					const U32* hashTable,
					const BYTE* tagTable,
					const BYTE* base,
					U32 idx,
					const U32  hashLog,
					const U32  rowLog,
					const U32  mls,
					const U64  hashSalt);  // 33598
	}
	ZSTD_row_update_internalImpl(ZSTD_MatchState_t* ms,
					U32 updateStartIdx,
					const U32  updateEndIdx,
					const U32  mls,
					const U32  rowLog,
					const U32  rowMask,
					const U32  useCache);  // 33642
	ZSTD_row_update_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const U32  mls,
				const U32  rowLog,
				const U32  rowMask,
				const U32  useCache);  // 33907
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33572
	ZSTD_row_prefetch(const U32* hashTable,
				const BYTE* tagTable,
				const U32  relRow,
				const U32  rowLog);  // 33574
	{
		const U32  hash; // 33575
	}
	ZSTD_row_nextCachedHash(U32* cache,
				const U32* hashTable,
				const BYTE* tagTable,
				const BYTE* base,
				U32 idx,
				const U32  hashLog,
				const U32  rowLog,
				const U32  mls,
				const U64  hashSalt);  // 33908
	{
		const U32  relRow; // 33919
		const U32  tag; // 33920
		const U32* row; // 33921
		BYTE* tagRow; // 33922
		const U32  headGrouped; // 33923
		U32 matchBuffer; // 33924
		size_t numMatches; // 33925
		size_t currMatch; // 33926
		ZSTD_VecMask matches; // 33927
		_mm_set_epi8(char __q15,
				char __q14,
				char __q13,
				char __q12,
				char __q11,
				char __q10,
				char __q09,
				char __q08,
				char __q07,
				char __q06,
				char __q05,
				char __q04,
				char __q03,
				char __q02,
				char __q01,
				char __q00);  // 660
		_mm_set1_epi8(char __A); // 33691
		{
			const __m128i  chunk; // 33696
			const __m128i  equalMask; // 33697
			_mm_cmpeq_epi8(__m128i __A,
					__m128i __B);  // 33697
			_mm_movemask_epi8(__m128i __A); // 33698
			_mm_loadu_si128(const __m128i_u* __P); // 33696
		}
		ZSTD_row_getSSEMask(int nbChunks,
					const BYTE* src,
					const BYTE  tag,
					const U32  head);  // 33771
		ZSTD_row_getMatchMask(const BYTE* tagRow,
					const BYTE  tag,
					const U32  headGrouped,
					const U32  rowEntries);  // 33927
		{
			const U32  matchPos; // 33931
			const U32  matchIndex; // 33932
			ZSTD_countTrailingZeros64(U64 val); // 33492
			ZSTD_VecMask_next(ZSTD_VecMask val); // 33931
		}
		{
			const U32  pos; // 33949
			ZSTD_row_nextIndex(const BYTE* tagRow,
						const U32  rowMask);  // 33949
		}
		{
			const U32  matchIndex; // 33956
			size_t currentMl; // 33957
			{
				const BYTE* match; // 33962
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 33966
				MEM_read32(const void* ptr); // 33965
				MEM_read32(const void* ptr); // 33965
			}
		}
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20070
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20070
	ZSTD_hash6PtrS(const void* p,
			U32 h,
			U64 s);  // 20112
	ZSTD_hashPtrSalted(const void* p,
				U32 hBits,
				U32 mls,
				const U64  hashSalt);  // 33913
	ZSTD_RowFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode,
				const U32  rowLog);  // 34119
} /* size: 1793, inline expansions: 15 (2977 bytes) */

// <06FA76B6> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_noDict_4_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA7671> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_noDict_4_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA762C> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_noDict_4_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA755D> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_noDict_5_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA748E> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_noDict_6_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA7449> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_extDict_4_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA7404> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_extDict_4_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA73BF> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_extDict_4_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA72F0> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_extDict_5_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA7221> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_extDict_6_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA71DC> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dictMatchState_4_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA7197> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dictMatchState_4_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA7152> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dictMatchState_4_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA7083> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dictMatchState_5_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6F6F> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_4_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6F2A> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_4_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6EE5> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_4_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6E16> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_5_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6D47> thirdparty/zstd/zstd.c:34119
size_t ZSTD_RowFindBestMatch_dedicatedDictSearch_6_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <07057E20> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_noDict_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 84, inline expansions: 1 (59 bytes) */

// <07057D05> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_noDict_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 84, inline expansions: 1 (59 bytes) */

// <07057BEA> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_noDict_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 84, inline expansions: 1 (59 bytes) */

// <07057ACF> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_extDict_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 87, inline expansions: 1 (62 bytes) */

// <070579B4> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_extDict_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 87, inline expansions: 1 (62 bytes) */

// <07057899> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_extDict_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 87, inline expansions: 1 (62 bytes) */

// <0705777E> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_dictMatchState_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 87, inline expansions: 1 (62 bytes) */

// <07057663> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_dictMatchState_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 87, inline expansions: 1 (62 bytes) */

// <07057548> thirdparty/zstd/zstd.c:34121
// function call: 1
size_t ZSTD_BtFindBestMatch_dictMatchState_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
	ZSTD_BtFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offBasePtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34121
} /* size: 87, inline expansions: 1 (62 bytes) */

// <06FA6D02> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_noDict_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6CBD> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_noDict_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6C78> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_noDict_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6C33> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_extDict_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6BEE> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_extDict_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6BA9> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_extDict_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6B64> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_dictMatchState_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6B1F> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_dictMatchState_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6ADA> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_dictMatchState_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6A95> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_dedicatedDictSearch_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6A50> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_dedicatedDictSearch_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <06FA6A0B> thirdparty/zstd/zstd.c:34121
size_t ZSTD_BtFindBestMatch_dedicatedDictSearch_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offBasePtr)
{
} /* size: 0 */

// <07055227> thirdparty/zstd/zstd.c:34123
// variables: 26
// function calls: 91
size_t ZSTD_HcFindBestMatch_dedicatedDictSearch_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33397
	{
		const U32* entry; // 33402
	}
	{
		const size_t  h; // 33347
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33437
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 2512, inline expansions: 13 (4360 bytes) */

// <07053AD6> thirdparty/zstd/zstd.c:34123
// variables: 26
// function calls: 91
size_t ZSTD_HcFindBestMatch_dedicatedDictSearch_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33397
	{
		const U32* entry; // 33402
	}
	{
		const size_t  h; // 33347
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33437
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 2583, inline expansions: 13 (4573 bytes) */

// <07052385> thirdparty/zstd/zstd.c:34123
// variables: 26
// function calls: 91
size_t ZSTD_HcFindBestMatch_dedicatedDictSearch_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33397
	{
		const U32* entry; // 33402
	}
	{
		const size_t  h; // 33347
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	{
		const U32  chainPackedPointer; // 33250
		const U32  chainIndex; // 33251
	}
	{
		size_t currentMl; // 33257
		const BYTE* match; // 33258
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 33272
		MEM_read32(const void* ptr); // 33270
		MEM_read32(const void* ptr); // 33270
	}
	{
		const U32  chainPackedPointer; // 33287
		U32 chainIndex; // 33288
		const U32  chainLength; // 33289
		const U32  chainAttempts; // 33290
		const U32  chainLimit; // 33291
		U32 chainAttempt; // 33292
		{
			size_t currentMl; // 33299
			const BYTE* match; // 33300
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33309
			MEM_read32(const void* ptr); // 33307
			MEM_read32(const void* ptr); // 33307
		}
	}
	ZSTD_dedicatedDictSearch_lazy_search(size_t* offsetPtr,
						size_t ml,
						U32 nbAttempts,
						const ZSTD_MatchState_t* dms,
						const BYTE* ip,
						const BYTE* iLimit,
						const BYTE* prefixStart,
						const U32  curr,
						const U32  dictLimit,
						const size_t  ddsIdx);  // 33437
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 2583, inline expansions: 13 (4573 bytes) */

// <07041A4E> thirdparty/zstd/zstd.c:34123
// variables: 10
// function calls: 56
size_t ZSTD_HcFindBestMatch_extDict_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
		{
			const BYTE* match; // 33418
			MEM_read32(const void* ptr); // 33420
			MEM_read32(const void* ptr); // 33420
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33421
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 1416, inline expansions: 7 (1585 bytes) */

// <07040C46> thirdparty/zstd/zstd.c:34123
// variables: 10
// function calls: 56
size_t ZSTD_HcFindBestMatch_extDict_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
		{
			const BYTE* match; // 33418
			MEM_read32(const void* ptr); // 33420
			MEM_read32(const void* ptr); // 33420
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33421
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 1495, inline expansions: 7 (1736 bytes) */

// <0703FE3E> thirdparty/zstd/zstd.c:34123
// variables: 10
// function calls: 56
size_t ZSTD_HcFindBestMatch_extDict_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
		{
			const BYTE* match; // 33418
			MEM_read32(const void* ptr); // 33420
			MEM_read32(const void* ptr); // 33420
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33421
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 1495, inline expansions: 7 (1736 bytes) */

// <0702BD14> thirdparty/zstd/zstd.c:34123
// variables: 20
// function calls: 61
size_t ZSTD_HcFindBestMatch_dictMatchState_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	{
		const U32* dmsChainTable; // 33440
		const U32  dmsChainSize; // 33441
		const U32  dmsChainMask; // 33442
		const U32  dmsLowestIndex; // 33443
		const BYTE* dmsBase; // 33444
		const BYTE* dmsEnd; // 33445
		const U32  dmsSize; // 33446
		const U32  dmsIndexDelta; // 33447
		const U32  dmsMinChain; // 33448
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33450
		{
			size_t currentMl; // 33453
			const BYTE* match; // 33454
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33457
			MEM_read32(const void* ptr); // 33456
			MEM_read32(const void* ptr); // 33456
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 1656, inline expansions: 7 (1852 bytes) */

// <0702AD31> thirdparty/zstd/zstd.c:34123
// variables: 20
// function calls: 61
size_t ZSTD_HcFindBestMatch_dictMatchState_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	{
		const U32* dmsChainTable; // 33440
		const U32  dmsChainSize; // 33441
		const U32  dmsChainMask; // 33442
		const U32  dmsLowestIndex; // 33443
		const BYTE* dmsBase; // 33444
		const BYTE* dmsEnd; // 33445
		const U32  dmsSize; // 33446
		const U32  dmsIndexDelta; // 33447
		const U32  dmsMinChain; // 33448
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33450
		{
			size_t currentMl; // 33453
			const BYTE* match; // 33454
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33457
			MEM_read32(const void* ptr); // 33456
			MEM_read32(const void* ptr); // 33456
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 1670, inline expansions: 7 (1917 bytes) */

// <07029D4E> thirdparty/zstd/zstd.c:34123
// variables: 20
// function calls: 61
size_t ZSTD_HcFindBestMatch_dictMatchState_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	{
		const U32* dmsChainTable; // 33440
		const U32  dmsChainSize; // 33441
		const U32  dmsChainMask; // 33442
		const U32  dmsLowestIndex; // 33443
		const BYTE* dmsBase; // 33444
		const BYTE* dmsEnd; // 33445
		const U32  dmsSize; // 33446
		const U32  dmsIndexDelta; // 33447
		const U32  dmsMinChain; // 33448
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33450
		{
			size_t currentMl; // 33453
			const BYTE* match; // 33454
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 33457
			MEM_read32(const void* ptr); // 33456
			MEM_read32(const void* ptr); // 33456
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 1670, inline expansions: 7 (1917 bytes) */

// <0701CFFF> thirdparty/zstd/zstd.c:34123
// variables: 5
// function calls: 27
size_t ZSTD_HcFindBestMatch_noDict_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 737, inline expansions: 7 (957 bytes) */

// <0701C831> thirdparty/zstd/zstd.c:34123
// variables: 5
// function calls: 27
size_t ZSTD_HcFindBestMatch_noDict_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 726, inline expansions: 7 (948 bytes) */

// <0701C063> thirdparty/zstd/zstd.c:34123
// variables: 5
// function calls: 27
size_t ZSTD_HcFindBestMatch_noDict_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
	{
		const size_t  h; // 33347
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 33347
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 33357
	ZSTD_insertAndFindFirstIndex_internal(ZSTD_MatchState_t* ms,
						const ZSTD_compressionParameters* cParams,
						const BYTE* ip,
						const U32  mls,
						const U32  lazySkipping);  // 33407
	{
		size_t currentMl; // 33410
		{
			const BYTE* match; // 33412
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 33416
			MEM_read32(const void* ptr); // 33415
			MEM_read32(const void* ptr); // 33415
		}
	}
	ZSTD_HcFindBestMatch(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iLimit,
				size_t* offsetPtr,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 34123
} /* size: 726, inline expansions: 7 (948 bytes) */

// <06FA69C6> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_noDict_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6981> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_noDict_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA693C> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_noDict_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA68F7> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_extDict_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA68B2> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_extDict_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA686D> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_extDict_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6828> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_dictMatchState_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA67E3> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_dictMatchState_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA679E> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_dictMatchState_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6759> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_dedicatedDictSearch_4(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6714> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_dedicatedDictSearch_5(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA66CF> thirdparty/zstd/zstd.c:34123
size_t ZSTD_HcFindBestMatch_dedicatedDictSearch_6(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iLimit, size_t* offsetPtr)
{
} /* size: 0 */

// <06FA6656> thirdparty/zstd/zstd.c:34188
inline size_t ZSTD_searchMax(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iend, size_t* offsetPtr, const U32 mls, const U32 rowLog, const searchMethod_e searchMethod, const ZSTD_dictMode_e dictMode)
{
} /* size: 0 */

// <06FA61F2> thirdparty/zstd/zstd.c:34217
// variables: 70
inline size_t ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const searchMethod_e searchMethod, const U32 depth, const ZSTD_dictMode_e dictMode)
{

_storeSequence: // 34423
	const BYTE* istart; // 34224
	const BYTE* ip; // 34225
	const BYTE* anchor; // 34226
	const BYTE* iend; // 34227
	const BYTE* ilimit; // 34228
	const BYTE* base; // 34229
	const U32  prefixLowestIndex; // 34230
	const BYTE* prefixLowest; // 34231
	const U32  mls; // 34232
	const U32  rowLog; // 34233
	U32 offset_1; // 34235
	U32 offset_2; // 34235
	U32 offsetSaved1; // 34236
	U32 offsetSaved2; // 34236
	const int  isDMS; // 34238
	const int  isDDS; // 34239
	const int  isDxS; // 34240
	const ZSTD_MatchState_t* dms; // 34241
	const U32  dictLowestIndex; // 34242
	const BYTE* dictBase; // 34243
	const BYTE* dictLowest; // 34244
	const BYTE* dictEnd; // 34245
	const U32  dictIndexDelta; // 34246
	const U32  dictAndPrefixLength; // 34249
	{
		const U32  curr; // 34254
		const U32  windowLow; // 34255
		const U32  maxRep; // 34256
	}
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			{
				const BYTE* repMatchEnd; // 34296
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const size_t  mlRep; // 34335
			const int  gain2; // 34336
			const int  gain1; // 34337
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
			}
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
		}
		{
			const size_t  mlRep; // 34371
			const int  gain2; // 34372
			const int  gain1; // 34373
		}
		{
			const U32  repIndex; // 34378
			const BYTE* repMatch; // 34379
			{
				const BYTE* repMatchEnd; // 34384
				const size_t  mlRep; // 34385
				const int  gain2; // 34386
				const int  gain1; // 34387
			}
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
		}
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			{
				const BYTE* repEnd2; // 34446
			}
		}
	}
} /* size: 0, variables: 24, goto labels: 1 */

// <06FA51F1> thirdparty/zstd/zstd.c:34485
// variables: 16
// function calls: 53
size_t ZSTD_compressBlock_greedy(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  curr; // 34254
		const U32  windowLow; // 34255
		const U32  maxRep; // 34256
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34255
	}
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34303
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		MEM_read32(const void* ptr); // 34460
		MEM_read32(const void* ptr); // 34460
		MEM_read32(const void* ptr); // 34302
		MEM_read32(const void* ptr); // 34302
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34462
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 34464
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34489
} /* size: 2097, goto labels: 1, inline expansions: 1 (2003 bytes) */

// <06FA4043> thirdparty/zstd/zstd.c:34492
// variables: 23
// function calls: 55
size_t ZSTD_compressBlock_greedy_dictMatchState(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34297
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34496
} /* size: 1957, goto labels: 1, inline expansions: 1 (1859 bytes) */

// <06FA2E95> thirdparty/zstd/zstd.c:34499
// variables: 23
// function calls: 55
size_t ZSTD_compressBlock_greedy_dedicatedDictSearch(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34297
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34503
} /* size: 1957, goto labels: 1, inline expansions: 1 (1859 bytes) */

// <06FA165A> thirdparty/zstd/zstd.c:34506
// variables: 20
// function calls: 83
size_t ZSTD_compressBlock_greedy_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  curr; // 34254
		const U32  windowLow; // 34255
		const U32  maxRep; // 34256
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34255
	}
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		MEM_read32(const void* ptr); // 34302
		MEM_read32(const void* ptr); // 34302
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34303
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		MEM_read32(const void* ptr); // 34460
		MEM_read32(const void* ptr); // 34460
		{
			const size_t  step; // 34315
		}
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34462
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 34464
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34510
} /* size: 2748, goto labels: 1, inline expansions: 2 (2957 bytes) */

// <06FA0236> thirdparty/zstd/zstd.c:34513
// variables: 23
// function calls: 58
size_t ZSTD_compressBlock_greedy_dictMatchState_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34517
} /* size: 2300, goto labels: 1, inline expansions: 2 (2491 bytes) */

// <06F9EE12> thirdparty/zstd/zstd.c:34520
// variables: 23
// function calls: 58
size_t ZSTD_compressBlock_greedy_dedicatedDictSearch_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34524
} /* size: 2300, goto labels: 1, inline expansions: 2 (2491 bytes) */

// <06F9D90C> thirdparty/zstd/zstd.c:34529
// variables: 25
// function calls: 75
size_t ZSTD_compressBlock_lazy(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  curr; // 34254
		const U32  windowLow; // 34255
		const U32  maxRep; // 34256
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34255
	}
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		MEM_read32(const void* ptr); // 34302
		MEM_read32(const void* ptr); // 34302
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34303
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const size_t  mlRep; // 34335
			const int  gain2; // 34336
			const int  gain1; // 34337
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 34335
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34337
		}
		MEM_read32(const void* ptr); // 34334
		MEM_read32(const void* ptr); // 34334
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		MEM_read32(const void* ptr); // 34460
		MEM_read32(const void* ptr); // 34460
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34462
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 34464
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34533
} /* size: 2815, goto labels: 1, inline expansions: 1 (2719 bytes) */

// <06F9B9AA> thirdparty/zstd/zstd.c:34536
// variables: 41
// function calls: 119
size_t ZSTD_compressBlock_lazy_dictMatchState(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34297
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34349
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34447
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34540
} /* size: 3579, goto labels: 1, inline expansions: 1 (3481 bytes) */

// <06F999FA> thirdparty/zstd/zstd.c:34543
// variables: 41
// function calls: 119
size_t ZSTD_compressBlock_lazy_dedicatedDictSearch(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34297
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34349
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34447
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34547
} /* size: 3731, goto labels: 1, inline expansions: 1 (3631 bytes) */

// <06F97C52> thirdparty/zstd/zstd.c:34550
// variables: 29
// function calls: 105
size_t ZSTD_compressBlock_lazy_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  curr; // 34254
		const U32  windowLow; // 34255
		const U32  maxRep; // 34256
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34255
	}
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		MEM_read32(const void* ptr); // 34302
		MEM_read32(const void* ptr); // 34302
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34303
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const size_t  mlRep; // 34335
			const int  gain2; // 34336
			const int  gain1; // 34337
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 34335
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34337
		}
		MEM_read32(const void* ptr); // 34334
		MEM_read32(const void* ptr); // 34334
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		MEM_read32(const void* ptr); // 34460
		MEM_read32(const void* ptr); // 34460
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34462
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 34464
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34554
} /* size: 3809, goto labels: 1, inline expansions: 2 (3971 bytes) */

// <06F96578> thirdparty/zstd/zstd.c:34557
// variables: 33
// function calls: 68
size_t ZSTD_compressBlock_lazy_dictMatchState_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34561
} /* size: 2861, goto labels: 1, inline expansions: 2 (3041 bytes) */

// <06F94E9E> thirdparty/zstd/zstd.c:34564
// variables: 33
// function calls: 68
size_t ZSTD_compressBlock_lazy_dedicatedDictSearch_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34568
} /* size: 2861, goto labels: 1, inline expansions: 2 (3041 bytes) */

// <06F933E2> thirdparty/zstd/zstd.c:34573
// variables: 34
// function calls: 95
size_t ZSTD_compressBlock_lazy2(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  curr; // 34254
		const U32  windowLow; // 34255
		const U32  maxRep; // 34256
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34255
	}
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		MEM_read32(const void* ptr); // 34302
		MEM_read32(const void* ptr); // 34302
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34303
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const size_t  mlRep; // 34335
			const int  gain2; // 34336
			const int  gain1; // 34337
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 34335
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34337
		}
		MEM_read32(const void* ptr); // 34334
		MEM_read32(const void* ptr); // 34334
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		MEM_read32(const void* ptr); // 34370
		MEM_read32(const void* ptr); // 34370
		{
			const size_t  mlRep; // 34371
			const int  gain2; // 34372
			const int  gain1; // 34373
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 34371
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34393
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34394
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34395
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		MEM_read32(const void* ptr); // 34460
		MEM_read32(const void* ptr); // 34460
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34462
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 34464
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34577
} /* size: 3387, goto labels: 1, inline expansions: 1 (3289 bytes) */

// <06F91612> thirdparty/zstd/zstd.c:34580
// variables: 47
// function calls: 100
size_t ZSTD_compressBlock_lazy2_dictMatchState(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34297
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34349
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const U32  repIndex; // 34378
			const BYTE* repMatch; // 34379
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34382
			MEM_read32(const void* ptr); // 34383
			MEM_read32(const void* ptr); // 34383
			{
				const BYTE* repMatchEnd; // 34384
				const size_t  mlRep; // 34385
				const int  gain2; // 34386
				const int  gain1; // 34387
			}
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34393
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34394
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34395
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34584
} /* size: 3416, goto labels: 1, inline expansions: 1 (3295 bytes) */

// <06F8F822> thirdparty/zstd/zstd.c:34587
// variables: 47
// function calls: 100
size_t ZSTD_compressBlock_lazy2_dedicatedDictSearch(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34297
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34349
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const U32  repIndex; // 34378
			const BYTE* repMatch; // 34379
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34382
			MEM_read32(const void* ptr); // 34383
			MEM_read32(const void* ptr); // 34383
			{
				const BYTE* repMatchEnd; // 34384
				const size_t  mlRep; // 34385
				const int  gain2; // 34386
				const int  gain1; // 34387
			}
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34393
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34394
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34395
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34591
} /* size: 3448, goto labels: 1, inline expansions: 1 (3332 bytes) */

// <06F8D462> thirdparty/zstd/zstd.c:34594
// variables: 38
// function calls: 125
size_t ZSTD_compressBlock_lazy2_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  curr; // 34254
		const U32  windowLow; // 34255
		const U32  maxRep; // 34256
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34255
	}
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		MEM_read32(const void* ptr); // 34302
		MEM_read32(const void* ptr); // 34302
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34303
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const size_t  mlRep; // 34335
			const int  gain2; // 34336
			const int  gain1; // 34337
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 34335
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34337
		}
		MEM_read32(const void* ptr); // 34334
		MEM_read32(const void* ptr); // 34334
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		MEM_read32(const void* ptr); // 34370
		MEM_read32(const void* ptr); // 34370
		{
			const size_t  mlRep; // 34371
			const int  gain2; // 34372
			const int  gain1; // 34373
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 34371
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34393
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34394
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34395
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		MEM_read32(const void* ptr); // 34460
		MEM_read32(const void* ptr); // 34460
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34462
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 34464
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34598
} /* size: 4601, goto labels: 1, inline expansions: 2 (4796 bytes) */

// <06F8B886> thirdparty/zstd/zstd.c:34601
// variables: 43
// function calls: 76
size_t ZSTD_compressBlock_lazy2_dictMatchState_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34393
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34394
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34395
		}
		{
			const U32  repIndex; // 34378
			const BYTE* repMatch; // 34379
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34382
			MEM_read32(const void* ptr); // 34383
			MEM_read32(const void* ptr); // 34383
			{
				const BYTE* repMatchEnd; // 34384
				const size_t  mlRep; // 34385
				const int  gain2; // 34386
				const int  gain1; // 34387
			}
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34605
} /* size: 3648, goto labels: 1, inline expansions: 2 (3806 bytes) */

// <06F89CAA> thirdparty/zstd/zstd.c:34608
// variables: 43
// function calls: 76
size_t ZSTD_compressBlock_lazy2_dedicatedDictSearch_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34271
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34393
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34394
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34395
		}
		{
			const U32  repIndex; // 34378
			const BYTE* repMatch; // 34379
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34382
			MEM_read32(const void* ptr); // 34383
			MEM_read32(const void* ptr); // 34383
			{
				const BYTE* repMatchEnd; // 34384
				const size_t  mlRep; // 34385
				const int  gain2; // 34386
				const int  gain1; // 34387
			}
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34431
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34612
} /* size: 3648, goto labels: 1, inline expansions: 2 (3806 bytes) */

// <06F8823A> thirdparty/zstd/zstd.c:34617
// variables: 34
// function calls: 95
size_t ZSTD_compressBlock_btlazy2(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		const U32  curr; // 34254
		const U32  windowLow; // 34255
		const U32  maxRep; // 34256
		ZSTD_getLowestPrefixIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34255
	}
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		MEM_read32(const void* ptr); // 34302
		MEM_read32(const void* ptr); // 34302
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34303
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const size_t  mlRep; // 34335
			const int  gain2; // 34336
			const int  gain1; // 34337
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 34335
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34337
		}
		MEM_read32(const void* ptr); // 34334
		MEM_read32(const void* ptr); // 34334
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		MEM_read32(const void* ptr); // 34370
		MEM_read32(const void* ptr); // 34370
		{
			const size_t  mlRep; // 34371
			const int  gain2; // 34372
			const int  gain1; // 34373
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 34371
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34393
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34394
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34395
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		MEM_read32(const void* ptr); // 34460
		MEM_read32(const void* ptr); // 34460
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 34462
		_mm_loadu_si128(const __m128i_u* __P); // 15683
		_mm_storeu_si128(__m128i_u* __P,
				__m128i __B);  // 15683
		ZSTD_copy16(void* dst,
				const void* src);  // 19955
		{
			const size_t  mlBase; // 19911
		}
		ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
					size_t litLength,
					U32 offBase,
					size_t matchLength);  // 19964
		ZSTD_storeSeq(SeqStore_t* seqStorePtr,
				size_t litLength,
				const BYTE* literals,
				const BYTE* litLimit,
				U32 offBase,
				size_t matchLength);  // 34464
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34621
} /* size: 3355, goto labels: 1, inline expansions: 1 (3257 bytes) */

// <06F86460> thirdparty/zstd/zstd.c:34624
// variables: 47
// function calls: 100
size_t ZSTD_compressBlock_btlazy2_dictMatchState(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34423
	{
		size_t matchLength; // 34282
		size_t offBase; // 34283
		const BYTE* start; // 34284
		{
			const U32  repIndex; // 34289
			const BYTE* repMatch; // 34290
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34294
			MEM_read32(const void* ptr); // 34295
			MEM_read32(const void* ptr); // 34295
			{
				const BYTE* repMatchEnd; // 34296
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34297
			}
		}
		{
			size_t offbaseFound; // 34308
			const size_t  ml2; // 34309
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34309
		}
		{
			const size_t  step; // 34315
		}
		{
			const U32  repIndex; // 34342
			const BYTE* repMatch; // 34343
			{
				const BYTE* repMatchEnd; // 34348
				const size_t  mlRep; // 34349
				const int  gain2; // 34350
				const int  gain1; // 34351
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34349
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34351
			}
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34346
			MEM_read32(const void* ptr); // 34347
			MEM_read32(const void* ptr); // 34347
		}
		{
			size_t ofbCandidate; // 34356
			const size_t  ml2; // 34357
			const int  gain2; // 34358
			const int  gain1; // 34359
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34357
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34358
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34359
		}
		{
			const U32  repIndex; // 34378
			const BYTE* repMatch; // 34379
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34382
			MEM_read32(const void* ptr); // 34383
			MEM_read32(const void* ptr); // 34383
			{
				const BYTE* repMatchEnd; // 34384
				const size_t  mlRep; // 34385
				const int  gain2; // 34386
				const int  gain1; // 34387
			}
		}
		{
			size_t ofbCandidate; // 34392
			const size_t  ml2; // 34393
			const int  gain2; // 34394
			const int  gain1; // 34395
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34393
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34394
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34395
		}
		{
			const U32  matchIndex; // 34415
			const BYTE* match; // 34416
			const BYTE* mStart; // 34417
		}
		{
			const size_t  litLength; // 34424
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34425
		}
		{
			const U32  current2; // 34439
			const U32  repIndex; // 34440
			const BYTE* repMatch; // 34441
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34444
			MEM_read32(const void* ptr); // 34445
			MEM_read32(const void* ptr); // 34445
			{
				const BYTE* repEnd2; // 34446
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34449
			}
		}
	}
	ZSTD_compressBlock_lazy_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const searchMethod_e  searchMethod,
					const U32  depth,
					const ZSTD_dictMode_e  dictMode);  // 34628
} /* size: 3480, goto labels: 1, inline expansions: 1 (3364 bytes) */

// <06F86083> thirdparty/zstd/zstd.c:34638
// variables: 62
inline size_t ZSTD_compressBlock_lazy_extDict_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const searchMethod_e searchMethod, const U32 depth)
{

_storeSequence: // 34797
	const BYTE* istart; // 34644
	const BYTE* ip; // 34645
	const BYTE* anchor; // 34646
	const BYTE* iend; // 34647
	const BYTE* ilimit; // 34648
	const BYTE* base; // 34649
	const U32  dictLimit; // 34650
	const BYTE* prefixStart; // 34651
	const BYTE* dictBase; // 34652
	const BYTE* dictEnd; // 34653
	const BYTE* dictStart; // 34654
	const U32  windowLog; // 34655
	const U32  mls; // 34656
	const U32  rowLog; // 34657
	U32 offset_1; // 34659
	U32 offset_2; // 34659
	{
		size_t matchLength; // 34680
		size_t offBase; // 34681
		const BYTE* start; // 34682
		U32 curr; // 34683
		{
			const U32  windowLow; // 34686
			const U32  repIndex; // 34687
			const BYTE* repBase; // 34688
			const BYTE* repMatch; // 34689
			{
				const BYTE* repEnd; // 34694
			}
		}
		{
			size_t ofbCandidate; // 34700
			const size_t  ml2; // 34701
		}
		{
			const size_t  step; // 34707
		}
		{
			const U32  windowLow; // 34727
			const U32  repIndex; // 34728
			const BYTE* repBase; // 34729
			const BYTE* repMatch; // 34730
			{
				const BYTE* repEnd; // 34735
				const size_t  repLength; // 34736
				const int  gain2; // 34737
				const int  gain1; // 34738
			}
		}
		{
			size_t ofbCandidate; // 34744
			const size_t  ml2; // 34745
			const int  gain2; // 34746
			const int  gain1; // 34747
		}
		{
			const U32  windowLow; // 34759
			const U32  repIndex; // 34760
			const BYTE* repBase; // 34761
			const BYTE* repMatch; // 34762
			{
				const BYTE* repEnd; // 34767
				const size_t  repLength; // 34768
				const int  gain2; // 34769
				const int  gain1; // 34770
			}
		}
		{
			size_t ofbCandidate; // 34776
			const size_t  ml2; // 34777
			const int  gain2; // 34778
			const int  gain1; // 34779
		}
		{
			const U32  matchIndex; // 34789
			const BYTE* match; // 34790
			const BYTE* mStart; // 34791
		}
		{
			const size_t  litLength; // 34798
		}
		{
			const U32  repCurrent; // 34812
			const U32  windowLow; // 34813
			const U32  repIndex; // 34814
			const BYTE* repBase; // 34815
			const BYTE* repMatch; // 34816
			{
				const BYTE* repEnd; // 34821
			}
		}
	}
} /* size: 0, variables: 16, goto labels: 1 */

// <06F84E5E> thirdparty/zstd/zstd.c:34842
// variables: 28
// function calls: 57
size_t ZSTD_compressBlock_greedy_extDict(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34797
	{
		size_t matchLength; // 34680
		size_t offBase; // 34681
		const BYTE* start; // 34682
		U32 curr; // 34683
		{
			const U32  windowLow; // 34686
			const U32  repIndex; // 34687
			const BYTE* repBase; // 34688
			const BYTE* repMatch; // 34689
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34686
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34690
			MEM_read32(const void* ptr); // 34692
			MEM_read32(const void* ptr); // 34692
			{
				const BYTE* repEnd; // 34694
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34695
			}
		}
		{
			size_t ofbCandidate; // 34700
			const size_t  ml2; // 34701
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34701
		}
		{
			const size_t  step; // 34707
		}
		{
			const U32  matchIndex; // 34789
			const BYTE* match; // 34790
			const BYTE* mStart; // 34791
		}
		{
			const size_t  litLength; // 34798
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34799
		}
		{
			const U32  repCurrent; // 34812
			const U32  windowLow; // 34813
			const U32  repIndex; // 34814
			const BYTE* repBase; // 34815
			const BYTE* repMatch; // 34816
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34813
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34817
			MEM_read32(const void* ptr); // 34819
			MEM_read32(const void* ptr); // 34819
			{
				const BYTE* repEnd; // 34821
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34824
			}
		}
	}
	ZSTD_compressBlock_lazy_extDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const searchMethod_e  searchMethod,
						const U32  depth);  // 34846
} /* size: 1972, goto labels: 1, inline expansions: 1 (1879 bytes) */

// <06F83967> thirdparty/zstd/zstd.c:34849
// variables: 28
// function calls: 60
size_t ZSTD_compressBlock_greedy_extDict_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34797
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34669
	{
		size_t matchLength; // 34680
		size_t offBase; // 34681
		const BYTE* start; // 34682
		U32 curr; // 34683
		{
			const U32  windowLow; // 34686
			const U32  repIndex; // 34687
			const BYTE* repBase; // 34688
			const BYTE* repMatch; // 34689
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34686
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34690
			MEM_read32(const void* ptr); // 34692
			MEM_read32(const void* ptr); // 34692
			{
				const BYTE* repEnd; // 34694
			}
		}
		{
			size_t ofbCandidate; // 34700
			const size_t  ml2; // 34701
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34701
		}
		{
			const size_t  step; // 34707
		}
		{
			const size_t  litLength; // 34798
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34799
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34805
		{
			const U32  repCurrent; // 34812
			const U32  windowLow; // 34813
			const U32  repIndex; // 34814
			const BYTE* repBase; // 34815
			const BYTE* repMatch; // 34816
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34813
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34817
			MEM_read32(const void* ptr); // 34819
			MEM_read32(const void* ptr); // 34819
			{
				const BYTE* repEnd; // 34821
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34824
			}
		}
		{
			const U32  matchIndex; // 34789
			const BYTE* match; // 34790
			const BYTE* mStart; // 34791
		}
	}
	ZSTD_compressBlock_lazy_extDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const searchMethod_e  searchMethod,
						const U32  depth);  // 34853
} /* size: 2293, goto labels: 1, inline expansions: 2 (2480 bytes) */

// <06F823C7> thirdparty/zstd/zstd.c:34858
// variables: 40
// function calls: 68
size_t ZSTD_compressBlock_lazy_extDict(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34797
	{
		size_t matchLength; // 34680
		size_t offBase; // 34681
		const BYTE* start; // 34682
		U32 curr; // 34683
		{
			const U32  windowLow; // 34686
			const U32  repIndex; // 34687
			const BYTE* repBase; // 34688
			const BYTE* repMatch; // 34689
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34686
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34690
			MEM_read32(const void* ptr); // 34692
			MEM_read32(const void* ptr); // 34692
			{
				const BYTE* repEnd; // 34694
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20038
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 20045
				ZSTD_count_2segments(const BYTE* ip,
							const BYTE* match,
							const BYTE* iEnd,
							const BYTE* mEnd,
							const BYTE* iStart);  // 34695
			}
		}
		{
			const size_t  step; // 34707
		}
		{
			size_t ofbCandidate; // 34700
			const size_t  ml2; // 34701
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34701
		}
		{
			const U32  windowLow; // 34727
			const U32  repIndex; // 34728
			const BYTE* repBase; // 34729
			const BYTE* repMatch; // 34730
			{
				const BYTE* repEnd; // 34735
				const size_t  repLength; // 34736
				const int  gain2; // 34737
				const int  gain1; // 34738
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34738
			}
			MEM_read32(const void* ptr); // 34733
			MEM_read32(const void* ptr); // 34733
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34731
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34727
		}
		{
			size_t ofbCandidate; // 34744
			const size_t  ml2; // 34745
			const int  gain2; // 34746
			const int  gain1; // 34747
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34745
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34746
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34747
		}
		{
			const U32  matchIndex; // 34789
			const BYTE* match; // 34790
			const BYTE* mStart; // 34791
		}
		{
			const size_t  litLength; // 34798
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34799
		}
		{
			const U32  repCurrent; // 34812
			const U32  windowLow; // 34813
			const U32  repIndex; // 34814
			const BYTE* repBase; // 34815
			const BYTE* repMatch; // 34816
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34813
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34817
			MEM_read32(const void* ptr); // 34819
			MEM_read32(const void* ptr); // 34819
			{
				const BYTE* repEnd; // 34821
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34824
			}
		}
	}
	ZSTD_compressBlock_lazy_extDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const searchMethod_e  searchMethod,
						const U32  depth);  // 34863
} /* size: 2677, goto labels: 1, inline expansions: 1 (2576 bytes) */

// <06F80B7D> thirdparty/zstd/zstd.c:34866
// variables: 40
// function calls: 71
size_t ZSTD_compressBlock_lazy_extDict_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34797
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34669
	{
		size_t matchLength; // 34680
		size_t offBase; // 34681
		const BYTE* start; // 34682
		U32 curr; // 34683
		{
			const U32  windowLow; // 34686
			const U32  repIndex; // 34687
			const BYTE* repBase; // 34688
			const BYTE* repMatch; // 34689
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34686
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34690
			MEM_read32(const void* ptr); // 34692
			MEM_read32(const void* ptr); // 34692
			{
				const BYTE* repEnd; // 34694
			}
		}
		{
			size_t ofbCandidate; // 34700
			const size_t  ml2; // 34701
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34701
		}
		{
			const size_t  step; // 34707
		}
		{
			const U32  windowLow; // 34727
			const U32  repIndex; // 34728
			const BYTE* repBase; // 34729
			const BYTE* repMatch; // 34730
			{
				const BYTE* repEnd; // 34735
				const size_t  repLength; // 34736
				const int  gain2; // 34737
				const int  gain1; // 34738
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34738
			}
			MEM_read32(const void* ptr); // 34733
			MEM_read32(const void* ptr); // 34733
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34731
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34727
		}
		{
			size_t ofbCandidate; // 34744
			const size_t  ml2; // 34745
			const int  gain2; // 34746
			const int  gain1; // 34747
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34745
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34746
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34747
		}
		{
			const U32  matchIndex; // 34789
			const BYTE* match; // 34790
			const BYTE* mStart; // 34791
		}
		{
			const size_t  litLength; // 34798
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34799
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34805
		{
			const U32  repCurrent; // 34812
			const U32  windowLow; // 34813
			const U32  repIndex; // 34814
			const BYTE* repBase; // 34815
			const BYTE* repMatch; // 34816
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34813
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34817
			MEM_read32(const void* ptr); // 34819
			MEM_read32(const void* ptr); // 34819
			{
				const BYTE* repEnd; // 34821
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34824
			}
		}
	}
	ZSTD_compressBlock_lazy_extDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const searchMethod_e  searchMethod,
						const U32  depth);  // 34871
} /* size: 3102, goto labels: 1, inline expansions: 2 (3279 bytes) */

// <06F7F6F5> thirdparty/zstd/zstd.c:34876
// variables: 48
// function calls: 50
size_t ZSTD_compressBlock_lazy2_extDict(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34797
	{
		size_t matchLength; // 34680
		size_t offBase; // 34681
		const BYTE* start; // 34682
		U32 curr; // 34683
		{
			const U32  windowLow; // 34686
			const U32  repIndex; // 34687
			const BYTE* repBase; // 34688
			const BYTE* repMatch; // 34689
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34686
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34690
			MEM_read32(const void* ptr); // 34692
			MEM_read32(const void* ptr); // 34692
			{
				const BYTE* repEnd; // 34694
			}
		}
		{
			const size_t  step; // 34707
		}
		{
			size_t ofbCandidate; // 34700
			const size_t  ml2; // 34701
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34701
		}
		{
			const U32  windowLow; // 34727
			const U32  repIndex; // 34728
			const BYTE* repBase; // 34729
			const BYTE* repMatch; // 34730
			{
				const BYTE* repEnd; // 34735
				const size_t  repLength; // 34736
				const int  gain2; // 34737
				const int  gain1; // 34738
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34738
			}
			MEM_read32(const void* ptr); // 34733
			MEM_read32(const void* ptr); // 34733
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34731
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34727
		}
		{
			size_t ofbCandidate; // 34744
			const size_t  ml2; // 34745
			const int  gain2; // 34746
			const int  gain1; // 34747
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34745
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34746
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34747
		}
		{
			const U32  windowLow; // 34759
			const U32  repIndex; // 34760
			const BYTE* repBase; // 34761
			const BYTE* repMatch; // 34762
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34759
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34763
			MEM_read32(const void* ptr); // 34765
			MEM_read32(const void* ptr); // 34765
			{
				const BYTE* repEnd; // 34767
				const size_t  repLength; // 34768
				const int  gain2; // 34769
				const int  gain1; // 34770
			}
		}
		{
			size_t ofbCandidate; // 34776
			const size_t  ml2; // 34777
			const int  gain2; // 34778
			const int  gain1; // 34779
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34777
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34778
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34779
		}
		{
			const U32  matchIndex; // 34789
			const BYTE* match; // 34790
			const BYTE* mStart; // 34791
		}
		{
			const size_t  litLength; // 34798
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34799
		}
		{
			const U32  repCurrent; // 34812
			const U32  windowLow; // 34813
			const U32  repIndex; // 34814
			const BYTE* repBase; // 34815
			const BYTE* repMatch; // 34816
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34813
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34817
			MEM_read32(const void* ptr); // 34819
			MEM_read32(const void* ptr); // 34819
			{
				const BYTE* repEnd; // 34821
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34824
			}
		}
	}
	ZSTD_compressBlock_lazy_extDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const searchMethod_e  searchMethod,
						const U32  depth);  // 34881
} /* size: 2609, goto labels: 1, inline expansions: 1 (2512 bytes) */

// <06F7D956> thirdparty/zstd/zstd.c:34884
// variables: 52
// function calls: 80
size_t ZSTD_compressBlock_lazy2_extDict_row(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34797
	{
		const U32  hash; // 33549
		const U32  row; // 33550
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20060
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20060
		ZSTD_hash4PtrS(const void* ptr,
				U32 h,
				U32 s);  // 20110
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20065
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20065
		ZSTD_hash5PtrS(const void* p,
				U32 h,
				U64 s);  // 20111
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20070
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20070
		ZSTD_hash6PtrS(const void* p,
				U32 h,
				U64 s);  // 20112
		ZSTD_hashPtrSalted(const void* p,
					U32 hBits,
					U32 mls,
					const U64  hashSalt);  // 33549
		ZSTD_row_prefetch(const U32* hashTable,
					const BYTE* tagTable,
					const U32  relRow,
					const U32  rowLog);  // 33551
	}
	ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
				const BYTE* base,
				const U32  rowLog,
				const U32  mls,
				U32 idx,
				const BYTE* iLimit);  // 34669
	{
		size_t matchLength; // 34680
		size_t offBase; // 34681
		const BYTE* start; // 34682
		U32 curr; // 34683
		{
			const U32  windowLow; // 34686
			const U32  repIndex; // 34687
			const BYTE* repBase; // 34688
			const BYTE* repMatch; // 34689
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34686
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34690
			MEM_read32(const void* ptr); // 34692
			MEM_read32(const void* ptr); // 34692
			{
				const BYTE* repEnd; // 34694
			}
		}
		{
			size_t ofbCandidate; // 34700
			const size_t  ml2; // 34701
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34701
		}
		{
			const size_t  step; // 34707
		}
		{
			const U32  windowLow; // 34727
			const U32  repIndex; // 34728
			const BYTE* repBase; // 34729
			const BYTE* repMatch; // 34730
			{
				const BYTE* repEnd; // 34735
				const size_t  repLength; // 34736
				const int  gain2; // 34737
				const int  gain1; // 34738
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34738
			}
			MEM_read32(const void* ptr); // 34733
			MEM_read32(const void* ptr); // 34733
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34731
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34727
		}
		{
			size_t ofbCandidate; // 34744
			const size_t  ml2; // 34745
			const int  gain2; // 34746
			const int  gain1; // 34747
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34745
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34746
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34747
		}
		{
			const U32  windowLow; // 34759
			const U32  repIndex; // 34760
			const BYTE* repBase; // 34761
			const BYTE* repMatch; // 34762
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34759
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34763
			MEM_read32(const void* ptr); // 34765
			MEM_read32(const void* ptr); // 34765
			{
				const BYTE* repEnd; // 34767
				const size_t  repLength; // 34768
				const int  gain2; // 34769
				const int  gain1; // 34770
			}
		}
		{
			size_t ofbCandidate; // 34776
			const size_t  ml2; // 34777
			const int  gain2; // 34778
			const int  gain1; // 34779
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34777
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34778
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34779
		}
		{
			const U32  matchIndex; // 34789
			const BYTE* match; // 34790
			const BYTE* mStart; // 34791
		}
		{
			const size_t  litLength; // 34798
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34799
		}
		{
			const U32  hash; // 33549
			const U32  row; // 33550
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 20060
			ZSTD_hash4(U32 u,
					U32 h,
					U32 s);  // 20060
			ZSTD_hash4PtrS(const void* ptr,
					U32 h,
					U32 s);  // 20110
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20070
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20070
			ZSTD_hash6PtrS(const void* p,
					U32 h,
					U64 s);  // 20112
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20065
			ZSTD_hash5(U64 u,
					U32 h,
					U64 s);  // 20065
			ZSTD_hash5PtrS(const void* p,
					U32 h,
					U64 s);  // 20111
			ZSTD_hashPtrSalted(const void* p,
						U32 hBits,
						U32 mls,
						const U64  hashSalt);  // 33549
			ZSTD_row_prefetch(const U32* hashTable,
						const BYTE* tagTable,
						const U32  relRow,
						const U32  rowLog);  // 33551
		}
		ZSTD_row_fillHashCache(ZSTD_MatchState_t* ms,
					const BYTE* base,
					const U32  rowLog,
					const U32  mls,
					U32 idx,
					const BYTE* iLimit);  // 34805
		{
			const U32  repCurrent; // 34812
			const U32  windowLow; // 34813
			const U32  repIndex; // 34814
			const BYTE* repBase; // 34815
			const BYTE* repMatch; // 34816
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34813
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34817
			MEM_read32(const void* ptr); // 34819
			MEM_read32(const void* ptr); // 34819
			{
				const BYTE* repEnd; // 34821
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34824
			}
		}
	}
	ZSTD_compressBlock_lazy_extDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const searchMethod_e  searchMethod,
						const U32  depth);  // 34888
} /* size: 3918, goto labels: 1, inline expansions: 2 (4090 bytes) */

// <06F7C4CE> thirdparty/zstd/zstd.c:34893
// variables: 48
// function calls: 50
size_t ZSTD_compressBlock_btlazy2_extDict(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{

_storeSequence: // 34797
	{
		size_t matchLength; // 34680
		size_t offBase; // 34681
		const BYTE* start; // 34682
		U32 curr; // 34683
		{
			const U32  windowLow; // 34686
			const U32  repIndex; // 34687
			const BYTE* repBase; // 34688
			const BYTE* repMatch; // 34689
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34686
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34690
			MEM_read32(const void* ptr); // 34692
			MEM_read32(const void* ptr); // 34692
			{
				const BYTE* repEnd; // 34694
			}
		}
		{
			const size_t  step; // 34707
		}
		{
			size_t ofbCandidate; // 34700
			const size_t  ml2; // 34701
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34701
		}
		{
			const U32  windowLow; // 34727
			const U32  repIndex; // 34728
			const BYTE* repBase; // 34729
			const BYTE* repMatch; // 34730
			{
				const BYTE* repEnd; // 34735
				const size_t  repLength; // 34736
				const int  gain2; // 34737
				const int  gain1; // 34738
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 34738
			}
			MEM_read32(const void* ptr); // 34733
			MEM_read32(const void* ptr); // 34733
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34731
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34727
		}
		{
			size_t ofbCandidate; // 34744
			const size_t  ml2; // 34745
			const int  gain2; // 34746
			const int  gain1; // 34747
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34745
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34746
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34747
		}
		{
			const U32  windowLow; // 34759
			const U32  repIndex; // 34760
			const BYTE* repBase; // 34761
			const BYTE* repMatch; // 34762
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34759
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34763
			MEM_read32(const void* ptr); // 34765
			MEM_read32(const void* ptr); // 34765
			{
				const BYTE* repEnd; // 34767
				const size_t  repLength; // 34768
				const int  gain2; // 34769
				const int  gain1; // 34770
			}
		}
		{
			size_t ofbCandidate; // 34776
			const size_t  ml2; // 34777
			const int  gain2; // 34778
			const int  gain1; // 34779
			ZSTD_searchMax(ZSTD_MatchState_t* ms,
					const BYTE* ip,
					const BYTE* iend,
					size_t* offsetPtr,
					const U32  mls,
					const U32  rowLog,
					const searchMethod_e  searchMethod,
					const ZSTD_dictMode_e  dictMode);  // 34777
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34778
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 34779
		}
		{
			const U32  matchIndex; // 34789
			const BYTE* match; // 34790
			const BYTE* mStart; // 34791
		}
		{
			const size_t  litLength; // 34798
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 34799
		}
		{
			const U32  repCurrent; // 34812
			const U32  windowLow; // 34813
			const U32  repIndex; // 34814
			const BYTE* repBase; // 34815
			const BYTE* repMatch; // 34816
			ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
						U32 curr,
						unsigned int windowLog);  // 34813
			ZSTD_index_overlap_check(const U32  prefixLowestIndex,
						const U32  repIndex);  // 34817
			MEM_read32(const void* ptr); // 34819
			MEM_read32(const void* ptr); // 34819
			{
				const BYTE* repEnd; // 34821
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 19955
				{
					const size_t  mlBase; // 19911
				}
				ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
							size_t litLength,
							U32 offBase,
							size_t matchLength);  // 19964
				ZSTD_storeSeq(SeqStore_t* seqStorePtr,
						size_t litLength,
						const BYTE* literals,
						const BYTE* litLimit,
						U32 offBase,
						size_t matchLength);  // 34824
			}
		}
	}
	ZSTD_compressBlock_lazy_extDict_generic(ZSTD_MatchState_t* ms,
						SeqStore_t* seqStore,
						U32* rep,
						const void* src,
						size_t srcSize,
						const searchMethod_e  searchMethod,
						const U32  depth);  // 34898
} /* size: 2609, goto labels: 1, inline expansions: 1 (2512 bytes) */

// <06F3D835> thirdparty/zstd/zstd.c:35032
// member variables: 2
// struct size: 16
typedef struct {
	U64 rolling; /* 0 8 */
	U64 stopMask; /* 8 8 */
} ldmRollingHashState_t;

// <06F7C48B> thirdparty/zstd/zstd.c:35041
// variables: 2
void ZSTD_ldm_gear_init(ldmRollingHashState_t* state, const ldmParams_t* params)
{
	unsigned int maxBitsInMask; // 35043
	unsigned int hashRateLog; // 35044
} /* size: 0, variables: 2 */

// <06F7C43D> thirdparty/zstd/zstd.c:35074
// variables: 2
void ZSTD_ldm_gear_reset(ldmRollingHashState_t* state, const BYTE* data, size_t minMatchLength)
{
	U64 hash; // 35077
	size_t n; // 35078
} /* size: 0, variables: 2 */

// <07018417> thirdparty/zstd/zstd.c:35105
// variables: 3
size_t ZSTD_ldm_gear_feed(ldmRollingHashState_t* state, const BYTE* data, size_t size, size_t* splits, unsigned int* numSplits)
{

done: // 35139
	size_t n; // 35109
	U64 hash; // 35110
	U64 mask; // 35110
} /* size: 398, variables: 3, goto labels: 1 */

// <06F7C369> thirdparty/zstd/zstd.c:35144
void ZSTD_ldm_adjustParameters(ldmParams_t* params, const ZSTD_compressionParameters* cParams)
{
} /* size: 202 */

// <06F7C310> thirdparty/zstd/zstd.c:35178
// variables: 4
size_t ZSTD_ldm_getTableSize(ldmParams_t params)
{
	const size_t  ldmHSize; // 35180
	const size_t  ldmBucketSizeLog; // 35181
	const size_t  ldmBucketSize; // 35182
	const size_t  totalSize; // 35183
} /* size: 0, variables: 4 */

// <06F7C2DE> thirdparty/zstd/zstd.c:35188
size_t ZSTD_ldm_getMaxNbSeq(ldmParams_t params, size_t maxChunkSize)
{
} /* size: 0 */

// <06F7C29E> thirdparty/zstd/zstd.c:35195
ldmEntry_t* ZSTD_ldm_getBucket(const ldmState_t* ldmState, size_t hash, const U32 bucketSizeLog)
{
} /* size: 0 */

// <06F7C241> thirdparty/zstd/zstd.c:35203
// variables: 2
void ZSTD_ldm_insertEntry(ldmState_t* ldmState, const size_t hash, const ldmEntry_t entry, const U32 bucketSizeLog)
{
	const BYTE* pOffset; // 35207
	const unsigned int  offset; // 35208
} /* size: 0, variables: 2 */

// <06F7C1ED> thirdparty/zstd/zstd.c:35219
// variable: 1
size_t ZSTD_ldm_countBackwardsMatch(const BYTE* pIn, const BYTE* pAnchor, const BYTE* pMatch, const BYTE* pMatchBase)
{
	size_t matchLength; // 35223
} /* size: 0, variables: 1 */

// <06F7C17F> thirdparty/zstd/zstd.c:35237
// variable: 1
size_t ZSTD_ldm_countBackwardsMatch_2segments(const BYTE* pIn, const BYTE* pAnchor, const BYTE* pMatch, const BYTE* pMatchBase, const BYTE* pExtDictStart, const BYTE* pExtDictEnd)
{
	size_t matchLength; // 35242
} /* size: 0, variables: 1 */

// <06F7C146> thirdparty/zstd/zstd.c:35260
// variable: 1
size_t ZSTD_ldm_fillFastTables(ZSTD_MatchState_t* ms, const void* end)
{
	const BYTE* iend; // 35263
} /* size: 0, variables: 1 */

// <06F7B911> thirdparty/zstd/zstd.c:35294
// variables: 20
// function calls: 29
void ZSTD_ldm_fillHashTable(ldmState_t* ldmState, const BYTE* ip, const BYTE* iend, const ldmParams_t* params)
{
	const U32  minMatchLength; // 35298
	const U32  bucketSizeLog; // 35299
	const U32  hBits; // 35300
	const BYTE* base; // 35301
	const BYTE* istart; // 35302
	ldmRollingHashState_t hashState; // 35303
	const size_t* splits; // 35304
	unsigned int numSplits; // 35305
	{
		size_t hashed; // 35311
		unsigned int n; // 35312
		{
			const BYTE* split; // 35319
			const U64  xxhash; // 35320
			const U32  hash; // 35321
			ldmEntry_t entry; // 35322
			{
				const xxh_u8* bEnd; // 11768
				const xxh_u8* limit; // 11769
				xxh_u64 v1; // 11770
				xxh_u64 v2; // 11771
				xxh_u64 v3; // 11772
				xxh_u64 v4; // 11773
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11682
				XXH64_mergeRound(xxh_u64 acc,
						xxh_u64 val);  // 11783
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11776
				XXH_read64(const void* ptr); // 11611
				XXH_readLE64(const void* ptr); // 11624
				XXH_readLE64_align(const void* ptr,
							XXH_alignment align);  // 11776
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11777
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11778
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11779
				XXH_read64(const void* ptr); // 11611
				XXH_readLE64(const void* ptr); // 11624
				XXH_readLE64_align(const void* ptr,
							XXH_alignment align);  // 11777
				XXH_read64(const void* ptr); // 11611
				XXH_readLE64(const void* ptr); // 11624
				XXH_readLE64_align(const void* ptr,
							XXH_alignment align);  // 11778
				XXH_read64(const void* ptr); // 11611
				XXH_readLE64(const void* ptr); // 11624
				XXH_readLE64_align(const void* ptr,
							XXH_alignment align);  // 11779
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11682
				XXH64_mergeRound(xxh_u64 acc,
						xxh_u64 val);  // 11786
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11682
				XXH64_mergeRound(xxh_u64 acc,
						xxh_u64 val);  // 11784
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11682
				XXH64_mergeRound(xxh_u64 acc,
						xxh_u64 val);  // 11785
			}
			XXH64_endian_align(const xxh_u8* input,
						size_t len,
						xxh_u64 seed,
						XXH_alignment align);  // 11813
			XXH_INLINE_XXH64(const void* input,
					size_t len,
					XXH64_hash_t seed);  // 35320
			ZSTD_ldm_getBucket(const ldmState_t* ldmState,
						size_t hash,
						const U32  bucketSizeLog);  // 35210
			ZSTD_ldm_insertEntry(ldmState_t* ldmState,
						const size_t  hash,
						const ldmEntry_t  entry,
						const U32  bucketSizeLog);  // 35326
		}
	}
	ZSTD_ldm_gear_init(ldmRollingHashState_t* state,
				const ldmParams_t* params);  // 35309
} /* size: 999, variables: 8, inline expansions: 1 (56 bytes) */

// <06F7B8DC> thirdparty/zstd/zstd.c:35340
// variable: 1
void ZSTD_ldm_limitTableUpdate(ZSTD_MatchState_t* ms, const BYTE* anchor)
{
	const U32  curr; // 35342
} /* size: 0, variables: 1 */

// <06F7B60A> thirdparty/zstd/zstd.c:35351
// variables: 46
size_t ZSTD_ldm_generateSequences_internal(ldmState_t* ldmState, RawSeqStore_t* rawSeqStore, const ldmParams_t* params, const void* src, size_t srcSize)
{
	const int  extDict; // 35356
	const U32  minMatchLength; // 35357
	const U32  entsPerBucket; // 35358
	const U32  hBits; // 35359
	const U32  dictLimit; // 35361
	const U32  lowestIndex; // 35362
	const BYTE* base; // 35363
	const BYTE* dictBase; // 35364
	const BYTE* dictStart; // 35365
	const BYTE* dictEnd; // 35366
	const BYTE* lowPrefixPtr; // 35367
	const BYTE* istart; // 35369
	const BYTE* iend; // 35370
	const BYTE* ilimit; // 35371
	const BYTE* anchor; // 35373
	const BYTE* ip; // 35374
	ldmRollingHashState_t hashState; // 35376
	const size_t* splits; // 35378
	const ldmMatchCandidate_t* candidates; // 35379
	unsigned int numSplits; // 35380
	{
		size_t hashed; // 35391
		unsigned int n; // 35392
		{
			const BYTE* split; // 35399
			const U64  xxhash; // 35400
			const U32  hash; // 35401
		}
		{
			size_t forwardMatchLength; // 35411
			size_t backwardMatchLength; // 35411
			size_t bestMatchLength; // 35412
			size_t mLength; // 35412
			U32 offset; // 35413
			const BYTE* split; // 35414
			const U32  checksum; // 35415
			const U32  hash; // 35416
			const ldmEntry_t* bucket; // 35417
			const ldmEntry_t* cur; // 35418
			const ldmEntry_t* bestEntry; // 35419
			ldmEntry_t newEntry; // 35420
			{
				size_t curForwardMatchLength; // 35434
				size_t curBackwardMatchLength; // 35434
				size_t curTotalMatchLength; // 35435
				{
					const BYTE* curMatchBase; // 35440
					const BYTE* pMatch; // 35442
					const BYTE* matchEnd; // 35443
					const BYTE* lowMatchPtr; // 35445
				}
				{
					const BYTE* pMatch; // 35455
				}
			}
			{
				const rawSeq* seq; // 35484
			}
		}
	}
} /* size: 0, variables: 20 */

// <06F7B5C9> thirdparty/zstd/zstd.c:35525
// variable: 1
void ZSTD_ldm_reduceTable(const ldmEntry_t* table, const U32 size, const U32 reducerValue)
{
	U32 u; // 35528
} /* size: 0, variables: 1 */

// <06F79CF9> thirdparty/zstd/zstd.c:35535
// variables: 55
// function calls: 84
size_t ZSTD_ldm_generateSequences(ldmState_t* ldmState, RawSeqStore_t* sequences, const ldmParams_t* params, const void* src, size_t srcSize)
{
	const U32  maxDist; // 35539
	const BYTE* istart; // 35540
	const BYTE* iend; // 35541
	const size_t  kMaxChunkSize; // 35542
	const size_t  nbChunks; // 35543
	size_t chunk; // 35544
	size_t leftoverSize; // 35545
	{
		const BYTE* chunkStart; // 35558
		const size_t  remaining; // 35559
		const BYTE* chunkEnd; // 35560
		const size_t  chunkSize; // 35562
		size_t newLeftoverSize; // 35563
		const size_t  prevSize; // 35564
		{
			const U32  ldmHSize; // 35569
			const U32  correction; // 35570
			ZSTD_window_correctOverflow(ZSTD_window_t* window,
							U32 cycleLog,
							U32 maxDist,
							const void* src);  // 35570
			ZSTD_ldm_reduceTable(const ldmEntry_t* table,
						const U32  size,
						const U32  reducerValue);  // 35572
		}
		ZSTD_window_needOverflowCorrection(const ZSTD_window_t  window,
							U32 cycleLog,
							U32 maxDist,
							U32 loadedDictEnd,
							const void* src,
							const void* srcEnd);  // 35568
		{
			const U32  newLowLimit; // 20435
		}
		{
			const U32  newLowLimit; // 20435
		}
		ZSTD_window_enforceMaxDist(ZSTD_window_t* window,
						const void* blockEnd,
						U32 maxDist,
						U32* loadedDictEndPtr,
						const ZSTD_MatchState_t ** dictMatchStatePtr);  // 20410
		ZSTD_window_enforceMaxDist(ZSTD_window_t* window,
						const void* blockEnd,
						U32 maxDist,
						U32* loadedDictEndPtr,
						const ZSTD_MatchState_t ** dictMatchStatePtr);  // 35590
		ZSTD_ldm_gear_init(ldmRollingHashState_t* state,
					const ldmParams_t* params);  // 35386
		{
			size_t hashed; // 35391
			unsigned int n; // 35392
			{
				size_t forwardMatchLength; // 35411
				size_t backwardMatchLength; // 35411
				size_t bestMatchLength; // 35412
				size_t mLength; // 35412
				U32 offset; // 35413
				const BYTE* split; // 35414
				const U32  checksum; // 35415
				const U32  hash; // 35416
				const ldmEntry_t* bucket; // 35417
				const ldmEntry_t* cur; // 35418
				const ldmEntry_t* bestEntry; // 35419
				ldmEntry_t newEntry; // 35420
				{
					size_t curForwardMatchLength; // 35434
					size_t curBackwardMatchLength; // 35434
					size_t curTotalMatchLength; // 35435
					{
						const BYTE* pMatch; // 35455
						{
							const size_t  diff; // 20014
							MEM_readST(const void* ptr); // 20014
							MEM_readST(const void* ptr); // 20014
							ZSTD_countTrailingZeros64(U64 val); // 2090
							ZSTD_NbCommonBytes(size_t val); // 20015
						}
						{
							const size_t  diff; // 20018
							MEM_readST(const void* ptr); // 20018
							MEM_readST(const void* ptr); // 20018
							ZSTD_countTrailingZeros64(U64 val); // 2090
							ZSTD_NbCommonBytes(size_t val); // 20020
						}
						MEM_read32(const void* ptr); // 20023
						MEM_read32(const void* ptr); // 20023
						MEM_read16(const void* ptr); // 20024
						MEM_read16(const void* ptr); // 20024
						ZSTD_count(const BYTE* pIn,
								const BYTE* pMatch,
								const BYTE* pInLimit);  // 35456
						ZSTD_ldm_countBackwardsMatch(const BYTE* pIn,
										const BYTE* pAnchor,
										const BYTE* pMatch,
										const BYTE* pMatchBase);  // 35461
					}
					{
						const BYTE* curMatchBase; // 35440
						const BYTE* pMatch; // 35442
						const BYTE* matchEnd; // 35443
						const BYTE* lowMatchPtr; // 35445
						ZSTD_ldm_countBackwardsMatch(const BYTE* pIn,
										const BYTE* pAnchor,
										const BYTE* pMatch,
										const BYTE* pMatchBase);  // 35242
						ZSTD_ldm_countBackwardsMatch(const BYTE* pIn,
										const BYTE* pAnchor,
										const BYTE* pMatch,
										const BYTE* pMatchBase);  // 35248
						ZSTD_ldm_countBackwardsMatch_2segments(const BYTE* pIn,
											const BYTE* pAnchor,
											const BYTE* pMatch,
											const BYTE* pMatchBase,
											const BYTE* pExtDictStart,
											const BYTE* pExtDictEnd);  // 35452
						{
							const size_t  diff; // 20014
							MEM_readST(const void* ptr); // 20014
							MEM_readST(const void* ptr); // 20014
							ZSTD_countTrailingZeros64(U64 val); // 2090
							ZSTD_NbCommonBytes(size_t val); // 20015
						}
						{
							const size_t  diff; // 20018
							MEM_readST(const void* ptr); // 20018
							MEM_readST(const void* ptr); // 20018
							ZSTD_countTrailingZeros64(U64 val); // 2090
							ZSTD_NbCommonBytes(size_t val); // 20020
						}
						MEM_read32(const void* ptr); // 20023
						MEM_read32(const void* ptr); // 20023
						MEM_read16(const void* ptr); // 20024
						MEM_read16(const void* ptr); // 20024
						ZSTD_count(const BYTE* pIn,
								const BYTE* pMatch,
								const BYTE* pInLimit);  // 20038
						{
							const size_t  diff; // 20014
							MEM_readST(const void* ptr); // 20014
							MEM_readST(const void* ptr); // 20014
							ZSTD_countTrailingZeros64(U64 val); // 2090
							ZSTD_NbCommonBytes(size_t val); // 20015
						}
						{
							const size_t  diff; // 20018
							MEM_readST(const void* ptr); // 20018
							MEM_readST(const void* ptr); // 20018
							ZSTD_countTrailingZeros64(U64 val); // 2090
							ZSTD_NbCommonBytes(size_t val); // 20020
						}
						MEM_read32(const void* ptr); // 20023
						MEM_read32(const void* ptr); // 20023
						MEM_read16(const void* ptr); // 20024
						MEM_read16(const void* ptr); // 20024
						ZSTD_count(const BYTE* pIn,
								const BYTE* pMatch,
								const BYTE* pInLimit);  // 20045
						ZSTD_count_2segments(const BYTE* ip,
									const BYTE* match,
									const BYTE* iEnd,
									const BYTE* mEnd,
									const BYTE* iStart);  // 35448
					}
				}
				{
					const rawSeq* seq; // 35484
				}
				ZSTD_ldm_getBucket(const ldmState_t* ldmState,
							size_t hash,
							const U32  bucketSizeLog);  // 35210
				ZSTD_ldm_insertEntry(ldmState_t* ldmState,
							const size_t  hash,
							const ldmEntry_t  entry,
							const U32  bucketSizeLog);  // 35497
				ZSTD_ldm_getBucket(const ldmState_t* ldmState,
							size_t hash,
							const U32  bucketSizeLog);  // 35210
				ZSTD_ldm_insertEntry(ldmState_t* ldmState,
							const size_t  hash,
							const ldmEntry_t  entry,
							const U32  bucketSizeLog);  // 35429
				ZSTD_ldm_getBucket(const ldmState_t* ldmState,
							size_t hash,
							const U32  bucketSizeLog);  // 35210
				ZSTD_ldm_insertEntry(ldmState_t* ldmState,
							const size_t  hash,
							const ldmEntry_t  entry,
							const U32  bucketSizeLog);  // 35476
			}
			{
				const BYTE* split; // 35399
				const U64  xxhash; // 35400
				const U32  hash; // 35401
				{
					const xxh_u8* bEnd; // 11768
					const xxh_u8* limit; // 11769
					xxh_u64 v1; // 11770
					xxh_u64 v2; // 11771
					xxh_u64 v3; // 11772
					xxh_u64 v4; // 11773
					XXH64_round(xxh_u64 acc,
							xxh_u64 input);  // 11776
					XXH_read64(const void* ptr); // 11611
					XXH_readLE64(const void* ptr); // 11624
					XXH_readLE64_align(const void* ptr,
								XXH_alignment align);  // 11776
					XXH64_round(xxh_u64 acc,
							xxh_u64 input);  // 11777
					XXH64_round(xxh_u64 acc,
							xxh_u64 input);  // 11778
					XXH64_round(xxh_u64 acc,
							xxh_u64 input);  // 11779
					XXH_read64(const void* ptr); // 11611
					XXH_readLE64(const void* ptr); // 11624
					XXH_readLE64_align(const void* ptr,
								XXH_alignment align);  // 11777
					XXH_read64(const void* ptr); // 11611
					XXH_readLE64(const void* ptr); // 11624
					XXH_readLE64_align(const void* ptr,
								XXH_alignment align);  // 11778
					XXH_read64(const void* ptr); // 11611
					XXH_readLE64(const void* ptr); // 11624
					XXH_readLE64_align(const void* ptr,
								XXH_alignment align);  // 11779
					XXH64_round(xxh_u64 acc,
							xxh_u64 input);  // 11682
					XXH64_mergeRound(xxh_u64 acc,
							xxh_u64 val);  // 11786
					XXH64_round(xxh_u64 acc,
							xxh_u64 input);  // 11682
					XXH64_mergeRound(xxh_u64 acc,
							xxh_u64 val);  // 11783
					XXH64_round(xxh_u64 acc,
							xxh_u64 input);  // 11682
					XXH64_mergeRound(xxh_u64 acc,
							xxh_u64 val);  // 11784
					XXH64_round(xxh_u64 acc,
							xxh_u64 input);  // 11682
					XXH64_mergeRound(xxh_u64 acc,
							xxh_u64 val);  // 11785
				}
				XXH64_endian_align(const xxh_u8* input,
							size_t len,
							xxh_u64 seed,
							XXH_alignment align);  // 11813
				XXH_INLINE_XXH64(const void* input,
						size_t len,
						XXH64_hash_t seed);  // 35400
				ZSTD_ldm_getBucket(const ldmState_t* ldmState,
							size_t hash,
							const U32  bucketSizeLog);  // 35406
			}
		}
		ZSTD_ldm_generateSequences_internal(ldmState_t* ldmState,
							RawSeqStore_t* rawSeqStore,
							const ldmParams_t* params,
							const void* src,
							size_t srcSize);  // 35592
	}
} /* size: 4045, variables: 7 */

// <06F79C7D> thirdparty/zstd/zstd.c:35613
// variable: 1
void ZSTD_ldm_skipSequences(RawSeqStore_t* rawSeqStore, size_t srcSize, const U32 minMatch)
{
	{
		rawSeq* seq; // 35616
	}
} /* size: 152 */

// <06F79C36> thirdparty/zstd/zstd.c:35649
// variable: 1
rawSeq maybeSplitSequence(RawSeqStore_t* rawSeqStore, const U32 remaining, const U32 minMatch)
{
	rawSeq sequence; // 35652
} /* size: 0, variables: 1 */

// <06F79BB4> thirdparty/zstd/zstd.c:35673
// variables: 2
void ZSTD_ldm_skipRawSeqStoreBytes(RawSeqStore_t* rawSeqStore, size_t nbBytes)
{
	U32 currPos; // 35674
	{
		rawSeq currSeq; // 35676
	}
} /* size: 95, variables: 1 */

// <06F7911B> thirdparty/zstd/zstd.c:35690
// variables: 11
// function calls: 30
size_t ZSTD_ldm_blockCompress(RawSeqStore_t* rawSeqStore, ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, ZSTD_ParamSwitch_e useRowMatchFinder, const void* src, size_t srcSize)
{
	const ZSTD_compressionParameters* cParams; // 35695
	const unsigned int  minMatch; // 35696
	const ZSTD_BlockCompressor_f  blockCompressor; // 35697
	const BYTE* istart; // 35700
	const BYTE* iend; // 35701
	const BYTE* ip; // 35703
	{
		size_t lastLLSize; // 35708
	}
	{
		const rawSeq  sequence; // 35720
		{
			int i; // 35734
			const size_t  newLitLength; // 35735
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 19955
			{
				const size_t  mlBase; // 19911
			}
			ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
						size_t litLength,
						U32 offBase,
						size_t matchLength);  // 19964
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 19957
			ZSTD_storeSeq(SeqStore_t* seqStorePtr,
					size_t litLength,
					const BYTE* literals,
					const BYTE* litLimit,
					U32 offBase,
					size_t matchLength);  // 35743
		}
		ZSTD_ldm_limitTableUpdate(ZSTD_MatchState_t* ms,
						const BYTE* anchor);  // 35729
		maybeSplitSequence(RawSeqStore_t* rawSeqStore,
					const U32  remaining,
					const U32  minMatch);  // 35720
		ZSTD_fillDoubleHashTable(ZSTD_MatchState_t* ms,
					const void* end,
					ZSTD_dictTableLoadMethod_e dtlm,
					ZSTD_tableFillPurpose_e tfp);  // 35273
		ZSTD_fillHashTable(ZSTD_MatchState_t* ms,
					const void* end,
					ZSTD_dictTableLoadMethod_e dtlm,
					ZSTD_tableFillPurpose_e tfp);  // 35268
		ZSTD_ldm_fillFastTables(ZSTD_MatchState_t* ms,
					const void* end);  // 35730
	}
	ZSTD_window_hasExtDict(const ZSTD_window_t window); // 20227
	ZSTD_matchState_dictMode(const ZSTD_MatchState_t* ms); // 20225
	ZSTD_matchState_dictMode(const ZSTD_MatchState_t* ms); // 35698
	ZSTD_rowMatchFinderSupported(const ZSTD_strategy strategy); // 23188
	ZSTD_rowMatchFinderUsed(const ZSTD_strategy  strategy,
				const ZSTD_ParamSwitch_e  mode);  // 26075
	{
	}
	ZSTD_selectBlockCompressor(ZSTD_strategy strat,
					ZSTD_ParamSwitch_e useRowMatchFinder,
					ZSTD_dictMode_e dictMode);  // 35698
	ZSTD_fillDoubleHashTable(ZSTD_MatchState_t* ms,
				const void* end,
				ZSTD_dictTableLoadMethod_e dtlm,
				ZSTD_tableFillPurpose_e tfp);  // 35273
	ZSTD_fillHashTable(ZSTD_MatchState_t* ms,
				const void* end,
				ZSTD_dictTableLoadMethod_e dtlm,
				ZSTD_tableFillPurpose_e tfp);  // 35268
	ZSTD_ldm_fillFastTables(ZSTD_MatchState_t* ms,
				const void* end);  // 35751
	ZSTD_ldm_limitTableUpdate(ZSTD_MatchState_t* ms,
					const BYTE* anchor);  // 35750
} /* size: 1199, variables: 6, inline expansions: 10 (375 bytes) */

// <06F790FB> thirdparty/zstd/zstd.c:35801
inline U32 ZSTD_bitWeight(U32 stat)
{
} /* size: 0 */

// <06F7909B> thirdparty/zstd/zstd.c:35809
// variables: 5
inline U32 ZSTD_fracWeight(U32 rawStat)
{
	const U32  stat; // 35811
	const U32  hb; // 35812
	const U32  BWeight; // 35813
	const U32  FWeight; // 35817
	const U32  weight; // 35818
} /* size: 0, variables: 5 */

// <06F7907B> thirdparty/zstd/zstd.c:35833
int ZSTD_compressedLiterals(const optState_t* optPtr)
{
} /* size: 0 */

// <06F78B8A> thirdparty/zstd/zstd.c:35838
// function calls: 25
void ZSTD_setBasePrices(optState_t* optPtr, int optLevel)
{
	ZSTD_compressedLiterals(const optState_t* optPtr); // 35840
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35812
	ZSTD_fracWeight(U32 rawStat); // 35841
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35803
	ZSTD_bitWeight(U32 stat); // 35841
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35803
	ZSTD_bitWeight(U32 stat); // 35842
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35803
	ZSTD_bitWeight(U32 stat); // 35843
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35803
	ZSTD_bitWeight(U32 stat); // 35844
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35812
	ZSTD_fracWeight(U32 rawStat); // 35842
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35812
	ZSTD_fracWeight(U32 rawStat); // 35843
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35812
	ZSTD_fracWeight(U32 rawStat); // 35844
} /* size: 277, inline expansions: 25 (394 bytes) */

// <06F78B3F> thirdparty/zstd/zstd.c:35848
// variables: 2
U32 sum_u32(const unsigned int* table, size_t nbElts)
{
	size_t n; // 35850
	U32 total; // 35851
} /* size: 0, variables: 2 */

// <06F78AC4> thirdparty/zstd/zstd.c:35861
// variables: 4
U32 ZSTD_downscaleStats(unsigned int* table, U32 lastEltIndex, U32 shift, base_directive_e base1)
{
	U32 s; // 35863
	U32 sum; // 35863
	{
		const unsigned int  base; // 35868
		const unsigned int  newStat; // 35869
	}
} /* size: 0, variables: 2 */

// <06F78A70> thirdparty/zstd/zstd.c:35879
// variables: 2
U32 ZSTD_scaleStats(unsigned int* table, U32 lastEltIndex, U32 logTarget)
{
	const U32  prevsum; // 35881
	const U32  factor; // 35882
} /* size: 0, variables: 2 */

// <06F77C82> thirdparty/zstd/zstd.c:35897
// variables: 30
// function calls: 37
void ZSTD_rescaleFreqs(const optState_t* optPtr, const BYTE* src, const size_t srcSize, const int optLevel)
{
	const int  compressedLiterals; // 35901
	{
		unsigned int lit; // 35921
		{
			const U32  scaleLog; // 35925
			const U32  bitCost; // 35926
			HUF_readCTableHeader(const HUF_CElt* ctable); // 17106
			HUF_getNbBits(HUF_CElt elt); // 17108
			HUF_getNbBitsFromCTable(const HUF_CElt* CTable,
						U32 symbolValue);  // 17102
			HUF_getNbBitsFromCTable(const HUF_CElt* CTable,
						U32 symbolValue);  // 35926
		}
	}
	{
		unsigned int ll; // 35932
		FSE_CState_t llstate; // 35933
		{
			const U32  scaleLog; // 35937
			const U32  bitCost; // 35938
			FSE_getMaxNbBits(const void* symbolTTPtr,
					U32 symbolValue);  // 35938
		}
		MEM_read16(const void* ptr); // 2763
		FSE_initCState(FSE_CState_t* statePtr,
				const FSE_CTable* ct);  // 35934
	}
	{
		unsigned int ml; // 35944
		FSE_CState_t mlstate; // 35945
		{
			const U32  scaleLog; // 35949
			const U32  bitCost; // 35950
			FSE_getMaxNbBits(const void* symbolTTPtr,
					U32 symbolValue);  // 35950
		}
		MEM_read16(const void* ptr); // 2763
		FSE_initCState(FSE_CState_t* statePtr,
				const FSE_CTable* ct);  // 35946
	}
	{
		unsigned int of; // 35956
		FSE_CState_t ofstate; // 35957
		{
			const U32  scaleLog; // 35961
			const U32  bitCost; // 35962
			FSE_getMaxNbBits(const void* symbolTTPtr,
					U32 symbolValue);  // 35962
		}
		MEM_read16(const void* ptr); // 2763
		FSE_initCState(FSE_CState_t* statePtr,
				const FSE_CTable* ct);  // 35958
	}
	{
		unsigned int lit; // 35973
		{
			const unsigned int  base; // 35868
			const unsigned int  newStat; // 35869
		}
		ZSTD_downscaleStats(unsigned int* table,
					U32 lastEltIndex,
					U32 shift,
					base_directive_e base1);  // 35975
	}
	{
		const unsigned int  baseLLfreqs; // 35978
		sum_u32(const unsigned int* table,
			size_t nbElts);  // 35986
	}
	{
		unsigned int ml; // 35989
	}
	{
		const unsigned int  baseOFCfreqs; // 35995
		sum_u32(const unsigned int* table,
			size_t nbElts);  // 36002
	}
	ZSTD_compressedLiterals(const optState_t* optPtr); // 35901
	sum_u32(const unsigned int* table,
		size_t nbElts);  // 35881
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35886
	{
		const unsigned int  base; // 35868
		const unsigned int  newStat; // 35869
	}
	ZSTD_downscaleStats(unsigned int* table,
				U32 lastEltIndex,
				U32 shift,
				base_directive_e base1);  // 35886
	ZSTD_scaleStats(unsigned int* table,
			U32 lastEltIndex,
			U32 logTarget);  // 36010
	sum_u32(const unsigned int* table,
		size_t nbElts);  // 35881
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35886
	{
		const unsigned int  base; // 35868
		const unsigned int  newStat; // 35869
	}
	ZSTD_downscaleStats(unsigned int* table,
				U32 lastEltIndex,
				U32 shift,
				base_directive_e base1);  // 35886
	ZSTD_scaleStats(unsigned int* table,
			U32 lastEltIndex,
			U32 logTarget);  // 36011
	sum_u32(const unsigned int* table,
		size_t nbElts);  // 35881
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35886
	{
		const unsigned int  base; // 35868
		const unsigned int  newStat; // 35869
	}
	ZSTD_downscaleStats(unsigned int* table,
				U32 lastEltIndex,
				U32 shift,
				base_directive_e base1);  // 35886
	ZSTD_scaleStats(unsigned int* table,
			U32 lastEltIndex,
			U32 logTarget);  // 36012
	sum_u32(const unsigned int* table,
		size_t nbElts);  // 35881
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 35886
	{
		const unsigned int  base; // 35868
		const unsigned int  newStat; // 35869
	}
	ZSTD_downscaleStats(unsigned int* table,
				U32 lastEltIndex,
				U32 shift,
				base_directive_e base1);  // 35886
	ZSTD_scaleStats(unsigned int* table,
			U32 lastEltIndex,
			U32 logTarget);  // 36013
} /* size: 2131, variables: 1, inline expansions: 21 (1255 bytes) */

// <06F77C05> thirdparty/zstd/zstd.c:36022
// variables: 4
U32 ZSTD_rawLiteralsCost(const BYTE* literals, const U32 litLength, const optState_t* optPtr, int optLevel)
{
	{
		U32 price; // 36036
		const U32  litPriceMax; // 36037
		U32 u; // 36038
		{
			U32 litPrice; // 36041
		}
	}
} /* size: 0 */

// <07018C77> thirdparty/zstd/zstd.c:36051
// variable: 1
// function calls: 7
U32 ZSTD_litLengthPrice(const U32 litLength, int optLevel, const optState_t* optPtr)
{
	{
		const U32  llCode; // 36066
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 19749
		ZSTD_LLcode(U32 litLength); // 36066
		ZSTD_highbit32(U32 val); // 35812
		ZSTD_fracWeight(U32 rawStat); // 36069
	}
	ZSTD_highbit32(U32 val); // 35812
	ZSTD_fracWeight(U32 rawStat); // 36055
} /* size: 170, inline expansions: 2 (27 bytes) */

// <06F77BBC> thirdparty/zstd/zstd.c:36051
// variable: 1
U32 ZSTD_litLengthPrice(const U32 litLength, const optState_t* optPtr, int optLevel)
{
	{
		const U32  llCode; // 36066
	}
} /* size: 0 */

// <06F77B34> thirdparty/zstd/zstd.c:36080
// variables: 4
inline U32 ZSTD_getMatchPrice(const U32 offBase, const U32 matchLength, const optState_t* optPtr, const int optLevel)
{
	U32 price; // 36085
	const U32  offCode; // 36086
	const U32  mlBase; // 36087
	{
		const U32  mlCode; // 36100
	}
} /* size: 0, variables: 3 */

// <06F7787F> thirdparty/zstd/zstd.c:36112
// variables: 5
// function calls: 9
void ZSTD_updateStats(const optState_t* optPtr, U32 litLength, const BYTE* literals, U32 offBase, U32 matchLength)
{
	{
		U32 u; // 36118
	}
	{
		const U32  llCode; // 36125
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 19749
		ZSTD_LLcode(U32 litLength); // 36125
	}
	{
		const U32  offCode; // 36131
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 36131
	}
	{
		const U32  mlBase; // 36138
		const U32  mlCode; // 36139
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 19766
		ZSTD_MLcode(U32 mlBase); // 36139
	}
	ZSTD_compressedLiterals(const optState_t* optPtr); // 36117
} /* size: 185, inline expansions: 1 (0 bytes) */

// <06F77852> thirdparty/zstd/zstd.c:36149
inline U32 ZSTD_readMINMATCH(const void* memPtr, U32 length)
{
} /* size: 0 */

// <06F775D2> thirdparty/zstd/zstd.c:36167
// variables: 6
// function calls: 8
U32 ZSTD_insertAndFindFirstIndexHash3(const ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip)
{
	const U32* hashTable3; // 36171
	const U32  hashLog3; // 36172
	const BYTE* base; // 36173
	U32 idx; // 36174
	const U32  target; // 36175
	const size_t  hash3; // 36176
	ZSTD_hash3(U32 u,
			U32 h,
			U32 s);  // 20054
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20054
	ZSTD_hash3Ptr(const void* ptr,
			U32 h);  // 36176
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20054
	ZSTD_hash3(U32 u,
			U32 h,
			U32 s);  // 20054
	ZSTD_hash3Ptr(const void* ptr,
			U32 h);  // 36180
} /* size: 95, variables: 6, inline expansions: 8 (49 bytes) */

// <06F764DB> thirdparty/zstd/zstd.c:36198
// variables: 34
// function calls: 62
U32 ZSTD_insertBt1(const ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iend, const U32 target, const U32 mls, const int extDict)
{
	const ZSTD_compressionParameters* cParams; // 36204
	const U32* hashTable; // 36205
	const U32  hashLog; // 36206
	const size_t  h; // 36207
	const U32* bt; // 36208
	const U32  btLog; // 36209
	const U32  btMask; // 36210
	U32 matchIndex; // 36211
	size_t commonLengthSmaller; // 36212
	size_t commonLengthLarger; // 36212
	const BYTE* base; // 36213
	const BYTE* dictBase; // 36214
	const U32  dictLimit; // 36215
	const BYTE* dictEnd; // 36216
	const BYTE* prefixStart; // 36217
	const BYTE* match; // 36218
	const U32  curr; // 36219
	const U32  btLow; // 36220
	U32* smallerPtr; // 36221
	U32* largerPtr; // 36222
	U32 dummy32; // 36223
	const U32  windowLow; // 36227
	U32 matchEndIdx; // 36228
	size_t bestLength; // 36229
	U32 nbCompares; // 36230
	{
		const U32* nextPtr; // 36246
		size_t matchLength; // 36247
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36274
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 36277
	}
	{
		U32 positions; // 36309
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20079
	ZSTD_hash8(U64 u,
			U32 h,
			U64 s);  // 20079
	ZSTD_hash8Ptr(const void* p,
			U32 h);  // 20097
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20074
	ZSTD_hash7(U64 u,
			U32 h,
			U64 s);  // 20074
	ZSTD_hash7Ptr(const void* p,
			U32 h);  // 20096
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36207
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36227
} /* size: 1850, variables: 25, inline expansions: 22 (386 bytes) */

// <06F7641E> thirdparty/zstd/zstd.c:36339
void ZSTD_updateTree(ZSTD_MatchState_t* ms, const BYTE* ip, const BYTE* iend)
{
} /* size: 0 */

// <06F76078> thirdparty/zstd/zstd.c:36346
// variables: 58
inline U32 ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iLimit, const ZSTD_dictMode_e dictMode, const U32* rep, const U32 ll0, const U32 lengthToBeat, const U32 mls)
{
	const ZSTD_compressionParameters* cParams; // 36357
	const U32  sufficient_len; // 36358
	const BYTE* base; // 36359
	const U32  curr; // 36360
	const U32  hashLog; // 36361
	const U32  minMatch; // 36362
	const U32* hashTable; // 36363
	const size_t  h; // 36364
	U32 matchIndex; // 36365
	const U32* bt; // 36366
	const U32  btLog; // 36367
	const U32  btMask; // 36368
	size_t commonLengthSmaller; // 36369
	size_t commonLengthLarger; // 36369
	const BYTE* dictBase; // 36370
	const U32  dictLimit; // 36371
	const BYTE* dictEnd; // 36372
	const BYTE* prefixStart; // 36373
	const U32  btLow; // 36374
	const U32  windowLow; // 36375
	const U32  matchLow; // 36376
	U32* smallerPtr; // 36377
	U32* largerPtr; // 36378
	U32 matchEndIdx; // 36379
	U32 dummy32; // 36380
	U32 mnum; // 36381
	U32 nbCompares; // 36382
	const ZSTD_MatchState_t* dms; // 36384
	const ZSTD_compressionParameters* dmsCParams; // 36385
	const BYTE* dmsBase; // 36387
	const BYTE* dmsEnd; // 36388
	const U32  dmsHighLimit; // 36389
	const U32  dmsLowLimit; // 36390
	const U32  dmsIndexDelta; // 36391
	const U32  dmsHashLog; // 36392
	const U32  dmsBtLog; // 36393
	const U32  dmsBtMask; // 36394
	const U32  dmsBtLow; // 36395
	size_t bestLength; // 36397
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
			}
		}
	}
	{
		const U32  matchIndex3; // 36448
		{
			size_t mlen; // 36451
			{
				const BYTE* match; // 36453
			}
			{
				const BYTE* match; // 36456
			}
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
	}
	{
		const size_t  dmsH; // 36534
		U32 dictMatchIndex; // 36535
		const U32* dmsBt; // 36536
		{
			const U32* nextPtr; // 36539
			size_t matchLength; // 36540
			const BYTE* match; // 36541
		}
	}
} /* size: 0, variables: 39 */

// <06F75FE5> thirdparty/zstd/zstd.c:36588
inline U32 ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat, const ZSTD_dictMode_e dictMode, const U32 mls)
{
} /* size: 0 */

// <06F75041> thirdparty/zstd/zstd.c:36632
// variables: 18
// function calls: 52
U32 ZSTD_btGetAllMatches_noDict_3(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			MEM_read32(const void* ptr); // 36156
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36156
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32  matchIndex3; // 36448
		{
			size_t mlen; // 36451
			{
				const BYTE* match; // 36453
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 36454
			}
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36632
} /* size: 2083, inline expansions: 9 (4046 bytes) */

// <06F743C7> thirdparty/zstd/zstd.c:36632
// variables: 13
// function calls: 39
U32 ZSTD_btGetAllMatches_noDict_4(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36632
} /* size: 1676, inline expansions: 9 (3241 bytes) */

// <06F7374D> thirdparty/zstd/zstd.c:36632
// variables: 13
// function calls: 39
U32 ZSTD_btGetAllMatches_noDict_5(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36632
} /* size: 1676, inline expansions: 9 (3267 bytes) */

// <06F72AD3> thirdparty/zstd/zstd.c:36632
// variables: 13
// function calls: 39
U32 ZSTD_btGetAllMatches_noDict_6(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36632
} /* size: 1676, inline expansions: 9 (3267 bytes) */

// <06F71388> thirdparty/zstd/zstd.c:36633
// variables: 24
// function calls: 84
U32 ZSTD_btGetAllMatches_extDict_3(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
				ZSTD_index_overlap_check(const U32  prefixLowestIndex,
							const U32  repIndex);  // 36423
				MEM_read32(const void* ptr); // 36156
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36424
				MEM_read32(const void* ptr); // 36156
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36424
			}
			MEM_read32(const void* ptr); // 36156
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36156
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32  matchIndex3; // 36448
		{
			size_t mlen; // 36451
			{
				const BYTE* match; // 36453
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 36454
			}
			{
				const BYTE* match; // 36456
			}
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 36494
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36633
} /* size: 3039, inline expansions: 9 (5938 bytes) */

// <06F6FFC5> thirdparty/zstd/zstd.c:36633
// variables: 18
// function calls: 71
U32 ZSTD_btGetAllMatches_extDict_4(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
				ZSTD_index_overlap_check(const U32  prefixLowestIndex,
							const U32  repIndex);  // 36423
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36424
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36424
			}
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 36494
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36633
} /* size: 2620, inline expansions: 9 (5136 bytes) */

// <06F6EC02> thirdparty/zstd/zstd.c:36633
// variables: 18
// function calls: 71
U32 ZSTD_btGetAllMatches_extDict_5(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
				ZSTD_index_overlap_check(const U32  prefixLowestIndex,
							const U32  repIndex);  // 36423
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36424
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36424
			}
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 36494
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36633
} /* size: 2620, inline expansions: 9 (5162 bytes) */

// <06F6D83F> thirdparty/zstd/zstd.c:36633
// variables: 18
// function calls: 71
U32 ZSTD_btGetAllMatches_extDict_6(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
				ZSTD_index_overlap_check(const U32  prefixLowestIndex,
							const U32  repIndex);  // 36423
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36424
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36424
			}
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20045
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 20038
		ZSTD_count_2segments(const BYTE* ip,
					const BYTE* match,
					const BYTE* iEnd,
					const BYTE* mEnd,
					const BYTE* iStart);  // 36494
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36633
} /* size: 2620, inline expansions: 9 (5162 bytes) */

// <06F6BF8E> thirdparty/zstd/zstd.c:36634
// variables: 29
// function calls: 89
U32 ZSTD_btGetAllMatches_dictMatchState_3(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
				ZSTD_index_overlap_check(const U32  prefixLowestIndex,
							const U32  repIndex);  // 36429
				MEM_read32(const void* ptr); // 36156
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36430
				MEM_read32(const void* ptr); // 36156
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36430
			}
			MEM_read32(const void* ptr); // 36156
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36156
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const size_t  dmsH; // 36534
		U32 dictMatchIndex; // 36535
		const U32* dmsBt; // 36536
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 36534
		{
			const U32* nextPtr; // 36539
			size_t matchLength; // 36540
			const BYTE* match; // 36541
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 36542
		}
	}
	{
		const U32  matchIndex3; // 36448
		{
			size_t mlen; // 36451
			{
				const BYTE* match; // 36453
				{
					const size_t  diff; // 20014
					MEM_readST(const void* ptr); // 20014
					MEM_readST(const void* ptr); // 20014
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20015
				}
				{
					const size_t  diff; // 20018
					MEM_readST(const void* ptr); // 20018
					MEM_readST(const void* ptr); // 20018
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 20020
				}
				MEM_read32(const void* ptr); // 20023
				MEM_read32(const void* ptr); // 20023
				MEM_read16(const void* ptr); // 20024
				MEM_read16(const void* ptr); // 20024
				ZSTD_count(const BYTE* pIn,
						const BYTE* pMatch,
						const BYTE* pInLimit);  // 36454
			}
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36634
} /* size: 3486, inline expansions: 9 (6843 bytes) */

// <06F6A9F9> thirdparty/zstd/zstd.c:36634
// variables: 24
// function calls: 76
U32 ZSTD_btGetAllMatches_dictMatchState_4(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	ZSTD_hash4(U32 u,
			U32 h,
			U32 s);  // 20059
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 20059
	ZSTD_hash4Ptr(const void* ptr,
			U32 h);  // 20093
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
				ZSTD_index_overlap_check(const U32  prefixLowestIndex,
							const U32  repIndex);  // 36429
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36430
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36430
			}
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	{
		const size_t  dmsH; // 36534
		U32 dictMatchIndex; // 36535
		const U32* dmsBt; // 36536
		{
			const U32* nextPtr; // 36539
			size_t matchLength; // 36540
			const BYTE* match; // 36541
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 36542
		}
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 20059
		ZSTD_hash4(U32 u,
				U32 h,
				U32 s);  // 20059
		ZSTD_hash4Ptr(const void* ptr,
				U32 h);  // 20093
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 36534
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36634
} /* size: 3089, inline expansions: 9 (6048 bytes) */

// <06F69464> thirdparty/zstd/zstd.c:36634
// variables: 24
// function calls: 76
U32 ZSTD_btGetAllMatches_dictMatchState_5(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	ZSTD_hash5(U64 u,
			U32 h,
			U64 s);  // 20064
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20064
	ZSTD_hash5Ptr(const void* p,
			U32 h);  // 20094
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
				ZSTD_index_overlap_check(const U32  prefixLowestIndex,
							const U32  repIndex);  // 36429
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36430
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36430
			}
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	{
		const size_t  dmsH; // 36534
		U32 dictMatchIndex; // 36535
		const U32* dmsBt; // 36536
		{
			const U32* nextPtr; // 36539
			size_t matchLength; // 36540
			const BYTE* match; // 36541
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 36542
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20064
		ZSTD_hash5(U64 u,
				U32 h,
				U64 s);  // 20064
		ZSTD_hash5Ptr(const void* p,
				U32 h);  // 20094
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 36534
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36634
} /* size: 3105, inline expansions: 9 (6106 bytes) */

// <06F67ECF> thirdparty/zstd/zstd.c:36634
// variables: 24
// function calls: 76
U32 ZSTD_btGetAllMatches_dictMatchState_6(ZSTD_match_t* matches, ZSTD_MatchState_t* ms, U32* nextToUpdate3, const BYTE* ip, const BYTE* iHighLimit, const U32* rep, const U32 ll0, const U32 lengthToBeat)
{
	{
		const U32  forward; // 36330
	}
	ZSTD_updateTree_internal(ZSTD_MatchState_t* ms,
				const BYTE* ip,
				const BYTE* iend,
				const U32  mls,
				const ZSTD_dictMode_e  dictMode);  // 36604
	ZSTD_hash6(U64 u,
			U32 h,
			U64 s);  // 20069
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 20069
	ZSTD_hash6Ptr(const void* p,
			U32 h);  // 20095
	ZSTD_hashPtr(const void* p,
			U32 hBits,
			U32 mls);  // 36364
	ZSTD_getLowestMatchIndex(const ZSTD_MatchState_t* ms,
				U32 curr,
				unsigned int windowLog);  // 36375
	{
		const U32  lastR; // 36402
		U32 repCode; // 36403
		{
			const U32  repOffset; // 36405
			const U32  repIndex; // 36406
			U32 repLen; // 36407
			{
				const BYTE* repMatch; // 36417
				ZSTD_index_overlap_check(const U32  prefixLowestIndex,
							const U32  repIndex);  // 36429
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36430
				MEM_read32(const void* ptr); // 36154
				ZSTD_readMINMATCH(const void* memPtr,
							U32 length);  // 36430
			}
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			MEM_read32(const void* ptr); // 36154
			ZSTD_readMINMATCH(const void* memPtr,
						U32 length);  // 36413
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 36414
		}
	}
	{
		const U32* nextPtr; // 36481
		const BYTE* match; // 36482
		size_t matchLength; // 36483
		{
			const size_t  diff; // 20014
			MEM_readST(const void* ptr); // 20014
			MEM_readST(const void* ptr); // 20014
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20015
		}
		{
			const size_t  diff; // 20018
			MEM_readST(const void* ptr); // 20018
			MEM_readST(const void* ptr); // 20018
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 20020
		}
		MEM_read32(const void* ptr); // 20023
		MEM_read32(const void* ptr); // 20023
		MEM_read16(const void* ptr); // 20024
		MEM_read16(const void* ptr); // 20024
		ZSTD_count(const BYTE* pIn,
				const BYTE* pMatch,
				const BYTE* pInLimit);  // 36490
	}
	{
		const size_t  dmsH; // 36534
		U32 dictMatchIndex; // 36535
		const U32* dmsBt; // 36536
		{
			const U32* nextPtr; // 36539
			size_t matchLength; // 36540
			const BYTE* match; // 36541
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20045
			{
				const size_t  diff; // 20014
				MEM_readST(const void* ptr); // 20014
				MEM_readST(const void* ptr); // 20014
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20015
			}
			{
				const size_t  diff; // 20018
				MEM_readST(const void* ptr); // 20018
				MEM_readST(const void* ptr); // 20018
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 20020
			}
			MEM_read32(const void* ptr); // 20023
			MEM_read32(const void* ptr); // 20023
			MEM_read16(const void* ptr); // 20024
			MEM_read16(const void* ptr); // 20024
			ZSTD_count(const BYTE* pIn,
					const BYTE* pMatch,
					const BYTE* pInLimit);  // 20038
			ZSTD_count_2segments(const BYTE* ip,
						const BYTE* match,
						const BYTE* iEnd,
						const BYTE* mEnd,
						const BYTE* iStart);  // 36542
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 20069
		ZSTD_hash6(U64 u,
				U32 h,
				U64 s);  // 20069
		ZSTD_hash6Ptr(const void* p,
				U32 h);  // 20095
		ZSTD_hashPtr(const void* p,
				U32 hBits,
				U32 mls);  // 36534
	}
	ZSTD_insertBtAndGetAllMatches(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iLimit,
					const ZSTD_dictMode_e  dictMode,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const U32  mls);  // 36605
	ZSTD_btGetAllMatches_internal(ZSTD_match_t* matches,
					ZSTD_MatchState_t* ms,
					U32* nextToUpdate3,
					const BYTE* ip,
					const BYTE* iHighLimit,
					const U32* rep,
					const U32  ll0,
					const U32  lengthToBeat,
					const ZSTD_dictMode_e  dictMode,
					const U32  mls);  // 36634
} /* size: 3105, inline expansions: 9 (6106 bytes) */

// <07018C23> thirdparty/zstd/zstd.c:36645
// variables: 2
ZSTD_getAllMatchesFn ZSTD_selectBtGetAllMatches(const ZSTD_dictMode_e dictMode, const ZSTD_MatchState_t* ms)
{
	const ZSTD_getAllMatchesFn  getAllMatchesFns; // 36647
	const U32  mls; // 36652
} /* size: 244, variables: 2 */

// <06F3D8F3> thirdparty/zstd/zstd.c:36663
// member variables: 4
// struct size: 56
typedef struct {
	RawSeqStore_t seqStore; /* 0 40 */
	U32 startPosInBlock; /* 40 4 */
	U32 endPosInBlock; /* 44 4 */
	U32 offset; /* 48 4 */
} ZSTD_optLdm_t;

// <06F67E23> thirdparty/zstd/zstd.c:36674
// variables: 2
void ZSTD_optLdm_skipRawSeqStoreBytes(RawSeqStore_t* rawSeqStore, size_t nbBytes)
{
	U32 currPos; // 36676
	{
		rawSeq currSeq; // 36678
	}
} /* size: 0, variables: 1 */

// <06F67D16> thirdparty/zstd/zstd.c:36697
// variables: 4
void ZSTD_opt_getNextMatchAndUpdateSeqStore(ZSTD_optLdm_t* optLdm, U32 currPosInBlock, U32 blockBytesRemaining)
{
	rawSeq currSeq; // 36700
	U32 currBlockEndPos; // 36701
	U32 literalsBytesRemaining; // 36702
	U32 matchBytesRemaining; // 36703
} /* size: 175, variables: 4 */

// <07017240> thirdparty/zstd/zstd.c:36752
// variables: 3
void ZSTD_optLdm_maybeAddMatch(ZSTD_match_t* matches, U32* nbMatches, const ZSTD_optLdm_t* optLdm, U32 currPosInBlock, U32 minMatch)
{
	const U32  posDiff; // 36756
	const U32  candidateMatchLength; // 36758
	{
		const U32  candidateOffBase; // 36768
	}
} /* size: 87, variables: 2 */

// <070982B5> thirdparty/zstd/zstd.c:36781
// variables: 2
// function call: 1
void ZSTD_optLdm_processMatchCandidate(ZSTD_optLdm_t* optLdm, ZSTD_match_t* matches, U32* nbMatches, U32 currPosInBlock, U32 remainingBytes, U32 minMatch)
{
	{
		const U32  posOvershoot; // 36796
	}
	{
		const U32  candidateOffBase; // 36768
	}
	ZSTD_optLdm_maybeAddMatch(ZSTD_match_t* matches,
					U32* nbMatches,
					const ZSTD_optLdm_t* optLdm,
					U32 currPosInBlock,
					U32 minMatch);  // 36801
} /* size: 198, inline expansions: 1 (84 bytes) */

// <06F67C25> thirdparty/zstd/zstd.c:36781
// variable: 1
void ZSTD_optLdm_processMatchCandidate(ZSTD_optLdm_t* optLdm, ZSTD_match_t* matches, U32* nbMatches, U32 currPosInBlock, U32 remainingBytes, U32 minMatch)
{
	{
		const U32  posOvershoot; // 36796
	}
} /* size: 0 */

// <06F6782A> thirdparty/zstd/zstd.c:36833
// variables: 62
inline size_t ZSTD_compressBlock_opt_generic(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const int optLevel, const ZSTD_dictMode_e dictMode)
{

_shortestPath: // 37100
	const optState_t* optStatePtr; // 36840
	const BYTE* istart; // 36841
	const BYTE* ip; // 36842
	const BYTE* anchor; // 36843
	const BYTE* iend; // 36844
	const BYTE* ilimit; // 36845
	const BYTE* base; // 36846
	const BYTE* prefixStart; // 36847
	const ZSTD_compressionParameters* cParams; // 36848
	ZSTD_getAllMatchesFn getAllMatches; // 36850
	const U32  sufficient_len; // 36852
	const U32  minMatch; // 36853
	U32 nextToUpdate3; // 36854
	const ZSTD_optimal_t* opt; // 36856
	const ZSTD_match_t* matches; // 36857
	ZSTD_optimal_t lastStretch; // 36858
	ZSTD_optLdm_t optLdm; // 36859
	{
		U32 cur; // 36876
		U32 last_pos; // 36876
		{
			const U32  litlen; // 36879
			const U32  ll0; // 36880
			U32 nbMatches; // 36881
			{
				const U32  maxML; // 36911
				const U32  maxOffBase; // 36912
			}
			{
				U32 pos; // 36929
				U32 matchNb; // 36930
				{
					const U32  offBase; // 36937
					const U32  end; // 36938
					{
						const int  matchPrice; // 36940
						const int  sequencePrice; // 36941
					}
				}
			}
		}
		{
			const BYTE* inr; // 36957
			{
				const U32  litlen; // 36962
				const int  price; // 36963
				{
					const ZSTD_optimal_t  prevMatch; // 36968
					{
						int with1literal; // 36981
						int withMoreLiterals; // 36982
						{
							const U32  prev; // 36988
							const Repcodes_t  newReps; // 36989
						}
					}
				}
			}
			{
				const U32  prev; // 37014
				const Repcodes_t  newReps; // 37015
			}
			{
				const U32  ll0; // 37031
				const int  previousPrice; // 37032
				const int  basePrice; // 37033
				U32 nbMatches; // 37034
				U32 matchNb; // 37035
				{
					const U32  longestML; // 37046
				}
				{
					const U32  offset; // 37062
					const U32  lastML; // 37063
					const U32  startML; // 37064
					U32 mlen; // 37065
					{
						const U32  pos; // 37071
						const int  price; // 37072
					}
				}
			}
		}
		{
			const Repcodes_t  reps; // 37116
		}
		{
			const U32  storeEnd; // 37132
			U32 storeStart; // 37133
			U32 stretchPos; // 37134
			{
				ZSTD_optimal_t nextStretch; // 37152
			}
			{
				U32 storePos; // 37168
				{
					const U32  llen; // 37170
					const U32  mlen; // 37171
					const U32  offBase; // 37172
					const U32  advance; // 37173
				}
			}
		}
	}
} /* size: 0, variables: 17, goto labels: 1 */

// <06F6638B> thirdparty/zstd/zstd.c:37202
// variables: 53
// function calls: 61
size_t ZSTD_compressBlock_opt0(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const ZSTD_dictMode_e dictMode)
{

_shortestPath: // 37100
	{
		U32 cur; // 36876
		U32 last_pos; // 36876
		{
			const U32  litlen; // 36879
			const U32  ll0; // 36880
			U32 nbMatches; // 36881
			ZSTD_optLdm_processMatchCandidate(ZSTD_optLdm_t* optLdm,
								ZSTD_match_t* matches,
								U32* nbMatches,
								U32 currPosInBlock,
								U32 remainingBytes,
								U32 minMatch);  // 36882
			{
				const U32  maxML; // 36911
				const U32  maxOffBase; // 36912
			}
			{
				U32 pos; // 36929
				U32 matchNb; // 36930
				{
					const U32  offBase; // 36937
					const U32  end; // 36938
					{
						const int  matchPrice; // 36940
						const int  sequencePrice; // 36941
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 36086
						{
							const U32  mlCode; // 36100
							ZSTD_countLeadingZeros32(U32 val); // 2106
							ZSTD_highbit32(U32 val); // 19766
							ZSTD_MLcode(U32 mlBase); // 36100
							ZSTD_countLeadingZeros32(U32 val); // 2106
							ZSTD_highbit32(U32 val); // 35803
							ZSTD_bitWeight(U32 stat); // 36101
						}
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35803
						ZSTD_bitWeight(U32 stat); // 36095
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35803
						ZSTD_bitWeight(U32 stat); // 36091
						ZSTD_getMatchPrice(const U32  offBase,
									const U32  matchLength,
									const optState_t* optPtr,
									const int  optLevel);  // 36940
					}
				}
			}
		}
		{
			const Repcodes_t  reps; // 37116
			{
				const U32  repCode; // 19979
				{
					const U32  currentOffset; // 19981
				}
			}
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 19972
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 20000
			ZSTD_newRep(const U32* rep,
					const U32  offBase,
					const U32  ll0);  // 37116
		}
		{
			const U32  storeEnd; // 37132
			U32 storeStart; // 37133
			U32 stretchPos; // 37134
			{
				ZSTD_optimal_t nextStretch; // 37152
			}
			{
				U32 storePos; // 37168
				{
					const U32  llen; // 37170
					const U32  mlen; // 37171
					const U32  offBase; // 37172
					const U32  advance; // 37173
					{
						const size_t  mlBase; // 19911
					}
					ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
								size_t litLength,
								U32 offBase,
								size_t matchLength);  // 19964
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 19955
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 19957
					ZSTD_storeSeq(SeqStore_t* seqStorePtr,
							size_t litLength,
							const BYTE* literals,
							const BYTE* litLimit,
							U32 offBase,
							size_t matchLength);  // 37185
				}
			}
		}
		{
			const BYTE* inr; // 36957
			{
				const U32  ll0; // 37031
				const int  previousPrice; // 37032
				const int  basePrice; // 37033
				U32 nbMatches; // 37034
				U32 matchNb; // 37035
				{
					const U32  offset; // 37062
					const U32  lastML; // 37063
					const U32  startML; // 37064
					U32 mlen; // 37065
					{
						const U32  pos; // 37071
						const int  price; // 37072
						{
							const U32  mlCode; // 36100
							ZSTD_countLeadingZeros32(U32 val); // 2106
							ZSTD_highbit32(U32 val); // 19766
							ZSTD_MLcode(U32 mlBase); // 36100
							ZSTD_countLeadingZeros32(U32 val); // 2106
							ZSTD_highbit32(U32 val); // 35803
							ZSTD_bitWeight(U32 stat); // 36101
						}
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 36086
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35803
						ZSTD_bitWeight(U32 stat); // 36095
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35803
						ZSTD_bitWeight(U32 stat); // 36091
						ZSTD_getMatchPrice(const U32  offBase,
									const U32  matchLength,
									const optState_t* optPtr,
									const int  optLevel);  // 37072
					}
				}
				{
					const U32  posOvershoot; // 36796
				}
				ZSTD_optLdm_processMatchCandidate(ZSTD_optLdm_t* optLdm,
									ZSTD_match_t* matches,
									U32* nbMatches,
									U32 currPosInBlock,
									U32 remainingBytes,
									U32 minMatch);  // 36781
				ZSTD_optLdm_processMatchCandidate(ZSTD_optLdm_t* optLdm,
									ZSTD_match_t* matches,
									U32* nbMatches,
									U32 currPosInBlock,
									U32 remainingBytes,
									U32 minMatch);  // 37037
				{
					const U32  longestML; // 37046
				}
			}
			{
				const U32  prev; // 37014
				const Repcodes_t  newReps; // 37015
				{
					const U32  repCode; // 19979
					{
						const U32  currentOffset; // 19981
					}
				}
				ZSTD_updateRep(U32* rep,
						const U32  offBase,
						const U32  ll0);  // 19972
				ZSTD_updateRep(U32* rep,
						const U32  offBase,
						const U32  ll0);  // 20000
				ZSTD_newRep(const U32* rep,
						const U32  offBase,
						const U32  ll0);  // 37015
			}
			{
				const U32  litlen; // 36962
				const int  price; // 36963
				{
					const ZSTD_optimal_t  prevMatch; // 36968
				}
				ZSTD_compressedLiterals(const optState_t* optPtr); // 36029
				{
					U32 price; // 36036
					const U32  litPriceMax; // 36037
					U32 u; // 36038
					{
						U32 litPrice; // 36041
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35803
						ZSTD_bitWeight(U32 stat); // 36041
					}
				}
				ZSTD_rawLiteralsCost(const BYTE* literals,
							const U32  litLength,
							const optState_t* optPtr,
							int optLevel);  // 36022
				ZSTD_rawLiteralsCost(const BYTE* literals,
							const U32  litLength,
							const optState_t* optPtr,
							int optLevel);  // 36964
			}
		}
	}
	ZSTD_compressBlock_opt_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const int  optLevel,
					const ZSTD_dictMode_e  dictMode);  // 37206
} /* size: 4539, goto labels: 1, inline expansions: 1 (4425 bytes) */

// <06F64980> thirdparty/zstd/zstd.c:37211
// variables: 63
// function calls: 70
size_t ZSTD_compressBlock_opt2(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize, const ZSTD_dictMode_e dictMode)
{

_shortestPath: // 37100
	{
		U32 cur; // 36876
		U32 last_pos; // 36876
		{
			const U32  litlen; // 36879
			const U32  ll0; // 36880
			U32 nbMatches; // 36881
			{
				U32 pos; // 36929
				U32 matchNb; // 36930
				{
					const U32  offBase; // 36937
					const U32  end; // 36938
					{
						const int  matchPrice; // 36940
						const int  sequencePrice; // 36941
						{
							const U32  mlCode; // 36100
							ZSTD_countLeadingZeros32(U32 val); // 2106
							ZSTD_highbit32(U32 val); // 19766
							ZSTD_MLcode(U32 mlBase); // 36100
							ZSTD_countLeadingZeros32(U32 val); // 2106
							ZSTD_highbit32(U32 val); // 35812
							ZSTD_fracWeight(U32 rawStat); // 36101
						}
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 36086
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35812
						ZSTD_fracWeight(U32 rawStat); // 36095
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35812
						ZSTD_fracWeight(U32 rawStat); // 36091
						ZSTD_getMatchPrice(const U32  offBase,
									const U32  matchLength,
									const optState_t* optPtr,
									const int  optLevel);  // 36940
					}
				}
			}
			ZSTD_optLdm_processMatchCandidate(ZSTD_optLdm_t* optLdm,
								ZSTD_match_t* matches,
								U32* nbMatches,
								U32 currPosInBlock,
								U32 remainingBytes,
								U32 minMatch);  // 36882
			{
				const U32  maxML; // 36911
				const U32  maxOffBase; // 36912
			}
		}
		{
			const Repcodes_t  reps; // 37116
			{
				const U32  repCode; // 19979
				{
					const U32  currentOffset; // 19981
				}
			}
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 19972
			ZSTD_updateRep(U32* rep,
					const U32  offBase,
					const U32  ll0);  // 20000
			ZSTD_newRep(const U32* rep,
					const U32  offBase,
					const U32  ll0);  // 37116
		}
		{
			const U32  storeEnd; // 37132
			U32 storeStart; // 37133
			U32 stretchPos; // 37134
			{
				ZSTD_optimal_t nextStretch; // 37152
			}
			{
				U32 storePos; // 37168
				{
					const U32  llen; // 37170
					const U32  mlen; // 37171
					const U32  offBase; // 37172
					const U32  advance; // 37173
					{
						const size_t  mlBase; // 19911
					}
					ZSTD_storeSeqOnly(SeqStore_t* seqStorePtr,
								size_t litLength,
								U32 offBase,
								size_t matchLength);  // 19964
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 19955
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 19957
					ZSTD_storeSeq(SeqStore_t* seqStorePtr,
							size_t litLength,
							const BYTE* literals,
							const BYTE* litLimit,
							U32 offBase,
							size_t matchLength);  // 37185
				}
			}
		}
		{
			const BYTE* inr; // 36957
			{
				const U32  ll0; // 37031
				const int  previousPrice; // 37032
				const int  basePrice; // 37033
				U32 nbMatches; // 37034
				U32 matchNb; // 37035
				{
					const U32  offset; // 37062
					const U32  lastML; // 37063
					const U32  startML; // 37064
					U32 mlen; // 37065
					{
						const U32  pos; // 37071
						const int  price; // 37072
						{
							const U32  mlCode; // 36100
							ZSTD_countLeadingZeros32(U32 val); // 2106
							ZSTD_highbit32(U32 val); // 19766
							ZSTD_MLcode(U32 mlBase); // 36100
							ZSTD_countLeadingZeros32(U32 val); // 2106
							ZSTD_highbit32(U32 val); // 35812
							ZSTD_fracWeight(U32 rawStat); // 36101
						}
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 36086
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35812
						ZSTD_fracWeight(U32 rawStat); // 36095
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35812
						ZSTD_fracWeight(U32 rawStat); // 36091
						ZSTD_getMatchPrice(const U32  offBase,
									const U32  matchLength,
									const optState_t* optPtr,
									const int  optLevel);  // 37072
					}
				}
				{
					const U32  posOvershoot; // 36796
				}
				ZSTD_optLdm_processMatchCandidate(ZSTD_optLdm_t* optLdm,
									ZSTD_match_t* matches,
									U32* nbMatches,
									U32 currPosInBlock,
									U32 remainingBytes,
									U32 minMatch);  // 36781
				ZSTD_optLdm_processMatchCandidate(ZSTD_optLdm_t* optLdm,
									ZSTD_match_t* matches,
									U32* nbMatches,
									U32 currPosInBlock,
									U32 remainingBytes,
									U32 minMatch);  // 37037
				{
					const U32  longestML; // 37046
				}
			}
			{
				const U32  litlen; // 36962
				const int  price; // 36963
				ZSTD_compressedLiterals(const optState_t* optPtr); // 36029
				{
					U32 price; // 36036
					const U32  litPriceMax; // 36037
					U32 u; // 36038
					{
						U32 litPrice; // 36041
						ZSTD_countLeadingZeros32(U32 val); // 2106
						ZSTD_highbit32(U32 val); // 35812
						ZSTD_fracWeight(U32 rawStat); // 36041
					}
				}
				ZSTD_rawLiteralsCost(const BYTE* literals,
							const U32  litLength,
							const optState_t* optPtr,
							int optLevel);  // 36022
				ZSTD_rawLiteralsCost(const BYTE* literals,
							const U32  litLength,
							const optState_t* optPtr,
							int optLevel);  // 36964
				{
					const ZSTD_optimal_t  prevMatch; // 36968
					{
						int with1literal; // 36981
						int withMoreLiterals; // 36982
						ZSTD_compressedLiterals(const optState_t* optPtr); // 36029
						{
							U32 price; // 36036
							const U32  litPriceMax; // 36037
							U32 u; // 36038
							{
								U32 litPrice; // 36041
								ZSTD_countLeadingZeros32(U32 val); // 2106
								ZSTD_highbit32(U32 val); // 35812
								ZSTD_fracWeight(U32 rawStat); // 36041
							}
						}
						ZSTD_rawLiteralsCost(const BYTE* literals,
									const U32  litLength,
									const optState_t* optPtr,
									int optLevel);  // 36022
						ZSTD_rawLiteralsCost(const BYTE* literals,
									const U32  litLength,
									const optState_t* optPtr,
									int optLevel);  // 36981
						{
							const U32  prev; // 36988
							const Repcodes_t  newReps; // 36989
							{
								const U32  repCode; // 19979
								{
									const U32  currentOffset; // 19981
								}
							}
							ZSTD_updateRep(U32* rep,
									const U32  offBase,
									const U32  ll0);  // 19972
							ZSTD_updateRep(U32* rep,
									const U32  offBase,
									const U32  ll0);  // 20000
							ZSTD_newRep(const U32* rep,
									const U32  offBase,
									const U32  ll0);  // 36989
						}
					}
				}
			}
			{
				const U32  prev; // 37014
				const Repcodes_t  newReps; // 37015
				{
					const U32  repCode; // 19979
					{
						const U32  currentOffset; // 19981
					}
				}
				ZSTD_updateRep(U32* rep,
						const U32  offBase,
						const U32  ll0);  // 19972
				ZSTD_updateRep(U32* rep,
						const U32  offBase,
						const U32  ll0);  // 20000
				ZSTD_newRep(const U32* rep,
						const U32  offBase,
						const U32  ll0);  // 37015
			}
		}
	}
	ZSTD_compressBlock_opt_generic(ZSTD_MatchState_t* ms,
					SeqStore_t* seqStore,
					U32* rep,
					const void* src,
					size_t srcSize,
					const int  optLevel,
					const ZSTD_dictMode_e  dictMode);  // 37215
} /* size: 4868, goto labels: 1, inline expansions: 1 (4748 bytes) */

// <06F648B6> thirdparty/zstd/zstd.c:37220
size_t ZSTD_compressBlock_btopt(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 12 */

// <06F6485A> thirdparty/zstd/zstd.c:37240
// variable: 1
void ZSTD_initStats_ultra(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
	U32 tmpRep; // 37245
} /* size: 0, variables: 1 */

// <06F64790> thirdparty/zstd/zstd.c:37265
size_t ZSTD_compressBlock_btultra(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 12 */

// <06F645F1> thirdparty/zstd/zstd.c:37273
// variable: 1
// function calls: 2
size_t ZSTD_compressBlock_btultra2(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
	const U32  curr; // 37277
	ZSTD_resetSeqStore(SeqStore_t* ssPtr); // 37257
	ZSTD_initStats_ultra(ZSTD_MatchState_t* ms,
				SeqStore_t* seqStore,
				U32* rep,
				const void* src,
				size_t srcSize);  // 37295
} /* size: 238, variables: 1, inline expansions: 2 (146 bytes) */

// <06F64527> thirdparty/zstd/zstd.c:37303
size_t ZSTD_compressBlock_btopt_dictMatchState(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 15 */

// <06F6445D> thirdparty/zstd/zstd.c:37310
size_t ZSTD_compressBlock_btopt_extDict(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 15 */

// <06F64393> thirdparty/zstd/zstd.c:37319
size_t ZSTD_compressBlock_btultra_dictMatchState(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 15 */

// <06F642C9> thirdparty/zstd/zstd.c:37326
size_t ZSTD_compressBlock_btultra_extDict(ZSTD_MatchState_t* ms, SeqStore_t* seqStore, U32* rep, const void* src, size_t srcSize)
{
} /* size: 15 */

// <06F3D954> thirdparty/zstd/zstd.c:37429
// member variables: 2
// struct size: 16
struct buffer_s {
	void * start; /* 0 8 */
	size_t capacity; /* 8 8 */
};

// <06F3D9B3> thirdparty/zstd/zstd.c:37436
// member variables: 6
// struct size: 88
struct ZSTDMT_bufferPool_s {
	pthread_mutex_t poolMutex; /* 0 40 */
	size_t bufferSize; /* 40 8 */
	unsigned int totalBuffers; /* 48 4 */
	unsigned int nbBuffers; /* 52 4 */
	ZSTD_customMem cMem; /* 56 24 */
	Buffer * buffers; /* 80 8 */
};

// <06F64183> thirdparty/zstd/zstd.c:37445
// variable: 1
// function calls: 3
void ZSTDMT_freeBufferPool(ZSTDMT_bufferPool* bufPool)
{
	{
		unsigned int u; // 37450
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 37453
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 37455
	}
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 37458
} /* size: 218, inline expansions: 1 (34 bytes) */

// <06F63F05> thirdparty/zstd/zstd.c:37461
// variables: 3
// function calls: 5
ZSTDMT_bufferPool* ZSTDMT_createBufferPool(unsigned int maxNbBuffers, ZSTD_customMem cMem)
{
	const ZSTDMT_bufferPool* bufPool; // 37463
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 37464
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 37470
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 37467
} /* size: 332, variables: 1, inline expansions: 5 (174 bytes) */

// <06F63EB3> thirdparty/zstd/zstd.c:37483
// variables: 4
size_t ZSTDMT_sizeof_bufferPool(ZSTDMT_bufferPool* bufPool)
{
	const size_t  poolSize; // 37485
	const size_t  arraySize; // 37486
	unsigned int u; // 37487
	size_t totalBufferSize; // 37488
} /* size: 0, variables: 4 */

// <06F63E8A> thirdparty/zstd/zstd.c:37501
void ZSTDMT_setBufferSize(const ZSTDMT_bufferPool* bufPool, const size_t bSize)
{
} /* size: 0 */

// <0701D7E3> thirdparty/zstd/zstd.c:37510
// variables: 3
// function call: 1
ZSTDMT_bufferPool* ZSTDMT_expandBufferPool(ZSTDMT_bufferPool* srcBufPool, unsigned int maxNbBuffers)
{
	{
		const ZSTD_customMem  cMem; // 37516
		const size_t  bSize; // 37517
		ZSTDMT_bufferPool* newBufPool; // 37518
		ZSTDMT_setBufferSize(const ZSTDMT_bufferPool* bufPool,
					const size_t  bSize);  // 37522
	}
} /* size: 127 */

// <06F63E34> thirdparty/zstd/zstd.c:37510
// variables: 3
ZSTDMT_bufferPool* ZSTDMT_expandBufferPool(ZSTDMT_bufferPool* srcBufPool, unsigned int maxNbBuffers)
{
	{
		const ZSTD_customMem  cMem; // 37516
		const size_t  bSize; // 37517
		ZSTDMT_bufferPool* newBufPool; // 37518
	}
} /* size: 0 */

// <06F63C9D> thirdparty/zstd/zstd.c:37531
// variables: 5
// function calls: 2
Buffer ZSTDMT_getBuffer(ZSTDMT_bufferPool* bufPool)
{
	const size_t  bSize; // 37533
	{
		const Buffer  buf; // 37537
		const size_t  availBufferSize; // 37538
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 37549
	}
	{
		Buffer buffer; // 37554
		const void* start; // 37555
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 37555
	}
} /* size: 234, variables: 1 */

// <06F63BC6> thirdparty/zstd/zstd.c:37594
// function call: 1
void ZSTDMT_releaseBuffer(ZSTDMT_bufferPool* bufPool, Buffer buf)
{
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 37609
} /* size: 154, inline expansions: 1 (26 bytes) */

// <06F63BA6> thirdparty/zstd/zstd.c:37627
size_t ZSTDMT_sizeof_seqPool(ZSTDMT_seqPool* seqPool)
{
} /* size: 0 */

// <06F63B79> thirdparty/zstd/zstd.c:37632
// variable: 1
RawSeqStore_t bufferToSeq(Buffer buffer)
{
	RawSeqStore_t seq; // 37634
} /* size: 0, variables: 1 */

// <06F63B4C> thirdparty/zstd/zstd.c:37640
// variable: 1
Buffer seqToBuffer(RawSeqStore_t seq)
{
	Buffer buffer; // 37642
} /* size: 0, variables: 1 */

// <06F63B2C> thirdparty/zstd/zstd.c:37648
RawSeqStore_t ZSTDMT_getSeq(ZSTDMT_seqPool* seqPool)
{
} /* size: 0 */

// <06F63B03> thirdparty/zstd/zstd.c:37663
void ZSTDMT_releaseSeq(ZSTDMT_seqPool* seqPool, RawSeqStore_t seq)
{
} /* size: 0 */

// <06F63ADA> thirdparty/zstd/zstd.c:37668
void ZSTDMT_setNbSeq(const ZSTDMT_seqPool* seqPool, const size_t nbSeq)
{
} /* size: 0 */

// <06F63AA0> thirdparty/zstd/zstd.c:37673
// variable: 1
ZSTDMT_seqPool* ZSTDMT_createSeqPool(unsigned int nbWorkers, ZSTD_customMem cMem)
{
	const ZSTDMT_seqPool* seqPool; // 37675
} /* size: 0, variables: 1 */

// <06F63A84> thirdparty/zstd/zstd.c:37681
void ZSTDMT_freeSeqPool(ZSTDMT_seqPool* seqPool)
{
} /* size: 0 */

// <06F63A57> thirdparty/zstd/zstd.c:37686
ZSTDMT_seqPool* ZSTDMT_expandSeqPool(ZSTDMT_seqPool* pool, U32 nbWorkers)
{
} /* size: 0 */

// <07099DA3> thirdparty/zstd/zstd.c:37704
// variable: 1
// function calls: 2
void ZSTDMT_freeCCtxPool(ZSTDMT_CCtxPool* pool)
{
	{
		int cid; // 37709
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 37712
	}
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 37714
} /* size: 164, inline expansions: 1 (29 bytes) */

// <06F63A2C> thirdparty/zstd/zstd.c:37704
// variable: 1
void ZSTDMT_freeCCtxPool(ZSTDMT_CCtxPool* pool)
{
	{
		int cid; // 37709
	}
} /* size: 0 */

// <06F63776> thirdparty/zstd/zstd.c:37719
// variables: 3
// function calls: 6
ZSTDMT_CCtxPool* ZSTDMT_createCCtxPool(int nbWorkers, ZSTD_customMem cMem)
{
	const ZSTDMT_CCtxPool* cctxPool; // 37722
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 37723
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 37731
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 37727
	ZSTDMT_freeCCtxPool(ZSTDMT_CCtxPool* pool); // 37738
} /* size: 372, variables: 1, inline expansions: 6 (174 bytes) */

// <06F6373A> thirdparty/zstd/zstd.c:37744
// variable: 1
ZSTDMT_CCtxPool* ZSTDMT_expandCCtxPool(ZSTDMT_CCtxPool* srcPool, int nbWorkers)
{
	{
		const ZSTD_customMem  cMem; // 37750
	}
} /* size: 0 */

// <06F636D9> thirdparty/zstd/zstd.c:37757
// variables: 5
size_t ZSTDMT_sizeof_CCtxPool(ZSTDMT_CCtxPool* cctxPool)
{
	{
		const unsigned int  nbWorkers; // 37760
		const size_t  poolSize; // 37761
		const size_t  arraySize; // 37762
		size_t totalCCtxSize; // 37763
		unsigned int u; // 37764
	}
} /* size: 0 */

// <06F636AA> thirdparty/zstd/zstd.c:37774
// variable: 1
ZSTD_CCtx* ZSTDMT_getCCtx(ZSTDMT_CCtxPool* cctxPool)
{
	{
		const ZSTD_CCtx* cctx; // 37780
	}
} /* size: 0 */

// <06F63681> thirdparty/zstd/zstd.c:37789
void ZSTDMT_releaseCCtx(ZSTDMT_CCtxPool* pool, ZSTD_CCtx* cctx)
{
} /* size: 0 */

// <06F3DAB4> thirdparty/zstd/zstd.c:37805
// member variables: 2
// struct size: 16
typedef struct {
	const void * start; /* 0 8 */
	size_t size; /* 8 8 */
} Range;

// <06F3DAF5> thirdparty/zstd/zstd.c:37810
// member variables: 9
// struct size: 2,648
typedef struct {
	pthread_mutex_t mutex; /* 0 40 */
	pthread_cond_t cond; /* 40 48 */
	ZSTD_CCtx_params params; /* 88 224 */
	ldmState_t ldmState; /* 312 2112 */
	XXH_NAMESPACEXXH64_state_t xxhState; /* 2424 88 */
	unsigned int nextJobID; /* 2512 4 */
	pthread_mutex_t ldmWindowMutex; /* 2520 40 */
	pthread_cond_t ldmWindowCond; /* 2560 48 */
	ZSTD_window_t ldmWindow; /* 2608 40 */
} SerialState;

// <06F635B4> thirdparty/zstd/zstd.c:37827
// variables: 7
int ZSTDMT_serialState_reset(SerialState* serialState, ZSTDMT_seqPool* seqPool, ZSTD_CCtx_params params, size_t jobSize, const void* dict, const size_t dictSize, ZSTD_dictContentType_e dictContentType)
{
	{
		ZSTD_customMem cMem; // 37847
		const unsigned int  hashLog; // 37848
		const size_t  hashSize; // 37849
		const unsigned int  bucketLog; // 37850
		const unsigned int  prevBucketLog; // 37852
		const size_t  numBuckets; // 37855
		{
			const BYTE* dictEnd; // 37879
		}
	}
} /* size: 0 */

// <06F63587> thirdparty/zstd/zstd.c:37897
// variable: 1
int ZSTDMT_serialState_init(SerialState* serialState)
{
	int initError; // 37899
} /* size: 0, variables: 1 */

// <06F6355E> thirdparty/zstd/zstd.c:37908
// variable: 1
void ZSTDMT_serialState_free(SerialState* serialState)
{
	ZSTD_customMem cMem; // 37910
} /* size: 0, variables: 1 */

// <06F63506> thirdparty/zstd/zstd.c:37920
// variable: 1
void ZSTDMT_serialState_genSequences(SerialState* serialState, RawSeqStore_t* seqStore, Range src, unsigned int jobID)
{
	{
		size_t error; // 37934
	}
} /* size: 0 */

// <06F634CA> thirdparty/zstd/zstd.c:37963
void ZSTDMT_serialState_applySequences(const SerialState* serialState, ZSTD_CCtx* jobCCtx, const RawSeqStore_t* seqStore)
{
} /* size: 0 */

// <06F63494> thirdparty/zstd/zstd.c:37975
void ZSTDMT_serialState_ensureFinished(SerialState* serialState, unsigned int jobID, size_t cSize)
{
} /* size: 0 */

// <06F3DBCB> thirdparty/zstd/zstd.c:38001
// member variables: 19
// struct size: 456
typedef struct {
	size_t consumed; /* 0 8 */
	size_t cSize; /* 8 8 */
	pthread_mutex_t job_mutex; /* 16 40 */
	pthread_cond_t job_cond; /* 56 48 */
	ZSTDMT_CCtxPool * cctxPool; /* 104 8 */
	ZSTDMT_bufferPool * bufPool; /* 112 8 */
	ZSTDMT_seqPool * seqPool; /* 120 8 */
	SerialState * serial; /* 128 8 */
	Buffer dstBuff; /* 136 16 */
	Range prefix; /* 152 16 */
	Range src; /* 168 16 */
	unsigned int jobID; /* 184 4 */
	unsigned int firstJob; /* 188 4 */
	unsigned int lastJob; /* 192 4 */
	ZSTD_CCtx_params params; /* 200 224 */
	const ZSTD_CDict * cdict; /* 424 8 */
	long long unsigned int fullFrameSize; /* 432 8 */
	size_t dstFlushed; /* 440 8 */
	unsigned int frameChecksumNeeded; /* 448 4 */
} ZSTDMT_jobDescription;

// <06F623C4> thirdparty/zstd/zstd.c:38032
// variables: 31
// function calls: 22
void ZSTDMT_compressionJob(void* jobDescription)
{

_endJob: // 38144
	const ZSTDMT_jobDescription* job; // 38034
	ZSTD_CCtx_params jobParams; // 38035
	const ZSTD_CCtx* cctx; // 38036
	RawSeqStore_t rawSeqStore; // 38037
	Buffer dstBuff; // 38038
	size_t lastCBlockSize; // 38039
	{
		const size_t  initError; // 38068
		{
			const size_t  err_code; // 28249
		}
		ZSTD_compressBegin_advanced_internal(ZSTD_CCtx* cctx,
							const void* dict,
							size_t dictSize,
							ZSTD_dictContentType_e dictContentType,
							ZSTD_dictTableLoadMethod_e dtlm,
							const ZSTD_CDict* cdict,
							const ZSTD_CCtx_params* params,
							long long unsigned int pledgedSrcSize);  // 28239
		ZSTD_compressBegin_advanced_internal(ZSTD_CCtx* cctx,
							const void* dict,
							size_t dictSize,
							ZSTD_dictContentType_e dictContentType,
							ZSTD_dictTableLoadMethod_e dtlm,
							const ZSTD_CDict* cdict,
							const ZSTD_CCtx_params* params,
							long long unsigned int pledgedSrcSize);  // 38068
	}
	{
		const U64  pledgedSrcSize; // 38072
		{
			const size_t  forceWindowError; // 38073
		}
		{
			const size_t  err; // 38077
		}
		{
			const size_t  initError; // 38081
			{
				const size_t  err_code; // 28249
			}
			ZSTD_compressBegin_advanced_internal(ZSTD_CCtx* cctx,
								const void* dict,
								size_t dictSize,
								ZSTD_dictContentType_e dictContentType,
								ZSTD_dictTableLoadMethod_e dtlm,
								const ZSTD_CDict* cdict,
								const ZSTD_CCtx_params* params,
								long long unsigned int pledgedSrcSize);  // 28239
			ZSTD_compressBegin_advanced_internal(ZSTD_CCtx* cctx,
								const void* dict,
								size_t dictSize,
								ZSTD_dictContentType_e dictContentType,
								ZSTD_dictTableLoadMethod_e dtlm,
								const ZSTD_CDict* cdict,
								const ZSTD_CCtx_params* params,
								long long unsigned int pledgedSrcSize);  // 38081
		}
	}
	{
		const size_t  hSize; // 38093
		{
			const size_t  err_code; // 27762
		}
		ZSTD_compressContinue_internal(ZSTD_CCtx* cctx,
						void* dst,
						size_t dstCapacity,
						const void* src,
						size_t srcSize,
						U32 frame,
						U32 lastFrameChunk);  // 27812
		ZSTD_compressContinue_public(ZSTD_CCtx* cctx,
						void* dst,
						size_t dstCapacity,
						const void* src,
						size_t srcSize);  // 38093
		ZSTD_invalidateRepCodes(ZSTD_CCtx* cctx); // 38096
	}
	{
		const size_t  chunkSize; // 38100
		const int  nbChunks; // 38101
		const BYTE* ip; // 38102
		const BYTE* ostart; // 38103
		BYTE* op; // 38104
		BYTE* oend; // 38105
		int chunkNb; // 38106
		{
			const size_t  cSize; // 38111
			ZSTD_compressContinue_public(ZSTD_CCtx* cctx,
							void* dst,
							size_t dstCapacity,
							const void* src,
							size_t srcSize);  // 38111
		}
		{
			const size_t  lastBlockSize1; // 38128
			const size_t  lastBlockSize; // 38129
			const size_t  cSize; // 38130
			ZSTD_compressContinue_public(ZSTD_CCtx* cctx,
							void* dst,
							size_t dstCapacity,
							const void* src,
							size_t srcSize);  // 38132
		}
	}
	{
		const ZSTD_CCtx* cctx; // 37780
	}
	ZSTDMT_getCCtx(ZSTDMT_CCtxPool* cctxPool); // 38036
	bufferToSeq(Buffer buffer); // 37653
	ZSTDMT_getSeq(ZSTDMT_seqPool* seqPool); // 38037
	{
		size_t error; // 37934
		{
			const size_t  distanceFromBase; // 20522
		}
		{
			const size_t  highInputIdx; // 20537
			const U32  lowLimitMax; // 20538
		}
		ZSTD_window_update(ZSTD_window_t* window,
					const void* src,
					size_t srcSize,
					int forceNonContiguous);  // 20508
		ZSTD_window_update(ZSTD_window_t* window,
					const void* src,
					size_t srcSize,
					int forceNonContiguous);  // 37939
	}
	ZSTDMT_serialState_genSequences(SerialState* serialState,
					RawSeqStore_t* seqStore,
					Range src,
					unsigned int jobID);  // 38065
	ZSTD_referenceExternalSequences(ZSTD_CCtx* cctx,
					rawSeq* seq,
					size_t nbSeq);  // 37971
	ZSTDMT_serialState_applySequences(const SerialState* serialState,
						ZSTD_CCtx* jobCCtx,
						const RawSeqStore_t* seqStore);  // 38090
	ZSTD_window_clear(ZSTD_window_t* window); // 37986
	ZSTDMT_serialState_ensureFinished(SerialState* serialState,
						unsigned int jobID,
						size_t cSize);  // 38145
	seqToBuffer(RawSeqStore_t seq); // 37665
	ZSTDMT_releaseSeq(ZSTDMT_seqPool* seqPool,
				RawSeqStore_t seq);  // 38150
	ZSTDMT_releaseCCtx(ZSTDMT_CCtxPool* pool,
				ZSTD_CCtx* cctx);  // 38151
} /* size: 2961, variables: 6, goto labels: 1, inline expansions: 11 (1300 bytes) */

// <06F3DD4B> thirdparty/zstd/zstd.c:38166
// member variables: 3
// struct size: 40
typedef struct {
	Range prefix; /* 0 16 */
	Buffer buffer; /* 16 16 */
	size_t filled; /* 32 8 */
} InBuff_t;

// <06F3DD97> thirdparty/zstd/zstd.c:38172
// member variables: 3
// struct size: 24
typedef struct {
	BYTE * buffer; /* 0 8 */
	size_t capacity; /* 8 8 */
	size_t pos; /* 16 8 */
} RoundBuff_t;

// <06F3DE0E> thirdparty/zstd/zstd.c:38199
// member variables: 3
// struct size: 24
typedef struct {
	U64 hash; /* 0 8 */
	U64 hitMask; /* 8 8 */
	U64 primePower; /* 16 8 */
} RSyncState_t;

// <06F3C128> thirdparty/zstd/zstd.c:38205
// member variables: 25
// struct size: 3,120
struct ZSTDMT_CCtx_s {
	POOL_ctx * factory; /* 0 8 */
	ZSTDMT_jobDescription * jobs; /* 8 8 */
	ZSTDMT_bufferPool * bufPool; /* 16 8 */
	ZSTDMT_CCtxPool * cctxPool; /* 24 8 */
	ZSTDMT_seqPool * seqPool; /* 32 8 */
	ZSTD_CCtx_params params; /* 40 224 */
	size_t targetSectionSize; /* 264 8 */
	size_t targetPrefixSize; /* 272 8 */
	int jobReady; /* 280 4 */
	InBuff_t inBuff; /* 288 40 */
	RoundBuff_t roundBuff; /* 328 24 */
	SerialState serial; /* 352 2648 */
	RSyncState_t rsync; /* 3000 24 */
	unsigned int jobIDMask; /* 3024 4 */
	unsigned int doneJobID; /* 3028 4 */
	unsigned int nextJobID; /* 3032 4 */
	unsigned int frameEnded; /* 3036 4 */
	unsigned int allJobsCompleted; /* 3040 4 */
	long long unsigned int frameContentSize; /* 3048 8 */
	long long unsigned int consumed; /* 3056 8 */
	long long unsigned int produced; /* 3064 8 */
	ZSTD_customMem cMem; /* 3072 24 */
	ZSTD_CDict * cdictLocal; /* 3096 8 */
	const ZSTD_CDict * cdict; /* 3104 8 */
	unsigned int providedFactory:1; /* 3112: 0 4 */
};

// <0701B956> thirdparty/zstd/zstd.c:38233
// variable: 1
// function call: 1
void ZSTDMT_freeJobsTable(ZSTDMT_jobDescription* jobTable, U32 nbJobs, ZSTD_customMem cMem)
{
	U32 jobNb; // 38235
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 38241
} /* size: 150, variables: 1, inline expansions: 1 (37 bytes) */

// <06F62381> thirdparty/zstd/zstd.c:38233
// variable: 1
void ZSTDMT_freeJobsTable(ZSTDMT_jobDescription* jobTable, U32 nbJobs, ZSTD_customMem cMem)
{
	U32 jobNb; // 38235
} /* size: 0, variables: 1 */

// <06F6212F> thirdparty/zstd/zstd.c:38247
// variables: 6
// function calls: 4
ZSTDMT_jobDescription* ZSTDMT_createJobsTable(U32* nbJobsPtr, ZSTD_customMem cMem)
{
	const U32  nbJobsLog2; // 38249
	const U32  nbJobs; // 38250
	U32 jobNb; // 38251
	const ZSTDMT_jobDescription* jobTable; // 38252
	int initError; // 38254
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 38249
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 38253
} /* size: 256, variables: 5, inline expansions: 4 (78 bytes) */

// <06F620F5> thirdparty/zstd/zstd.c:38268
// variable: 1
size_t ZSTDMT_expandJobsTable(ZSTDMT_CCtx* mtctx, U32 nbWorkers)
{
	U32 nbJobs; // 38269
} /* size: 0, variables: 1 */

// <06F620C2> thirdparty/zstd/zstd.c:38284
size_t ZSTDMT_CCtxParam_setNbWorkers(ZSTD_CCtx_params* params, unsigned int nbWorkers)
{
} /* size: 0 */

// <06F61CB7> thirdparty/zstd/zstd.c:38289
// variables: 4
// function calls: 7
ZSTDMT_CCtx* ZSTDMT_createCCtx_advanced_internal(unsigned int nbWorkers, ZSTD_customMem cMem, ZSTD_threadPool* pool)
{
	ZSTDMT_CCtx* mtctx; // 38291
	U32 nbJobs; // 38292
	int initError; // 38293
	{
		const void* ptr; // 7516
	}
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 7511
	ZSTD_customCalloc(size_t size,
				ZSTD_customMem customMem);  // 38302
	ZSTDMT_CCtxParam_setNbWorkers(ZSTD_CCtx_params* params,
					unsigned int nbWorkers);  // 38304
	ZSTDMT_setBufferSize(const ZSTDMT_bufferPool* bufPool,
				const size_t  bSize);  // 37670
	ZSTDMT_setNbSeq(const ZSTDMT_seqPool* seqPool,
			const size_t  nbSeq);  // 37677
	ZSTDMT_createSeqPool(unsigned int nbWorkers,
				ZSTD_customMem cMem);  // 38320
	ZSTDMT_serialState_init(SerialState* serialState); // 38321
} /* size: 768, variables: 3, inline expansions: 7 (283 bytes) */

// <06F61C78> thirdparty/zstd/zstd.c:38331
ZSTDMT_CCtx* ZSTDMT_createCCtx_advanced(unsigned int nbWorkers, ZSTD_customMem cMem, ZSTD_threadPool* pool)
{
} /* size: 0 */

// <06F61BE5> thirdparty/zstd/zstd.c:38346
// variables: 3
void ZSTDMT_releaseAllJobResources(ZSTDMT_CCtx* mtctx)
{
	unsigned int jobID; // 38348
	{
		const pthread_mutex_t  mutex; // 38352
		const pthread_cond_t  cond; // 38353
	}
} /* size: 400, variables: 1 */

// <06F61B6B> thirdparty/zstd/zstd.c:38368
// variable: 1
void ZSTDMT_waitForAllJobsCompleted(ZSTDMT_CCtx* mtctx)
{
	{
		const unsigned int  jobID; // 38372
	}
} /* size: 140 */

// <06F6176B> thirdparty/zstd/zstd.c:38383
// function calls: 10
size_t ZSTDMT_freeCCtx(ZSTDMT_CCtx* mtctx)
{
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 38241
	ZSTDMT_freeJobsTable(ZSTDMT_jobDescription* jobTable,
				U32 nbJobs,
				ZSTD_customMem cMem);  // 38389
	ZSTDMT_freeCCtxPool(ZSTDMT_CCtxPool* pool); // 38391
	ZSTDMT_freeSeqPool(ZSTDMT_seqPool* seqPool); // 38392
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 37915
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 37916
	ZSTDMT_serialState_free(SerialState* serialState); // 38393
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 38396
	ZSTD_customFree(void* ptr,
			ZSTD_customMem customMem);  // 38397
	POOL_free(POOL_ctx* ctx); // 38387
} /* size: 581, inline expansions: 10 (541 bytes) */

// <06F61515> thirdparty/zstd/zstd.c:38401
// variables: 5
// function calls: 5
size_t ZSTDMT_sizeof_CCtx(ZSTDMT_CCtx* mtctx)
{
	POOL_sizeof(const POOL_ctx* ctx); // 38405
	ZSTDMT_sizeof_bufferPool(ZSTDMT_bufferPool* bufPool); // 38406
	{
		const unsigned int  nbWorkers; // 37760
		const size_t  poolSize; // 37761
		const size_t  arraySize; // 37762
		size_t totalCCtxSize; // 37763
		unsigned int u; // 37764
	}
	ZSTDMT_sizeof_CCtxPool(ZSTDMT_CCtxPool* cctxPool); // 38408
	ZSTDMT_sizeof_bufferPool(ZSTDMT_bufferPool* bufPool); // 37629
	ZSTDMT_sizeof_seqPool(ZSTDMT_seqPool* seqPool); // 38409
} /* size: 456, inline expansions: 5 (385 bytes) */

// <06F614D9> thirdparty/zstd/zstd.c:38417
// variable: 1
size_t ZSTDMT_resize(ZSTDMT_CCtx* mtctx, unsigned int nbWorkers)
{
	{
		const size_t  err_code; // 38420
	}
} /* size: 0 */

// <06F61405> thirdparty/zstd/zstd.c:38435
// variables: 3
void ZSTDMT_updateCParams_whileCompressing(ZSTDMT_CCtx* mtctx, const ZSTD_CCtx_params* cctxParams)
{
	const U32  saved_wlog; // 38437
	const int  compressionLevel; // 38438
	{
		ZSTD_compressionParameters cParams; // 38442
	}
} /* size: 125, variables: 2 */

// <06F612B9> thirdparty/zstd/zstd.c:38452
// variables: 8
ZSTD_frameProgression ZSTDMT_getFrameProgression(ZSTDMT_CCtx* mtctx)
{
	ZSTD_frameProgression fps; // 38454
	{
		unsigned int jobNb; // 38461
		unsigned int lastJobNb; // 38462
		{
			const unsigned int  wJobID; // 38466
			ZSTDMT_jobDescription* jobPtr; // 38467
			{
				const size_t  cResult; // 38469
				const size_t  produced; // 38470
				const size_t  flushed; // 38471
			}
		}
	}
} /* size: 336, variables: 1 */

// <06F6118B> thirdparty/zstd/zstd.c:38486
// variables: 7
size_t ZSTDMT_toFlushNow(ZSTDMT_CCtx* mtctx)
{
	size_t toFlush; // 38488
	const unsigned int  jobID; // 38489
	{
		const unsigned int  wJobID; // 38494
		const ZSTDMT_jobDescription* jobPtr; // 38495
		{
			const size_t  cResult; // 38497
			const size_t  produced; // 38498
			const size_t  flushed; // 38499
		}
	}
} /* size: 145, variables: 2 */

// <06F6115E> thirdparty/zstd/zstd.c:38523
// variable: 1
unsigned int ZSTDMT_computeTargetJobLog(const ZSTD_CCtx_params* params)
{
	unsigned int jobLog; // 38525
} /* size: 0, variables: 1 */

// <06F6113E> thirdparty/zstd/zstd.c:38537
int ZSTDMT_overlapLog_default(ZSTD_strategy strat)
{
} /* size: 0 */

// <06F61111> thirdparty/zstd/zstd.c:38558
int ZSTDMT_overlapLog(int ovlog, ZSTD_strategy strat)
{
} /* size: 0 */

// <06F610D1> thirdparty/zstd/zstd.c:38565
// variables: 2
size_t ZSTDMT_computeOverlapSize(const ZSTD_CCtx_params* params)
{
	const int  overlapRLog; // 38567
	int ovLog; // 38568
} /* size: 0, variables: 2 */

// <06F60213> thirdparty/zstd/zstd.c:38588
// variables: 21
// function calls: 36
size_t ZSTDMT_initCStream_internal(ZSTDMT_CCtx* mtctx, const void* dict, size_t dictSize, ZSTD_dictContentType_e dictContentType, const ZSTD_CDict* cdict, ZSTD_CCtx_params params, long long unsigned int pledgedSrcSize)
{
	{
		const size_t  err_code; // 38603
		POOL_resize(POOL_ctx* ctx,
				size_t numThreads);  // 7847
		POOL_resize(POOL_ctx* ctx,
				size_t numThreads);  // 38419
		{
			const size_t  err_code; // 38420
			ZSTDMT_expandJobsTable(ZSTDMT_CCtx* mtctx,
						U32 nbWorkers);  // 38420
		}
		ZSTDMT_expandBufferPool(ZSTDMT_bufferPool* srcBufPool,
					unsigned int maxNbBuffers);  // 38421
		{
			const ZSTD_customMem  cMem; // 37750
			ZSTDMT_freeCCtxPool(ZSTDMT_CCtxPool* pool); // 37751
		}
		ZSTDMT_expandCCtxPool(ZSTDMT_CCtxPool* srcPool,
					int nbWorkers);  // 38423
		ZSTDMT_expandBufferPool(ZSTDMT_bufferPool* srcBufPool,
					unsigned int maxNbBuffers);  // 37688
		ZSTDMT_expandSeqPool(ZSTDMT_seqPool* pool,
					U32 nbWorkers);  // 38425
		ZSTDMT_CCtxParam_setNbWorkers(ZSTD_CCtx_params* params,
						unsigned int nbWorkers);  // 38427
		ZSTDMT_resize(ZSTDMT_CCtx* mtctx,
				unsigned int nbWorkers);  // 38603
	}
	{
		const U32  jobSizeKB; // 38638
		const U32  rsyncBits; // 38639
		ZSTD_countLeadingZeros32(U32 val); // 2106
		ZSTD_highbit32(U32 val); // 38639
		ZSTD_ipow(U64 base,
				U64 exponent);  // 20163
		ZSTD_rollingHash_primePower(U32 length); // 38646
	}
	{
		const size_t  windowSize; // 38654
		const size_t  nbSlackBuffers; // 38661
		const size_t  slackSize; // 38662
		const size_t  nbWorkers; // 38664
		const size_t  sectionsSize; // 38665
		const size_t  capacity; // 38666
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 38669
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 38670
	}
	ZSTDMT_overlapLog_default(ZSTD_strategy strat); // 38561
	ZSTDMT_overlapLog(int ovlog,
				ZSTD_strategy strat);  // 38567
	ZSTD_cycleLog(U32 hashLog,
			ZSTD_strategy strat);  // 38530
	ZSTDMT_computeTargetJobLog(const ZSTD_CCtx_params* params); // 38575
	ZSTDMT_computeOverlapSize(const ZSTD_CCtx_params* params); // 38628
	ZSTDMT_computeTargetJobLog(const ZSTD_CCtx_params* params); // 38632
	ZSTDMT_setBufferSize(const ZSTDMT_bufferPool* bufPool,
				const size_t  bSize);  // 38651
	{
		ZSTD_customMem cMem; // 37847
		const unsigned int  hashLog; // 37848
		const size_t  hashSize; // 37849
		const unsigned int  bucketLog; // 37850
		const unsigned int  prevBucketLog; // 37852
		const size_t  numBuckets; // 37855
		ZSTDMT_setBufferSize(const ZSTDMT_bufferPool* bufPool,
					const size_t  bSize);  // 37670
		ZSTDMT_setNbSeq(const ZSTDMT_seqPool* seqPool,
				const size_t  nbSeq);  // 37857
		ZSTD_ldm_getMaxNbSeq(ldmParams_t params,
					size_t maxChunkSize);  // 37857
		ZSTD_window_init(ZSTD_window_t* window); // 37859
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 37862
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 37863
		{
			const BYTE* dictEnd; // 37879
			{
				const size_t  distanceFromBase; // 20522
			}
			{
				const size_t  highInputIdx; // 20537
				const U32  lowLimitMax; // 20538
			}
			ZSTD_window_update(ZSTD_window_t* window,
						const void* src,
						size_t srcSize,
						int forceNonContiguous);  // 20508
			ZSTD_window_update(ZSTD_window_t* window,
						const void* src,
						size_t srcSize,
						int forceNonContiguous);  // 37880
		}
		ZSTD_customFree(void* ptr,
				ZSTD_customMem customMem);  // 37866
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 37867
	}
	memset(void* __dest,
		int __ch,
		size_t __len);  // 11842
	XXH_INLINE_XXH64_reset(XXH_NAMESPACEXXH64_state_t* statePtr,
				XXH64_hash_t seed);  // 37845
	ZSTDMT_serialState_reset(SerialState* serialState,
				ZSTDMT_seqPool* seqPool,
				ZSTD_CCtx_params params,
				size_t jobSize,
				const void* dict,
				const size_t  dictSize,
				ZSTD_dictContentType_e dictContentType);  // 38710
} /* size: 4420, inline expansions: 10 (2358 bytes) */

// <06F601F7> thirdparty/zstd/zstd.c:38724
void ZSTDMT_writeLastEmptyBlock(ZSTDMT_jobDescription* job)
{
} /* size: 0 */

// <06F60185> thirdparty/zstd/zstd.c:38742
// variables: 4
size_t ZSTDMT_createCompressionJob(ZSTDMT_CCtx* mtctx, size_t srcSize, ZSTD_EndDirective endOp)
{
	const unsigned int  jobID; // 38744
	const int  endFrame; // 38745
	{
		const BYTE* src; // 38754
		{
			const size_t  newPrefixSize; // 38783
		}
	}
} /* size: 0, variables: 2 */

// <06F600E6> thirdparty/zstd/zstd.c:38827
// variables: 6
size_t ZSTDMT_flushProduced(ZSTDMT_CCtx* mtctx, ZSTD_outBuffer* output, unsigned int blockToFlush, ZSTD_EndDirective end)
{
	const unsigned int  wJobID; // 38829
	{
		size_t cSize; // 38850
		const size_t  srcConsumed; // 38851
		const size_t  srcSize; // 38852
		{
			const U32  checksum; // 38865
		}
		{
			const size_t  toFlush; // 38874
		}
	}
} /* size: 0, variables: 1 */

// <06F6005A> thirdparty/zstd/zstd.c:38918
// variables: 8
Range ZSTDMT_getInputDataInUse(ZSTDMT_CCtx* mtctx)
{
	const unsigned int  firstJobID; // 38920
	const unsigned int  lastJobID; // 38921
	unsigned int jobID; // 38922
	size_t roundBuffCapacity; // 38925
	size_t nbJobs1stRoundMin; // 38926
	{
		const unsigned int  wJobID; // 38930
		size_t consumed; // 38931
		{
			Range range; // 38938
		}
	}
} /* size: 0, variables: 5 */

// <06F5FFF7> thirdparty/zstd/zstd.c:38954
// variables: 4
int ZSTDMT_isOverlapped(Buffer buffer, Range range)
{
	const BYTE* bufferStart; // 38956
	const BYTE* rangeStart; // 38957
	{
		const BYTE* bufferEnd; // 38963
		const BYTE* rangeEnd; // 38964
	}
} /* size: 0, variables: 2 */

// <06F5FFB0> thirdparty/zstd/zstd.c:38974
// variables: 2
int ZSTDMT_doesOverlapWindow(Buffer buffer, ZSTD_window_t window)
{
	Range extDict; // 38976
	Range prefix; // 38977
} /* size: 0, variables: 2 */

// <06F5FE00> thirdparty/zstd/zstd.c:38996
// variables: 5
// function calls: 3
void ZSTDMT_waitForLdmComplete(ZSTDMT_CCtx* mtctx, Buffer buffer)
{
	{
		pthread_mutex_t* mutex; // 38999
		{
			const BYTE* bufferEnd; // 38963
			const BYTE* rangeEnd; // 38964
		}
		ZSTDMT_isOverlapped(Buffer buffer,
					Range range);  // 38992
		{
			const BYTE* bufferEnd; // 38963
			const BYTE* rangeEnd; // 38964
		}
		ZSTDMT_isOverlapped(Buffer buffer,
					Range range);  // 38993
		ZSTDMT_doesOverlapWindow(Buffer buffer,
					ZSTD_window_t window);  // 39005
	}
} /* size: 250 */

// <06F5FD90> thirdparty/zstd/zstd.c:39019
// variables: 6
int ZSTDMT_tryGetInputRange(ZSTDMT_CCtx* mtctx)
{
	const Range  inUse; // 39021
	const size_t  spaceLeft; // 39022
	const size_t  spaceNeeded; // 39023
	Buffer buffer; // 39024
	{
		const BYTE* start; // 39034
		const size_t  prefixSize; // 39035
	}
} /* size: 0, variables: 4 */

// <06F3DE70> thirdparty/zstd/zstd.c:39073
// member variables: 2
// struct size: 16
typedef struct {
	size_t toLoad; /* 0 8 */
	int flush; /* 8 4 */
} SyncPoint;

// <06F5FCF9> thirdparty/zstd/zstd.c:39085
// variables: 8
SyncPoint findSynchronizationPoint(const ZSTDMT_CCtx* mtctx, const ZSTD_inBuffer input)
{
	const BYTE* istart; // 39087
	const U64  primePower; // 39088
	const U64  hitMask; // 39089
	SyncPoint syncPoint; // 39091
	U64 hash; // 39092
	const BYTE* prev; // 39093
	size_t pos; // 39094
	{
		const BYTE  toRemove; // 39162
	}
} /* size: 0, variables: 7 */

// <06F5FCC1> thirdparty/zstd/zstd.c:39181
// variable: 1
size_t ZSTDMT_nextInputSizeHint(const ZSTDMT_CCtx* mtctx)
{
	size_t hintInSize; // 39183
} /* size: 0, variables: 1 */

// <06F5F0BB> thirdparty/zstd/zstd.c:39192
// variables: 23
// function calls: 31
size_t ZSTDMT_compressStream_generic(ZSTDMT_CCtx* mtctx, ZSTD_outBuffer* output, ZSTD_inBuffer* input, ZSTD_EndDirective endOp)
{
	unsigned int forwardInputProgress; // 39197
	{
		const SyncPoint  syncPoint; // 39223
		ZSTD_rollingHash_append(U64 hash,
					const void* buf,
					size_t size);  // 20154
		ZSTD_rollingHash_compute(const void* buf,
					size_t size);  // 39124
		{
			const BYTE  toRemove; // 39162
			ZSTD_rollingHash_rotate(U64 hash,
						BYTE toRemove,
						BYTE toAdd,
						U64 primePower);  // 39168
		}
		ZSTD_rollingHash_append(U64 hash,
					const void* buf,
					size_t size);  // 20154
		ZSTD_rollingHash_compute(const void* buf,
					size_t size);  // 39140
		ZSTD_rollingHash_append(U64 hash,
					const void* buf,
					size_t size);  // 20154
		ZSTD_rollingHash_compute(const void* buf,
					size_t size);  // 39128
		ZSTD_rollingHash_append(U64 hash,
					const void* buf,
					size_t size);  // 39129
		findSynchronizationPoint(const ZSTDMT_CCtx* mtctx,
					const ZSTD_inBuffer  input);  // 39223
	}
	{
		const size_t  jobSize; // 39251
		{
			const size_t  err_code; // 39253
			{
				const BYTE* src; // 38754
				{
					const size_t  newPrefixSize; // 38783
				}
				{
					const U32  cBlockHeader24; // 27728
					MEM_write16(void* memPtr,
							U16 value);  // 1279
					MEM_writeLE16(void* memPtr,
							U16 val);  // 1294
					MEM_writeLE24(void* memPtr,
							U32 val);  // 27729
				}
				ZSTD_writeLastEmptyBlock(void* dst,
							size_t dstCapacity);  // 38737
				ZSTDMT_writeLastEmptyBlock(ZSTDMT_jobDescription* job); // 38798
			}
			ZSTDMT_createCompressionJob(ZSTDMT_CCtx* mtctx,
							size_t srcSize,
							ZSTD_EndDirective endOp);  // 39253
		}
	}
	{
		const size_t  remainingToFlush; // 39257
		{
			size_t cSize; // 38850
			const size_t  srcConsumed; // 38851
			const size_t  srcSize; // 38852
			{
				const U32  checksum; // 38865
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11682
				XXH64_mergeRound(xxh_u64 acc,
						xxh_u64 val);  // 11909
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11682
				XXH64_mergeRound(xxh_u64 acc,
						xxh_u64 val);  // 11911
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11682
				XXH64_mergeRound(xxh_u64 acc,
						xxh_u64 val);  // 11910
				XXH64_round(xxh_u64 acc,
						xxh_u64 input);  // 11682
				XXH64_mergeRound(xxh_u64 acc,
						xxh_u64 val);  // 11912
				XXH_INLINE_XXH64_digest(const XXH_NAMESPACEXXH64_state_t* state); // 38865
				MEM_write32(void* memPtr,
						U32 value);  // 1309
				MEM_writeLE32(void* memPtr,
						U32 val32);  // 38867
			}
			{
				const size_t  toFlush; // 38874
			}
		}
		ZSTDMT_flushProduced(ZSTDMT_CCtx* mtctx,
					ZSTD_outBuffer* output,
					unsigned int blockToFlush,
					ZSTD_EndDirective end);  // 39257
	}
	{
		const unsigned int  wJobID; // 38930
		size_t consumed; // 38931
		{
			Range range; // 38938
		}
	}
	ZSTDMT_getInputDataInUse(ZSTDMT_CCtx* mtctx); // 39021
	{
		const BYTE* start; // 39034
		const size_t  prefixSize; // 39035
		{
			const BYTE* bufferEnd; // 38963
			const BYTE* rangeEnd; // 38964
		}
		ZSTDMT_isOverlapped(Buffer buffer,
					Range range);  // 39039
	}
	{
		const BYTE* bufferEnd; // 38963
		const BYTE* rangeEnd; // 38964
	}
	ZSTDMT_isOverlapped(Buffer buffer,
				Range range);  // 39051
	ZSTDMT_tryGetInputRange(ZSTDMT_CCtx* mtctx); // 39213
} /* size: 3684, variables: 1, inline expansions: 3 (838 bytes) */

// <06F3DEED> thirdparty/zstd/zstd.c:39407
// member variables: 4
// struct size: 4
typedef struct {
	BYTE maxTableLog; /* 0 1 */
	BYTE tableType; /* 1 1 */
	BYTE tableLog; /* 2 1 */
	BYTE reserved; /* 3 1 */
} DTableDesc;

// <06F5F08E> thirdparty/zstd/zstd.c:39409
// variable: 1
DTableDesc HUF_getDTableDesc(const HUF_DTable* table)
{
	DTableDesc dtd; // 39411
} /* size: 0, variables: 1 */

// <06F3DF4E> thirdparty/zstd/zstd.c:39439
// member variables: 7
// struct size: 152
typedef struct {
	const BYTE * ip[4]; /* 0 32 */
	BYTE * op[4]; /* 32 32 */
	U64 bits[4]; /* 64 32 */
	const void * dt; /* 96 8 */
	const BYTE * ilowest; /* 104 8 */
	BYTE * oend; /* 112 8 */
	const BYTE * iend[4]; /* 120 32 */
} HUF_DecompressFastArgs;

// <06F5E862> thirdparty/zstd/zstd.c:39457
// variables: 8
// function calls: 32
size_t HUF_DecompressFastArgs_init(HUF_DecompressFastArgs* args, void* dst, size_t dstSize, const void* src, size_t srcSize, const HUF_DTable* DTable)
{
	const void* dt; // 39459
	const U32  dtLog; // 39460
	const BYTE* istart; // 39462
	const BYTE* oend; // 39464
	{
		const size_t  length1; // 39490
		const size_t  length2; // 39491
		const size_t  length3; // 39492
		const size_t  length4; // 39493
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39490
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39491
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39492
	}
	HUF_getDTableDesc(const HUF_DTable* table); // 39460
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 39464
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 39418
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 39419
	HUF_initFastDStream(const BYTE* ip); // 39529
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 39418
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 39419
	HUF_initFastDStream(const BYTE* ip); // 39530
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 39418
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 39419
	HUF_initFastDStream(const BYTE* ip); // 39531
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 39418
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 39419
	HUF_initFastDStream(const BYTE* ip); // 39532
} /* size: 512, variables: 4, inline expansions: 26 (202 bytes) */

// <06F5E815> thirdparty/zstd/zstd.c:39547
size_t HUF_initRemainingDStream(BIT_DStream_t* bit, const HUF_DecompressFastArgs* args, int stream, BYTE* segmentEnd)
{
} /* size: 0 */

// <06F3E030> thirdparty/zstd/zstd.c:39595
// member variables: 2
// struct size: 2
typedef struct {
	BYTE nbBits; /* 0 1 */
	BYTE byte; /* 1 1 */
} HUF_DEltX1;

// <06F5E7DC> thirdparty/zstd/zstd.c:39601
// variable: 1
U64 HUF_DEltX1_set4(BYTE symbol, BYTE nbBits)
{
	U64 D4; // 39602
} /* size: 0, variables: 1 */

// <06F5E76E> thirdparty/zstd/zstd.c:39618
// variables: 2
U32 HUF_rescaleStats(BYTE* huffWeight, U32* rankVal, U32 nbSymbols, U32 tableLog, U32 targetTableLog)
{
	{
		const U32  scale; // 39623
		U32 s; // 39624
	}
} /* size: 0 */

// <06F3E071> thirdparty/zstd/zstd.c:39643
// member variables: 5
// struct size: 1,492
typedef struct {
	U32 rankVal[13]; /* 0 52 */
	U32 rankStart[13]; /* 52 52 */
	U32 statsWksp[219]; /* 104 876 */
	BYTE symbols[256]; /* 980 256 */
	BYTE huffWeight[256]; /* 1236 256 */
} HUF_ReadDTableX1_Workspace;

// <06F5E098> thirdparty/zstd/zstd.c:39651
// variables: 33
// function calls: 9
size_t HUF_readDTableX1_wksp(HUF_DTable* DTable, const void* src, size_t srcSize, void* workSpace, size_t wkspSize, int flags)
{
	U32 tableLog; // 39653
	U32 nbSymbols; // 39654
	size_t iSize; // 39655
	const void* dtPtr; // 39656
	const HUF_DEltX1* dt; // 39657
	HUF_ReadDTableX1_Workspace* wksp; // 39658
	{
		DTableDesc dtd; // 39671
		const U32  maxTableLog; // 39672
		const U32  targetTableLog; // 39673
		HUF_getDTableDesc(const HUF_DTable* table); // 39671
		{
			const U32  scale; // 39623
			U32 s; // 39624
		}
		HUF_rescaleStats(BYTE* huffWeight,
				U32* rankVal,
				U32 nbSymbols,
				U32 tableLog,
				U32 targetTableLog);  // 39674
	}
	{
		int n; // 39693
		U32 nextRankStart; // 39694
		const int  unroll; // 39695
		const int  nLimit; // 39696
		{
			const U32  curr; // 39698
		}
		{
			int u; // 39703
			{
				const size_t  w; // 39705
			}
		}
		{
			const size_t  w; // 39710
		}
	}
	{
		U32 w; // 39721
		int symbol; // 39722
		int rankStart; // 39723
		{
			const int  symbolCount; // 39725
			const int  length; // 39726
			int uStart; // 39727
			const BYTE  nbBits; // 39728
			int s; // 39729
			int u; // 39730
			{
				HUF_DEltX1 D; // 39734
			}
			{
				HUF_DEltX1 D; // 39743
			}
			{
				const U64  D4; // 39753
				HUF_DEltX1_set4(BYTE symbol,
						BYTE nbBits);  // 39753
				MEM_write64(void* memPtr,
						U64 value);  // 39754
			}
			{
				const U64  D4; // 39760
				HUF_DEltX1_set4(BYTE symbol,
						BYTE nbBits);  // 39760
				MEM_write64(void* memPtr,
						U64 value);  // 39761
			}
			{
				const U64  D4; // 39768
				HUF_DEltX1_set4(BYTE symbol,
						BYTE nbBits);  // 39768
				MEM_write64(void* memPtr,
						U64 value);  // 39770
			}
		}
	}
	HUF_readStats_wksp(BYTE* huffWeight,
				size_t hwSize,
				U32* rankStats,
				U32* nbSymbolsPtr,
				U32* tableLogPtr,
				const void* src,
				size_t srcSize,
				void* workSpace,
				size_t wkspSize,
				int flags);  // 39666
} /* size: 1689, variables: 6, inline expansions: 1 (103 bytes) */

// <06F5E047> thirdparty/zstd/zstd.c:39788
// variables: 2
inline BYTE HUF_decodeSymbolX1(BIT_DStream_t* Dstream, const HUF_DEltX1* dt, const U32 dtLog)
{
	const size_t  val; // 39790
	const BYTE  c; // 39791
} /* size: 0, variables: 2 */

// <06F5DFE9> thirdparty/zstd/zstd.c:39812
// variable: 1
inline size_t HUF_decodeStreamX1(BYTE* p, const BIT_DStream_t* bitDPtr, const BYTE* pEnd, const HUF_DEltX1* dt, const U32 dtLog)
{
	const BYTE* pStart; // 39814
} /* size: 0, variables: 1 */

// <06F5DF2D> thirdparty/zstd/zstd.c:39841
// variables: 8
inline size_t HUF_decompress1X1_usingDTable_internal_body(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	BYTE* op; // 39846
	const BYTE* oend; // 39847
	const void* dtPtr; // 39848
	const HUF_DEltX1* dt; // 39849
	BIT_DStream_t bitD; // 39850
	const DTableDesc  dtd; // 39851
	const U32  dtLog; // 39852
	{
		const size_t  _var_err__; // 39854
	}
} /* size: 0, variables: 7 */

// <06F5DCF9> thirdparty/zstd/zstd.c:39868
// variables: 34
inline size_t HUF_decompress4X1_usingDTable_internal_body(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	{
		const BYTE* istart; // 39877
		const BYTE* ostart; // 39878
		const BYTE* oend; // 39879
		const BYTE* olimit; // 39880
		const void* dtPtr; // 39881
		const HUF_DEltX1* dt; // 39882
		BIT_DStream_t bitD1; // 39885
		BIT_DStream_t bitD2; // 39886
		BIT_DStream_t bitD3; // 39887
		BIT_DStream_t bitD4; // 39888
		const size_t  length1; // 39889
		const size_t  length2; // 39890
		const size_t  length3; // 39891
		const size_t  length4; // 39892
		const BYTE* istart1; // 39893
		const BYTE* istart2; // 39894
		const BYTE* istart3; // 39895
		const BYTE* istart4; // 39896
		const size_t  segmentSize; // 39897
		const BYTE* opStart2; // 39898
		const BYTE* opStart3; // 39899
		const BYTE* opStart4; // 39900
		BYTE* op1; // 39901
		BYTE* op2; // 39902
		BYTE* op3; // 39903
		BYTE* op4; // 39904
		const DTableDesc  dtd; // 39905
		const U32  dtLog; // 39906
		U32 endSignal; // 39907
		{
			const size_t  _var_err__; // 39912
		}
		{
			const size_t  _var_err__; // 39913
		}
		{
			const size_t  _var_err__; // 39914
		}
		{
			const size_t  _var_err__; // 39915
		}
		{
			const U32  endCheck; // 39958
		}
	}
} /* size: 0 */

// <0707662C> thirdparty/zstd/zstd.c:39968
// variables: 58
// function calls: 242
size_t HUF_decompress4X1_usingDTable_internal_bmi2(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	HUF_decompress4X1_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 39970
	{
		const BYTE* istart; // 39877
		const BYTE* ostart; // 39878
		const BYTE* oend; // 39879
		const BYTE* olimit; // 39880
		const void* dtPtr; // 39881
		const HUF_DEltX1* dt; // 39882
		BIT_DStream_t bitD1; // 39885
		BIT_DStream_t bitD2; // 39886
		BIT_DStream_t bitD3; // 39887
		BIT_DStream_t bitD4; // 39888
		const size_t  length1; // 39889
		const size_t  length2; // 39890
		const size_t  length3; // 39891
		const size_t  length4; // 39892
		const BYTE* istart1; // 39893
		const BYTE* istart2; // 39894
		const BYTE* istart3; // 39895
		const BYTE* istart4; // 39896
		const size_t  segmentSize; // 39897
		const BYTE* opStart2; // 39898
		const BYTE* opStart3; // 39899
		const BYTE* opStart4; // 39900
		BYTE* op1; // 39901
		BYTE* op2; // 39902
		BYTE* op3; // 39903
		BYTE* op4; // 39904
		const DTableDesc  dtd; // 39905
		const U32  dtLog; // 39906
		U32 endSignal; // 39907
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39890
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39889
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39891
		HUF_getDTableDesc(const HUF_DTable* table); // 39905
		{
			const size_t  _var_err__; // 39912
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 39912
		}
		{
			const size_t  _var_err__; // 39913
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 39913
		}
		{
			const size_t  _var_err__; // 39914
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 39914
		}
		{
			const size_t  _var_err__; // 39915
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 39915
		}
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39920
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 39936
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 39937
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 39938
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 39939
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39921
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39922
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39923
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39924
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39925
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39926
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39927
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39928
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39929
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39930
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39931
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39932
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39933
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39934
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39935
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39819
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39822
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39835
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
		HUF_decodeStreamX1(BYTE* p,
					const BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39952
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39819
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39822
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39835
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
		HUF_decodeStreamX1(BYTE* p,
					const BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39953
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39819
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39822
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39835
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
		HUF_decodeStreamX1(BYTE* p,
					const BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39954
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39819
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39822
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39835
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
		HUF_decodeStreamX1(BYTE* p,
					const BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39955
		{
			const U32  endCheck; // 39958
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 39958
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 39958
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 39958
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 39958
		}
	}
	HUF_decompress4X1_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 39970
	HUF_decompress4X1_usingDTable_internal_bmi2(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 39968
} /* size: 4953, inline expansions: 3 (9796 bytes) */

// <06F5DCA5> thirdparty/zstd/zstd.c:39968
size_t HUF_decompress4X1_usingDTable_internal_bmi2(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
} /* size: 0 */

// <06F5DC51> thirdparty/zstd/zstd.c:39975
size_t HUF_decompress4X1_usingDTable_internal_default(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
} /* size: 0 */

// <06F5D476> thirdparty/zstd/zstd.c:39987
// variables: 64
// function calls: 8
void HUF_decompress4X1_usingDTable_internal_fast_c_loop(HUF_DecompressFastArgs* args)
{

_out: // 40091
	U64 bits; // 39989
	const BYTE* ip; // 39990
	BYTE* op; // 39991
	const U16* dtable; // 39992
	const BYTE* oend; // 39993
	const BYTE* ilowest; // 39994
	{
		BYTE* olimit; // 40005
		int stream; // 40006
		{
			const size_t  oiters; // 40018
			const size_t  iiters; // 40022
			const size_t  iters; // 40024
			const size_t  symbols; // 40025
		}
		{
			const int  index; // 40077
			const int  entry; // 40077
		}
		{
			const int  index; // 40077
			const int  entry; // 40077
		}
		{
			const int  index; // 40077
			const int  entry; // 40077
		}
		{
			const int  index; // 40077
			const int  entry; // 40077
		}
		{
			const int  index; // 40078
			const int  entry; // 40078
		}
		{
			const int  index; // 40078
			const int  entry; // 40078
		}
		{
			const int  index; // 40078
			const int  entry; // 40078
		}
		{
			const int  index; // 40078
			const int  entry; // 40078
		}
		{
			const int  index; // 40079
			const int  entry; // 40079
		}
		{
			const int  index; // 40079
			const int  entry; // 40079
		}
		{
			const int  index; // 40079
			const int  entry; // 40079
		}
		{
			const int  index; // 40079
			const int  entry; // 40079
		}
		{
			const int  index; // 40080
			const int  entry; // 40080
		}
		{
			const int  index; // 40080
			const int  entry; // 40080
		}
		{
			const int  index; // 40080
			const int  entry; // 40080
		}
		{
			const int  index; // 40080
			const int  entry; // 40080
		}
		{
			const int  index; // 40081
			const int  entry; // 40081
		}
		{
			const int  index; // 40081
			const int  entry; // 40081
		}
		{
			const int  index; // 40081
			const int  entry; // 40081
		}
		{
			const int  index; // 40081
			const int  entry; // 40081
		}
		{
			const int  ctz; // 40084
			const int  nbBits; // 40084
			const int  nbBytes; // 40084
			ZSTD_countTrailingZeros64(U64 val); // 40084
			MEM_read64(const void* ptr); // 40084
		}
		{
			const int  ctz; // 40084
			const int  nbBits; // 40084
			const int  nbBytes; // 40084
			ZSTD_countTrailingZeros64(U64 val); // 40084
			MEM_read64(const void* ptr); // 40084
		}
		{
			const int  ctz; // 40084
			const int  nbBits; // 40084
			const int  nbBytes; // 40084
			ZSTD_countTrailingZeros64(U64 val); // 40084
			MEM_read64(const void* ptr); // 40084
		}
		{
			const int  ctz; // 40084
			const int  nbBits; // 40084
			const int  nbBytes; // 40084
			ZSTD_countTrailingZeros64(U64 val); // 40084
			MEM_read64(const void* ptr); // 40084
		}
	}
} /* size: 1382, variables: 6, goto labels: 1 */

// <0701919A> thirdparty/zstd/zstd.c:40106
// variables: 15
// function calls: 38
size_t HUF_decompress4X1_usingDTable_internal_fast(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, HUF_DecompressFastLoopFn loopFn)
{
	const void* dt; // 40112
	const BYTE* ilowest; // 40113
	const BYTE* oend; // 40114
	HUF_DecompressFastArgs args; // 40115
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 40114
	{
		const size_t  ret; // 40116
		{
			const size_t  err_code; // 40117
		}
	}
	{
		const size_t  segmentSize; // 40140
		BYTE* segmentEnd; // 40141
		int i; // 40142
		{
			BIT_DStream_t bit; // 40144
			{
				const size_t  err_code; // 40149
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 39562
				ZSTD_countTrailingZeros64(U64 val); // 39563
				HUF_initRemainingDStream(BIT_DStream_t* bit,
							const HUF_DecompressFastArgs* args,
							int stream,
							BYTE* segmentEnd);  // 40149
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2495
			BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
			{
				U32 nbBytes; // 2538
				BIT_DStream_status result; // 2539
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2546
			}
			BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 39790
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 39792
			HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
						const HUF_DEltX1* dt,
						const U32  dtLog);  // 39819
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 39790
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 39792
			HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
						const HUF_DEltX1* dt,
						const U32  dtLog);  // 39820
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 39790
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 39792
			HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
						const HUF_DEltX1* dt,
						const U32  dtLog);  // 39821
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 39790
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 39792
			HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
						const HUF_DEltX1* dt,
						const U32  dtLog);  // 39822
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 39790
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 39792
			HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
						const HUF_DEltX1* dt,
						const U32  dtLog);  // 39835
			{
				U32 nbBytes; // 2538
				BIT_DStream_status result; // 2539
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2546
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2495
			BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
			BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
			HUF_decodeStreamX1(BYTE* p,
						const BIT_DStream_t* bitDPtr,
						const BYTE* pEnd,
						const HUF_DEltX1* dt,
						const U32  dtLog);  // 40151
		}
	}
} /* size: 801, variables: 4, inline expansions: 1 (14 bytes) */

// <06F5D37B> thirdparty/zstd/zstd.c:40106
// variables: 11
size_t HUF_decompress4X1_usingDTable_internal_fast(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, HUF_DecompressFastLoopFn loopFn)
{
	const void* dt; // 40112
	const BYTE* ilowest; // 40113
	const BYTE* oend; // 40114
	HUF_DecompressFastArgs args; // 40115
	{
		const size_t  ret; // 40116
		{
			const size_t  err_code; // 40117
		}
	}
	{
		const size_t  segmentSize; // 40140
		BYTE* segmentEnd; // 40141
		int i; // 40142
		{
			BIT_DStream_t bit; // 40144
			{
				const size_t  err_code; // 40149
			}
		}
	}
} /* size: 0, variables: 4 */

// <06F5C942> thirdparty/zstd/zstd.c:40161
// variables: 7
// function calls: 44
size_t HUF_decompress1X1_usingDTable_internal_default(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 39847
	HUF_getDTableDesc(const HUF_DTable* table); // 39851
	{
		const size_t  _var_err__; // 39854
		{
			const BYTE  lastByte; // 2369
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2370
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2368
		{
			const BYTE  lastByte; // 2397
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2398
		}
		BIT_initDStream(BIT_DStream_t* bitD,
				const void* srcBuffer,
				size_t srcSize);  // 39854
	}
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39819
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 2495
	BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
	{
	}
	{
		U32 nbBytes; // 2538
		BIT_DStream_status result; // 2539
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2546
	}
	BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39820
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39821
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39822
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39835
	{
		U32 nbBytes; // 2538
		BIT_DStream_status result; // 2539
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2546
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 2495
	BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
	BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
	HUF_decodeStreamX1(BYTE* p,
				const BIT_DStream_t* bitDPtr,
				const BYTE* pEnd,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39856
	BIT_endOfDStream(const BIT_DStream_t* DStream); // 39858
	HUF_decompress1X1_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40161
} /* size: 821, inline expansions: 30 (1926 bytes) */

// <06F5BF09> thirdparty/zstd/zstd.c:40161
// variables: 7
// function calls: 44
size_t HUF_decompress1X1_usingDTable_internal_bmi2(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 39847
	HUF_getDTableDesc(const HUF_DTable* table); // 39851
	{
		const size_t  _var_err__; // 39854
		{
			const BYTE  lastByte; // 2369
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2370
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2368
		{
			const BYTE  lastByte; // 2397
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2398
		}
		BIT_initDStream(BIT_DStream_t* bitD,
				const void* srcBuffer,
				size_t srcSize);  // 39854
	}
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39819
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 2495
	BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
	{
	}
	{
		U32 nbBytes; // 2538
		BIT_DStream_status result; // 2539
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2546
	}
	BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39820
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39821
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39822
	BIT_lookBitsFast(const BIT_DStream_t* bitD,
			U32 nbBits);  // 39790
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 39792
	HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39835
	{
		U32 nbBytes; // 2538
		BIT_DStream_status result; // 2539
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2546
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 2495
	BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
	BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
	HUF_decodeStreamX1(BYTE* p,
				const BIT_DStream_t* bitDPtr,
				const BYTE* pEnd,
				const HUF_DEltX1* dt,
				const U32  dtLog);  // 39856
	BIT_endOfDStream(const BIT_DStream_t* DStream); // 39858
	HUF_decompress1X1_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40161
} /* size: 813, inline expansions: 30 (1856 bytes) */

// <06F5BEA8> thirdparty/zstd/zstd.c:40161
size_t HUF_decompress1X1_usingDTable_internal(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, int flags)
{
} /* size: 0 */

// <07079CDF> thirdparty/zstd/zstd.c:40163
// variables: 61
// function calls: 244
size_t HUF_decompress4X1_usingDTable_internal(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, int flags)
{
	HUF_DecompressUsingDTableFn fallbackFn; // 40166
	HUF_DecompressFastLoopFn loopFn; // 40167
	{
		const size_t  ret; // 40189
	}
	HUF_decompress4X1_usingDTable_internal(void* dst,
						size_t dstSize,
						const void* cSrc,
						size_t cSrcSize,
						const HUF_DTable* DTable,
						int flags);  // 40163
	HUF_decompress4X1_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 39977
	{
		const BYTE* istart; // 39877
		const BYTE* ostart; // 39878
		const BYTE* oend; // 39879
		const BYTE* olimit; // 39880
		const void* dtPtr; // 39881
		const HUF_DEltX1* dt; // 39882
		BIT_DStream_t bitD1; // 39885
		BIT_DStream_t bitD2; // 39886
		BIT_DStream_t bitD3; // 39887
		BIT_DStream_t bitD4; // 39888
		const size_t  length1; // 39889
		const size_t  length2; // 39890
		const size_t  length3; // 39891
		const size_t  length4; // 39892
		const BYTE* istart1; // 39893
		const BYTE* istart2; // 39894
		const BYTE* istart3; // 39895
		const BYTE* istart4; // 39896
		const size_t  segmentSize; // 39897
		const BYTE* opStart2; // 39898
		const BYTE* opStart3; // 39899
		const BYTE* opStart4; // 39900
		BYTE* op1; // 39901
		BYTE* op2; // 39902
		BYTE* op3; // 39903
		BYTE* op4; // 39904
		const DTableDesc  dtd; // 39905
		const U32  dtLog; // 39906
		U32 endSignal; // 39907
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39889
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39890
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 39891
		HUF_getDTableDesc(const HUF_DTable* table); // 39905
		{
			const size_t  _var_err__; // 39912
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 39912
		}
		{
			const size_t  _var_err__; // 39913
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 39913
		}
		{
			const size_t  _var_err__; // 39914
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 39914
		}
		{
			const size_t  _var_err__; // 39915
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 39915
		}
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39920
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 39936
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 39937
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 39938
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 39939
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39921
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39922
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39923
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39924
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39925
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39926
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39927
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39928
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39929
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39930
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39931
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39932
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39933
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39934
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39935
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39819
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39822
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39835
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
		HUF_decodeStreamX1(BYTE* p,
					const BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39952
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39819
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39822
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39835
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
		HUF_decodeStreamX1(BYTE* p,
					const BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39953
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39819
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39822
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39835
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
		HUF_decodeStreamX1(BYTE* p,
					const BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39954
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39818
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39819
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39820
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39821
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39822
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 39790
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 39792
		HUF_decodeSymbolX1(BIT_DStream_t* Dstream,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39835
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 39825
		HUF_decodeStreamX1(BYTE* p,
					const BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX1* dt,
					const U32  dtLog);  // 39955
		{
			const U32  endCheck; // 39958
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 39958
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 39958
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 39958
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 39958
		}
	}
	HUF_decompress4X1_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 39977
	HUF_decompress4X1_usingDTable_internal_default(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 39975
	HUF_decompress4X1_usingDTable_internal_default(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40178
} /* size: 5641, variables: 2, inline expansions: 5 (16559 bytes) */

// <06F5BE1E> thirdparty/zstd/zstd.c:40163
// variables: 3
size_t HUF_decompress4X1_usingDTable_internal(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, int flags)
{
	HUF_DecompressUsingDTableFn fallbackFn; // 40166
	HUF_DecompressFastLoopFn loopFn; // 40167
	{
		const size_t  ret; // 40189
	}
} /* size: 0, variables: 2 */

// <06F5BD8A> thirdparty/zstd/zstd.c:40196
// variables: 2
size_t HUF_decompress4X1_DCtx_wksp(HUF_DTable* dctx, void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, void* workSpace, size_t wkspSize, int flags)
{
	const BYTE* ip; // 40200
	const size_t  hSize; // 40202
} /* size: 0, variables: 2 */

// <06F3E110> thirdparty/zstd/zstd.c:40219
// member variables: 3
// struct size: 4
typedef struct {
	U16 sequence; /* 0 2 */
	BYTE nbBits; /* 2 1 */
	BYTE length; /* 3 1 */
} HUF_DEltX2;

// <06F3E161> thirdparty/zstd/zstd.c:40220
// member variable: 1
// struct size: 1
typedef struct {
	BYTE symbol; /* 0 1 */
} sortedSymbol_t;

// <06F5BD36> thirdparty/zstd/zstd.c:40227
// variable: 1
U32 HUF_buildDEltX2U32(U32 symbol, U32 nbBits, U32 baseSeq, int level)
{
	U32 seq; // 40229
} /* size: 0, variables: 1 */

// <06F5BCD5> thirdparty/zstd/zstd.c:40246
// variables: 2
HUF_DEltX2 HUF_buildDEltX2(U32 symbol, U32 nbBits, U32 baseSeq, int level)
{
	HUF_DEltX2 DElt; // 40248
	const U32  val; // 40249
} /* size: 0, variables: 2 */

// <06F5BC81> thirdparty/zstd/zstd.c:40258
// variable: 1
U64 HUF_buildDEltX2U64(U32 symbol, U32 nbBits, U16 baseSeq, int level)
{
	U32 DElt; // 40260
} /* size: 0, variables: 1 */

// <06F5B781> thirdparty/zstd/zstd.c:40276
// variables: 8
// function calls: 10
void HUF_fillDTableX2ForWeight(HUF_DEltX2* DTableRank, const sortedSymbol_t* begin, const sortedSymbol_t* end, U32 nbBits, U32 tableLog, U16 baseSeq, const int level)
{
	const U32  length; // 40282
	const sortedSymbol_t* ptr; // 40283
	{
		const HUF_DEltX2  DElt; // 40288
		HUF_buildDEltX2U32(U32 symbol,
					U32 nbBits,
					U32 baseSeq,
					int level);  // 40249
		HUF_buildDEltX2(U32 symbol,
				U32 nbBits,
				U32 baseSeq,
				int level);  // 40288
	}
	{
		const HUF_DEltX2  DElt; // 40294
		HUF_buildDEltX2U32(U32 symbol,
					U32 nbBits,
					U32 baseSeq,
					int level);  // 40249
		HUF_buildDEltX2(U32 symbol,
				U32 nbBits,
				U32 baseSeq,
				int level);  // 40294
	}
	{
		const U64  DEltX2; // 40302
		HUF_buildDEltX2U32(U32 symbol,
					U32 nbBits,
					U32 baseSeq,
					int level);  // 40260
		HUF_buildDEltX2U64(U32 symbol,
					U32 nbBits,
					U16 baseSeq,
					int level);  // 40302
	}
	{
		const U64  DEltX2; // 40310
		HUF_buildDEltX2U32(U32 symbol,
					U32 nbBits,
					U32 baseSeq,
					int level);  // 40260
		HUF_buildDEltX2U64(U32 symbol,
					U32 nbBits,
					U16 baseSeq,
					int level);  // 40310
	}
	{
		const U64  DEltX2; // 40320
		const HUF_DEltX2* DTableRankEnd; // 40321
		HUF_buildDEltX2U32(U32 symbol,
					U32 nbBits,
					U32 baseSeq,
					int level);  // 40260
		HUF_buildDEltX2U64(U32 symbol,
					U32 nbBits,
					U16 baseSeq,
					int level);  // 40320
	}
} /* size: 746, variables: 2 */

// <06F5B673> thirdparty/zstd/zstd.c:40335
// variables: 9
void HUF_fillDTableX2Level2(HUF_DEltX2* DTable, U32 targetLog, const U32 consumedBits, const U32* rankVal, const int minWeight, const int maxWeight1, const sortedSymbol_t* sortedSymbols, const U32* rankStart, U32 nbBitsBaseline, U16 baseSeq)
{
	{
		const U32  length; // 40345
		const U64  DEltX2; // 40346
		const int  skipSize; // 40347
		{
			int i; // 40362
		}
	}
	{
		int w; // 40375
		{
			const int  begin; // 40377
			const int  end; // 40378
			const U32  nbBits; // 40379
			const U32  totalBits; // 40380
		}
	}
} /* size: 0 */

// <06F5B561> thirdparty/zstd/zstd.c:40390
// variables: 12
void HUF_fillDTableX2(HUF_DEltX2* DTable, const U32 targetLog, const sortedSymbol_t* sortedList, const U32* rankStart, rankValCol_t* rankValOrigin, const U32 maxWeight, const U32 nbBitsBaseline)
{
	const U32* rankVal; // 40395
	const int  scaleLog; // 40396
	const U32  minBits; // 40397
	int w; // 40398
	const int  wEnd; // 40399
	{
		const int  begin; // 40403
		const int  end; // 40404
		const U32  nbBits; // 40405
		{
			int start; // 40409
			const U32  length; // 40410
			int minWeight; // 40411
			int s; // 40412
		}
	}
} /* size: 0, variables: 5 */

// <06F3E1B5> thirdparty/zstd/zstd.c:40436
// member variables: 6
// struct size: 2,124
typedef struct {
	U32 rankVal[12][13]; /* 0 624 */
	U32 rankStats[13]; /* 624 52 */
	U32 rankStart0[15]; /* 676 60 */
	sortedSymbol_t sortedSymbol[256]; /* 736 256 */
	BYTE weightList[256]; /* 992 256 */
	U32 calleeWksp[219]; /* 1248 876 */
} HUF_ReadDTableX2_Workspace;

// <06F5ADF3> thirdparty/zstd/zstd.c:40445
// variables: 41
// function calls: 6
size_t HUF_readDTableX2_wksp(HUF_DTable* DTable, const void* src, size_t srcSize, void* workSpace, size_t wkspSize, int flags)
{
	U32 tableLog; // 40449
	U32 maxW; // 40449
	U32 nbSymbols; // 40449
	DTableDesc dtd; // 40450
	U32 maxTableLog; // 40451
	size_t iSize; // 40452
	void* dtPtr; // 40453
	const HUF_DEltX2* dt; // 40454
	U32* rankStart; // 40455
	const HUF_ReadDTableX2_Workspace* wksp; // 40457
	{
		U32 w; // 40480
		U32 nextRankStart; // 40480
		{
			U32 curr; // 40482
		}
	}
	{
		U32 s; // 40491
		{
			const U32  w; // 40493
			const U32  r; // 40494
		}
	}
	{
		const U32* rankVal0; // 40501
		{
			const int  rescale; // 40502
			U32 nextRankVal; // 40503
			U32 w; // 40504
			{
				U32 curr; // 40506
			}
		}
		{
			const U32  minBits; // 40510
			U32 consumed; // 40511
			{
				const U32* rankValPtr; // 40513
				U32 w; // 40514
			}
		}
	}
	HUF_getDTableDesc(const HUF_DTable* table); // 40450
	HUF_readStats_wksp(BYTE* huffWeight,
				size_t hwSize,
				U32* rankStats,
				U32* nbSymbolsPtr,
				U32* tableLogPtr,
				const void* src,
				size_t srcSize,
				void* workSpace,
				size_t wkspSize,
				int flags);  // 40469
	{
		const int  begin; // 40403
		const int  end; // 40404
		const U32  nbBits; // 40405
		{
			int start; // 40409
			const U32  length; // 40410
			int minWeight; // 40411
			int s; // 40412
			{
				const U32  length; // 40345
				const U64  DEltX2; // 40346
				const int  skipSize; // 40347
				HUF_buildDEltX2U32(U32 symbol,
							U32 nbBits,
							U32 baseSeq,
							int level);  // 40260
				HUF_buildDEltX2U64(U32 symbol,
							U32 nbBits,
							U16 baseSeq,
							int level);  // 40346
				{
					int i; // 40362
				}
			}
			{
				int w; // 40375
				{
					const int  begin; // 40377
					const int  end; // 40378
					const U32  nbBits; // 40379
					const U32  totalBits; // 40380
				}
			}
			HUF_fillDTableX2Level2(HUF_DEltX2* DTable,
						U32 targetLog,
						const U32  consumedBits,
						const U32* rankVal,
						const int  minWeight,
						const int  maxWeight1,
						const sortedSymbol_t* sortedSymbols,
						const U32* rankStart,
						U32 nbBitsBaseline,
						U16 baseSeq);  // 40418
		}
	}
	HUF_fillDTableX2(HUF_DEltX2* DTable,
			const U32  targetLog,
			const sortedSymbol_t* sortedList,
			const U32* rankStart,
			rankValCol_t* rankValOrigin,
			const U32  maxWeight,
			const U32  nbBitsBaseline);  // 40519
} /* size: 1822, variables: 10, inline expansions: 3 (906 bytes) */

// <06F5ADA1> thirdparty/zstd/zstd.c:40532
// variable: 1
inline U32 HUF_decodeSymbolX2(void* op, BIT_DStream_t* DStream, const HUF_DEltX2* dt, const U32 dtLog)
{
	const size_t  val; // 40534
} /* size: 0, variables: 1 */

// <06F5AD4F> thirdparty/zstd/zstd.c:40541
// variable: 1
inline U32 HUF_decodeLastSymbolX2(void* op, BIT_DStream_t* DStream, const HUF_DEltX2* dt, const U32 dtLog)
{
	const size_t  val; // 40543
} /* size: 0, variables: 1 */

// <06F5ACF1> thirdparty/zstd/zstd.c:40574
// variable: 1
inline size_t HUF_decodeStreamX2(BYTE* p, BIT_DStream_t* bitDPtr, const BYTE* pEnd, const HUF_DEltX2* dt, const U32 dtLog)
{
	const BYTE* pStart; // 40577
} /* size: 0, variables: 1 */

// <06F5AC3B> thirdparty/zstd/zstd.c:40619
// variables: 7
inline size_t HUF_decompress1X2_usingDTable_internal_body(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	BIT_DStream_t bitD; // 40624
	{
		const size_t  _var_err__; // 40627
	}
	{
		const BYTE* ostart; // 40630
		const BYTE* oend; // 40631
		const void* dtPtr; // 40632
		const HUF_DEltX2* dt; // 40633
		const DTableDesc  dtd; // 40634
	}
} /* size: 0, variables: 1 */

// <06F5AA12> thirdparty/zstd/zstd.c:40650
// variables: 34
inline size_t HUF_decompress4X2_usingDTable_internal_body(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	{
		const BYTE* istart; // 40658
		const BYTE* ostart; // 40659
		const BYTE* oend; // 40660
		const BYTE* olimit; // 40661
		const void* dtPtr; // 40662
		const HUF_DEltX2* dt; // 40663
		BIT_DStream_t bitD1; // 40666
		BIT_DStream_t bitD2; // 40667
		BIT_DStream_t bitD3; // 40668
		BIT_DStream_t bitD4; // 40669
		const size_t  length1; // 40670
		const size_t  length2; // 40671
		const size_t  length3; // 40672
		const size_t  length4; // 40673
		const BYTE* istart1; // 40674
		const BYTE* istart2; // 40675
		const BYTE* istart3; // 40676
		const BYTE* istart4; // 40677
		const size_t  segmentSize; // 40678
		const BYTE* opStart2; // 40679
		const BYTE* opStart3; // 40680
		const BYTE* opStart4; // 40681
		BYTE* op1; // 40682
		BYTE* op2; // 40683
		BYTE* op3; // 40684
		BYTE* op4; // 40685
		U32 endSignal; // 40686
		const DTableDesc  dtd; // 40687
		const U32  dtLog; // 40688
		{
			const size_t  _var_err__; // 40693
		}
		{
			const size_t  _var_err__; // 40694
		}
		{
			const size_t  _var_err__; // 40695
		}
		{
			const size_t  _var_err__; // 40696
		}
		{
			const U32  endCheck; // 40761
		}
	}
} /* size: 0 */

// <07070E39> thirdparty/zstd/zstd.c:40771
// variables: 74
// function calls: 394
size_t HUF_decompress4X2_usingDTable_internal_bmi2(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	HUF_decompress4X2_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40773
	{
		const BYTE* istart; // 40658
		const BYTE* ostart; // 40659
		const BYTE* oend; // 40660
		const BYTE* olimit; // 40661
		const void* dtPtr; // 40662
		const HUF_DEltX2* dt; // 40663
		BIT_DStream_t bitD1; // 40666
		BIT_DStream_t bitD2; // 40667
		BIT_DStream_t bitD3; // 40668
		BIT_DStream_t bitD4; // 40669
		const size_t  length1; // 40670
		const size_t  length2; // 40671
		const size_t  length3; // 40672
		const size_t  length4; // 40673
		const BYTE* istart1; // 40674
		const BYTE* istart2; // 40675
		const BYTE* istart3; // 40676
		const BYTE* istart4; // 40677
		const size_t  segmentSize; // 40678
		const BYTE* opStart2; // 40679
		const BYTE* opStart3; // 40680
		const BYTE* opStart4; // 40681
		BYTE* op1; // 40682
		BYTE* op2; // 40683
		BYTE* op3; // 40684
		BYTE* op4; // 40685
		U32 endSignal; // 40686
		const DTableDesc  dtd; // 40687
		const U32  dtLog; // 40688
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 40671
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 40670
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 40672
		HUF_getDTableDesc(const HUF_DTable* table); // 40687
		{
			const size_t  _var_err__; // 40693
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 40693
		}
		{
			const size_t  _var_err__; // 40694
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 40694
		}
		{
			const size_t  _var_err__; // 40695
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 40695
		}
		{
			const size_t  _var_err__; // 40696
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 40696
		}
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40723
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 40739
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 40739
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 40739
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 40739
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40724
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40725
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40726
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40727
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40728
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40729
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40730
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40731
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40732
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40733
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40734
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40735
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40736
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40737
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40738
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40755
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40756
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40757
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40758
		{
			const U32  endCheck; // 40761
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 40761
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 40761
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 40761
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 40761
		}
	}
	HUF_decompress4X2_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40773
	HUF_decompress4X2_usingDTable_internal_bmi2(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40771
} /* size: 8955, inline expansions: 3 (17802 bytes) */

// <06F5A9BE> thirdparty/zstd/zstd.c:40771
size_t HUF_decompress4X2_usingDTable_internal_bmi2(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
} /* size: 0 */

// <06F5A96A> thirdparty/zstd/zstd.c:40778
size_t HUF_decompress4X2_usingDTable_internal_default(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
} /* size: 0 */

// <06F59CC7> thirdparty/zstd/zstd.c:40790
// variables: 72
// function calls: 28
void HUF_decompress4X2_usingDTable_internal_fast_c_loop(HUF_DecompressFastArgs* args)
{

_out: // 40923
	U64 bits; // 40792
	const BYTE* ip; // 40793
	BYTE* op; // 40794
	BYTE* oend; // 40795
	const HUF_DEltX2* dtable; // 40796
	const BYTE* ilowest; // 40797
	{
		BYTE* olimit; // 40813
		int stream; // 40814
		{
			size_t iters; // 40833
			{
				const size_t  oiters; // 40839
			}
		}
		{
			const int  index; // 40903
			const HUF_DEltX2  entry; // 40903
			MEM_write16(void* memPtr,
					U16 value);  // 40903
		}
		{
			const int  index; // 40903
			const HUF_DEltX2  entry; // 40903
			MEM_write16(void* memPtr,
					U16 value);  // 40903
		}
		{
			const int  index; // 40903
			const HUF_DEltX2  entry; // 40903
			MEM_write16(void* memPtr,
					U16 value);  // 40903
		}
		{
			const int  index; // 40903
			const HUF_DEltX2  entry; // 40903
		}
		{
			const int  index; // 40904
			const HUF_DEltX2  entry; // 40904
			MEM_write16(void* memPtr,
					U16 value);  // 40904
		}
		{
			const int  index; // 40904
			const HUF_DEltX2  entry; // 40904
			MEM_write16(void* memPtr,
					U16 value);  // 40904
		}
		{
			const int  index; // 40904
			const HUF_DEltX2  entry; // 40904
			MEM_write16(void* memPtr,
					U16 value);  // 40904
		}
		{
			const int  index; // 40904
			const HUF_DEltX2  entry; // 40904
		}
		{
			const int  index; // 40905
			const HUF_DEltX2  entry; // 40905
			MEM_write16(void* memPtr,
					U16 value);  // 40905
		}
		{
			const int  index; // 40905
			const HUF_DEltX2  entry; // 40905
			MEM_write16(void* memPtr,
					U16 value);  // 40905
		}
		{
			const int  index; // 40905
			const HUF_DEltX2  entry; // 40905
			MEM_write16(void* memPtr,
					U16 value);  // 40905
		}
		{
			const int  index; // 40905
			const HUF_DEltX2  entry; // 40905
		}
		{
			const int  index; // 40906
			const HUF_DEltX2  entry; // 40906
			MEM_write16(void* memPtr,
					U16 value);  // 40906
		}
		{
			const int  index; // 40906
			const HUF_DEltX2  entry; // 40906
			MEM_write16(void* memPtr,
					U16 value);  // 40906
		}
		{
			const int  index; // 40906
			const HUF_DEltX2  entry; // 40906
			MEM_write16(void* memPtr,
					U16 value);  // 40906
		}
		{
			const int  index; // 40906
			const HUF_DEltX2  entry; // 40906
		}
		{
			const int  index; // 40907
			const HUF_DEltX2  entry; // 40907
			MEM_write16(void* memPtr,
					U16 value);  // 40907
		}
		{
			const int  index; // 40907
			const HUF_DEltX2  entry; // 40907
			MEM_write16(void* memPtr,
					U16 value);  // 40907
		}
		{
			const int  index; // 40907
			const HUF_DEltX2  entry; // 40907
			MEM_write16(void* memPtr,
					U16 value);  // 40907
		}
		{
			const int  index; // 40907
			const HUF_DEltX2  entry; // 40907
		}
		{
			const int  index; // 40910
			const HUF_DEltX2  entry; // 40910
			MEM_write16(void* memPtr,
					U16 value);  // 40910
		}
		{
			const int  index; // 40916
			const HUF_DEltX2  entry; // 40916
			MEM_write16(void* memPtr,
					U16 value);  // 40916
		}
		{
			const int  ctz; // 40916
			const int  nbBits; // 40916
			const int  nbBytes; // 40916
			ZSTD_countTrailingZeros64(U64 val); // 40916
			MEM_read64(const void* ptr); // 40916
		}
		{
			const int  index; // 40916
			const HUF_DEltX2  entry; // 40916
			MEM_write16(void* memPtr,
					U16 value);  // 40916
		}
		{
			const int  ctz; // 40916
			const int  nbBits; // 40916
			const int  nbBytes; // 40916
			ZSTD_countTrailingZeros64(U64 val); // 40916
			MEM_read64(const void* ptr); // 40916
		}
		{
			const int  index; // 40916
			const HUF_DEltX2  entry; // 40916
			MEM_write16(void* memPtr,
					U16 value);  // 40916
		}
		{
			const int  ctz; // 40916
			const int  nbBits; // 40916
			const int  nbBytes; // 40916
			ZSTD_countTrailingZeros64(U64 val); // 40916
			MEM_read64(const void* ptr); // 40916
		}
		{
			const int  index; // 40916
			const HUF_DEltX2  entry; // 40916
			MEM_write16(void* memPtr,
					U16 value);  // 40916
		}
		{
			const int  ctz; // 40916
			const int  nbBits; // 40916
			const int  nbBytes; // 40916
			ZSTD_countTrailingZeros64(U64 val); // 40916
			MEM_read64(const void* ptr); // 40916
		}
	}
} /* size: 1832, variables: 6, goto labels: 1 */

// <0701769A> thirdparty/zstd/zstd.c:40933
// variables: 17
// function calls: 54
size_t HUF_decompress4X2_usingDTable_internal_fast(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, HUF_DecompressFastLoopFn loopFn)
{
	const void* dt; // 40938
	const BYTE* ilowest; // 40939
	const BYTE* oend; // 40940
	HUF_DecompressFastArgs args; // 40941
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 40940
	{
		const size_t  ret; // 40943
		{
			const size_t  err_code; // 40944
		}
	}
	{
		const size_t  segmentSize; // 40965
		BYTE* segmentEnd; // 40966
		int i; // 40967
		{
			BIT_DStream_t bit; // 40969
			{
				const size_t  err_code; // 40974
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 39562
				ZSTD_countTrailingZeros64(U64 val); // 39563
				HUF_initRemainingDStream(BIT_DStream_t* bit,
							const HUF_DecompressFastArgs* args,
							int stream,
							BYTE* segmentEnd);  // 40974
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2495
			BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
			{
				U32 nbBytes; // 2538
				BIT_DStream_status result; // 2539
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2546
			}
			BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 40534
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 40536
			HUF_decodeSymbolX2(void* op,
						BIT_DStream_t* DStream,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40584
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 40534
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 40536
			HUF_decodeSymbolX2(void* op,
						BIT_DStream_t* DStream,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40585
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 40534
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 40536
			HUF_decodeSymbolX2(void* op,
						BIT_DStream_t* DStream,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40586
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 40534
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 40536
			HUF_decodeSymbolX2(void* op,
						BIT_DStream_t* DStream,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40587
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 40534
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 40536
			HUF_decodeSymbolX2(void* op,
						BIT_DStream_t* DStream,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40588
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 40534
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 40536
			HUF_decodeSymbolX2(void* op,
						BIT_DStream_t* DStream,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40609
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 40543
			HUF_decodeLastSymbolX2(void* op,
						BIT_DStream_t* DStream,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40613
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2495
			BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
			{
				U32 nbBytes; // 2538
				BIT_DStream_status result; // 2539
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2546
			}
			BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
			BIT_lookBitsFast(const BIT_DStream_t* bitD,
					U32 nbBits);  // 40534
			BIT_skipBits(BIT_DStream_t* bitD,
					U32 nbBits);  // 40536
			HUF_decodeSymbolX2(void* op,
						BIT_DStream_t* DStream,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40606
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2495
			BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
			{
				U32 nbBytes; // 2538
				BIT_DStream_status result; // 2539
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2546
			}
			BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
			HUF_decodeStreamX2(BYTE* p,
						BIT_DStream_t* bitDPtr,
						const BYTE* pEnd,
						const HUF_DEltX2* dt,
						const U32  dtLog);  // 40975
		}
	}
} /* size: 1182, variables: 4, inline expansions: 1 (14 bytes) */

// <06F59BCC> thirdparty/zstd/zstd.c:40933
// variables: 11
size_t HUF_decompress4X2_usingDTable_internal_fast(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, HUF_DecompressFastLoopFn loopFn)
{
	const void* dt; // 40938
	const BYTE* ilowest; // 40939
	const BYTE* oend; // 40940
	HUF_DecompressFastArgs args; // 40941
	{
		const size_t  ret; // 40943
		{
			const size_t  err_code; // 40944
		}
	}
	{
		const size_t  segmentSize; // 40965
		BYTE* segmentEnd; // 40966
		int i; // 40967
		{
			BIT_DStream_t bit; // 40969
			{
				const size_t  err_code; // 40974
			}
		}
	}
} /* size: 0, variables: 4 */

// <0707D5F1> thirdparty/zstd/zstd.c:40985
// variables: 77
// function calls: 396
size_t HUF_decompress4X2_usingDTable_internal(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, int flags)
{
	HUF_DecompressUsingDTableFn fallbackFn; // 40988
	HUF_DecompressFastLoopFn loopFn; // 40989
	{
		const size_t  ret; // 41011
	}
	HUF_decompress4X2_usingDTable_internal(void* dst,
						size_t dstSize,
						const void* cSrc,
						size_t cSrcSize,
						const HUF_DTable* DTable,
						int flags);  // 40985
	HUF_decompress4X2_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40780
	{
		const BYTE* istart; // 40658
		const BYTE* ostart; // 40659
		const BYTE* oend; // 40660
		const BYTE* olimit; // 40661
		const void* dtPtr; // 40662
		const HUF_DEltX2* dt; // 40663
		BIT_DStream_t bitD1; // 40666
		BIT_DStream_t bitD2; // 40667
		BIT_DStream_t bitD3; // 40668
		BIT_DStream_t bitD4; // 40669
		const size_t  length1; // 40670
		const size_t  length2; // 40671
		const size_t  length3; // 40672
		const size_t  length4; // 40673
		const BYTE* istart1; // 40674
		const BYTE* istart2; // 40675
		const BYTE* istart3; // 40676
		const BYTE* istart4; // 40677
		const size_t  segmentSize; // 40678
		const BYTE* opStart2; // 40679
		const BYTE* opStart3; // 40680
		const BYTE* opStart4; // 40681
		BYTE* op1; // 40682
		BYTE* op2; // 40683
		BYTE* op3; // 40684
		BYTE* op4; // 40685
		U32 endSignal; // 40686
		const DTableDesc  dtd; // 40687
		const U32  dtLog; // 40688
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 40670
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 40671
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 40672
		HUF_getDTableDesc(const HUF_DTable* table); // 40687
		{
			const size_t  _var_err__; // 40693
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 40693
		}
		{
			const size_t  _var_err__; // 40694
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 40694
		}
		{
			const size_t  _var_err__; // 40695
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 40695
		}
		{
			const size_t  _var_err__; // 40696
			{
				const BYTE  lastByte; // 2369
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2370
			}
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2368
			{
				const BYTE  lastByte; // 2397
				ZSTD_countLeadingZeros32(U32 val); // 2106
				ZSTD_highbit32(U32 val); // 2398
			}
			BIT_initDStream(BIT_DStream_t* bitD,
					const void* srcBuffer,
					size_t srcSize);  // 40696
		}
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40723
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 40739
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 40739
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 40739
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2509
		BIT_reloadDStreamFast(BIT_DStream_t* bitD); // 40739
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40725
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40724
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40727
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40726
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40728
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40732
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40729
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40730
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40731
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40733
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40734
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40735
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40738
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40736
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40737
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40755
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40756
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40757
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		{
		}
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40758
		{
			const U32  endCheck; // 40761
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 40761
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 40761
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 40761
			BIT_endOfDStream(const BIT_DStream_t* DStream); // 40761
		}
	}
	HUF_decompress4X2_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40780
	HUF_decompress4X2_usingDTable_internal_default(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 40778
	HUF_decompress4X2_usingDTable_internal_default(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 41000
} /* size: 9481, variables: 2, inline expansions: 5 (28097 bytes) */

// <06F59B42> thirdparty/zstd/zstd.c:40985
// variables: 3
size_t HUF_decompress4X2_usingDTable_internal(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, int flags)
{
	HUF_DecompressUsingDTableFn fallbackFn; // 40988
	HUF_DecompressFastLoopFn loopFn; // 40989
	{
		const size_t  ret; // 41011
	}
} /* size: 0, variables: 2 */

// <06F5890C> thirdparty/zstd/zstd.c:41018
// variables: 16
// function calls: 82
size_t HUF_decompress1X2_usingDTable_internal_default(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	{
		const size_t  _var_err__; // 40627
		{
			const BYTE  lastByte; // 2369
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2370
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2368
		{
			const BYTE  lastByte; // 2397
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2398
		}
		BIT_initDStream(BIT_DStream_t* bitD,
				const void* srcBuffer,
				size_t srcSize);  // 40627
	}
	{
		const BYTE* ostart; // 40630
		const BYTE* oend; // 40631
		const void* dtPtr; // 40632
		const HUF_DEltX2* dt; // 40633
		const DTableDesc  dtd; // 40634
		ZSTD_maybeNullPtrAdd(unsigned char* ptr,
					ptrdiff_t add);  // 40631
		HUF_getDTableDesc(const HUF_DTable* table); // 40634
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40635
	}
	BIT_endOfDStream(const BIT_DStream_t* DStream); // 40639
	HUF_decompress1X2_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 41018
} /* size: 1648, inline expansions: 2 (1648 bytes) */

// <06F5768E> thirdparty/zstd/zstd.c:41018
// variables: 16
// function calls: 82
size_t HUF_decompress1X2_usingDTable_internal_bmi2(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable)
{
	{
		const size_t  _var_err__; // 40627
		{
			const BYTE  lastByte; // 2369
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2370
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2368
		{
			const BYTE  lastByte; // 2397
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2398
		}
		BIT_initDStream(BIT_DStream_t* bitD,
				const void* srcBuffer,
				size_t srcSize);  // 40627
	}
	{
		const BYTE* ostart; // 40630
		const BYTE* oend; // 40631
		const void* dtPtr; // 40632
		const HUF_DEltX2* dt; // 40633
		const DTableDesc  dtd; // 40634
		ZSTD_maybeNullPtrAdd(unsigned char* ptr,
					ptrdiff_t add);  // 40631
		HUF_getDTableDesc(const HUF_DTable* table); // 40634
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40593
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40592
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40594
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40595
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40596
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40584
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40583
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40585
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40586
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40587
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40588
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40600
		{
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2495
		BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
		{
			U32 nbBytes; // 2538
			BIT_DStream_status result; // 2539
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 1335
			MEM_readLEST(const void* memPtr); // 2546
		}
		BIT_reloadDStream(BIT_DStream_t* bitD); // 40605
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40606
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40534
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40536
		HUF_decodeSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40609
		BIT_lookBitsFast(const BIT_DStream_t* bitD,
				U32 nbBits);  // 40543
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40549
		BIT_skipBits(BIT_DStream_t* bitD,
				U32 nbBits);  // 40546
		HUF_decodeLastSymbolX2(void* op,
					BIT_DStream_t* DStream,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40613
		HUF_decodeStreamX2(BYTE* p,
					BIT_DStream_t* bitDPtr,
					const BYTE* pEnd,
					const HUF_DEltX2* dt,
					const U32  dtLog);  // 40635
	}
	BIT_endOfDStream(const BIT_DStream_t* DStream); // 40639
	HUF_decompress1X2_usingDTable_internal_body(void* dst,
							size_t dstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable);  // 41018
} /* size: 1584, inline expansions: 2 (1588 bytes) */

// <06F5762D> thirdparty/zstd/zstd.c:41018
size_t HUF_decompress1X2_usingDTable_internal(void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, int flags)
{
} /* size: 0 */

// <06F5746D> thirdparty/zstd/zstd.c:41020
// variables: 2
// function call: 1
size_t HUF_decompress1X2_DCtx_wksp(HUF_DTable* DCtx, void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, void* workSpace, size_t wkspSize, int flags)
{
	const BYTE* ip; // 41024
	const size_t  hSize; // 41026
	HUF_decompress1X2_usingDTable_internal(void* dst,
						size_t dstSize,
						const void* cSrc,
						size_t cSrcSize,
						const HUF_DTable* DTable,
						int flags);  // 41032
} /* size: 0, variables: 2, inline expansions: 1 (0 bytes) */

// <06F3E257> thirdparty/zstd/zstd.c:41059
// member variables: 2
// struct size: 8
typedef struct {
	U32 tableTime; /* 0 4 */
	U32 decode256Time; /* 4 4 */
} algo_time_t;

// <06F5732C> thirdparty/zstd/zstd.c:41087
// variables: 4
U32 HUF_selectDecoder(size_t dstSize, size_t cSrcSize)
{
	{
		const U32  Q; // 41101
		const U32  D256; // 41102
		const U32  DTime0; // 41103
		U32 DTime1; // 41104
	}
} /* size: 110 */

// <06F57191> thirdparty/zstd/zstd.c:41111
// variable: 1
size_t HUF_decompress1X_DCtx_wksp(HUF_DTable* dctx, void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, void* workSpace, size_t wkspSize, int flags)
{
	{
		const U32  algoNb; // 41121
	}
} /* size: 0 */

// <06F56F3D> thirdparty/zstd/zstd.c:41142
// variable: 1
// function calls: 3
size_t HUF_decompress1X_usingDTable(void* dst, size_t maxDstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, int flags)
{
	const DTableDesc  dtd; // 41144
	HUF_getDTableDesc(const HUF_DTable* table); // 41144
	HUF_decompress1X2_usingDTable_internal(void* dst,
						size_t dstSize,
						const void* cSrc,
						size_t cSrcSize,
						const HUF_DTable* DTable,
						int flags);  // 41154
	HUF_decompress1X1_usingDTable_internal(void* dst,
						size_t dstSize,
						const void* cSrc,
						size_t cSrcSize,
						const HUF_DTable* DTable,
						int flags);  // 41155
} /* size: 61, variables: 1, inline expansions: 3 (50 bytes) */

// <06F56D7D> thirdparty/zstd/zstd.c:41160
// variables: 2
// function call: 1
size_t HUF_decompress1X1_DCtx_wksp(HUF_DTable* dctx, void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, void* workSpace, size_t wkspSize, int flags)
{
	const BYTE* ip; // 41162
	const size_t  hSize; // 41164
	HUF_decompress1X1_usingDTable_internal(void* dst,
						size_t dstSize,
						const void* cSrc,
						size_t cSrcSize,
						const HUF_DTable* DTable,
						int flags);  // 41169
} /* size: 179, variables: 2, inline expansions: 1 (36 bytes) */

// <06F56D0A> thirdparty/zstd/zstd.c:41173
// variable: 1
size_t HUF_decompress4X_usingDTable(void* dst, size_t maxDstSize, const void* cSrc, size_t cSrcSize, const HUF_DTable* DTable, int flags)
{
	const DTableDesc  dtd; // 41175
} /* size: 0, variables: 1 */

// <06F56C75> thirdparty/zstd/zstd.c:41190
// variable: 1
size_t HUF_decompress4X_hufOnly_wksp(HUF_DTable* dctx, void* dst, size_t dstSize, const void* cSrc, size_t cSrcSize, void* workSpace, size_t wkspSize, int flags)
{
	{
		const U32  algoNb; // 41196
	}
} /* size: 0 */

// <06F3E37A> thirdparty/zstd/zstd.c:41298
// member variables: 2
// struct size: 8
typedef struct {
	U32 fastMode; /* 0 4 */
	U32 tableLog; /* 4 4 */
} ZSTD_seqSymbol_header;

// <06F3E3BB> thirdparty/zstd/zstd.c:41303
// member variables: 4
// struct size: 8
typedef struct {
	U16 nextState; /* 0 2 */
	BYTE nbAdditionalBits; /* 2 1 */
	BYTE nbBits; /* 3 1 */
	U32 baseValue; /* 4 4 */
} ZSTD_seqSymbol;

// <06F3E41C> thirdparty/zstd/zstd.c:41316
// member variables: 6
// struct size: 27,292
typedef struct {
	ZSTD_seqSymbol LLTable[513]; /* 0 4104 */
	ZSTD_seqSymbol OFTable[257]; /* 4104 2056 */
	ZSTD_seqSymbol MLTable[513]; /* 6160 4104 */
	HUF_DTable hufTable[4097]; /* 10264 16388 */
	U32 rep[3]; /* 26652 12 */
	U32 workspace[157]; /* 26664 628 */
} ZSTD_entropyDTables_t;

// <06F3E5A7> thirdparty/zstd/zstd.c:41340
// member variables: 3
// struct size: 24
typedef struct {
	const ZSTD_DDict * * ddictPtrTable; /* 0 8 */
	size_t ddictPtrTableSize; /* 8 8 */
	size_t ddictPtrCount; /* 16 8 */
} ZSTD_DDictHashSet;

// <06F3A57B> thirdparty/zstd/zstd.c:41362
// member variables: 59
// struct size: 95,968
struct ZSTD_DCtx_s {
	const ZSTD_seqSymbol * LLTptr; /* 0 8 */
	const ZSTD_seqSymbol * MLTptr; /* 8 8 */
	const ZSTD_seqSymbol * OFTptr; /* 16 8 */
	const HUF_DTable * HUFptr; /* 24 8 */
	ZSTD_entropyDTables_t entropy; /* 32 27292 */
	U32 workspace[640]; /* 27324 2560 */
	const void * previousDstEnd; /* 29888 8 */
	const void * prefixStart; /* 29896 8 */
	const void * virtualStart; /* 29904 8 */
	const void * dictEnd; /* 29912 8 */
	size_t expected; /* 29920 8 */
	ZSTD_FrameHeader fParams; /* 29928 48 */
	U64 processedCSize; /* 29976 8 */
	U64 decodedSize; /* 29984 8 */
	blockType_e bType; /* 29992 4 */
	ZSTD_dStage stage; /* 29996 4 */
	U32 litEntropy; /* 30000 4 */
	U32 fseEntropy; /* 30004 4 */
	XXH_NAMESPACEXXH64_state_t xxhState; /* 30008 88 */
	size_t headerSize; /* 30096 8 */
	ZSTD_format_e format; /* 30104 4 */
	ZSTD_forceIgnoreChecksum_e forceIgnoreChecksum; /* 30108 4 */
	U32 validateChecksum; /* 30112 4 */
	const BYTE * litPtr; /* 30120 8 */
	ZSTD_customMem customMem; /* 30128 24 */
	size_t litSize; /* 30152 8 */
	size_t rleSize; /* 30160 8 */
	size_t staticSize; /* 30168 8 */
	int isFrameDecompression; /* 30176 4 */
	int bmi2; /* 30180 4 */
	ZSTD_DDict * ddictLocal; /* 30184 8 */
	const ZSTD_DDict * ddict; /* 30192 8 */
	U32 dictID; /* 30200 4 */
	int ddictIsCold; /* 30204 4 */
	ZSTD_dictUses_e dictUses; /* 30208 4 */
	ZSTD_DDictHashSet * ddictSet; /* 30216 8 */
	ZSTD_refMultipleDDicts_e refMultipleDDicts; /* 30224 4 */
	int disableHufAsm; /* 30228 4 */
	int maxBlockSizeParam; /* 30232 4 */
	ZSTD_dStreamStage streamStage; /* 30236 4 */
	char * inBuff; /* 30240 8 */
	size_t inBuffSize; /* 30248 8 */
	size_t inPos; /* 30256 8 */
	size_t maxWindowSize; /* 30264 8 */
	char * outBuff; /* 30272 8 */
	size_t outBuffSize; /* 30280 8 */
	size_t outStart; /* 30288 8 */
	size_t outEnd; /* 30296 8 */
	size_t lhSize; /* 30304 8 */
	U32 hostageByte; /* 30312 4 */
	int noForwardProgress; /* 30316 4 */
	ZSTD_bufferMode_e outBufferMode; /* 30320 4 */
	ZSTD_outBuffer expectedOutBuffer; /* 30328 24 */
	BYTE * litBuffer; /* 30352 8 */
	const BYTE * litBufferEnd; /* 30360 8 */
	ZSTD_litLocation_e litBufferLocation; /* 30368 4 */
	BYTE litExtraBuffer[65568]; /* 30372 65568 */
	BYTE headerBuffer[18]; /* 95940 18 */
	size_t oversizedDuration; /* 95960 8 */
};

// <06F56C4F> thirdparty/zstd/zstd.c:41449
inline int ZSTD_DCtx_get_bmi2(const ZSTD_DCtx_s* dctx)
{
} /* size: 0 */

// <06F3ADE7> thirdparty/zstd/zstd.c:41534
// member variables: 7
// struct size: 27,352
struct ZSTD_DDict_s {
	void * dictBuffer; /* 0 8 */
	const void * dictContent; /* 8 8 */
	size_t dictSize; /* 16 8 */
	ZSTD_entropyDTables_t entropy; /* 24 27292 */
	U32 dictID; /* 27316 4 */
	U32 entropyPresent; /* 27320 4 */
	ZSTD_customMem cMem; /* 27328 24 */
};

// <06F56C2A> thirdparty/zstd/zstd.c:41544
const void* ZSTD_DDict_dictContent(const ZSTD_DDict* ddict)
{
} /* size: 0 */

// <06F56C05> thirdparty/zstd/zstd.c:41550
size_t ZSTD_DDict_dictSize(const ZSTD_DDict* ddict)
{
} /* size: 0 */

// <06F56BD7> thirdparty/zstd/zstd.c:41556
void ZSTD_copyDDictParameters(ZSTD_DCtx* dctx, const ZSTD_DDict* ddict)
{
} /* size: 0 */

// <06F56B9B> thirdparty/zstd/zstd.c:41588
// variable: 1
size_t ZSTD_loadEntropy_intoDDict(ZSTD_DDict* ddict, ZSTD_dictContentType_e dictContentType)
{
	{
		const U32  magic; // 41600
	}
} /* size: 0 */

// <06F56913> thirdparty/zstd/zstd.c:41618
// variables: 3
// function calls: 6
size_t ZSTD_initDDict_internal(ZSTD_DDict* ddict, const void* dict, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType)
{
	{
		const void* internalBuffer; // 41628
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 41628
	}
	{
		const size_t  err_code; // 41638
		{
			const U32  magic; // 41600
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 41600
		}
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 41607
		ZSTD_loadEntropy_intoDDict(ZSTD_DDict* ddict,
						ZSTD_dictContentType_e dictContentType);  // 41638
	}
} /* size: 424 */

// <06F56799> thirdparty/zstd/zstd.c:41643
// variables: 2
// function call: 1
ZSTD_DDict* ZSTD_createDDict_advanced(const void* dict, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType, ZSTD_customMem customMem)
{
	{
		const ZSTD_DDict* ddict; // 41650
		{
			const size_t  initResult; // 41653
		}
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 41650
	}
} /* size: 222 */

// <06F56711> thirdparty/zstd/zstd.c:41668
// variable: 1
ZSTD_DDict* ZSTD_createDDict(const void* dict, size_t dictSize)
{
	const ZSTD_customMem  allocator; // 41670
} /* size: 57, variables: 1 */

// <06F56567> thirdparty/zstd/zstd.c:41685
// variables: 2
const ZSTD_DDict* ZSTD_initStaticDDict(void* sBuffer, size_t sBufferSize, const void* dict, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType)
{
	const size_t  neededSpace; // 41691
	const ZSTD_DDict* ddict; // 41693
} /* size: 151, variables: 2 */

// <06F56537> thirdparty/zstd/zstd.c:41710
// variable: 1
size_t ZSTD_freeDDict(ZSTD_DDict* ddict)
{
	{
		const ZSTD_customMem  cMem; // 41713
	}
} /* size: 0 */

// <06F564EC> thirdparty/zstd/zstd.c:41723
size_t ZSTD_estimateDDictSize(size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod)
{
} /* size: 24 */

// <06F564B6> thirdparty/zstd/zstd.c:41728
size_t ZSTD_sizeof_DDict(const ZSTD_DDict* ddict)
{
} /* size: 46 */

// <06F56440> thirdparty/zstd/zstd.c:41913
// variable: 1
size_t ZSTD_DDictHashSet_getIndex(const ZSTD_DDictHashSet* hashSet, U32 dictID)
{
	const U64  hash; // 41914
} /* size: 0, variables: 1 */

// <06F56274> thirdparty/zstd/zstd.c:41923
// variables: 3
// function calls: 3
size_t ZSTD_DDictHashSet_emplaceDDict(ZSTD_DDictHashSet* hashSet, const ZSTD_DDict* ddict)
{
	const U32  dictID; // 41924
	size_t idx; // 41925
	const size_t  idxRangeMask; // 41926
	XXH64_endian_align(const xxh_u8* input,
				size_t len,
				xxh_u64 seed,
				XXH_alignment align);  // 11813
	XXH_INLINE_XXH64(const void* input,
			size_t len,
			XXH64_hash_t seed);  // 41914
	ZSTD_DDictHashSet_getIndex(const ZSTD_DDictHashSet* hashSet,
					U32 dictID);  // 41925
} /* size: 254, variables: 3, inline expansions: 3 (112 bytes) */

// <06F561F9> thirdparty/zstd/zstd.c:41949
// variables: 6
size_t ZSTD_DDictHashSet_expand(ZSTD_DDictHashSet* hashSet, ZSTD_customMem customMem)
{
	size_t newTableSize; // 41950
	const ZSTD_DDict ** newTable; // 41951
	const ZSTD_DDict ** oldTable; // 41952
	size_t oldTableSize; // 41953
	size_t i; // 41954
	{
		const size_t  err_code; // 41963
	}
} /* size: 0, variables: 5 */

// <06F561A3> thirdparty/zstd/zstd.c:41974
// variables: 3
const ZSTD_DDict* ZSTD_DDictHashSet_getDDict(ZSTD_DDictHashSet* hashSet, U32 dictID)
{
	size_t idx; // 41975
	const size_t  idxRangeMask; // 41976
	{
		size_t currDictID; // 41979
	}
} /* size: 0, variables: 2 */

// <06F56176> thirdparty/zstd/zstd.c:41996
// variable: 1
ZSTD_DDictHashSet* ZSTD_createDDictHashSet(ZSTD_customMem customMem)
{
	ZSTD_DDictHashSet* ret; // 41997
} /* size: 0, variables: 1 */

// <06F5614D> thirdparty/zstd/zstd.c:42014
void ZSTD_freeDDictHashSet(ZSTD_DDictHashSet* hashSet, ZSTD_customMem customMem)
{
} /* size: 0 */

// <06F560F1> thirdparty/zstd/zstd.c:42027
// variables: 2
size_t ZSTD_DDictHashSet_addDDict(ZSTD_DDictHashSet* hashSet, const ZSTD_DDict* ddict, ZSTD_customMem customMem)
{
	{
		const size_t  err_code; // 42030
	}
	{
		const size_t  err_code; // 42032
	}
} /* size: 0 */

// <06F560D0> thirdparty/zstd/zstd.c:42039
size_t ZSTD_sizeof_DCtx(const ZSTD_DCtx* dctx)
{
} /* size: 0 */

// <06F560AE> thirdparty/zstd/zstd.c:42047
size_t ZSTD_estimateDCtxSize(void)
{
} /* size: 10 */

// <06F56081> thirdparty/zstd/zstd.c:42050
// variable: 1
size_t ZSTD_startingInputLength(ZSTD_format_e format)
{
	const size_t  startingInputLength; // 42052
} /* size: 0, variables: 1 */

// <06F56065> thirdparty/zstd/zstd.c:42058
void ZSTD_DCtx_resetParameters(ZSTD_DCtx* dctx)
{
} /* size: 0 */

// <06F56049> thirdparty/zstd/zstd.c:42070
void ZSTD_initDCtx_internal(ZSTD_DCtx* dctx)
{
} /* size: 0 */

// <06F5600E> thirdparty/zstd/zstd.c:42099
// variable: 1
ZSTD_DCtx* ZSTD_initStaticDCtx(void* workspace, size_t workspaceSize)
{
	const ZSTD_DCtx* dctx; // 42101
} /* size: 0, variables: 1 */

// <06F55E43> thirdparty/zstd/zstd.c:42112
// variables: 3
// function calls: 5
ZSTD_DCtx* ZSTD_createDCtx_internal(ZSTD_customMem customMem)
{
	{
		const ZSTD_DCtx* dctx; // 42115
		ZSTD_customMalloc(size_t size,
					ZSTD_customMem customMem);  // 42115
		{
			U32 f1a; // 8142
		}
		{
			U32 f7a; // 8146
		}
		ZSTD_cpuid(void); // 15816
		ZSTD_cpuSupportsBmi2(void); // 42090
		ZSTD_DCtx_resetParameters(ZSTD_DCtx* dctx); // 42093
		ZSTD_initDCtx_internal(ZSTD_DCtx* dctx); // 42118
	}
} /* size: 377 */

// <06F55E22> thirdparty/zstd/zstd.c:42123
ZSTD_DCtx* ZSTD_createDCtx_advanced(ZSTD_customMem customMem)
{
} /* size: 0 */

// <06F55DF6> thirdparty/zstd/zstd.c:42134
void ZSTD_clearDict(ZSTD_DCtx* dctx)
{
} /* size: 0 */

// <06F55B76> thirdparty/zstd/zstd.c:42164
// variable: 1
void ZSTD_copyDCtx(ZSTD_DCtx* dstDCtx, const ZSTD_DCtx* srcDCtx)
{
	const size_t  toCopy; // 42166
} /* size: 14, variables: 1 */

// <0709A4C9> thirdparty/zstd/zstd.c:42178
// variables: 2
// function calls: 5
void ZSTD_DCtx_selectFrameDDict(ZSTD_DCtx* dctx)
{
	{
		const ZSTD_DDict* frameDDict; // 42182
		XXH64_endian_align(const xxh_u8* input,
					size_t len,
					xxh_u64 seed,
					XXH_alignment align);  // 11813
		XXH_INLINE_XXH64(const void* input,
				size_t len,
				XXH64_hash_t seed);  // 41914
		ZSTD_DDictHashSet_getIndex(const ZSTD_DDictHashSet* hashSet,
						U32 dictID);  // 41975
		{
			size_t currDictID; // 41979
		}
		ZSTD_DDictHashSet_getDDict(ZSTD_DDictHashSet* hashSet,
						U32 dictID);  // 42182
		ZSTD_clearDict(ZSTD_DCtx* dctx); // 42185
	}
} /* size: 249 */

// <06F55B4B> thirdparty/zstd/zstd.c:42178
// variable: 1
void ZSTD_DCtx_selectFrameDDict(ZSTD_DCtx* dctx)
{
	{
		const ZSTD_DDict* frameDDict; // 42182
	}
} /* size: 0 */

// <06F55A7D> thirdparty/zstd/zstd.c:42203
// variable: 1
// function calls: 2
unsigned int ZSTD_isFrame(const void* buffer, size_t size)
{
	{
		const U32  magic; // 42206
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 42206
	}
} /* size: 42 */

// <06F55A40> thirdparty/zstd/zstd.c:42220
// variable: 1
unsigned int ZSTD_isSkippableFrame(const void* buffer, size_t size)
{
	{
		const U32  magic; // 42223
	}
} /* size: 0 */

// <070172D6> thirdparty/zstd/zstd.c:42234
// variables: 5
// function call: 1
size_t ZSTD_frameHeaderSize_internal(const void* src, size_t srcSize, ZSTD_format_e format)
{
	const size_t  minInputSize; // 42236
	ZSTD_startingInputLength(ZSTD_format_e format); // 42236
	{
		const BYTE  fhd; // 42239
		const U32  dictID; // 42240
		const U32  singleSegment; // 42241
		const U32  fcsId; // 42242
	}
} /* size: 105, variables: 1, inline expansions: 1 (13 bytes) */

// <06F559C3> thirdparty/zstd/zstd.c:42234
// variables: 5
size_t ZSTD_frameHeaderSize_internal(const void* src, size_t srcSize, ZSTD_format_e format)
{
	const size_t  minInputSize; // 42236
	{
		const BYTE  fhd; // 42239
		const U32  dictID; // 42240
		const U32  singleSegment; // 42241
		const U32  fcsId; // 42242
	}
} /* size: 0, variables: 1 */

// <06F558DE> thirdparty/zstd/zstd.c:42253
// variables: 4
// function call: 1
size_t ZSTD_frameHeaderSize(const void* src, size_t srcSize)
{
	{
		const BYTE  fhd; // 42239
		const U32  dictID; // 42240
		const U32  singleSegment; // 42241
		const U32  fcsId; // 42242
	}
	ZSTD_frameHeaderSize_internal(const void* src,
					size_t srcSize,
					ZSTD_format_e format);  // 42255
} /* size: 91, inline expansions: 1 (86 bytes) */

// <06F55351> thirdparty/zstd/zstd.c:42265
// variables: 16
// function calls: 17
size_t ZSTD_getFrameHeader_advanced(ZSTD_FrameHeader* zfhPtr, const void* src, size_t srcSize, ZSTD_format_e format)
{
	const BYTE* ip; // 42267
	const size_t  minInputSize; // 42268
	{
		const size_t  toCopy; // 42282
		unsigned char hbuf; // 42283
	}
	{
		const size_t  fhsize; // 42315
	}
	{
		const BYTE  fhdByte; // 42320
		size_t pos; // 42321
		const U32  dictIDSizeCode; // 42322
		const U32  checksumFlag; // 42323
		const U32  singleSegment; // 42324
		const U32  fcsID; // 42325
		U64 windowSize; // 42326
		U32 dictID; // 42327
		U64 frameContentSize; // 42328
		{
			const BYTE  wlByte; // 42333
			const U32  windowLog; // 42334
		}
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 42355
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 42357
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 42356
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 42347
		MEM_read16(const void* ptr); // 1269
		MEM_readLE16(const void* memPtr); // 42346
	}
	ZSTD_startingInputLength(ZSTD_format_e format); // 42268
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 42299
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 42306
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 42308
} /* size: 749, variables: 2, inline expansions: 7 (14 bytes) */

// <06F552BC> thirdparty/zstd/zstd.c:42377
size_t ZSTD_getFrameHeader(ZSTD_FrameHeader* zfhPtr, const void* src, size_t srcSize)
{
} /* size: 11 */

// <06F551C9> thirdparty/zstd/zstd.c:42405
// variables: 3
size_t readSkippableFrameSize(const void* src, size_t srcSize)
{
	const size_t  skippableHeaderSize; // 42407
	U32 sizeU32; // 42408
	{
		const size_t  skippableSize; // 42415
	}
} /* size: 0, variables: 2 */

// <06F54F6E> thirdparty/zstd/zstd.c:42432
// variables: 4
// function calls: 5
size_t ZSTD_readSkippableFrame(void* dst, size_t dstCapacity, unsigned int* magicVariant, const void* src, size_t srcSize)
{
	{
		const U32  magicNumber; // 42438
		size_t skippableFrameSize; // 42439
		size_t skippableContentSize; // 42440
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 42412
		{
			const size_t  skippableSize; // 42415
		}
		readSkippableFrameSize(const void* src,
					size_t srcSize);  // 42439
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 42438
	}
} /* size: 270 */

// <06F54CF4> thirdparty/zstd/zstd.c:42461
// variables: 6
// function calls: 5
long long unsigned int ZSTD_findDecompressedSize(const void* src, size_t srcSize)
{
	long long unsigned int totalDstSize; // 42463
	{
		const U32  magicNumber; // 42466
		{
			const size_t  skippableSize; // 42469
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 42412
			{
				const size_t  skippableSize; // 42415
			}
			readSkippableFrameSize(const void* src,
						size_t srcSize);  // 42469
		}
		{
			const long long unsigned int  fcs; // 42478
		}
		{
			const size_t  frameSrcSize; // 42486
		}
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 42466
	}
} /* size: 245, variables: 1 */

// <06F54C71> thirdparty/zstd/zstd.c:42508
// variable: 1
long long unsigned int ZSTD_getDecompressedSize(const void* src, size_t srcSize)
{
	const long long unsigned int  ret; // 42510
} /* size: 25, variables: 1 */

// <0709A9F3> thirdparty/zstd/zstd.c:42520
// variable: 1
// function calls: 4
size_t ZSTD_decodeFrameHeader(ZSTD_DCtx* dctx, const void* src, size_t headerSize)
{
	const size_t  result; // 42522
	ZSTD_DCtx_selectFrameDDict(ZSTD_DCtx* dctx); // 42528
	memset(void* __dest,
		int __ch,
		size_t __len);  // 11842
	XXH_INLINE_XXH64_reset(XXH_NAMESPACEXXH64_state_t* statePtr,
				XXH64_hash_t seed);  // 42539
	ZSTD_decodeFrameHeader(ZSTD_DCtx* dctx,
				const void* src,
				size_t headerSize);  // 42520
} /* size: 277, variables: 1, inline expansions: 4 (269 bytes) */

// <06F54C2A> thirdparty/zstd/zstd.c:42520
// variable: 1
size_t ZSTD_decodeFrameHeader(ZSTD_DCtx* dctx, const void* src, size_t headerSize)
{
	const size_t  result; // 42522
} /* size: 0, variables: 1 */

// <06F54BFD> thirdparty/zstd/zstd.c:42544
// variable: 1
ZSTD_frameSizeInfo ZSTD_errorFrameSizeInfo(size_t ret)
{
	ZSTD_frameSizeInfo frameSizeInfo; // 42546
} /* size: 0, variables: 1 */

// <06F54770> thirdparty/zstd/zstd.c:42552
// variables: 12
// function calls: 12
ZSTD_frameSizeInfo ZSTD_findFrameSizeInfo(const void* src, size_t srcSize, ZSTD_format_e format)
{
	ZSTD_frameSizeInfo frameSizeInfo; // 42554
	{
		const BYTE* ip; // 42569
		const BYTE* ipstart; // 42570
		size_t remainingSize; // 42571
		size_t nbBlocks; // 42572
		ZSTD_FrameHeader zfh; // 42573
		{
			const size_t  ret; // 42576
			ZSTD_errorFrameSizeInfo(size_t ret); // 42578
			ZSTD_errorFrameSizeInfo(size_t ret); // 42580
		}
		{
			blockProperties_t blockProperties; // 42588
			const size_t  cBlockSize; // 42589
			{
				const U32  cBlockHeader; // 44298
				const U32  cSize; // 44299
				MEM_read16(const void* ptr); // 1269
				MEM_readLE16(const void* memPtr); // 1289
				MEM_readLE24(const void* memPtr); // 44298
			}
			ZSTD_getcBlockSize(const void* src,
						size_t srcSize,
						blockProperties_t* bpPtr);  // 42589
			ZSTD_errorFrameSizeInfo(size_t ret); // 42594
		}
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 42563
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 42412
	{
		const size_t  skippableSize; // 42415
	}
	readSkippableFrameSize(const void* src,
				size_t srcSize);  // 42564
} /* size: 513, variables: 1, inline expansions: 5 (39 bytes) */

// <06F54729> thirdparty/zstd/zstd.c:42619
// variable: 1
size_t ZSTD_findFrameCompressedSize_advanced(const void* src, size_t srcSize, ZSTD_format_e format)
{
	const ZSTD_frameSizeInfo  frameSizeInfo; // 42620
} /* size: 0, variables: 1 */

// <06F54660> thirdparty/zstd/zstd.c:42627
// function call: 1
size_t ZSTD_findFrameCompressedSize(const void* src, size_t srcSize)
{
	ZSTD_findFrameCompressedSize_advanced(const void* src,
						size_t srcSize,
						ZSTD_format_e format);  // 42629
} /* size: 68, inline expansions: 1 (16 bytes) */

// <06F54568> thirdparty/zstd/zstd.c:42638
// variables: 4
long long unsigned int ZSTD_decompressBound(const void* src, size_t srcSize)
{
	long long unsigned int bound; // 42640
	{
		const ZSTD_frameSizeInfo  frameSizeInfo; // 42643
		const size_t  compressedSize; // 42644
		const long long unsigned int  decompressedBound; // 42645
	}
} /* size: 168, variables: 1 */

// <06F54409> thirdparty/zstd/zstd.c:42656
// variables: 7
size_t ZSTD_decompressionMargin(const void* src, size_t srcSize)
{
	size_t margin; // 42658
	unsigned int maxBlockSize; // 42659
	{
		const ZSTD_frameSizeInfo  frameSizeInfo; // 42663
		const size_t  compressedSize; // 42664
		const long long unsigned int  decompressedBound; // 42665
		ZSTD_FrameHeader zfh; // 42666
		{
			const size_t  err_code; // 42668
		}
	}
} /* size: 322, variables: 2 */

// <06F5435E> thirdparty/zstd/zstd.c:42705
// function call: 1
size_t ZSTD_insertBlock(ZSTD_DCtx* dctx, const void* blockStart, size_t blockSize)
{
	ZSTD_checkContinuity(ZSTD_DCtx* dctx,
				const void* dst,
				size_t dstSize);  // 42708
} /* size: 69, inline expansions: 1 (51 bytes) */

// <06F54317> thirdparty/zstd/zstd.c:42714
size_t ZSTD_copyRawBlock(void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
} /* size: 0 */

// <06F542D2> thirdparty/zstd/zstd.c:42727
size_t ZSTD_setRleBlock(void* dst, size_t dstCapacity, BYTE b, size_t regenSize)
{
} /* size: 0 */

// <06F5428F> thirdparty/zstd/zstd.c:42740
void ZSTD_DCtx_trace_end(const ZSTD_DCtx* dctx, U64 uncompressedSize, U64 compressedSize, int streaming)
{
} /* size: 0 */

// <06F54162> thirdparty/zstd/zstd.c:42771
// variables: 15
size_t ZSTD_decompressFrame(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, const void ** srcPtr, size_t* srcSizePtr)
{
	const BYTE* istart; // 42775
	const BYTE* ip; // 42776
	const BYTE* ostart; // 42777
	const BYTE* oend; // 42778
	BYTE* op; // 42779
	size_t remainingSrcSize; // 42780
	{
		const size_t  frameHeaderSize; // 42790
		{
			const size_t  err_code; // 42795
		}
	}
	{
		BYTE* oBlockEnd; // 42805
		size_t decodedSize; // 42806
		blockProperties_t blockProperties; // 42807
		const size_t  cBlockSize; // 42808
		{
			const size_t  err_code; // 42849
		}
	}
	{
		const U32  checkCalc; // 42870
		U32 checkRead; // 42871
	}
} /* size: 0, variables: 6 */

// <06F53653> thirdparty/zstd/zstd.c:42888
// variables: 20
// function calls: 29
size_t ZSTD_decompressMultiFrame(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const void* dict, size_t dictSize, const ZSTD_DDict* ddict)
{
	const void* dststart; // 42894
	int moreThan1Frame; // 42895
	{
		const U32  magicNumber; // 42939
		{
			const size_t  skippableSize; // 42943
			{
				const size_t  err_code; // 42944
			}
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 42412
			{
				const size_t  skippableSize; // 42415
			}
			readSkippableFrameSize(const void* src,
						size_t srcSize);  // 42943
		}
		MEM_read32(const void* ptr); // 1301
		MEM_readLE32(const void* memPtr); // 42939
	}
	{
		const size_t  err_code; // 42954
	}
	{
		const size_t  err_code; // 42958
	}
	{
		const size_t  res; // 42962
		{
			BYTE* oBlockEnd; // 42805
			size_t decodedSize; // 42806
			blockProperties_t blockProperties; // 42807
			const size_t  cBlockSize; // 42808
			{
				const U32  cBlockHeader; // 44298
				const U32  cSize; // 44299
				MEM_read16(const void* ptr); // 1269
				MEM_readLE16(const void* memPtr); // 1289
				MEM_readLE24(const void* memPtr); // 44298
			}
			ZSTD_getcBlockSize(const void* src,
						size_t srcSize,
						blockProperties_t* bpPtr);  // 42808
			ZSTD_setRleBlock(void* dst,
					size_t dstCapacity,
					BYTE b,
					size_t regenSize);  // 42843
			{
				const size_t  err_code; // 42849
			}
			ZSTD_copyRawBlock(void* dst,
						size_t dstCapacity,
						const void* src,
						size_t srcSize);  // 42840
			ZSTD_blockSizeMax(const ZSTD_DCtx* dctx); // 46311
			ZSTD_decompressBlock_internal(ZSTD_DCtx* dctx,
							void* dst,
							size_t dstCapacity,
							const void* src,
							size_t srcSize,
							const streaming_operation  streaming);  // 42836
		}
		{
			const size_t  frameHeaderSize; // 42790
			{
				const size_t  err_code; // 42795
			}
		}
		{
			const U32  checkCalc; // 42870
			U32 checkRead; // 42871
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11909
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11910
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11911
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11912
			XXH_INLINE_XXH64_digest(const XXH_NAMESPACEXXH64_state_t* state); // 42870
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 42872
		}
		ZSTD_decompressFrame(ZSTD_DCtx* dctx,
					void* dst,
					size_t dstCapacity,
					const void ** srcPtr,
					size_t* srcSizePtr);  // 42962
	}
	ZSTD_DDict_dictContent(const ZSTD_DDict* ddict); // 42901
	ZSTD_DDict_dictSize(const ZSTD_DDict* ddict); // 42902
	ZSTD_startingInputLength(ZSTD_format_e format); // 42905
	ZSTD_checkContinuity(ZSTD_DCtx* dctx,
				const void* dst,
				size_t dstSize);  // 42960
} /* size: 1901, variables: 2, inline expansions: 4 (111 bytes) */

// <06F53565> thirdparty/zstd/zstd.c:42989
size_t ZSTD_decompress_usingDict(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const void* dict, size_t dictSize)
{
} /* size: 20 */

// <06F53545> thirdparty/zstd/zstd.c:42998
const ZSTD_DDict* ZSTD_getDDict(ZSTD_DCtx* dctx)
{
} /* size: 0 */

// <06F533F3> thirdparty/zstd/zstd.c:43015
// function calls: 3
size_t ZSTD_decompressDCtx(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	ZSTD_clearDict(ZSTD_DCtx* dctx); // 43005
	ZSTD_getDDict(ZSTD_DCtx* dctx); // 42998
	ZSTD_getDDict(ZSTD_DCtx* dctx); // 43017
} /* size: 153, inline expansions: 3 (267 bytes) */

// <06F532C6> thirdparty/zstd/zstd.c:43042
size_t ZSTD_nextSrcSizeToDecompress(ZSTD_DCtx* dctx)
{
} /* size: 12 */

// <06F53299> thirdparty/zstd/zstd.c:43054
size_t ZSTD_nextSrcSizeToDecompressWithInputSize(ZSTD_DCtx* dctx, size_t inputSize)
{
} /* size: 0 */

// <06F53263> thirdparty/zstd/zstd.c:43062
ZSTD_nextInputType_e ZSTD_nextInputType(ZSTD_DCtx* dctx)
{
} /* size: 33 */

// <06F53243> thirdparty/zstd/zstd.c:43087
int ZSTD_isSkipFrame(ZSTD_DCtx* dctx)
{
} /* size: 0 */

// <06F5316B> thirdparty/zstd/zstd.c:43093
// variables: 8
size_t ZSTD_decompressContinue(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	{
		const size_t  err_code; // 43124
	}
	{
		blockProperties_t bp; // 43130
		const size_t  cBlockSize; // 43131
	}
	{
		size_t rSize; // 43160
		{
			const size_t  err_code; // 43172
		}
		{
			const size_t  err_code; // 43184
		}
	}
	{
		const U32  h32; // 43221
		const U32  check32; // 43222
	}
} /* size: 0 */

// <06F53131> thirdparty/zstd/zstd.c:43253
size_t ZSTD_refDictContent(ZSTD_DCtx* dctx, const void* dict, size_t dictSize)
{
} /* size: 0 */

// <06F52911> thirdparty/zstd/zstd.c:43270
// variables: 20
// function calls: 11
size_t ZSTD_loadDEntropy(ZSTD_entropyDTables_t* entropy, const void* dict, const size_t dictSize)
{
	const BYTE* dictPtr; // 43273
	const BYTE* dictEnd; // 43274
	{
		const void* workspace; // 43283
		const size_t  workspaceSize; // 43284
		const size_t  hSize; // 43291
	}
	{
		short int offcodeNCount; // 43299
		unsigned int offcodeMaxValue; // 43300
		unsigned int offcodeLog; // 43300
		const size_t  offcodeHeaderSize; // 43301
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3437
		FSE_readNCount(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 43301
		ZSTD_buildFSETable(ZSTD_seqSymbol* dt,
					const short int* normalizedCounter,
					unsigned int maxSymbolValue,
					const U32* baseValue,
					const U8* nbAdditionalBits,
					unsigned int tableLog,
					void* wksp,
					size_t wkspSize,
					int bmi2);  // 43305
	}
	{
		short int matchlengthNCount; // 43314
		unsigned int matchlengthMaxValue; // 43315
		unsigned int matchlengthLog; // 43315
		const size_t  matchlengthHeaderSize; // 43316
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3437
		FSE_readNCount(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 43316
		ZSTD_buildFSETable(ZSTD_seqSymbol* dt,
					const short int* normalizedCounter,
					unsigned int maxSymbolValue,
					const U32* baseValue,
					const U8* nbAdditionalBits,
					unsigned int tableLog,
					void* wksp,
					size_t wkspSize,
					int bmi2);  // 43320
	}
	{
		short int litlengthNCount; // 43329
		unsigned int litlengthMaxValue; // 43330
		unsigned int litlengthLog; // 43330
		const size_t  litlengthHeaderSize; // 43331
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3437
		FSE_readNCount(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 43331
		ZSTD_buildFSETable(ZSTD_seqSymbol* dt,
					const short int* normalizedCounter,
					unsigned int maxSymbolValue,
					const U32* baseValue,
					const U8* nbAdditionalBits,
					unsigned int tableLog,
					void* wksp,
					size_t wkspSize,
					int bmi2);  // 43335
	}
	{
		int i; // 43345
		const size_t  dictContentSize; // 43346
		{
			const U32  rep; // 43348
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 43348
		}
	}
} /* size: 682, variables: 2 */

// <06F528B5> thirdparty/zstd/zstd.c:43357
// variables: 2
size_t ZSTD_decompress_insertDictionary(ZSTD_DCtx* dctx, const void* dict, size_t dictSize)
{
	{
		const U32  magic; // 43360
	}
	{
		const size_t  eSize; // 43367
	}
} /* size: 0 */

// <06F52853> thirdparty/zstd/zstd.c:43378
// function call: 1
size_t ZSTD_decompressBegin(ZSTD_DCtx* dctx)
{
	ZSTD_startingInputLength(ZSTD_format_e format); // 43384
} /* size: 188, inline expansions: 1 (17 bytes) */

// <06F52765> thirdparty/zstd/zstd.c:43419
// variables: 4
size_t ZSTD_decompressBegin_usingDDict(ZSTD_DCtx* dctx, const ZSTD_DDict* ddict)
{
	{
		const char* dictStart; // 43424
		const size_t  dictSize; // 43425
		const void* dictEnd; // 43426
	}
	{
		const size_t  err_code; // 43431
	}
} /* size: 0 */

// <06F52737> thirdparty/zstd/zstd.c:43442
unsigned int ZSTD_getDictID_fromDict(const void* dict, size_t dictSize)
{
} /* size: 0 */

// <06F52593> thirdparty/zstd/zstd.c:43490
ZSTD_DStream* ZSTD_createDStream(void)
{
} /* size: 0 */

// <06F52525> thirdparty/zstd/zstd.c:43496
ZSTD_DStream* ZSTD_initStaticDStream(void* workspace, size_t workspaceSize)
{
} /* size: 9 */

// <06F52505> thirdparty/zstd/zstd.c:43501
ZSTD_DStream* ZSTD_createDStream_advanced(ZSTD_customMem customMem)
{
} /* size: 0 */

// <06F524B3> thirdparty/zstd/zstd.c:43506
size_t ZSTD_freeDStream(ZSTD_DStream* zds)
{
} /* size: 9 */

// <06F52491> thirdparty/zstd/zstd.c:43514
size_t ZSTD_DStreamInSize(void)
{
} /* size: 10 */

// <06F52482> thirdparty/zstd/zstd.c:43515
size_t ZSTD_DStreamOutSize(void)
{
} /* size: 0 */

// <06F5242D> thirdparty/zstd/zstd.c:43517
size_t ZSTD_DCtx_loadDictionary_advanced(ZSTD_DCtx* dctx, const void* dict, size_t dictSize, ZSTD_dictLoadMethod_e dictLoadMethod, ZSTD_dictContentType_e dictContentType)
{
} /* size: 0 */

// <06F52399> thirdparty/zstd/zstd.c:43533
size_t ZSTD_DCtx_loadDictionary_byReference(ZSTD_DCtx* dctx, const void* dict, size_t dictSize)
{
} /* size: 17 */

// <06F52305> thirdparty/zstd/zstd.c:43538
size_t ZSTD_DCtx_loadDictionary(ZSTD_DCtx* dctx, const void* dict, size_t dictSize)
{
} /* size: 14 */

// <06F52240> thirdparty/zstd/zstd.c:43543
// variable: 1
size_t ZSTD_DCtx_refPrefix_advanced(ZSTD_DCtx* dctx, const void* prefix, size_t prefixSize, ZSTD_dictContentType_e dictContentType)
{
	{
		const size_t  err_code; // 43545
	}
} /* size: 53 */

// <06F521B1> thirdparty/zstd/zstd.c:43550
size_t ZSTD_DCtx_refPrefix(ZSTD_DCtx* dctx, const void* prefix, size_t prefixSize)
{
} /* size: 14 */

// <06F5209D> thirdparty/zstd/zstd.c:43559
// variables: 2
// function call: 1
size_t ZSTD_initDStream_usingDict(ZSTD_DStream* zds, const void* dict, size_t dictSize)
{
	{
		const size_t  err_code; // 43562
	}
	{
		const size_t  err_code; // 43563
	}
	ZSTD_startingInputLength(ZSTD_format_e format); // 43564
} /* size: 104, inline expansions: 1 (17 bytes) */

// <06F51FBA> thirdparty/zstd/zstd.c:43568
// variables: 2
// function call: 1
size_t ZSTD_initDStream(ZSTD_DStream* zds)
{
	{
		const size_t  err_code; // 43571
	}
	{
		const size_t  err_code; // 43572
	}
	ZSTD_startingInputLength(ZSTD_format_e format); // 43573
} /* size: 87, inline expansions: 1 (17 bytes) */

// <06F51EC1> thirdparty/zstd/zstd.c:43579
// variables: 2
// function call: 1
size_t ZSTD_initDStream_usingDDict(ZSTD_DStream* dctx, const ZSTD_DDict* ddict)
{
	{
		const size_t  err_code; // 43582
	}
	{
		const size_t  err_code; // 43583
	}
	ZSTD_startingInputLength(ZSTD_format_e format); // 43584
} /* size: 87, inline expansions: 1 (17 bytes) */

// <06F51E16> thirdparty/zstd/zstd.c:43590
// variable: 1
// function call: 1
size_t ZSTD_resetDStream(ZSTD_DStream* dctx)
{
	{
		const size_t  err_code; // 43593
	}
	ZSTD_startingInputLength(ZSTD_format_e format); // 43594
} /* size: 55, inline expansions: 1 (17 bytes) */

// <06F51DD9> thirdparty/zstd/zstd.c:43598
// variable: 1
size_t ZSTD_DCtx_refDDict(ZSTD_DCtx* dctx, const ZSTD_DDict* ddict)
{
	{
		const size_t  err_code; // 43613
	}
} /* size: 0 */

// <06F51D44> thirdparty/zstd/zstd.c:43622
// variables: 3
size_t ZSTD_DCtx_setMaxWindowSize(ZSTD_DCtx* dctx, size_t maxWindowSize)
{
	const ZSTD_bounds  bounds; // 43624
	const size_t  min; // 43625
	const size_t  max; // 43626
} /* size: 108, variables: 3 */

// <06F51CCF> thirdparty/zstd/zstd.c:43634
size_t ZSTD_DCtx_setFormat(ZSTD_DCtx* dctx, ZSTD_format_e format)
{
} /* size: 16 */

// <06F51C84> thirdparty/zstd/zstd.c:43639
// variable: 1
ZSTD_bounds ZSTD_dParam_getBounds(ZSTD_dParameter dParam)
{
	ZSTD_bounds bounds; // 43641
} /* size: 154, variables: 1 */

// <06F51C4A> thirdparty/zstd/zstd.c:43682
// variable: 1
int ZSTD_dParam_withinBounds(ZSTD_dParameter dParam, int value)
{
	const ZSTD_bounds  bounds; // 43684
} /* size: 0, variables: 1 */

// <06F51B8E> thirdparty/zstd/zstd.c:43695
// function calls: 2
size_t ZSTD_DCtx_getParameter(ZSTD_DCtx* dctx, ZSTD_dParameter param, int* value)
{
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 43699
} /* size: 176, inline expansions: 2 (9 bytes) */

// <06F51805> thirdparty/zstd/zstd.c:43724
// function calls: 7
size_t ZSTD_DCtx_setParameter(ZSTD_DCtx* dctx, ZSTD_dParameter dParam, int value)
{
	ZSTD_dParam_withinBounds(ZSTD_dParameter dParam,
				int value);  // 43746
	ZSTD_dParam_withinBounds(ZSTD_dParameter dParam,
				int value);  // 43742
	ZSTD_dParam_withinBounds(ZSTD_dParameter dParam,
				int value);  // 43738
	ZSTD_dParam_withinBounds(ZSTD_dParameter dParam,
				int value);  // 43753
	ZSTD_dParam_withinBounds(ZSTD_dParameter dParam,
				int value);  // 43734
	ZSTD_dParam_withinBounds(ZSTD_dParameter dParam,
				int value);  // 43730
	ZSTD_dParam_withinBounds(ZSTD_dParameter dParam,
				int value);  // 43757
} /* size: 668, inline expansions: 7 (348 bytes) */

// <06F5174C> thirdparty/zstd/zstd.c:43765
// function calls: 2
size_t ZSTD_DCtx_reset(ZSTD_DCtx* dctx, ZSTD_ResetDirective reset)
{
	ZSTD_clearDict(ZSTD_DCtx* dctx); // 43776
	ZSTD_DCtx_resetParameters(ZSTD_DCtx* dctx); // 43777
} /* size: 189, inline expansions: 2 (94 bytes) */

// <06F516FA> thirdparty/zstd/zstd.c:43783
size_t ZSTD_sizeof_DStream(const ZSTD_DStream* dctx)
{
} /* size: 9 */

// <06F5168C> thirdparty/zstd/zstd.c:43788
// variables: 4
size_t ZSTD_decodingBufferSize_internal(long long unsigned int windowSize, long long unsigned int frameContentSize, size_t blockSizeMax)
{
	const size_t  blockSize; // 43790
	const long long unsigned int  neededRBSize; // 43798
	const long long unsigned int  neededSize; // 43799
	const size_t  minRBSize; // 43800
} /* size: 0, variables: 4 */

// <06F515C8> thirdparty/zstd/zstd.c:43806
// function call: 1
size_t ZSTD_decodingBufferSize_min(long long unsigned int windowSize, long long unsigned int frameContentSize)
{
	ZSTD_decodingBufferSize_internal(long long unsigned int windowSize,
					long long unsigned int frameContentSize,
					size_t blockSizeMax);  // 43808
} /* size: 29, inline expansions: 1 (24 bytes) */

// <06F51521> thirdparty/zstd/zstd.c:43811
// variables: 3
size_t ZSTD_estimateDStreamSize(size_t windowSize)
{
	const size_t  blockSize; // 43813
	const size_t  inBuffSize; // 43814
	const size_t  outBuffSize; // 43815
} /* size: 57, variables: 3 */

// <06F5143D> thirdparty/zstd/zstd.c:43819
// variables: 3
size_t ZSTD_estimateDStreamSize_fromFrame(const void* src, size_t srcSize)
{
	const U32  windowSizeMax; // 43821
	ZSTD_FrameHeader zfh; // 43822
	const size_t  err; // 43823
} /* size: 134, variables: 3 */

// <06F51403> thirdparty/zstd/zstd.c:43834
int ZSTD_DCtx_isOverflow(ZSTD_DStream* zds, const size_t neededInBuffSize, const size_t neededOutBuffSize)
{
} /* size: 0 */

// <06F513CD> thirdparty/zstd/zstd.c:43839
void ZSTD_DCtx_updateOversizedDuration(ZSTD_DStream* zds, const size_t neededInBuffSize, const size_t neededOutBuffSize)
{
} /* size: 0 */

// <06F513AD> thirdparty/zstd/zstd.c:43847
int ZSTD_DCtx_isOversizedTooLong(ZSTD_DStream* zds)
{
} /* size: 0 */

// <06F51367> thirdparty/zstd/zstd.c:43853
// variable: 1
size_t ZSTD_checkOutBuffer(const ZSTD_DStream* zds, const ZSTD_outBuffer* output)
{
	const ZSTD_outBuffer  expect; // 43855
} /* size: 0, variables: 1 */

// <06F511A1> thirdparty/zstd/zstd.c:43875
// variables: 7
// function call: 1
size_t ZSTD_decompressContinueStream(ZSTD_DStream* zds, char** op, char* oend, const void* src, size_t srcSize)
{
	const int  isSkipFrame; // 43878
	{
		const size_t  dstSize; // 43880
		const size_t  decodedSize; // 43881
		{
			const size_t  err_code; // 43883
		}
	}
	{
		const size_t  dstSize; // 43892
		const size_t  decodedSize; // 43893
		{
			const size_t  err_code; // 43894
		}
	}
	ZSTD_isSkipFrame(ZSTD_DCtx* dctx); // 43878
} /* size: 212, variables: 1, inline expansions: 1 (6 bytes) */

// <06F50658> thirdparty/zstd/zstd.c:43904
// variables: 33
// function calls: 23
size_t ZSTD_decompressStream(ZSTD_DStream* zds, ZSTD_outBuffer* output, ZSTD_inBuffer* input)
{
	const char* src; // 43906
	const char* istart; // 43907
	const char* iend; // 43908
	const char* ip; // 43909
	const char* dst; // 43910
	const char* ostart; // 43911
	const char* oend; // 43912
	char* op; // 43913
	U32 someMoreWork; // 43914
	{
		const size_t  err_code; // 43929
		ZSTD_checkOutBuffer(const ZSTD_DStream* zds,
					const ZSTD_outBuffer* output);  // 43929
	}
	{
		const size_t  hSize; // 43956
		{
			const size_t  toLoad; // 43982
			const size_t  remainingInput; // 43983
			{
				const size_t  err_code; // 43992
			}
		}
		ZSTD_DCtx_selectFrameDDict(ZSTD_DCtx* dctx); // 43958
	}
	{
		const size_t  cSize; // 44007
		{
			const size_t  decompressedSize; // 44010
			ZSTD_clearDict(ZSTD_DCtx* dctx); // 43005
			ZSTD_getDDict(ZSTD_DCtx* dctx); // 42998
			ZSTD_getDDict(ZSTD_DCtx* dctx); // 44010
		}
		ZSTD_findFrameCompressedSize_advanced(const void* src,
							size_t srcSize,
							ZSTD_format_e format);  // 44007
	}
	{
		const size_t  err_code; // 44032
		ZSTD_clearDict(ZSTD_DCtx* dctx); // 43005
		ZSTD_getDDict(ZSTD_DCtx* dctx); // 42998
		ZSTD_getDDict(ZSTD_DCtx* dctx); // 44032
	}
	{
		const size_t  err_code; // 44039
	}
	{
		const size_t  neededInBuffSize; // 44055
		const size_t  neededOutBuffSize; // 44056
		{
			const int  tooSmall; // 44062
			const int  tooLarge; // 44063
			{
				const size_t  bufferSize; // 44066
				ZSTD_customFree(void* ptr,
						ZSTD_customMem customMem);  // 44078
				ZSTD_customMalloc(size_t size,
							ZSTD_customMem customMem);  // 44081
			}
		}
		ZSTD_decodingBufferSize_internal(long long unsigned int windowSize,
						long long unsigned int frameContentSize,
						size_t blockSizeMax);  // 44057
		ZSTD_DCtx_isOverflow(ZSTD_DStream* zds,
					const size_t  neededInBuffSize,
					const size_t  neededOutBuffSize);  // 43841
		ZSTD_DCtx_updateOversizedDuration(ZSTD_DStream* zds,
							const size_t  neededInBuffSize,
							const size_t  neededOutBuffSize);  // 44060
	}
	{
		const size_t  neededInSize; // 44093
		{
			const size_t  err_code; // 44101
		}
		ZSTD_nextSrcSizeToDecompressWithInputSize(ZSTD_DCtx* dctx,
								size_t inputSize);  // 43054
		ZSTD_nextSrcSizeToDecompressWithInputSize(ZSTD_DCtx* dctx,
								size_t inputSize);  // 44093
	}
	{
		const size_t  neededInSize; // 44112
		const size_t  toLoad; // 44113
		const int  isSkipFrame; // 44114
		size_t loadedSize; // 44115
		{
			const size_t  err_code; // 44135
		}
		ZSTD_isSkipFrame(ZSTD_DCtx* dctx); // 44114
		ZSTD_limitCopy(void* dst,
				size_t dstCapacity,
				const void* src,
				size_t srcSize);  // 44124
	}
	{
		const size_t  toFlushSize; // 44141
		const size_t  flushedSize; // 44142
		ZSTD_limitCopy(void* dst,
				size_t dstCapacity,
				const void* src,
				size_t srcSize);  // 44142
	}
	{
		size_t nextSrcSizeHint; // 44184
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 44035
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 44036
} /* size: 0, variables: 9, inline expansions: 4 (0 bytes) */

// <06F50533> thirdparty/zstd/zstd.c:44210
// variables: 3
size_t ZSTD_decompressStream_simpleArgs(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, size_t* dstPos, const void* src, size_t srcSize, size_t* srcPos)
{
	ZSTD_outBuffer output; // 44215
	ZSTD_inBuffer input; // 44216
	{
		const size_t  cErr; // 44223
	}
} /* size: 128, variables: 2 */

// <06F5050A> thirdparty/zstd/zstd.c:44277
void ZSTD_copy4(void* dst, const void* src)
{
} /* size: 0 */

// <06F504D7> thirdparty/zstd/zstd.c:44284
// variable: 1
size_t ZSTD_blockSizeMax(const ZSTD_DCtx* dctx)
{
	const size_t  blockSizeMax; // 44286
} /* size: 0, variables: 1 */

// <06F50476> thirdparty/zstd/zstd.c:44293
// variables: 2
size_t ZSTD_getcBlockSize(const void* src, size_t srcSize, blockProperties_t* bpPtr)
{
	{
		const U32  cBlockHeader; // 44298
		const U32  cSize; // 44299
	}
} /* size: 0 */

// <06F50387> thirdparty/zstd/zstd.c:44310
// variable: 1
// function call: 1
void ZSTD_allocateLiteralsBuffer(ZSTD_DCtx* dctx, const void* dst, const size_t dstCapacity, const size_t litSize, const streaming_operation streaming, const size_t expectedWriteSize, const unsigned int splitImmediately)
{
	const size_t  blockSizeMax; // 44313
	ZSTD_blockSizeMax(const ZSTD_DCtx* dctx); // 44313
} /* size: 213, variables: 1, inline expansions: 1 (23 bytes) */

// <06F4FB4B> thirdparty/zstd/zstd.c:44364
// variables: 23
// function calls: 16
size_t ZSTD_decodeLiteralsBlock(ZSTD_DCtx* dctx, const void* src, size_t srcSize, void* dst, size_t dstCapacity, const streaming_operation streaming)
{
	{
		const BYTE* istart; // 44371
		const SymbolEncodingType_e  litEncType; // 44372
		const size_t  blockSizeMax; // 44373
		{
			size_t lhSize; // 44384
			size_t litSize; // 44384
			size_t litCSize; // 44384
			U32 singleStream; // 44385
			const U32  lhlCode; // 44386
			const U32  lhc; // 44387
			size_t hufSuccess; // 44388
			size_t expectedWriteSize; // 44389
			const int  flags; // 44390
			{
				const char* _ptr; // 44427
				const size_t  _size; // 44427
				size_t _pos; // 44427
			}
			MEM_read32(const void* ptr); // 1301
			MEM_readLE32(const void* memPtr); // 44387
			ZSTD_DCtx_get_bmi2(const ZSTD_DCtx_s* dctx); // 44391
			HUF_getDTableDesc(const HUF_DTable* table); // 41175
			HUF_decompress4X_usingDTable(void* dst,
							size_t maxDstSize,
							const void* cSrc,
							size_t cSrcSize,
							const HUF_DTable* DTable,
							int flags);  // 44437
		}
		{
			size_t litSize; // 44481
			size_t lhSize; // 44481
			const U32  lhlCode; // 44482
			size_t expectedWriteSize; // 44483
			MEM_read16(const void* ptr); // 1269
			MEM_readLE16(const void* memPtr); // 44492
			MEM_read16(const void* ptr); // 1269
			MEM_readLE16(const void* memPtr); // 1289
			MEM_readLE24(const void* memPtr); // 44497
		}
		{
			const U32  lhlCode; // 44529
			size_t litSize; // 44530
			size_t lhSize; // 44530
			size_t expectedWriteSize; // 44531
			MEM_read16(const void* ptr); // 1269
			MEM_readLE16(const void* memPtr); // 44541
			MEM_read16(const void* ptr); // 1269
			MEM_readLE16(const void* memPtr); // 1289
			MEM_readLE24(const void* memPtr); // 44546
		}
		ZSTD_blockSizeMax(const ZSTD_DCtx* dctx); // 44373
	}
} /* size: 1845 */

// <06F4FA80> thirdparty/zstd/zstd.c:44576
size_t ZSTD_decodeLiteralsBlock_wrapper(ZSTD_DCtx* dctx, const void* src, size_t srcSize, void* dst, size_t dstCapacity)
{
} /* size: 0 */

// <06F4FA19> thirdparty/zstd/zstd.c:44693
// variables: 3
void ZSTD_buildSeqTable_rle(ZSTD_seqSymbol* dt, U32 baseValue, U8 nbAddBits)
{
	void* ptr; // 44695
	const ZSTD_seqSymbol_header* DTableH; // 44696
	const ZSTD_seqSymbol* cell; // 44697
} /* size: 0, variables: 3 */

// <06F4F7FD> thirdparty/zstd/zstd.c:44715
// variables: 31
inline void ZSTD_buildFSETable_body(ZSTD_seqSymbol* dt, const short int* normalizedCounter, unsigned int maxSymbolValue, const U32* baseValue, const U8* nbAdditionalBits, unsigned int tableLog, void* wksp, size_t wkspSize)
{
	const ZSTD_seqSymbol* tableDecode; // 44720
	const U32  maxSV1; // 44721
	const U32  tableSize; // 44722
	U16* symbolNext; // 44724
	BYTE* spread; // 44725
	U32 highThreshold; // 44726
	{
		ZSTD_seqSymbol_header DTableH; // 44735
		{
			const S16  largeLimit; // 44738
			U32 s; // 44739
		}
	}
	{
		const size_t  tableMask; // 44760
		const size_t  step; // 44761
		{
			const U64  add; // 44769
			size_t pos; // 44770
			U64 sv; // 44771
			U32 s; // 44772
			{
				int i; // 44774
				const int  n; // 44775
			}
		}
		{
			size_t position; // 44791
			size_t s; // 44792
			const size_t  unroll; // 44793
			{
				size_t u; // 44796
				{
					const size_t  uPosition; // 44798
				}
			}
		}
	}
	{
		const U32  tableMask; // 44806
		const U32  step; // 44807
		U32 s; // 44808
		U32 position; // 44808
		{
			int i; // 44810
			const int  n; // 44811
		}
	}
	{
		U32 u; // 44822
		{
			const U32  symbol; // 44824
			const U32  nextState; // 44825
		}
	}
} /* size: 0, variables: 6 */

// <07018856> thirdparty/zstd/zstd.c:44836
// variables: 25
// function calls: 5
void ZSTD_buildFSETable_body_default(ZSTD_seqSymbol* dt, const short int* normalizedCounter, unsigned int maxSymbolValue, const U32* baseValue, const U8* nbAdditionalBits, unsigned int tableLog, void* wksp, size_t wkspSize)
{
	{
		ZSTD_seqSymbol_header DTableH; // 44735
		{
			const S16  largeLimit; // 44738
			U32 s; // 44739
		}
	}
	{
		const size_t  tableMask; // 44760
		const size_t  step; // 44761
		{
			const U64  add; // 44769
			size_t pos; // 44770
			U64 sv; // 44771
			U32 s; // 44772
			{
				int i; // 44774
				const int  n; // 44775
				MEM_write64(void* memPtr,
						U64 value);  // 44776
				MEM_write64(void* memPtr,
						U64 value);  // 44778
			}
		}
		{
			size_t position; // 44791
			size_t s; // 44792
			const size_t  unroll; // 44793
			{
				size_t u; // 44796
				{
					const size_t  uPosition; // 44798
				}
			}
		}
	}
	{
		const U32  tableMask; // 44806
		const U32  step; // 44807
		U32 s; // 44808
		U32 position; // 44808
		{
			int i; // 44810
			const int  n; // 44811
		}
	}
	{
		U32 u; // 44822
		{
			const U32  symbol; // 44824
			const U32  nextState; // 44825
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 44826
		}
	}
	ZSTD_buildFSETable_body(ZSTD_seqSymbol* dt,
				const short int* normalizedCounter,
				unsigned int maxSymbolValue,
				const U32* baseValue,
				const U8* nbAdditionalBits,
				unsigned int tableLog,
				void* wksp,
				size_t wkspSize);  // 44841
} /* size: 770, inline expansions: 1 (736 bytes) */

// <06F4F787> thirdparty/zstd/zstd.c:44836
void ZSTD_buildFSETable_body_default(ZSTD_seqSymbol* dt, const short int* normalizedCounter, unsigned int maxSymbolValue, const U32* baseValue, const U8* nbAdditionalBits, unsigned int tableLog, void* wksp, size_t wkspSize)
{
} /* size: 0 */

// <070184A5> thirdparty/zstd/zstd.c:44846
// variables: 25
// function calls: 5
void ZSTD_buildFSETable_body_bmi2(ZSTD_seqSymbol* dt, const short int* normalizedCounter, unsigned int maxSymbolValue, const U32* baseValue, const U8* nbAdditionalBits, unsigned int tableLog, void* wksp, size_t wkspSize)
{
	{
		ZSTD_seqSymbol_header DTableH; // 44735
		{
			const S16  largeLimit; // 44738
			U32 s; // 44739
		}
	}
	{
		const size_t  tableMask; // 44760
		const size_t  step; // 44761
		{
			const U64  add; // 44769
			size_t pos; // 44770
			U64 sv; // 44771
			U32 s; // 44772
			{
				int i; // 44774
				const int  n; // 44775
				MEM_write64(void* memPtr,
						U64 value);  // 44776
				MEM_write64(void* memPtr,
						U64 value);  // 44778
			}
		}
		{
			size_t position; // 44791
			size_t s; // 44792
			const size_t  unroll; // 44793
			{
				size_t u; // 44796
				{
					const size_t  uPosition; // 44798
				}
			}
		}
	}
	{
		const U32  tableMask; // 44806
		const U32  step; // 44807
		U32 s; // 44808
		U32 position; // 44808
		{
			int i; // 44810
			const int  n; // 44811
		}
	}
	{
		U32 u; // 44822
		{
			const U32  symbol; // 44824
			const U32  nextState; // 44825
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 44826
		}
	}
	ZSTD_buildFSETable_body(ZSTD_seqSymbol* dt,
				const short int* normalizedCounter,
				unsigned int maxSymbolValue,
				const U32* baseValue,
				const U8* nbAdditionalBits,
				unsigned int tableLog,
				void* wksp,
				size_t wkspSize);  // 44851
} /* size: 770, inline expansions: 1 (739 bytes) */

// <06F4F711> thirdparty/zstd/zstd.c:44846
void ZSTD_buildFSETable_body_bmi2(ZSTD_seqSymbol* dt, const short int* normalizedCounter, unsigned int maxSymbolValue, const U32* baseValue, const U8* nbAdditionalBits, unsigned int tableLog, void* wksp, size_t wkspSize)
{
} /* size: 0 */

// <06F4F683> thirdparty/zstd/zstd.c:44856
void ZSTD_buildFSETable(ZSTD_seqSymbol* dt, const short int* normalizedCounter, unsigned int maxSymbolValue, const U32* baseValue, const U8* nbAdditionalBits, unsigned int tableLog, void* wksp, size_t wkspSize, int bmi2)
{
} /* size: 0 */

// <0701BA30> thirdparty/zstd/zstd.c:44877
// variables: 11
// function calls: 4
size_t ZSTD_buildSeqTable(ZSTD_seqSymbol* DTableSpace, const ZSTD_seqSymbol ** DTablePtr, SymbolEncodingType_e type, unsigned int max, U32 maxLog, const void* src, size_t srcSize, const U32* baseValue, const U8* nbAdditionalBits, const ZSTD_seqSymbol* defaultTable, U32 flagRepeatTable, int ddictIsCold, int nbSeq, U32* wksp, int bmi2, size_t wkspSize)
{
	{
		const U32  symbol; // 44890
		const U32  baseline; // 44891
		const U8  nbBits; // 44892
		ZSTD_buildSeqTable_rle(ZSTD_seqSymbol* dt,
					U32 baseValue,
					U8 nbAddBits);  // 44893
	}
	{
		const void* pStart; // 44904
		const size_t  pSize; // 44905
		{
			const char* _ptr; // 44906
			const size_t  _size; // 44906
			size_t _pos; // 44906
		}
	}
	{
		unsigned int tableLog; // 44910
		S16 norm; // 44911
		const size_t  headerSize; // 44912
		FSE_readNCount_bmi2(short int* normalizedCounter,
					unsigned int* maxSVPtr,
					unsigned int* tableLogPtr,
					const void* headerBuffer,
					size_t hbSize,
					int bmi2);  // 3437
		FSE_readNCount(short int* normalizedCounter,
				unsigned int* maxSVPtr,
				unsigned int* tableLogPtr,
				const void* headerBuffer,
				size_t hbSize);  // 44912
		ZSTD_buildFSETable(ZSTD_seqSymbol* dt,
					const short int* normalizedCounter,
					unsigned int maxSymbolValue,
					const U32* baseValue,
					const U8* nbAdditionalBits,
					unsigned int tableLog,
					void* wksp,
					size_t wkspSize,
					int bmi2);  // 44915
	}
} /* size: 553 */

// <06F4F4EA> thirdparty/zstd/zstd.c:44877
// variables: 11
size_t ZSTD_buildSeqTable(ZSTD_seqSymbol* DTableSpace, const ZSTD_seqSymbol ** DTablePtr, SymbolEncodingType_e type, unsigned int max, U32 maxLog, const void* src, size_t srcSize, const U32* baseValue, const U8* nbAdditionalBits, const ZSTD_seqSymbol* defaultTable, U32 flagRepeatTable, int ddictIsCold, int nbSeq, U32* wksp, size_t wkspSize, int bmi2)
{
	{
		const U32  symbol; // 44890
		const U32  baseline; // 44891
		const U8  nbBits; // 44892
	}
	{
		const void* pStart; // 44904
		const size_t  pSize; // 44905
		{
			const char* _ptr; // 44906
			const size_t  _size; // 44906
			size_t _pos; // 44906
		}
	}
	{
		unsigned int tableLog; // 44910
		S16 norm; // 44911
		const size_t  headerSize; // 44912
	}
} /* size: 0 */

// <06F4F168> thirdparty/zstd/zstd.c:44925
// variables: 10
// function calls: 5
size_t ZSTD_decodeSeqHeaders(ZSTD_DCtx* dctx, int* nbSeqPtr, const void* src, size_t srcSize)
{
	const BYTE* istart; // 44928
	const BYTE* iend; // 44929
	const BYTE* ip; // 44930
	int nbSeq; // 44931
	{
		const SymbolEncodingType_e  LLtype; // 44961
		const SymbolEncodingType_e  OFtype; // 44962
		const SymbolEncodingType_e  MLtype; // 44963
		{
			const size_t  llhSize; // 44967
			ZSTD_DCtx_get_bmi2(const ZSTD_DCtx_s* dctx); // 44967
		}
		{
			const size_t  ofhSize; // 44979
			ZSTD_DCtx_get_bmi2(const ZSTD_DCtx_s* dctx); // 44979
		}
		{
			const size_t  mlhSize; // 44991
			ZSTD_DCtx_get_bmi2(const ZSTD_DCtx_s* dctx); // 44991
		}
	}
	MEM_read16(const void* ptr); // 1269
	MEM_readLE16(const void* memPtr); // 44942
} /* size: 680, variables: 4, inline expansions: 2 (0 bytes) */

// <06F3E729> thirdparty/zstd/zstd.c:45008
// member variables: 3
// struct size: 24
typedef struct {
	size_t litLength; /* 0 8 */
	size_t matchLength; /* 8 8 */
	size_t offset; /* 16 8 */
} seq_t;

// <06F3E77A> thirdparty/zstd/zstd.c:45014
// member variables: 2
// struct size: 16
typedef struct {
	size_t state; /* 0 8 */
	const ZSTD_seqSymbol * table; /* 8 8 */
} ZSTD_fseState;

// <06F3E7B6> thirdparty/zstd/zstd.c:45019
// member variables: 5
// struct size: 112
typedef struct {
	BIT_DStream_t DStream; /* 0 40 */
	ZSTD_fseState stateLL; /* 40 16 */
	ZSTD_fseState stateOffb; /* 56 16 */
	ZSTD_fseState stateML; /* 72 16 */
	size_t prevOffset[3]; /* 88 24 */
} seqState_t;

// <06F4F0C7> thirdparty/zstd/zstd.c:45034
// variables: 3
inline void ZSTD_overlapCopy8(BYTE** op, const BYTE ** ip, size_t offset)
{
	{
		const U32   dec32table; // 45038
		const int   dec64table; // 45039
		const int  sub2; // 45040
	}
} /* size: 0 */

// <06F4EA28> thirdparty/zstd/zstd.c:45067
// variables: 3
// function calls: 25
void ZSTD_safecopy(BYTE* op, const BYTE* oend_w, const BYTE* ip, ptrdiff_t length, ZSTD_overlap_e ovtype)
{
	const ptrdiff_t  diff; // 45068
	const BYTE* oend; // 45069
	ZSTD_copy8(void* dst,
			const void* src);  // 45049
	{
		const int  sub2; // 45040
		ZSTD_copy4(void* dst,
				const void* src);  // 45046
	}
	ZSTD_overlapCopy8(BYTE** op,
				const BYTE ** ip,
				size_t offset);  // 45082
	ZSTD_copy8(void* dst,
			const void* src);  // 15722
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 45096
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_copy8(void* dst,
			const void* src);  // 15722
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 45090
} /* size: 555, variables: 2, inline expansions: 24 (453 bytes) */

// <06F4E760> thirdparty/zstd/zstd.c:45107
// variables: 2
// function calls: 10
void ZSTD_safecopyDstBeforeSrc(BYTE* op, const BYTE* ip, ptrdiff_t length)
{
	const ptrdiff_t  diff; // 45108
	const BYTE* oend; // 45109
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15732
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15737
	_mm_loadu_si128(const __m128i_u* __P); // 15683
	_mm_storeu_si128(__m128i_u* __P,
			__m128i __B);  // 15683
	ZSTD_copy16(void* dst,
			const void* src);  // 15738
	ZSTD_wildcopy(void* dst,
			const void* src,
			ptrdiff_t length,
			const ZSTD_overlap_e  ovtype);  // 45118
} /* size: 201, variables: 2, inline expansions: 10 (112 bytes) */

// <06F4E569> thirdparty/zstd/zstd.c:45137
// variables: 6
size_t ZSTD_execSequenceEnd(BYTE* op, const BYTE* oend, seq_t sequence, const BYTE ** litPtr, const BYTE* litLimit, const BYTE* prefixStart, const BYTE* virtualStart, const BYTE* dictEnd)
{
	const BYTE* oLitEnd; // 45142
	const size_t  sequenceLength; // 45143
	const BYTE* iLitEnd; // 45144
	const BYTE* match; // 45145
	const BYTE* oend_w; // 45146
	{
		const size_t  length1; // 45169
	}
} /* size: 285, variables: 5 */

// <06F4E37D> thirdparty/zstd/zstd.c:45185
// variables: 5
size_t ZSTD_execSequenceEndSplitLitBuffer(BYTE* op, const BYTE* oend, const BYTE* oend_w, seq_t sequence, const BYTE ** litPtr, const BYTE* litLimit, const BYTE* prefixStart, const BYTE* virtualStart, const BYTE* dictEnd)
{
	const BYTE* oLitEnd; // 45190
	const size_t  sequenceLength; // 45191
	const BYTE* iLitEnd; // 45192
	const BYTE* match; // 45193
	{
		const size_t  length1; // 45218
	}
} /* size: 269, variables: 4 */

// <06F4E2A6> thirdparty/zstd/zstd.c:45231
// variables: 7
inline size_t ZSTD_execSequence(BYTE* op, const BYTE* oend, seq_t sequence, const BYTE ** litPtr, const BYTE* litLimit, const BYTE* prefixStart, const BYTE* virtualStart, const BYTE* dictEnd)
{
	const BYTE* oLitEnd; // 45236
	const size_t  sequenceLength; // 45237
	const BYTE* oMatchEnd; // 45238
	const BYTE* oend_w; // 45239
	const BYTE* iLitEnd; // 45240
	const BYTE* match; // 45241
	{
		const size_t  length1; // 45291
	}
} /* size: 0, variables: 6 */

// <06F4E1C9> thirdparty/zstd/zstd.c:45330
// variables: 6
inline size_t ZSTD_execSequenceSplitLitBuffer(BYTE* op, const BYTE* oend, const BYTE* oend_w, seq_t sequence, const BYTE ** litPtr, const BYTE* litLimit, const BYTE* prefixStart, const BYTE* virtualStart, const BYTE* dictEnd)
{
	const BYTE* oLitEnd; // 45335
	const size_t  sequenceLength; // 45336
	const BYTE* oMatchEnd; // 45337
	const BYTE* iLitEnd; // 45338
	const BYTE* match; // 45339
	{
		const size_t  length1; // 45384
	}
} /* size: 0, variables: 5 */

// <06F4DEB5> thirdparty/zstd/zstd.c:45422
// variables: 4
// function calls: 12
void ZSTD_initFseState(ZSTD_fseState* DStatePtr, BIT_DStream_t* bitD, const ZSTD_seqSymbol* dt)
{
	const void* ptr; // 45424
	const ZSTD_seqSymbol_header* DTableH; // 45425
	BIT_getMiddleBits(BitContainerType bitContainer,
				const U32  start,
				const U32  nbBits);  // 2441
	BIT_lookBits(const BIT_DStream_t* bitD,
			U32 nbBits);  // 2469
	BIT_skipBits(BIT_DStream_t* bitD,
			U32 nbBits);  // 2470
	BIT_readBits(BIT_DStream_t* bitD,
			unsigned int nbBits);  // 45426
	{
	}
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 1335
	MEM_readLEST(const void* memPtr); // 2495
	BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
	{
		U32 nbBytes; // 2538
		BIT_DStream_status result; // 2539
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2546
	}
	BIT_reloadDStream(BIT_DStream_t* bitD); // 45429
} /* size: 212, variables: 2, inline expansions: 9 (266 bytes) */

// <06F4DE54> thirdparty/zstd/zstd.c:45434
// variable: 1
inline void ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr, BIT_DStream_t* bitD, U16 nextState, U32 nbBits)
{
	const size_t  lowBits; // 45436
} /* size: 0, variables: 1 */

// <06F4DD0F> thirdparty/zstd/zstd.c:45459
// variables: 19
inline seq_t ZSTD_decodeSequence(seqState_t* seqState, const ZSTD_longOffset_e longOffsets, const int isLastSeq)
{
	seq_t seq; // 45461
	const ZSTD_seqSymbol* llDInfo; // 45479
	const ZSTD_seqSymbol* mlDInfo; // 45480
	const ZSTD_seqSymbol* ofDInfo; // 45481
	{
		const U32  ofBase; // 45485
		const BYTE  llBits; // 45486
		const BYTE  mlBits; // 45487
		const BYTE  ofBits; // 45488
		const BYTE  totalBits; // 45489
		const U16  llNext; // 45491
		const U16  mlNext; // 45492
		const U16  ofNext; // 45493
		const U32  llnbBits; // 45494
		const U32  mlnbBits; // 45495
		const U32  ofnbBits; // 45496
		{
			size_t offset; // 45508
			{
				const U32  extraBits; // 45518
			}
			{
				const U32  ll0; // 45530
				{
					size_t temp; // 45537
				}
			}
		}
	}
} /* size: 0, variables: 4 */

// <06F4DB6A> thirdparty/zstd/zstd.c:45633
// variables: 21
inline size_t ZSTD_decompressSequences_bodySplitLitBuffer(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
	const BYTE* ip; // 45638
	const BYTE* iend; // 45639
	const BYTE* ostart; // 45640
	const BYTE* oend; // 45641
	BYTE* op; // 45642
	const BYTE* litPtr; // 45643
	const BYTE* litBufferEnd; // 45644
	const BYTE* prefixStart; // 45645
	const BYTE* vBase; // 45646
	const BYTE* dictEnd; // 45647
	{
		seqState_t seqState; // 45652
		{
			U32 i; // 45654
		}
		{
			seq_t sequence; // 45669
			{
				const size_t  oneSeqSize; // 45734
			}
			{
				const size_t  leftoverLit; // 45748
				{
					const size_t  oneSeqSize; // 45759
				}
			}
		}
		{
			const seq_t  sequence; // 45794
			const size_t  oneSeqSize; // 45795
		}
		{
			U32 i; // 45813
		}
	}
	{
		const size_t  lastLLSize; // 45819
	}
	{
		const size_t  lastLLSize; // 45831
	}
} /* size: 0, variables: 10 */

// <06F4DA1C> thirdparty/zstd/zstd.c:45845
// variables: 16
inline size_t ZSTD_decompressSequences_body(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
	const BYTE* ip; // 45850
	const BYTE* iend; // 45851
	const BYTE* ostart; // 45852
	const BYTE* oend; // 45853
	BYTE* op; // 45854
	const BYTE* litPtr; // 45855
	const BYTE* litEnd; // 45856
	const BYTE* prefixStart; // 45857
	const BYTE* vBase; // 45858
	const BYTE* dictEnd; // 45859
	{
		seqState_t seqState; // 45864
		{
			U32 i; // 45866
		}
		{
			const seq_t  sequence; // 45890
			const size_t  oneSeqSize; // 45891
		}
		{
			U32 i; // 45906
		}
	}
	{
		const size_t  lastLLSize; // 45910
	}
} /* size: 0, variables: 10 */

// <06F4D9AE> thirdparty/zstd/zstd.c:45923
size_t ZSTD_decompressSequences_default(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <06F4D940> thirdparty/zstd/zstd.c:45932
size_t ZSTD_decompressSequencesSplitLitBuffer_default(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <06F4D8DD> thirdparty/zstd/zstd.c:45945
// variables: 2
inline size_t ZSTD_prefetchMatch(size_t prefetchPos, const seq_t sequence, const BYTE* prefixStart, const BYTE* dictEnd)
{
	{
		const BYTE* matchBase; // 45949
		const BYTE* match; // 45952
	}
} /* size: 0 */

// <06F4D6B7> thirdparty/zstd/zstd.c:45963
// variables: 28
inline size_t ZSTD_decompressSequencesLong_body(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
	const BYTE* ip; // 45969
	const BYTE* iend; // 45970
	const BYTE* ostart; // 45971
	const BYTE* oend; // 45972
	BYTE* op; // 45973
	const BYTE* litPtr; // 45974
	const BYTE* litBufferEnd; // 45975
	const BYTE* prefixStart; // 45976
	const BYTE* dictStart; // 45977
	const BYTE* dictEnd; // 45978
	{
		seq_t sequences; // 45985
		const int  seqAdvance; // 45986
		seqState_t seqState; // 45987
		int seqNb; // 45988
		size_t prefetchPos; // 45989
		{
			int i; // 45992
		}
		{
			const seq_t  sequence; // 46004
		}
		{
			seq_t sequence; // 46011
			{
				const size_t  leftoverLit; // 46015
				{
					const size_t  oneSeqSize; // 46026
				}
			}
			{
				const size_t  oneSeqSize; // 46040
			}
		}
		{
			seq_t* sequence; // 46059
			{
				const size_t  leftoverLit; // 46061
				{
					const size_t  oneSeqSize; // 46071
				}
			}
			{
				const size_t  oneSeqSize; // 46082
			}
		}
		{
			U32 i; // 46095
		}
	}
	{
		const size_t  lastLLSize; // 46100
	}
	{
		const size_t  lastLLSize; // 46109
	}
} /* size: 0, variables: 10 */

// <0708E08D> thirdparty/zstd/zstd.c:46121
// variables: 72
// function calls: 327
size_t ZSTD_decompressSequencesLong_default(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 45972
	{
		seq_t sequences; // 45985
		const int  seqAdvance; // 45986
		seqState_t seqState; // 45987
		int seqNb; // 45988
		size_t prefetchPos; // 45989
		{
			const BYTE  lastByte; // 2369
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2370
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2368
		{
			const BYTE  lastByte; // 2397
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2398
		}
		BIT_initDStream(BIT_DStream_t* bitD,
				const void* srcBuffer,
				size_t srcSize);  // 45995
		{
			int i; // 45992
		}
		{
			const seq_t  sequence; // 46004
			{
				const U32  ofBase; // 45485
				const BYTE  llBits; // 45486
				const BYTE  mlBits; // 45487
				const BYTE  ofBits; // 45488
				const BYTE  totalBits; // 45489
				const U16  llNext; // 45491
				const U16  mlNext; // 45492
				const U16  ofNext; // 45493
				const U32  llnbBits; // 45494
				const U32  mlnbBits; // 45495
				const U32  ofnbBits; // 45496
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45567
				{
					size_t offset; // 45508
					BIT_lookBitsFast(const BIT_DStream_t* bitD,
							U32 nbBits);  // 2478
					BIT_skipBits(BIT_DStream_t* bitD,
							U32 nbBits);  // 2480
					BIT_readBitsFast(BIT_DStream_t* bitD,
							unsigned int nbBits);  // 45523
					{
						const U32  ll0; // 45530
						BIT_lookBitsFast(const BIT_DStream_t* bitD,
								U32 nbBits);  // 2478
						BIT_skipBits(BIT_DStream_t* bitD,
								U32 nbBits);  // 2480
						BIT_readBitsFast(BIT_DStream_t* bitD,
								unsigned int nbBits);  // 45536
						{
							size_t temp; // 45537
						}
					}
				}
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45547
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45557
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45568
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2470
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45570
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45571
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45552
			}
			ZSTD_decodeSequence(seqState_t* seqState,
						const ZSTD_longOffset_e  longOffsets,
						const int  isLastSeq);  // 46004
			{
				const BYTE* matchBase; // 45949
				const BYTE* match; // 45952
				ZSTD_wrappedPtrSub(const unsigned char* ptr,
							ptrdiff_t sub);  // 45952
			}
			ZSTD_prefetchMatch(size_t prefetchPos,
						const seq_t  sequence,
						const BYTE* prefixStart,
						const BYTE* dictEnd);  // 46005
		}
		{
			seq_t sequence; // 46011
			{
				const U32  ofBase; // 45485
				const BYTE  llBits; // 45486
				const BYTE  mlBits; // 45487
				const BYTE  ofBits; // 45488
				const BYTE  totalBits; // 45489
				const U16  llNext; // 45491
				const U16  mlNext; // 45492
				const U16  ofNext; // 45493
				const U32  llnbBits; // 45494
				const U32  mlnbBits; // 45495
				const U32  ofnbBits; // 45496
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45567
				{
					size_t offset; // 45508
					BIT_lookBitsFast(const BIT_DStream_t* bitD,
							U32 nbBits);  // 2478
					BIT_skipBits(BIT_DStream_t* bitD,
							U32 nbBits);  // 2480
					BIT_readBitsFast(BIT_DStream_t* bitD,
							unsigned int nbBits);  // 45523
					{
						const U32  ll0; // 45530
						BIT_lookBitsFast(const BIT_DStream_t* bitD,
								U32 nbBits);  // 2478
						BIT_skipBits(BIT_DStream_t* bitD,
								U32 nbBits);  // 2480
						BIT_readBitsFast(BIT_DStream_t* bitD,
								unsigned int nbBits);  // 45536
						{
							size_t temp; // 45537
						}
					}
				}
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45547
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45557
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45568
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2470
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45570
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45571
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45552
			}
			ZSTD_decodeSequence(seqState_t* seqState,
						const ZSTD_longOffset_e  longOffsets,
						const int  isLastSeq);  // 46011
			{
				const size_t  oneSeqSize; // 46040
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45274
				{
					const size_t  length1; // 45291
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45312
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45276
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45318
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45323
				ZSTD_execSequence(BYTE* op,
							const BYTE* oend,
							seq_t sequence,
							const BYTE ** litPtr,
							const BYTE* litLimit,
							const BYTE* prefixStart,
							const BYTE* virtualStart,
							const BYTE* dictEnd);  // 46042
				{
					const BYTE* matchBase; // 45949
					const BYTE* match; // 45952
					ZSTD_wrappedPtrSub(const unsigned char* ptr,
								ptrdiff_t sub);  // 45952
				}
				ZSTD_prefetchMatch(size_t prefetchPos,
							const seq_t  sequence,
							const BYTE* prefixStart,
							const BYTE* dictEnd);  // 46049
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45367
				{
					const size_t  length1; // 45384
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45404
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45369
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45410
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45415
				ZSTD_execSequenceSplitLitBuffer(BYTE* op,
								const BYTE* oend,
								const BYTE* oend_w,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 46041
			}
			{
				const size_t  leftoverLit; // 46015
				{
					const size_t  oneSeqSize; // 46026
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 45274
					{
						const size_t  length1; // 45291
					}
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45312
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45276
					ZSTD_copy8(void* dst,
							const void* src);  // 45049
					{
						const int  sub2; // 45040
						ZSTD_copy4(void* dst,
								const void* src);  // 45046
					}
					ZSTD_overlapCopy8(BYTE** op,
								const BYTE ** ip,
								size_t offset);  // 45318
					ZSTD_copy8(void* dst,
							const void* src);  // 15722
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45323
					ZSTD_execSequence(BYTE* op,
								const BYTE* oend,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 46026
				}
			}
		}
		BIT_endOfDStream(const BIT_DStream_t* DStream); // 46054
		{
			seq_t* sequence; // 46059
			{
				const size_t  oneSeqSize; // 46082
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45274
				{
					const size_t  length1; // 45291
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45312
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45276
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45318
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45323
				ZSTD_execSequence(BYTE* op,
							const BYTE* oend,
							seq_t sequence,
							const BYTE ** litPtr,
							const BYTE* litLimit,
							const BYTE* prefixStart,
							const BYTE* virtualStart,
							const BYTE* dictEnd);  // 46084
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45367
				{
					const size_t  length1; // 45384
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45404
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45369
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45410
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45415
				ZSTD_execSequenceSplitLitBuffer(BYTE* op,
								const BYTE* oend,
								const BYTE* oend_w,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 46083
			}
			{
				const size_t  leftoverLit; // 46061
				{
					const size_t  oneSeqSize; // 46071
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 45274
					{
						const size_t  length1; // 45291
					}
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45312
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45276
					{
						const int  sub2; // 45040
						ZSTD_copy4(void* dst,
								const void* src);  // 45046
					}
					ZSTD_copy8(void* dst,
							const void* src);  // 45049
					ZSTD_overlapCopy8(BYTE** op,
								const BYTE ** ip,
								size_t offset);  // 45318
					ZSTD_copy8(void* dst,
							const void* src);  // 15722
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45323
					ZSTD_execSequence(BYTE* op,
								const BYTE* oend,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 46071
				}
			}
		}
		{
			U32 i; // 46095
		}
	}
	{
		const size_t  lastLLSize; // 46100
	}
	{
		const size_t  lastLLSize; // 46109
	}
	ZSTD_decompressSequencesLong_body(ZSTD_DCtx* dctx,
						void* dst,
						size_t maxDstSize,
						const void* seqStart,
						size_t seqSize,
						int nbSeq,
						const ZSTD_longOffset_e  isLongOffset);  // 46126
} /* size: 8926, inline expansions: 2 (8843 bytes) */

// <06F4D649> thirdparty/zstd/zstd.c:46121
size_t ZSTD_decompressSequencesLong_default(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <0708375D> thirdparty/zstd/zstd.c:46137
// variables: 28
// function calls: 91
size_t ZSTD_decompressSequences_bmi2(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 45853
	{
		seqState_t seqState; // 45864
		{
			const BYTE  lastByte; // 2369
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2370
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2368
		{
			const BYTE  lastByte; // 2397
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2398
		}
		BIT_initDStream(BIT_DStream_t* bitD,
				const void* srcBuffer,
				size_t srcSize);  // 45867
		{
			U32 i; // 45866
		}
		{
			const seq_t  sequence; // 45890
			const size_t  oneSeqSize; // 45891
			{
				const U32  ofBase; // 45485
				const BYTE  llBits; // 45486
				const BYTE  mlBits; // 45487
				const BYTE  ofBits; // 45488
				const BYTE  totalBits; // 45489
				const U16  llNext; // 45491
				const U16  mlNext; // 45492
				const U16  ofNext; // 45493
				const U32  llnbBits; // 45494
				const U32  mlnbBits; // 45495
				const U32  ofnbBits; // 45496
				{
					size_t offset; // 45508
					BIT_lookBitsFast(const BIT_DStream_t* bitD,
							U32 nbBits);  // 2478
					BIT_skipBits(BIT_DStream_t* bitD,
							U32 nbBits);  // 2480
					BIT_readBitsFast(BIT_DStream_t* bitD,
							unsigned int nbBits);  // 45523
					{
						const U32  ll0; // 45530
						BIT_lookBitsFast(const BIT_DStream_t* bitD,
								U32 nbBits);  // 2478
						BIT_skipBits(BIT_DStream_t* bitD,
								U32 nbBits);  // 2480
						BIT_readBitsFast(BIT_DStream_t* bitD,
								unsigned int nbBits);  // 45536
						{
							size_t temp; // 45537
						}
					}
				}
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45547
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45557
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45567
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45568
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2470
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45570
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45571
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45552
			}
			ZSTD_decodeSequence(seqState_t* seqState,
						const ZSTD_longOffset_e  longOffsets,
						const int  isLastSeq);  // 45890
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 45274
			{
				const size_t  length1; // 45291
			}
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 45312
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 45276
			{
				const int  sub2; // 45040
				ZSTD_copy4(void* dst,
						const void* src);  // 45046
			}
			ZSTD_copy8(void* dst,
					const void* src);  // 45049
			ZSTD_overlapCopy8(BYTE** op,
						const BYTE ** ip,
						size_t offset);  // 45318
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_copy8(void* dst,
					const void* src);  // 15722
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 45323
			ZSTD_execSequence(BYTE* op,
						const BYTE* oend,
						seq_t sequence,
						const BYTE ** litPtr,
						const BYTE* litLimit,
						const BYTE* prefixStart,
						const BYTE* virtualStart,
						const BYTE* dictEnd);  // 45891
		}
		BIT_endOfDStream(const BIT_DStream_t* DStream); // 45904
		{
			U32 i; // 45906
		}
	}
	{
		const size_t  lastLLSize; // 45910
	}
	ZSTD_decompressSequences_body(ZSTD_DCtx* dctx,
					void* dst,
					size_t maxDstSize,
					const void* seqStart,
					size_t seqSize,
					int nbSeq,
					const ZSTD_longOffset_e  isLongOffset);  // 46142
} /* size: 2597, inline expansions: 2 (2522 bytes) */

// <06F4D5DB> thirdparty/zstd/zstd.c:46137
size_t ZSTD_decompressSequences_bmi2(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <07085128> thirdparty/zstd/zstd.c:46146
// variables: 55
// function calls: 209
size_t ZSTD_decompressSequencesSplitLitBuffer_bmi2(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 45641
	{
		seqState_t seqState; // 45652
		{
			const BYTE  lastByte; // 2369
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2370
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2368
		{
			const BYTE  lastByte; // 2397
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2398
		}
		BIT_initDStream(BIT_DStream_t* bitD,
				const void* srcBuffer,
				size_t srcSize);  // 45655
		{
			U32 i; // 45654
		}
		{
			seq_t sequence; // 45669
			{
				const U32  ofBase; // 45485
				const BYTE  llBits; // 45486
				const BYTE  mlBits; // 45487
				const BYTE  ofBits; // 45488
				const BYTE  totalBits; // 45489
				const U16  llNext; // 45491
				const U16  mlNext; // 45492
				const U16  ofNext; // 45493
				const U32  llnbBits; // 45494
				const U32  mlnbBits; // 45495
				const U32  ofnbBits; // 45496
				{
					size_t offset; // 45508
					BIT_lookBitsFast(const BIT_DStream_t* bitD,
							U32 nbBits);  // 2478
					BIT_skipBits(BIT_DStream_t* bitD,
							U32 nbBits);  // 2480
					BIT_readBitsFast(BIT_DStream_t* bitD,
							unsigned int nbBits);  // 45523
					{
						const U32  ll0; // 45530
						BIT_lookBitsFast(const BIT_DStream_t* bitD,
								U32 nbBits);  // 2478
						BIT_skipBits(BIT_DStream_t* bitD,
								U32 nbBits);  // 2480
						BIT_readBitsFast(BIT_DStream_t* bitD,
								unsigned int nbBits);  // 45536
						{
							size_t temp; // 45537
						}
					}
				}
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45547
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45557
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45567
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45568
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2470
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45570
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45571
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45552
			}
			ZSTD_decodeSequence(seqState_t* seqState,
						const ZSTD_longOffset_e  longOffsets,
						const int  isLastSeq);  // 45732
			{
				const size_t  oneSeqSize; // 45734
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45367
				{
					const size_t  length1; // 45384
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45404
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45369
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45410
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45415
				ZSTD_execSequenceSplitLitBuffer(BYTE* op,
								const BYTE* oend,
								const BYTE* oend_w,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 45734
			}
			{
				const size_t  leftoverLit; // 45748
				{
					const size_t  oneSeqSize; // 45759
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 45274
					{
						const size_t  length1; // 45291
					}
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45312
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45276
					{
						const int  sub2; // 45040
						ZSTD_copy4(void* dst,
								const void* src);  // 45046
					}
					ZSTD_copy8(void* dst,
							const void* src);  // 45049
					ZSTD_overlapCopy8(BYTE** op,
								const BYTE ** ip,
								size_t offset);  // 45318
					ZSTD_copy8(void* dst,
							const void* src);  // 15722
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45323
					ZSTD_execSequence(BYTE* op,
								const BYTE* oend,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 45759
				}
			}
		}
		BIT_endOfDStream(const BIT_DStream_t* DStream); // 45811
		{
			U32 i; // 45813
		}
		{
			const seq_t  sequence; // 45794
			const size_t  oneSeqSize; // 45795
			{
				const U32  ofBase; // 45485
				const BYTE  llBits; // 45486
				const BYTE  mlBits; // 45487
				const BYTE  ofBits; // 45488
				const BYTE  totalBits; // 45489
				const U16  llNext; // 45491
				const U16  mlNext; // 45492
				const U16  ofNext; // 45493
				const U32  llnbBits; // 45494
				const U32  mlnbBits; // 45495
				const U32  ofnbBits; // 45496
				{
					size_t offset; // 45508
					BIT_lookBitsFast(const BIT_DStream_t* bitD,
							U32 nbBits);  // 2478
					BIT_skipBits(BIT_DStream_t* bitD,
							U32 nbBits);  // 2480
					BIT_readBitsFast(BIT_DStream_t* bitD,
							unsigned int nbBits);  // 45523
					{
						const U32  ll0; // 45530
						BIT_lookBitsFast(const BIT_DStream_t* bitD,
								U32 nbBits);  // 2478
						BIT_skipBits(BIT_DStream_t* bitD,
								U32 nbBits);  // 2480
						BIT_readBitsFast(BIT_DStream_t* bitD,
								unsigned int nbBits);  // 45536
						{
							size_t temp; // 45537
						}
					}
				}
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45547
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45557
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45567
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45568
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2470
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45570
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45571
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45552
			}
			ZSTD_decodeSequence(seqState_t* seqState,
						const ZSTD_longOffset_e  longOffsets,
						const int  isLastSeq);  // 45794
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 45274
			{
				const size_t  length1; // 45291
			}
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 45312
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 45276
			{
				const int  sub2; // 45040
				ZSTD_copy4(void* dst,
						const void* src);  // 45046
			}
			ZSTD_copy8(void* dst,
					const void* src);  // 45049
			ZSTD_overlapCopy8(BYTE** op,
						const BYTE ** ip,
						size_t offset);  // 45318
			ZSTD_copy8(void* dst,
					const void* src);  // 15722
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15732
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15737
			_mm_loadu_si128(const __m128i_u* __P); // 15683
			_mm_storeu_si128(__m128i_u* __P,
					__m128i __B);  // 15683
			ZSTD_copy16(void* dst,
					const void* src);  // 15738
			ZSTD_wildcopy(void* dst,
					const void* src,
					ptrdiff_t length,
					const ZSTD_overlap_e  ovtype);  // 45323
			ZSTD_execSequence(BYTE* op,
						const BYTE* oend,
						seq_t sequence,
						const BYTE ** litPtr,
						const BYTE* litLimit,
						const BYTE* prefixStart,
						const BYTE* virtualStart,
						const BYTE* dictEnd);  // 45795
		}
	}
	{
		const size_t  lastLLSize; // 45819
	}
	{
		const size_t  lastLLSize; // 45831
	}
	ZSTD_decompressSequences_bodySplitLitBuffer(ZSTD_DCtx* dctx,
							void* dst,
							size_t maxDstSize,
							const void* seqStart,
							size_t seqSize,
							int nbSeq,
							const ZSTD_longOffset_e  isLongOffset);  // 46151
} /* size: 5434, inline expansions: 2 (5376 bytes) */

// <06F4D56D> thirdparty/zstd/zstd.c:46146
size_t ZSTD_decompressSequencesSplitLitBuffer_bmi2(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <07088A18> thirdparty/zstd/zstd.c:46157
// variables: 72
// function calls: 327
size_t ZSTD_decompressSequencesLong_bmi2(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
	ZSTD_maybeNullPtrAdd(unsigned char* ptr,
				ptrdiff_t add);  // 45972
	{
		seq_t sequences; // 45985
		const int  seqAdvance; // 45986
		seqState_t seqState; // 45987
		int seqNb; // 45988
		size_t prefetchPos; // 45989
		{
			const BYTE  lastByte; // 2369
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2370
		}
		MEM_read64(const void* ptr); // 1317
		MEM_readLE64(const void* memPtr); // 1335
		MEM_readLEST(const void* memPtr); // 2368
		{
			const BYTE  lastByte; // 2397
			ZSTD_countLeadingZeros32(U32 val); // 2106
			ZSTD_highbit32(U32 val); // 2398
		}
		BIT_initDStream(BIT_DStream_t* bitD,
				const void* srcBuffer,
				size_t srcSize);  // 45995
		{
			int i; // 45992
		}
		{
			const seq_t  sequence; // 46004
			{
				const U32  ofBase; // 45485
				const BYTE  llBits; // 45486
				const BYTE  mlBits; // 45487
				const BYTE  ofBits; // 45488
				const BYTE  totalBits; // 45489
				const U16  llNext; // 45491
				const U16  mlNext; // 45492
				const U16  ofNext; // 45493
				const U32  llnbBits; // 45494
				const U32  mlnbBits; // 45495
				const U32  ofnbBits; // 45496
				{
					size_t offset; // 45508
					BIT_lookBitsFast(const BIT_DStream_t* bitD,
							U32 nbBits);  // 2478
					BIT_skipBits(BIT_DStream_t* bitD,
							U32 nbBits);  // 2480
					BIT_readBitsFast(BIT_DStream_t* bitD,
							unsigned int nbBits);  // 45523
					{
						const U32  ll0; // 45530
						BIT_lookBitsFast(const BIT_DStream_t* bitD,
								U32 nbBits);  // 2478
						BIT_skipBits(BIT_DStream_t* bitD,
								U32 nbBits);  // 2480
						BIT_readBitsFast(BIT_DStream_t* bitD,
								unsigned int nbBits);  // 45536
						{
							size_t temp; // 45537
						}
					}
				}
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45547
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45557
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45567
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45568
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2470
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45570
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45571
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45552
			}
			ZSTD_decodeSequence(seqState_t* seqState,
						const ZSTD_longOffset_e  longOffsets,
						const int  isLastSeq);  // 46004
			{
				const BYTE* matchBase; // 45949
				const BYTE* match; // 45952
				ZSTD_wrappedPtrSub(const unsigned char* ptr,
							ptrdiff_t sub);  // 45952
			}
			ZSTD_prefetchMatch(size_t prefetchPos,
						const seq_t  sequence,
						const BYTE* prefixStart,
						const BYTE* dictEnd);  // 46005
		}
		{
			seq_t sequence; // 46011
			{
				const U32  ofBase; // 45485
				const BYTE  llBits; // 45486
				const BYTE  mlBits; // 45487
				const BYTE  ofBits; // 45488
				const BYTE  totalBits; // 45489
				const U16  llNext; // 45491
				const U16  mlNext; // 45492
				const U16  ofNext; // 45493
				const U32  llnbBits; // 45494
				const U32  mlnbBits; // 45495
				const U32  ofnbBits; // 45496
				{
					size_t offset; // 45508
					BIT_lookBitsFast(const BIT_DStream_t* bitD,
							U32 nbBits);  // 2478
					BIT_skipBits(BIT_DStream_t* bitD,
							U32 nbBits);  // 2480
					BIT_readBitsFast(BIT_DStream_t* bitD,
							unsigned int nbBits);  // 45523
					{
						const U32  ll0; // 45530
						BIT_lookBitsFast(const BIT_DStream_t* bitD,
								U32 nbBits);  // 2478
						BIT_skipBits(BIT_DStream_t* bitD,
								U32 nbBits);  // 2480
						BIT_readBitsFast(BIT_DStream_t* bitD,
								unsigned int nbBits);  // 45536
						{
							size_t temp; // 45537
						}
					}
				}
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45547
				BIT_lookBitsFast(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2478
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2480
				BIT_readBitsFast(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45557
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45567
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45568
				BIT_getMiddleBits(BitContainerType bitContainer,
							const U32  start,
							const U32  nbBits);  // 2441
				BIT_lookBits(const BIT_DStream_t* bitD,
						U32 nbBits);  // 2469
				BIT_skipBits(BIT_DStream_t* bitD,
						U32 nbBits);  // 2470
				BIT_readBits(BIT_DStream_t* bitD,
						unsigned int nbBits);  // 45436
				ZSTD_updateFseStateWithDInfo(ZSTD_fseState* DStatePtr,
								BIT_DStream_t* bitD,
								U16 nextState,
								U32 nbBits);  // 45570
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45571
				{
				}
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 1335
				MEM_readLEST(const void* memPtr); // 2495
				BIT_reloadDStream_internal(BIT_DStream_t* bitD); // 2530
				{
					U32 nbBytes; // 2538
					BIT_DStream_status result; // 2539
					MEM_read64(const void* ptr); // 1317
					MEM_readLE64(const void* memPtr); // 1335
					MEM_readLEST(const void* memPtr); // 2546
				}
				BIT_reloadDStream(BIT_DStream_t* bitD); // 45552
			}
			ZSTD_decodeSequence(seqState_t* seqState,
						const ZSTD_longOffset_e  longOffsets,
						const int  isLastSeq);  // 46011
			{
				const size_t  oneSeqSize; // 46040
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45274
				{
					const size_t  length1; // 45291
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45312
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45276
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45318
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45323
				ZSTD_execSequence(BYTE* op,
							const BYTE* oend,
							seq_t sequence,
							const BYTE ** litPtr,
							const BYTE* litLimit,
							const BYTE* prefixStart,
							const BYTE* virtualStart,
							const BYTE* dictEnd);  // 46042
				{
					const BYTE* matchBase; // 45949
					const BYTE* match; // 45952
					ZSTD_wrappedPtrSub(const unsigned char* ptr,
								ptrdiff_t sub);  // 45952
				}
				ZSTD_prefetchMatch(size_t prefetchPos,
							const seq_t  sequence,
							const BYTE* prefixStart,
							const BYTE* dictEnd);  // 46049
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45367
				{
					const size_t  length1; // 45384
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45404
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45369
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45410
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45415
				ZSTD_execSequenceSplitLitBuffer(BYTE* op,
								const BYTE* oend,
								const BYTE* oend_w,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 46041
			}
			{
				const size_t  leftoverLit; // 46015
				{
					const size_t  oneSeqSize; // 46026
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 45274
					{
						const size_t  length1; // 45291
					}
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45312
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45276
					ZSTD_copy8(void* dst,
							const void* src);  // 45049
					{
						const int  sub2; // 45040
						ZSTD_copy4(void* dst,
								const void* src);  // 45046
					}
					ZSTD_overlapCopy8(BYTE** op,
								const BYTE ** ip,
								size_t offset);  // 45318
					ZSTD_copy8(void* dst,
							const void* src);  // 15722
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45323
					ZSTD_execSequence(BYTE* op,
								const BYTE* oend,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 46026
				}
			}
		}
		BIT_endOfDStream(const BIT_DStream_t* DStream); // 46054
		{
			seq_t* sequence; // 46059
			{
				const size_t  oneSeqSize; // 46082
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45274
				{
					const size_t  length1; // 45291
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45312
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45276
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45318
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45323
				ZSTD_execSequence(BYTE* op,
							const BYTE* oend,
							seq_t sequence,
							const BYTE ** litPtr,
							const BYTE* litLimit,
							const BYTE* prefixStart,
							const BYTE* virtualStart,
							const BYTE* dictEnd);  // 46084
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 45367
				{
					const size_t  length1; // 45384
				}
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45404
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45369
				{
					const int  sub2; // 45040
					ZSTD_copy4(void* dst,
							const void* src);  // 45046
				}
				ZSTD_copy8(void* dst,
						const void* src);  // 45049
				ZSTD_overlapCopy8(BYTE** op,
							const BYTE ** ip,
							size_t offset);  // 45410
				ZSTD_copy8(void* dst,
						const void* src);  // 15722
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15732
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15737
				_mm_loadu_si128(const __m128i_u* __P); // 15683
				_mm_storeu_si128(__m128i_u* __P,
						__m128i __B);  // 15683
				ZSTD_copy16(void* dst,
						const void* src);  // 15738
				ZSTD_wildcopy(void* dst,
						const void* src,
						ptrdiff_t length,
						const ZSTD_overlap_e  ovtype);  // 45415
				ZSTD_execSequenceSplitLitBuffer(BYTE* op,
								const BYTE* oend,
								const BYTE* oend_w,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 46083
			}
			{
				const size_t  leftoverLit; // 46061
				{
					const size_t  oneSeqSize; // 46071
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 45274
					{
						const size_t  length1; // 45291
					}
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45312
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45276
					{
						const int  sub2; // 45040
						ZSTD_copy4(void* dst,
								const void* src);  // 45046
					}
					ZSTD_copy8(void* dst,
							const void* src);  // 45049
					ZSTD_overlapCopy8(BYTE** op,
								const BYTE ** ip,
								size_t offset);  // 45318
					ZSTD_copy8(void* dst,
							const void* src);  // 15722
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15732
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15737
					_mm_loadu_si128(const __m128i_u* __P); // 15683
					_mm_storeu_si128(__m128i_u* __P,
							__m128i __B);  // 15683
					ZSTD_copy16(void* dst,
							const void* src);  // 15738
					ZSTD_wildcopy(void* dst,
							const void* src,
							ptrdiff_t length,
							const ZSTD_overlap_e  ovtype);  // 45323
					ZSTD_execSequence(BYTE* op,
								const BYTE* oend,
								seq_t sequence,
								const BYTE ** litPtr,
								const BYTE* litLimit,
								const BYTE* prefixStart,
								const BYTE* virtualStart,
								const BYTE* dictEnd);  // 46071
				}
			}
		}
		{
			U32 i; // 46095
		}
	}
	{
		const size_t  lastLLSize; // 46100
	}
	{
		const size_t  lastLLSize; // 46109
	}
	ZSTD_decompressSequencesLong_body(ZSTD_DCtx* dctx,
						void* dst,
						size_t maxDstSize,
						const void* seqStart,
						size_t seqSize,
						int nbSeq,
						const ZSTD_longOffset_e  isLongOffset);  // 46162
} /* size: 8798, inline expansions: 2 (8711 bytes) */

// <06F4D4FF> thirdparty/zstd/zstd.c:46157
size_t ZSTD_decompressSequencesLong_bmi2(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <06F4D491> thirdparty/zstd/zstd.c:46170
size_t ZSTD_decompressSequences(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <06F4D423> thirdparty/zstd/zstd.c:46183
size_t ZSTD_decompressSequencesSplitLitBuffer(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <06F4D3B5> thirdparty/zstd/zstd.c:46205
size_t ZSTD_decompressSequencesLong(ZSTD_DCtx* dctx, void* dst, size_t maxDstSize, const void* seqStart, size_t seqSize, int nbSeq, const ZSTD_longOffset_e isLongOffset)
{
} /* size: 0 */

// <06F4D389> thirdparty/zstd/zstd.c:46226
size_t ZSTD_totalHistorySize(BYTE* op, const BYTE* virtualStart)
{
} /* size: 0 */

// <06F4D30E> thirdparty/zstd/zstd.c:46243
// variables: 6
ZSTD_OffsetInfo ZSTD_getOffsetInfo(const ZSTD_seqSymbol* offTable, int nbSeq)
{
	ZSTD_OffsetInfo info; // 46245
	{
		const void* ptr; // 46250
		const U32  tableLog; // 46251
		const ZSTD_seqSymbol* table; // 46252
		const U32  max; // 46253
		U32 u; // 46254
	}
} /* size: 0, variables: 1 */

// <06F4D2DF> thirdparty/zstd/zstd.c:46275
// variables: 2
size_t ZSTD_maxShortOffset(void)
{
	{
		const size_t  maxOffbase; // 46288
		const size_t  maxOffset; // 46289
	}
} /* size: 0 */

// <06F4D1EC> thirdparty/zstd/zstd.c:46296
// variables: 10
size_t ZSTD_decompressBlock_internal(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize, const streaming_operation streaming)
{
	const BYTE* ip; // 46300
	{
		const size_t  litCSize; // 46314
	}
	{
		const size_t  blockSizeMax; // 46326
		const size_t  totalHistorySize; // 46327
		ZSTD_longOffset_e isLongOffset; // 46339
		int usePrefetchDecoder; // 46346
		int nbSeq; // 46353
		const size_t  seqHSize; // 46354
		{
			const ZSTD_OffsetInfo  info; // 46368
			{
				const U32  minShare; // 46377
			}
		}
	}
} /* size: 0, variables: 1 */

// <06F4D1B1> thirdparty/zstd/zstd.c:46408
void ZSTD_checkContinuity(ZSTD_DCtx* dctx, const void* dst, size_t dstSize)
{
} /* size: 0 */

// <06F4D13C> thirdparty/zstd/zstd.c:46419
// variables: 2
size_t ZSTD_decompressBlock_deprecated(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	size_t dSize; // 46423
	{
		const size_t  err_code; // 46427
	}
} /* size: 0, variables: 1 */

// <06F4CF1D> thirdparty/zstd/zstd.c:46434
// variable: 1
// function calls: 3
size_t ZSTD_decompressBlock(ZSTD_DCtx* dctx, void* dst, size_t dstCapacity, const void* src, size_t srcSize)
{
	ZSTD_checkContinuity(ZSTD_DCtx* dctx,
				const void* dst,
				size_t dstSize);  // 46425
	ZSTD_decompressBlock_internal(ZSTD_DCtx* dctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize,
					const streaming_operation  streaming);  // 46426
	{
		const size_t  err_code; // 46427
	}
	ZSTD_decompressBlock_deprecated(ZSTD_DCtx* dctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize);  // 46438
} /* size: 143, inline expansions: 3 (227 bytes) */

// <06F3EB18> thirdparty/zstd/zstd.c:46703
// member variables: 3
// struct size: 12
typedef struct {
	int compressionLevel; /* 0 4 */
	unsigned int notificationLevel; /* 4 4 */
	unsigned int dictID; /* 8 4 */
} ZDICT_params_t;

// <06F3EB64> thirdparty/zstd/zstd.c:46802
// member variables: 8
// struct size: 48
typedef struct {
	unsigned int k; /* 0 4 */
	unsigned int d; /* 4 4 */
	unsigned int steps; /* 8 4 */
	unsigned int nbThreads; /* 12 4 */
	double splitPoint; /* 16 8 */
	unsigned int shrinkDict; /* 24 4 */
	unsigned int shrinkDictMaxRegression; /* 28 4 */
	ZDICT_params_t zParams; /* 32 12 */
} ZDICT_cover_params_t;

// <06F3EC01> thirdparty/zstd/zstd.c:46813
// member variables: 10
// struct size: 56
typedef struct {
	unsigned int k; /* 0 4 */
	unsigned int d; /* 4 4 */
	unsigned int f; /* 8 4 */
	unsigned int steps; /* 12 4 */
	unsigned int nbThreads; /* 16 4 */
	double splitPoint; /* 24 8 */
	unsigned int accel; /* 32 4 */
	unsigned int shrinkDict; /* 36 4 */
	unsigned int shrinkDictMaxRegression; /* 40 4 */
	ZDICT_params_t zParams; /* 44 12 */
} ZDICT_fastCover_params_t;

// <06F3ECB7> thirdparty/zstd/zstd.c:46912
// member variables: 2
// struct size: 16
typedef struct {
	unsigned int selectivityLevel; /* 0 4 */
	ZDICT_params_t zParams; /* 4 12 */
} ZDICT_legacy_params_t;

// <06F3ECF3> thirdparty/zstd/zstd.c:46999
// member variables: 7
// struct size: 168
struct COVER_best_s {
	pthread_mutex_t mutex; /* 0 40 */
	pthread_cond_t cond; /* 40 48 */
	size_t liveJobs; /* 88 8 */
	void * dict; /* 96 8 */
	size_t dictSize; /* 104 8 */
	ZDICT_cover_params_t parameters; /* 112 48 */
	size_t compressedSize; /* 160 8 */
};

// <06F3ED80> thirdparty/zstd/zstd.c:47012
// member variables: 3
// struct size: 12
typedef struct {
	U32 begin; /* 0 4 */
	U32 end; /* 4 4 */
	U32 score; /* 8 4 */
} COVER_segment_t;

// <06F3EDCC> thirdparty/zstd/zstd.c:47021
// member variables: 2
// struct size: 8
typedef struct {
	U32 num; /* 0 4 */
	U32 size; /* 4 4 */
} COVER_epoch_info_t;

// <06F3EE0D> thirdparty/zstd/zstd.c:47029
// member variables: 3
// struct size: 24
struct COVER_dictSelection {
	BYTE * dictContent; /* 0 8 */
	size_t dictSize; /* 8 8 */
	size_t totalCompressedSize; /* 16 8 */
};

// <06F3EE98> thirdparty/zstd/zstd.c:47185
// member variables: 2
// struct size: 8
struct COVER_map_pair_t_s {
	U32 key; /* 0 4 */
	U32 value; /* 4 4 */
};

// <06F3EED4> thirdparty/zstd/zstd.c:47190
// member variables: 4
// struct size: 24
struct COVER_map_s {
	COVER_map_pair_t * data; /* 0 8 */
	U32 sizeLog; /* 8 4 */
	U32 size; /* 12 4 */
	U32 sizeMask; /* 16 4 */
};

// <06F4CED3> thirdparty/zstd/zstd.c:47200
void COVER_map_clear(COVER_map_t* map)
{
} /* size: 0 */

// <06F4CEA6> thirdparty/zstd/zstd.c:47210
int COVER_map_init(COVER_map_t* map, U32 size)
{
} /* size: 0 */

// <06F4CE79> thirdparty/zstd/zstd.c:47228
U32 COVER_map_hash(COVER_map_t* map, U32 key)
{
} /* size: 0 */

// <06F4CE25> thirdparty/zstd/zstd.c:47235
// variables: 3
U32 COVER_map_index(COVER_map_t* map, U32 key)
{
	const U32  hash; // 47236
	U32 i; // 47237
	{
		COVER_map_pair_t* pos; // 47239
	}
} /* size: 0, variables: 2 */

// <06F4CDEB> thirdparty/zstd/zstd.c:47254
// variable: 1
U32* COVER_map_at(COVER_map_t* map, U32 key)
{
	COVER_map_pair_t* pos; // 47255
} /* size: 0, variables: 1 */

// <06F4CD8E> thirdparty/zstd/zstd.c:47266
// variables: 4
void COVER_map_remove(COVER_map_t* map, U32 key)
{
	U32 i; // 47267
	COVER_map_pair_t* del; // 47268
	U32 shift; // 47269
	{
		const COVER_map_pair_t* pos; // 47274
	}
} /* size: 0, variables: 3 */

// <06F4CD72> thirdparty/zstd/zstd.c:47295
void COVER_map_destroy(COVER_map_t* map)
{
} /* size: 0 */

// <06F3EF4C> thirdparty/zstd/zstd.c:47307
// member variables: 11
// struct size: 88
typedef struct {
	const BYTE * samples; /* 0 8 */
	size_t * offsets; /* 8 8 */
	const size_t * samplesSizes; /* 16 8 */
	size_t nbSamples; /* 24 8 */
	size_t nbTrainSamples; /* 32 8 */
	size_t nbTestSamples; /* 40 8 */
	U32 * suffix; /* 48 8 */
	size_t suffixSize; /* 56 8 */
	U32 * freqs; /* 64 8 */
	U32 * dmerAt; /* 72 8 */
	unsigned int d; /* 80 4 */
} COVER_ctx_t;

// <070174D6> thirdparty/zstd/zstd.c:47347
// variables: 2
int COVER_cmp(COVER_ctx_t* ctx, const void* lp, const void* rp)
{
	const U32  lhs; // 47348
	const U32  rhs; // 47349
} /* size: 34, variables: 2 */

// <070173A2> thirdparty/zstd/zstd.c:47355
// variables: 3
// function calls: 4
int COVER_cmp8(COVER_ctx_t* ctx, const void* lp, const void* rp)
{
	const U64  mask; // 47356
	const U64  lhs; // 47357
	const U64  rhs; // 47358
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 47357
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 47358
} /* size: 87, variables: 3, inline expansions: 4 (0 bytes) */

// <06F4CC77> thirdparty/zstd/zstd.c:47355
// variables: 3
int COVER_cmp8(COVER_ctx_t* ctx, const void* lp, const void* rp)
{
	const U64  mask; // 47356
	const U64  lhs; // 47357
	const U64  rhs; // 47358
} /* size: 0, variables: 3 */

// <06F4CB8C> thirdparty/zstd/zstd.c:47371
// variable: 1
// function call: 1
int COVER_strict_cmp(const void* lp, const void* rp, void* g_coverCtx)
{
	int result; // 47375
	COVER_cmp(COVER_ctx_t* ctx,
			const void* lp,
			const void* rp);  // 47375
} /* size: 55, variables: 1, inline expansions: 1 (21 bytes) */

// <06F4C9ED> thirdparty/zstd/zstd.c:47387
// variable: 1
// function calls: 5
int COVER_strict_cmp8(const void* lp, const void* rp, void* g_coverCtx)
{
	int result; // 47391
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 47357
	MEM_read64(const void* ptr); // 1317
	MEM_readLE64(const void* memPtr); // 47358
	COVER_cmp8(COVER_ctx_t* ctx,
			const void* lp,
			const void* rp);  // 47391
} /* size: 94, variables: 1, inline expansions: 5 (61 bytes) */

// <06F4C9D1> thirdparty/zstd/zstd.c:47403
void stableSort(COVER_ctx_t* ctx)
{
} /* size: 0 */

// <06F4C96E> thirdparty/zstd/zstd.c:47432
// variables: 3
const size_t* COVER_lower_bound(const size_t* first, const size_t* last, size_t value)
{
	size_t count; // 47434
	{
		size_t step; // 47437
		const size_t* ptr; // 47438
	}
} /* size: 0, variables: 1 */

// <06F4C8AE> thirdparty/zstd/zstd.c:47456
// variables: 3
void COVER_groupBy(const void* data, size_t count, size_t size, COVER_ctx_t* ctx, int (*cmp)(COVER_ctx_t *, const void *, const void *), void (*grp)(COVER_ctx_t *, const void *, const void *))
{
	const BYTE* ptr; // 47459
	size_t num; // 47460
	{
		const BYTE* grpEnd; // 47462
	}
} /* size: 0, variables: 2 */

// <06F4C80E> thirdparty/zstd/zstd.c:47482
// variables: 8
void COVER_group(COVER_ctx_t* ctx, const void* group, const void* groupEnd)
{
	const U32* grpPtr; // 47485
	const U32* grpEnd; // 47486
	const U32  dmerId; // 47491
	U32 freq; // 47493
	const size_t* curOffsetPtr; // 47495
	const size_t* offsetsEnd; // 47496
	size_t curSampleEnd; // 47500
	{
		const size_t* sampleEndPtr; // 47517
	}
} /* size: 0, variables: 7 */

// <06F4C6E9> thirdparty/zstd/zstd.c:47543
// variables: 14
COVER_segment_t COVER_selectSegment(const COVER_ctx_t* ctx, U32* freqs, COVER_map_t* activeDmers, U32 begin, U32 end, ZDICT_cover_params_t parameters)
{
	const U32  k; // 47548
	const U32  d; // 47549
	const U32  dmersInK; // 47550
	COVER_segment_t bestSegment; // 47552
	COVER_segment_t activeSegment; // 47553
	{
		U32 newDmer; // 47565
		U32* newDmerOcc; // 47567
		{
			U32 delDmer; // 47581
			U32* delDmerOcc; // 47582
		}
	}
	{
		U32 newBegin; // 47599
		U32 newEnd; // 47600
		U32 pos; // 47601
		{
			U32 freq; // 47603
		}
	}
	{
		U32 pos; // 47614
	}
} /* size: 0, variables: 5 */

// <06F4C6BC> thirdparty/zstd/zstd.c:47626
int COVER_checkParameters(ZDICT_cover_params_t parameters, size_t maxDictSize)
{
} /* size: 0 */

// <06F4C6A0> thirdparty/zstd/zstd.c:47650
void COVER_ctx_destroy(COVER_ctx_t* ctx)
{
} /* size: 0 */

// <06F4BE27> thirdparty/zstd/zstd.c:47679
// variables: 12
// function calls: 17
size_t COVER_ctx_init(COVER_ctx_t* ctx, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, unsigned int d, double splitPoint)
{
	const BYTE* samples; // 47683
	const size_t  totalSamplesSize; // 47684
	const unsigned int  nbTrainSamples; // 47686
	const unsigned int  nbTestSamples; // 47687
	const size_t  trainingSamplesSize; // 47688
	const size_t  testSamplesSize; // 47689
	{
		U32 i; // 47735
	}
	{
		U32 i; // 47747
		stableSort(COVER_ctx_t* ctx); // 47751
	}
	COVER_sum(const size_t* samplesSizes,
			unsigned int nbSamples);  // 47684
	memset(void* __dest,
		int __ch,
		size_t __len);  // 47708
	{
		const BYTE* grpEnd; // 47462
		{
			const size_t* sampleEndPtr; // 47517
			{
				size_t step; // 47437
				const size_t* ptr; // 47438
			}
			COVER_lower_bound(const size_t* first,
						const size_t* last,
						size_t value);  // 47518
		}
		COVER_group(COVER_ctx_t* ctx,
				const void* group,
				const void* groupEnd);  // 47468
	}
	COVER_groupBy(const void* data,
			size_t count,
			size_t size,
			COVER_ctx_t* ctx,
			int (*cmp)(COVER_ctx_t *, const void *, const void *),
			void (*grp)(COVER_ctx_t *, const void *, const void *));  // 47761
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47693
	COVER_sum(const size_t* samplesSizes,
			unsigned int nbSamples);  // 47688
	COVER_sum(const size_t* samplesSizes,
			unsigned int nbSamples);  // 47689
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47699
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47753
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47741
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47709
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47711
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47704
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47726
	COVER_ctx_destroy(COVER_ctx_t* ctx); // 47727
} /* size: 1730, variables: 6, inline expansions: 14 (807 bytes) */

// <06F4BDDF> thirdparty/zstd/zstd.c:47768
// variable: 1
void COVER_warnOnSmallCorpus(size_t maxDictSize, size_t nbDmers, int displayLevel)
{
	const double  ratio; // 47770
} /* size: 0, variables: 1 */

// <06F4BD7B> thirdparty/zstd/zstd.c:47784
// variables: 2
COVER_epoch_info_t COVER_computeEpochs(U32 maxDictSize, U32 nbDmers, U32 k, U32 passes)
{
	const U32  minEpochSize; // 47787
	COVER_epoch_info_t epochs; // 47788
} /* size: 0, variables: 2 */

// <0701B1B5> thirdparty/zstd/zstd.c:47804
// variables: 23
// function calls: 18
size_t COVER_buildDictionary(const COVER_ctx_t* ctx, U32* freqs, COVER_map_t* activeDmers, void* dictBuffer, size_t dictBufferCapacity, ZDICT_cover_params_t parameters)
{
	const BYTE* dict; // 47808
	size_t tail; // 47809
	const COVER_epoch_info_t  epochs; // 47811
	const size_t  maxZeroScoreRun; // 47813
	size_t zeroScoreRun; // 47814
	size_t epoch; // 47815
	COVER_computeEpochs(U32 maxDictSize,
				U32 nbDmers,
				U32 k,
				U32 passes);  // 47811
	{
		const U32  epochBegin; // 47822
		const U32  epochEnd; // 47823
		size_t segmentSize; // 47824
		COVER_segment_t segment; // 47826
		{
			U32 newDmer; // 47565
			U32* newDmerOcc; // 47567
			COVER_map_hash(COVER_map_t* map,
					U32 key);  // 47236
			{
				COVER_map_pair_t* pos; // 47239
			}
			COVER_map_index(COVER_map_t* map,
					U32 key);  // 47255
			COVER_map_at(COVER_map_t* map,
					U32 key);  // 47567
			{
				U32 delDmer; // 47581
				U32* delDmerOcc; // 47582
				COVER_map_hash(COVER_map_t* map,
						U32 key);  // 47236
				{
					COVER_map_pair_t* pos; // 47239
				}
				COVER_map_index(COVER_map_t* map,
						U32 key);  // 47255
				COVER_map_at(COVER_map_t* map,
						U32 key);  // 47582
				COVER_map_hash(COVER_map_t* map,
						U32 key);  // 47236
				{
					COVER_map_pair_t* pos; // 47239
				}
				COVER_map_index(COVER_map_t* map,
						U32 key);  // 47267
				{
					const COVER_map_pair_t* pos; // 47274
					COVER_map_hash(COVER_map_t* map,
							U32 key);  // 47281
				}
				COVER_map_remove(COVER_map_t* map,
						U32 key);  // 47587
			}
		}
		memset(void* __dest,
			int __ch,
			size_t __len);  // 47201
		COVER_map_clear(COVER_map_t* map); // 47555
		{
			U32 newBegin; // 47599
			U32 newEnd; // 47600
			U32 pos; // 47601
			{
				U32 freq; // 47603
			}
		}
		{
			U32 pos; // 47614
		}
		COVER_selectSegment(const COVER_ctx_t* ctx,
					U32* freqs,
					COVER_map_t* activeDmers,
					U32 begin,
					U32 end,
					ZDICT_cover_params_t parameters);  // 47826
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 47847
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 47848
	}
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47852
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47816
} /* size: 1505, variables: 6, inline expansions: 3 (136 bytes) */

// <06F4BC90> thirdparty/zstd/zstd.c:47804
// variables: 10
size_t COVER_buildDictionary(const COVER_ctx_t* ctx, U32* freqs, COVER_map_t* activeDmers, void* dictBuffer, size_t dictBufferCapacity, ZDICT_cover_params_t parameters)
{
	const BYTE* dict; // 47808
	size_t tail; // 47809
	const COVER_epoch_info_t  epochs; // 47811
	const size_t  maxZeroScoreRun; // 47813
	size_t zeroScoreRun; // 47814
	size_t epoch; // 47815
	{
		const U32  epochBegin; // 47822
		const U32  epochEnd; // 47823
		size_t segmentSize; // 47824
		COVER_segment_t segment; // 47826
	}
} /* size: 0, variables: 6 */

// <06F4B4D8> thirdparty/zstd/zstd.c:47856
// variables: 6
// function calls: 17
size_t ZDICT_trainFromBuffer_cover(void* dictBuffer, size_t dictBufferCapacity, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, ZDICT_cover_params_t parameters)
{
	const BYTE* dict; // 47861
	COVER_ctx_t ctx; // 47862
	COVER_map_t activeDmers; // 47863
	{
		const size_t  initVal; // 47883
	}
	{
		const size_t  tail; // 47898
		const size_t  dictionarySize; // 47901
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 47905
		COVER_ctx_destroy(COVER_ctx_t* ctx); // 47908
		COVER_map_destroy(COVER_map_t* map); // 47909
	}
	COVER_checkParameters(ZDICT_cover_params_t parameters,
				size_t maxDictSize);  // 47868
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47869
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47774
	COVER_warnOnSmallCorpus(size_t maxDictSize,
				size_t nbDmers,
				int displayLevel);  // 47889
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 47211
	memset(void* __dest,
		int __ch,
		size_t __len);  // 47201
	COVER_map_clear(COVER_map_t* map); // 47220
	COVER_map_init(COVER_map_t* map,
			U32 size);  // 47890
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47896
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47877
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47873
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47891
	COVER_ctx_destroy(COVER_ctx_t* ctx); // 47892
} /* size: 1469, variables: 3, inline expansions: 14 (743 bytes) */

// <06F4B1BF> thirdparty/zstd/zstd.c:47916
// variables: 8
size_t COVER_checkTotalCompressedSize(const ZDICT_cover_params_t parameters, const size_t* samplesSizes, const BYTE* samples, size_t* offsets, size_t nbTrainSamples, size_t nbSamples, const BYTE* dict, size_t dictBufferCapacity)
{

_compressCleanup: // 47959
	size_t totalCompressedSize; // 47921
	ZSTD_CCtx* cctx; // 47923
	ZSTD_CDict* cdict; // 47924
	void* dst; // 47925
	size_t dstCapacity; // 47927
	size_t i; // 47928
	{
		size_t maxSampleSize; // 47931
	}
	{
		const size_t  size; // 47950
	}
} /* size: 473, variables: 6, goto labels: 1 */

// <06F4B19E> thirdparty/zstd/zstd.c:47972
void COVER_best_init(COVER_best_t* best)
{
} /* size: 0 */

// <06F4B17D> thirdparty/zstd/zstd.c:47986
void COVER_best_wait(COVER_best_t* best)
{
} /* size: 0 */

// <06F4B15C> thirdparty/zstd/zstd.c:48000
void COVER_best_destroy(COVER_best_t* best)
{
} /* size: 0 */

// <06F4B13B> thirdparty/zstd/zstd.c:48016
void COVER_best_start(COVER_best_t* best)
{
} /* size: 0 */

// <06F4AFA8> thirdparty/zstd/zstd.c:48030
// variables: 4
// function call: 1
void COVER_best_finish(COVER_best_t* best, ZDICT_cover_params_t parameters, COVER_dictSelection_t selection)
{
	void* dict; // 48034
	size_t compressedSize; // 48035
	size_t dictSize; // 48036
	{
		size_t liveJobs; // 48041
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 48063
	}
} /* size: 285, variables: 3 */

// <06F4AF64> thirdparty/zstd/zstd.c:48076
// variable: 1
COVER_dictSelection_t setDictSelection(BYTE* buf, size_t s, size_t csz)
{
	COVER_dictSelection_t ds; // 48078
} /* size: 0, variables: 1 */

// <06F4AF3F> thirdparty/zstd/zstd.c:48085
COVER_dictSelection_t COVER_dictSelectionError(size_t error)
{
} /* size: 0 */

// <06F4AF1A> thirdparty/zstd/zstd.c:48089
unsigned int COVER_dictSelectionIsError(COVER_dictSelection_t selection)
{
} /* size: 0 */

// <06F4AEF9> thirdparty/zstd/zstd.c:48093
void COVER_dictSelectionFree(COVER_dictSelection_t selection)
{
} /* size: 0 */

// <06F4A724> thirdparty/zstd/zstd.c:48097
// variables: 6
// function calls: 14
COVER_dictSelection_t COVER_selectDict(BYTE* customDictContent, size_t dictBufferCapacity, size_t dictContentSize, const BYTE* samplesBuffer, const size_t* samplesSizes, unsigned int nbFinalizeSamples, size_t nbCheckSamples, size_t nbSamples, ZDICT_cover_params_t params, size_t* offsets, size_t totalCompressedSize)
{
	size_t largestDict; // 48101
	size_t largestCompressed; // 48102
	BYTE* customDictContentEnd; // 48103
	BYTE* largestDictbuffer; // 48105
	BYTE* candidateDictBuffer; // 48106
	double regressionTolerance; // 48107
	memcpy(void* __dest,
		const void* __src,
		size_t __len);  // 48116
	memcpy(void* __dest,
		const void* __src,
		size_t __len);  // 48149
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48086
	COVER_dictSelectionError(size_t error); // 48124
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48086
	COVER_dictSelectionError(size_t error); // 48112
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48086
	COVER_dictSelectionError(size_t error); // 48135
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48140
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48086
	COVER_dictSelectionError(size_t error); // 48157
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48086
	COVER_dictSelectionError(size_t error); // 48169
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48174
} /* size: 983, variables: 6, inline expansions: 14 (187 bytes) */

// <06F3F02C> thirdparty/zstd/zstd.c:48187
// member variables: 4
// struct size: 72
struct COVER_tryParameters_data_s {
	const COVER_ctx_t * ctx; /* 0 8 */
	COVER_best_t * best; /* 8 8 */
	size_t dictBufferCapacity; /* 16 8 */
	ZDICT_cover_params_t parameters; /* 24 48 */
};

// <06F4A0E2> thirdparty/zstd/zstd.c:48199
// variables: 10
// function calls: 14
void COVER_tryParameters(void* opaque)
{

_cleanup: // 48235
	const COVER_tryParameters_data_t* data; // 48202
	const COVER_ctx_t* ctx; // 48203
	const ZDICT_cover_params_t  parameters; // 48204
	size_t dictBufferCapacity; // 48205
	size_t totalCompressedSize; // 48206
	COVER_map_t activeDmers; // 48208
	const BYTE* dict; // 48209
	COVER_dictSelection_t selection; // 48210
	const U32* freqs; // 48211
	{
		const size_t  tail; // 48224
		COVER_dictSelectionIsError(COVER_dictSelection_t selection); // 48230
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 48231
	}
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48086
	COVER_dictSelectionError(size_t error); // 48210
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 47211
	memset(void* __dest,
		int __ch,
		size_t __len);  // 47201
	COVER_map_clear(COVER_map_t* map); // 47220
	COVER_map_init(COVER_map_t* map,
			U32 size);  // 48212
	memcpy(void* __dest,
		const void* __src,
		size_t __len);  // 48221
	COVER_map_destroy(COVER_map_t* map); // 48239
	COVER_dictSelectionFree(COVER_dictSelection_t selection); // 48240
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 48213
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 48217
} /* size: 908, variables: 9, goto labels: 1, inline expansions: 12 (300 bytes) */

// <06F4925A> thirdparty/zstd/zstd.c:48244
// variables: 22
// function calls: 32
size_t ZDICT_optimizeTrainFromBuffer_cover(void* dictBuffer, size_t dictBufferCapacity, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, ZDICT_cover_params_t* parameters)
{
	const unsigned int  nbThreads; // 48250
	const double  splitPoint; // 48251
	const unsigned int  kMinD; // 48253
	const unsigned int  kMaxD; // 48254
	const unsigned int  kMinK; // 48255
	const unsigned int  kMaxK; // 48256
	const unsigned int  kSteps; // 48257
	const unsigned int  kStepSize; // 48258
	const unsigned int  kIterations; // 48259
	const unsigned int  shrinkDict; // 48261
	const int  displayLevel; // 48263
	unsigned int iteration; // 48264
	unsigned int d; // 48265
	unsigned int k; // 48266
	COVER_best_t best; // 48267
	POOL_ctx* pool; // 48268
	int warned; // 48269
	{
		COVER_ctx_t ctx; // 48304
		{
			const size_t  initVal; // 48307
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 48309
			COVER_best_destroy(COVER_best_t* best); // 48310
			POOL_free(POOL_ctx* ctx); // 48311
		}
		{
			COVER_tryParameters_data_t* data; // 48322
			COVER_checkParameters(ZDICT_cover_params_t parameters,
						size_t maxDictSize);  // 48343
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 48344
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 48324
			COVER_best_start(COVER_best_t* best); // 48016
			COVER_best_start(COVER_best_t* best); // 48349
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 48356
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 48326
			COVER_best_destroy(COVER_best_t* best); // 48327
			COVER_ctx_destroy(COVER_ctx_t* ctx); // 48328
			POOL_free(POOL_ctx* ctx); // 48329
		}
		COVER_best_wait(COVER_best_t* best); // 47986
		COVER_best_wait(COVER_best_t* best); // 48360
		COVER_ctx_destroy(COVER_ctx_t* ctx); // 48361
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 48305
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 47774
		COVER_warnOnSmallCorpus(size_t maxDictSize,
					size_t nbDmers,
					int displayLevel);  // 48316
	}
	{
		const size_t  dictSize; // 48366
		{
			const size_t  compressedSize; // 48368
			COVER_best_destroy(COVER_best_t* best); // 48369
		}
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 48374
		COVER_best_destroy(COVER_best_t* best); // 48375
		POOL_free(POOL_ctx* ctx); // 48376
	}
	POOL_create(size_t numThreads,
			size_t queueSize);  // 48290
	memset(void* __dest,
		int __ch,
		size_t __len);  // 47980
	COVER_best_init(COVER_best_t* best); // 47972
	COVER_best_init(COVER_best_t* best); // 48296
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 48273
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 48300
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 48363
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 48281
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 48285
} /* size: 2818, variables: 17, inline expansions: 9 (454 bytes) */

// <06F4923C> thirdparty/zstd/zstd.c:48594
inline int ss_ilg(int n)
{
} /* size: 0 */

// <06F49208> thirdparty/zstd/zstd.c:48637
// variables: 2
inline int ss_isqrt(int x)
{
	int y; // 48638
	int e; // 48638
} /* size: 0, variables: 2 */

// <06F49193> thirdparty/zstd/zstd.c:48670
// variables: 4
inline int ss_compare(const unsigned char* T, const int* p1, const int* p2, int depth)
{
	const unsigned char* U1; // 48673
	const unsigned char* U2; // 48673
	const unsigned char* U1n; // 48673
	const unsigned char* U2n; // 48673
} /* size: 0, variables: 4 */

// <06F4911A> thirdparty/zstd/zstd.c:48696
// variables: 4
void ss_insertionsort(const unsigned char* T, const int* PA, int* first, int* last, int depth)
{
	int* i; // 48698
	int* j; // 48698
	int t; // 48699
	int r; // 48700
} /* size: 0, variables: 4 */

// <06F4908D> thirdparty/zstd/zstd.c:48721
// variables: 6
inline void ss_fixdown(const unsigned char* Td, const int* PA, int* SA, int i, int size)
{
	int j; // 48723
	int k; // 48723
	int v; // 48724
	int c; // 48725
	int d; // 48725
	int e; // 48725
} /* size: 0, variables: 6 */

// <06F4902C> thirdparty/zstd/zstd.c:48738
// variables: 3
void ss_heapsort(const unsigned char* Td, const int* PA, int* SA, int size)
{
	int i; // 48739
	int m; // 48739
	int t; // 48740
} /* size: 0, variables: 3 */

// <06F48FD2> thirdparty/zstd/zstd.c:48763
// variable: 1
inline int* ss_median3(const unsigned char* Td, const int* PA, int* v1, int* v2, int* v3)
{
	int* t; // 48765
} /* size: 0, variables: 1 */

// <06F48F60> thirdparty/zstd/zstd.c:48777
// variable: 1
inline int* ss_median5(const unsigned char* Td, const int* PA, int* v1, int* v2, int* v3, int* v4, int* v5)
{
	int* t; // 48779
} /* size: 0, variables: 1 */

// <06F48F03> thirdparty/zstd/zstd.c:48792
// variables: 2
inline int* ss_pivot(const unsigned char* Td, const int* PA, int* first, int* last)
{
	int* middle; // 48793
	int t; // 48794
} /* size: 0, variables: 2 */

// <06F48E9C> thirdparty/zstd/zstd.c:48820
// variables: 3
inline int* ss_partition(const int* PA, int* first, int* last, int depth)
{
	int* a; // 48822
	int* b; // 48822
	int t; // 48823
} /* size: 0, variables: 3 */

// <07056986> thirdparty/zstd/zstd.c:48839
// variables: 14
// function calls: 37
void ss_mintrosort(const unsigned char* T, const int* PA, int* first, int* last, int depth)
{
	 stack; // 48843
	const unsigned char* Td; // 48844
	int* a; // 48845
	int* b; // 48845
	int* c; // 48845
	int* d; // 48845
	int* e; // 48845
	int* f; // 48845
	int s; // 48846
	int t; // 48846
	int ssize; // 48847
	int limit; // 48848
	int v; // 48849
	int x; // 48849
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48851
	ss_median3(const unsigned char* Td,
			const int* PA,
			int* v1,
			int* v2,
			int* v3);  // 48801
	ss_median3(const unsigned char* Td,
			const int* PA,
			int* v1,
			int* v2,
			int* v3);  // 48808
	ss_median3(const unsigned char* Td,
			const int* PA,
			int* v1,
			int* v2,
			int* v3);  // 48809
	ss_median3(const unsigned char* Td,
			const int* PA,
			int* v1,
			int* v2,
			int* v3);  // 48810
	ss_median3(const unsigned char* Td,
			const int* PA,
			int* v1,
			int* v2,
			int* v3);  // 48811
	ss_median5(const unsigned char* Td,
			const int* PA,
			int* v1,
			int* v2,
			int* v3,
			int* v4,
			int* v5);  // 48804
	ss_pivot(const unsigned char* Td,
		const int* PA,
		int* first,
		int* last);  // 48893
	ss_fixdown(const unsigned char* Td,
			const int* PA,
			int* SA,
			int i,
			int size);  // 48748
	ss_fixdown(const unsigned char* Td,
			const int* PA,
			int* SA,
			int i,
			int size);  // 48752
	ss_fixdown(const unsigned char* Td,
			const int* PA,
			int* SA,
			int i,
			int size);  // 48749
	ss_heapsort(const unsigned char* Td,
			const int* PA,
			int* SA,
			int size);  // 48862
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 48703
	ss_insertionsort(const unsigned char* T,
			const int* PA,
			int* first,
			int* last,
			int depth);  // 48855
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48933
	ss_partition(const int* PA,
			int* first,
			int* last,
			int depth);  // 48963
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48964
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48883
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48947
	ss_partition(const int* PA,
			int* first,
			int* last,
			int depth);  // 48872
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48886
	ss_partition(const int* PA,
			int* first,
			int* last,
			int depth);  // 48929
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48877
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48952
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48938
	ss_ilg(int n); // 48943
	ss_ilg(int n); // 48594
	ss_ilg(int n); // 48957
} /* size: 5029, variables: 14, inline expansions: 37 (4883 bytes) */

// <06F48CE1> thirdparty/zstd/zstd.c:48839
// variables: 14
void ss_mintrosort(const unsigned char* T, const int* PA, int* first, int* last, int depth)
{
	 stack; // 48843
	const unsigned char* Td; // 48844
	int* a; // 48845
	int* b; // 48845
	int* c; // 48845
	int* d; // 48845
	int* e; // 48845
	int* f; // 48845
	int s; // 48846
	int t; // 48846
	int ssize; // 48847
	int limit; // 48848
	int v; // 48849
	int x; // 48849
} /* size: 0, variables: 14 */

// <06F48CA6> thirdparty/zstd/zstd.c:48981
// variable: 1
inline void ss_blockswap(int* a, int* b, int n)
{
	int t; // 48982
} /* size: 0, variables: 1 */

// <06F48C39> thirdparty/zstd/zstd.c:48990
// variables: 5
inline void ss_rotate(int* first, int* middle, int* last)
{
	int* a; // 48991
	int* b; // 48991
	int t; // 48991
	int l; // 48992
	int r; // 48992
} /* size: 0, variables: 5 */

// <06F48B83> thirdparty/zstd/zstd.c:49031
// variables: 8
void ss_inplacemerge(const unsigned char* T, const int* PA, int* first, int* middle, int* last, int depth)
{
	const int* p; // 49034
	int* a; // 49035
	int* b; // 49035
	int len; // 49036
	int half; // 49036
	int q; // 49037
	int r; // 49037
	int x; // 49038
} /* size: 0, variables: 8 */

// <06F48AD8> thirdparty/zstd/zstd.c:49074
// variables: 6
void ss_mergeforward(const unsigned char* T, const int* PA, int* first, int* middle, int* last, int* buf, int depth)
{
	int* a; // 49077
	int* b; // 49077
	int* c; // 49077
	int* bufend; // 49077
	int t; // 49078
	int r; // 49079
} /* size: 0, variables: 6 */

// <06F48A0A> thirdparty/zstd/zstd.c:49124
// variables: 9
void ss_mergebackward(const unsigned char* T, const int* PA, int* first, int* middle, int* last, int* buf, int depth)
{
	const int* p1; // 49127
	const int* p2; // 49127
	int* a; // 49128
	int* b; // 49128
	int* c; // 49128
	int* bufend; // 49128
	int t; // 49129
	int r; // 49130
	int x; // 49131
} /* size: 0, variables: 9 */

// <0706D5F3> thirdparty/zstd/zstd.c:49183
// variables: 11
// function calls: 15
void ss_swapmerge(const unsigned char* T, const int* PA, int* first, int* middle, int* last, int* buf, int bufsize, int depth)
{
	 stack; // 49198
	int* l; // 49199
	int* r; // 49199
	int* lm; // 49199
	int* rm; // 49199
	int m; // 49200
	int len; // 49200
	int half; // 49200
	int ssize; // 49201
	int check; // 49202
	int next; // 49202
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49209
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49218
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49209
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49218
	ss_blockswap(int* a,
			int* b,
			int n);  // 49134
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49142
	ss_mergebackward(const unsigned char* T,
			const int* PA,
			int* first,
			int* middle,
			int* last,
			int* buf,
			int depth);  // 49207
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49226
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49257
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49260
	ss_blockswap(int* a,
			int* b,
			int n);  // 49082
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49085
	ss_mergeforward(const unsigned char* T,
			const int* PA,
			int* first,
			int* middle,
			int* last,
			int* buf,
			int depth);  // 49216
	ss_blockswap(int* a,
			int* b,
			int n);  // 49235
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49260
} /* size: 4021, variables: 11, inline expansions: 15 (2997 bytes) */

// <06F48842> thirdparty/zstd/zstd.c:49183
// variables: 11
void ss_swapmerge(const unsigned char* T, const int* PA, int* first, int* middle, int* last, int* buf, int bufsize, int depth)
{
	 stack; // 49198
	int* l; // 49199
	int* r; // 49199
	int* lm; // 49199
	int* rm; // 49199
	int m; // 49200
	int len; // 49200
	int half; // 49200
	int ssize; // 49201
	int check; // 49202
	int next; // 49202
} /* size: 0, variables: 11 */

// <06F48749> thirdparty/zstd/zstd.c:49275
// variables: 10
void sssort(const unsigned char* T, const int* PA, int* first, int* last, int* buf, int bufsize, int depth, int n, int lastsuffix)
{
	int* a; // 49279
	int* b; // 49281
	int* middle; // 49281
	int* curbuf; // 49281
	int j; // 49282
	int k; // 49282
	int curbufsize; // 49282
	int limit; // 49282
	int i; // 49284
	{
		int PAi; // 49335
	}
} /* size: 0, variables: 9 */

// <06F486C9> thirdparty/zstd/zstd.c:49366
// variables: 4
void tr_insertionsort(const int* ISAd, int* first, int* last)
{
	int* a; // 49367
	int* b; // 49367
	int t; // 49368
	int r; // 49368
} /* size: 0, variables: 4 */

// <06F48647> thirdparty/zstd/zstd.c:49385
// variables: 6
inline void tr_fixdown(const int* ISAd, int* SA, int i, int size)
{
	int j; // 49386
	int k; // 49386
	int v; // 49387
	int c; // 49388
	int d; // 49388
	int e; // 49388
} /* size: 0, variables: 6 */

// <06F485F1> thirdparty/zstd/zstd.c:49401
// variables: 3
void tr_heapsort(const int* ISAd, int* SA, int size)
{
	int i; // 49402
	int m; // 49402
	int t; // 49403
} /* size: 0, variables: 3 */

// <06F485A2> thirdparty/zstd/zstd.c:49426
// variable: 1
inline int* tr_median3(const int* ISAd, int* v1, int* v2, int* v3)
{
	int* t; // 49427
} /* size: 0, variables: 1 */

// <06F4853B> thirdparty/zstd/zstd.c:49439
// variable: 1
inline int* tr_median5(const int* ISAd, int* v1, int* v2, int* v3, int* v4, int* v5)
{
	int* t; // 49441
} /* size: 0, variables: 1 */

// <06F484E9> thirdparty/zstd/zstd.c:49454
// variables: 2
inline int* tr_pivot(const int* ISAd, int* first, int* last)
{
	int* middle; // 49455
	int t; // 49456
} /* size: 0, variables: 2 */

// <06F3F0E9> thirdparty/zstd/zstd.c:49480
// member variables: 4
// struct size: 16
struct _trbudget_t {
	int chance; /* 0 4 */
	int remain; /* 4 4 */
	int incval; /* 8 4 */
	int count; /* 12 4 */
};

// <06F484B3> thirdparty/zstd/zstd.c:49489
inline void trbudget_init(trbudget_t* budget, int chance, int incval)
{
} /* size: 0 */

// <06F48486> thirdparty/zstd/zstd.c:49496
inline int trbudget_check(trbudget_t* budget, int size)
{
} /* size: 0 */

// <06F48329> thirdparty/zstd/zstd.c:49509
// variables: 9
void tr_partition(const int* ISAd, int* first, int* middle, int* last, int** pa, int** pb, int v)
{
	int* a; // 49512
	int* b; // 49512
	int* c; // 49512
	int* d; // 49512
	int* e; // 49512
	int* f; // 49512
	int t; // 49513
	int s; // 49513
	int x; // 49514
} /* size: 598, variables: 9 */

// <06F4828D> thirdparty/zstd/zstd.c:49551
// variables: 5
void tr_copy(int* ISA, const int* SA, int* first, int* a, int* b, int* last, int depth)
{
	int* c; // 49556
	int* d; // 49556
	int* e; // 49556
	int s; // 49557
	int v; // 49557
} /* size: 0, variables: 5 */

// <06F481CA> thirdparty/zstd/zstd.c:49576
// variables: 8
void tr_partialcopy(int* ISA, const int* SA, int* first, int* a, int* b, int* last, int depth)
{
	int* c; // 49579
	int* d; // 49579
	int* e; // 49579
	int s; // 49580
	int v; // 49580
	int rank; // 49581
	int lastrank; // 49581
	int newrank; // 49581
} /* size: 0, variables: 8 */

// <0706CAB5> thirdparty/zstd/zstd.c:49614
// variables: 12
// function calls: 36
void tr_introsort(int* ISA, const int* ISAd, int* SA, int* first, int* last, trbudget_t* budget)
{
	 stack; // 49618
	int* a; // 49619
	int* b; // 49619
	int* c; // 49619
	int t; // 49620
	int v; // 49621
	int x; // 49621
	int incr; // 49622
	int limit; // 49623
	int next; // 49623
	int ssize; // 49624
	int trlink; // 49624
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49626
	tr_copy(int* ISA,
		const int* SA,
		int* first,
		int* a,
		int* b,
		int* last,
		int depth);  // 49670
	tr_median3(const int* ISAd,
			int* v1,
			int* v2,
			int* v3);  // 49463
	tr_median3(const int* ISAd,
			int* v1,
			int* v2,
			int* v3);  // 49470
	tr_median3(const int* ISAd,
			int* v1,
			int* v2,
			int* v3);  // 49471
	tr_median3(const int* ISAd,
			int* v1,
			int* v2,
			int* v3);  // 49472
	tr_median3(const int* ISAd,
			int* v1,
			int* v2,
			int* v3);  // 49473
	tr_median5(const int* ISAd,
			int* v1,
			int* v2,
			int* v3,
			int* v4,
			int* v5);  // 49466
	tr_pivot(const int* ISAd,
		int* first,
		int* last);  // 49732
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49739
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49685
	trbudget_check(trbudget_t* budget,
			int size);  // 49689
	trbudget_check(trbudget_t* budget,
			int size);  // 49823
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49824
	tr_insertionsort(const int* ISAd,
			int* first,
			int* last);  // 49717
	tr_partialcopy(int* ISA,
			const int* SA,
			int* first,
			int* a,
			int* b,
			int* last,
			int depth);  // 49673
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49661
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49649
	tr_ilg(int n); // 49650
	trbudget_check(trbudget_t* budget,
			int size);  // 49746
	tr_fixdown(const int* ISAd,
			int* SA,
			int i,
			int size);  // 49411
	tr_fixdown(const int* ISAd,
			int* SA,
			int i,
			int size);  // 49415
	tr_fixdown(const int* ISAd,
			int* SA,
			int i,
			int size);  // 49412
	tr_heapsort(const int* ISAd,
			int* SA,
			int size);  // 49723
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49658
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49659
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49652
} /* size: 6883, variables: 12, inline expansions: 36 (5609 bytes) */

// <06F47FFD> thirdparty/zstd/zstd.c:49614
// variables: 12
void tr_introsort(int* ISA, const int* ISAd, int* SA, int* first, int* last, trbudget_t* budget)
{
	 stack; // 49618
	int* a; // 49619
	int* b; // 49619
	int* c; // 49619
	int t; // 49620
	int v; // 49621
	int x; // 49621
	int incr; // 49622
	int limit; // 49623
	int next; // 49623
	int ssize; // 49624
	int trlink; // 49624
} /* size: 0, variables: 12 */

// <06F47F64> thirdparty/zstd/zstd.c:49841
// variables: 7
void trsort(int* ISA, int* SA, int n, int depth)
{
	int* ISAd; // 49842
	int* first; // 49843
	int* last; // 49843
	trbudget_t budget; // 49844
	int t; // 49845
	int skip; // 49845
	int unsorted; // 49845
} /* size: 0, variables: 7 */

// <0706DE93> thirdparty/zstd/zstd.c:49880
// variables: 12
// function calls: 11
int sort_typeBstar(const unsigned char* T, int* SA, int* bucket_A, int* bucket_B, int n, int openMP)
{
	int* PAb; // 49883
	int* ISAb; // 49883
	int* buf; // 49883
	int i; // 49888
	int j; // 49888
	int k; // 49888
	int t; // 49888
	int m; // 49888
	int bufsize; // 49888
	int c0; // 49889
	int c1; // 49889
	{
		int PAi; // 49335
		ss_compare(const unsigned char* T,
				const int* p1,
				const int* p2,
				int depth);  // 49337
	}
	ss_compare(const unsigned char* T,
			const int* p1,
			const int* p2,
			int depth);  // 49047
	ss_blockswap(int* a,
			int* b,
			int n);  // 48995
	ss_rotate(int* first,
			int* middle,
			int* last);  // 49057
	ss_inplacemerge(const unsigned char* T,
			const int* PA,
			int* first,
			int* middle,
			int* last,
			int depth);  // 49329
	ss_isqrt(int x); // 49293
	sssort(const unsigned char* T,
		const int* PA,
		int* first,
		int* last,
		int* buf,
		int bufsize,
		int depth,
		int n,
		int lastsuffix);  // 49995
	tr_ilg(int n); // 49350
	tr_ilg(int n); // 49847
	trbudget_init(trbudget_t* budget,
			int chance,
			int incval);  // 49847
	trsort(int* ISA,
		int* SA,
		int n,
		int depth);  // 50016
} /* size: 4474, variables: 11, inline expansions: 10 (4971 bytes) */

// <06F47E85> thirdparty/zstd/zstd.c:49880
// variables: 11
int sort_typeBstar(const unsigned char* T, int* SA, int* bucket_A, int* bucket_B, int n, int openMP)
{
	int* PAb; // 49883
	int* ISAb; // 49883
	int* buf; // 49883
	int i; // 49888
	int j; // 49888
	int k; // 49888
	int t; // 49888
	int m; // 49888
	int bufsize; // 49888
	int c0; // 49889
	int c1; // 49889
} /* size: 0, variables: 11 */

// <06F47DDF> thirdparty/zstd/zstd.c:50052
// variables: 7
void construct_SA(const unsigned char* T, int* SA, int* bucket_A, int* bucket_B, int n, int m)
{
	int* i; // 50055
	int* j; // 50055
	int* k; // 50055
	int s; // 50056
	int c0; // 50057
	int c1; // 50057
	int c2; // 50057
} /* size: 0, variables: 7 */

// <06F47D28> thirdparty/zstd/zstd.c:50116
// variables: 8
int construct_BWT(const unsigned char* T, int* SA, int* bucket_A, int* bucket_B, int n, int m)
{
	int* i; // 50119
	int* j; // 50119
	int* k; // 50119
	int* orig; // 50119
	int s; // 50120
	int c0; // 50121
	int c1; // 50121
	int c2; // 50121
} /* size: 0, variables: 8 */

// <06F47C4A> thirdparty/zstd/zstd.c:50187
// variables: 9
int construct_BWT_indexes(const unsigned char* T, int* SA, int* bucket_A, int* bucket_B, int n, int m, unsigned char* num_indexes, int* indexes)
{
	int* i; // 50191
	int* j; // 50191
	int* k; // 50191
	int* orig; // 50191
	int s; // 50192
	int c0; // 50193
	int c1; // 50193
	int c2; // 50193
	int mod; // 50195
} /* size: 0, variables: 9 */

// <06F47A26> thirdparty/zstd/zstd.c:50286
// variables: 4
// function call: 1
int divsufsort(const unsigned char* T, int* SA, int n, int openMP)
{
	int* bucket_A; // 50287
	int* bucket_B; // 50287
	int m; // 50288
	int err; // 50289
	construct_SA(const unsigned char* T,
			int* SA,
			int* bucket_A,
			int* bucket_B,
			int n,
			int m);  // 50303
} /* size: 642, variables: 4, inline expansions: 1 (390 bytes) */

// <06F47649> thirdparty/zstd/zstd.c:50315
// variables: 6
// function calls: 2
int divbwt(const unsigned char* T, unsigned char* U, int* A, int n, unsigned char* num_indexes, int* indexes, int openMP)
{
	int* B; // 50316
	int* bucket_A; // 50317
	int* bucket_B; // 50317
	int m; // 50318
	int pidx; // 50318
	int i; // 50318
	construct_BWT_indexes(const unsigned char* T,
				int* SA,
				int* bucket_A,
				int* bucket_B,
				int n,
				int m,
				unsigned char* num_indexes,
				int* indexes);  // 50335
	construct_BWT(const unsigned char* T,
			int* SA,
			int* bucket_A,
			int* bucket_B,
			int n,
			int m);  // 50333
} /* size: 0, variables: 6, inline expansions: 2 (0 bytes) */

// <06F47615> thirdparty/zstd/zstd.c:50446
size_t FASTCOVER_hashPtrToIndex(const void* p, U32 f, unsigned int d)
{
} /* size: 0 */

// <06F3F138> thirdparty/zstd/zstd.c:50457
// member variables: 2
// struct size: 8
typedef struct {
	unsigned int finalize; /* 0 4 */
	unsigned int skip; /* 4 4 */
} FASTCOVER_accel_t;

// <06F3F1A5> thirdparty/zstd/zstd.c:50481
// member variables: 11
// struct size: 80
typedef struct {
	const BYTE * samples; /* 0 8 */
	size_t * offsets; /* 8 8 */
	const size_t * samplesSizes; /* 16 8 */
	size_t nbSamples; /* 24 8 */
	size_t nbTrainSamples; /* 32 8 */
	size_t nbTestSamples; /* 40 8 */
	size_t nbDmers; /* 48 8 */
	U32 * freqs; /* 56 8 */
	unsigned int d; /* 64 4 */
	unsigned int f; /* 68 4 */
	FASTCOVER_accel_t accelParams; /* 72 8 */
} FASTCOVER_ctx_t;

// <06F4751B> thirdparty/zstd/zstd.c:50510
// variables: 11
COVER_segment_t FASTCOVER_selectSegment(const FASTCOVER_ctx_t* ctx, U32* freqs, U32 begin, U32 end, ZDICT_cover_params_t parameters, U16* segmentFreqs)
{
	const U32  k; // 50515
	const U32  d; // 50516
	const U32  f; // 50517
	const U32  dmersInK; // 50518
	COVER_segment_t bestSegment; // 50521
	COVER_segment_t activeSegment; // 50522
	{
		const size_t  idx; // 50535
		{
			const size_t  delIndex; // 50547
		}
	}
	{
		const size_t  delIndex; // 50565
	}
	{
		U32 pos; // 50572
		{
			const size_t  i; // 50574
		}
	}
} /* size: 0, variables: 6 */

// <06F474BA> thirdparty/zstd/zstd.c:50622
void FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx)
{
} /* size: 0 */

// <06F4742B> thirdparty/zstd/zstd.c:50638
// variables: 8
void FASTCOVER_computeFrequency(U32* freqs, const FASTCOVER_ctx_t* ctx)
{
	const unsigned int  f; // 50640
	const unsigned int  d; // 50641
	const unsigned int  skip; // 50642
	const unsigned int  readLength; // 50643
	size_t i; // 50644
	{
		size_t start; // 50648
		const size_t  currSampleEnd; // 50649
		{
			const size_t  dmerIndex; // 50651
		}
	}
} /* size: 0, variables: 5 */

// <06F46B6C> thirdparty/zstd/zstd.c:50667
// variables: 10
// function calls: 24
size_t FASTCOVER_ctx_init(FASTCOVER_ctx_t* ctx, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, unsigned int d, double splitPoint, unsigned int f, FASTCOVER_accel_t accelParams)
{
	const BYTE* samples; // 50673
	const size_t  totalSamplesSize; // 50674
	const unsigned int  nbTrainSamples; // 50676
	const unsigned int  nbTestSamples; // 50677
	const size_t  trainingSamplesSize; // 50678
	const size_t  testSamplesSize; // 50679
	{
		U32 i; // 50727
	}
	COVER_sum(const size_t* samplesSizes,
			unsigned int nbSamples);  // 50674
	memset(void* __dest,
		int __ch,
		size_t __len);  // 50702
	{
		size_t start; // 50648
		const size_t  currSampleEnd; // 50649
		{
			const size_t  dmerIndex; // 50651
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 50448
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 50450
			FASTCOVER_hashPtrToIndex(const void* p,
						U32 f,
						unsigned int d);  // 50651
		}
	}
	FASTCOVER_computeFrequency(U32* freqs,
					const FASTCOVER_ctx_t* ctx);  // 50744
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50684
	COVER_sum(const size_t* samplesSizes,
			unsigned int nbSamples);  // 50678
	COVER_sum(const size_t* samplesSizes,
			unsigned int nbSamples);  // 50679
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50691
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50743
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50703
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50705
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50697
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50721
	FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 50622
	FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 50722
	FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 50622
	FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 50739
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50738
} /* size: 1466, variables: 6, inline expansions: 17 (740 bytes) */

// <0701A834> thirdparty/zstd/zstd.c:50754
// variables: 15
// function calls: 34
size_t FASTCOVER_buildDictionary(const FASTCOVER_ctx_t* ctx, U32* freqs, void* dictBuffer, size_t dictBufferCapacity, U16* segmentFreqs, ZDICT_cover_params_t parameters)
{
	const BYTE* dict; // 50760
	size_t tail; // 50761
	const COVER_epoch_info_t  epochs; // 50763
	const size_t  maxZeroScoreRun; // 50765
	size_t zeroScoreRun; // 50766
	size_t epoch; // 50767
	COVER_computeEpochs(U32 maxDictSize,
				U32 nbDmers,
				U32 k,
				U32 passes);  // 50763
	{
		const U32  epochBegin; // 50774
		const U32  epochEnd; // 50775
		size_t segmentSize; // 50776
		COVER_segment_t segment; // 50778
		{
			const size_t  idx; // 50535
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 50448
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 50450
			FASTCOVER_hashPtrToIndex(const void* p,
						U32 f,
						unsigned int d);  // 50535
			{
				const size_t  delIndex; // 50547
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20069
				ZSTD_hash6(U64 u,
						U32 h,
						U64 s);  // 20069
				ZSTD_hash6Ptr(const void* p,
						U32 h);  // 50448
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 50450
				FASTCOVER_hashPtrToIndex(const void* p,
							U32 f,
							unsigned int d);  // 50547
			}
		}
		{
			const size_t  delIndex; // 50565
			ZSTD_hash8(U64 u,
					U32 h,
					U64 s);  // 20079
			ZSTD_hash8Ptr(const void* p,
					U32 h);  // 50450
			MEM_read64(const void* ptr); // 1317
			MEM_readLE64(const void* memPtr); // 20069
			ZSTD_hash6(U64 u,
					U32 h,
					U64 s);  // 20069
			ZSTD_hash6Ptr(const void* p,
					U32 h);  // 50448
			FASTCOVER_hashPtrToIndex(const void* p,
						U32 f,
						unsigned int d);  // 50565
		}
		{
			U32 pos; // 50572
			{
				const size_t  i; // 50574
				ZSTD_hash8(U64 u,
						U32 h,
						U64 s);  // 20079
				ZSTD_hash8Ptr(const void* p,
						U32 h);  // 50450
				MEM_read64(const void* ptr); // 1317
				MEM_readLE64(const void* memPtr); // 20069
				ZSTD_hash6(U64 u,
						U32 h,
						U64 s);  // 20069
				ZSTD_hash6Ptr(const void* p,
						U32 h);  // 50448
				FASTCOVER_hashPtrToIndex(const void* p,
							U32 f,
							unsigned int d);  // 50574
			}
		}
		FASTCOVER_selectSegment(const FASTCOVER_ctx_t* ctx,
					U32* freqs,
					U32 begin,
					U32 end,
					ZDICT_cover_params_t parameters,
					U16* segmentFreqs);  // 50778
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 50802
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 50803
	}
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50807
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50768
} /* size: 1228, variables: 6, inline expansions: 3 (130 bytes) */

// <06F46A87> thirdparty/zstd/zstd.c:50754
// variables: 10
size_t FASTCOVER_buildDictionary(const FASTCOVER_ctx_t* ctx, U32* freqs, void* dictBuffer, size_t dictBufferCapacity, ZDICT_cover_params_t parameters, U16* segmentFreqs)
{
	const BYTE* dict; // 50760
	size_t tail; // 50761
	const COVER_epoch_info_t  epochs; // 50763
	const size_t  maxZeroScoreRun; // 50765
	size_t zeroScoreRun; // 50766
	size_t epoch; // 50767
	{
		const U32  epochBegin; // 50774
		const U32  epochEnd; // 50775
		size_t segmentSize; // 50776
		COVER_segment_t segment; // 50778
	}
} /* size: 0, variables: 6 */

// <06F3F272> thirdparty/zstd/zstd.c:50814
// member variables: 4
// struct size: 72
struct FASTCOVER_tryParameters_data_s {
	const FASTCOVER_ctx_t * ctx; /* 0 8 */
	COVER_best_t * best; /* 8 8 */
	size_t dictBufferCapacity; /* 16 8 */
	ZDICT_cover_params_t parameters; /* 24 48 */
};

// <06F465EF> thirdparty/zstd/zstd.c:50827
// variables: 11
// function calls: 7
void FASTCOVER_tryParameters(void* opaque)
{

_cleanup: // 50861
	const FASTCOVER_tryParameters_data_t* data; // 50830
	const FASTCOVER_ctx_t* ctx; // 50831
	const ZDICT_cover_params_t  parameters; // 50832
	size_t dictBufferCapacity; // 50833
	size_t totalCompressedSize; // 50834
	U16* segmentFreqs; // 50836
	const BYTE* dict; // 50838
	COVER_dictSelection_t selection; // 50839
	U32* freqs; // 50840
	{
		const size_t  tail; // 50848
		const unsigned int  nbFinalizeSamples; // 50851
		COVER_dictSelectionIsError(COVER_dictSelection_t selection); // 50856
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 50857
	}
	setDictSelection(BYTE* buf,
			size_t s,
			size_t csz);  // 48086
	COVER_dictSelectionError(size_t error); // 50839
	memcpy(void* __dest,
		const void* __src,
		size_t __len);  // 50846
	COVER_dictSelectionFree(COVER_dictSelection_t selection); // 50866
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50842
} /* size: 800, variables: 9, goto labels: 1, inline expansions: 5 (96 bytes) */

// <06F465C0> thirdparty/zstd/zstd.c:50872
void FASTCOVER_convertToCoverParams(ZDICT_fastCover_params_t fastCoverParams, ZDICT_cover_params_t* coverParams)
{
} /* size: 0 */

// <06F4657F> thirdparty/zstd/zstd.c:50886
void FASTCOVER_convertToFastCoverParams(ZDICT_cover_params_t coverParams, ZDICT_fastCover_params_t* fastCoverParams, unsigned int f, unsigned int accel)
{
} /* size: 0 */

// <06F45E5F> thirdparty/zstd/zstd.c:50903
// variables: 9
// function calls: 13
size_t ZDICT_trainFromBuffer_fastCover(void* dictBuffer, size_t dictBufferCapacity, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, ZDICT_fastCover_params_t parameters)
{
	const BYTE* dict; // 50908
	FASTCOVER_ctx_t ctx; // 50909
	ZDICT_cover_params_t coverParams; // 50910
	FASTCOVER_accel_t accelParams; // 50911
	{
		const size_t  initVal; // 50940
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 50944
	}
	{
		U16* segmentFreqs; // 50953
		const size_t  tail; // 50954
		const unsigned int  nbFinalizeSamples; // 50956
		const size_t  dictionarySize; // 50957
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 50961
		FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 50622
		FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 50964
	}
	memset(void* __dest,
		int __ch,
		size_t __len);  // 50919
	FASTCOVER_convertToCoverParams(ZDICT_fastCover_params_t fastCoverParams,
					ZDICT_cover_params_t* coverParams);  // 50920
	FASTCOVER_checkParameters(ZDICT_cover_params_t parameters,
					size_t maxDictSize,
					unsigned int f,
					unsigned int accel);  // 50922
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50924
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50932
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 47774
	COVER_warnOnSmallCorpus(size_t maxDictSize,
				size_t nbDmers,
				int displayLevel);  // 50948
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50928
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 50950
} /* size: 1206, variables: 4, inline expansions: 9 (454 bytes) */

// <06F44DC7> thirdparty/zstd/zstd.c:50972
// variables: 26
// function calls: 39
size_t ZDICT_optimizeTrainFromBuffer_fastCover(void* dictBuffer, size_t dictBufferCapacity, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, ZDICT_fastCover_params_t* parameters)
{
	ZDICT_cover_params_t coverParams; // 50978
	FASTCOVER_accel_t accelParams; // 50979
	const unsigned int  nbThreads; // 50981
	const double  splitPoint; // 50982
	const unsigned int  kMinD; // 50984
	const unsigned int  kMaxD; // 50985
	const unsigned int  kMinK; // 50986
	const unsigned int  kMaxK; // 50987
	const unsigned int  kSteps; // 50988
	const unsigned int  kStepSize; // 50989
	const unsigned int  kIterations; // 50990
	const unsigned int  f; // 50992
	const unsigned int  accel; // 50993
	const unsigned int  shrinkDict; // 50994
	const int  displayLevel; // 50996
	unsigned int iteration; // 50997
	unsigned int d; // 50998
	unsigned int k; // 50999
	COVER_best_t best; // 51000
	POOL_ctx* pool; // 51001
	int warned; // 51002
	{
		FASTCOVER_ctx_t ctx; // 51043
		{
			const size_t  initVal; // 51046
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 51048
			COVER_best_destroy(COVER_best_t* best); // 51049
			POOL_free(POOL_ctx* ctx); // 51050
		}
		{
			FASTCOVER_tryParameters_data_t* data; // 51061
			FASTCOVER_checkParameters(ZDICT_cover_params_t parameters,
							size_t maxDictSize,
							unsigned int f,
							unsigned int accel);  // 51082
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 51063
			COVER_best_start(COVER_best_t* best); // 48016
			COVER_best_start(COVER_best_t* best); // 51089
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 51065
			COVER_best_destroy(COVER_best_t* best); // 51066
			FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 50622
			FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 51067
			POOL_free(POOL_ctx* ctx); // 51068
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 51084
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 51096
		}
		COVER_best_wait(COVER_best_t* best); // 47986
		COVER_best_wait(COVER_best_t* best); // 51100
		FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 50622
		FASTCOVER_ctx_destroy(FASTCOVER_ctx_t* ctx); // 51101
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51044
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 47774
		COVER_warnOnSmallCorpus(size_t maxDictSize,
					size_t nbDmers,
					int displayLevel);  // 51055
	}
	{
		const size_t  dictSize; // 51106
		{
			const size_t  compressedSize; // 51108
			COVER_best_destroy(COVER_best_t* best); // 51109
		}
		FASTCOVER_convertToFastCoverParams(ZDICT_cover_params_t coverParams,
							ZDICT_fastCover_params_t* fastCoverParams,
							unsigned int f,
							unsigned int accel);  // 51113
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 51114
		COVER_best_destroy(COVER_best_t* best); // 51115
		POOL_free(POOL_ctx* ctx); // 51116
	}
	POOL_create(size_t numThreads,
			size_t queueSize);  // 51026
	memset(void* __dest,
		int __ch,
		size_t __len);  // 47980
	COVER_best_init(COVER_best_t* best); // 47972
	COVER_best_init(COVER_best_t* best); // 51032
	memset(void* __dest,
		int __ch,
		size_t __len);  // 51033
	FASTCOVER_convertToCoverParams(ZDICT_fastCover_params_t fastCoverParams,
					ZDICT_cover_params_t* coverParams);  // 51034
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51005
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51009
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51013
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51039
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51103
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51017
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51021
} /* size: 3224, variables: 21, inline expansions: 13 (534 bytes) */

// <06F44DA7> thirdparty/zstd/zstd.c:51203
clock_t ZDICT_clockSpan(clock_t nPrevious)
{
} /* size: 0 */

// <06F44D5B> thirdparty/zstd/zstd.c:51205
// variables: 3
void ZDICT_printHex(const void* ptr, size_t length)
{
	const BYTE* b; // 51207
	size_t u; // 51208
	{
		BYTE c; // 51210
	}
} /* size: 0, variables: 2 */

// <06F44D3B> thirdparty/zstd/zstd.c:51220
unsigned int ZDICT_isError(size_t errorCode)
{
} /* size: 0 */

// <06F44D1B> thirdparty/zstd/zstd.c:51222
const char* ZDICT_getErrorName(size_t errorCode)
{
} /* size: 0 */

// <06F44CEE> thirdparty/zstd/zstd.c:51224
unsigned int ZDICT_getDictID(const void* dictBuffer, size_t dictSize)
{
} /* size: 0 */

// <06F44B44> thirdparty/zstd/zstd.c:51231
// variables: 3
// function calls: 3
size_t ZDICT_getDictHeaderSize(const void* dictBuffer, size_t dictSize)
{
	size_t headerSize; // 51233
	{
		ZSTD_compressedBlockState_t* bs; // 51236
		U32* wksp; // 51237
		ZSTD_reset_compressedBlockState(ZSTD_compressedBlockState_t* bs); // 51241
	}
	MEM_read32(const void* ptr); // 1301
	MEM_readLE32(const void* memPtr); // 51234
} /* size: 187, variables: 1, inline expansions: 2 (0 bytes) */

// <06F44AFB> thirdparty/zstd/zstd.c:51259
// variables: 2
size_t ZDICT_count(const void* pIn, const void* pMatch)
{
	const char* pStart; // 51261
	{
		const size_t  diff; // 51263
	}
} /* size: 0, variables: 1 */

// <06F3F2E7> thirdparty/zstd/zstd.c:51275
// member variables: 3
// struct size: 12
typedef struct {
	U32 pos; /* 0 4 */
	U32 length; /* 4 4 */
	U32 savings; /* 8 4 */
} dictItem;

// <06F44AE1> thirdparty/zstd/zstd.c:51281
void ZDICT_initDictItem(dictItem* d)
{
} /* size: 0 */

// <06F43E35> thirdparty/zstd/zstd.c:51291
// variables: 38
// function calls: 38
dictItem ZDICT_analyzePos(BYTE* doneMarks, const int* suffix, U32 start, const void* buffer, U32 minRatio, U32 notificationLevel)
{
	U32 lengthList; // 51296
	U32 cumulLength; // 51297
	U32 savings; // 51298
	const BYTE* b; // 51299
	size_t maxLength; // 51300
	size_t pos; // 51301
	U32 end; // 51302
	dictItem solution; // 51303
	{
		const U16  pattern16; // 51314
		U32 u; // 51315
		U32 patternEnd; // 51315
		MEM_read16(const void* ptr); // 51316
	}
	{
		size_t length; // 51324
		{
			const size_t  diff; // 51263
			MEM_readST(const void* ptr); // 51263
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 51269
			MEM_readST(const void* ptr); // 51263
		}
		ZDICT_count(const void* pIn,
				const void* pMatch);  // 51327
	}
	{
		size_t length; // 51332
		{
			const size_t  diff; // 51263
			MEM_readST(const void* ptr); // 51263
			MEM_readST(const void* ptr); // 51263
			ZSTD_countTrailingZeros64(U64 val); // 2090
			ZSTD_NbCommonBytes(size_t val); // 51269
		}
		ZDICT_count(const void* pIn,
				const void* pMatch);  // 51334
	}
	{
		U32 idx; // 51341
	}
	{
		int i; // 51347
		U32 mml; // 51348
		U32 refinedStart; // 51349
		U32 refinedEnd; // 51350
		{
			BYTE currentChar; // 51357
			U32 currentCount; // 51358
			U32 currentID; // 51359
			U32 id; // 51360
			U32 selectedCount; // 51361
			U32 selectedID; // 51362
		}
		{
			size_t length; // 51393
			{
				const size_t  diff; // 51263
				MEM_readST(const void* ptr); // 51263
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 51269
				MEM_readST(const void* ptr); // 51263
			}
			ZDICT_count(const void* pIn,
					const void* pMatch);  // 51396
		}
		{
			size_t length; // 51403
			{
				const size_t  diff; // 51263
				MEM_readST(const void* ptr); // 51263
				MEM_readST(const void* ptr); // 51263
				ZSTD_countTrailingZeros64(U64 val); // 2090
				ZSTD_NbCommonBytes(size_t val); // 51269
			}
			ZDICT_count(const void* pIn,
					const void* pMatch);  // 51405
		}
		{
			U32 l; // 51422
			const BYTE  c; // 51423
		}
		{
			U32 id; // 51442
			{
				U32 p; // 51444
				U32 pEnd; // 51444
				U32 length; // 51444
				const U32  testedPos; // 51445
				{
					const size_t  diff; // 51263
					ZSTD_countTrailingZeros64(U64 val); // 2090
					ZSTD_NbCommonBytes(size_t val); // 51269
					MEM_readST(const void* ptr); // 51263
					MEM_readST(const void* ptr); // 51263
				}
				ZDICT_count(const void* pIn,
						const void* pMatch);  // 51449
			}
		}
		memset(void* __dest,
			int __ch,
			size_t __len);  // 51390
		memset(void* __dest,
			int __ch,
			size_t __len);  // 51413
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51352
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51353
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51354
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51434
	}
	memset(void* __dest,
		int __ch,
		size_t __len);  // 51306
	MEM_read16(const void* ptr); // 51310
	MEM_read16(const void* ptr); // 51310
	MEM_read16(const void* ptr); // 51312
	MEM_read16(const void* ptr); // 51311
	MEM_read16(const void* ptr); // 51311
} /* size: 2117, variables: 8, inline expansions: 6 (33 bytes) */

// <06F43DD8> thirdparty/zstd/zstd.c:51461
// variables: 3
int isIncluded(const void* in, const void* container, size_t length)
{
	const char* ip; // 51463
	const char* into; // 51464
	size_t u; // 51465
} /* size: 0, variables: 3 */

// <06F43BFA> thirdparty/zstd/zstd.c:51478
// variables: 7
// function calls: 3
U32 ZDICT_tryMerge(dictItem* table, dictItem elt, U32 eltNbToSkip, const void* buffer)
{
	const U32  tableSize; // 51480
	const U32  eltEnd; // 51481
	const char* buf; // 51482
	U32 u; // 51485
	{
		const U32  addedLength; // 51489
	}
	{
		const int  addedLength; // 51508
	}
	{
		const size_t  addedLength; // 51524
	}
	MEM_read64(const void* ptr); // 51522
	MEM_read64(const void* ptr); // 51522
	isIncluded(const void* in,
			const void* container,
			size_t length);  // 51523
} /* size: 823, variables: 4, inline expansions: 3 (69 bytes) */

// <06F43BBA> thirdparty/zstd/zstd.c:51537
// variables: 2
void ZDICT_removeDictItem(dictItem* table, U32 id)
{
	const U32  max; // 51540
	U32 u; // 51541
} /* size: 0, variables: 2 */

// <06F43B3B> thirdparty/zstd/zstd.c:51549
// variables: 4
void ZDICT_insertDictItem(dictItem* table, U32 maxSize, dictItem elt, const void* buffer)
{
	U32 mergeId; // 51552
	{
		U32 newMerge; // 51554
	}
	{
		U32 current; // 51564
		U32 nextElt; // 51565
	}
} /* size: 0, variables: 1 */

// <06F43AFD> thirdparty/zstd/zstd.c:51578
// variables: 2
U32 ZDICT_dictSize(const dictItem* dictList)
{
	U32 u; // 51580
	U32 dictSize; // 51580
} /* size: 0, variables: 2 */

// <06F439C2> thirdparty/zstd/zstd.c:51587
// variables: 12
size_t ZDICT_trainBuffer_legacy(dictItem* dictList, U32 dictListSize, const void* buffer, size_t bufferSize, const size_t* fileSizes, unsigned int nbFiles, unsigned int minRatio, U32 notificationLevel)
{

_cleanup: // 51657
	const int* suffix0; // 51592
	const int* suffix; // 51593
	U32* reverseSuffix; // 51594
	BYTE* doneMarks; // 51595
	U32* filePos; // 51596
	size_t result; // 51597
	clock_t displayClock; // 51598
	const clock_t  refreshRate; // 51599
	{
		const int  divSuftSortResult; // 51628
	}
	{
		size_t pos; // 51634
	}
	{
		U32 cursor; // 51647
		{
			dictItem solution; // 51648
		}
	}
} /* size: 0, variables: 8, goto labels: 1 */

// <06F43967> thirdparty/zstd/zstd.c:51666
// variables: 4
void ZDICT_fillNoise(void* buffer, size_t length)
{
	const unsigned int  prime1; // 51668
	const unsigned int  prime2; // 51669
	unsigned int acc; // 51670
	size_t p; // 51671
} /* size: 0, variables: 4 */

// <06F3F338> thirdparty/zstd/zstd.c:51680
// member variables: 3
// struct size: 24
typedef struct {
	ZSTD_CDict * dict; /* 0 8 */
	ZSTD_CCtx * zc; /* 8 8 */
	void * workPlace; /* 16 8 */
} EStats_ress_t;

// <06F437D9> thirdparty/zstd/zstd.c:51688
// variables: 15
void ZDICT_countEStats(EStats_ress_t esr, const ZSTD_parameters* params, unsigned int* countLit, unsigned int* offsetcodeCount, unsigned int* matchlengthCount, unsigned int* litlengthCount, U32* repOffsets, const void* src, size_t srcSize, U32 notificationLevel)
{
	const size_t  blockSizeMax; // 51693
	size_t cSize; // 51694
	{
		const size_t  errorCode; // 51697
	}
	{
		const SeqStore_t* seqStorePtr; // 51705
		{
			const BYTE* bytePtr; // 51708
		}
		{
			const U32  nbSeq; // 51714
			{
				const BYTE* codePtr; // 51717
				U32 u; // 51718
			}
			{
				const BYTE* codePtr; // 51722
				U32 u; // 51723
			}
			{
				const BYTE* codePtr; // 51727
				U32 u; // 51728
			}
			{
				const SeqDef* seq; // 51733
				U32 offset1; // 51734
				U32 offset2; // 51735
			}
		}
	}
} /* size: 0, variables: 2 */

// <06F43794> thirdparty/zstd/zstd.c:51743
// variables: 2
size_t ZDICT_totalSampleSize(const size_t* fileSizes, unsigned int nbFiles)
{
	size_t total; // 51745
	unsigned int u; // 51746
} /* size: 0, variables: 2 */

// <06F3F383> thirdparty/zstd/zstd.c:51751
// member variables: 2
// struct size: 8
typedef struct {
	U32 offset; /* 0 4 */
	U32 count; /* 4 4 */
} offsetCount_t;

// <06F4373E> thirdparty/zstd/zstd.c:51753
// variables: 2
void ZDICT_insertSortCount(offsetCount_t* table, U32 val, U32 count)
{
	U32 u; // 51755
	{
		offsetCount_t tmp; // 51759
	}
} /* size: 0, variables: 1 */

// <06F43717> thirdparty/zstd/zstd.c:51771
// variable: 1
void ZDICT_flatLit(unsigned int* countLit)
{
	int u; // 51773
} /* size: 0, variables: 1 */

// <06F422FA> thirdparty/zstd/zstd.c:51781
// variables: 47
// function calls: 32
size_t ZDICT_analyzeEntropy(void* dstBuffer, size_t maxDstSize, int compressionLevel, const void* srcBuffer, const size_t* fileSizes, unsigned int nbFiles, const void* dictBuffer, size_t dictBufferSize, unsigned int notificationLevel)
{

_cleanup: // 51959
	unsigned int countLit; // 51787
	HUF_CElt hufTable; // 51788
	unsigned int offcodeCount; // 51789
	short int offcodeNCount; // 51790
	U32 offcodeMax; // 51791
	unsigned int matchLengthCount; // 51792
	short int matchLengthNCount; // 51793
	unsigned int litLengthCount; // 51794
	short int litLengthNCount; // 51795
	U32 repOffset; // 51796
	offsetCount_t bestRepOffset; // 51797
	EStats_ress_t esr; // 51798
	ZSTD_parameters params; // 51799
	U32 u; // 51800
	U32 huffLog; // 51800
	U32 Offlog; // 51800
	U32 mlLog; // 51800
	U32 llLog; // 51800
	U32 total; // 51800
	size_t pos; // 51801
	size_t errorCode; // 51801
	size_t eSize; // 51802
	const size_t  totalSrcSize; // 51803
	const size_t  averageSampleSize; // 51804
	BYTE* dstPtr; // 51805
	U32 wksp; // 51806
	{
		size_t maxNbBits; // 51847
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51854
		ZDICT_flatLit(unsigned int* countLit); // 51855
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51850
	}
	{
		U32 offset; // 51863
		{
			offsetCount_t tmp; // 51759
		}
		ZDICT_insertSortCount(offsetCount_t* table,
					U32 val,
					U32 count);  // 51865
	}
	{
		const size_t  hhSize; // 51897
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51900
	}
	{
		const size_t  ohSize; // 51908
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51911
	}
	{
		const size_t  mhSize; // 51919
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51922
	}
	{
		const size_t  lhSize; // 51930
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51933
	}
	ZSTD_countLeadingZeros32(U32 val); // 2106
	ZSTD_highbit32(U32 val); // 51791
	ZDICT_totalSampleSize(const size_t* fileSizes,
				unsigned int nbFiles);  // 51803
	memset(void* __dest,
		int __ch,
		size_t __len);  // 51815
	memset(void* __dest,
		int __ch,
		size_t __len);  // 51817
	{
		const size_t  errorCode; // 51697
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 51698
		ZSTD_compressBegin_usingCDict_deprecated(ZSTD_CCtx* cctx,
							const ZSTD_CDict* cdict);  // 51697
	}
	{
		const size_t  blockSizeMax; // 27840
		ZSTD_getBlockSize_deprecated(const ZSTD_CCtx* cctx); // 27840
	}
	ZSTD_compressBlock_deprecated(ZSTD_CCtx* cctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize);  // 27837
	ZSTD_compressBlock_deprecated(ZSTD_CCtx* cctx,
					void* dst,
					size_t dstCapacity,
					const void* src,
					size_t srcSize);  // 51701
	{
		const SeqStore_t* seqStorePtr; // 51705
		ZSTD_getSeqStore(const ZSTD_CCtx* ctx); // 51705
		{
			const BYTE* bytePtr; // 51708
		}
		{
			const U32  nbSeq; // 51714
			{
				const BYTE* codePtr; // 51717
				U32 u; // 51718
			}
			{
				const BYTE* codePtr; // 51722
				U32 u; // 51723
			}
			{
				const BYTE* codePtr; // 51727
				U32 u; // 51728
			}
			{
				const SeqDef* seq; // 51733
				U32 offset1; // 51734
				U32 offset2; // 51735
			}
		}
	}
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51702
	ZDICT_countEStats(EStats_ress_t esr,
				const ZSTD_parameters* params,
				unsigned int* countLit,
				unsigned int* offsetcodeCount,
				unsigned int* matchlengthCount,
				unsigned int* litlengthCount,
				U32* repOffsets,
				const void* src,
				size_t srcSize,
				U32 notificationLevel);  // 51832
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51841
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51843
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51826
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51873
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51891
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51882
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 51953
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 51955
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51943
} /* size: 3839, variables: 26, goto labels: 1, inline expansions: 20 (1281 bytes) */

// <06F422C2> thirdparty/zstd/zstd.c:51971
// variables: 2
U32 ZDICT_maxRep(const U32* reps)
{
	U32 maxRep; // 51973
	int r; // 51974
} /* size: 0, variables: 2 */

// <06F41815> thirdparty/zstd/zstd.c:51980
// variables: 20
// function calls: 35
size_t ZDICT_finalizeDictionary(void* dictBuffer, size_t dictBufferCapacity, const void* customDictContent, size_t dictContentSize, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, ZDICT_params_t params)
{
	size_t hSize; // 51985
	BYTE header; // 51987
	const int  compressionLevel; // 51988
	const U32  notificationLevel; // 51989
	const size_t  minContentSize; // 51991
	size_t paddingSize; // 51992
	{
		const U64  randomID; // 52001
		const U32  compliantID; // 52002
		const U32  dictID; // 52003
		{
			const xxh_u8* bEnd; // 11768
			const xxh_u8* limit; // 11769
			xxh_u64 v1; // 11770
			xxh_u64 v2; // 11771
			xxh_u64 v3; // 11772
			xxh_u64 v4; // 11773
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11776
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11624
			XXH_readLE64_align(const void* ptr,
						XXH_alignment align);  // 11776
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11777
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11778
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11779
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11624
			XXH_readLE64_align(const void* ptr,
						XXH_alignment align);  // 11777
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11624
			XXH_readLE64_align(const void* ptr,
						XXH_alignment align);  // 11778
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11624
			XXH_readLE64_align(const void* ptr,
						XXH_alignment align);  // 11779
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11783
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11784
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11785
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11786
		}
		XXH64_endian_align(const xxh_u8* input,
					size_t len,
					xxh_u64 seed,
					XXH_alignment align);  // 11813
		XXH_INLINE_XXH64(const void* input,
				size_t len,
				XXH64_hash_t seed);  // 52001
		MEM_write32(void* memPtr,
				U32 value);  // 1309
		MEM_writeLE32(void* memPtr,
				U32 val32);  // 52004
	}
	{
		const size_t  eSize; // 52011
	}
	{
		const size_t  dictSize; // 52035
		const BYTE* outDictHeader; // 52041
		const BYTE* outDictPadding; // 52042
		const BYTE* outDictContent; // 52043
		memmove(void* __dest,
			const void* __src,
			size_t __len);  // 52053
		memcpy(void* __dest,
			const void* __src,
			size_t __len);  // 52054
		memset(void* __dest,
			int __ch,
			size_t __len);  // 52055
	}
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 52000
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 52009
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 52010
} /* size: 1146, variables: 6, inline expansions: 4 (100 bytes) */

// <06F40EF5> thirdparty/zstd/zstd.c:52062
// variables: 13
// function calls: 33
size_t ZDICT_addEntropyTablesFromBuffer_advanced(void* dictBuffer, size_t dictContentSize, size_t dictBufferCapacity, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, ZDICT_params_t params)
{
	const int  compressionLevel; // 52067
	const U32  notificationLevel; // 52068
	size_t hSize; // 52069
	{
		const size_t  eSize; // 52074
	}
	{
		const U64  randomID; // 52085
		const U32  compliantID; // 52086
		const U32  dictID; // 52087
		{
			const xxh_u8* bEnd; // 11768
			const xxh_u8* limit; // 11769
			xxh_u64 v1; // 11770
			xxh_u64 v2; // 11771
			xxh_u64 v3; // 11772
			xxh_u64 v4; // 11773
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11776
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11624
			XXH_readLE64_align(const void* ptr,
						XXH_alignment align);  // 11776
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11777
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11778
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11779
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11624
			XXH_readLE64_align(const void* ptr,
						XXH_alignment align);  // 11777
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11624
			XXH_readLE64_align(const void* ptr,
						XXH_alignment align);  // 11778
			XXH_read64(const void* ptr); // 11611
			XXH_readLE64(const void* ptr); // 11624
			XXH_readLE64_align(const void* ptr,
						XXH_alignment align);  // 11779
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11783
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11784
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11785
			XXH64_round(xxh_u64 acc,
					xxh_u64 input);  // 11682
			XXH64_mergeRound(xxh_u64 acc,
					xxh_u64 val);  // 11786
		}
		XXH64_endian_align(const xxh_u8* input,
					size_t len,
					xxh_u64 seed,
					XXH_alignment align);  // 11813
		XXH_INLINE_XXH64(const void* input,
				size_t len,
				XXH64_hash_t seed);  // 52085
		MEM_write32(void* memPtr,
				U32 value);  // 1309
		MEM_writeLE32(void* memPtr,
				U32 val32);  // 52088
	}
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 52072
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 52073
	MEM_write32(void* memPtr,
			U32 value);  // 1309
	MEM_writeLE32(void* memPtr,
			U32 val32);  // 52084
	memmove(void* __dest,
		const void* __src,
		size_t __len);  // 52092
} /* size: 801, variables: 3, inline expansions: 5 (105 bytes) */

// <06F40D5A> thirdparty/zstd/zstd.c:52100
// variables: 22
size_t ZDICT_trainFromBuffer_unsafe_legacy(void* dictBuffer, size_t maxDictSize, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, ZDICT_legacy_params_t params)
{
	const U32  dictListSize; // 52105
	const dictItem* dictList; // 52106
	const unsigned int  selectivity; // 52107
	const unsigned int  minRep; // 52108
	const size_t  targetDictSize; // 52109
	const size_t  samplesBuffSize; // 52110
	size_t dictSize; // 52111
	const U32  notificationLevel; // 52112
	{
		const unsigned int  nb; // 52130
		const unsigned int  dictContentSize; // 52131
		unsigned int u; // 52132
		{
			const unsigned int  pos; // 52136
			const unsigned int  length; // 52137
			const U32  printedLength; // 52138
		}
	}
	{
		unsigned int dictContentSize; // 52151
		{
			unsigned int proposedSelectivity; // 52164
		}
		{
			const U32  max; // 52172
			U32 currentSize; // 52173
			U32 n; // 52174
		}
		{
			U32 u; // 52183
			BYTE* ptr; // 52184
			{
				U32 l; // 52186
			}
		}
	}
} /* size: 0, variables: 8 */

// <06F3F88A> thirdparty/zstd/zstd.c:52206
// variables: 25
// function calls: 33
size_t ZDICT_trainFromBuffer_legacy(void* dictBuffer, size_t dictBufferCapacity, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples, ZDICT_legacy_params_t params)
{
	size_t result; // 52210
	void* newBuff; // 52211
	const size_t  sBuffSize; // 52212
	ZDICT_totalSampleSize(const size_t* fileSizes,
				unsigned int nbFiles);  // 52212
	memcpy(void* __dest,
		const void* __src,
		size_t __len);  // 52218
	ZDICT_fillNoise(void* buffer,
			size_t length);  // 52219
	ZDICT_totalSampleSize(const size_t* fileSizes,
				unsigned int nbFiles);  // 52110

_cleanup: // 51657
	memset(void* __dest,
		int __ch,
		size_t __len);  // 51620
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51627
	{
		const int  divSuftSortResult; // 51628
	}
	{
		size_t pos; // 51634
	}
	{
		U32 cursor; // 51647
		{
			dictItem solution; // 51648
			{
				U32 newMerge; // 51554
				ZDICT_removeDictItem(dictItem* table,
							U32 id);  // 51557
			}
			{
				U32 current; // 51564
				U32 nextElt; // 51565
			}
			ZDICT_insertDictItem(dictItem* table,
						U32 maxSize,
						dictItem elt,
						const void* buffer);  // 51652
			ZDICT_clockSpan(clock_t nPrevious); // 51654
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 51654
		}
	}
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51614
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51644
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51645
	fprintf(FILE* __stream,
		const char* __fmt, ...);  // 51623
	ZDICT_trainBuffer_legacy(dictItem* dictList,
				U32 dictListSize,
				const void* buffer,
				size_t bufferSize,
				const size_t* fileSizes,
				unsigned int nbFiles,
				unsigned int minRatio,
				U32 notificationLevel);  // 52123
	ZDICT_initDictItem(dictItem* d); // 52120
	{
		const unsigned int  nb; // 52130
		const unsigned int  dictContentSize; // 52131
		unsigned int u; // 52132
		ZDICT_dictSize(const dictItem* dictList); // 52131
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 52133
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 52134
		{
			const unsigned int  pos; // 52136
			const unsigned int  length; // 52137
			const U32  printedLength; // 52138
			{
				BYTE c; // 51210
				fprintf(FILE* __stream,
					const char* __fmt, ...);  // 51212
			}
			ZDICT_printHex(const void* ptr,
					size_t length);  // 52145
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 52146
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 52143
		}
	}
	{
		unsigned int dictContentSize; // 52151
		ZDICT_dictSize(const dictItem* dictList); // 52151
		{
			unsigned int proposedSelectivity; // 52164
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 52166
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 52167
			fprintf(FILE* __stream,
				const char* __fmt, ...);  // 52168
		}
		{
			const U32  max; // 52172
			U32 currentSize; // 52173
			U32 n; // 52174
		}
		{
			U32 u; // 52183
			BYTE* ptr; // 52184
			{
				U32 l; // 52186
				memcpy(void* __dest,
					const void* __src,
					size_t __len);  // 52189
			}
		}
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 52154
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 52158
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 52159
		fprintf(FILE* __stream,
			const char* __fmt, ...);  // 52156
	}
	ZDICT_trainFromBuffer_unsafe_legacy(void* dictBuffer,
						size_t maxDictSize,
						const void* samplesBuffer,
						const size_t* samplesSizes,
						unsigned int nbSamples,
						ZDICT_legacy_params_t params);  // 52222
} /* size: 3865, variables: 3, goto labels: 1, inline expansions: 13 (5910 bytes) */

// <06F3F75C> thirdparty/zstd/zstd.c:52229
// variable: 1
// function call: 1
size_t ZDICT_trainFromBuffer(void* dictBuffer, size_t dictBufferCapacity, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples)
{
	ZDICT_fastCover_params_t params; // 52232
	memset(void* __dest,
		int __ch,
		size_t __len);  // 52234
} /* size: 102, variables: 1, inline expansions: 1 (15 bytes) */

// <06F3F60E> thirdparty/zstd/zstd.c:52247
// variable: 1
// function call: 1
size_t ZDICT_addEntropyTablesFromBuffer(void* dictBuffer, size_t dictContentSize, size_t dictBufferCapacity, const void* samplesBuffer, const size_t* samplesSizes, unsigned int nbSamples)
{
	ZDICT_params_t params; // 52250
	memset(void* __dest,
		int __ch,
		size_t __len);  // 52251
} /* size: 85, variables: 1, inline expansions: 1 (15 bytes) */

